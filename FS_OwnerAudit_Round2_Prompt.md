OWNER AUDIT ROUND 2, PRESENTATION FLOW AND FEATURE LAYOUT, 2026-07-24b. Conventions, locks and reporting as pinned; merge PR #86 first; fresh branch; proofs to reports/screens/owner-audit-v2/. (1) FEATURE PRESENTATION FLOW, unified: entry scatter award presents as its own card with an explicit CLICK TO CONTINUE gate before the first free spin (autoplay/turbo do not bypass it); during the feature NO win banners fire above the small tier, wins accumulate into a single combined TOTAL WIN field that derives ONLY from per-spin events already played, never the round's book total (this is the spoiler bug: add a hard assert that after spin k the displayed total equals the sum of spins 1..k and the final total is never displayed early); the feature ends with one end-of-feature celebration presenting the total. (2) WIN BANNER V3, replacing the shaking rectangle: a full-width neon band spanning the stage edge to edge, layered glow borders, tier word and amount in front with the count-up, reels visible above and below (no box, no reel blockage), shake retained big-and-above, used for base-game big wins and the feature-end celebration alike; commit stress proofs at $1,000 / $100,000 / $1,000,000 in both orientations. (3) FEATURE HUD RELAYOUT, both layouts, reels at maximum size: remove the top-right overlay panel from the feature entirely. Portrait: below the grid, two fields only: OVERDRIVE FREE SPINS count and TOTAL WIN; retrigger moments pop a "+5 FREE SPINS" element layered at the reel edge, outside the grid, small but alive (scale-in with glow, roughly 900ms). Landscape/desktop: right-side instrument column ordered tachometer, FREE SPINS, TOTAL WIN, existing fields pushed down; the tachometer loses the spins text inside it and becomes the animated neon centrepiece (needle sweep on meter change, colour-graded glow, reduced-motion static fallback). Resize the MAX WIN 5000X element responsively for portrait. (4) FEATURE COLOURWAYS AND FLAME CHOREOGRAPHY, Fable's ruling, contrast law throughout (flame hue at least 90 degrees from backdrop hue, never green-on-green): natural entry keeps the green flames and original outward flicker on the standard backdrop shift; Overdrive buy shifts the backdrop magenta with CYAN flames in a slow synchronised top-to-bottom pulse; NITRO intensifies the pink/magenta backdrop with MAGENTA-core white-tipped flames in a distinctive left-to-right sweeping wave; reduced-motion static glows per colourway; proofs of all three entries. (5) ANTELITE TRIGGER FUEL: curate natural trigger rounds into the antelite pool at its designed elevated rate, and extend the pool assert to require natural triggers in EVERY standing-mode pool so this gap class is closed permanently. (6) FEATURES MENU ITERATION 3: merge the spin-mode and spin-cost header lines (cost text moves into the selector line before the bet amount); Normal and Cruise become a paired switch; condense so the buys sit higher and more prominent with less scrolling; apply to both phone and desktop menus; keep the radio-clarity and live-cost asserts green. (7) SMALL FIXES: open up the bet stepper row spacing on portrait (full-width line, generous gaps, targets stay 44px+); clean up the pulsing square shadow artefact behind the FEATURES button in landscape (likely a glow container's box leaking); regenerate every interface-guide icon in the paytable from the CURRENT live components so the guide matches the shipped UI, with a proof grid committed. (8) Re-run the full conformance suite plus all owner-audit asserts; all gates green; session report with per-item root cause and assert table.
