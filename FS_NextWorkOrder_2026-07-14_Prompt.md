NEXT WORK ORDER, 2026-07-14. Conventions, locks and per-job reporting as pinned. ITEM 0, AUDIO SEAM AND WARM-UP FIX: extend tools/audio_forge/master.py with a loop-conditioning step: equal-power crossfade the final 500ms of each loop row into its head material and trim at the blend so end-vs-start RMS over 20ms windows lands within 2 dB; re-master and re-encode the three loop rows (bgm_loop, bgm_tension, anticipation_build) in both formats; add the seam metric to audio_verify.mjs as a hard gate; add a first-gesture warm-up that primes audio elements (and any first-spin textures) on the initial user interaction, then re-run the frame-gate attribution to confirm the spin-click cluster shrinks; delete the unreferenced legacy sounds/scatter.mp3 and sounds/win.mp3; move SUBMISSION_BLURB.md to reports/archive/SUBMISSION_BLURB_superseded.md with a SUPERSEDED header pointing at SUBMISSION_DOSSIER.md section 3; note the measured -21.8 LUFS beds as accepted-as-shipped in the session report, correcting the earlier -18 claim. ITEM 1, JOB 5b IN-GAME RULES CONFORMANCE, exactly as previously specified: per-mode cost, RTP and max win displayed to the player, feature access described, special-symbol values listed, UI button guide added, screenshots of every rules page committed as proofs. ITEM 2, JOB 2 ADDENDUM EXTENSIONS a-g, exactly as previously specified: same-origin sweep, spacebar bet assert, mini-player popout proofs, bet-level conformance including a high-minimum currency, language fuzz, incremental count-up assert, fastplay legibility. ITEM 3, JOB 3b MATH SELF-AUDIT: reports/qa/math_selfaudit_2026-07-14.md walking every criterion on the approval math page with numbers from the shipped tables, flagging anything marginal. ITEM 4, JOB 9b SOCIAL-MODE STRING AUDIT: flag-only report of strings needing stake.us variants; Fable rules on wording before any change. Execute in order; ITEM 0 completes before the owner's mix play-test is requested.

SANCTIONED LOCKED PASS, BOOKS REGENERATION AND CLEANUP, owner authority relayed by Fable. Scope: the gitignored books artefacts under games/future_spinner/library/publish_files/ only. Lift, as a temporary working-tree edit of .claude/settings.json, only the deny lines covering games/future_spinner/ writes; restore with a verified empty diff before any commit; writing via Bash while lifted is still forbidden for any file OTHER than the books artefacts named here. Steps: enumerate exactly which books files index.json requires; regenerate every required books file deterministically from the shipped configuration; delete the orphaned books_*.jsonl.zst files that index.json does not reference. Hard gates, all mandatory: every lookUpTable_*.csv and index.json byte-identical after the pass (empty git status on tracked locked files); the statelessness scan re-run against the fresh books reproduces meter 1 for cruise and antelite and meter 5 for super across 100,000 rounds each, results committed as a dated addendum in reports/qa/; per-mode RTP recomputed via scripts/validate_math.py unchanged at 96.3500 percent; SHA-256 hashes of the final books set recorded in SUBMISSION_DOSSIER.md section 5 as the upload inventory. Session report documents the deny lines lifted and restored with the empty-diff proof.
