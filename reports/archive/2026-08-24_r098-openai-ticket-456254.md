# Session Report - R098 OPENAI TICKET 456254: the provider ruling is on file, and the machine gate still reads BARRED (2026-08-24)

Brief saved verbatim: `reports/briefs/FS_FABLE_R098_OPENAI_TICKET_456254_Prompt.md`. Branch:
`claude/r098-openai-ruling`, review lane, records only, held for Fable and the owner. **THE FENCE
HELD: zero rasters staged or committed. No code change of any kind. No kit. `output/` read only.
The 27 working-tree placeholders are untouched**, verified by sha256 fingerprint before and after.

## Preconditions: all met

| Precondition | State |
|---|---|
| On `main`, up to date | Yes. R097 merged as `c00b9dd5`; checked out `main` and pulled |
| Working-tree placeholders | 27 rasters plus the R091 provenance record, left exactly as found |

## TASK: the ruling is recorded

**New file: `docs/legal/openai-ticket-456254-ruling.md`.** `docs/legal/` did not previously exist
and was created for it, as the brief suggested.

It records, in full: the date received (24 August 2026), the ticket reference (456254), the
recipient (Joshua / Synergistic Interaction Pty Ltd, We Roll Spinners), and the four-part
substance of the confirmation, namely that ChatGPT image generation (`gpt-image-1`) may be used
to create visual art assets including symbols, backgrounds and other artwork for incorporation
into real-money slot machine games published on licensed gambling platforms; that this is
permitted as development-stage artwork; that the service must not be used to operate gambling,
accept or process wagers, process payments or interact with players; and that the permission is
subject to the current Usage Policies and Terms of Use.

**Section 2 of the file states explicitly that this closes the previous "comps and style-targets
only" restriction on ChatGPT-generated art under the stated conditions**, which is what the brief
required.

The substance is reproduced as supplied, not paraphrased, per convention (l.7).

## Three things this session deliberately did NOT do

Each is recorded because leaving any of them unstated would let the record read as more than it
is.

### 1. The machine gate is unchanged, so nothing is operationally unblocked

`docs/licences/PROVIDER_GATE_2026-08-22.md` records OpenAI as BARRED, and that mark is
**machine-enforced**: `scripts/assets/assetforge/provider_gate.json` carries
`"openai": {"mark": "BARRED"}`, and `scripts/assets/assetforge/generate.py:63` raises
`GateRefusal` for any provider whose mark is not `CLEARED`.

**This brief instructed no change to asset pipeline behaviour or ingest rules, so neither file
was touched. A generation call to OpenAI still refuses today.** The record establishes the
ruling; it does not lift the gate.

`provider_gate.json`'s own comment sets the mechanism, quoted: *"Changing a mark here without a
Fable ruling is the violation."* **So lifting the mark is a Fable ruling in its own review-lane
session**, and that is the concrete next step if the owner wants generation unblocked.

### 2. The underlying correspondence is not in the repository

There is no capture of the OpenAI support reply for Ticket 456254 anywhere under
`docs/licences/openai/`. **What is on file is the owner's transcription of that reply, not the
reply itself**, and the new record says so in its own section 3 rather than implying a primary
source exists.

This matters because convention (m) requires external documents to physically exist in the
repository before work cites them, and because this record is load-bearing for a compliance
decision. **Recommended and not done here, the brief being records only:** archive the original
at `docs/licences/openai/2026-08-24/`, in the same shape as the existing 2026-08-22 OpenAI
captures and the Google Gemini capture at `docs/licences/google-gemini/2026-07-15/`. That would
put the primary source beside the transcription and satisfy (m) in full.

### 3. A tension is surfaced rather than decided, per convention (l.8)

R084's BARRED assessment did not rest on an opinion. It rested on a four-link contractual chain,
each link quoted verbatim in
`docs/licences/openai/2026-08-22/openai-services-agreement-operative-clauses.txt`: the Services
Agreement incorporates the OpenAI Policies by reference (16.1), the definitions bring the Usage
Policies inside that incorporation, clause 3.3(a) restricts use that violates them, and the Usage
Policies prohibit "real money gambling". R084 also recorded that a breach of 3.3 is carved out of
the liability cap (14.1) and indemnified (13.2).

**The new confirmation is a statement from OpenAI support, and it states of itself that the
permission is "subject to the current Usage Policies and Terms of Use".** A support confirmation
and the terms it is subject to are different instruments. Whether the confirmation resolves the
chain R084 documented, or sits alongside it, is a legal judgement for the owner and Fable.

**Nothing in this session argues against the ruling.** It is recorded in full and at face value,
which is what the brief asked for. The tension is noted only so that whoever changes the machine
gate does so with both documents in view rather than one.

## Verification

Records-only commit. Close gates chained with `&&` per (u.1), each exit code the direct left
operand. Explicit paths per (k). Remote CI verified with the full SHA per rule 10. The 27
placeholder rasters plus the provenance record were sha256-fingerprinted before and after and are
byte-for-byte identical.

## ESCALATIONS

**E1 (R098). The machine gate and the record now disagree, and the gate wins until a ruling
changes it.** `provider_gate.json` says BARRED; `docs/legal/openai-ticket-456254-ruling.md` says
development-stage artwork is permitted. That is not a defect, it is the correct state after a
records-only session, but it should not persist unexamined: either the mark changes by Fable
ruling, or the gate's `reason` field should cite this record so a future reader is not left with
a mark that looks stale.

**E2 (R098). The primary source is missing.** Archive the Ticket 456254 correspondence at
`docs/licences/openai/2026-08-24/` before the gate mark is changed, so the ruling that unblocks a
real-money product rests on a captured document rather than on a transcription.

**E3 (R098). This ruling does not by itself close R097's provider item**, which is listed there as
the blocking decision the whole arc is downstream of. It closes the *question*; the *operational*
block persists until E1 and E2 are done. R097's finding 34 should be read with this record beside
it.

R097's E1 through E3 stand, as do the open items in R097's ledger: 4 open art decisions, 3 open
code decisions, 11 open tooling.

## FOR THE NEXT SESSION

**The working tree is exactly as R097 left it: 27 modified rasters plus the R091 provenance
record.** The restore command is unchanged and is recorded in full in
`reports/archive/2026-08-24_r097-arc2-placeholder-audit.md`.

**Kit packaging stays forbidden while any placeholder differs from HEAD, and `npm run assets`
remains the second hazard** per R097's E1: it would silently revert 16 of the 27 placeholders.

**The ranked path from here**, updated by this session:

1. **Archive the Ticket 456254 correspondence** at `docs/licences/openai/2026-08-24/` (E2).
2. **Fable ruling on the `provider_gate.json` mark** (E1), with that capture as its evidence.
3. Guard the tree against `npm run assets` (R097 E1).
4. Decide the FX set, which is the entire remaining coverage gap at P4 0 of 8.
5. The tooling items: `--compare-against-shipped`, `ALPHA_SNAP_FLOOR`, the misleading refusal
   message.

Model and effort: one session, unattended, review lane, records only. No raster, no code and no
gate was touched.
