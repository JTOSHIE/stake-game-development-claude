Fable verdicts, 2026-07-07 second update, all from first-hand reads of main.

1. PR #48 WIRING INTEGRITY AUDIT: RATIFIED, and this is the standard. All five deliverables verified present and correct: the trace, the cost-integrity gate (before/after runs reproduce the standingMode bug then pass 5/5 at exact integer micros), the wallet float scan with its handleBuy rounding fix, the fsModes/index.json drift test at 5/5, and both autoplay asserts with minSpinMs confirmed genuinely defaulting to 0. The standingMode fix itself is correct: threading via the one store, selectedBetMode set before play, the per-tier affordability guard properly compensating the locked canSpin's 1x-only check with zero locked-file edits, proofs at the convention path. The escalation handling (flag, owner decision, same-day fix, gate re-run) is exactly the protocol. PR #49 is docs-only, accurate against my own diff, already on main, ratified; the living-handover convention is APPROVED as standing: one handover document per arc with dated appended sections, a fresh document only at a new arc.

2. NEW REQUIRED ITEM (mine, pre-submission): OVERBOOST cost visibility on the HUD. HudOverlay has no standingMode awareness, so with the FEATURES menu closed the BET display shows the base bet while the real debit is 1.25x. Required fix, small and unlocked: derive an effective cost from standingMode; while OVERBOOST is on, the HUD BET display shows the effective amount (bet x 1.25) with a persistent OVERBOOST badge, per the standard ante-bet pattern, and the FEATURES chip reflects the toggle state; Cruise needs only a subtle active-mode label since its cost is 1.0x. Add a cost-visibility assert to the soak (badge and effective figure present whenever standingMode is not base). Execute this as item 0 of the next session, since the same session's copy work documents the toggle.

3. RULINGS ON TRACE FINDINGS 2 THROUGH 8: (2) canBuyBonus locked hardcode: NO lock lift now; it is compensated and unreachable; record it in a LOCKED_FILE_DEBTS note in CLAUDE.md so it rides along with any future sanctioned locked pass, and add a boundary assert to the re-soak (balance above 100x but below 400x the bet must block a super buy). (3) The five sibling verification scripts with the dead .spin-btn selector: fix all five in the hygiene pass; build_diet_verify.mjs first if any build verification runs before then. (6) and (7): benign, add one-line comments marking them intentionally redundant, nothing more. (8) FeatureButton.svelte and ControlBar.svelte: REMOVE in the hygiene pass, ratified; ControlBar's duplicate autoplay call site makes it a live hazard and the stale-artefact precedent applies.

4. SEQUENCING CONFIRMED, no reorder: NEXT SESSION is the dossier and copy update WITH item 2 above as item 0. Copy scope: SUBMISSION_DOSSIER.md, paytable and game-rules copy updated to the five-mode reality with a mode table (Normal 1x; Cruise 1x low volatility; OVERBOOST ante toggle, 1.25x debited every spin while ON, stated plainly; Buy Overdrive 100x; NITRO OVERDRIVE 400x including the 5x pre-revved meter disclosure), RTP 96.35% identical across all modes, 5,000x cap, explicit supersession of the PAR section 11 base-only framing, a short responsible-play paragraph covering the autoplay stop-conditions, and NO soundtrack claim anywhere until audio ships. Then the REVIEW_EVENTS pass with the statelessness artefact and per-mode replay IDs, then the hygiene pass with its now-expanded scope (Collection Meter relocation to a reference branch, dead component removal, the five selector fixes, HANDOVER supersession, prompt archive, explicit-paths commit line in CLAUDE.md, QA log archive). QA re-soak stays gated on audio and picks up the two new asserts from items 2 and 3.

Fable relay, owner eye-calls closed. 1. REEL MODE: ships as DROP. Work order, fold into the already-greenlit dossier/copy session as item 0b alongside the OVERBOOST HUD visibility fix: flip the shipping default in the reelMode store from strip to drop (unlocked change); confirm the reel-mode cycle toggle is dev-only and hidden in the production submission build; update any docs or copy that state strip as default; note in the session report that the QA re-soak must exercise drop as the default path, and if reel_v3_proof.mjs is re-run, fix its stale .spin-btn selector first per the audit's item 3. 2. FLAME-JET: closed with NO code change. Orientation stays as committed (outward from the frame), scale stays at the committed 0.34; the closeout's 0.55 figure is superseded and this eye-call is retired. Both owner eye-calls are now closed; nothing on the board waits on owner input except audio delivery.
