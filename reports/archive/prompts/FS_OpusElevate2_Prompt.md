FS_OpusElevate2_Prompt.md. Save this brief verbatim and commit it with this session per convention.

# FS OPUS ELEVATE 2: flame jets, buy modal, paytable, proof suite, conditional audio

READ FIRST: reports/SESSION_REPORT.md including its FOR THE NEXT SESSION block, reports/archive/2026-07-04_opus-elevate.md, design-system/LAYOUT_SPEC.md through v3.3, DESIGN_SYSTEM.md. Context: you are Opus 4.8 at High; this session completes the scope your previous pass honestly deferred. Autonomy posture per CLAUDE.md (g). Hard locks unchanged; no exception expected. Branch claude/opus-elevate-2 from up-to-date main.

## Task 1: Flame jets (owner concept, the centrepiece)
Extend the asset pipeline to export from M3_master_v3.svg two sprites: the nozzle assembly, and a short flame frame sequence (4 to 6 frames, generated by parametrically varying the flame path scale and core opacity in the SVG before rasterising, deterministic). Mount jet nozzles around the frame edges, at least two per side, positions appended to LAYOUT_SPEC as v3.4. They IGNITE during the Overdrive entry transition in sync with the gauge slam, burn for the entire bonus with a flickering frame loop plus subtle scale breathing, and extinguish on exit. Pooled sprites, zero per-frame allocation, prefers-reduced-motion shows a static glow variant.

## Task 2: Buy modal presentation
Replace the text labels in the buy preview with real symbol images at readable scale. Elevate the modal: scale-in entrance with glow, the Grille art as header, the price on an instrument-plate styled element. Jurisdiction hiding and social behaviour unchanged.

## Task 3: Paytable elevation
A designer pass on the full-page paytable: consistent section header treatment, even spacing grid, aligned symbol cells now showing the new lineup, polished ways diagram, clear typographic hierarchy. Before and after screenshots committed.

## Task 4: Proof suite
Commit to reports/screens/opus-elevate-2/: before and after GIFs for the H2 idle-charge elevation from your last pass, a bonus GIF with the jets burning, and a buy modal GIF, each under 3MB, plus the paytable before and after shots.

## Task 5: Audio, conditional
If ~/Desktop/fs_audio/ exists and contains a music file: transcode to webm opus around 128k with an m4a fallback, integrate as the looping BGM respecting the existing sound toggle with persistence, layer any rain stem at low gain beneath, duck volume briefly under BIG or larger win stingers, and add the files to the served assets with sizes reported. If the folder is absent, skip cleanly and note it.

## Task 6: Gates
Headless fps across 20 spins INCLUDING a full bonus with jets burning averages 55 or better; occlusion and position audits re-run clean including v3.4 jet positions at all viewports; exact-total 58/58; tsc and build clean.

## Task 7: Ship
Session report with verdicts, fps log, and the FOR THE NEXT SESSION block; archive; commit explicit paths; push; PR into main via gh titled "Opus elevate 2: flame jets, buy modal, paytable, audio" with the report as description.
