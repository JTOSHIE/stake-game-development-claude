Fable verdicts on the 2026-07-07 handover, all from first-hand recomputation and reads of main.

1. BUY-TIER BILLING BUG: fix RATIFIED, and yes, a broader pass is warranted. The fix is correct by construction: MODE_COST is derived from FS_MODES (single source, no second table), the mode threads FeatureMenu to BuyBonus to handleBuy with no hardcoded tier, selectedBetMode is set before play(), and the locked rgsService needed no change because its wallet request already reads that store. Proofs at reports/screens/fix-buy-tier-billing/ per convention, and the Playwright balance arithmetic (100.00 minus 40.00 plus 0.06) checks out. On confidence: it should dent it. The bug had three simultaneous silent-discard points and survived the 44/44 exact-total gate and prior QA because every existing gate verifies maths and round totals, not menu-to-wallet integrity. That is a class gap, not a one-off, and it also slipped my own PR #42 ratification, which covered maths and scope but not end-to-end wiring; the gate below closes that hole for both of us. SANCTIONED NEXT WORK ORDER, WIRING INTEGRITY AUDIT (read-only plus tests, no locked files, no sanction needed): (a) static trace of every dispatch/handler pairing in the buy, mode-select, bet-change and autoplay paths, flagging any consumer that ignores e.detail or hardcodes a mode or cost; (b) a permanent cost-integrity gate in qa_soak.mjs asserting, for each of the five modes at a fixed bet, the exact integer-micros debit equals bet times MODE_COST[mode] and the served round's book cost matches the mode, never mixing the 1,000,000 wallet scale and the 100 book scale; (c) a wallet-path float scan asserting all currency arithmetic flows through the micros helpers per the Math.floor convention, with raw float dollars never reaching a request (the telemetry buy event's Math.round is acceptable, it is analytics only); (d) a unit test cross-checking fsModes labels, serverMode ids and the published index.json costs so a future tier cannot drift; (e) autoplay asserts per item 3 below. Commit the audit report, the new tests, session report and brief per convention.

2. FEATUREMATH V2: signed off, locked and done. Git proves games/future_spinner is untouched since the PR #42 merge, so my independent recomputation from the shipped tables stands in full: all five modes exactly 96.350000 percent, spread 0.0000pp, 5,000.00x cap in every table, tail gate 3.20e-3 under 1e-2. The one standing directive remains: the REVIEW_EVENTS pass must commit the statelessness artefact (scan script plus output summary under reports/qa/) and per-mode replay IDs for cruise, antelite and super.

3. RG MODULE AND TELEMETRY: approved with two required adjustments. Telemetry is exactly right: pluggable sink, no-op until wired, zero network calls, observe-only off the outcome path, and win tiers correctly pinned to the shipped C1 thresholds. The RG design is also right in shape (jurisdiction-derived, limits in integer micros, stopOnFeature default true). The compliance detail you asked me to check: the platform manual's only hard autoplay rule is that the player must explicitly confirm autoplay and games must never place consecutive bets from one click; there is NO platform minimum spin interval, and the 2.5s figure is UKGC-specific. Required: (i) confirm minSpinMs resolves to 0 unless the authenticate jurisdiction flags demand otherwise, so Stake sessions keep native game feel, and (ii) assert autoplay start always requires an explicit confirm tap. Fold both asserts into the wiring audit above.

4. COLLECTION METER: stays shelved, and it must come off main. The prototype landed as a full sibling package at games/future_spinner_collect/, which is outside the lock (no violation) but breaks the standing guard that reference maths (the 11-mode library, MASTER_TEMPLATE) never sits on main, and a second maths package beside the shipping one is precisely the stale-artefact misread that cost us a 2-star external audit before. In the hygiene pass, relocate it to a reference branch (claude/collect-prototype) with a one-line pointer in docs. Creatively it is worth keeping: it reads as a LUMEN mechanic or a Future Spinner v1.1 post-launch mode, not a pre-submission addition.

5. STILL OPEN FROM PR #38 AND SINCE: the two owner eye-calls (strip versus drop reel mode; flame-jet 0.55 scale and orientation) remain unanswered and are now the oldest open items; audio is in progress on the owner side via the AudioForge pipeline; the hygiene pass scope now adds the future_spinner_collect relocation to the previously agreed items (HANDOVER supersession, prompt archive, explicit-paths CLAUDE.md line, QA log archive); the dossier and paytable five-mode copy update remains open; QA re-soak stays gated on audio and should land together with the wiring audit's new gates.

Sequencing: wiring integrity audit now, then dossier and copy update, then hygiene pass; re-soak plus external audit refresh when audio ships.
