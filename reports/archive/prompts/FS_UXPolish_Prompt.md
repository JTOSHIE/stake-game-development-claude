# FS UX POLISH: HUD v3.2, full-page paytable, wincap flow, mock pool

Autonomy posture per CLAUDE.md (g). Hard locks unchanged; no exception expected; stop and report if you conclude otherwise. Branch claude/ux-polish from up-to-date main.

1. LAYOUT_SPEC v3.2 amendment (edit design-system/LAYOUT_SPEC.md, appending an AMENDMENT v3.2 section, then implement it): the HUD panel widens to x 296 to 984; TURBO 72 centred at (268,604) OUTSIDE the panel left; SPIN 84 centred at (1004,604) OUTSIDE the panel right; AUTOPLAY 48 at (936,672). Inside the panel from x 344: hamburger; BALANCE in a FIXED box x 400 width 200; WIN in a FIXED box x 616 width 150; BET at x 782 with the value right-aligned in a fixed 120 width and the stacked cyan arrows at a FIXED column x 916, never repositioned by content. All numeric values render with tabular numerals (font-variant-numeric: tabular-nums) so digits never reflow, and no element may move as values grow. GATE: re-run the occlusion and position audits with stress values loaded (balance $10,000.00, win $5,000.00, bet $5,000.00) at 1280x720 and all six compliance viewports; commit screenshots to reports/screens/ux-v1/; zero intersections and the bet arrows visible and clickable in every shot.

2. PAYTABLE rebuilt full-page: the modal fills about 92 percent of the stage with a scrollable body in system style: large symbol cells (the 240 exports) with per-count values in a readable grid, an Overdrive section (trigger table, meter rules, retrigger, buy price where jurisdiction allows), a WAYS TO WIN explainer with the standard adjacent-reels block diagram (draw it in CSS or inline SVG, left to right adjacency illustrated), RTP 96.35 percent both modes, the 5,000x cap, and the existing seven point disclaimer reachable as today. All 16 locales, social variants intact. GATE: paytable screenshots (top and scrolled) committed to reports/screens/ux-v1/.

3. WINCAP FLOW per owner decision: when a round is capped, show the MAX WIN celebration immediately as now; on COLLECT, present the complete round sequence through the interpreter (the how it happened), finishing on the total win summary. Applies in normal play, bonus, and replay. GATE: headless run of the wincap sample round demonstrating splash, collect, full presentation, summary, screenshot at the presentation stage committed.

4. MOCK POOL: extend frontend/src/lib/mock/sample_rounds.json using the real books (read-only) to at least 30 bonus-mode rounds spanning small, mid and large outcomes with only one wincap round, and select uniformly at random per buy in mock mode. GATE: exact-total interpreter test passes on the ENTIRE expanded pool.

5. tsc and build clean; session report with the audit matrices plus archive; commit explicit paths; push; PR into main via gh titled "UX polish: HUD v3.2 fixed fields, full-page paytable, wincap flow, mock pool" with the report as description.
