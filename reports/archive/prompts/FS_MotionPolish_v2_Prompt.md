# FS MOTION POLISH v2: the game comes alive (reels, symbols, wins, transitions, brand screens)

Autonomy posture per CLAUDE.md (g). Hard locks unchanged; no exception expected; stop and report otherwise. Branch claude/motion-polish-v2 from up-to-date main. The energy bar for win states is the old video set in the repo (frontend/public/assets/themes/future-spinner/symbols win videos in git history and any remaining local copies): explosive, alive; match or beat that energy with engine motion, never with video.

1. REEL FEEL (defect 4, owner priority one): ticker-driven transforms at 60fps; symbols blur vertically during spin (pre-blurred texture swap or velocity-scaled stretch, no live blur filters); per-reel staggered stops left to right with overshoot slam and settle; when two scatters are landed the final reel extends its spin under an anticipation glow before stopping; pressing SPIN mid-spin slam-stops all reels instantly (outcome unchanged); the three tiers from speedMode scale every duration (Normal, Turbo, Super Turbo, with Super Turbo near-instant); autoplay honours the active tier.
2. SYMBOL LIFE: using the layered exports, H1's spoke sprite rotates continuously at idle and spins up on wins; H2's needle sprite idles with a small flicker and slams into the redline on wins; every other symbol gets its lineup-table idle micro-motion (grille light bar sweep, booster flame flicker via 2-frame alternation or scale pulse, piston heat shimmer pulse, plug arc blink, gentle breathing on the rest) and a WIN state at the energy bar: the tile plate edge blooms in the symbol's signature colour, the symbol scales with a punch, and a particle burst fires in the signature colour (Pixi particles, pooled, no allocations per frame).
3. WIN PRESENTATION (defect 3): winning symbols brighten while others dim; the ways breakdown cycles group by group after settle showing symbol, count and pay, matching the interpreter's script; win amounts count up incrementally as today.
4. CELEBRATIONS: tiered BIG, MEGA, EPIC thresholds with staged count-up and escalating particles; screen shake on feature trigger and on wins at 50x and above; every effect respects prefers-reduced-motion.
5. OVERDRIVE TRANSITION: on trigger, scatters flare, the screen dips, the gauge slams to centre with the needle ripping into the redline, the awarded spin count bursts out as a text burst, then the gauge settles into its column position while the background crossfades to bg_overdrive and the frame neon shifts hue; reverse the shift on exit behind the total win.
6. SCENE LIFE: the car's underglow pulses slowly, the booster flickers faintly, the character breathes with an occasional visor glint; subtle, never competing with the reels.
7. BRAND SCREENS: the loading screen becomes the WRS standard: the brand mark rendered large with its inner layer spinning as the loader, WE ROLL SPINNERS wordmark above and the game logo below in CSS Orbitron; after load, the INTRO SPLASH explainer card per the design system (Overdrive rules at a glance, Continue control), localised across all 16 locales with social variants, shown once per session.
8. PROOF GATES: fps sampled headless across 20 spins including a bonus entry must average 55 or better with no long-frame spikes over 100ms logged; capture three short proof GIFs (a spin with stop stagger, a win with plate bloom and particles, the Overdrive transition), each under 3MB, committed to reports/screens/motion-v2/; exact-total test passes on the full expanded pool; tsc and build clean; occlusion audit re-run clean at 1280x720.
9. Session report with fps log plus archive; commit explicit paths; push; PR into main via gh titled "Motion Polish v2: reel feel, symbol life, celebrations, Overdrive transition, brand screens" with the report as description.
