FUTURE SPINNER CONSOLIDATED WORK ORDER, 2026-07-13. Execute jobs strictly in sequence. Per job: explicit-path commits only per convention (k), session report plus dated archive copy, proofs to reports/screens/ or reports/qa/ per convention, never touch frontend/src/lib/services/rgsService.ts, frontend/src/lib/stores/gameStore.ts, games/future_spinner/ or .claude/settings.json, and end each session with FOR THE NEXT SESSION. Commit this brief verbatim before starting.

JOB 1, AUDIO INTEGRATION. (1a) If the incremental-logging fix to generate.py is still uncommitted, commit it first. (1b) Promote seed 20260707 for all twelve rows via promote.py so ~/Desktop/fs_audio/ holds the twelve final WAVs. (1c) Create tools/audio_forge/master.py, a deterministic mastering script so any single row can be re-mastered later with one command: strips leading and trailing silence below -60 dBFS; fades reel_stop's tail to 250ms; trims the three loop rows to clean loop points, the two beds to the longest whole-bar count that fits their generated length at their BPM (bgm_loop 100, bgm_tension 140) cut at zero crossings, anticipation_build to an even seamless cycle; loudness: both beds normalised to about -18 LUFS integrated, every SFX peak-normalised to -3 dBFS with the win family checked to escalate small to medium to big to epic; encodes one-shots to MP3 192k, and the three loop rows to WebM Opus (128k beds, 96k anticipation) plus an MP3 fallback trimmed at zero crossings; outputs into frontend/public/assets/themes/future-spinner/sounds/ at the exact filenames the theme manifest expects. (1d) Extend soundService minimally: the three loop entries pick WebM via canPlayType('audio/webm; codecs="opus"') with MP3 fallback; anticipationBuild gets loop=true and stops on reel resolve; add a bed swap so bgm_tension replaces bgm_loop on Overdrive feature enter and reverts on exit via a short volume-ramp crossfade using the existing duck plumbing; align the win-sound tier thresholds to the shipped C1 celebration tiers (10x, 30x, 100x, with MAX reusing the epic echo as designed); confirm playback only ever starts after a user gesture per browser autoplay policy and that the mute toggle and both sliders persist. (1e) Commit the mastered shipping files (not the WAVs, nothing from ~/Desktop except these derivatives), plus provenance: copy GENERATION_LOG.md to reports/audio/GENERATION_LOG_2026-07-13.md and add a sounds/README.md noting per-file model, seed, prompt pointer and the Stability AI Community License. (1f) Verify: npm run build green, svelte-check no new errors, then a Playwright audio pass asserting every sound URL returns 200, spin/reel_stop/win sounds fire on a real spin, the bed swap fires on a bonus buy, and no console errors; commit the log. (1g) Hand the owner a working link: run the production preview bound to the LAN (vite preview --host), verify it serves, and print the exact URL as the final line of your reply in the form http://<mac-lan-ip>:4173 with a note that the phone must be on the same wifi; leave it running.

JOB 2, QA RE-SOAK, ALL FIVE MODES, now un-gated. Run the full soak on the built bundle exercising drop as default: per-mode cost-integrity gate re-run (exact integer micros for all five); NEW boundary assert per Fable's ruling: with balance above 100x but below 400x the bet, a NITRO OVERDRIVE buy must be blocked while Buy Overdrive remains available; NEW cost-visibility assert: whenever standingMode is not base the HUD shows the badge and the effective bet figure; audio asserts folded in from JOB 1; frame gate with attribution: any frame over 100ms logs its timestamp and the nearest game event so the known ~150ms buy-moment hitch is either attributed or shown gone; refresh layout_v1_audit's stale expected position values from the current build (occlusion stays the only hard gate); confirm the reel-mode toggle is absent from the production bundle; a reduced-motion pass. Commit fresh dated logs under reports/qa/ replacing nothing (archives stay archived).

JOB 3, COMPLIANCE RE-VALIDATION. Re-walk COMPLIANCE_WATCH.md line by line against current main with dated evidence pointers: RG jurisdiction defaults (minSpinMs 0 unless flags demand), autoplay explicit-confirm gate, provably-fair determinism test green, telemetry confirmed no-op by default with zero external network calls in the bundle, bet levels, and the RGS failure paths (disconnect mid-spin, insufficient funds on buy, resume/replay) each exercised once with the observed behaviour recorded. Update COMPLIANCE_WATCH.md with a dated re-validation block.

JOB 4, BUILD BUDGET RE-VERIFY. Run build_diet_verify on the audio-bearing bundle; record the new dist size in the session report; assert under 25MB and no requests into pruned paths.

JOB 5, DOSSIER AND COPY. Restore the soundtrack sentence to the submission blurb as a DRAFT marked PENDING OWNER APPROVAL, never marked approved by you; write dossier section 5 in full as the staging protocol: exact portal upload steps, the publish_files inventory with fresh SHA-256 hashes, the frontend build artefact and how it is produced, and a one-page owner checklist separating one-time portal actions from per-update actions; update GAME_FACTS.md's audio row from "pending" to shipped with the provenance pointer.

JOB 6, EXTERNAL AUDIT REFRESH PREP. Refresh the audit pack to point ONLY at current artefacts (the living handover, current QA logs, COMPLIANCE_WATCH, the dossier, the PAR sheet), with an explicit list of superseded documents an auditor must ignore; flag ready in the session report; the audit itself runs later as a separate fresh session after Fable's next check-in, not in this work order.

JOB 7, STOREFRONT TILE AND LOGO SCAFFOLD. Locate the official Stake Engine game tile and thumbnail specification in the repo docs or the developer manual; record the exact required dimensions, formats and any text-safe areas in the session report; scaffold AssetForge output slots for the tile and the logo at those dimensions. DO NOT author any artwork; Fable authors both art masters at the next check-in from your reported spec.

JOB 8, ROUND-TWO AUDIO SLOTS, placeholder only. Note in the session report, without generating anything: bonus_trigger, buy_confirm, wild_land, coin_count, win_max and ambience_rain are deferred until the owner has played the JOB 1 build and Fable has ruled on the mix.

STOP CONDITIONS. If any job's gate fails twice, stop that job, write the failure state to the session report, and continue to the next job only if independent; JOB 2 depends on JOB 1; JOBS 3 to 7 are independent of each other.
