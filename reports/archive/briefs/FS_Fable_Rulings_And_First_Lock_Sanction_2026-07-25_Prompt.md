# FABLE RULINGS AND THE FIRST LOCK SANCTION, 2026-07-25

Saved verbatim per conventions (b) and (f). Pasted by the owner, relaying Fable.

---

FABLE RULINGS AND THE FIRST LOCK SANCTION, 2026-07-25 (true date; see ruling 4). COMMS-ACK: entry 010; append receipt. MERGES: PRs 98, 99, 100, 101, 102 approved, merge in that order, delete branches, tracker rows updated per merge.

DECISION 1, XEC: HOLD, resolved empirically at the Developer Testing Tool session, confirming my earlier ruling. A citation-free FAIL from one reviewer does not outrank three first-party sources with no trace of it; the SC-family scaffolding means implementation is an afternoon if the DTT shows XEC live; the parked row already records the resolution path. DECISION 2, THE R1a SANCTION, granted per convention (e), owner authority relayed: lift, as a temporary working-tree edit of .claude/settings.json, ONLY the deny entries matching Edit and Write on frontend/src/lib/services/rgsService.ts; restore with a verified empty diff before any commit; branch fix/R1a-rgs-locked-pass, one isolated commit, nothing else in it. Enumerated scope, all in the one pass since debts ride sanctioned passes: (a) align the live event handling to the canonical reveal/winInfo schema by delegation to roundInterpreter, only as R1's diagnosis requires; (b) route endRound through _withRetry with idempotent retry, closing the R11 gap inside this file; (c) delete the unreferenced-and-wrong canIncreaseBet and canBuyBonus helpers; (d) replace the local CURRENCY_SCALE copy with the canonical import; (e) nothing else, however tempting. Gates: every other locked path untouched, fixture tests proving the schema alignment against decoded book rows, the settings empty-diff proof in the session report, and Fable reviews the locked diff line by line before merge; this PR does not merge on the standing same-day authorisation, it waits for my explicit block. DECISION 3, RG WORDING: the English masters are mine and are these, verbatim: menu item "Session"; panel title "Session information"; rows "Time played", "Spins", "Total wagered", "Total won", "Net result"; reality-check title "Reality check"; body "You have been playing for {time}. Your net result this session is {amount}."; buttons "Continue playing" and "Stop playing" (stop halts autoplay and returns to idle). Translate using the standard casino responsible-gambling lexicon per locale rather than literal machine translation, note translation confidence in the tracker row, and the DTT language pass validates rendering; professional review triggers only if a mandatory-display jurisdiction ever activates. DECISION 4, DATES: commit dates are authoritative and the calendar is right where it says: today is 25 July 2026, and the 26/27/28 labels on my recent briefs were my drift, now owned; relabel nothing retroactively, note the drift once in the tracker, and all documents from here use the true date. ALSO: the parked PAR question converts to a numbered comms item with two or three options like everything else; conversations-to-be-had-later are how items rot. SECURITY: the Drive-sharing closure is confirmed and correctly recorded; the backup stays exactly as it now is, private, sharing off, location on record; the lesson line for the register: backups of pre-release game internals are private by default, verified at creation, not after.

SEQUENCE FROM HERE: Wave 2 merges, R1a sanctioned pass with my line-by-line review, Wave 3 remainder (R1 adapter, R2, R3, R11 balance), feature/scatter-anticipation and feature/cohesion-pass, closing conformance with both heavy suites in a fresh environment, then the Developer Testing Tool staging session which empirically settles XEC and the CVaR definition, then portal, Tile Editor, dossier walk, submit.
