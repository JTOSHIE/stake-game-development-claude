OWNER AUDIT ROUND 3, IDENTITY CANONICALISATION AND HUD LOCK, 2026-07-25. Conventions, locks and reporting as pinned; merge PR #87 first; fresh branch; proofs to reports/screens/owner-audit-v3/. (1) LOGO CANONICALISATION: the hero emblem is the sole WRS mark. Build tools/brand/derive_hero_icon.py: a deterministic circular centre-crop of design-system/brand/hero_emblem/master_1024.png capturing the wheel-and-reel core without the wordmark arcs, exported at 192, 96, 48 and 32 on transparent-circle-over-dark-plate; commit under design-system/brand/hero_icon/ with proofs; swap the in-game logo pod asset to the 96 derivative; move vector_mark v2 and v3 (all files) to design-system/brand/archive/ with a SUPERSEDED.md pointing at the hero; update the WRS_MASTER_DOCUMENT.md brand rows: provider upload file is hero_emblem/master_512.png, icon set is hero-derived, vector track retired. (2) NAMING UNIFORMITY: every feature-HUD field label renders from ONE shared constant across portrait and landscape templates (canonical: OVERDRIVE FREE SPINS and TOTAL WIN); add an assert comparing the rendered label strings across all four profiles for equality, and sweep for any other cross-layout label divergence, fixing to shared constants throughout. (3) CLICK TO CONTINUE placement: move the CTA down to sit on the card's lower border, centred, never overlapping the award text at any viewport; proofs both orientations. (4) NITRO goes pink: shift the NITRO colourway pink-forward per the owner (pink backdrop treatment, deep-pink-core white-tipped flames, sweep motion unchanged), preserving the 90-degree flame-versus-backdrop contrast law; refresh the colourway proofs. (5) INTERFACE GUIDE: the turbo and max icons captured identically; fix the capture selectors so every control captures its own element, regenerate, and extend the icon proof grid with a uniqueness assert (no two guide icons byte-identical). (6) FEATURES BUTTON: standardise both layouts on the mobile treatment: small hamburger glyph plus FEATURES text with the purple accent; remove the large desktop button variant. (7) HUD BANNER RE-MEASURE AND LOCK, desktop: adopt the mobile MAX button styling on desktop; dock the AUTO control as a circle tangent to the SPIN button's right edge (touching, never overlapping); then re-measure the entire control banner (spin, auto, turbo, max, steppers, balance, win, bet): consistent spacing grid, true centring, all targets 44px+, stress values fitting via autofit; write the locked measurements to docs/HUD_SPEC.md and update the conformance asserts to enforce that spec permanently; before/after proofs. (8) BONUS REEL SIZE, the point of the whole relayout: the feature must render the grid at EXACTLY base-game size in both layouts; diagnose the residual shrink (instrument column reserving stage width in landscape is the prime suspect: it must overlay or sit outside the stage scale, never compress it); add a hard assert that the grid bounding box in feature screenshots matches base-game within 2px on every profile. (9) AUTOPLAY MENU: enlarge the panel with generous spacing; single-win limit and loss limit get bigger, easily tappable controls; targets 44px+; proofs. (10) SUITES: run the FULL conformance suite clean this session (mandatory gate); attempt the full qa_soak matrix, and if local resource pressure recurs, document precisely and defer the soak alone to the external audit's fresh environment where it remains a named gate. Session report with per-item root cause and the updated assert table.
