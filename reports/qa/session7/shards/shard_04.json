[
  {
    "id": "S2-C048",
    "verdict": "STILL_OPEN",
    "evidence": "frontend/package.json:6-14 lists exactly seven scripts (dev, build, preview, check, assets, qa:soak, owner:preview) and there is no \"scan\" entry: `grep -n '\"scan\"' frontend/package.json` returns nothing. .github/workflows/checks.yml:293-297 is still just the bare `Install frontend dependencies` step (`run: npm ci --ignore-scripts`) followed at 299-302 by `# Gate 1: type safety` / `- name: svelte-check (no new type errors)`; no step was inserted between them. Repo-wide, `grep -rn \"audit\\|licen\" .github/workflows/` returns only four prose comment lines (checks.yml:20, 24, 92, 703) and zero `npm audit`, `audit-level`, or ClamAV/anti-malware invocation in any of the three workflow files (checks.yml, publish-stake-engine.yml, validate-math.yml). Nothing runs over frontend/dist after the production build. No committed dated scan record exists: `find reports -iname \"*vuln*\" -o -iname \"*malware*\" -o -iname \"*clam*\" -o -iname \"*npm_audit*\" -o -iname \"*dependency*\"` returns no matches.",
    "remaining": "All three parts: the `\"scan\": \"npm audit --omit=dev --audit-level=high\"` script at frontend/package.json:13, the matching static-job step after checks.yml:297, the anti-malware pass over frontend/dist after the production build step, and the committed dated scan output as the submission record. Separately, whether the Errors half has actually been split off to TR-115 / TR-086 is bookkeeping I could not settle: the ledger files that would record the split are on the do-not-open list. The repository-side defect above is settled regardless of how the split is recorded.",
    "self_verifying": "YES"
  },
  {
    "id": "S2-C049",
    "verdict": "STILL_OPEN",
    "evidence": ".github/workflows/checks.yml:297 reads `run: npm ci --ignore-scripts` (inside the `Install frontend dependencies` step at 293-297). The next non-blank content is checks.yml:299-302, the `# Gate 1: type safety, regression-only` comment and `- name: svelte-check (no new type errors)`. No vulnerability-scan step was inserted after 297. No workflow in .github/workflows/ invokes `npm audit`, `audit-level`, or any other lockfile scanner. frontend/package-lock.json is present and fully resolved (it pins the git dependency at line 2094), so there is a lockfile to scan, but nothing scans it. The \"and record\" half is also unmet: reports/qa/ contains 84 entries and none is a dependency-vulnerability record; the find for *vuln*/*npm_audit*/*dependency* under reports/ returns nothing.",
    "remaining": "The scan step after checks.yml:297 that runs against the resolved lockfile, and the committed dated artefact under reports/qa/ that a reviewer can open. Per the row this should be folded together with S2-C048's scan step as one mechanism; at HEAD neither half of that single mechanism exists.",
    "self_verifying": "YES"
  },
  {
    "id": "S2-C051",
    "verdict": "STILL_OPEN",
    "evidence": "frontend/package.json:25-29 still declares the three production dependencies unaccompanied by any inventory: `\"@fontsource/orbitron\": \"^5.2.8\"`, `\"pixi.js\": \"^7.4.3\"`, `\"stake-engine\": \"github:StakeEngine/ts-client\"`. No inventory step exists after checks.yml:297 (see S2-C049 evidence: 297 is `npm ci --ignore-scripts`, 299 is the Gate 1 comment). No licence inventory is committed under reports/qa/: no file matching *licen* exists there. The only third-party licence document in the repo is /Users/jt/math-sdk/THIRD_PARTY_LICENSES.md, which I read in full (80 lines) - it covers ONLY the inherited Python Stake Engine math SDK (src/, utils/, optimization_program/, tests/, uploads/, setup.py, \"79 tracked files in total\") plus the verbatim MIT text. It says nothing about the npm production closure, nothing about pixi.js, nothing about the ts-client git dependency, and nothing about Orbitron. The git dependency is pinned at frontend/package-lock.json:2094 to `git+ssh://git@github.com/StakeEngine/ts-client.git#df9e126d79b3fe1ef353f4fac9c1699cd79a4d3e`, and no fetched LICENSE text or hash for that commit is recorded anywhere. On OFL-1.1 attribution: a repo-wide grep for `OFL|Open Font License|SIL ` (excluding node_modules, .git and the do-not-open dirs) returns only design-system/brand/archive/vector_mark/GENERATION_NOTE.md:12 (an archived asset-generation note pointing at frontend/node_modules/@fontsource/orbitron/LICENSE) and reports/archive/2026-07-23_fable-art-masters.md:43. Neither is a shipped credits surface.",
    "remaining": "The production-closure licence inventory committed dated under reports/qa/; the regenerating, allowlist-enforcing static-job step after checks.yml:297; the LICENSE text fetched at the pinned commit df9e126d with its hash recorded beside the inventory rather than trusting the package.json field; and the OFL-1.1 attribution for Orbitron in the shipped credits.",
    "self_verifying": "YES"
  },
  {
    "id": "S2-C052",
    "verdict": "STILL_OPEN",
    "evidence": "The telemetry sink is still source-guarded only. frontend/src/App.svelte:206-209 reads `if (import.meta.env.DEV) {` / `const buf: TelemetryEvent[] = []` / `;(window as unknown as { __telemetry: unknown[] }).__telemetry = buf` / `setTelemetrySink(bufferSink(buf))`. (The recorded location 198-201 has drifted: at HEAD those lines are the replay-route comment block.) No static assertion over the built bundle exists: `grep -rn \"setTelemetrySink\\|__telemetry\" frontend/scripts/ scripts/` returns ZERO matches, so neither of the two named fix sites carries it. frontend/scripts/dist_hygiene_gate.mjs (18116 bytes) contains no occurrence of `setTelemetrySink`, `__telemetry`, `telemetry`, or an origin/http check - its only `http` hit is line 260, a literal `'<a href=\"https://stake.com/casino\">more</a>'` used as fixture text. It is wired at checks.yml:723-725 as `- name: dist hygiene, no documentation ships` / `run: node scripts/dist_hygiene_gate.mjs`, unchanged. Nothing scans frontend/dist/assets/*.js for those two identifiers or for non-RGS absolute http(s) origins, and no convention (p) seed moving the setTelemetrySink call outside the DEV guard exists.",
    "remaining": "The whole instrument: a scan of frontend/dist/assets/*.js for `setTelemetrySink`, `__telemetry`, and any absolute http(s) origin that is not the RGS host, failing on a hit; plus the convention (p) seed that hoists the App.svelte:209 setTelemetrySink call out of the `import.meta.env.DEV` guard at 206 and proves the gate goes RED. Per the row this is the same instrument as S2-C058, and neither row's half of it exists at HEAD.",
    "self_verifying": "YES"
  },
  {
    "id": "S2-C058",
    "verdict": "STILL_OPEN",
    "evidence": "PARTIALLY built, but the defect stands. The wiring half IS done: checks.yml:890-891 is a browser matrix leg `- name: \"browser: build diet, pruned paths and budget\"` / `run: node scripts/build_diet_verify.mjs --self-test && node scripts/build_diet_verify.mjs`, in the same `--self-test && ...` form as the other legs. But the capability the row exists for is absent, and the gate says so itself. frontend/scripts/build_diet_verify.mjs:3-13 (its own header, at HEAD): \"WHAT THIS GATE DOES NOT TEST... It does NOT test request ORIGIN. Line 110 computes `rel` by splitting the response URL on the base URL, so for any off-origin URL `rel` is undefined and the pruned-path block is skipped entirely... a SUCCESSFUL external load (status 200) is invisible here.\" And lines 10-13: \"MEASURED 2026-07-31... injecting the exact historical TR-001 defect, a `fonts.googleapis.com` stylesheet link, into a scratch copy of dist and running this gate unmodified returns exit 0 and ALL CHECKS PASS.\" The self-test seeds the wrong class: build_diet_verify.mjs:349 sets `const SEEDED_PATH = 'assets/symbols/tr111_seeded_violation.png'`, a pruned-asset path, and lines 340-347 explain the seed form is a pruned-asset reference, not an external request. The leg was also deliberately NOT given the row's name - checks.yml:884-889 records the 2026-07-31 rename away from \"browser: build diet network hygiene\" because that name \"asserted by its name the one capability Session 6 proved this gate lacks.\" The correct origin assertion at frontend/scripts/platform_conformance_item2.mjs remains unwired: `grep -rn \"platform_conformance_item2\" .github/workflows/` returns nothing. No telemetry class is asserted either (grep for `setTelemetrySink|__telemetry` across frontend/scripts/ and scripts/ returns zero). The defect location frontend/src/main.ts:2-4 is unchanged, still the three self-hosted `@fontsource/orbitron` CSS imports under the comment \"Self-hosted Orbitron font weights (Stake Engine CDN-only compliance)\" - the substance is correct but nothing in CI proves it.",
    "remaining": "The origin assertion itself (either added to build_diet_verify.mjs or by wiring platform_conformance_item2.mjs, which compares `new URL(u).origin` properly); the convention (p) seed in the real defect form, patching the SERVED bundle with an external font or CDN request and proving RED; the leg name \"browser: network hygiene\", which the workflow deliberately withholds until the capability exists; and the second assertion class (studio-side telemetry sinks) from the same request log, which is S2-C052's half.",
    "self_verifying": "YES"
  }
]
