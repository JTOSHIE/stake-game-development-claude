[
  {
    "id": "S2-C010",
    "verdict": "STILL_OPEN",
    "evidence": "Both halves of the row are absent at HEAD. (1) No gate file: `ls /Users/jt/math-sdk/frontend/scripts/brand_token_gate.mjs` -> \"No such file or directory\"; `grep -rn brand_token /Users/jt/math-sdk/.github /Users/jt/math-sdk/frontend` returns nothing. (2) No workflow steps: .github/workflows/checks.yml:457-463 the machine-tell pair ends at \"machine tell gate, source scan\" (line 461-463) and line 465 goes straight to the comment banner \"# -- Gate 8: authenticated bet ladder\". There is no Gate 7b pair and no match for \"7b\" anywhere in checks.yml. (3) The document lines are unchanged and still timeless: COMPLIANCE_WATCH.md:19-22 read `- **Original IP:** verified. Original designs, produced in-house from vector masters. / No pre-purchased or third-party licensed content.` (19-20), `- **No Stake branding:** verified. No Stake trademark or themes in any shipped asset or text.` (21), `- **No underage appeal:** verified. No child or child-like characters.` (22). No date, no assessor, no gate name on any of them, and no owner/human label distinguishing the underage-appeal and original-IP claims from a machine-checkable one.",
    "remaining": "Write frontend/scripts/brand_token_gate.mjs (dist/ + frontend/src/ + design-system/brand/** filename scan with the platform-reference allowlist); add the two convention-(p) steps after the machine-tell source scan at checks.yml:463; rewrite COMPLIANCE_WATCH.md:19-22 to carry date, assessor and gate name, and to label original-IP and underage-appeal as owner/human rather than gated.",
    "self_verifying": "YES"
  },
  {
    "id": "S2-C024",
    "verdict": "STILL_OPEN",
    "evidence": "(a) The swallow is untouched. frontend/scripts/layout_fit_gate.mjs:190 is still `    if (!el) continue` (confirmed by `grep -n \"if (!el) continue\"` -> single hit at 190). The surrounding loop at :188-196 pushes into `readouts` only for elements that were found, so an absent hud-balance/hud-win/hud-bet leaves no trace for the caller; `grep -n missing layout_fit_gate.mjs` returns no hits, so there is no `{ id: q, missing: true }` record and no caller-side failure. (b) The currency clause still has no wired assertion. `grep -n currency_conformance .github/workflows/checks.yml` matches only line 19, a prose comment listing it among things that \"stay local and in the external\" evidence set; the browser matrix at checks.yml:826-860 lists layout fit, contrast, splash calm, turbo intensity, paytable card fill, scrim coverage, win count-up steady, win count-up sync, max-win hold, bet selector and onward, with no currency_conformance leg. And the fallback path was not taken either: `grep -n \"currencySymbolFor\\|isVirtualCurrency\" frontend/scripts/layout_fit_gate.mjs frontend/scripts/max_win_hold_gate.mjs` returns nothing, so neither gate asserts the hud-balance/hud-win symbol against the same two functions HudOverlay.svelte uses.",
    "remaining": "Convert layout_fit_gate.mjs:190 to a recorded `{ id: q, missing: true }` with a caller-side failure scoped to \"at least one of the three mounted per profile\"; and either add a currency_conformance leg to the checks.yml browser matrix or add the currencySymbolFor/isVirtualCurrency DOM assertion to the layout gate.",
    "self_verifying": "YES"
  },
  {
    "id": "S2-C025",
    "verdict": "STILL_OPEN",
    "evidence": "The swallow is still there verbatim. frontend/scripts/max_win_hold_gate.mjs:309-310:\n```\nconst readout = (page, id) =>\n  page.locator(`[data-testid=\"${id}\"]`).first().innerText().catch(() => '<absent>')\n```\nNo `throw new Error(`hud readout ...`)` anywhere in the file. The positive precondition was not added either: the comparison block now sits at :461-464 (`ok(balance0 === balance1, ...)` / `ok(win0 === win1, ...)`) with no preceding assertion on win0; `grep -n \"test(win0)\"` and a scan of :455-470 show no non-empty or currency-shape check, so two `'<absent>'` reads, or two empty text nodes, still satisfy the equality. The shared half is also open: frontend/scripts/layout_fit_gate.mjs:190 is still `if (!el) continue` (same finding as S2-C024a), so the collision the row warns about has not been resolved in either direction.",
    "remaining": "Replace the `.catch(() => '<absent>')` at max_win_hold_gate.mjs:310 with a hard throw; add a `/[0-9]/.test(win0)` non-empty/currency-shaped precondition before the equality checks at :461-464; and apply the same shape to layout_fit_gate.mjs:190, coordinated with S2-C024.",
    "self_verifying": "YES"
  },
  {
    "id": "S2-C028",
    "verdict": "STILL_OPEN",
    "evidence": "None of the three driveReplay loads exist, and neither does the seed. The REQ-077 optional-parameter block now sits at frontend/scripts/replay_contract_gate.mjs:455-479 (it ends with the `no NaN in the rendered money figure` check at :478-479) and is followed immediately at :481-494 by the GUIDELINE ITEM 50 flat-multiplier block, i.e. nothing was inserted after it. Confirming greps over the whole 826-line file: `XSC` -> no hits; `'abc'` -> no hits; `amountMicros: '-` -> no hits; `mode.*strip` / `delete .*mode` -> no hits. The only amountMicros overrides are the default `'10000000'` at :113 and `'1000000000'` in the JPY load at :473, both valid. The seed roster (`grep -n \"await seed(\"`) is unchanged at seven: hardcoded-mode (:616), multiplier-suppressed-at-1x (:641), figures-lost-after-play (:661), social-label-not-swapped (:684), transposed-segments (:694), leaked-authenticated-call (:703), duplicate-replay-request (:712). There is no seed that byte-patches out the isVirtualCurrency disjunct. The disjunct it would cover is still live and unasserted at frontend/src/lib/services/replayService.ts:96 `const social = socialFlag || isVirtualCurrency(currency)`.",
    "remaining": "Add the three driveReplay loads after replay_contract_gate.mjs:479 -- (a) XSC + social=false asserting SC symbol and social vocabulary with no raw 'XSC', (b) amountMicros 'abc' and '-5' asserting the 1.00 CURRENCY_SCALE fallback and no 'NaN', (c) a mode-stripped load asserting a visible error state -- plus the convention-(p) seed for (a) using the findBundleWithTemplate/patches machinery and the seed helper at :585.",
    "self_verifying": "YES"
  },
  {
    "id": "S2-C044",
    "verdict": "STILL_OPEN",
    "evidence": "The build-stamp block at frontend/scripts/dist_hygiene_gate.mjs:124-133 still carries only the two original checks: `check('the build stamp names a commit', /^[0-9a-f]{40}$/.test(info.commit || ''), String(info.commit))` at :126-127, and `check('the build stamp reconciles with the bundle on disk', ...)` at :128-132. Neither new assertion is present: `grep -n \"rev-parse\\|cleanTree\\|info.commit\" frontend/scripts/dist_hygiene_gate.mjs` returns only lines 118, 123 and 127 -- no `execSync('git rev-parse HEAD')` and no `info.cleanTree === true`. No new seeds were added either: the `seeded` array is still built at :322 from `SEEDS.map(...)` over the doc-extension list and emitted at :328-341, and the only other seed arrays are uriSeeded (:206), brandSeeded (:269) and touchSeeded (:302). The defect is live in the artefact, which is what makes the omission load-bearing: frontend/dist/build-info.json records `\"commit\": \"2fbeac120d690d009cdc46cd7412d33521fdfc02\"` while HEAD is 563c695 (df04d8c at the time the row was cut), so a HEAD-equality assertion would go red today and the shape-only check passes regardless. `cleanTree: true` is present in the JSON but nothing reads it.",
    "remaining": "Add the two checks beside dist_hygiene_gate.mjs:126-131 -- `info.commit === execSync('git rev-parse HEAD')` and `info.cleanTree === true` -- and seed both into the existing `seeded` array per convention (p), including the stale-commit negative control.",
    "self_verifying": "YES"
  }
]
