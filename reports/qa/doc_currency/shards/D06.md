# D06 quality-charter-and-methods: document currency shard

Scope: `docs/QUALITY_CHARTER.md` (511), `docs/skills/FULL_AUDIT_METHOD.md` (355),
`docs/skills/AGENT_BUDGET_AND_SCHEDULING.md` (477), `docs/RESKIN_BOUNDARY.md` (593),
`docs/HUD_SPEC.md` (74), `docs/RGS_CONTRACT_REFERENCE.md` (104),
`docs/TILE_COMPOSITION_GUIDE.md` (73), `docs/TOOLING_REVIEW.md` (119). 2306 lines.

| id | kind | doc_path:line | claim_quote | reality_quote | verdict | severity |
|---|---|---|---|---|---|---|
| D06-01 | STALE_NUMBER | docs/QUALITY_CHARTER.md:17 | "Quoted verbatim from `CLAUDE.md:483` to `CLAUDE.md:487`, which records it as the owner's order of 2026-07-26" | `sed -n '536,543p' CLAUDE.md`: `536:Recorded verbatim as given, and it outranks every convenience below it.` then `538:> this title is the studio's flagship and the template for every future build.` The mandate block is CLAUDE.md:538-543. `sed -n '480,506p' CLAUDE.md` shows 480-506 is convention (j) living-handover, (k) explicit-paths commits, (o) fresh-clone bundle and (p) seeded self-tests. A reader following 483-487 lands on handover prose. | STALE | MEDIUM |
| D06-02 | STALE_NUMBER | docs/QUALITY_CHARTER.md:29 | "**What it changes in practice**, restated from `CLAUDE.md:489` to `CLAUDE.md:493`" | Same drift as D06-01. `sed -n '498,506p' CLAUDE.md` returns convention (o)'s branding/`public/` paragraph, ending `Building from a clone makes that property structural`. The "minor is not a disposition" restatement is not at 489-493. | STALE | MEDIUM |
| D06-03 | STALE_NUMBER | docs/QUALITY_CHARTER.md:308 | "`CLAUDE.md:540` says it plainly: \"When a job turns out to contain a genuinely hard bounded problem, it is written up as its own surgical brief and handed back, rather than absorbed into the session that found it.\"" | `grep -n "genuinely hard bounded problem" CLAUDE.md` returns exactly one hit: `612:contain a genuinely hard bounded problem, it is written up as its own surgical`. CLAUDE.md:540 at HEAD reads `> minor-defer category, only fixed or explicitly owner-parked with reasons. The`, i.e. line 3 of the standing mandate. The quoted text is real and current; only the line pointer is wrong, and it points at a different rule. | STALE | MEDIUM |
| D06-04 | STALE_NUMBER | docs/QUALITY_CHARTER.md:180 (Q-19) | "**NOT A DEFECT**: the theme selector is dev-only and not rendered in production (`CLAUDE.md:345`)" | `sed -n '344,347p' CLAUDE.md` returns "of it. Two adoptions since have not fitted on either side of that line, and both were / accepted on their merits, so the rule is restated ...". The supporting statement is at CLAUDE.md:396-400: `396:## Theme selector` / `398:The theme selector is dev-only. In production (\`!import.meta.env.DEV\`) it is not rendered`. Disposition CURRENT, citation dead. | STALE | LOW |
| D06-05 | STALE_NUMBER | docs/QUALITY_CHARTER.md:179 (Q-18) | "all five modes are `available: true` (`frontend/src/lib/config/fsModes.ts:67,78,89,101,112`)" | `grep -n "available" frontend/src/lib/config/fsModes.ts`: `76:    available: true,` `86:    available: true,` `96:`, `106:`, `116:`. Five modes all `true`, so the substance is CURRENT; every one of the five line numbers is off by 9 or 10. | STALE | LOW |
| D06-06 | DEAD_REFERENCE | docs/QUALITY_CHARTER.md:200 (Q-26) | "Four more player-visible instances survive in `frontend/src/lib/config/fsModes.ts` blurbs and social blurbs: `1.6x`, `1.25x` twice, and `5x`." | `grep -n "1\.6x\|1\.25x\|5x" frontend/src/lib/config/fsModes.ts` returns NOTHING. The strings now live in `frontend/src/lib/i18n/prose.ts:90` "about 1.6x the feature trigger rate. Debits 1.25x every spin while ON.", `:94` "pre-revved to 5x", and the social siblings `:189`, `:192`. Note `frontend/src/lib/config/prose.ts` also does not exist; the file is under `lib/i18n/`. The DEFECT is open (KNOWN(Q-26)); the file citation a reader would open is dead, and that is not what the KNOWN row records. | STALE | MEDIUM |
| D06-07 | STALE_NUMBER | docs/QUALITY_CHARTER.md:198 (Q-24) | "**All eighteen `font-variant-numeric: tabular-nums` declarations in the codebase are inert**" | `grep -rn "tabular-nums" frontend/src \| wc -l` returns `19`. One more declaration than the row counts, so the row understates its own class by one. | STALE | LOW |
| D06-08 | STALE_NUMBER | docs/QUALITY_CHARTER.md:479 | "A fresh clone shows **ten entries under `games/`**, of which exactly one ships. The other nine are the upstream `math-sdk`'s sample games and template, and they ARE documented as such in `docs/math_docs/directory.md`" | `git ls-files games/ \| cut -d/ -f2 \| sort -u` returns exactly two lines: `README.md` and `future_spinner`. A fresh clone shows TWO entries. Corroborated by `WRS_MASTER_DOCUMENT.md:283` (2026-07-28 entry): "the nine upstream SDK sample packages removed from `games/` per ruling 4 (TR-088)". `ls games/` on this machine still shows 8 because 5 are untracked local dirs. The TR-088 finding this section records was actioned the next day and the section was never updated. | STALE | MEDIUM |
| D06-09 | SELF_CONTRADICTION | docs/QUALITY_CHARTER.md:479 vs :483 | :479 "A fresh clone shows **ten entries under `games/`** ... The other nine are the upstream `math-sdk`'s sample games and template"; :483 "`games/lumen`, `future_spinner_collect`, `future_spinner_bigwin`, `future_spinner_multiwild` and `future_spinner_rtp94` are all **untracked local directories** and cannot reach a clone, a kit or a reviewer." | Both cannot hold at once: five of the nine are stated four lines later to be unable to reach a clone, so the clone count cannot be ten. `git ls-files games/` settles it at two. Same shape as the known `COMPLIANCE_WATCH.md:434`/`:447` contradiction: a later paragraph refuting an earlier count in the same section. | STALE | MEDIUM |
| D06-10 | SUPERSEDED | docs/skills/FULL_AUDIT_METHOD.md:330 | Wave table: "**Social-mode capture** \| It forces English and swaps the whole vocabulary layer ... \| Never captured" | Committed social-mode frames exist at HEAD: `ls reports/screens/social-dom-conformance/` returns `autoplay-social.png`, `features-social.png`, `first-paint-social.png`, `paytable-social.png`, `session-social.png` (each with a `-real` counterpart), and `ls reports/screens/social-strings-item-c/` returns `bet-modes-social.png`, `feature-menu-social.png`. `docs/QUALITY_CHARTER.md:470` states the same wave as "**NEVER CAPTURED**" but qualifies it, "Absent from the 91-frame polish set", which is defensible; the FULL_AUDIT_METHOD cell carries no qualifier, so a reader cannot tell that five social frames are already committed. | STALE | LOW |
| D06-11 | STALE_NUMBER | docs/RGS_CONTRACT_REFERENCE.md:56 | "`rgsService._rgsSpinReal` calls `play()` then `endRound(params, roundId)`, and sets `newBalance` only after `endRound` completes (rgsService.ts:473, 536)" | `sed -n '473p;536p' frontend/src/lib/services/rgsService.ts`: 473 is `throw _makeRGSError('ERR_GEN', \`Non-JSON response from ${url} (${res.status})\`)`; 536 is inside `parseSessionParams` (`533:  _devLog('parseSessionParams', params)`, `534:  return params`). `grep -n` gives `774:async function _rgsSpinReal(req: SpinRequest)`, `806:    result.newBalance = authBalance   // carry authoritative balance to App.svelte`, `888:    // newBalance is set by _rgsSpinReal after endRound completes`. Behaviour claim CURRENT; both line pointers dead. LOCKED file: no edit proposed to `rgsService.ts`, the correction belongs in the doc. | STALE | MEDIUM |
| D06-12 | DEAD_REFERENCE | docs/TILE_COMPOSITION_GUIDE.md:12 | "The vector mark (`design-system/brand/vector_mark/wrs_mark_v2.svg` / `_mono_cyan.svg`, 512/192/96/48 PNG exports) is available if the tile format calls for a small corner badge" | `ls design-system/brand/vector_mark/` returns "No such file or directory". `ls design-system/brand/` returns `GENERATION_NOTE_background.md archive delivery hero_emblem hero_icon provider_mark tile`. `find design-system -iname "*wrs_mark*"` puts every asset under `design-system/brand/archive/vector_mark/`, which also holds `wrs_mark_v3.svg`, `wrs_mark_v3_mono_cyan.svg` and the full v3 PNG set. The guide is an owner-facing Tile Editor page pointing at a moved directory, for the older of two marks. | STALE | MEDIUM |
| D06-13 | DEAD_REFERENCE | docs/RESKIN_BOUNDARY.md:410 (GAP 2) | "`spinButton`, `btnMinus`, `btnPlus`, `btnAutoplay`, `btnMenu`, `panelBalance`, `panelWin`, `symbols`, `backgroundVideo`, `isVideo` are declared in `themeStore.ts` and read nowhere." | `themeStore.ts` no longer declares `backgroundVideo`. The file says so in place: "`backgroundVideo` REMOVED 2026-07-28 (asset reference gate, JOB 1). It / derived `${b}/backgrounds/bg-1.mp4` for every theme". The field list at HEAD (fields of the `themeAssets` derived object) is `id assetBase symbols background isVideo frame logo spinButton btnMinus btnPlus btnAutoplay btnMenu panelBalance panelWin sounds`. GAP 2's enumeration names a removed field and omits `sounds`. | STALE | LOW |
| D06-14 | SELF_CONTRADICTION | docs/RESKIN_BOUNDARY.md:409-413 (GAP 2) | ":409 **Thirteen of the fifteen derived `themeAssets` fields have zero consumers**" then ":413 Only `assetBase`, `background`, `logo` and `frame` are read." | 13 dead plus 4 read is 17, which exceeds the stated 15. The field list above counts 15 at HEAD, so at most 11 can be dead if 4 are read. The bullet also enumerates only 10 dead fields by name, not 13. Three numbers in five lines that do not reconcile; a planner sizing a reskin from GAP 2 gets a different answer depending on which sentence they trust. | STALE | LOW |
| D06-15 | STALE_NUMBER | docs/RESKIN_BOUNDARY.md:393 (GAP 1) | "**152 hex colours in `HudOverlay.svelte`, 68 in `FeatureMenu.svelte`, 62 in `PaytableModal.svelte`, 28 in `WinBanner.svelte`, 24 in `App.svelte`**" | `grep -o "#[0-9a-fA-F]\{3,8\}\b" <file> \| wc -l` returns HudOverlay **153**, FeatureMenu 68, PaytableModal 62, WinBanner 28, App.svelte 24. Four of five match exactly, which is strong evidence the instrument matches the document's own; HudOverlay has drifted up by one since the count. | STALE | LOW |
| D06-16 | STALE_NUMBER | docs/TOOLING_REVIEW.md:30-32 | "Math bundle: `games/future_spinner/library/publish_files/` - has `index.json` with modes `base` (cost 1.0) + `bonus` (cost 100.0), 3-column `lookUpTable_*_0.csv`, and `books_*.jsonl.zst` (bonus book ~151 MB uses the multipart path). Matches the contract." | `cat games/future_spinner/library/publish_files/index.json` lists FIVE modes: `"name": "base", "cost": 1.0`, `"name": "cruise", "cost": 1.0`, `"name": "antelite", "cost": 1.25`, plus super and bonus; `ls *.zst` returns five books (`books_antelite`, `books_base`, `books_bonus`, `books_cruise`, `books_super`). `CLAUDE.md:300` confirms "**RTP: 96.3500% at 4dp in ALL FIVE modes**". The review is dated "Reviewed 2026-07-04" (line 8) but the mode enumeration is presented as a live fit statement under "**Our fit (confirmed against our tree):**", and a reader preparing a submission upload would take away two modes where five ship. The `~151 MB` bonus-book figure is still exact (151,905,143 bytes). | STALE | MEDIUM |
| D06-17 | UNKNOWN | docs/skills/FULL_AUDIT_METHOD.md:327,333 | Wave table: "**Audio** ... **A scored axis with zero coverage.** \| Never swept"; "**Animation quality and timing** ... \| Never examined" | Could not establish either way. `ls reports/qa/` shows `audio_verify_2026-07-13.json` and `audio_verify_2026-07-27.json`, and `ls reports/audio/` shows `GENERATION_LOG_2026-07-13.md`, so audio artefacts exist; but the claim is specifically about loudness consistency between rows, tails and the bed swap heard in context, and I did not open the two JSON files to test whether they measure those. Establishing "never examined" requires proving a negative across `reports/` and I did not attempt it within budget. Recording as UNKNOWN rather than guessing, per the wave rule. | UNKNOWN | LOW |
| D06-18 | VERIFIED_CURRENT | docs/HUD_SPEC.md:33-41 | Locked coordinate table, e.g. "TURBO \| `.fs-turbo` \| 227 \| 563 \| 82 \| 82" and "SPIN \| `.fs-spin` \| 1027 \| 562 \| 84 \| 84" | `grep -n` on `frontend/src/lib/components/HudOverlay.svelte`: `1472:  .fs-turbo{position:absolute;left:227px;top:563px;width:82px;height:82px;z-index:60;`, `1524:  .fs-spin{position:absolute;left:1027px;top:562px;width:84px;height:84px;z-index:61;`, `1411:  .fs-max{position:absolute;left:325px;top:580px;width:48px;height:48px;`, `1312:  .fs-balance{left:449px;width:200px;`, `1313:  .fs-win    {left:665px;width:150px;`, `1314:  .fs-bet    {left:831px;width:120px;`, `1386:  .fs-arrows{position:absolute;left:967px;top:578px;width:44px;height:52px;`. Seven of the nine rows verified exact; the CSS at `1426` even names the lock, "(locked spec, docs/HUD_SPEC.md)". The tightest document in scope. | CURRENT | LOW |
| D06-19 | VERIFIED_CURRENT | docs/HUD_SPEC.md:70 | "`frontend/scripts/hud_banner_spec_check.mjs` re-measures every control above via a live headless-browser render" | `ls frontend/scripts/hud_banner_spec_check.mjs` returns the path. NOT EXECUTED: it is a `_check.mjs` writer and this pass is read-only. Existence verified, behaviour not. | CURRENT | LOW |
| D06-20 | VERIFIED_CURRENT | docs/TILE_COMPOSITION_GUIDE.md:8-10 | "`tile_background_master.jpg` \| 2048x1152", "`tile_hero_full.png` \| 4159x1875, transparent", "`tile_hero_pilot_only.png` \| 854x1875" | `sips -g pixelWidth -g pixelHeight` on all three under `design-system/brand/tile/`: 2048x1152, 4159x1875, 854x1875. Exact on all three. `GENERATION_NOTE_background.md`, cited at :50 and :61, also exists in that directory. | CURRENT | LOW |
| D06-21 | VERIFIED_CURRENT | docs/QUALITY_CHARTER.md:201 (Q-27) | "Still at HEAD in `frontend/src/app.css`: `color-scheme: light dark`, `background-color: #242424`, `a { color: #646cff }`, `a:hover { #535bf2 }`, and `body { display: flex; place-items: center }`" | `grep -n` on `frontend/src/app.css`: `103:  color-scheme: light dark;`, `105:  background-color: #242424;`, `115:  color: #646cff;`, `119:  color: #535bf2;`, `125:  place-items: center;`. All five present. The row remains under-stated relative to KNOWN(Q-27), which adds `162:  outline: 4px auto -webkit-focus-ring-color;` and `143:  text-align: center;`; the charter row itself does not name those two. | CURRENT | LOW |
| D06-22 | VERIFIED_CURRENT | docs/QUALITY_CHARTER.md:184 (Q-21) | "`translations.ts` `locationRestricted` exists in all sixteen locales and is rendered by no component" | `grep -c "locationRestricted" frontend/src/lib/i18n/translations.ts` returns `17` (sixteen values plus the key declaration); `grep -rn "locationRestricted" frontend/src \| grep -v translations.ts` returns nothing. Both halves hold. | CURRENT | LOW |
| D06-23 | VERIFIED_CURRENT | docs/QUALITY_CHARTER.md:466-476, :481, :494-499 | The 5.3 gate names and section 6's "`node scripts/machine_tell_gate.mjs`" | `ls` confirms all seven cited scripts exist under `frontend/scripts/`: `machine_tell_gate.mjs`, `locale_completeness_check.mjs`, `hud_naming_uniformity_check.mjs`, `a11y_social_terms_check.mjs`, `contrast_gate.mjs`, `reel_v3_proof.mjs`, `dist_hygiene_gate.mjs`. `docs/math_docs/directory.md` also exists as cited at :481. None executed. | CURRENT | LOW |
| D06-24 | VERIFIED_CURRENT | docs/QUALITY_CHARTER.md:141 (4.1) | "`🏆` \| 17 \| `wincap` in all sixteen locales plus the social override \| YES, via `WinDisplay.svelte:79`" | `sed -n '79p' frontend/src/lib/components/WinDisplay.svelte` returns `      <div class="win-label wincap">{$tr('wincap')}</div>`. The render-site citation still resolves to the exact line, in a table explicitly dated to HEAD `3f0d686`. Spot-checked because 4.1 is the section a platform reviewer would most likely quote. | CURRENT | LOW |
| D06-25 | VERIFIED_CURRENT | docs/RGS_CONTRACT_REFERENCE.md:24, :44 | "`{ gameID, minBet, maxBet, stepBet, defaultBetLevel, betLevels[], betModes, jurisdiction{...} }`" and "`betLevels` is a micros array" | FRESH capture `docs/stake-engine-live/2026-07-29/rgs.md:288-311`: "The bet must fall between minBet and maxBet (returned from /wallet/authenticate).", "The bet must be divisible by stepBet.", `"minBet": 100000,` `"maxBet": 1000000000,` `"stepBet": 10000,` `"betLevels": [`, `1000000000 // $1000`. Field names and micro-unit encoding agree with the 2026-07-04 reference. NOTE an upstream, not ours: the sibling capture `approval_guidelines_rgs_communication.md` says "Bet increments must reflect allowed values within authenticate/config/minStep", a name the wire page never uses. | CURRENT | LOW |
| D06-26 | VERIFIED_CURRENT | docs/RGS_CONTRACT_REFERENCE.md:22, :4 | "`/bet/event` \| POST \| `{ sessionID, event }` \| `{ event }` (echo)"; reference cross-checked against the endpoint set | `docs/stake-engine-live/2026-07-29/rgs_wallet.md:121` `POST /bet/event`. `docs/stake-engine-live/2026-07-29/rgs.md:35` "All flows require the /wallet/authenticate API to be called when the game first loads. This authorizes the sessionID to be used by the /wallet/play, /wallet/balance and /wallet/end-round endpoints." All five wallet/bet endpoints in the reference table are still present in the fresh capture. | CURRENT | LOW |
| D06-27 | VERIFIED_CURRENT | docs/RGS_CONTRACT_REFERENCE.md:29, :83, :86 | "replay uses `GET {rgs_url}/bet/replay/{game}/{version}/{mode}/{event}` in `replayService.ts`"; "(flagged in `REPLAY_TEST_EVENTS.md`)"; "see `reports/archive/superseded/MATH_VALIDATION.md` / `scripts/validate_math.py`" | `ls` resolves every one: `frontend/src/lib/services/replayService.ts`, `frontend/src/lib/components/ReplayMode.svelte`, `reports/archive/superseded/MATH_VALIDATION.md`, `scripts/validate_math.py`, and `find . -name REPLAY_TEST_EVENTS.md` returns `./REPLAY_TEST_EVENTS.md` (repo root; the doc cites it unqualified but it resolves). | CURRENT | LOW |
| D06-28 | VERIFIED_CURRENT | docs/RESKIN_BOUNDARY.md:557-560 (GAP 12) | "`frontend/public/assets/symbols/README.md` states \"Recommended size: 200x200 px, transparent PNG\" and lists filenames as `H1.png` through `S.png`. The shipped pipeline exports **240x240 and 120x120 under lower-case names**." | `frontend/public/assets/symbols/README.md:18` "Recommended size: 200×200 px, transparent PNG." and `:7` "| H1.png       | Spinning Rim    |". Shipped: `ls frontend/public/assets/themes/future-spinner/symbols/` returns `h1.png h1_1x.png h1_base.png h1_spin.png h2.png h2_1x.png l1.png ...` all lower case, and `sips` on `h1.png` returns 240x240. The gap is exactly as described, both halves. | CURRENT | LOW |
| D06-29 | VERIFIED_CURRENT | docs/RESKIN_BOUNDARY.md:455-461 (GAP 5) | "`themes.ts`: `DEFAULT_THEME_ID = 'future-spinner'`, forced unconditionally in production"; "`frontend/package.json`: `\"name\": \"future-spinner-frontend\"`"; "localStorage keys `wrs_theme` and `fs_muted`" | `frontend/src/lib/config/themes.ts:117:export const DEFAULT_THEME_ID = 'future-spinner'`, `:125-126` `? localStorage.getItem('wrs_theme') ?? DEFAULT_THEME_ID` / `: DEFAULT_THEME_ID`; `frontend/src/App.svelte:33:    switchTheme(DEFAULT_THEME_ID)`; `frontend/package.json:2:  "name": "future-spinner-frontend",`; `wrs_theme` at `themes.ts:125,132` and `themeStore.ts:15,23`. Four checkable atoms of GAP 5 all hold. The "nine theme-identity branches" count itself was NOT verified: counting branches reliably needs the whole of `App.svelte` and I did not spend it. | CURRENT | LOW |
| D06-30 | VERIFIED_CURRENT | docs/RESKIN_BOUNDARY.md:414 (GAP 2) | "153 MB of alternate-theme art sits in `public/` and is pruned from `dist` at build." | `du -sm frontend/public/assets/themes` returns `152	frontend/public/assets/themes`. 152 against 153 is inside the MiB-versus-MB and block-rounding margin, so I am calling the figure current rather than manufacturing a one-unit finding. The pruning half is corroborated by this wave's own recorded false finding: `vite.config.ts` carries a `pruneLegacyAssets` step. | CURRENT | LOW |
| D06-31 | VERIFIED_CURRENT | docs/RESKIN_BOUNDARY.md:396 (GAP 1) | "Compounded by `docs/design/CHROME_PRIMITIVES.md`, which ratifies the duplication as correct." | `ls docs/design/CHROME_PRIMITIVES.md` returns the path. Existence verified; I did not open it to test that it ratifies the duplication, so only the reference is confirmed. | CURRENT | LOW |
| D06-32 | VERIFIED_CURRENT | docs/TOOLING_REVIEW.md:43, :86-90, :100 | "**Action taken:** `.github/workflows/publish-stake-engine.yml` drafted"; "It will confirm base 96.35% / bonus 96.35% (cost-adjusted), base hit-rate 29.11% inside 5-33%, 100k rows at the boundary, and max win 5000x"; "our stated \"volatility 17.28x\"" | `ls .github/workflows/` returns `checks.yml`, `publish-stake-engine.yml`, `validate-math.yml`. `CLAUDE.md:300` "**RTP: 96.3500% at 4dp in ALL FIVE modes**", `:304` "**Base mode:** hit rate 29.11%, volatility (weighted SD) 17.28x", `:306` "**Bonus mode:** average bought outcome 96.35x (RTP 96.35% at 100x)". Every quoted maths figure matches the project's own True-game-facts block; `frontend/src/lib/mock/sample_rounds.json` cited at :69 also exists. | CURRENT | LOW |
| D06-33 | VERIFIED_CURRENT | docs/skills/FULL_AUDIT_METHOD.md:9, :322 | "Cross-referenced from `WRS_MASTER_DOCUMENT.md` section 7b"; "**Wave 1, COMPLETE for Future Spinner: the machine-tell sweep** ... Recorded in `docs/QUALITY_CHARTER.md` with a live CI gate." | `find . -name WRS_MASTER_DOCUMENT.md` returns `./WRS_MASTER_DOCUMENT.md`, and `grep -n "7b" WRS_MASTER_DOCUMENT.md` returns `239:### 7b. Next-title template (applies to LUMEN, queued after Future Spinner submits)`, plus `:283` which independently records FULL_AUDIT_METHOD's creation and its wave-2-to-5 list. `docs/QUALITY_CHARTER.md` exists and `frontend/scripts/machine_tell_gate.mjs` exists. Mutual citation intact in both directions. | CURRENT | LOW |
| D06-34 | HISTORICAL_OK | docs/skills/AGENT_BUDGET_AND_SCHEDULING.md:31-42 | "## 1. The measured base, from this session ... Three runs completed and reported usage. These are cited, not estimated." then the 36-agent / 6,601,691-token table | The section names itself a measurement of one past session and the numbers are agent-run telemetry, not tree state, so there is nothing in the tree that could make them stale. Not flagged. Its two path citations resolve: `FULL_AUDIT_METHOD.md` (:12) and `CLAUDE.md` (:114) both exist. Recorded so the marshal has the denominator: this document's factual load is almost entirely historical measurement, which is why it contributes no STALE rows. | HISTORICAL_OK | LOW |

## NOTES

Cross-referenced against `reports/qa/stream_test/KNOWN_OPEN.md` before opening anything.
Q-26 and Q-27 both appear there; my rows about them (D06-06, D06-07, D06-21) are about the
CHARTER TEXT's currency, not fresh discoveries of the underlying defects, and D06-06 is
called out as distinct from KNOWN(Q-26) because the KNOWN row records an incomplete
enumeration, not a moved file. The `COMPLIANCE_WATCH.md:434`/`:447` contradiction was not
re-reported; D06-09 is a second, independent instance of the same shape.

Two judgement calls, stated so the marshal can overrule them:
- D06-30: I measured 152 MB against a documented 153 MB and called it CURRENT rather than
  open a one-unit finding, because `du -sm` and MB/MiB conventions differ by about that much.
- D06-10: `QUALITY_CHARTER.md:470` makes the same "never captured" claim but qualifies it
  ("Absent from the 91-frame polish set"), which is defensible; only the unqualified
  FULL_AUDIT_METHOD cell is flagged, per the historical-document rule.

The dominant pattern in this scope is not wrong facts, it is **dead line pointers**: seven of
the sixteen stale rows are a correct statement attached to a line number that has drifted
(D06-01 through D06-05, D06-11, and the citation half of D06-06). `CLAUDE.md` has grown to
1011 lines and every charter cross-reference into it that I sampled was wrong, all in the
same direction (the charter's numbers are about 55 to 72 lines low). Nothing in scope pins a
commit hash to those citations, so they cannot self-date.

doc_lines_in_scope: 2306
claims_sampled: 45
claims_stale: 16
claims_current: 28
claims_unknown: 1
sampling_basis: I sampled the claims a reader would ACT on, not prose. Priority order was (1) every cross-reference into `CLAUDE.md`, because the charter's authority rests on them, (2) the section-4 Q rows whose disposition is OPEN or PARKED, because a later pass will pick those up as work, (3) every `file:line` and `path` a reader would open, (4) counts a submission document could quote to the platform (RTP, hit rate, mode list, dimensions, hex counts, MB figures), (5) `RGS_CONTRACT_REFERENCE.md` against the 2026-07-29 fresh capture as instructed. I did NOT check the Valkyrie benchmark prose (section 2), the 4.3 parked string list line by line (about 75 citations, the single largest unchecked block in scope), `AGENT_BUDGET_AND_SCHEDULING.md` sections 2 to 12, or `RESKIN_BOUNDARY.md` sections 1 to 3 and GAPs 3, 4, 6 to 11. That is roughly 45 of about 340 factual claims in scope, so this is a bounded pass and the marshal should read the absence of a row as unchecked, not as clean.
scripts_executed: NONE
tree_clean_after: `?? reports/qa/compliance_register/walk_shards/` `?? reports/qa/doc_currency/` `?? reports/qa/file_census/` — three untracked directories, no tracked file modified or deleted. `reports/qa/doc_currency/` is my own shard's parent, expected. The other two are sibling squads' shard directories, not written by me.
