# D08: session report and the stream test ledgers

Scope: `reports/SESSION_REPORT.md` (7490 lines, append only) with attention on the MOST
RECENT session section and its `FOR THE NEXT SESSION` block, lines 7216 to 7490, which is
the block the session running now was booted from; plus `reports/qa/stream_test/KNOWN_OPEN.md`
(61 lines), `CLUSTERS.md` (248 lines), `shards/SHARD_INDEX.md` and spot checks on
`LEDGER.md`'s own counts.

| id | kind | doc_path:line | claim_quote | reality_quote | verdict | severity |
|---|---|---|---|---|---|---|
| D08-02 | SELF_CONTRADICTION | reports/qa/stream_test/CLUSTERS.md:224 | `| **OWNER-PARKED, verified, diagnosis corrected by panel** | 18 | C-01, C-02, C-04 to C-09, C-13, C-15 to C-19, C-21, C-22, C-24 |` | The enumeration holds SEVENTEEN clusters, not eighteen: C-01, C-02, C-04, C-05, C-06, C-07, C-08, C-09, C-13, C-15, C-16, C-17, C-18, C-19, C-21, C-22, C-24. With the other two rows of the same table, `| **CLOSED, not a defect** | 1 | C-20 |` and `| **REOPENED by the ruled safeguard, handed to Session 2** | 7 | C-03, C-10, C-11, C-14, C-23, C-25, C-26 |`, the table accounts for 25 of the 26 clusters the same file declares at `:31` (`Twenty six.`). **C-12 appears in no disposition row**, though the file analyses it at `:198` and `:208-216` and the session report schedules it at `SESSION_REPORT.md:7361`. | STALE | HIGH |
| D08-03 | SELF_CONTRADICTION | reports/SESSION_REPORT.md:7349-7350 | `**The seven REOPENED clusters** (C-03, C-10, C-11, C-14, C-23, C-25, C-26). Six carry a known marshalling fault with the fault named` | `CLUSTERS.md:195-202` names marshalling faults for exactly six clusters, and the six are C-03, C-10, C-11, **C-12**, C-23, C-26. Only FIVE of them are among the reopened seven. C-12 is not reopened (and is dispositioned nowhere, per D08-02); C-14 and C-25 are reopened with no fault named in that table or anywhere else in the file. So five of the seven carry a named fault. | STALE | HIGH |
| D08-19 | SUPERSEDED | reports/qa/stream_test/shards/SHARD_INDEX.md:9-11 | `## Status: discovery COMPLETE, verification NOT RUN` / `**540 findings, none of them verified.** The adversarial pass did not run` | Verification has since run and is recorded two files away. `CLUSTERS.md:220` heads `## FINAL DISPOSITION of the STREAM tier` and dispositions 26 clusters; `SESSION_REPORT.md:7264` heads `## JOB 3: 52 adversarial seats over 26 clusters`; `KNOWN_OPEN.md:45-46` reads `**26 STREAM clusters are dispositioned**`. The index attributes itself to `the recovery session (2026-07-29, ...FS_STREAM_TEST_RECOVERY_Prompt.md)` at `:3-5`, but the superseding session ran the SAME DAY under a different brief, and the section is headed `Status:` with no date, so a reader cannot tell it is a snapshot. | STALE | HIGH |
| D08-01 | SELF_CONTRADICTION | reports/qa/stream_test/CLUSTERS.md:13 | `**571 active findings: 60 STREAM, 183 HIGH, 242 MEDIUM, 81 LOW.**` | The four tier figures sum to 566, not 571 (`python3 -c "print(60+183+242+81)"` returns `566`). `KNOWN_OPEN.md:42-43` reads `it holds 571 findings of which 506 are HIGH, MEDIUM or LOW`, and 183+242+81 = 506 exactly, so both documents agree on the HIGH/MEDIUM/LOW legs and the STREAM leg is what does not reconcile: 571 minus 506 is 65 against the 60 stated here. | STALE | MEDIUM |
| D08-04 | SELF_CONTRADICTION | reports/SESSION_REPORT.md:7352 | `because two of the seven exist only because the tier filter hid their corroboration` | `CLUSTERS.md:205-206` names the tier-filter pair as `**C-12 and C-23 are the important pair, because they are a METHOD fault rather than a slip.**` C-12 is not one of the seven reopened clusters listed three lines earlier at `SESSION_REPORT.md:7349`, so exactly ONE of the seven (C-23) is a tier-filter case. | STALE | MEDIUM |
| D08-17 | STALE_NUMBER | reports/qa/stream_test/CLUSTERS.md:7 | `Marshalled in the main loop at grep level, 2026-07-29, over 55 active shards.` | 54 shard files exist. `ls -1 reports/qa/stream_test/shards/*.md | wc -l` returns `55`, but one of those 55 is `SHARD_INDEX.md`, which is the index rather than a shard. Counted by lens prefix: STC 20, STT 20, STM 8, STL 4, STV 2 (`STV.md` and `STV-REST.md`) = 54. | STALE | MEDIUM |
| D08-20 | STALE_NUMBER | reports/qa/stream_test/KNOWN_OPEN.md:20 | `The four prose and config instances (`1.6x`, `1.25x` twice, `5x`, now in `prose.ts`) REMAIN OPEN.` | `frontend/src/lib/i18n/prose.ts` holds SIX, each literal twice, in two prose blocks: `:90` `modeOverboostBlurb: 'Double-chance: about 1.6x the feature trigger rate. Debits 1.25x every spin while ON.',` and `:189` `modeOverboostBlurb: 'Double-chance: about 1.6x the feature trigger rate. Costs 1.25x every spin while ON.',`, plus `:94` and `:192` each carrying `pre-revved to 5x`. `grep -c` on `frontend/src/lib/i18n/prose.locales.ts` returns `15` matching lines, one per non-English locale, e.g. `:120` `modeOverboostBlurb: 'Doppelte Chance: ca. 1.6x höhere Auslöserate. Zieht 1.25x pro Drehung ab, solange AN.',`. `grep -rln` over `frontend/src` also returns `App.svelte`, `betMode.ts`, `HudOverlay.svelte`, `FeatureMenu.svelte`, `PaytableModal.svelte` and `BetSelector.svelte`, which I did NOT open and do not claim are player-visible instances. | STALE | MEDIUM |
| D08-07 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7429-7430 | `**MID-02**, the ASCII `x` on the multiplier, now U+00D7.` and `**TR-104's remaining half**, the unit now routed through `t($locale, 'bet', ...)`` | Both land in `frontend/src/lib/components/WinBanner.svelte` at HEAD. `:211` reads `$: multLabel = ` then a template literal `${Math.round(shownMultiplier)}×`; `:221` reads `$: multUnitLabel = t($locale, 'bet', $isSocial ? 'social' : 'real')`. | CURRENT | MEDIUM |
| D08-09 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7353-7354 | `The sound fix is `GameGrid.svelte:499` `DROP_H` to at most 208, or widen `STRIP`` | `frontend/src/lib/components/GameGrid.svelte:499` reads `      const DROP_H = 520`. Consistent with `KNOWN_OPEN.md:51` (`**C-01's real cause is `GameGrid.svelte:499`, `DROP_H = 520`**`), and 520 minus 208 is 312, the `312 of 412 pixels` that same line states. | CURRENT | MEDIUM |
| D08-13 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7458-7459 | `The script now asks git whether anything at the destination is tracked and refuses if so, requiring an explicit `--regenerate`.` | `frontend/scripts/stream_test_capture.mjs:496` reads `    if (tracked && !process.argv.includes('--regenerate')) {`, and `:76` reads `const DATE = process.env.STREAM_TEST_DATE || (() => {`, so the hardcoded `'2026-07-28'` the same paragraph describes is gone. Read as text only; the script was NOT executed. | CURRENT | MEDIUM |
| D08-05 | STALE_NUMBER | reports/qa/stream_test/LEDGER.md:132 | `**540 findings: 43 STREAM, 160 HIGH, 228 MEDIUM, 106 LOW.** Zero squads lost.` | The tier figures sum to 537, not 540. The 540 total is independently correct: the lens table immediately above at `:124-130` sums 236+163+77+42+22 = 540. So the total holds and the tier split is three findings short. | STALE | LOW |
| D08-18 | STALE_NUMBER | reports/qa/stream_test/shards/SHARD_INDEX.md:10 | `47 shards. Every one of the 519 frames is read by at least one lens.` | 46 by two independent routes. `LEDGER.md:122` reads `46 shards, 43 of them written by this session's squads`, and its lens table sums to the same 46 (16+16+8+4+2). Independently from disk: 54 active shards (D08-17) minus the 28 re-run shards minus nothing plus the 20 superseded gives 46, where the 28 is itself countable as the thirds-per-squad set (STC `-1/-2/-3` x 4 viewports = 12, STT the same = 12, plus STM-MOBILEL, STM-MOBILEM, STM-MOBILES, STM-POPOUTS = 4). The likely mechanism, worth naming because it propagated: 46 plus `SHARD_INDEX.md` itself is 47, and 47 minus 20 plus 28 is the 55 at `CLUSTERS.md:7`. | STALE | LOW |
| D08-06 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7434-7435 | `Proof at `reports/screens/winbanner-fix-2026-07-29/`.` | `ls -1` on that directory returns 5 entries: `README.md`, `after_de_015_bigwin_settled.png`, `after_en_015_desktop_bigwin_settled.png`, `before_de_430_bigwin_settled.png`, `before_en_015_desktop_bigwin_settled.png`. Both before and after halves on both locales the claim names. | CURRENT | LOW |
| D08-08 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7435 | `Tracker TR-117 FIXED.` | `docs/records/reviews/REVIEW_TRACKER.md:298` is the `TR-117` row and its status cell reads `**FIXED 2026-07-29**, both halves, re-proven from fresh frames`. | CURRENT | LOW |
| D08-10 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7356 | `at 0.88 alpha (`:1259`) even a fully painted board passes 12 per cent of the scene through every cell` | `frontend/src/lib/components/GameGrid.svelte:1259` reads `    background: rgba(28, 29, 46, 0.88);`. Exact line, exact alpha. | CURRENT | LOW |
| D08-11 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7341-7342 | `` `reports/qa/stream_test/` (LEDGER, CLUSTERS, SHARD_INDEX, 28 new shards, 20 superseded moved aside) `` | `ls -1 reports/qa/stream_test/shards/superseded/ | wc -l` returns `20`. The 28 re-run shards are countable on disk as the thirds-per-squad set (see D08-18). `LEDGER.md`, `CLUSTERS.md` and `shards/SHARD_INDEX.md` all exist. Only the derived 55 in `CLUSTERS.md` is off (D08-17); this list itself checks out. | CURRENT | LOW |
| D08-12 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7446 and :7460 | `overwrote all 519 frames` ... `it goes RED and names the 521 tracked files` | `find reports/screens/stream-test-2026-07-28 -name '*.png' | wc -l` returns `519`; total tracked files `521`, the extra two being `README.md` and `MANIFEST.json`. Both figures in the report are right and describe different populations, which is why they differ. | CURRENT | LOW |
| D08-14 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7409-7410 | `the 207 upscaled frames were written to the gitignored `.evidence-scratch/`` | `.gitignore:54` reads `.evidence-scratch/`. The 207 count itself is UNKNOWN, since the directory is gitignored and absent from a clean tree by design. | CURRENT | LOW |
| D08-15 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7343 | `` `docs/skills/AGENT_BUDGET_AND_SCHEDULING.md` (4.3, 4.4) `` | That file carries `## 4.3 The MEASURE multiplier, and why verification always costs more than the estimate` at `:195` and `## 4.4 Grep-level clustering is provisional until a panel tests it` at `:226`. Both sections exist and both match the use the report makes of them. | CURRENT | LOW |
| D08-21 | VERIFIED_CURRENT | reports/qa/stream_test/CLUSTERS.md:210 | `` `prose.ts:72` defines `ProseStrings` as a total `Record<ProseKey, string>` `` | `frontend/src/lib/i18n/prose.ts:72` reads `export type ProseStrings = Record<ProseKey, string>`. Exact line, exact shape. The document names the file without its directory, and it resolves to `frontend/src/lib/i18n/prose.ts`; `frontend/src/lib/config/prose.ts` does not exist, so a reader following the path literally would miss it. Not filed as a dead reference: the file is unambiguous by name. | CURRENT | LOW |
| D08-22 | VERIFIED_CURRENT | reports/qa/stream_test/KNOWN_OPEN.md:21 | `` `app.css:160-162` styles `button:focus` with Chrome's own `-webkit-focus-ring-color` `` | `frontend/src/app.css:160-162` reads `button:focus,` / `button:focus-visible {` / `  outline: 4px auto -webkit-focus-ring-color;`. The line range is exact. The `rgb(0, 95, 204)` in the same claim is the browser's own resolved value and is not in the file, as the claim implies. | CURRENT | LOW |
| D08-23 | VERIFIED_CURRENT | reports/qa/stream_test/KNOWN_OPEN.md:26 | `` `frontend/index.html:2` is hardcoded `lang="en"` `` | `frontend/index.html:2` reads `<html lang="en">`. | CURRENT | LOW |
| D08-24 | VERIFIED_CURRENT | reports/qa/stream_test/KNOWN_OPEN.md:7 | `and `docs/QUALITY_CHARTER.md` section 4 at HEAD `d9bdf22`.` | `git log --oneline -1 d9bdf22` returns `d9bdf22 docs(brief): FS_STREAM_TEST saved verbatim per convention (f)`. The SHA resolves. | CURRENT | LOW |
| D08-25 | VERIFIED_CURRENT | reports/qa/stream_test/KNOWN_OPEN.md:25 | `is FIXED (per-digit 0.834em boxes) and held by `win_countup_steady_gate.mjs`` | `frontend/scripts/win_countup_steady_gate.mjs` exists, 10,382 bytes. The gate's file presence is what I checked; I did not run it and did not read its assertions, so the `0.834em` half of the claim is UNVERIFIED here. | CURRENT | LOW |
| D08-16 | UNKNOWN | reports/SESSION_REPORT.md:7380-7382 | `ran remote CI as run **30393688459** and it is **green**: https://github.com/JTOSHIE/stake-game-development-claude/actions/runs/30393688459` | UNKNOWN. Establishing a GitHub Actions run status needs a network fetch or `gh run view`, both outside a read-only local pass. Local corroboration only: `git log --oneline` confirms `57fb01e docs(session): Session 1, the stream close, graded against its Plan of Record`, so the commit the run is attributed to is real. Same position for run `30398189558` at `:7485-7486` and its commit `9602728 fix(gate): the social vocabulary guard pinned a mechanism, not its guarantee`, which also resolves, and for the two chained runs at `:7384-7386`. | UNKNOWN | LOW |
| D08-26 | UNKNOWN | reports/qa/stream_test/CLUSTERS.md:38 | `| **C-02** | ... | **8** | STC-LAPTOP-B, STC-MOBILES-3, STC-POPOUTL-B, STC-POPOUTS-3, STT-LAPTOP-B, STT-POPOUTS-2, STT-POPOUTS-3 |` | The instance count is 8 and SEVEN squads are listed. Every other row I read has the two equal (C-01 12 and 12, C-03 4 and 4, C-04 3 and 3, C-26 2 and 2). UNKNOWN rather than STALE: a single squad reporting the same defect twice would make 8 instances over 7 squads legitimate, and settling it needs the seven shards opened, which I did not do. Flagged because C-02 is an OWNER-PARKED cluster whose weight is its corroboration, and because `CLUSTERS.md:197-200` already records two instance-count faults of exactly this kind (C-10 double-counted one image, C-11 counted a retraction as a corroboration). | UNKNOWN | LOW |

## NOTES

**D08-02, D08-03 and D08-04 are one fault seen three times, and it is the load bearing one.**
`CLUSTERS.md`'s FINAL DISPOSITION table is the artefact Session 2 was told to work from, and
C-12 falls straight through it. C-12 has a named marshalling fault, it has a documented fix
that does not compile, it is named in the session report's own pick-up list at `:7361`
(`**C-12's fix must land key and copy together.** It cannot be staged.`), and it appears in
none of the three disposition rows. The `18` against seventeen enumerated clusters is the
tell, and it propagates into the handover as two wrong sentences about the reopened seven. A
reader following the handover would re-cluster six clusters expecting named faults and find
five, and would look for two tier-filter cases among the seven and find one.

**The 47/55/54 chain (D08-17, D08-18) is a counting error that travelled.** `SHARD_INDEX.md`
counted the index file as a shard, giving 47 where the population is 46; the marshal then
carried the same off-by-one forward as 55 where 54 shards exist. Nothing downstream of it is
wrong by more than one, and the finding is worth a line only because the shard count is how
the marshal's coverage claim is checked.

**What is legitimately historical and therefore NOT flagged.** Everything above
`SESSION_REPORT.md:7216` is prior sessions' appended history per conventions (a) and (i),
and was sampled only where the current block depends on it. `REVIEW_TRACKER.md:298`'s line
citations (`WinBanner.svelte:205`, `:210`) no longer point at the code they describe, because
the fix recorded in the same row shifted the file; the row is an explicitly closed finding
dated at discovery, so it is HISTORICAL_OK and is not filed as a dead reference.
`LEDGER.md:122-132` is scoped to the recovery session's 46 shards while `CLUSTERS.md` is
scoped to the post-re-run 54, so 540 findings against 571 is two populations rather than a
contradiction, and only the internal tier arithmetic (D08-05) fails. `SHARD_INDEX.md` is the
one document in scope that I did flag as presented-as-current, and the reason is stated in
D08-19: it is headed `Status:` with no date, and its own attribution names a same-day session
that a reader would not distinguish from the one that superseded it.

**On the KNOWN_OPEN cross reference.** No row above duplicates a `KNOWN_OPEN.md` row. D08-01
uses `KNOWN_OPEN.md:42-43` as the corroborating side rather than the defect, because the 506
there reconciles exactly and the gap sits in `CLUSTERS.md`. D08-20 is adjacent to
**KNOWN(Q-26)** but is not that row: Q-26 is the glyph defect, and the finding here is that
the refreshed row's own remaining-work count of four is wrong by the same kind of incomplete
enumeration Q-26 exists to record, six in `prose.ts` alone before the fifteen locale siblings.
The `COMPLIANCE_WATCH.md:434`/`:447` self-contradiction named in the brief was not
re-reported, and no second instance of that shape was found in this scope; the closest is
D08-19, which is a superseded status rather than two live claims in one file.

doc_lines_in_scope: 7490 in `SESSION_REPORT.md`, of which 275 (7216 to 7490) read closely and the rest read only as a section-header map; plus 61 (`KNOWN_OPEN.md`) and 248 (`CLUSTERS.md`) read in full or near full; plus about 25 lines of `SHARD_INDEX.md` and about 40 of `LEDGER.md`
claims_sampled: 31
claims_stale: 9
claims_current: 20
claims_unknown: 2
sampling_basis: Every claim in the current FOR THE NEXT SESSION block and its ADDENDUM that names a path, a count, a line number or a closure a Session 2 reader would act on, taken first; then every arithmetic self-consistency check available inside the two ledgers and the shard index, because those are free and this project has twice been bitten by an enumeration nobody added up; prose, rationale, run-time tables and the pre-7216 history were sampled only where the current block depends on them. Rows are ordered by severity, not by id.
scripts_executed: NONE
tree_clean_after: ` M reports/qa/doc_currency/shards/D05.md` and ` M reports/qa/doc_currency/shards/D08.md`. Two notes, because the shape is not the expected one. (1) `reports/qa/doc_currency/` was UNTRACKED (`??`) when this squad opened, so the directory was committed by another party mid-wave and my own shard therefore reads as MODIFIED rather than untracked. (2) `D05.md` is another squad's shard and was not touched by this one; I wrote exactly one file, `reports/qa/doc_currency/shards/D08.md`. Nothing else in the tree is dirty: no source, no ledger, no evidence directory, and no project script was executed.
