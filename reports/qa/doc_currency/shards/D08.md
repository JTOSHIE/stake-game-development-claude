# D08: session report and the stream test ledgers

Scope: `reports/SESSION_REPORT.md` (7490 lines, append only) with attention on the MOST
RECENT session section and its `FOR THE NEXT SESSION` block at 7321 to 7490, plus
`reports/qa/stream_test/KNOWN_OPEN.md` (61 lines), `CLUSTERS.md` (248 lines) and
`LEDGER.md` (spot checks on its own counts).

| id | kind | doc_path:line | claim_quote | reality_quote | verdict | severity |
|---|---|---|---|---|---|---|
| D08-01 | SELF_CONTRADICTION | reports/qa/stream_test/CLUSTERS.md:13 | `**571 active findings: 60 STREAM, 183 HIGH, 242 MEDIUM, 81 LOW.**` | The four tier figures sum to 566, not 571. `python3 -c "print(60+183+242+81)"` returns `566`. `KNOWN_OPEN.md:42-43` reads `it holds 571 findings of which 506 are HIGH, MEDIUM or LOW`, and 183+242+81 = 506 exactly, so the HIGH/MEDIUM/LOW legs agree across both documents and the STREAM leg is the one that does not reconcile: 571 minus 506 is 65, while this line says 60. | STALE | MEDIUM |
| D08-02 | SELF_CONTRADICTION | reports/qa/stream_test/CLUSTERS.md:224 | `| **OWNER-PARKED, verified, diagnosis corrected by panel** | 18 | C-01, C-02, C-04 to C-09, C-13, C-15 to C-19, C-21, C-22, C-24 |` | The enumeration holds SEVENTEEN clusters, not eighteen: C-01, C-02, C-04, C-05, C-06, C-07, C-08, C-09, C-13, C-15, C-16, C-17, C-18, C-19, C-21, C-22, C-24. With `CLOSED, not a defect | 1 | C-20` and `REOPENED ... | 7 | C-03, C-10, C-11, C-14, C-23, C-25, C-26` that accounts for 25 of the 26 clusters the same file declares at line 31 (`Twenty six.`). **C-12 appears in no disposition row at all**, despite this file giving it its own analysis at 194 and 208 to 216. | STALE | HIGH |
| D08-03 | SELF_CONTRADICTION | reports/SESSION_REPORT.md:7349-7350 | `**The seven REOPENED clusters** (C-03, C-10, C-11, C-14, C-23, C-25, C-26). Six carry a known marshalling fault with the fault named` | `CLUSTERS.md:195-202` names marshalling faults for exactly six clusters: C-03, C-10, C-11, **C-12**, C-23, C-26. Only FIVE of those six are in the reopened seven; C-12 is not reopened (and is dispositioned nowhere, per D08-02), while C-14 and C-25 are reopened with no fault named anywhere in that table. So five of the seven carry a named fault, not six. | STALE | HIGH |
| D08-04 | SELF_CONTRADICTION | reports/SESSION_REPORT.md:7352 | `because two of the seven exist only because the tier filter hid their corroboration` | `CLUSTERS.md:205-206` names the tier-filter pair as `**C-12 and C-23 are the important pair**`. C-12 is NOT one of the seven reopened clusters listed one line earlier at 7349, so exactly ONE of the seven (C-23) is a tier-filter case. | STALE | MEDIUM |
| D08-05 | STALE_NUMBER | reports/qa/stream_test/LEDGER.md:132 | `**540 findings: 43 STREAM, 160 HIGH, 228 MEDIUM, 106 LOW.** Zero squads lost.` | The tier figures sum to 537, not 540. The 540 total is independently correct as the sum of the lens table immediately above it (`LEDGER.md:124-130`: 236+163+77+42+22 = 540), so the total is right and the tier split is three findings short. | STALE | LOW |
| D08-06 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7435 | `Proof at ` + `reports/screens/winbanner-fix-2026-07-29/` | `ls -1 reports/screens/winbanner-fix-2026-07-29/` returns 5 entries: `README.md`, `after_de_015_bigwin_settled.png`, `after_en_015_desktop_bigwin_settled.png`, `before_de_430_bigwin_settled.png`, `before_en_015_desktop_bigwin_settled.png`. Directory exists with both before and after halves on both locales named in the claim. | CURRENT | LOW |
| D08-07 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7429-7430 | `**MID-02**, the ASCII `x` on the multiplier, now U+00D7.` and `**TR-104's remaining half**, the unit now routed through `t($locale, 'bet', ...)`` | Both land in `frontend/src/lib/components/WinBanner.svelte` at HEAD: `:211` reads `$: multLabel = ` + backtick + `${Math.round(shownMultiplier)}×` and `:221` reads `$: multUnitLabel = t($locale, 'bet', $isSocial ? 'social' : 'real')`. | CURRENT | MEDIUM |
| D08-08 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7435 | `Tracker TR-117 FIXED.` | `docs/records/reviews/REVIEW_TRACKER.md:298` carries `| TR-117 |` with status cell `**FIXED 2026-07-29**, both halves, re-proven from fresh frames`. | CURRENT | LOW |
| D08-09 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7353-7354 | `The sound fix is `GameGrid.svelte:499` `DROP_H` to at most 208` | `frontend/src/lib/components/GameGrid.svelte:499` reads `      const DROP_H = 520`. Matches `KNOWN_OPEN.md:51` (`**C-01's real cause is `GameGrid.svelte:499`, `DROP_H = 520`**`) and 520-208 = 312, the `312 of 412 pixels` that line also states. | CURRENT | MEDIUM |
| D08-10 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7356 | `at 0.88 alpha (`:1259`)` | `frontend/src/lib/components/GameGrid.svelte:1259` reads `    background: rgba(28, 29, 46, 0.88);`. Exact line, exact alpha. | CURRENT | LOW |
| D08-11 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7341-7342 | `reports/qa/stream_test/` (LEDGER, CLUSTERS, SHARD_INDEX, 28 new shards, 20 superseded moved aside) | `ls -1 reports/qa/stream_test/shards/superseded/ | wc -l` returns `20`. `ls -1 reports/qa/stream_test/shards/*.md | wc -l` returns `55`, which matches `CLUSTERS.md:7` (`over 55 active shards`). LEDGER, CLUSTERS and SHARD_INDEX.md all present. | CURRENT | LOW |
| D08-12 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7446 | `so **any re-run wrote into the committed evidence directory and overwrote all 519 frames.**` | `find reports/screens/stream-test-2026-07-28 -name '*.png' | wc -l` returns `519`; total files `521` (the extra two being `README.md` and `MANIFEST.json`), which also reconciles line 7460's `names the 521 tracked files`. Both figures in the report are right and describe different things. | CURRENT | LOW |
| D08-13 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7458-7459 | `The script now asks git whether anything at the destination is tracked and refuses if so, requiring an explicit `--regenerate`.` | `frontend/scripts/stream_test_capture.mjs:496` reads `    if (tracked && !process.argv.includes('--regenerate')) {`, and `:76` reads `const DATE = process.env.STREAM_TEST_DATE || (() => {`, so the hardcoded `'2026-07-28'` the same paragraph describes is gone. | CURRENT | MEDIUM |
| D08-14 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7409-7410 | `the 207 upscaled frames were written to the gitignored `.evidence-scratch/`` | `.gitignore:54` reads `.evidence-scratch/`. | CURRENT | LOW |
| D08-15 | VERIFIED_CURRENT | reports/SESSION_REPORT.md:7343 | `` `docs/skills/AGENT_BUDGET_AND_SCHEDULING.md` (4.3, 4.4) `` | That file carries `## 4.3 The MEASURE multiplier, and why verification always costs more than the estimate` at line 195 and `## 4.4 Grep-level clustering is provisional until a panel tests it` at line 226. Both sections exist. | CURRENT | LOW |
| D08-16 | UNKNOWN | reports/SESSION_REPORT.md:7380-7382 | `ran remote CI as run **30393688459** and it is **green**: https://github.com/JTOSHIE/stake-game-development-claude/actions/runs/30393688459` | UNKNOWN. Verifying a GitHub Actions run status needs a network fetch or `gh run view`, and both are outside a read-only local pass; `gh` would also authenticate against the remote. Local corroboration only: `git log --oneline` confirms `57fb01e docs(session): Session 1, the stream close, graded against its Plan of Record` exists as a real commit, so the SHA the run is attributed to is real. Same position for run `30398189558` at 7485-7486 and its commit `9602728 fix(gate): the social vocabulary guard pinned a mechanism, not its guarantee`. | UNKNOWN | LOW |

## NOTES

**On D08-02 and D08-03, which are one fault seen twice.** These are the load bearing rows.
`CLUSTERS.md`'s FINAL DISPOSITION table is the artefact Session 2 was told to work from, and
C-12 falls through it: it has a named marshalling fault, it has a documented fix that does
not compile (`prose.ts:72` totality, per `CLUSTERS.md:210-213`), and it is named in the
session report's own pick-up list at 7361 (`**C-12's fix must land key and copy together.**`),
yet no disposition row contains it. A reader working the table alone would count 25 clusters
against a declared 26 and not know which one was missing. The `18` in that row is the tell.

**On what is legitimately historical and therefore NOT flagged.** Everything above line 7216
in `SESSION_REPORT.md` is prior sessions' appended history per convention (a) and (i) and was
sampled only for the current block's dependencies. `REVIEW_TRACKER.md:298`'s line citations
(`WinBanner.svelte:205`, `:210`) no longer point at the code they described, because the fix
shifted the file; that row is explicitly a closed finding record dated at discovery, so it is
HISTORICAL_OK and is not filed as a dead reference. The same reasoning covers
`LEDGER.md:124-132`, which is scoped to the recovery session's 46 shards while `CLUSTERS.md`
is scoped to 55, so 540 against 571 is two different populations rather than a contradiction:
only the internal tier arithmetic (D08-05) fails.

**On the KNOWN_OPEN cross reference.** No row here duplicates a `KNOWN_OPEN.md` row. D08-01
uses `KNOWN_OPEN.md:42-43` as the corroborating side rather than reporting it as the defect,
because the 506 figure there reconciles exactly and the 571/566 gap sits in `CLUSTERS.md`.
`KNOWN_OPEN.md`'s own factual claims that I re-checked came back CURRENT: line 51's
`DROP_H = 520` (D08-09), line 15's proof directory (D08-06) and line 20's `prose.ts`
relocation of the surviving glyph instances.

doc_lines_in_scope: 7490 in `SESSION_REPORT.md` of which 275 (7216 to 7490) read closely, plus 61 (`KNOWN_OPEN.md`) and 248 (`CLUSTERS.md`) read in full or near full, plus about 40 lines of `LEDGER.md`
claims_sampled: 24
claims_stale: 5
claims_current: 11
claims_unknown: 1
sampling_basis: Every claim in the current `FOR THE NEXT SESSION` block and its ADDENDUM that names a path, a count or a closure a Session 2 reader would act on, plus every self-consistency arithmetic check available inside the two ledgers; prose, rationale and the pre-7216 history were sampled only where the current block depends on them.
scripts_executed: NONE
tree_clean_after: UNKNOWN pending close
