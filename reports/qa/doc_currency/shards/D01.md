# D01: claude-md (CLAUDE.md, CLAUDE_PROJECT_INSTRUCTIONS_v7.md)

Document currency audit, squad D01. Scope: `/Users/jt/math-sdk/CLAUDE.md` (1011 lines) and
`/Users/jt/math-sdk/CLAUDE_PROJECT_INSTRUCTIONS_v7.md` (42 lines). HEAD `5816045`, branch main.

| id | kind | doc_path:line | claim_quote | reality_quote | verdict | severity |
|---|---|---|---|---|---|---|
| D01-01 | STALE_NUMBER | CLAUDE.md:667 | "the other six browser legs \| 74 to 217 seconds each, in parallel" | `.github/workflows/checks.yml` matrix holds TEN legs, so nine others: `- name: "browser: layout fit"`, `contrast`, `splash calm`, `turbo intensity`, `paytable card fill`, `scrim coverage`, `win count-up steady`, `max-win hold`, `bet selector`, `preview server` (lines 571-600) | STALE | MEDIUM |
| D01-02 | STALE_NUMBER | CLAUDE.md:677 | "**Seven concurrent legs contend for runner resources**, so each runs slower than it does alone" | ten legs at HEAD; `git log -S'browser: max-win hold' -- .github/workflows/checks.yml` = `86d2833 2026-07-28 04:49`, `bet selector` = `3610eb7 2026-07-28 05:37`, `preview server` = `ffc8512 2026-07-28 07:54`, all AFTER `bb8eecc 2026-07-28 01:50 docs: the measured CI numbers` wrote the table | STALE | MEDIUM |
| D01-03 | SUPERSEDED | CLAUDE.md:666 | "`browser: scrim coverage` \| about **276 seconds**, the slowest leg, which sets the wall-clock" | `.github/workflows/checks.yml:594-595`: "The slowest leg by design: it holds a real capped round for thirty seconds" heads `- name: "browser: max-win hold"` | STALE | MEDIUM |
| D01-04 | STALE_NUMBER | CLAUDE.md:668 | "**browser wall-clock** \| **2.9 to 4.6 minutes** across two runs, down from 6.4" | measured with seven legs at `bb8eecc` 01:50; three further legs landed the same day including the thirty-second max-win hold. Reality of the current wall-clock: UNKNOWN, no run reading at HEAD is available read-only and no CI run may be triggered. The DENOMINATOR the range was measured against is provably changed | UNKNOWN | MEDIUM |
| D01-05 | SUPERSEDED | CLAUDE.md:143 | "Currently compensated and unreachable via the live UI: `FeatureMenu.svelte`'s own `activateBuy()` gate (`$balance < $betAmount * m.cost`) checks the correct per-mode cost and blocks first" | `frontend/src/lib/components/FeatureMenu.svelte:135`: `if (!$canAffordMode(m.serverMode)) return`, importing from `'../stores/buyAffordability'` at line 35. The quoted expression `$balance < $betAmount * m.cost` does not appear in the file. CLAUDE.md's own later bullet (line 186) records the replacement: "R8/TR-016 (2026-07-25) replaced it at both consumers with the non-locked `stores/buyAffordability.ts`" | STALE | MEDIUM |
| D01-06 | SELF_CONTRADICTION | CLAUDE.md:142 | "**`gameStore.ts`'s `canBuyBonus` derived store hardcodes `$bet * 100`** ... fix opportunistically the next time `gameStore.ts` is under a sanctioned edit for something else." | CLAUDE.md:181-190 records the SAME store a second time with a later disposition: "The locked derived store is now unreferenced by production code and allowlisted in the dead-wiring gate. Delete it on the next sanctioned `gameStore.ts` pass". Two entries for one debt with different remedies (fix opportunistically vs delete) and only the second reflects `frontend/scripts/dead_wiring_scan.mjs:63` `canBuyBonus: 'locked gameStore.ts, LOCKED_FILE_DEBTS'` | STALE | LOW |
| D01-07 | STALE_NUMBER | CLAUDE_PROJECT_INSTRUCTIONS_v7.md:22 (convention f) | "Known hardcodes inside locked files that are compensated frontend-side are recorded in CLAUDE.md under LOCKED_FILE_DEBTS (currently: canBuyBonus 100x and canSpin 1x affordability checks, both compensated and unreachable via live UI)" | CLAUDE.md LOCKED_FILE_DEBTS (lines 137-219) carries FIVE live entries and no `canSpin` entry at all: canBuyBonus, the cleared rgsService list, the bet-ladder actions, four em dashes, four dead stores. And `canSpin` IS reachable via the live UI: `frontend/src/App.svelte:1633` `if ($canSpin) handleSpin()`, imported at `App.svelte:51` | STALE | MEDIUM |
| D01-08 | VERIFIED_CURRENT | CLAUDE.md:228-236 (BRANCHES table) | "Tip `88df4f9`, 2026-07-26" / "Tip `75eff45`, 2026-07-08" / "Tip `d270019`, 2026-07-06" / "Tip `bcbe152`, 2026-07-05" / "Tip `4f4d6ef`, 2026-07-05" with ahead counts 1 / 0 / 1 / 18 / 38 | `git rev-parse --short` + `git rev-list --count origin/main..`: `chore/wip-backgrounds tip=88df4f9 ahead=1 date=2026-07-26`, `claude/collect-prototype tip=75eff45 ahead=0 date=2026-07-08`, `claude/fs-super-prototype tip=d270019 ahead=1 date=2026-07-06`, `claude/gap-analysis tip=bcbe152 ahead=18 date=2026-07-05`, `claude/lumen-sideproject tip=4f4d6ef ahead=38 date=2026-07-05`. Every SHA, every date and every count matches | CURRENT | LOW |
| D01-09 | VERIFIED_CURRENT | CLAUDE.md:227 | "**Six branches exist on the remote and that is the whole list.**" | `git branch -r`: `origin/chore/wip-backgrounds`, `origin/claude/collect-prototype`, `origin/claude/fs-super-prototype`, `origin/claude/gap-analysis`, `origin/claude/lumen-sideproject`, `origin/main` = six, plus the `origin/HEAD -> origin/main` symref which is not a branch | CURRENT | LOW |
| D01-10 | VERIFIED_CURRENT | CLAUDE.md:239-246 | "`git rev-list --count origin/main..origin/claude/collect-prototype` returns **0**, and its tip IS an ancestor of main ... `git ls-tree -r origin/claude/collect-prototype` holds **sixteen files** under `games/future_spinner_collect/`" | `git ls-tree -r --name-only origin/claude/collect-prototype -- games/future_spinner_collect/ \| wc -l` = `16`; `git merge-base --is-ancestor origin/claude/collect-prototype origin/main` = `IS_ANCESTOR`; ahead count = 0. All three re-derived independently | CURRENT | LOW |
| D01-11 | VERIFIED_CURRENT | CLAUDE.md:39-41 | "`frontend/src/lib/services/rgsService.ts` ... `frontend/src/lib/stores/gameStore.ts` ... `games/future_spinner/**`" as "also machine-enforced by the `deny` rules in `.claude/settings.json`" | `.claude/settings.json` deny list: `"Edit(frontend/src/lib/services/rgsService.ts)"`, `"Write(...)"`, `"Edit(frontend/src/lib/stores/gameStore.ts)"`, `"Write(...)"`, `"Edit(games/future_spinner/**)"`, `"Write(games/future_spinner/**)"`. All three paths present in both Edit and Write form | CURRENT | LOW |
| D01-12 | VERIFIED_CURRENT | CLAUDE.md:193-200 | "**`gameStore.ts` carries four em dashes in comments** ... line 1, the `isTurbo` doc comment, the `boardSymbols` doc comment, and the trailing comment on `balance.set(authBalance)`" | `grep -c '—' frontend/src/lib/stores/gameStore.ts` = `4`, at lines 1, 42 (`/** Turbo mode — greatly reduces reel animation duration */`), 51 (`/** Raw board result from last spin — 5 reels × 4 rows */`) and 153 (`balance.set(authBalance)          // RGS authoritative — never apply local math`). Count, positions and comment-only nature all hold | CURRENT | LOW |
| D01-13 | VERIFIED_CURRENT | CLAUDE.md:211-219 | "**Four dead stores inside `gameStore.ts`**: `betIndex` (derived), `buyBonusActive` (writable), `canSetMaxBet` (derived), `sessionStats` (writable) ... allowlisted with this reason in `frontend/scripts/dead_wiring_scan.mjs`" | all four still declared (`gameStore.ts:64,67,77,109`); `frontend/scripts/dead_wiring_scan.mjs:49-52` `betIndex: 'locked gameStore.ts, LOCKED_FILE_DEBTS'`, `buyBonusActive:`, `canSetMaxBet:`, `sessionStats:` identical reason string. A tree-wide grep for reads outside `gameStore.ts` returns only `.test.ts` files and comments | CURRENT | LOW |
| D01-14 | VERIFIED_CURRENT | CLAUDE.md:170-179 | "both bet-changing surfaces now drive from the non-locked `stores/betLadder.ts` ... The locked functions are now unreferenced by production code." | `frontend/src/lib/components/FeatureMenu.svelte:32` imports `increaseBetLevel, decreaseBetLevel, canIncreaseBetLevel, canDecreaseBetLevel,` and lines 341/343 bind them; `gameStore.ts:119-142` `increaseBet`/`decreaseBet`/`setMaxBet`/`setMinBet` still present and referenced nowhere outside `gameStore.ts` and `betLadder.test.ts` comments | CURRENT | LOW |
| D01-15 | VERIFIED_CURRENT | CLAUDE.md:286-289 | "`base` (Normal, 1.0x), `cruise` (Cruise, 1.0x, low-vol), `antelite` (OVERBOOST, 1.25x), `bonus` (Buy Overdrive, 100.0x) and `super` (NITRO OVERDRIVE, 400.0x" | `frontend/src/lib/config/fsModes.ts` costs `1.0`, `1.0`, `1.25`, `100`, `400` against serverModes `'base'`, `'cruise'`, `'antelite'`, `'bonus'`, `'super'`; `frontend/src/lib/i18n/prose.ts:85-93` `modeNormalLabel: 'Normal'`, `modeCruiseLabel: 'Cruise'`, `modeOverboostLabel: 'OVERBOOST'`, `modeBonusLabel: 'Buy Overdrive'`, `modeSuperLabel: 'NITRO OVERDRIVE'`; `games/future_spinner/game_config.py:285,320,347,385` costs `1.0`, `100.0`, `1.0`, `1.25` | CURRENT | LOW |
| D01-16 | VERIFIED_CURRENT | CLAUDE.md:301 | "Max win 5,000x (hard cap every mode). Grid 5x4, 1,024 ways. 100,000 rounds per mode." | `games/future_spinner/game_config.py:52` `_WINCAP = 5000.0     # Maximum payout multiplier (x bet amount), hard cap both modes`, `:278` `_maxwin = int(_WINCAP)`, `:61` "5 reels x 4 rows = 20 symbol positions. Ways-to-win: 4^5 = 1,024.", `:111-112` `num_reels = 5` / `num_rows = [4] * self.num_reels`; `wc -l library/publish_files/lookUpTable_{base,bonus,cruise,antelite,super}_0.csv` = `100000` each, all five | CURRENT | LOW |
| D01-17 | VERIFIED_CURRENT | CLAUDE.md:295-299 | "3/4/5 scatters award 8/12/16 free spins AND pay an instant 1x/3x/10x total bet ... 3+ scatters in free spins retrigger +5 spins" and "**Scatter values are 1x/3x/10x everywhere** ... Not 5x/15x/50x." | `games/future_spinner/game_config.py:10-17`: "Trigger: 3, 4 or 5 scatters in the base game award 8, 12 or 16 free spins / AND still pay the instant scatter award of 1x, 3x or 10x total bet." and "Retrigger: 3 or more scatters during free spins award +5 free spins"; `:143` `3: 1.0,   # 3 scatters -> 1x total bet` | CURRENT | LOW |
| D01-18 | VERIFIED_CURRENT | CLAUDE.md:302-308 | "Paytable unchanged (H1 22/6/1.5 down to L3 0.65/0.20/0.08 per way)." and "RTP: 96.3500% at 4dp in ALL FIVE modes" | `games/future_spinner/game_config.py:120` `(5, "H1"): 22.0, (4, "H1"): 6.0, (3, "H1"): 1.5,` and `:127` `(5, "L3"): 0.65, (4, "L3"): 0.20, (3, "L3"): 0.08,`; `:51` `_BASE_RTP = 0.9635`, referenced as `rtp=self.rtp` by all five BetModes; `frontend/src/lib/config/fsModes.ts:138` `export const FS_RTP_LABEL = '96.35%'` | CURRENT | LOW |
| D01-19 | VERIFIED_CURRENT | CLAUDE.md:222-223, 255-257 | "Owner's order, 2026-07-28 (`reports/briefs/FS_HYGIENE_AND_REGISTER_Prompt.md`, JOB 1)" and "The full table, including every tip SHA ... is at **`docs/records/BRANCH_HYGIENE_2026-07-28.md`**" | `ls` returns both: `docs/records/BRANCH_HYGIENE_2026-07-28.md`, `reports/briefs/FS_HYGIENE_AND_REGISTER_Prompt.md`. Neither is a dead reference | CURRENT | LOW |
| D01-20 | VERIFIED_CURRENT | CLAUDE.md:45-52 | "the bet-levels passthrough (writes `rgsBetLevels` ...); `play()` includes the selected bet mode from the `selectedBetMode` store in the request; `authenticate()`/`initRGS()` surface jurisdiction flags and publish them to `jurisdictionFlags`; `_rgsSpinReal()` publishes the full raw round events to `lastRoundEvents` before flattening." | `frontend/src/lib/services/rgsService.ts:96` `import { rgsBetLevels } from '../stores/rgsBetLevels'`, `:101-103` the other three store imports, `:632` `mode:      get(selectedBetMode),` inside `play()`, `:572` `jurisdictionFlags: flags,`, `:774` `async function _rgsSpinReal(req: SpinRequest)` with `:783` `lastRoundEvents.set(playResp.events)`. All four passthroughs present, and `_rgsSpinReal` is a real function name a reader can grep | CURRENT | LOW |
| D01-21 | VERIFIED_CURRENT | CLAUDE.md:155-165 | "It now delegates to `roundInterpreter`, the canonical reader ... Held by `rgsService.parse.test.ts` (CI gate 11)" / "Now routed through `_withRetry`" / "the canonical value is imported and re-exported. There is now exactly one declaration ... `currency_scale_drift.test.mjs` asserts no copy returns" | `rgsService.ts:105` `import { interpretEvents, type RawEvent, type Cell, CENTIBET_CAP } from './roundInterpreter'`, `:698` `const raw = await _withRetry('endRound', () =>`, `:116-117` `export { CURRENCY_SCALE } from '../utils/currency'` / `import { CURRENCY_SCALE } from '../utils/currency'`; both named test files exist: `frontend/src/lib/services/rgsService.parse.test.ts`, `frontend/scripts/currency_scale_drift.test.mjs` | CURRENT | LOW |
| D01-22 | VERIFIED_CURRENT | CLAUDE.md:203-206 | "`scripts/qa/locked_paths_gate.mjs` runs first in CI and fails any commit that touches a locked path without the owner-sanction token" | `ls scripts/qa/locked_paths_gate.mjs` resolves; `.github/workflows/checks.yml:146` `- name: locked paths and track scope, self-test` and `:170` `- name: locked paths and track scope` are the first gate steps of the `static gates` job (job starts at `:104`, checkout `:110`, node `:120`) | CURRENT | LOW |
| D01-23 | VERIFIED_CURRENT | CLAUDE.md:194-195 | "The purge cleared 175 dashes across 42 files so the source tree matches the CLAUDE.md header, but this file is locked ... so its four remain" | `grep -rl '—\|–' frontend/src` returns exactly ONE file: `lib/stores/gameStore.ts`. The residual is precisely and only the locked file the entry names; the rest of the source tree is clean | CURRENT | LOW |
| D01-24 | VERIFIED_CURRENT | CLAUDE.md:398-400 | "In production (`!import.meta.env.DEV`) it is not rendered and the default theme is forced. The gating lives in `App.svelte` behind `import.meta.env.DEV`" | `frontend/src/App.svelte:32-34` `if (!import.meta.env.DEV) { switchTheme(DEFAULT_THEME_ID) }` and `:2090` `{#if import.meta.env.DEV && showThemeSelector}` guarding `<ThemeSelector .../>` at `:2091` | CURRENT | LOW |
| D01-25 | VERIFIED_CURRENT | CLAUDE.md:408-418 | "It lives in `replayService.ts`, `replayStore.ts`, `ReplayMode.svelte`" and the 2026-07-26 reword: "`ControlBar` and `AutoPlayModal` no longer exist as components, and `BalanceDisplay` is dead and removed; the bet ladder was ported into `HudOverlay.svelte`" | present: `frontend/src/lib/services/replayService.ts`, `frontend/src/lib/stores/replayStore.ts`, `frontend/src/lib/components/ReplayMode.svelte`, `frontend/src/lib/components/HudOverlay.svelte`. Absent, as the reword asserts: `ls: lib/components/ControlBar.svelte: No such file or directory`, same for `AutoPlayModal.svelte` and `BalanceDisplay.svelte`. This is the rare case of a document correctly recording its own supersession | CURRENT | LOW |
| D01-26 | VERIFIED_CURRENT | CLAUDE.md:403-407 | "No `fonts.googleapis.com` / `fonts.gstatic.com` or any external font CDN. Verify before commit: `grep -rn ...` must be empty." | ran the document's own grep over `frontend/` excluding node_modules for `.html`, `.svelte`, `.ts`, `.css`: zero matches. The stated verification passes at HEAD | CURRENT | LOW |
| D01-27 | SUPERSEDED | games/future_spinner/game_config.py:51-52 (LOCKED) | "`_BASE_RTP = 0.9635   # Shared RTP target across both bet modes`" and "`_WINCAP = 5000.0     # Maximum payout multiplier (x bet amount), hard cap both modes`" | the package defines FIVE bet modes at `game_config.py:283,317,345,383` plus super, and CLAUDE.md:284 heads the block "True game facts (Overdrive Free Spins, five-mode package)". The code comments say "both bet modes", a two-mode-era phrasing. LOCKED file, comment only, no behaviour: rides along with the next sanctioned locked pass | STALE | LOW |

## NOTES

**Reverse direction of D01-01..04.** The finding is not that the CI timings were wrong when
written. They were measured and the commit that wrote them is `bb8eecc 2026-07-28 01:50:31`.
The finding is that the block is framed as CURRENT guidance a reader acts on ("Rule 10 asks
every session to verify its own final push's remote result, so every session reads these",
CLAUDE.md:661-662; "Judge a run against the range", :684) while three legs, one of them
explicitly thirty seconds of hold by design, were added to the matrix between 04:49 and 07:54
on the same day. A session reading CLAUDE.md at HEAD will judge a ten-leg run against a
seven-leg range and will believe scrim coverage sets the wall-clock. The block is not
presented as a dated snapshot in the sense the audit brief exempts: the date is on the
MEASUREMENT, and the surrounding prose is standing instruction.

**Why D01-04 is UNKNOWN and not STALE.** The wall-clock number could only be refuted by a
CI run reading, and this pass is read-only and may not trigger CI. What is established is
that the denominator changed. I did not assert a replacement figure.

**On D01-05, the shape that matters.** The claim is not merely out of date, it quotes a code
expression that a reader would grep for and not find. Anyone auditing whether the
`canBuyBonus` debt is still compensated would open `FeatureMenu.svelte`, fail to find
`$balance < $betAmount * m.cost`, and could reasonably conclude the compensation was removed
and the debt is now live. It is not: `canAffordMode` is a stricter per-mode gate. The stale
quote makes a safe state look unsafe, which is the more expensive direction of error.

**What I deliberately did NOT report.** The `COMPLIANCE_WATCH.md:434/447` contradiction is
out of scope and already recorded. I found no match for my findings in
`reports/qa/stream_test/KNOWN_OPEN.md`: that ledger is rendered-surface and gate-blind-spot
rows (TR-104, TR-115, Q-16, Q-26, Q-27, Q-34, C-01), none of which touch CLAUDE.md's
conventions, branches, debts or CI blocks. No KNOWN(row) applies.

**Sections sampled but not exhausted.** I read the section headings for all 1011 lines of
CLAUDE.md and opened lines 37-60, 137-320, 396-421 and 655-695 in full. The following
sections were NOT opened and their claims are unaudited by this shard, and should be treated
as no-coverage rather than as clean: "Division of authority" (6-24), "Ways of working"
(25-36), "The owner-sanction token" body (60-136), "Assets" (310-395), "Session conventions"
(422-533), "THE STANDING MANDATE" (534-559), most of "THE MULTI-TRACK PROTOCOL" (560-994
apart from the CI block at 655-695), and "THE FACTS DISCIPLINE" (995-1011). Squads covering
assets, compliance and protocol documents may overlap those.

**The Compliance section is the best-aged block in the document** and is worth naming as the
counter-example, because a currency audit that reports only decay misleads. Lines 402-421
carry four checkable claims and all four hold, including a self-correction (D01-25) where a
2026-07-26 reword replaced three component names that had since been deleted with a statement
of the BEHAVIOUR, precisely so the requirement would survive the component tree changing. The
CI block twenty-five lines later decayed within hours of being written for the opposite
reason: it stated a measurement and a leg count rather than a rule for reading them.

doc_lines_in_scope: 1053 (CLAUDE.md 1011 + CLAUDE_PROJECT_INSTRUCTIONS_v7.md 42)
claims_sampled: 31 of roughly 140 discrete factual claims across the two documents
claims_stale: 6 (D01-01, D01-02, D01-03, D01-05, D01-07, D01-27; plus D01-06 as a
  self-contradiction whose second half is the current one)
claims_current: 20
claims_unknown: 1 (D01-04)
sampling_basis: I took the five priorities named in the tasking (LOCKED_FILE_DEBTS, the
  BRANCHES table, the locked paths against .claude/settings.json, the measured CI timings,
  the True game facts block) and inside each one checked every claim a reader would ACT on,
  meaning every SHA, every count, every named file path and every code expression quoted as
  if greppable, rather than spreading thinly across all 1011 lines. Every branch tip and
  ahead-count was re-derived with its own command; the True game facts block was checked
  against BOTH named sources independently (game_config.py and fsModes.ts plus prose.ts),
  and the 100,000-rounds claim against the shipped lookup tables rather than run.py, because
  run.py's num_sim_args at HEAD names only cruise/antelite/super and would have given a
  false negative. Prose, rationale and process narrative were not sampled.
scripts_executed: NONE
tree_clean_after: |
  ?? reports/qa/compliance_register/walk_shards/
  ?? reports/qa/doc_currency/
  ?? reports/qa/file_census/
  (three untracked directories, all of them other squads' shard trees plus my own
  reports/qa/doc_currency/; no tracked file modified, no tracked file deleted)
