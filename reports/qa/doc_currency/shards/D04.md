# D04, root-facts-docs: document currency audit

Scope: `GAME_FACTS.md` (395), `WRS_MASTER_DOCUMENT.md` (290), `DTT_PROTOCOL.md` (355),
`OWNER_CHECKLIST.md` (163), `BOOKS_MANIFEST.md` (121), `README.md` (103),
`REPLAY_TEST_EVENTS.md` (88). HEAD `581604555793b0f0deee7698cf741b167b281180`,
2026-07-29 14:42:46 +1000, branch main. Specification authority per convention (l.1) is
`games/future_spinner/game_config.py`, derived from first, never measured backwards.

Nothing in the tree was modified. No project script was executed. `git log` and `git show`
were used read-only, as permitted.

| id | kind | doc_path:line | claim_quote | reality_quote | verdict | severity |
|---|---|---|---|---|---|---|
| D04-16 | STALE_NUMBER | REPLAY_TEST_EVENTS.md:72 | `Cruise/antelite/super have no curated entries in `sample_rounds.json` yet (a known, previously-flagged limitation ...); in dev/mock mode these three modes correctly price and label their spins but fall back to `_mockSpin`'s generic random board rather than a curated feature demo.` | `frontend/src/lib/mock/sample_rounds.json` has curated entries for all three: `grep -o '"mode": *"[a-z]*"' \| sort \| uniq -c` returns `12 "mode": "antelite"`, `12 "mode": "cruise"`, `7 "mode": "super"` (plus 27 base, 31 bonus). Categories present include `cruise_loss`, `cruise_win_small/mid/large`, `antelite_loss`, `antelite_win_small/mid/large`, `super_win_small/mid/large`, `super_wincap`, `super_retrigger`, `super_high_meter`, `super_cap_adjacent`. `frontend/src/lib/mock/roundProvider.ts:90` selects generically: `const entry = (await samplesFor(mode)).find((s) => s.category === category) ?? null`, so no per-mode wiring is required | STALE | HIGH |
| D04-17 | STALE_NUMBER | REPLAY_TEST_EVENTS.md:66-71 | the Cruise (dev), Antelite (dev) and Super (dev) columns read `n/a - no curated sample yet` in every scenario cell, 15 cells in total | Same reality as D04-16: 31 curated rounds exist across those three modes. A tester following this table would skip local reproduction for three of the five shipped modes, including the whole `super_*` set that `frontend/src/lib/services/roundInterpreter.test.ts:92` already asserts against (`FAIL: super/${s.category} ... meterBefore on first free spin`) | STALE | HIGH |
| D04-18 | DEAD_REFERENCE | REPLAY_TEST_EVENTS.md:74 | `see `games/future_spinner_collect/COLLECT_PROTOTYPE_FINDINGS.md` for the same pattern noted elsewhere` | `ls games/future_spinner_collect/` returns exactly `__pycache__`, `game_executables`, `library`. `find . -name COLLECT_PROTOTYPE_FINDINGS.md` (excluding `.git`) returns nothing anywhere in the tree. The parent directory exists, so this is a missing file rather than a missing package | STALE | MEDIUM |
| D04-19 | SELF_CONTRADICTION | DTT_PROTOCOL.md:6 | `This is the operational form of the seven confirmations recorded in `reports/SESSION_REPORT.md`'s FOR THE NEXT SESSION blocks.` and at :22-23 `One live session converts seven assumptions into seven facts.` | The same document contains TEN numbered items: `grep -n '^## ' DTT_PROTOCOL.md` returns `## 1.` through `## 10.` (lines 44, 86, 102, 142, 163, 198, 219, 240, 275, 307). And `DTT_PROTOCOL.md:339` says `3. For each of the ten items, record **confirmed** or **the observed value**`. The header count is stale against the body of its own file; a reader planning the single portal session would budget for seven observations and meet ten | STALE | MEDIUM |
| D04-20 | STALE_NUMBER | OWNER_CHECKLIST.md:31 | `**Nine of them are OWNER items**, meaning only you can answer them.` | The document it tells the owner to open beside it tallies eight, not nine: `docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md:169` reads `| OWNER | 8 |`, and that summary sums correctly to 58 (31+14+8+3+1+1). Off by one against its own cited source | STALE | MEDIUM |
| D04-21 | SELF_CONTRADICTION | docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md:169 | `| OWNER | 8 |` | Only SEVEN rows in that file carry an OWNER status in any form. `grep -n 'OWNER\|Owner'` returns nine lines, of which :8 is prose, :21 is the legend row `| **OWNER** | Not a build item...` and :169 is this tally; the seven data rows are item 7 (:48), item 32 (:118, marked `**OWNER/OBSERVE**`), item 53 (:154), 54 (:155), 55 (:156), 57 (:158), 58 (:159). Strict `**OWNER**` alone is six. Out of my named scope, recorded because it is the source D04-20 depends on and it makes the true figure six, seven or eight depending on the counting rule, never nine. Caveat on method: I matched case-sensitive `OWNER`/`Owner`; a row with a lowercase status would have been missed, though every other status in that table is bolded caps | STALE | LOW |
| D04-22 | STALE_NUMBER | frontend/src/lib/mock/roundProvider.ts:57 | `NOTE: sample_rounds.json currently curates only 'base'/'bonus' samples; a buy tier with no curated pool yet (e.g. 'super') returns null here and the caller falls back to the raw _mockSpin board` | Same reality as D04-16: cruise, antelite and super are all curated, and `super` has seven rounds. Not a document, so outside my scope by letter, recorded because it is the SAME stale fact as D04-16/17 in a second place and shows the pool grew while three prose statements did not follow | STALE | LOW |
| D04-23 | STALE_NUMBER | frontend/src/lib/mock/roundProvider.ts:76 | `The curated pool is a multi-MB JSON file (58 rounds' worth of full board/event data)` | `grep -c '"category":' frontend/src/lib/mock/sample_rounds.json` returns `89`. The pool is 89 rounds, not 58 | STALE | LOW |
| D04-24 | SUPERSEDED | WRS_MASTER_DOCUMENT.md:287 | `Two real findings surfaced during the sweep and JOB 6/7 prep, both still open: `SUBMISSION_BLURB.md` (repo root) is stale pre-Overdrive text contradicting the shipped game (3a)` | Section 3a of the same file, at :80, records the resolution: `was moved to `reports/archive/SUBMISSION_BLURB_superseded.md` with a SUPERSEDED header in the 2026-07-14 work order's ITEM 0 - resolved`, and `reports/archive/SUBMISSION_BLURB_superseded.md` exists. NOT FLAGGED AS STALE: :287 sits under a dated `- 2026-07-14:` change-log bullet, so it is a record of that date and the convention protects it. Recorded only because both entries are dated the same day and a reader scanning the change log alone reads "still open" | HISTORICAL_OK | LOW |
| D04-25 | VERIFIED_CURRENT | DTT_PROTOCOL.md:82, 137, 159, 193, 194, 215, 235, 302, 329, 330 | ten `**Screenshot:**` filenames such as `01-authenticate-payload.png`, `03-play-payload.png`, `10b-real-popout-spinning.png` | None of the ten exists anywhere in the tree (`find . -name <basename>` returns nothing for each). NOT A DEAD REFERENCE: `DTT_PROTOCOL.md:33` establishes them as prospective, `Screenshots go to `reports/screens/dtt-<date>/` using the filenames given per item`, and :2 frames the whole file as `For the owner, to run once on the Stake Engine portal`. These are instructions to a future capture, not citations of evidence. Rejected as a finding on purpose | CURRENT | LOW |
| D04-26 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:5 | `The five event books are **not in this repository and never will be**` | `git show --stat HEAD:games/future_spinner/library/publish_files/` lists exactly seven entries and no `books_*.jsonl.zst`: `game_metadata.json`, `index.json`, `lookUpTable_{antelite,base,bonus,cruise,super}_0.csv`. The five books are present in the WORKING TREE only, which BOOKS_MANIFEST.md:32 already accounts for as the `Local upload set` on `The build machine`. Rejected as a finding | CURRENT | LOW |
| D04-27 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:49 | `Total 387,425,204 bytes (369.5 MB), 500,000 rows.` | The five listed byte counts sum to exactly 387,425,204, and 387,425,204 / 1048576 = 369.47. Arithmetic self-consistent, and every addend independently confirmed by `ls -la` per D04-09 | CURRENT | LOW |
| D04-28 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:24-25 | ``books_bonus.jsonl.zst` (144.9 MB) and `books_super.jsonl.zst` (142.4 MB) both exceed **GitHub's hard 100 MB per-file limit**` | 151905143 / 1048576 = 144.87 and 149331268 / 1048576 = 142.40 against the on-disk sizes. Both above 100 MB, so the mechanical argument holds | CURRENT | LOW |
| D04-29 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:55-57 | `The five declared modes (`base`, `cruise`, `antelite`, `bonus`, `super`) each have exactly one book and one lookup table, and the filenames match the `events` and `weights` fields the index declares.` | `games/future_spinner/library/publish_files/index.json` declares five modes with `"events": "books_<mode>.jsonl.zst"` and `"weights": "lookUpTable_<mode>_0.csv"` for each, costs `1.0, 1.0, 1.25, 100.0, 400.0`. Filenames match the directory listing exactly | CURRENT | LOW |
| D04-30 | VERIFIED_CURRENT | OWNER_CHECKLIST.md:3-4 | `**five things are outstanding**` / `# THE FIVE OUTSTANDING ITEMS` | `grep -n '^## [0-9]' OWNER_CHECKLIST.md` returns `## 0.` through `## 5.`, and `## 0.` closes with `Nothing to do. This is a pointer, not a task.` Exactly five tasks | CURRENT | LOW |
| D04-31 | VERIFIED_CURRENT | OWNER_CHECKLIST.md:27, 33 | `Tick the 58 Guidelines` / `It carries our position on every one of the 58` | `grep -c '^| *[0-9]' docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md` returns `58`, numbered rows 1 through 58 with row 58 present at :159 `| 58 | Game Released | **OWNER** |` | CURRENT | LOW |
| D04-32 | VERIFIED_CURRENT | OWNER_CHECKLIST.md:31 | `**The Provably Fair toggle is among those nine**` | `STAKE_GUIDELINES_SELF_ASSESSMENT.md:154`: `| 53 | Provably Fair and Replay enabled | **OWNER** | Replay is demonstrably working; the Provably Fair toggle is a portal setting. |` The membership claim is right even though the count in D04-20 is not | CURRENT | LOW |
| D04-33 | UNKNOWN | OWNER_CHECKLIST.md:63 | `**The kit is built and waiting:** `~/Desktop/FS_UPLOAD_KIT_V10/`. Follow **PART 9i** in the walkthrough inside it.` | UNKNOWN. The path is outside the repository, on the owner's Desktop, and I did not read outside the working directory. I could not test whether the kit, or PART 9i inside it, exists. Flagged so the marshal knows this is an unverifiable-by-design instruction, not a checked one | UNKNOWN | LOW |
| D04-34 | VERIFIED_CURRENT | README.md:29 | `**RTP is 96.35% in all five modes.** Maximum win is capped at 5,000x in every mode.` | `games/future_spinner/game_config.py:51-52` `_BASE_RTP = 0.9635` / `_WINCAP = 5000.0`, and all five `BetMode(...)` constructors at :283, :317, :345, :383, :409 take `rtp=self.rtp` and `max_win=_maxwin` where `:278` sets `_maxwin = int(_WINCAP)`. No mode overrides either | CURRENT | LOW |
| D04-35 | VERIFIED_CURRENT | REPLAY_TEST_EVENTS.md:11-14 | `Five bet modes ship (FeatureMath v2, 2026-07-07): **base**/Normal (1.0x), **cruise**/Cruise (1.0x), **antelite**/OVERBOOST (1.25x ...), **bonus**/Buy Overdrive (100.0x) and **super**/NITRO OVERDRIVE (400.0x, guaranteed trigger with the Overdrive meter pre-revved to 5x)` | `game_config.py` name/cost pairs match all five. The pre-rev is real: `games/future_spinner/gamestate.py:39-48` `NITRO OVERDRIVE (the 400x super buy) pre-revs to 5x; every other mode starts at 1x` / `return 5 if name == "super" else 1` | CURRENT | LOW |
| D04-01 | VERIFIED_CURRENT | GAME_FACTS.md:17 | `Grid \| 5 reels x 4 rows (20 symbol positions)` | `game_config.py:111-112`: `self.num_reels = 5` / `self.num_rows = [4] * self.num_reels` | CURRENT | LOW |
| D04-02 | VERIFIED_CURRENT | GAME_FACTS.md:18 | `Ways-to-win, up to 4^5 = 1,024 ways` | `game_config.py:61` `5 reels x 4 rows = 20 symbol positions. Ways-to-win: 4^5 = 1,024.`; `:102` `self.win_type = "ways"` | CURRENT | LOW |
| D04-03 | VERIFIED_CURRENT | GAME_FACTS.md:19 | `Five (FeatureMath v2, 2026-07-07): `base` 1.0x, `cruise` 1.0x, `antelite`/OVERBOOST 1.25x, `bonus`/Buy Overdrive 100.0x, `super`/NITRO OVERDRIVE 400.0x` | `game_config.py` `name=`/`cost=`: :284-285 `base`/`1.0`, :318-319 `bonus`/`100.0`, :346-347 `cruise`/`1.0`, :384-385 `antelite`/`1.25`, :410-411 `super`/`400.0`. Five modes, every cost exact | CURRENT | LOW |
| D04-04 | VERIFIED_CURRENT | GAME_FACTS.md:20 | `Min / max bet \| $0.10 / $100.00` | `game_config.py:104-105` `self.min_denomination = 0.10` and `self.bet_levels = [0.10, 0.20, 0.50, 1.00, 2.00, 5.00, 10.00, 20.00, 50.00, 100.00]` | CURRENT | LOW |
| D04-05 | VERIFIED_CURRENT | GAME_FACTS.md:107-111 | paytable `5-of \| 22.00 \| 10.00 \| 5.00 \| 4.00 \| 2.00 \| 1.50 \| 0.80 \| 0.65` plus the 4-of and 3-of rows | `game_config.py:119-128` `(5,"H1"):22.0, (4,"H1"):6.0, (3,"H1"):1.5 ... (5,"L3"):0.65, (4,"L3"):0.20, (3,"L3"):0.08`. All 24 cells match the spec dict cell for cell | CURRENT | LOW |
| D04-06 | VERIFIED_CURRENT | GAME_FACTS.md:126 | `3, 4 or 5 scatters in the base game award 8, 12 or 16 free spins and pay an instant scatter award of 1x, 3x or 10x total bet respectively` | `game_config.py:143-146` `{3: 1.0, 4: 3.0, 5: 10.0}` and `:157` `_base_award = {3: 8, 4: 12, 5: 16}` | CURRENT | LOW |
| D04-07 | VERIFIED_CURRENT | GAME_FACTS.md:52 | `Maximum win \| 5,000x bet (hard cap)` | `game_config.py:52` `_WINCAP = 5000.0     # Maximum payout multiplier (x bet amount), hard cap both modes`; `:278` `_maxwin = int(_WINCAP)`, passed to all five BetModes | CURRENT | LOW |
| D04-08 | VERIFIED_CURRENT | GAME_FACTS.md:41 | base mode `RTP \| **96.3500%**` | `game_config.py:51` `_BASE_RTP = 0.9635`; every BetMode takes `rtp=self.rtp` | CURRENT | LOW |
| D04-09 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:43-47 | five byte counts, e.g. `books_base.jsonl.zst \| 28,678,793` and `books_bonus.jsonl.zst \| 151,905,143` | `ls -la games/future_spinner/library/publish_files/`: `28678793`, `17203911`, `40306089`, `151905143`, `149331268` for base, cruise, antelite, bonus, super. All five exact | CURRENT | LOW |
| D04-10 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:43-44 | SHA-256 `b86c8bb484523a53b8a42db6dbaef0bc26c51843077b5f06d01f492c40d39331` (base), `7b5a1ddcfcdfde76a2f286a36992df5f9e8632cf9cfdc442fcc71dfd3fcc5b24` (cruise) | `shasum -a 256` recomputed on both working-tree files: digests reproduce byte for byte. The remaining three hashes were NOT recomputed (about 340 MB); they match `SUBMISSION_DOSSIER.md:290-294` textually, which is identity between two documents, not against the bytes | CURRENT | LOW |
| D04-11 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:31 | `index.json`, `game_metadata.json`, 5 x `lookUpTable_<mode>_0.csv` (7 files) ... `This repository` ... `Verifiable by Anyone, directly` | `git show --stat HEAD:games/future_spinner/library/publish_files/` lists exactly seven entries and no books. NEAR-MISS WORTH RECORDING: `.gitignore:9` is `**/library/**`, which made this look like a dead claim until `git log` produced `d36574a chore(math): force-add generated PAR sheet and lookup tables`. The ignore rule is real and overridden by a force-add | CURRENT | LOW |
| D04-12 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:53-54 | `every book holds 100,000 rows; every `lookUpTable_<mode>_0.csv` holds 100,000 rows` | `wc -l` on all five csvs returns `100000` each. The book half is not independently checked here; it is asserted by the equality report in D04-13, whose `"lookup_rows": 100000` and `"rounds": 100000` per mode agree | CURRENT | LOW |
| D04-13 | VERIFIED_CURRENT | BOOKS_MANIFEST.md:94 | `**Result: 500,000 rounds, 4,455,829 assertions, 0 failures.**` | `reports/qa/books_lookup_equality_2026-07-25.json` `"totals"`: `"rounds": 500000`, `"assertions": 4455829`, `"failures": 0`. Also matches `README.md:80-81` word for word, and `tools/verify_books_lookup_equality.py` exists (20274 bytes) | CURRENT | LOW |
| D04-14 | VERIFIED_CURRENT | README.md:27 | `super \| NITRO OVERDRIVE \| 400x \| meter pre-revved to 5x` | `games/future_spinner/gamestate.py:39-48` `pre-revs to 5x; every other mode starts at 1x` / `return 5 if name == "super" else 1` | CURRENT | LOW |
| D04-15 | VERIFIED_CURRENT | README.md:52 | `Sixteen locales` | `frontend/src/lib/i18n/translations.ts:2074-2077` `export const LOCALE_CODES: Locale[] = ['en','ar','de','es','fi','fr','hi','id','ja','ko','pl','pt','ru','tr','vi','zh']`, sixteen entries, with the comment `The sixteen codes, in one place` | CURRENT | LOW |

## NOTES

**Cross-reference.** I read `reports/qa/stream_test/KNOWN_OPEN.md` in full before opening any
row. None of D04-16 through D04-24 matches a row there: the ledger is a rendered-surface
register and carries no documentation-currency rows, so none of these is KNOWN(). The
`COMPLIANCE_WATCH.md:434` self-contradiction named in the brief was not re-reported. D04-19
and D04-21 are the same SHAPE as it, in different files.

**The headline.** `REPLAY_TEST_EVENTS.md` tells a reviewer-facing tester that three of the
five shipped modes have no local reproduction. They all do, 31 curated rounds between them,
and `roundProvider.ts:90` selects them with no per-mode work. This is the one finding here
with a downstream cost: it is the document the Stake Engine bet-replay review is prepared
from, and it steers effort away from `super`, the mode with the pre-revved meter that most
needs demonstrating. Corroborated three ways (the JSON, the selector, the existing
`roundInterpreter.test.ts` assertions over `super/${s.category}`).

**What I deliberately did NOT report, and why.** Three candidates died on inspection and are
kept as rows D04-25, D04-26 and D04-31's neighbours rather than deleted, because the brief
asks for the denominator:
1. Ten `.png` filenames in `DTT_PROTOCOL.md` exist nowhere. They are a capture instruction,
   not a citation (`:33`). Prospective, not stale.
2. Five books sit in `publish_files/` while `BOOKS_MANIFEST.md:5` says they are not in the
   repository. They are untracked working-tree files, which `:32` already describes.
3. `.gitignore:9` `**/library/**` appeared to make the seven committed artefacts unreachable.
   `git log` showed the force-add. Had I stopped at the grep I would have filed a false
   DEAD_REFERENCE on the single most load-bearing claim in `BOOKS_MANIFEST.md`.

**Under-sampled, stated plainly.** `GAME_FACTS.md` sections 4 to 12 (lines 260 to 395) and
`WRS_MASTER_DOCUMENT.md` sections 1 to 8 (lines 1 to 260) were skimmed for paths only, not
claim by claim. `GAME_FACTS.md` section 3's live-money reconciliation figures (the EUR
openings, the 524-spin panel, the `x91.60`/`x73.28` worked example) are untested: each needs a
capture opened. `WRS_MASTER_DOCUMENT.md`'s status column, about 40 DONE/OPEN/GATE
assertions, is the largest unexamined surface in my scope and is where I would send the next
pass. `DTT_PROTOCOL.md` items 3 to 10 were not read past their headings.

**Method note for the marshal.** Every `reality_quote` above is from a file I opened or a
command whose output I read in this session. The path-existence sweep was run over all seven
documents mechanically (247 quoted paths extracted, each tested with `-e` and then its
basename located tree-wide), so DEAD_REFERENCE coverage is near-complete for my scope even
though claim coverage is not. That sweep is why D04-18 was found and why the 60-odd
bare-basename prose references were correctly not reported.

doc_lines_in_scope: 1515
claims_sampled: 35
claims_stale: 8
claims_current: 26
claims_unknown: 1
sampling_basis: specification-first per convention (l.1), then a mechanical sweep of all 247 quoted paths across the seven documents, then hand-checked the numbers a reader would ACT on, quote to the platform or count against (bet modes, costs, paytable, RTP, cap, book bytes and hashes, row counts, locale count, item counts) in preference to prose; the four documents whose claims are mostly status and narrative rather than arithmetic are knowingly under-sampled and named above.
scripts_executed: NONE
tree_clean_after: `git status --porcelain` returned exactly three lines, all untracked, no modified or deleted tracked file:
```
?? reports/qa/compliance_register/walk_shards/
?? reports/qa/doc_currency/
?? reports/qa/file_census/
```
`reports/qa/doc_currency/` is my own shard directory and is expected. The other two are other squads' output in this same wave. Nothing tracked was touched.
