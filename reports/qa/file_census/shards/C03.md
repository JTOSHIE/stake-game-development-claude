# C03: frontend-gates. Scope `frontend/scripts/`, 107 tracked files.

Australian English, no em dashes or en dashes.

## The CI-wired list, built by reading every `run:` line of `.github/workflows/checks.yml`

`grep -n "run:.*scripts/" .github/workflows/checks.yml` returns 28 lines. Three of them are
`scripts/qa/locked_paths_gate.mjs`, which is the ROOT `scripts/` tree and out of my scope.
The remaining 25 lines invoke **20 distinct files** under `frontend/scripts/`
(`working-directory: frontend`, at checks.yml:180 and its 20 siblings).

**Static gates job, 10 files:**
`typecheck_baseline.mjs` (:190), `dead_wiring_scan.mjs` (:196), `scan_wallet_floats.mjs`
(:202), `currency_scale_drift.test.mjs` (:209), `locale_completeness_check.mjs` (:225),
`a11y_social_terms_check.mjs` (:235), `dash_gate.mjs` (:257 self-test, :261 source, :453
dist), `machine_tell_gate.mjs` (:287 self-test, :291 source, :462 dist),
`asset_reference_gate.mjs` (:492 self-test, :496 real), `dist_hygiene_gate.mjs` (:500).

**Browser matrix job, 10 files, checks.yml:571-600:**
`layout_fit_gate.mjs`, `contrast_gate.mjs`, `splash_calm_gate.mjs`,
`turbo_intensity_gate.mjs`, `paytable_card_fill_gate.mjs`, `scrim_coverage_gate.mjs`,
`win_countup_steady_gate.mjs`, `max_win_hold_gate.mjs`, `bet_selector_gate.mjs`,
`preview_server_gate.mjs`.

**Other invocation mechanisms I looked for and found:**
- `frontend/package.json:12` `"qa:soak": "node scripts/qa_soak.mjs"`. One more reachable.
- `frontend/scripts/lib/{dismissOverlays,evidencePaths,previewServer}.mjs`, reached by
  relative import from many siblings (28 import lines), never by a `run:` line.
- The other two workflows: `publish-stake-engine.yml` and `validate-math.yml` reference
  `frontend/dist` and `frontend/package-lock.json` only, no `frontend/scripts` file.

**105 `.mjs` files exist in the scope** (107 tracked minus the two `scene/*.svg`).
20 CI-wired + 1 npm-wired + 3 imported helpers = **24 reachable. 81 top-level `.mjs`
files are invoked by nothing in this repository.**

## Convention (p): which gates can actually be seen to go red

`grep -ln "self-test\|selfTest\|SELF_TEST" frontend/scripts/*.mjs` returns 15 files. That
grep is not the whole answer, and finding out why is the most useful thing in this shard:
**`contrast_gate.mjs` seeds a real violation and asserts the gate goes red, and it does so
INLINE in the normal run rather than behind a `--self-test` flag**, so the flag grep misses
it. See NOTES for the near-miss this created.

| script | ships a seeded violation | CI exercises the red | in CI |
|---|---|---|---|
| `dash_gate.mjs` | yes, flagged | yes (:257) | yes |
| `machine_tell_gate.mjs` | yes, flagged | yes (:287) | yes |
| `asset_reference_gate.mjs` | yes, flagged | yes (:492) | yes |
| `splash_calm_gate.mjs` | yes, flagged | yes (:576) | yes |
| `turbo_intensity_gate.mjs` | yes, flagged | yes (:578) | yes |
| `paytable_card_fill_gate.mjs` | yes, flagged | yes (:580) | yes |
| `scrim_coverage_gate.mjs` | yes, flagged | yes (:582) | yes |
| `win_countup_steady_gate.mjs` | yes, flagged | yes (:584) | yes |
| `max_win_hold_gate.mjs` | yes, flagged | yes (:590) | yes |
| `bet_selector_gate.mjs` | yes, flagged | yes (:595) | yes |
| `preview_server_gate.mjs` | yes, flagged | yes (:600) | yes |
| `contrast_gate.mjs` | **yes, INLINE at :310-341** | **yes, unavoidably, every run** | yes (:574) |
| `locale_completeness_check.mjs` | yes, flagged | **NO, :225 runs it bare** | yes |
| `entry_continue_touch_gate.mjs` | yes, flagged | n/a | **NO** |
| `smallscreen_composition_gate.mjs` | yes, flagged | n/a | **NO** |
| `locale_prose_conformance.mjs` | yes, flagged | n/a | **NO** |
| `layout_fit_gate.mjs` | **NONE, verified two ways** | n/a | yes (:572) |
| `dead_wiring_scan.mjs` | none found | n/a | yes (:196) |
| `scan_wallet_floats.mjs` | none found | n/a | yes (:202) |
| `a11y_social_terms_check.mjs` | none found | n/a | yes (:235) |
| `dist_hygiene_gate.mjs` | none found | n/a | yes (:500) |
| `typecheck_baseline.mjs` | none found | n/a | yes (:190) |
| `currency_scale_drift.test.mjs` | none found | n/a | yes (:209) |

## Rows

| id | kind | path | mechanical_class | true_class | finding | evidence_quote | severity |
|---|---|---|---|---|---|---|---|
| C03-01 | CONTRADICTION | reports/qa/file_census/CENSUS_MECHANICAL.md:11-25 | NA | records | The brief asks me to name files whose mechanical class is WRONG, but the census publishes only a five-row aggregate plus the 27 UNCLASSIFIED names. There is no per-file class for any file in my scope, and `ls -la reports/qa/file_census/` shows only `CENSUS_MECHANICAL.md` and `orphan_candidates.txt`. Every `mechanical_class` cell below therefore reads NOT-PUBLISHED, which is not the same as agreement. | `\| `tooling` \| 173 \| gates, build config, CI, developer scripts \|` and at :22 "## The 27 the rules refused to guess at", listing 27 names, none of them under `frontend/scripts/` | MEDIUM |
| C03-02 | ORPHAN | .github/workflows/checks.yml:147-600 | NOT-PUBLISHED | tooling | 81 of the 105 `.mjs` files in `frontend/scripts/` are invoked by no CI step, no npm script and no other script. Built by enumerating every `run:` line of all three workflow files against `git ls-files frontend/scripts`, then searching for the mechanisms the reference scan cannot see. | `frontend/package.json:12` is the only npm entry point into this tree: `"qa:soak": "node scripts/qa_soak.mjs"`. `grep -rIln "node scripts/"` across the tree returns `frontend/package.json` and 40-odd scripts documenting their OWN usage in a comment. There is no aggregator. | MEDIUM |
| C03-03 | HIGH-finding | frontend/scripts/layout_fit_gate.mjs:26 | NOT-PUBLISHED | tooling | The only CI-wired browser gate with no seeded violation anywhere in it. Verified two ways: `grep -in "self-test\|selfTest\|SELF_TEST"` returns 0 lines, and `grep -in "seed\|SELF\|expect.*fail"` also returns 0. Its own header states the exact defect class it exists to catch, and nothing proves it can catch it. Convention (p): "A gate that has never been seen to fail is not evidence. It is a script that prints PASS." | header: `// A fix that satisfies the first by breaking the second fails here.` and `//   Run (from frontend/, after `npm run build`): node scripts/layout_fit_gate.mjs`; checks.yml:571-572 `- name: "browser: layout fit"` / `run: node scripts/layout_fit_gate.mjs`, with no `--self-test &&`, against :576 `run: node scripts/splash_calm_gate.mjs --self-test && node scripts/splash_calm_gate.mjs` | HIGH |
| C03-04 | HIGH-finding | .github/workflows/checks.yml:225 | NOT-PUBLISHED | tooling | `locale_completeness_check.mjs` ships a convention (p) self-test that CI never calls. It is also the one gate whose detection blind spots `KNOWN_OPEN.md` already records, so the gate most known to be partially blind is the one whose red run is not exercised on every push. | checks.yml:224-225 `working-directory: frontend` / `run: node scripts/locale_completeness_check.mjs`, no `--self-test`. The script quotes the convention at :446 `// "Every gate that claims a class closed ships a self-test that plants a` and at :341 warns `* self-test that RESTATES the rule it is checking proves nothing about the` | HIGH |
| C03-05 | ORPHAN | frontend/scripts/entry_continue_touch_gate.mjs:37 | NOT-PUBLISHED | tooling | A gate built to convention (p), documenting its own invocation, in no `run:` line of any workflow. Nothing runs it, so neither its green nor its red means anything today. | `//   node scripts/entry_continue_touch_gate.mjs --self-test   convention (p)` and `:50 const SELF_TEST = process.argv.includes('--self-test')` | MEDIUM |
| C03-06 | ORPHAN | frontend/scripts/smallscreen_composition_gate.mjs:63 | NOT-PUBLISHED | tooling | Same shape as C03-05. | `//   node scripts/smallscreen_composition_gate.mjs --self-test     convention (p)` and `:104 const SELF_TEST = argv.includes('--self-test')` | MEDIUM |
| C03-07 | ORPHAN | frontend/scripts/locale_prose_conformance.mjs:44 | NOT-PUBLISHED | tooling | Same shape as C03-05, and the one with the most careful self-test in the scope: it seeds the defect in BOTH forms and injects the resolver so the seed is not a restatement. Nothing runs it. | `// Convention (p): --self-test plants the exact defect in BOTH forms and the` , `:127 * `resolve(locale, key, mode)` is injected so the self-test can hand in a`, `:308 function selfTest() {` | MEDIUM |
| C03-08 | STALE | frontend/scripts/tile_master_ingest.mjs:20 | NOT-PUBLISHED | tooling | The generator of the published tile master reads its input from outside the repository, in one machine's Downloads folder. The committed tile cannot be regenerated by anyone else, which is the situation convention (m) exists to prevent. CLAUDE.md's own asset test point 4 requires "Source path, source hash, shipped hash" be recorded; the source path recorded here is unreachable. | `const SOURCE = '/Users/jt/Downloads/we-roll-spinners_future-spinner_tile.png'` beside `:21 const TILE = '/Users/jt/math-sdk/design-system/brand/tile'` | HIGH |
| C03-09 | STALE | frontend/scripts/provider_mark_build.mjs:47-49 | NOT-PUBLISHED | tooling | 24 of the 105 scripts hardcode `/Users/jt/...`, so none can run on a CI runner or in a fresh clone. None of the 24 is CI-wired, so nothing is red today; the cost is that the brand artefacts several of them produced are not reproducible from the repository. Full list in NOTES. | `const BRAND = '/Users/jt/math-sdk/design-system/brand/provider_mark'` / `const DELIVERY = '/Users/jt/math-sdk/design-system/brand/delivery'` / `const PROOFS = '/Users/jt/math-sdk/reports/screens/provider-mark'` | MEDIUM |
| C03-10 | ORPHAN | frontend/scripts/evidence_buy.mjs:17 | NOT-PUBLISHED | tooling | Orphan candidate CONFIRMED. Nothing invokes it, and `grep -rIn "evidence_buy" .` outside the file returns only `orphan_candidates.txt:514`. It writes straight into a committed evidence directory of 5 tracked files, which is the convention (h.1) pattern by name. | `const OUT = '/Users/jt/math-sdk/reports/screens/buy-dialogs-2026-07-25'`; `git ls-files reports/screens/buy-dialogs-2026-07-25` returns 5 | MEDIUM |
| C03-11 | ORPHAN | frontend/scripts/evidence_celebrations.mjs:23 | NOT-PUBLISHED | tooling | As C03-10. Only reference outside the file is `orphan_candidates.txt:515`. Target holds 4 tracked files. | `const OUT = '/Users/jt/math-sdk/reports/screens/celebrations-2026-07-25'` | MEDIUM |
| C03-12 | ORPHAN | frontend/scripts/evidence_feature_flow.mjs:16 | NOT-PUBLISHED | tooling | As C03-10. Only reference outside the file is `orphan_candidates.txt:516`. Target holds 7 tracked files. | `const OUT = '/Users/jt/math-sdk/reports/screens/feature-flow-2026-07-25'` | MEDIUM |
| C03-13 | ORPHAN | frontend/scripts/evidence_layouts.mjs:8 | NOT-PUBLISHED | tooling | As C03-10. Only reference outside the file is `orphan_candidates.txt:517`. Target holds 6 tracked files. | `const OUT = '/Users/jt/math-sdk/reports/screens/layouts-2026-07-25'` | MEDIUM |
| C03-14 | NOT_ORPHAN | frontend/scripts/a11y_social_proof.mjs:59 | NOT-PUBLISHED | tooling | Orphan candidate REFUTED as dead. Uninvoked, but its committed OUTPUT is the cited evidence for a merged compliance row, so it is the provenance of a submission claim rather than dead weight. Deleting it would orphan the JSON. | script: `writeFileSync(join(OUT_DIR, 'a11y_social_proof_2026-07-25.json'), JSON.stringify(result, null, 2))`; `docs/records/reviews/REVIEW_TRACKER.md:99` TR-012: "Runtime proof `reports/qa/a11y_social_proof_2026-07-25.json`: social mode 0 restricted hits, real-money mode correctly retains bet wording." | LOW |
| C03-15 | NOT_ORPHAN | frontend/scripts/bet_ladder_proof.mjs:78 | NOT-PUBLISHED | tooling | REFUTED as C03-14. | script: `writeFileSync(join(OUT, 'bet_ladder_proof_2026-07-25.json'), JSON.stringify(result, null, 2))`; `REVIEW_TRACKER.md:103` TR-013: "Proof `reports/qa/bet_ladder_proof_2026-07-25.json`: both surfaces step 100 to 200 to 100 on a JPY-shaped ladder sharing no value with the hardcoded one" | LOW |
| C03-16 | NOT_ORPHAN | frontend/scripts/rg_enforcement_proof.mjs:99 | NOT-PUBLISHED | tooling | REFUTED as C03-14. | script: `writeFileSync(join(OUT, 'rg_enforcement_proof_2026-07-25.json'), JSON.stringify(result, null, 2))`; `REVIEW_TRACKER.md:106` TR-015: "Runtime proof `reports/qa/rg_enforcement_proof_2026-07-25.json` drives the real controls" | LOW |
| C03-17 | NOT_ORPHAN | frontend/scripts/owner_audit_v4_proof.mjs:6 | NOT-PUBLISHED | tooling | REFUTED as unreferenced: another census squad's shard names it as one of seven scripts holding the `interface-guide` testid. Still uninvoked, so the reference is a citation rather than a mechanism, but the scan's zero was wrong. | script: `// Run (from frontend/): node scripts/owner_audit_v4_proof.mjs`; `reports/qa/compliance_register/walk_shards/W02.md:23`: "returns `regen_interface_guide_icons.mjs`, `interface_guide_icon_proof.mjs`, `mini_player_proof.mjs`, `owner_audit_v4_proof.mjs` ... **None of those seven appears in any `run:` line of `.github/workflows/checks.yml`**" | LOW |
| C03-18 | NOT_ORPHAN | frontend/scripts/popout_conformance.mjs | NOT-PUBLISHED | tooling | Not an orphan candidate, recorded because it is the ONE uninvoked script whose absence from CI is deliberate and documented in checks.yml itself. It is the shape the other 80 should have been given. | checks.yml:82-86 `# NOT ADDED HERE ON PURPOSE: frontend/scripts/popout_conformance.mjs (R14). It` / `# asserts a REAL Playwright click at Stake's 400x225 mini-player size, which needs` / `# a browser and a dev server, so it stays local ... Its result is committed as` / `# reports/qa/popout_conformance_2026-07-27.json.` | LOW |
| C03-19 | NOT_ORPHAN | frontend/scripts/lib/previewServer.mjs | NOT-PUBLISHED | tooling | Recorded so the marshal has the mechanism for the three `lib/` helpers: relative import from siblings only, never a `run:` line, so a `run:`-line census would read them as dead. | `frontend/scripts/build_diet_verify.mjs:20 import { startStaticServer, assertNoSurvivors } from './lib/previewServer.mjs'`; `contrast_gate.mjs:49` and `bet_selector_gate.mjs:56` import the same pair | LOW |
| C03-20 | MISCLASSIFIED | frontend/scripts/scene/scene_car.svg, frontend/scripts/scene/scene_character.svg | NOT-PUBLISHED | skin | Two brand vector masters living inside a scripts tree. They are asset masters, not tooling: CLAUDE.md's Assets section makes SVG masters the source of shipped art, and every other master in the project lives under `design-system/`. Neither is an orphan candidate, but the only thing in the tree that names them is a design plan. | `docs/design/COHESION_PASS_PLAN.md:114-115` lists them as masters: `frontend/scripts/scene/scene_character.svg    5.0 KB` / `frontend/scripts/scene/scene_car.svg          3.8 KB` | LOW |
| C03-21 | CONTRADICTION | docs/design/COHESION_PASS_PLAN.md:135-138 | NA | records | CLAUDE.md asserts every visual asset is produced from SVG masters via a deterministic pipeline. The design plan states flatly that nothing in the repository rasterises the two scene masters, and my own search agrees: `grep -rIln "scene/scene_character\|scene/scene_car"` returns only that plan and this shard, and `scene_proof.mjs` copies an existing PNG rather than rendering one. So the two masters in my scope are disconnected from the shipped art. | plan: "## Blocker to clear first: there is no scripted render step" / "`scene_proof.mjs` only COPIES the existing PNG; it does not produce it. Nothing in the repository rasterises `scene_character.svg` to `scene_character.png`". Corroborated at `frontend/scripts/scene_proof.mjs:86-88`: `const charSrc = join(FRONTEND, 'public/assets/themes/future-spinner/ui/scene_character.png')` / `const charDst = join(SCREENS, 'scene_character_render.png')` / `copyFileSync(charSrc, charDst)` | MEDIUM |
| C03-22 | MISCLASSIFIED | frontend/screens/fsmenu_base.png and 13 siblings | UNCLASSIFIED | evidence | The census handed 23 PNGs under `frontend/screens/` to a squad as UNCLASSIFIED and asked whether they are evidence in the wrong place. My scope holds their GENERATORS, and the answer is yes: they are Playwright proof screenshots written by five scripts in `frontend/scripts/`, so their true class is `evidence` and convention (h) puts evidence under `reports/screens/<pass>/`. Generator map in NOTES. | `frontend/scripts/fsmenu_proof.mjs:19-20` `const ROOT = join(__dirname, '..')` / `const OUT = join(ROOT, 'screens')`, then `:57 await page.screenshot({ path: join(OUT, 'fsmenu_base.png') })`. Same two lines at `hud_reskin_proof.mjs:16-17` and `paytable_reskin_proof.mjs:16-17`. | MEDIUM |
| C03-23 | CLEAN_NOTE | frontend/scripts/contrast_gate.mjs:310-341 | NOT-PUBLISHED | tooling | Recorded as a REFUTATION of my own first-pass row, because the refutation is the useful part. `contrast_gate.mjs` looks like a convention (p) breach by the flag grep and by checks.yml, and is not: it seeds the violation inline and asserts the red on every run, which is stronger than a flag. Any later squad grepping for `--self-test` will make the same mistake. | `// ── SEEDED VIOLATION, convention (p) ────────────────────────────────────` / `// to fail. The seed is the real form: the label's colour is pushed toward` / `:339 check('the gate goes RED on a seeded low-contrast label',` / `:340 seeded !== null && seeded.worstRatio < AA_NORMAL,` | LOW |
| C03-24 | CLEAN_NOTE | frontend/scripts/qa_soak.mjs | NOT-PUBLISHED | tooling | The one script in the scope reachable by npm and not by CI. Recorded so it is not counted among the 81 by a later pass grepping only checks.yml. | `frontend/package.json:12` `"qa:soak": "node scripts/qa_soak.mjs"` | LOW |

## NOTES

**The near-miss, first, because it is the most transferable thing here.** My first-pass shard
carried a HIGH row saying `layout_fit_gate.mjs` AND `contrast_gate.mjs` ship no seeded
self-test, on the strength of `grep -ln "self-test\|selfTest\|SELF_TEST"` returning neither,
and of checks.yml running both bare while eight sibling legs run `--self-test &&` first. Two
independent-looking signals agreeing. They were not independent: both keyed on the FLAG, and
`contrast_gate.mjs` seeds its violation inline in the normal run, so no flag exists to find.
A second grep on `seed` returned 10 lines in `contrast_gate.mjs` and 0 in
`layout_fit_gate.mjs`, and the row halved. This is convention (l.4) in miniature and it is
recorded as C03-23 so the marshal can see the corrected shape rather than the corrected
conclusion alone. `layout_fit_gate.mjs` survives both greps at zero and is the real finding.

**Why every `mechanical_class` cell reads NOT-PUBLISHED.** I read
`reports/qa/file_census/CENSUS_MECHANICAL.md` in full, 118 lines, and listed its directory.
It gives a five-row aggregate and names the 27 UNCLASSIFIED files. It ships no per-file
classification, and there is no second artefact beside it. So I can say a rule must have
matched every file in my scope, because none of the 107 appears in the 27, but I cannot quote
what class it assigned. The marshal should not read any squad's "agrees with mechanical" as
corroboration of a per-file class that was never published. Noted also that
`reports/qa/file_census/` is itself untracked, per the porcelain output below.

**The 81, and why "uninvoked" is not "dead".** The reference scan flagged only 8 files in my
scope because the other 97 have their basename mentioned somewhere, usually in
`docs/records/reviews/REVIEW_TRACKER.md` or an archived session report. A mention is a
citation of a past run, not an invocation mechanism. I looked for each mechanism the scan
cannot see: `grep -rIln "node scripts/"` across the tree, which returns `frontend/package.json`
and about forty scripts documenting their own usage in a header comment and no aggregator;
every `run:` line of all three workflow files; the whole `scripts` block of
`frontend/package.json`; and relative imports between scripts, which found only the three
`lib/` helpers. There is no glob loader and no dynamic import in this tree. So the 81 are
genuinely one-shot historical proof generators whose value is the committed output, and the
right disposition for each is a judgement I did NOT make.

**The two conventions this scope is failing, kept apart because they have different owners.**
Convention (p) fails at two places and only two: `layout_fit_gate.mjs`, CI-wired with no
seeded violation at all (C03-03), and `locale_completeness_check.mjs`, whose self-test exists
and is never called (C03-04). Convention (h.1) fails at the six `evidence_*.mjs` scripts,
which write directly into committed evidence directories; CLAUDE.md already names
`anticipation_proof.mjs`, `layout_fit_gate.mjs` and `contrast_gate.mjs` as instances of that
class, so the six extend a recorded class rather than opening a new one, and none of the six
is invoked so none can fire by accident.

**Generator map for the `frontend/screens/` PNGs**, which resolves 14 of the census's 27
UNCLASSIFIED files from my side of the tree. `fsmenu_proof.mjs` writes `fsmenu_base.png`,
`fsmenu_open.png`, `fsmenu_betmodes.png`. `hud_reskin_proof.mjs` writes `hud_reskin_base.png`,
`hud_reskin_overdrive.png`, `hud_reskin_paytable_overdrive.png`. `paytable_reskin_proof.mjs`
writes `fs_paytable_reskin_top.png`, `fs_paytable_reskin_symbols.png`,
`fs_paytable_reskin_overdrive.png`. `scene_proof.mjs` writes `scene_after.png` and
`scene_character_render.png`. `symbol_life_proof.mjs` defaults to `screens/symbol_after.png`
(`:22 const peakArg = process.argv[2] ?? 'screens/symbol_after.png'`) and its siblings
`symbol_before.png` and `symbol_after_idle.png` sit beside it. I did NOT trace
`frontend/screens/c1_big.png`, `c1_epic.png`, `c1_max.png`, `c1_mega.png`,
`fs_paytable_reskin_rtp.png`, `rg_autoplay_menu.png` or `rg_session_panel.png` to a
generator, so those seven remain UNKNOWN from this scope.

**Twenty-four absolute-path scripts.** `grep -rln "/Users/jt" frontend/scripts/`:
`background_candidate_proof.mjs`, `background_local_testing_verify.mjs`, `evidence_buy.mjs`,
`evidence_celebrations.mjs`, `evidence_feature_flow.mjs`, `evidence_layouts.mjs`,
`evidence_menu.mjs`, `evidence_rules.mjs`, `fsmenu_proof.mjs`, `hud_reskin_proof.mjs`,
`paytable_reskin_proof.mjs`, `provider_logo_derivation.mjs`, `provider_mark_build.mjs`,
`provider_mark_compare.mjs`, `provider_mark_derive.mjs`, `provider_mark_ingest_e.mjs`,
`provider_mark_ingest_f.mjs`, `provider_mark_ingest_g.mjs`,
`published_tile_geometry_survey.mjs`, `scene_proof.mjs`, `symbol_life_proof.mjs`,
`tile_delivery_build.mjs`, `tile_layer_derivation.mjs`, `tile_master_ingest.mjs`. Two of them
point outside the repository entirely: `tile_master_ingest.mjs:20` at
`/Users/jt/Downloads/`, and `published_tile_geometry_survey.mjs:27` at
`/Users/jt/math-sdk/docs/stake-engine-live/2026-07-26`, which is inside the repo but
absolute. None of the 24 is CI-wired.

**Cross-reference against `KNOWN_OPEN.md`.** TR-112 is the nearest row, "Repo hygiene
(unreferenced sounds, npm check warnings)", OPEN and LOW. My C03-02 is the same FAMILY of
defect in a different tree, and TR-112's own text names sounds and npm warnings rather than
scripts, so I have recorded it as new rather than as KNOWN(TR-112); the marshal may wish to
fold it in. The KNOWN_OPEN blind-spot note on `locale_completeness_check.mjs` is about what
its DETECTOR can see, which is a different claim from C03-04's, that its self-test is never
run, so C03-04 is not a re-discovery of that note.

**What I did NOT establish, stated as UNKNOWN rather than guessed.** Whether each of the 81
uninvoked scripts should be deleted, archived or wired: not judged, and it needs an owner
ruling rather than a census. Whether the committed outputs of the six `evidence_*` scripts
still match what those scripts would produce today: not checked, and it cannot be checked
without running them, which this pass forbids. Whether the four `c1_*.png` files and three
others under `frontend/screens/` have a generator anywhere: searched my scope only, not found.
Whether the 15 `provider_mark_*` and `tile_*` scripts are superseded by each other: not
traced, they read as an iterative series (`ingest_e`, `ingest_f`, `ingest_g`) but I did not
open enough of them to say which is current, and guessing a supersession order from filename
suffixes is exactly the inference this wave forbids.

**Sampling basis.** I did not open all 107 files line by line inside the call budget. Every
one of the 105 `.mjs` files was read by CONTENT at least four times, through whole-tree greps
for `self-test|selfTest|SELF_TEST`, `/Users/jt`, `./lib/`, and screenshot output paths, so no
file in the scope is unexamined for the four questions the brief asked. 28 files were opened
at specific lines and are quoted above. The files I did not open at any line are the
single-purpose proof scripts in the middle of the 81, where the invocation question was
already answered mechanically by the workflow and package.json enumeration and opening each
would not have changed the answer.

files_in_scope: 107
files_examined: 107 read by content grep for all four census questions; 28 opened at named lines and quoted
orphan_candidates_in_scope: 8
orphans_confirmed: 4
orphans_refuted: 4
scripts_executed: NONE
tree_clean_after: `?? reports/qa/compliance_register/walk_shards/` and `?? reports/qa/file_census/` (my own shard's parent tree, plus another squad's shard tree; no tracked file modified)
