# V03: adversarially verify 7 findings (STREAM batch 1, seat 3 of 3)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C001  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: `      - name: asset references resolve in dist` / `        working-directory: frontend` / `        run: node scripts/asset_reference_gate.mjs` CI_GATE (partial, covers only the MISSING-file half)
- **path cited**: `frontend/src/lib/components/GameGrid.svelte:499`
- **raised by**: REQ-053 (W01)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `      - name: asset references resolve in dist` / `        working-directory: frontend` / `        run: node scripts/asset_reference_gate.mjs` CI_GATE (partial, covers only the MISSING-file half)

## S2-C002  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: `const LIVE_QS = (mode, event) =>` / `` `replay=true&game=0e872280-c94a-4bcf-a55b-b649c4a02fc0&version=1` + `` ... (no `sessionID`, no `session`) then `await page.locator('.start-replay').waitFor({ state: 'visible', timeout: 15000 })` `* No session is required, replay URLs are publicly shareable.` / `const response = await fetch(url, { method: 'GET' })` ; App: `// Skip all RGS initialisation in replay mode, ReplayMode handles its own flow` / `if (isReplay) return`
- **path cited**: `frontend/src/lib/services/replayService.ts:122-131 and frontend/src/App.svelte:1169-1170`
- **raised by**: REQ-079 (W04)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `const LIVE_QS = (mode, event) =>` / `` `replay=true&game=0e872280-c94a-4bcf-a55b-b649c4a02fc0&version=1` + `` ... (no `sessionID`, no `session`) then `await page.locator('.start-replay').waitFor({ st

## S2-C003  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: Nothing asserts the path shape. No test file imports `fetchReplay` (checked all 20). The only harness intercepts with a wildcard, `await page.route('**/bet/replay/**', (route) => route.fulfill({`, which fulfils any segment order, so it cannot see a wrong path. `` const url = `${p.rgsUrl}/bet/replay/${p.game}/${p.version}/${p.mode}/${p.event}` `` / `const response = await fetch(url, { method: 'GET' })`
- **path cited**: `frontend/src/lib/services/replayService.ts:125,131`
- **raised by**: REQ-083 (W04)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Nothing asserts the path shape. No test file imports `fetchReplay` (checked all 20). The only harness intercepts with a wildcard, `await page.route('**/bet/replay/**', (route) => route.fulfill({`, whi

## S2-C004  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: `` await page.goto(`http://localhost:${PORT}/?${LIVE_QS(mode, event)}`) `` then, with no interaction in between, `await page.locator('.start-replay').waitFor({ state: 'visible', timeout: 15000 })` `onMount(async () => {` / `const p = parseReplayParams()` / `const r = await fetchReplay(p)` / `phase = 'ready'` (no click or input anywhere in the path)
- **path cited**: `frontend/src/lib/components/ReplayMode.svelte:84-98`
- **raised by**: REQ-085 (W04)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `` await page.goto(`http://localhost:${PORT}/?${LIVE_QS(mode, event)}`) `` then, with no interaction in between, `await page.locator('.start-replay').waitFor({ state: 'visible', timeout: 15000 })` `on

## S2-C005  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: Animation is the real pipeline: `      // Drive the reel spin animation (identical pipeline to live game)` / `      isSpinning.set(true)` / `      if (gridRef && board.length > 0) {` / `        await gridRef.animateSpin(board)`, plus `<FreeSpinsPresentation script={featureScript} active={featureActive} on:complete={onFeatureComplete} />` and `<MaxWinCelebration show={$isWincap} on:collect={handleWincapCollect} />`. Audio is reduced: `if (isReplay) return` at `App.svelte:1170` returns from `onMount` BEFORE `    playBGM()` (line 1212) and before `      warmUpAudio()` (line 1239), and the win sound is only ever called from live paths, `playWin(bet > 0 ? $winMultiplier : 0)` (line 733, buy flow) and `playWin(bet > 0 ? win / bet : 0)` (lines 1476 and 1497, live spin). `ReplayMode.svelte` imports no `soundService` symbol at all. Wired: `          - name: "browser: max-win hold"` / `            run: node scripts/max_win_hold_gate.mjs --self-test && node scripts/max_win_hold_gate.mjs`. The assertion: `    name: 'replay raises its terminal splash after the round completes, not before',` / `    check: (s) => !/isWincap\.set\(wincapNow\)\n\n?\s*(\/\/ Let win-line\
- **path cited**: `frontend/src/lib/components/ReplayMode.svelte:211-215 and 299-303 for the animation half; the audio half is what is missing, at frontend/src/App.svelte:1170 against 1212 and 1239`
- **raised by**: REQ-095 (W05)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Animation is the real pipeline: `      // Drive the reel spin animation (identical pipeline to live game)` / `      isSpinning.set(true)` / `      if (gridRef && board.length > 0) {` / `        await 

## S2-C006  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: The bet cost lives inside the ready-phase button ONLY: `      {#if phase === 'ready'}` ... `            {mode === 'social' ? 'Play' : 'Bet'}: <strong>{formatBalance(Math.round(baseBet * CURRENCY_SCALE), params.currency, params.lang)}</strong>` / `            {#if showCostMultiplier}` / `              × {response.costMultiplier} {mode === 'social' ? '=' : 'cost ='}` / `              <strong>{formatBalance(Math.round(totalSpent * CURRENCY_SCALE), params.currency, params.lang)}</strong>`. At results time that whole block is gone, replaced by `      {:else if phase === 'complete'}` / `        <button class="replay-btn play-again" on:click={playAgain}>` / `          {t(params.lang as Locale, 'replayAgain', mode)}`, and the only figures left are the payout multiplier and win amount from `WinPod.svelte:25-26`, `    <div class="zone-mult">{multText}</div>` / `    <div class="zone-amt">{amtText}</div>`. The completed-replay frame shows `MULTIPLIER 390.0x`, `WIN 3900.00` and `MEGA WIN!!! €3,900.00`, and `REPLAY AGAIN` where the bet figure used to be: the bet cost is absent from the results view. Two of the three figures are present at results time, the third only before the press.
- **path cited**: `frontend/src/lib/components/ReplayMode.svelte:318-334`
- **raised by**: REQ-096 (W05)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: The bet cost lives inside the ready-phase button ONLY: `      {#if phase === 'ready'}` ... `            {mode === 'social' ? 'Play' : 'Bet'}: <strong>{formatBalance(Math.round(baseBet * CURRENCY_SCALE

## S2-C007  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: Looked in: `.github/workflows/checks.yml` (all 40 static-gate steps and all 10 browser matrix legs, only replay step is `replay rounds` at :420-422); `frontend/src/lib/services/replayRounds.test.ts` (its source scan asserts only `!/ev\.type === 'board'/`, `'win'`, `'scatter'`, `!/state\?\.board/` and the interpreter-call shape, nothing about `phase === 'error'`); `frontend/scripts/replay_blocker_proof.mjs` (its only route stub is `route.fulfill({ contentType: 'application/json', body: ... })`, a SUCCESS response; no failing-fetch pass exists); grep for `replayFailed`/`replayLoadError`/`replayPlaybackError`/`replay-status.error` across `frontend/src` and `frontend/scripts` returns only the 16 locale definitions and the component itself catch: `} catch (e: any) {` / `error = e?.message ?? $tr('replayLoadError')` / `phase = 'error'` / `replayPhase.set('error')` / `replayError.set(error)`; render: `{:else if phase === 'error'}` / `<div class="replay-status error">` / `<div class="error-title">{$tr('replayFailed')}</div>` / `<div class="error-detail">{error}</div>`; thrower: `` throw new Error(`Replay fetch failed (${response.status} ${response.statusText}). URL: ${url}`,) ``
- **path cited**: `frontend/src/lib/components/ReplayMode.svelte:99-104 (fetch catch) and :291-294 (rendered state); thrower at frontend/src/lib/services/replayService.ts:133-137`
- **raised by**: REQ-098 (W06)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Looked in: `.github/workflows/checks.yml` (all 40 static-gate steps and all 10 browser matrix legs, only replay step is `replay rounds` at :420-422); `frontend/src/lib/services/replayRounds.test.ts` (

