# V20: adversarially verify 1 findings (COMPLIANCE batch 11, seat 1 of 1)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C219  [LOW / COMPLIANCE / NOT_MET]

- **claim**: currency.ts: `const decimals = ZERO_DECIMAL.has(code) ? 0 : 2`; HudOverlay: `$: winLabel = formatBalance(Math.round(displayedWinAmount * CURRENCY_SCALE), $currencyCode \ `frontend/src/lib/utils/currency.ts:211` and `frontend/src/lib/components/HudOverlay.svelte:366`
- **path cited**: `frontend/src/lib/utils/currency.ts:211 and frontend/src/lib/components/HudOverlay.svelte:366`
- **raised by**: REQ-126 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: currency.ts: `const decimals = ZERO_DECIMAL.has(code) ? 0 : 2`; HudOverlay: `$: winLabel = formatBalance(Math.round(displayedWinAmount * CURRENCY_SCALE), $currencyCode \ `frontend/src/lib/utils/curren

