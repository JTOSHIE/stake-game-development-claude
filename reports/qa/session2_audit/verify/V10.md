# V10: adversarially verify 8 findings (COMPLIANCE batch 1, seat 1 of 1)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C022  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: ". Against `COMPLIANCE_WATCH.md:19-20`: "- **Original IP:** verified. Original designs, produced in-house from vector masters. No pre-purchased or third-party licensed content." `design-system/brand/tile/GENERATION_NOTE_composed_master.md:7` and `design-system/brand/GENERATION_NOTE_background.md:11`
- **path cited**: `design-system/brand/tile/GENERATION_NOTE_composed_master.md:7 and design-system/brand/GENERATION_NOTE_background.md:11`
- **raised by**: REQ-138 (W12), REQ-141 (W12)
- **corroboration**: 2 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: ". Against `COMPLIANCE_WATCH.md:19-20`: "- **Original IP:** verified. Original designs, produced in-house from vector masters. No pre-purchased or third-party licensed content." `design-system/brand/t

## S2-C023  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: Nothing. `.github/workflows/checks.yml` has no step whose subject is asset ORIGIN; `frontend/scripts/asset_reference_gate.mjs` only asserts referenced paths exist in `dist`, and `dist_hygiene_gate.mjs` only asserts documentation is absent. No gate, test or committed capture enumerates the shipped asset set against web-sdk sample assets. `design-system/brand/GENERATION_NOTE_background.md:11-24` and `frontend/src/lib/config/themes.ts:34`
- **path cited**: `design-system/brand/GENERATION_NOTE_background.md:11-24 and frontend/src/lib/config/themes.ts:34`
- **raised by**: REQ-052 (W01)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Nothing. `.github/workflows/checks.yml` has no step whose subject is asset ORIGIN; `frontend/scripts/asset_reference_gate.mjs` only asserts referenced paths exist in `dist`, and `dist_hygiene_gate.mjs

## S2-C024  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: Captured output: `"dom": { "USD": { "balance": "BALANCE\n$100.00", "bet": "BET\n$1.00", ... }, "JPY": { "balance": "BALANCE\n¥100" ...` . `currency_conformance.mjs` appears nowhere in `checks.yml`. The wired layout gate reads the same selector but cannot fail on a missing readout: `const el = document.querySelector(q)` / `if (!el) continue` 'USD')` and `<div class="fs-box fs-balance fs-plate" data-testid="hud-balance">` / `<span class="fs-label">{$tr('balance')}</span>` / `<span class="fs-value cyan" use:autofitText={balanceLabel}>{balanceLabel}</span>`
- **path cited**: `frontend/src/lib/components/HudOverlay.svelte:287 and :859-863 (plus :387-389, :603-605, :684-686 for portrait, mobile and compact)`
- **raised by**: REQ-067 (W02)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Captured output: `"dom": { "USD": { "balance": "BALANCE\n$100.00", "bet": "BET\n$1.00", ... }, "JPY": { "balance": "BALANCE\n¥100" ...` . `currency_conformance.mjs` appears nowhere in `checks.yml`. Th

## S2-C025  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: Wired gates touch `[data-testid="hud-win"]` only as `readouts.push({ id: q, scrollW: t.scrollWidth, clientW: t.clientWidth, clipped: t.scrollWidth - t.clientWidth > EPS ... })` and `ok(win0 === win1, \`WIN unchanged across the hold ("${win0}" then "${win1}")\`)`; both pass when the figure is absent or empty. No wired assertion that a non-zero payout renders its final amount 'USD')` fed by `$: if ($winAmount !== lastWinAmountSeen) { ... if ($winAmount > previous) { startWinCountUp($winAmount, $winMultiplier) } else { ... displayedWinAmount = $winAmount }`
- **path cited**: `frontend/src/lib/components/HudOverlay.svelte:366 and :328-339`
- **raised by**: REQ-068 (W02)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Wired gates touch `[data-testid="hud-win"]` only as `readouts.push({ id: q, scrollW: t.scrollWidth, clientW: t.clientWidth, clipped: t.scrollWidth - t.clientWidth > EPS ... })` and `ok(win0 === win1, 

## S2-C026  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: Gate text: "it asserts `isAutoPlay.set(true)` appears in EXACTLY ONE place in the whole frontend, and that occurrence sits inside a function reachable only from a real `on:click` handler in the same file. If a future change ever added a second path to start autoplay (a keyboard shortcut, a URL param, an automatic continuation), this fails loudly". Nothing in `checks.yml` runs it `function startAuto(requested: number) {` ... `autoPlayCount.set(count)` / `isAutoPlay.set(true)` / `dispatch('spin')`; and `<button class="auto-menu-item" role="menuitem" on:click={() => startAuto(n)}>{n}</button>`
- **path cited**: `frontend/src/lib/components/HudOverlay.svelte:179-195, click sites :515, :751, :957`
- **raised by**: REQ-072 (W03)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Gate text: "it asserts `isAutoPlay.set(true)` appears in EXACTLY ONE place in the whole frontend, and that occurrence sits inside a function reachable only from a real `on:click` handler in the same f

## S2-C027  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: Frontend: `{ name: 'H1', file: 'h1', pays: [null, null, 1.5, 6, 22] }` ... `{ name: 'L3', file: 'l3', pays: [null, null, 0.08, 0.20, 0.65] }`, under the comment "matching the validated maths in games/future_spinner/game_config.py exactly". Maths: `(5, "H1"): 22.0, (4, "H1"): 6.0, (3, "H1"): 1.5,` ... `(5, "L3"): 0.65, (4, "L3"): 0.20, (3, "L3"): 0.08,` `rules_conformance_proof.mjs:3-4` is a capture, not an assertion: "Opens the paytable/rules modal (base state and mid-Overdrive) and screenshots every section required by the approval checklist", and its only action is `await page.screenshot({ path: join(OUT_DIR, 'paytable-full-base.png'), fullPage: true })`. No artefact compares the two tables
- **path cited**: `frontend/src/lib/components/PaytableModal.svelte:66-83 against games/future_spinner/game_config.py:119-127`
- **raised by**: REQ-074 (W03)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Frontend: `{ name: 'H1', file: 'h1', pays: [null, null, 1.5, 6, 22] }` ... `{ name: 'L3', file: 'l3', pays: [null, null, 0.08, 0.20, 0.65] }`, under the comment "matching the validated maths in games/

## S2-C028  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: searched all 20 test files and `.github/workflows/checks.yml` for `parseReplayParams`: the only repository references are `App.svelte:20`, `App.svelte:41`, `ReplayMode.svelte:4`, `ReplayMode.svelte:86`, two prose comments in `currency.ts:96` and `currency.test.ts:51`, and one in the locked `rgsService.ts:491` !rgsUrlRaw) { throw new Error('Replay mode requires game, version, mode, event, and rgs_url query parameters.')` / `return { replay: true, game, version, mode, event, rgsUrl, currency, amount, lang: params.get('lang') ?? 'en', device: (params.get('device') ?? 'desktop') as 'mobile' \
- **path cited**: `frontend/src/lib/services/replayService.ts:56-118`
- **raised by**: REQ-077 (W03)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: searched all 20 test files and `.github/workflows/checks.yml` for `parseReplayParams`: the only repository references are `App.svelte:20`, `App.svelte:41`, `ReplayMode.svelte:4`, `ReplayMode.svelte:86

## S2-C029  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: `` &mode=${mode}&event=${event}&rgs_url=rgs.stake-engine.com `` (the full six-parameter live shape, then the script requires `.start-replay` to become visible, which the mandatory-parameter throw would prevent) !rgsUrlRaw) {` / `` const url = `${p.rgsUrl}/bet/replay/${p.game}/${p.version}/${p.mode}/${p.event}` ``
- **path cited**: `frontend/src/lib/services/replayService.ts:59-71,125`
- **raised by**: REQ-080 (W04)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `` &mode=${mode}&event=${event}&rgs_url=rgs.stake-engine.com `` (the full six-parameter live shape, then the script requires `.start-replay` to become visible, which the mandatory-parameter throw woul

