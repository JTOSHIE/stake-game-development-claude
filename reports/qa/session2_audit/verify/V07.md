# V07: adversarially verify 7 findings (STREAM batch 3, seat 1 of 3)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C015  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: currency.ts: `const decimals = ZERO_DECIMAL.has(code) ? 0 : 2` / `    return new Intl.NumberFormat(localeTag, {` / `      minimumFractionDigits: decimals,` / `      maximumFractionDigits: decimals,`; WinPod: `$: amtText  = $winAmount > 0` / `    ? formatBalance(Math.round($winAmount * CURRENCY_SCALE), $currencyCode \ no assertion requires an in-game win readout to be unrounded; the win-surface gate that exists (`win_countup_steady_gate.mjs`, `checks.yml:583-584`) is about digit-box jitter, not value fidelity
- **path cited**: `frontend/src/lib/utils/currency.ts:211-220 and frontend/src/lib/components/WinPod.svelte:14-15`
- **raised by**: REQ-127 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: currency.ts: `const decimals = ZERO_DECIMAL.has(code) ? 0 : 2` / `    return new Intl.NumberFormat(localeTag, {` / `      minimumFractionDigits: decimals,` / `      maximumFractionDigits: decimals,`; 

## S2-C016  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: Q-27: `**The Q-13 fix took the font line out of the Vite scaffold block and left the rest of the block.** Still at HEAD in \`frontend/src/app.css\`: \`color-scheme: light dark\`, \`background-color: #242424\`, \`a { color: #646cff }\`, \`a:hover { #535bf2 }\`, and \`body { display: flex; place-items: center }\`. Every one is a starter default.` marked `**OPEN.** The link colours are the sharpest, being a stock indigo in a cyan and magenta game.` `button:focus,` / `button:focus-visible {` / `  outline: 4px auto -webkit-focus-ring-color;` / `}`; and the charter's own B7 row reads `Partly enforced. The gate catches glyphs outside the Orbitron subset; declared font stacks that name no brand face are listed in section 4 with their dispositions.`
- **path cited**: `frontend/src/app.css:160-163, plus docs/QUALITY_CHARTER.md:66-72 as the project's own checkable rendering of the bar`
- **raised by**: REQ-042 (W21)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Q-27: `**The Q-13 fix took the font line out of the Vite scaffold block and left the rest of the block.** Still at HEAD in \`frontend/src/app.css\`: \`color-scheme: light dark\`, \`background-color: #

## S2-C017  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: KNOWN_OPEN Q-27: `**OPEN and WIDER than recorded.** Session 1 added a player-visible instance the row did not have: \`app.css:160-162\` styles \`button:focus\` with Chrome's own \`-webkit-focus-ring-color\` \`rgb(0, 95, 204)\`, so a stock browser blue ring sits on the last-touched control through spin, win and COLLECT on a cyan and magenta game.` `button:hover {` / `  border-color: #646cff;` / `}` / `button:focus,` / `button:focus-visible {` / `  outline: 4px auto -webkit-focus-ring-color;` / `}` - a browser-default border effect and a stock scaffold hover colour standing in for a designed focus state on every `<button>` in the game
- **path cited**: `frontend/src/app.css:157-163`
- **raised by**: REQ-045 (W21)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: KNOWN_OPEN Q-27: `**OPEN and WIDER than recorded.** Session 1 added a player-visible instance the row did not have: \`app.css:160-162\` styles \`button:focus\` with Chrome's own \`-webkit-focus-ring-c

## S2-C018  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: Q-34: `The same mode reads \`Cruise\` on the features menu, paytable and buy dialog and \`CRUISE\` on the HUD badge, via \`text-transform: uppercase\` present on one surface class and absent on three.` and the recorded blind spot `Cross-surface capitalisation and button casing (charter classes 4 and 7) are gated nowhere; the frames are the instrument.` `outline: 4px auto -webkit-focus-ring-color;` on the coherence half; the animation half is genuinely gated: matrix legs `- name: "browser: splash calm"`, `- name: "browser: turbo intensity"`, `- name: "browser: win count-up steady"`, each `run: node scripts/<gate>.mjs --self-test && node scripts/<gate>.mjs`
- **path cited**: `frontend/src/app.css:160-163 (one style system breached by scaffold defaults) with the animation half at .github/workflows/checks.yml:575-584`
- **raised by**: REQ-046 (W21)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Q-34: `The same mode reads \`Cruise\` on the features menu, paytable and buy dialog and \`CRUISE\` on the HUD badge, via \`text-transform: uppercase\` present on one surface class and absent on three.

## S2-C019  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: `if (gridRef) await gridRef.animateSpin(result.board)` ... `const deferSettle = !roundIsWincap && !!script?.triggered` / `if (!deferSettle) settleRound()` / `if ($isWincap) { lastRoundWasWincap = true; await waitForWincapCollect(); if (script) await presentFeature(script) } else if (script?.triggered) { await presentFeature(script) }` / `if (deferSettle) { lastRoundHadFeature = true; settleRound() }` Math.abs(expectedDollars - finalDollars) < 0.005, \`spin ${i + 1}: displayed total ($${displayedDollars.toFixed(2)}) is not the round's FINAL total ($${finalDollars.toFixed(2)}) early\`)`
- **path cited**: `frontend/src/App.svelte:1445 and frontend/src/App.svelte:1523-1537`
- **raised by**: REQ-132 (W23)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `if (gridRef) await gridRef.animateSpin(result.board)` ... `const deferSettle = !roundIsWincap && !!script?.triggered` / `if (!deferSettle) settleRound()` / `if ($isWincap) { lastRoundWasWincap = true

## S2-C020  [STREAM / CENSUS / CONTRADICTION]

- **claim**: `prose.ts:77` `// multiplication sign is U+00D7 everywhere, matching QUALITY_CHARTER Q-12.` against `prose.ts:90` `modeOverboostBlurb: 'Double-chance: about 1.6x the feature trigger rate. Debits 1.25x every spin while ON.'` and `:194` `rulesScatterMult: '3, 4, or 5 SCATTERs anywhere apply a 1×, 3×, or 10× multiplier to your total play prize.'` KNOWN(Q-26). The file states a project-wide glyph rule in its own header and then breaks it thirteen lines later, in the same file, with both glyph families reaching the same paytable and features surfaces.
- **path cited**: `frontend/src/lib/i18n/prose.ts:77`
- **raised by**: C02-08 (C02)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `prose.ts:77` `// multiplication sign is U+00D7 everywhere, matching QUALITY_CHARTER Q-12.` against `prose.ts:90` `modeOverboostBlurb: 'Double-chance: about 1.6x the feature trigger rate. Debits 1.25x

## S2-C021  [STREAM / CENSUS / STALE]

- **claim**: KNOWN(Q-26), ENUMERATION UNDERCOUNT BY AN ORDER OF MAGNITUDE. `KNOWN_OPEN.md:20` records four surviving letter-`x` instances in prose. The real count in the i18n prose layer is **51**: 6 in `prose.ts` (the `en` master plus `PROSE_SOCIAL`, which repeats both affected keys) and 45 in `prose.locales.ts` (15 locales times 3). The row's instrument read the English master and not the translated copies, which is the SAME instrument failure `KNOWN_OPEN.md` already records against this row. uniq -c` gives `15 1.25x` / `15 1.6x` / `15 5x`; the same over `prose.ts` gives `2 1.25x` / `2 1.6x` / `2 5x`. Sample: `prose.locales.ts:120` `modeOverboostBlurb: 'Doppelte Chance: ca. 1.6x höhere Auslöserate. Zieht 1.25x pro Drehung ab, solange AN.'`; `prose.ts:189` (PROSE_SOCIAL) `modeOverboostBlurb: 'Double-chance: about 1.6x the feature trigger rate. Costs 1.25x every spin while ON.'`
- **path cited**: `frontend/src/lib/i18n/prose.locales.ts:44,48,120,124 (and 13 further locale blocks)`
- **raised by**: C02-09 (C02)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: KNOWN(Q-26), ENUMERATION UNDERCOUNT BY AN ORDER OF MAGNITUDE. `KNOWN_OPEN.md:20` records four surviving letter-`x` instances in prose. The real count in the i18n prose layer is **51**: 6 in `prose.ts`

