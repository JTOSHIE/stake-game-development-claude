# V21: adversarially verify 8 findings (HIGH batch 1, seat 1 of 1)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C067  [HIGH / CENSUS / CONTRADICTION]

- **claim**: `` `isAutoPlay.set(true)` has exactly two call sites in the whole codebase, both inside `` / `` `startAuto(count)` in `ControlBar.svelte:57-63` and its `HudOverlay.svelte` duplicate, `` (COMPLIANCE_WATCH.md:148-149) against `// and that occurrence sits inside a function reachable only from a real` preceded by `// \`isAutoPlay.set(true)\` appears in EXACTLY ONE place in the whole frontend,` (frontend/scripts/check_autoplay_confirm_gate.mjs:11-13); the single tree hit is `frontend/src/lib/components/HudOverlay.svelte:192:    isAutoPlay.set(true)`; `git log -L 149,149:COMPLIANCE_WATCH.md` returns `03c9900 2026-07-13 docs(compliance): JOB 3 - re-validation with dated evidence, fix stale summary line` and `git log --diff-filter=D` for the component returns `56c0403 2026-07-08 chore(hygiene): dead components, five stale-selector scripts, Collection Meter relocation, repo tidy-up` The live compliance record's evidence for the autoplay explicit-confirm rule cites `ControlBar.svelte:57-63` and asserts TWO `isAutoPlay.set(true)` call sites. `ControlBar.svelte` was deleted on 2026-07-08; the line was WRITTEN on 2026-07-13, five days later, by a commit titled "re-validation with dated evidence". The gate for the same rule asserts exactly ONE call site, and the tree has exactly one. TR-006 closed this wording in `CLAUDE.md` only, so this instance is NEW.
- **path cited**: `COMPLIANCE_WATCH.md:149`
- **raised by**: C01-01 (C01)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `` `isAutoPlay.set(true)` has exactly two call sites in the whole codebase, both inside `` / `` `startAuto(count)` in `ControlBar.svelte:57-63` and its `HudOverlay.svelte` duplicate, `` (COMPLIANCE_WA

## S2-C068  [HIGH / CENSUS / STALE]

- **claim**: The generator of the published tile master reads its input from outside the repository, in one machine's Downloads folder. The committed tile cannot be regenerated by anyone else, which is the situation convention (m) exists to prevent. CLAUDE.md's own asset test point 4 requires "Source path, source hash, shipped hash" be recorded; the source path recorded here is unreachable. `const SOURCE = '/Users/jt/Downloads/we-roll-spinners_future-spinner_tile.png'` beside `:21 const TILE = '/Users/jt/math-sdk/design-system/brand/tile'`
- **path cited**: `frontend/scripts/tile_master_ingest.mjs:20`
- **raised by**: C03-08 (C03)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: The generator of the published tile master reads its input from outside the repository, in one machine's Downloads folder. The committed tile cannot be regenerated by anyone else, which is the situati

## S2-C069  [HIGH / CENSUS / GENERATED_DRIFT]

- **claim**: `LoadingScreen.svelte:82-89`: "circular crop of the emblem's wheel-and-reel core, tools/brand/ derive_hero_icon.py) as a single layer" then "`<img class=\"brand-still\" src=\"{$themeAssets.assetBase}/ui/hero_icon_96.png\" ... />`". `WRS_MASTER_DOCUMENT.md:289`: "the in-game loading-screen brand mark now uses the 96 derivative". Measured: `design-system/.../hero_icon_96.png 24547` sha `2affa2250bcb4a14d923` vs `frontend/public/.../ui/hero_icon_96.png 23135` sha `97ed2fe0abe14a424487`, both `PNG image data, 96 x 96, 8-bit/color RGBA`; pixel compare `identical_pixels False, differing_bytes 20879 of 36864, max_channel_delta 117`. `git log --oneline -- <both>` returns the same single commit `54544e4` for each. `grep -rl "oxipng\ **The shipped loading-screen brand mark is NOT the output of the generator the record says produced it.** Same dimensions and mode, different bytes AND different pixels: 20,879 of 36,864 channel bytes differ with a max channel delta of 117. `derive_hero_icon.py` writes only to `design-system/brand/hero_icon/`, so nothing in the repository produces or verifies the shipped copy, and no PNG optimiser exists in the tree that could explain the byte difference. The 32 rung's copy IS byte-identical, so this is an asymmetry inside one commit, not a systematic re-encode.
- **path cited**: `frontend/public/assets/themes/future-spinner/ui/hero_icon_96.png vs design-system/brand/hero_icon/hero_icon_96.png`
- **raised by**: C04-17 (C04)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `LoadingScreen.svelte:82-89`: "circular crop of the emblem's wheel-and-reel core, tools/brand/ derive_hero_icon.py) as a single layer" then "`<img class=\"brand-still\" src=\"{$themeAssets.assetBase}/

## S2-C070  [HIGH / CENSUS / CONTRADICTION]

- **claim**: CENSUS_MECHANICAL.md:60 `## Zero-reference candidates: 550`, then a table whose 24 rows are `frontend/public`, `design-system/brand`, `docs/reference`, `docs/assets`, `frontend/scripts`, `frontend/screens`, `tests/win_calculations`, `utils/game_analytics`, `docs/fe_assets`, `docs/records`, `src/calculations`, `src/config`, `src/write_data`, `utils/merge_luts`, `frontend/src`, `optimization_program/run_script.py`, `src/state`, `src/wins`, `uploads/aws_classes.py`, `uploads/aws_constants.py`, `uploads/aws_upload.py`, `utils/analysis`, `utils/rgs_verification.py`, `utils/search_tool`, `utils/swap_lookups.py`. No `reports` row. The zero-reference scan never examined `reports/`, so this scope's 1559 files were never candidates. Proven, not inferred: `reports/screens/scatter-anticipation/anticipation-active.png` has no literal reference anywhere in the tree (`grep -rl --binary-files=without-match "anticipation-active.png" .` returns nothing, while the control `grep -rl "scatter-anticipation" .` returns six files) and it is still absent from the candidate list. The directory table has no `reports/` row and no note declaring the exclusion.
- **path cited**: `reports/qa/file_census/orphan_candidates.txt:1 (and CENSUS_MECHANICAL.md:60-89)`
- **raised by**: C07-01 (C07)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: CENSUS_MECHANICAL.md:60 `## Zero-reference candidates: 550`, then a table whose 24 rows are `frontend/public`, `design-system/brand`, `docs/reference`, `docs/assets`, `frontend/scripts`, `frontend/scr

## S2-C071  [HIGH / CENSUS / GENERATED_DRIFT]

- **claim**: wc -l` at HEAD is **3664**. The census says 3630, and its own class counts sum to exactly 3630 (1556+1249+534+173+91+27), so the shortfall is in the input file list, not the arithmetic. `git show --diff-filter=A --name-only HEAD` shows HEAD `5816045` added exactly 34 files, every one under `reports/qa/compliance_register/`. So 34 files in C07's scope carry no mechanical class at all, and JOB 2's `DONE MEANS` ("every tracked file classified") cannot be met from this input. The census total is 34 files short of the tree it claims to cover, and all 34 missing files are in MY scope. `git ls-files \
- **path cited**: `reports/qa/file_census/CENSUS_MECHANICAL.md:20`
- **raised by**: C07-02 (C07)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: wc -l` at HEAD is **3664**. The census says 3630, and its own class counts sum to exactly 3630 (1556+1249+534+173+91+27), so the shortfall is in the input file list, not the arithmetic. `git show --di

## S2-C072  [HIGH / CENSUS / CONTRADICTION]

- **claim**: The wave's own boot document sends every squad to a ledger that does not hold what it is said to hold. KNOWN_OPEN tells squads the ledger holds 571 findings; LEDGER.md's own header says it holds two and names `shards/SHARD_INDEX.md` as the authority. LEDGER.md was in fact extended (a `## Consolidated shard findings` section at :77), so the header line is stale against its own file, and a squad that reads the header and stops has been told the wrong place to check before opening a new id. LEDGER.md:8 `JOB 4 consolidates the shards into this file. Until then this ledger holds only` / :9 `the two entries below, and \`shards/SHARD_INDEX.md\` is the authority on what has` / :10 `and has not been swept.` against KNOWN_OPEN.md:41 `**The stream test ledger is at \`reports/qa/stream_test/LEDGER.md\`, its clusters at` / :42 `\`CLUSTERS.md\`, and it holds 571 findings of which 506 are HIGH, MEDIUM or LOW and are` / :43 `UNCLUSTERED AND UNVERIFIED.**`
- **path cited**: `reports/qa/stream_test/LEDGER.md:8-10 vs reports/qa/stream_test/KNOWN_OPEN.md:41-43`
- **raised by**: C07-04 (C07)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: The wave's own boot document sends every squad to a ledger that does not hold what it is said to hold. KNOWN_OPEN tells squads the ledger holds 571 findings; LEDGER.md's own header says it holds two a

## S2-C073  [HIGH / CENSUS / CONTRADICTION]

- **claim**: The 5c table's LOCAL UPLOAD SET row states **eleven** files where seven plus five is twelve, contradicting step 3 at :236, the paragraph at :275 and the twelve-row hash table at :284 in the same section. The document's own :279 says the eleven was "Corrected in both places"; the 2026-07-28 rewording reintroduced it in a third. Those seven **plus** the five \`books_<mode>.jsonl.zst\`. **Eleven files.**` (:262) against `3. Upload the maths/publish bundle: the TWELVE files in 5c below, from` (:236) and `**TWELVE files, and the count was wrong here until 2026-07-26.**` (:275)
- **path cited**: `SUBMISSION_DOSSIER.md:262`
- **raised by**: C08-01 (C08)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: The 5c table's LOCAL UPLOAD SET row states **eleven** files where seven plus five is twelve, contradicting step 3 at :236, the paragraph at :275 and the twelve-row hash table at :284 in the same secti

## S2-C074  [HIGH / CENSUS / CONTRADICTION]

- **claim**: The same arithmetic error propagated into the manifest that the dossier calls "the bridge". Its own repo-committed row is seven, so its upload set is twelve, not 11. The harm is the one the dossier names in its own words at :281, "a first-time uploader counting twelve against a document saying eleven would reasonably think something was missing". The 7 above **plus** 5 x \`books_<mode>.jsonl.zst\` (11 files)
- **path cited**: `BOOKS_MANIFEST.md:32`
- **raised by**: C08-02 (C08)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: The same arithmetic error propagated into the manifest that the dossier calls "the bridge". Its own repo-committed row is seven, so its upload set is twelve, not 11. The harm is the one the dossier na

