# V18: adversarially verify 8 findings (COMPLIANCE batch 9, seat 1 of 1)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C111  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: the only asset steps in `checks.yml` are `- name: asset references, seeded-violation self-test` / `run: node scripts/asset_reference_gate.mjs --self-test` and `- name: asset references resolve in dist` / `run: node scripts/asset_reference_gate.mjs`; neither reads `design-system/brand/delivery/` and there is no `tile_delivery_build.mjs` step anywhere in the file `const LIMIT_BYTES = 3 * 1024 * 1024` / `if (!withinLimit) { console.error('TILE DELIVERY: FAIL, background plus foreground exceed the 3 MB ceiling'); process.exit(1) }`
- **path cited**: `frontend/scripts/tile_delivery_build.mjs:104 and :133-136`
- **raised by**: REQ-164 (W17)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: the only asset steps in `checks.yml` are `- name: asset references, seeded-violation self-test` / `run: node scripts/asset_reference_gate.mjs --self-test` and `- name: asset references resolve in dist

## S2-C112  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: `[PASS] fringe gate (hero): 0 offending pixels` (a captured line about the MASTER at build time; nothing re-reads the delivered file, so a re-save that flattened alpha, or a JPG renamed to `.png`, goes red nowhere) `design-system/brand/delivery/FutureSpinner-FG.png` (header read via `file`), corroborated at `design-system/brand/tile/GENERATION_NOTE_hero.md:56` and `:59`
- **path cited**: `design-system/brand/delivery/FutureSpinner-FG.png (header read via file), corroborated at design-system/brand/tile/GENERATION_NOTE_hero.md:56 and :59`
- **raised by**: REQ-169 (W18)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `[PASS] fringe gate (hero): 0 offending pixels` (a captured line about the MASTER at build time; nothing re-reads the delivered file, so a re-save that flattened alpha, or a JPG renamed to `.png`, goe

## S2-C113  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: Nothing in `.github/workflows/checks.yml`, none of the 20 test files, and no `reports/` artefact asserts that the delivered logo is the STUDIO mark rather than the game logo. `frontend/public/assets/ui/logo_future_spinner.png` (the game logo) and `logo_we_roll_spinners.png` (the studio mark) both exist in the tree, so the confusable pair is present with no check separating them. "; "2. **There is no wordmark.**"; "`WeRollSpinners-Logo.png` from `design-system/brand/provider_mark/provider_mark_f-owner-transparent_master_1024.png`"
- **path cited**: `design-system/brand/provider_mark/PROVENANCE_f.md:9 and :35, delivered as design-system/brand/delivery/WeRollSpinners-Logo.png per README.md:29`
- **raised by**: REQ-171 (W18)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Nothing in `.github/workflows/checks.yml`, none of the 20 test files, and no `reports/` artefact asserts that the delivered logo is the STUDIO mark rather than the game logo. `frontend/public/assets/u

## S2-C114  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: The alpha figure is a one-off ingest measurement committed beside the asset, not a gate. `frontend/scripts/provider_logo_derivation.mjs:299` writes `WeRollSpinners-Logo.png` and its only non-zero exit is `console.error('DELIVERY COPY FAILED: hash mismatch')` at `:302`, which checks the copy, not the format or the alpha channel. `design-system/brand/delivery/WeRollSpinners-Logo.png` (header read via `file`), measured at `design-system/brand/provider_mark/PROVENANCE_f.md:19`
- **path cited**: `design-system/brand/delivery/WeRollSpinners-Logo.png (header read via file), measured at design-system/brand/provider_mark/PROVENANCE_f.md:19`
- **raised by**: REQ-172 (W18)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: The alpha figure is a one-off ingest measurement committed beside the asset, not a gate. `frontend/scripts/provider_logo_derivation.mjs:299` writes `WeRollSpinners-Logo.png` and its only non-zero exit

## S2-C115  [MEDIUM / COMPLIANCE / NOT_MET]

- **claim**: 'desktop' = rawDevice === 'mobile' ? 'mobile' : 'desktop'` / `const lang = p.get('lang') ?? 'en'`. App: `const raw = new URLSearchParams(window.location.search).get('lang')` / `locale.set(resolveLaunchLocale(raw, socialAtBoot, locales))`. `device` is assigned into `SessionParams` and has NO reader: a whole-tree grep of `frontend/src` for `'device'`, `.device` and `device:` returns only this declaration, the `SessionParams` type at `rgsService.ts:253`, and `replayService.ts:31,115` which parses its own copy and likewise never reads it. CI step: `- name: launch params`. Assertions: `check('official sessionID is used', resolveToken(url('sessionID=abc123&rgs_url=x')), 'abc123')`, `check('no rgs_url is not a launch', hasLaunchParams(url('sessionID=abc')), false)`. The gate covers `sessionID` and `rgs_url` only; there is NO assertion anywhere about `device`, and none could fail, because nothing consumes it.
- **path cited**: `frontend/src/lib/services/rgsService.ts:511-535 and frontend/src/App.svelte:188-195`
- **raised by**: REQ-114 (W19)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: 'desktop' = rawDevice === 'mobile' ? 'mobile' : 'desktop'` / `const lang = p.get('lang') ?? 'en'`. App: `const raw = new URLSearchParams(window.location.search).get('lang')` / `locale.set(resolveLaunc

## S2-C116  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: `formatBalance` and `formatBalanceCompact` are the only currency-aware entry points and both start from micros: `const amount = micros / CURRENCY_SCALE` and `const amount = truncateToSignificant(micros / CURRENCY_SCALE, COMPACT_SIGNIFICANT_DIGITS)`. `roundInterpreter.ts`, the canonical round reader, contains ZERO occurrences of `currency` (grep for `currency` over `roundInterpreter.ts`, `betLadder.ts` and `config/fsModes.ts` returns one hit, and it is a comment at `betLadder.ts:13`: `//     normal case for any currency that is not USD-shaped: a JPY or KRW ladder`). Nothing asserts the ABSENCE of currency from round logic. `currency.test.ts` (wired at `.github/workflows/checks.yml:393-395`) asserts formatting only, e.g. `eq('the raw code XEC never appears in a formatted balance', formatBalance(1_000_000_000, 'XEC').includes('XEC'), false)`. `scan_wallet_floats.mjs` (wired at `:200-202`) tests for unrounded float multiplication, not currency coupling: `const MONEY_NAME = /\b(cost\
- **path cited**: `frontend/src/lib/utils/currency.ts:172-178, 282-288 and frontend/src/lib/services/roundInterpreter.ts (whole file)`
- **raised by**: REQ-119 (W19)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `formatBalance` and `formatBalanceCompact` are the only currency-aware entry points and both start from micros: `const amount = micros / CURRENCY_SCALE` and `const amount = truncateToSignificant(micro

## S2-C117  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: `That refusal is what makes the platform's \`minStep\`` / `hold without this module ever reading \`minStep\`: every value the panel can` / `offer came out of \`activeBetLevels\`, which is the authenticate response's own` / `\`betLevels\``; and `export function setBetLevel(level: number): boolean {` / `  const levels = get(activeBetLevels)` / `  if (!levels.includes(level)) return false` `**PARKED WITH A NAMED SANCTION REQUEST:** a defensive assertion that each level is itself a whole multiple of \`config.stepBet\` is NOT possible today and was not faked. \`rgsService.authenticate()\` reads \`stepBet\` into its \`AuthResponse\` and then drops it, because only \`betLevels\` is published to a non-locked store.`
- **path cited**: `frontend/src/lib/stores/betLadder.ts:105-112, 123-129`
- **raised by**: REQ-122 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `That refusal is what makes the platform's \`minStep\`` / `hold without this module ever reading \`minStep\`: every value the panel can` / `offer came out of \`activeBetLevels\`, which is the authenti

## S2-C118  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: `asset_reference_gate.mjs:26-30` states its scope as existence only: `//   1. Every path derived by \`themeStore.ts\`'s \`themeAssets\` for the SHIPPING` / `//      theme, resolved by substituting that theme's \`assetBase\`.` / `//   2. Every literal \`assets/...\` string in the source tree.` / `//   Both are then required to exist in \`dist/\`.` Nothing asserts the emitted `index.html` references are relative NONE (looked in `frontend/scripts/asset_reference_gate.mjs` header and its stated checks, `frontend/scripts/dist_hygiene_gate.mjs`, and grepped `frontend/scripts` + `scripts/qa` for `base: './'`, `startsWith('./')` and "relative" assertions: no hit)
- **path cited**: `frontend/vite.config.ts:299-301`
- **raised by**: REQ-131 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `asset_reference_gate.mjs:26-30` states its scope as existence only: `//   1. Every path derived by \`themeStore.ts\`'s \`themeAssets\` for the SHIPPING` / `//      theme, resolved by substituting tha

