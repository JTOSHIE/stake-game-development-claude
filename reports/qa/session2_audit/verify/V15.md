# V15: adversarially verify 8 findings (COMPLIANCE batch 6, seat 1 of 1)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C062  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: nothing asserts a sub-$0.10 level anywhere; `betLadder.test.ts:37` asserts the OPPOSITE floor as the expected fallback: `check('"+" DROPPED the bet to the ladder minimum', whatPlusWouldHaveDone, 0.10)` NONE (looked in `betLadder.test.ts`, `betSelector.test.ts`, `currency.test.ts`, `checks.yml`, `docs/records/reviews/REVIEW_TRACKER.md`, `docs/QUALITY_CHARTER.md`)
- **path cited**: `frontend/src/lib/stores/gameStore.ts:7 (LOCKED)`
- **raised by**: REQ-124 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: nothing asserts a sub-$0.10 level anywhere; `betLadder.test.ts:37` asserts the OPPOSITE floor as the expected fallback: `check('"+" DROPPED the bet to the ladder minimum', whatPlusWouldHaveDone, 0.10)

## S2-C063  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: the script computes the maths-side figure per mode, `max_x = ordered[0][0]` returned as `"max_x": max_x`, reading the frozen `lookUpTable_<mode>_0.csv` tables; its committed output records `"max_x": "5000"` for all five modes (base, cruise, antelite, bonus, super). It is wired into NO CI job: `checks.yml`'s only python step is `run: python3 tools/verify_books_lookup_equality.py --self-test` at `:510-511`. And it evidences one operand only, never the binding to the printed label `export const FS_MAX_WIN_LABEL = '5,000×'`; rendered as `<span class="fs-mode-stat-label">{maxWinStatLabel()}</span>` `<span class="fs-mode-stat-value fs-num">{FS_MAX_WIN_LABEL}</span>` on every mode card, and again at `:389` in the general rules row
- **path cited**: `frontend/src/lib/config/fsModes.ts:139 and frontend/src/lib/components/PaytableModal.svelte:337-338`
- **raised by**: REQ-040 (W21)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: the script computes the maths-side figure per mode, `max_x = ordered[0][0]` returned as `"max_x": max_x`, reading the frozen `lookUpTable_<mode>_0.csv` tables; its committed output records `"max_x": "

## S2-C064  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: 500,000"` over `frontend/src`, `frontend/scripts`, `scripts`, `games/future_spinner` returns only `roundInterpreter.ts:23` `export const CENTIBET_CAP = 500_000 // 5,000.00x in centibets` (a PAYOUT cap) and RGS fixture bet levels. The only record of the requirement is prose: `COMPLIANCE_WATCH.md:232-233` `the maximum bet size accepted by the RGS is` / `**$500,000 USD**. Anything above returns HTTP 400 with "invalid bet amount".` `export const activeBetLevels = derived(rgsBetLevels, ($levels) => $levels.length > 0 ? $levels : BET_LEVELS,)`; play sends `const amountMicros = Number(displayToMicros(betAmountDollars))` with `mode: get(selectedBetMode), amount: amountMicros`; fallback ladder `export const BET_LEVELS = [0.10, 0.20, 0.50, 1.00, 2.00, 5.00, 10.00, 20.00, 50.00, 100.00]`
- **path cited**: `frontend/src/lib/stores/betLadder.ts:39-41, frontend/src/lib/services/rgsService.ts:623-633 (LOCKED, read as text), frontend/src/lib/stores/gameStore.ts:7 (LOCKED, read as text)`
- **raised by**: REQ-041 (W21)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: 500,000"` over `frontend/src`, `frontend/scripts`, `scripts`, `games/future_spinner` returns only `roundInterpreter.ts:23` `export const CENTIBET_CAP = 500_000 // 5,000.00x in centibets` (a PAYOUT cap

## S2-C065  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: CI steps, quoted: `          - name: "browser: splash calm"` / `            run: node scripts/splash_calm_gate.mjs --self-test && node scripts/splash_calm_gate.mjs`, `          - name: "browser: turbo intensity"` / `            run: node scripts/turbo_intensity_gate.mjs --self-test && node scripts/turbo_intensity_gate.mjs`, and `          - name: "browser: win count-up steady"` / `            run: node scripts/win_countup_steady_gate.mjs --self-test && node scripts/win_countup_steady_gate.mjs`. The cohesion half has no gate: `reports/screens/cohesion-pass/` holds before and after PNGs and no `.md` or `.json` stating what was decided, so nothing goes red if the visual language drifts. `## SYSTEM LAWS` / `- One design output: every visual asset derives from vector masters in this` / `  directory. No external generation (Manus retired July 2026).` / `- Front facing, always. No perspective angles on symbols or UI objects.` / `- Material language: polished chrome, brushed gunmetal, warm gold accents.` / `  Emissives cyan #00FFFF and magenta #FF00FF together, gold #FFD700 sparingly,` / `  on deep navy #060610. Key light upper left. Rich saturation, never pastel.`
- **path cited**: `design-system/DESIGN_SYSTEM.md:7-21`
- **raised by**: REQ-050 (W22)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: CI steps, quoted: `          - name: "browser: splash calm"` / `            run: node scripts/splash_calm_gate.mjs --self-test && node scripts/splash_calm_gate.mjs`, `          - name: "browser: turbo

## S2-C066  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: CI steps, quoted: `      - name: machine tell gate, seeded-violation self-test` / `        working-directory: frontend` / `        run: node scripts/machine_tell_gate.mjs --self-test` and `      - name: machine tell gate, source scan` / `        working-directory: frontend` / `        run: node scripts/machine_tell_gate.mjs --source`. This covers the platform's "Over-reliance on generic AI-generated assets" cause. The other three named 1-star causes (shallow gameplay, inconsistent or low-quality visual design, missing engaging features) are gated nowhere, and the record that carries this requirement is itself stale: it still asserts `base-only single-mode` against the shipped five-mode package at `index.json:2-33`. `- **game-quality-rankings:** captured. NEW relative to prior notes. Games are rated 0 to` / `  3 stars; 1 star or lower is not published and is sent back to resubmit. **Owner` / `  attention:** the page lists "Missing engaging features: bonus modes and additional game` / `  mechanics ... are expected in competitive submissions" among common causes of low` / `  ratings, and warns against "over-reliance on generic AI-generated assets". Our canonical` / `  decision is deliberately base-only single-mode, which may cap the star rating even though` / `  it is fully compliant.`
- **path cited**: `COMPLIANCE_WATCH.md:87-95`
- **raised by**: REQ-159 (W22)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: CI steps, quoted: `      - name: machine tell gate, seeded-violation self-test` / `        working-directory: frontend` / `        run: node scripts/machine_tell_gate.mjs --self-test` and `      - nam

## S2-C092  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: `for (const f of ['FutureSpinner-BG.jpg', 'FutureSpinner-FG.png', 'WeRollSpinners-Logo.png']) {` then `cpSync(join(clone, 'design-system/brand/delivery', f), join(KIT, '03_branding', f))` `design-system/brand/delivery/FutureSpinner-FG.png` exists (directory listing) and is mapped to the rule at `design-system/brand/delivery/README.md:10`
- **path cited**: `design-system/brand/delivery/FutureSpinner-FG.png exists (directory listing) and is mapped to the rule at design-system/brand/delivery/README.md:10`
- **raised by**: REQ-170 (W18), REQ-173 (W18)
- **corroboration**: 2 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `for (const f of ['FutureSpinner-BG.jpg', 'FutureSpinner-FG.png', 'WeRollSpinners-Logo.png']) {` then `cpSync(join(clone, 'design-system/brand/delivery', f), join(KIT, '03_branding', f))` `design-syst

## S2-C093  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: Nothing asserts the loading state. `replayRounds.test.ts` never reads `phase`; `max_win_hold_gate.mjs:187-193` is the only CI-wired assertion against this component and it is about the terminal splash order; `replay_blocker_proof.mjs` waits for `.start-replay` and captures no loading frame, and is unwired anyway. No committed frame under `reports/screens/replay-blocker/` shows the loader (the six files are seeded-defect, base ready, base playing, base complete, super wincap, super after-collect). `{#if phase === 'loading'}` / `    <div class="replay-status loading">{$tr('replayLoading')}</div>`; the store initialises to that phase: `export const replayPhase = writable<ReplayPhase>('loading')`, and `onMount` only leaves it after `const r = await fetchReplay(p)` resolves (`ReplayMode.svelte:94-98`), so the indicator covers the whole fetch window
- **path cited**: `frontend/src/lib/components/ReplayMode.svelte:288-289, with the phase set at frontend/src/lib/stores/replayStore.ts:21`
- **raised by**: REQ-091 (W05)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Nothing asserts the loading state. `replayRounds.test.ts` never reads `phase`; `max_win_hold_gate.mjs:187-193` is the only CI-wired assertion against this component and it is about the terminal splash

## S2-C094  [MEDIUM / COMPLIANCE / UNKNOWN]

- **claim**: `- name: machine tell gate, seeded-violation self-test` / `run: node scripts/machine_tell_gate.mjs --self-test` / `- name: machine tell gate, source scan` / `run: node scripts/machine_tell_gate.mjs --source` The sweep list in section 3 is the operational form of this. It is enforced by \`frontend/scripts/machine_tell_gate.mjs\` for the sweepable subset.
- **path cited**: `docs/QUALITY_CHARTER.md:70`
- **raised by**: REQ-005 (W07)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `- name: machine tell gate, seeded-violation self-test` / `run: node scripts/machine_tell_gate.mjs --self-test` / `- name: machine tell gate, source scan` / `run: node scripts/machine_tell_gate.mjs --

