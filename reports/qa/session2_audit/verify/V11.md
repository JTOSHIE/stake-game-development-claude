# V11: adversarially verify 8 findings (COMPLIANCE batch 2, seat 1 of 1)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C030  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: `await page.route('**/bet/replay/**', (route) => route.fulfill({` ... `await page.goto(`http://localhost:${PORT}/?${LIVE_QS(mode, event)}`)` The glob only proves the request lands under `/bet/replay/`; it never asserts the path segments carry the query-string values. And the script is in neither CI job: `grep -n "replay_blocker" .github/workflows/checks.yml` returns nothing, while the only replay step in that file is `run: npx tsx src/lib/services/replayRounds.test.ts`, which never touches URL construction. `export async function fetchReplay(p: ReplayParams): Promise<ReplayResponse> {` / `  const url = `${p.rgsUrl}/bet/replay/${p.game}/${p.version}/${p.mode}/${p.event}`` / `  const response = await fetch(url, { method: 'GET' })`, with every component of `p` read from the query string at lines 61-75 and no default or literal among them (`if (!game \
- **path cited**: `frontend/src/lib/services/replayService.ts:124-131`
- **raised by**: REQ-090 (W05)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `await page.route('**/bet/replay/**', (route) => route.fulfill({` ... `await page.goto(`http://localhost:${PORT}/?${LIVE_QS(mode, event)}`)` The glob only proves the request lands under `/bet/replay/`

## S2-C031  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: `    // Skip all RGS initialisation in replay mode, ReplayMode handles its own flow` / `    if (isReplay) return` sits BEFORE `await initRGS(gameId, token)` at line 1182; the replay fetch is a bare public GET: `  const response = await fetch(url, { method: 'GET' })`, with no header, token or session argument. `ReplayMode.svelte` imports `parseReplayParams, fetchReplay, microsToDisplay, totalBetSpentMicros, currencySymbol` from `replayService` and never imports `rgsService` (grep for `rgsService` in that file returns one comment at line 137 and no import). No gate asserts the absence of authenticated calls in replay. Looked in `.github/workflows/checks.yml` (all 33 static steps and all ten browser matrix legs), `frontend/scripts/dead_wiring_scan.mjs` (its only replay content is the write-only-store allowlist at lines 77-84), `rgsService.contract.test.ts`, `rgsService.parse.test.ts`, `replayRounds.test.ts`. There is no network-hygiene gate in `checks.yml` despite `App.svelte:1163` citing one.
- **path cited**: `frontend/src/App.svelte:1169-1170 and frontend/src/lib/services/replayService.ts:131`
- **raised by**: REQ-094 (W05)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `    // Skip all RGS initialisation in replay mode, ReplayMode handles its own flow` / `    if (isReplay) return` sits BEFORE `await initRGS(gameId, token)` at line 1182; the replay fetch is a bare pu

## S2-C032  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: CI step: `- name: replay rounds` / `working-directory: frontend` / `run: npx tsx src/lib/services/replayRounds.test.ts`. The assertion the gate carries, `frontend/src/lib/services/replayRounds.test.ts:108-113`: `// Every ordinary round must produce a REAL 5x4 board. This is the assertion` ... `out.board.every((r) => r.every((c) => typeof c === 'string' && c.length > 0)))`. Fixture coverage read directly: `base`, `cruise`, `antelite` each carry `loss`/`win`/`bigWin`/`cap`/`feature`; `bonus` and `super` carry only `cap` and `feature` `, and the reason at :38-39 "The curated local trigger samples (below) are the stand-in for base until a real trigger sim ID is pinned on staging."; the parallel sheet at `docs/REVIEW_EVENTS_PLAN.md:15-25` has EVERY id cell empty, e.g. `
- **path cited**: `REPLAY_TEST_EVENTS.md:25-31 (the five-mode table) and docs/REVIEW_EVENTS_PLAN.md:13-25 (the staging capture sheet)`
- **raised by**: REQ-160 (W06)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: CI step: `- name: replay rounds` / `working-directory: frontend` / `run: npx tsx src/lib/services/replayRounds.test.ts`. The assertion the gate carries, `frontend/src/lib/services/replayRounds.test.ts

## S2-C033  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: Frame 01 opened: live DTT replay of `super` event `22975` at `Amount 750` EUR reaching `MULTIPLIER 5000.0x` / `WIN 3750000.00`, i.e. the 5,000x cap exercised through the replay endpoint on the live platform and captured. Tracker row records it: "`reports/screens/live-round2-2026-07-26/01_...` and `02_MAX_WIN_REACHED_overlay_5000x_bet_collect.png` show `super` event 22975 replaying through to its celebration and its **PLAY AGAIN** control, on the live platform, with the platform's own verification banner across the top". So the max-win half IS exercised and recorded; the rare-base-feature half is not `## Populate + review checklist` / `1. Deploy to Stake Engine staging; confirm each event ID above resolves to the intended round (adjust for the possible +1 offset).` / `3. Confirm: max-win (5,000x) replays and dwells in every mode; base loss (event 5) shows a clean zero round; a trigger event plays the full Overdrive free-spins sequence (including NITRO OVERDRIVE's meter starting visibly at 5x, not 1x)`. The rare-feature half is not dischargeable as written, because the base-mode trigger event ID is unpinned at `REPLAY_TEST_EVENTS.md:31`: `
- **path cited**: `REPLAY_TEST_EVENTS.md:80-88 (the populate-and-review checklist) and DTT_PROTOCOL.md:243-269 (the staging procedure)`
- **raised by**: REQ-161 (W06)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Frame 01 opened: live DTT replay of `super` event `22975` at `Amount 750` EUR reaching `MULTIPLIER 5000.0x` / `WIN 3750000.00`, i.e. the 5,000x cap exercised through the replay endpoint on the live pl

## S2-C034  [HIGH / COMPLIANCE / UNKNOWN]

- **claim**: NONE NONE
- **path cited**: `none cited`
- **raised by**: REQ-001 (W07)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: NONE NONE

## S2-C035  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: `labelKey: 'modeNormalLabel',` ... `cost: 1.0,` ... `serverMode: 'base',` ... `labelKey: 'modeSuperLabel',` ... `cost: 400,` ... `serverMode: 'super',` (five entries: base, cruise, antelite, bonus, super; no gamble, jackpot, collect or cashout mode) `frontend/src/lib/config/fsModes.ts:71-117`
- **path cited**: `frontend/src/lib/config/fsModes.ts:71-117`
- **raised by**: REQ-002 (W07)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `labelKey: 'modeNormalLabel',` ... `cost: 1.0,` ... `serverMode: 'base',` ... `labelKey: 'modeSuperLabel',` ... `cost: 400,` ... `serverMode: 'super',` (five entries: base, cruise, antelite, bonus, su

## S2-C036  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: \`frontend/public/assets/themes/future-spinner/backgrounds/bg_base.jpg\` `design-system/brand/GENERATION_NOTE_background.md:11-24`
- **path cited**: `design-system/brand/GENERATION_NOTE_background.md:11-24`
- **raised by**: REQ-003 (W07)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: \`frontend/public/assets/themes/future-spinner/backgrounds/bg_base.jpg\` `design-system/brand/GENERATION_NOTE_background.md:11-24`

## S2-C037  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: Opened. The rendered DISCLAIMER paragraph ends `Future Spinner™ and We Roll Spinners™ are trademarks of We Roll Spinners. © 2026 We Roll Spinners. All rights reserved.` with no Stake mark anywhere in the panel; `reports/screens/stream-test-2026-07-28/002_desktop_splash.png` shows the splash carrying the studio's own WE ROLL SPINNERS wheel emblem only. `$: disclaimerText = $tr('disclaimerBody')` / `+ ' Future Spinner™ and We Roll Spinners™ are trademarks of We Roll Spinners.'` / `+ ' © 2026 We Roll Spinners. All rights reserved.'` (the template's own trailing `TM and © 2026 Stake Engine.` is replaced, not copied)
- **path cited**: `frontend/src/lib/components/PaytableModal.svelte:62-64`
- **raised by**: REQ-004 (W07)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Opened. The rendered DISCLAIMER paragraph ends `Future Spinner™ and We Roll Spinners™ are trademarks of We Roll Spinners. © 2026 We Roll Spinners. All rights reserved.` with no Stake mark anywhere in 

