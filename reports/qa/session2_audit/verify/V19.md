# V19: adversarially verify 8 findings (COMPLIANCE batch 10, seat 1 of 1)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C119  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: kit_build: `mkdirSync(join(KIT, '02_frontend_upload'), { recursive: true })` / `cpSync(dist, join(KIT, '02_frontend_upload'), { recursive: true })` and the generated instruction `1. Upload the CONTENTS of \`02_frontend_upload\` as the Front End. Not the folder:` / `   if \`index.html\` ends up one level down the game will not load.`; READ_ME: `11. Still on the **Files** page, upload **the contents of \`02_frontend_upload/\`**.` / `**Upload the contents, not the folder.**` the only backstop found is prose in the same document: `**The game will not load after publishing.** Most likely the frontend files went up inside` / `a folder instead of at the top level. See step 11.` That is an instruction and a troubleshooting note, not something that goes red
- **path cited**: `scripts/kit_build.mjs:281-282, 334-335 and docs/records/upload-kit/00_READ_ME_FIRST.md:99-103`
- **raised by**: REQ-176 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: kit_build: `mkdirSync(join(KIT, '02_frontend_upload'), { recursive: true })` / `cpSync(dist, join(KIT, '02_frontend_upload'), { recursive: true })` and the generated instruction `1. Upload the CONTENT

## S2-C120  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: nothing in the repository measures or attests creativity or originality. `docs/QUALITY_CHARTER.md` section 2.2 is the only checkable rendering of a star band and its seven properties are all FINISH properties (tile geometry, publisher type, iteration depth, simulation depth, machine-tells, iconography, brand face); none addresses the 2-star creativity clause. `COMPLIANCE_WATCH.md:212` records only a target: `Our target tier is 3 stars, so the 3-star column is the` `{ id: 'super', labelKey: 'modeSuperLabel', kind: 'buy', cost: 400, volatility: 'Extreme', blurbKey: 'modeSuperBlurb', available: true, serverMode: 'super', }` and the file header `FeatureMath v2 shipped (2026-07-07): all five modes are now runtime-live.`
- **path cited**: `frontend/src/lib/config/fsModes.ts:68-119 (the five-mode package) and CLAUDE.md assets rule with design-system/brand/GENERATION_NOTE_background.md named as the provenance shape`
- **raised by**: REQ-043 (W21)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: nothing in the repository measures or attests creativity or originality. `docs/QUALITY_CHARTER.md` section 2.2 is the only checkable rendering of a star band and its seven properties are all FINISH pr

## S2-C121  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: no gate, test or committed capture measures depth or session length. The nearest wired gate, `.github/workflows/checks.yml:215-217`, proves cost parity between `fsModes.ts` and the shipped `index.json`, which is a different claim; treating it as depth proof would be exactly the substitution this audit forbids `frontend/src/lib/config/fsModes.ts:68-119`, `frontend/src/lib/components/BonusInstrumentColumn.svelte` and `FreeSpinsPresentation.svelte` exist as the feature surface
- **path cited**: `frontend/src/lib/config/fsModes.ts:68-119, frontend/src/lib/components/BonusInstrumentColumn.svelte and FreeSpinsPresentation.svelte exist as the feature surface`
- **raised by**: REQ-044 (W21)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: no gate, test or committed capture measures depth or session length. The nearest wired gate, `.github/workflows/checks.yml:215-217`, proves cost parity between `fsModes.ts` and the shipped `index.json

## S2-C122  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: Nothing checks either cap mechanically. `scripts/validate_math.py:55` holds only `MIN_ROWS = 100_000              # 100k-1M simulations`, a floor and not the 10,000,000 ceiling, and no byte-size check exists in it at all. `scripts/kit_build.mjs` totals bundle bytes (`bytes += statSync(p).size` at line 134) but compares them to nothing. `validate_math.py` is not wired into `checks.yml` in any case. NONE (looked in `.github/workflows/checks.yml`, `scripts/validate_math.py`, `scripts/kit_build.mjs`, `tools/verify_books_lookup_equality.py`)
- **path cited**: `BOOKS_MANIFEST.md:41-49`
- **raised by**: REQ-157 (W22)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Nothing checks either cap mechanically. `scripts/validate_math.py:55` holds only `MIN_ROWS = 100_000              # 100k-1M simulations`, a floor and not the 10,000,000 ceiling, and no byte-size check

## S2-C123  [MEDIUM / COMPLIANCE / NO_PROOF]

- **claim**: `check(r["unique_payouts"] >= MIN_UNIQUE_PAYOUTS, f"{name}: {r['unique_payouts']} unique payouts (< {MIN_UNIQUE_PAYOUTS})")` with `MIN_UNIQUE_PAYOUTS = 10` at line 56. This asserts a floor of ten, not the five figures the self-audit reports, and `validate_math.py` appears nowhere in `.github/workflows/checks.yml`. `SUBMISSION_DOSSIER.md` carries no non-zero weight payout count at all: `grep -n "non-zero\ `### "List the number of non-zero weight payouts. Zero-weight payouts should not dominate"` / `
- **path cited**: `reports/qa/math_selfaudit_2026-07-14.md:117-131`
- **raised by**: REQ-158 (W22)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `check(r["unique_payouts"] >= MIN_UNIQUE_PAYOUTS, f"{name}: {r['unique_payouts']} unique payouts (< {MIN_UNIQUE_PAYOUTS})")` with `MIN_UNIQUE_PAYOUTS = 10` at line 56. This asserts a floor of ten, not

## S2-C216  [LOW / COMPLIANCE / NO_PROOF]

- **claim**: Accepted at account creation. Record date if known. Re-read before submission; post-approval lockdown and removal conditions bind us. \ in-repo proof is impossible by design, see `WRS_MASTER_DOCUMENT.md:6-10`, quoted in notes
- **path cited**: `WRS_MASTER_DOCUMENT.md:27`
- **raised by**: REQ-190 (W15)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Accepted at account creation. Record date if known. Re-read before submission; post-approval lockdown and removal conditions bind us. \ in-repo proof is impossible by design, see `WRS_MASTER_DOCUMENT.

## S2-C217  [LOW / COMPLIANCE / NO_PROOF]

- **claim**: "**The platform publishes no pixel dimensions for the game tile.**"; "**408x546 is the platform's published tile geometry**, at 93.1% of the decoded sample."; README: "That is the platform's own published tile geometry, measured across a live sample of published assets rather than read off the docs, which give no dimensions at all" The repository's practice matches the requirement (no geometry is attributed to the requirements page), but nothing enforces it: no CI step, no test and no `reports/` artefact would fail if a later document attributed 408x546, or any other figure, to the platform docs. `COMPLIANCE_WATCH.md` and convention (d) are manual.
- **path cited**: `docs/stake-engine-live/2026-07-26/published-tile-geometry.md:12 and :48, cited as the source of the only geometry figure at design-system/brand/delivery/README.md:43-45`
- **raised by**: REQ-175 (W18)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: "**The platform publishes no pixel dimensions for the game tile.**"; "**408x546 is the platform's published tile geometry**, at 93.1% of the decoded sample."; README: "That is the platform's own publi

## S2-C218  [LOW / COMPLIANCE / NOT_MET]

- **claim**: fallback, code BEFORE the amount: `  } catch {` / `    // Unknown or unsupported currency code - plain fallback` / `    return \`${code} ${amount.toFixed(decimals)}\`` / `  }`. The platform's own helper at `docs/stake-engine-live/2026-07-29/rgs.md:265-277` puts it AFTER: `const meta = CurrencyMeta[balance.currency] ?? {` / `    symbol: balance.currency,` / `    decimals: 2,` / `    symbolAfter: true,` / `  };` / `  if (meta.symbolAfter) {` / `    return \`${formattedAmount} ${meta.symbol}\`` step: `- name: currency static assertions` / `working-directory: frontend` / `run: npx tsx src/lib/utils/currency.test.ts`; the unknown-code assertion checks only that it does not throw, never the order: `ok('unknown code does not throw', (() => {` / `  try { formatBalance(S, 'ZZZ', 'en'); return true } catch { return false }` / `})())`
- **path cited**: `frontend/src/lib/utils/currency.ts:221-224 (the fallback clause), against frontend/src/lib/utils/currency.ts:185-194 (the main clause, which IS satisfied)`
- **raised by**: REQ-120 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: fallback, code BEFORE the amount: `  } catch {` / `    // Unknown or unsupported currency code - plain fallback` / `    return \`${code} ${amount.toFixed(decimals)}\`` / `  }`. The platform's own help

