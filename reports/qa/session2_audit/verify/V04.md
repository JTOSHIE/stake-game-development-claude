# V04: adversarially verify 7 findings (STREAM batch 2, seat 1 of 3)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C008  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: Looked in: `.github/workflows/checks.yml` (no step asserts the replay branch; the only replay step is `replay rounds`); `frontend/src/lib/services/replayRounds.test.ts:154-162` source scan (asserts absence of the legacy event search only, nothing about controls, `isReplay`, spin or wallet calls); grep for `isReplay` and `replay=true` across `frontend/scripts` and `scripts/qa` matches exactly one file, `frontend/scripts/replay_blocker_proof.mjs`, which asserts only `.start-replay` clickability, `.play-again` appearance and `[data-testid="max-win-collect"]`, and is NOT wired into `checks.yml`; no test file among the 20 asserts the replay branch. Partial corroboration only, not a gate: the live frame at `reports/screens/live-round2-2026-07-26/01_...png` shows the replay surface with no balance readout, no bet ladder, no spin control and no autoplay entry `{#if isReplay}` / `<!-- Replay mode, no betting controls, balance, autoplay, or theme selector -->` / `<ReplayMode />` / `{:else}`; init: `// Skip all RGS initialisation in replay mode, ReplayMode handles its own flow` / `if (isReplay) return`; keys: `// Normal-play branch only, never drive a spin in replay mode.` / `if (isReplay) return`
- **path cited**: `frontend/src/App.svelte:1689-1692 (UI branch), :1169-1170 (no RGS init), :1597-1598 (spacebar inert)`
- **raised by**: REQ-099 (W06)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Looked in: `.github/workflows/checks.yml` (no step asserts the replay branch; the only replay step is `replay rounds`); `frontend/src/lib/services/replayRounds.test.ts:154-162` source scan (asserts ab

## S2-C009  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: `vocabulary.ts:183` `if (!opts.includeNeverRewrite && NEVER_REWRITE.has(key)) continue` means the only scanner in CI cannot see `currency`; the committed DOM scan lists it under `"termsReportedNotFailed": ["stake", "currency", "fund"]` and its `surfacesCovered` do not include replay NONE (looked at `vocabulary.ts:183`, `vocabulary.test.ts:128-132`, `a11y_social_terms_check.mjs:37-43`, `reports/qa/social_dom_conformance_2026-07-25.json:11-24`)
- **path cited**: `frontend/src/lib/components/ReplayMode.svelte:346`
- **raised by**: REQ-038 (W11)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `vocabulary.ts:183` `if (!opts.includeNeverRewrite && NEVER_REWRITE.has(key)) continue` means the only scanner in CI cannot see `currency`; the committed DOM scan lists it under `"termsReportedNotFail

## S2-C010  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: Nothing in `.github/workflows/checks.yml` scans for Stake branding, off-brand assets or offensive content. The nearest wired gate is `machine tell gate, source scan` (`checks.yml:290-291`), whose classes are typography and placeholders only: `frontend/scripts/machine_tell_gate.mjs:107` `const PLACEHOLDER_WORDS = /\b(TODO\ "- **Original IP:** verified. Original designs, produced in-house from vector masters. / No pre-purchased or third-party licensed content. / - **No Stake branding:** verified. No Stake trademark or themes in any shipped asset or text. / - **No underage appeal:** verified. No child or child-like characters."
- **path cited**: `COMPLIANCE_WATCH.md:19-22`
- **raised by**: REQ-142 (W13)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Nothing in `.github/workflows/checks.yml` scans for Stake branding, off-brand assets or offensive content. The nearest wired gate is `machine tell gate, source scan` (`checks.yml:290-291`), whose clas

## S2-C011  [STREAM / COMPLIANCE / NO_PROOF]

- **claim**: No gate checks asset provenance or hashes; `asset_reference_gate.mjs` (wired at `checks.yml:494-496`) only asserts references resolve in `dist`. The one committed clearance artefact covers the NAME, not the art: `docs/records/trademark/2026-07-18/au/au_variant_scan_data.json` plus four IP Australia result PNGs. Asset originality is recorded as unestablished at `docs/records/reviews/sources/round2_review3.md:23`. `design-system/brand/GENERATION_NOTE_background.md:8-13`
- **path cited**: `design-system/brand/GENERATION_NOTE_background.md:8-13`
- **raised by**: REQ-147 (W13)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: No gate checks asset provenance or hashes; `asset_reference_gate.mjs` (wired at `checks.yml:494-496`) only asserts references resolve in `dist`. The one committed clearance artefact covers the NAME, n

## S2-C012  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: **The Q-13 fix took the font line out of the Vite scaffold block and left the rest of the block.** Still at HEAD in `frontend/src/app.css`: `color-scheme: light dark`, `background-color: #242424`, `a { color: #646cff }`, `a:hover { #535bf2 }`, and `body { display: flex; place-items: center }`. Every one is a starter default. \ `button:hover { border-color: #646cff; } button:focus, button:focus-visible { outline: 4px auto -webkit-focus-ring-color; }`
- **path cited**: `frontend/src/app.css:157-162`
- **raised by**: REQ-148 (W13)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: **The Q-13 fix took the font line out of the Vite scaffold block and left the rest of the block.** Still at HEAD in `frontend/src/app.css`: `color-scheme: light dark`, `background-color: #242424`, `a 

## S2-C013  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: `  // Fiat. The symbol is derived by Intl from the code, never substituted.` / `  const decimals = ZERO_DECIMAL.has(code) ? 0 : 2` / `    return new Intl.NumberFormat(localeTag, { style: 'currency', currency: code, currencyDisplay: 'narrowSymbol', ... }).format(amount)` ; the platform token/side is honoured ONLY when the session supplies metadata, and `rgsService.ts:582-584` says of that field `    // UNKNOWN at the pin. Undefined here means "the platform sent nothing", not` / `    // "we dropped it", which is the distinction TR-012c needs at DTT.` / `    currencyDisplay: config.currencyDisplay,` `frontend/src/lib/utils/currency.ts:210-224`
- **path cited**: `frontend/src/lib/utils/currency.ts:210-224`
- **raised by**: REQ-108 (W16)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `  // Fiat. The symbol is derived by Intl from the code, never substituted.` / `  const decimals = ZERO_DECIMAL.has(code) ? 0 : 2` / `    return new Intl.NumberFormat(localeTag, { style: 'currency', c

## S2-C014  [STREAM / COMPLIANCE / NOT_MET]

- **claim**: the currency test's only decimal assertions are 2-place and 0-place: `eq('USD uses the narrow symbol', formatBalance(1.25 * S, 'USD', 'en'), '$1.25')` and `eq('JPY is zero-decimal', formatBalance(1250 * S, 'JPY', 'en'), '¥1,250')`. Nothing tests 3 or 4 places currency.ts: `const decimals = ZERO_DECIMAL.has(code) ? 0 : 2`; game_config.py: `(5, "L3"): 0.65, (4, "L3"): 0.20, (3, "L3"): 0.08,` so the minimum win is 0.08x, below 0.1x, and the platform text requires 4 points of precision
- **path cited**: `frontend/src/lib/utils/currency.ts:211 against games/future_spinner/game_config.py:127`
- **raised by**: REQ-125 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: the currency test's only decimal assertions are 2-place and 0-place: `eq('USD uses the narrow symbol', formatBalance(1.25 * S, 'USD', 'en'), '$1.25')` and `eq('JPY is zero-decimal', formatBalance(1250

