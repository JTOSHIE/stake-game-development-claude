# V14: adversarially verify 8 findings (COMPLIANCE batch 5, seat 1 of 1)

Your job is to REFUTE each finding below. Default to REFUTED when uncertain.

## S2-C054  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: "runs-on: ubuntu-latest" and "run: npx playwright install chromium --with-deps" . Eleven browser legs, every one a `node scripts/*_gate.mjs`, all on the one runner OS with the one engine. Device coverage inside the gates is chromium emulation only: `frontend/scripts/hud_reel_size_check.mjs:29-30` reads "{ label: 'iphone14-portrait', context: { ...devices['iPhone 14'] } }, / { label: 'pixel7-portrait', context: { ...devices['Pixel 7'] } }" . Grep for `firefox` or `webkit` across `frontend/scripts/*.mjs` returns no browser launch, only `window.webkitAudioContext` in `audio_verify.mjs:81` Nearest recording is `docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md:157` (pipes trimmed): "56  Works on older mobile devices (Android and iOS)  **OBSERVE**  Only DTT viewport emulation so far, which is not a real device." That is a devices row in a file the rubric does not count as a tracker, and it does not record the single-engine, single-OS gate coverage at all. `REVIEW_TRACKER.md` and `QUALITY_CHARTER.md` grepped for cross-browser, firefox, safari, webkit, operating system: only TR-066, which is about `touch-action` on iOS Safari, not about test coverage
- **path cited**: `.github/workflows/checks.yml:563 and :646 (browser job), matrix at :569-600`
- **raised by**: REQ-185 (W14)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: "runs-on: ubuntu-latest" and "run: npx playwright install chromium --with-deps" . Eleven browser legs, every one a `node scripts/*_gate.mjs`, all on the one runner OS with the one engine. Device cover

## S2-C055  [HIGH / COMPLIANCE / UNKNOWN]

- **claim**: "**The anti-forgetting document.** Rewritten 2026-07-28 to Fable's enumeration: **five things are outstanding**, and everything else that was ever on this list is DONE with its evidence path recorded below." The five are the 58 Guidelines ticks, verifying the payout wallet address, uploading kit V10, eyeballing three things, and deleting the old entry. Neither they nor the DONE table carry an identification or verification document item Grepped `identification and verification`, `verification documents` and `KYC` across every `*.md` outside the capture and the register: zero substantive hits. `WRS_MASTER_DOCUMENT.md:21` records the ASIC business name and that "the **ABN is on file offline** and is deliberately not recorded here", which is company registration, not the platform's ID submission
- **path cited**: `OWNER_CHECKLIST.md:3-5 (the enumeration), :25-113 (the five items), :141-153 (the DONE table)`
- **raised by**: REQ-186 (W14)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: "**The anti-forgetting document.** Rewritten 2026-07-28 to Fable's enumeration: **five things are outstanding**, and everything else that was ever on this list is DONE with its evidence path recorded 

## S2-C056  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: dossier: "**Everything in 5b onward targets the game entry `future-spinner-2`.** It is a fresh clean upload and it is the entry that will be submitted." / checklist: "**The kit is built and waiting:** `~/Desktop/FS_UPLOAD_KIT_V10/`. Follow **PART 9i** in the walkthrough inside it. Upload to `future-spinner-3`." step (c) is recorded as not started at `docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md:155`, quoted in notes; nothing gates it
- **path cited**: `SUBMISSION_DOSSIER.md:206-209 vs OWNER_CHECKLIST.md:61-65`
- **raised by**: REQ-191 (W15)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: dossier: "**Everything in 5b onward targets the game entry `future-spinner-2`.** It is a fresh clean upload and it is the entry that will be submitted." / checklist: "**The kit is built and waiting:**

## S2-C057  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: the string "1 August 2026" occurs in no project record; see the grep in notes `OWNER_CHECKLIST.md:25` (the whole document carries no date)
- **path cited**: `OWNER_CHECKLIST.md:25 (the whole document carries no date)`
- **raised by**: REQ-192 (W15)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: the string "1 August 2026" occurs in no project record; see the grep in notes `OWNER_CHECKLIST.md:25` (the whole document carries no date)

## S2-C058  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: `// (base spins plus a bonus buy) capturing every network request, asserting` / `// zero 404s and zero requests into any pruned legacy path.` — this script is NOT a step in `.github/workflows/checks.yml` (grepped the whole file: only `asset_reference_gate.mjs` and `dist_hygiene_gate.mjs` appear), and even if wired it asserts 404s and pruned paths, never "no external host" `import '@fontsource/orbitron/400.css'` / `import '@fontsource/orbitron/700.css'` / `import '@fontsource/orbitron/900.css'` ; no `fonts.googleapis.com`, `fonts.gstatic.com`, `cdn.`, `unpkg` or `jsdelivr` reference exists anywhere in `frontend/src` or `frontend/index.html` (grepped, zero hits)
- **path cited**: `frontend/src/main.ts:2-4`
- **raised by**: REQ-104 (W16)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `// (base spins plus a bonus buy) capturing every network request, asserting` / `// zero 404s and zero requests into any pruned legacy path.` — this script is NOT a step in `.github/workflows/checks.y

## S2-C059  [HIGH / COMPLIANCE / NO_PROOF]

- **claim**: `const verdictAt = (px) => { ... const gw = tests.filter((t) => t.winner === 'g').length; return { px, tests, gWins: gw, fWins: tests.length - gw, winner: gw >= 2 ? 'g' : 'f' } }` then `const verdicts = LADDER.map(verdictAt)`: this RANKS two candidates against each other and writes the winner into the delivery. There is no absolute legibility THRESHOLD anywhere, so it cannot fail on an illegible mark, only prefer the less illegible one. `frontend/scripts/provider_logo_derivation.mjs:216-226` (not present in `.github/workflows/checks.yml`); static sheets at `reports/screens/provider-mark/48px-legibility-comparison.png` and `f-vs-g-rendered-sizes.png`
- **path cited**: `design-system/brand/provider_mark/PROVIDER_LOGO_DERIVATION.md:7, :64 and :95`
- **raised by**: REQ-174 (W18)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: `const verdictAt = (px) => { ... const gw = tests.filter((t) => t.winner === 'g').length; return { px, tests, gWins: gw, fWins: tests.length - gw, winner: gw >= 2 ? 'g' : 'f' } }` then `const verdicts

## S2-C060  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: Service, inside `_rgsSpinReal`, before returning: `const needsEndRound = playResp.round ? playResp.active : playResp.winMicros > 0` / `if (needsEndRound) { const endResp = await endRound(params, playResp.roundId)`. Caller: `const result: SpinResult = await spin({ betAmount: bet, mode: 'base' })` at 1414, and only afterwards `if (gridRef) await gridRef.animateSpin(result.board)` at 1445. The round is therefore closed BEFORE any animation runs. CI step: `- name: wallet contract` / `run: npx tsx src/lib/services/rgsService.contract.test.ts`. Assertions: `check('posts to /wallet/end-round', sent[0].url, 'https://rgs.example/wallet/end-round')` and `checkThat('roundId is genuinely absent from the request body', !Object.prototype.hasOwnProperty.call(sent[0].body, 'roundId'))`. Nothing anywhere asserts the ORDER of end-round against the presentation.
- **path cited**: `frontend/src/lib/services/rgsService.ts:799-807 with frontend/src/App.svelte:1414, 1445`
- **raised by**: REQ-112 (W19)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: Service, inside `_rgsSpinReal`, before returning: `const needsEndRound = playResp.round ? playResp.active : playResp.winMicros > 0` / `if (needsEndRound) { const endResp = await endRound(params, playR

## S2-C061  [HIGH / COMPLIANCE / NOT_MET]

- **claim**: betLadder: `export const activeBetLevels = derived(rgsBetLevels, ($levels) =>` / `  $levels.length > 0 ? $levels : BET_LEVELS,` / `)`; rgsService parses then drops the limits: `minBet:          microsToDisplay(config.minBet ?? 0),` / `maxBet:          microsToDisplay(config.maxBet ?? 0),` / `stepBet:         microsToDisplay(config.stepBet ?? 0),` while only `rgsBetLevels.set(auth.betLevels)` is published no assertion anywhere references `minBet` or `maxBet` as a clamp; `rgsService.contract.test.ts:97-99` only asserts they are parsed (`check('minBet comes from config, in micros', auth.minBet, 0.1)`)
- **path cited**: `frontend/src/lib/stores/betLadder.ts:39-41 and frontend/src/lib/services/rgsService.ts:565-567, 735`
- **raised by**: REQ-121 (W20)
- **corroboration**: 1 row(s) from 1 distinct squad(s). PROVISIONAL.
- **as originally written**: betLadder: `export const activeBetLevels = derived(rgsBetLevels, ($levels) =>` / `  $levels.length > 0 ? $levels : BET_LEVELS,` / `)`; rgsService parses then drops the limits: `minBet:          micros

