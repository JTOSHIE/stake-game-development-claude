# V06: adversarial verification shard (STREAM batch 2, seat 3 of 3)

Read only. No project script executed. Seven clusters assigned.

| cluster | claim_true | diagnosis_sound | severity_verdict | outcome | evidence_quote |
|---|---|---|---|---|---|
| S2-C008 | YES | YES | AGREE, proof-debt tier (behaviour is met, nothing player-visible) | CONFIRMED | `.github/workflows/checks.yml:420` `      - name: replay rounds` / `:422` `        run: npx tsx src/lib/services/replayRounds.test.ts` is the ONLY replay step in the file; the test's component-side assertions are `frontend/src/lib/services/replayRounds.test.ts:155-157` `checkThat("no search for a 'board' event", !/ev\.type === 'board'/.test(src))` and siblings, none of which mentions controls, balance, `isReplay` or a wallet call |
| S2-C009 | YES | YES | AGREE, proof-debt tier | INCOMPLETE | `frontend/src/lib/i18n/vocabulary.ts:183` `    if (!opts.includeNeverRewrite && NEVER_REWRITE.has(key)) continue`, with `:120` `  currency: 'appears only in code identifiers, never player-visible',`; and `reports/qa/social_dom_conformance_2026-07-25.json:17-25` `"surfacesCovered": [ "first-paint", "features-menu", "paytable", "autoplay", "session", "spin", "win-banner" ]` |
| S2-C010 | YES | YES | AGREE, proof-debt tier | CONFIRMED | `frontend/scripts/machine_tell_gate.mjs:107` `const PLACEHOLDER_WORDS = /\b(TODO\|TBD\|FIXME\|XXX\|Lorem ipsum\|lorem ipsum)\b/`; the gate's whole class list is codepoint blocks, placeholder words, `TITLE_TAG`/`PACKAGE_SHAPED`, `LOCALE_BLOCK`, `MODE_BADGE_SELECTOR`, `TOKEN_DEF`/`FONT_FAMILY_DECL`, `CANONICAL_MONEY_MODULE`. Zero of the 49 `- name:` steps in `checks.yml` scans brand, asset or content standards |
| S2-C011 | PARTLY | NO | TOO_HIGH, proof-debt tier | CONFIRMED_WRONG_CAUSE | `frontend/scripts/asset_reference_gate.mjs:26-29` `//   1. Every path derived by themeStore.ts's themeAssets ... //   Both are then required to exist in dist/.` refutes nothing about provenance, BUT `design-system/brand/GENERATION_NOTE_background.md:10-16` is a second committed clearance artefact and it covers the ART: `| Origin | **Externally generated, commissioned by the owner** |` / `| Source SHA-256 | 65ef44c1ce96d351e96f69831bde8146... |` / `| Source bytes | 569,573 |` |
| S2-C012 | YES | YES | TOO_HIGH, cosmetic committed-surface tier | CONFIRMED | `frontend/src/app.css:103-105` `  color-scheme: light dark;` / `  color: rgba(255, 255, 255, 0.87);` / `  background-color: #242424;`, `:113-120` `a {` / `  font-weight: 500;` / `  color: #646cff;` ... `a:hover {` / `  color: #535bf2;`, `:122-127` `body {` / `  margin: 0;` / `  display: flex;` / `  place-items: center;`. But `grep -rnE "<a[ >]\|createElement\('a'" frontend/src frontend/index.html` returns ZERO hits |
| S2-C013 | YES | YES | AGREE, contingent on the platform omitting metadata | INCOMPLETE | `frontend/src/lib/utils/currency.ts:210-217` `  // Fiat. The symbol is derived by Intl from the code, never substituted.` / `  const decimals = ZERO_DECIMAL.has(code) ? 0 : 2` / `    return new Intl.NumberFormat(localeTag, { ... currencyDisplay: 'narrowSymbol', ...})`, against `docs/stake-engine-live/2026-07-29/rgs.md:122` `Tunisian Dinar	TND	TND	10.00 TND` and `:233` `  KWD: { symbol: 'KD', decimals: 3 },` |
| S2-C014 | YES | YES | AGREE, player-visible on the lowest shipped bet level | CONFIRMED | `docs/stake-engine-live/2026-07-29/rgs.md:295` `... while games with minimum wins <0.1x will require 4 points of precision.` and `:297` `it is only a requirement that wins in-game show exact win amounts`, against `frontend/src/lib/utils/currency.ts:211` `  const decimals = ZERO_DECIMAL.has(code) ? 0 : 2` and `frontend/src/lib/components/HudOverlay.svelte:366` `  $: winLabel = formatBalance(Math.round(displayedWinAmount * CURRENCY_SCALE), $currencyCode \|\| 'USD')` |

## PER CLUSTER REASONING

**S2-C008, CONFIRMED.** Every limb of the observation holds at HEAD. The three `isReplay`
guards exist verbatim: `App.svelte:1170` `if (isReplay) return` under the comment `// Skip all
RGS initialisation in replay mode, ReplayMode handles its own flow`, `:1598` `if (isReplay)
return` under `// Normal-play branch only, never drive a spin in replay mode.`, and `:1689`
`{#if isReplay}` with `<!-- Replay mode, no betting controls, balance, autoplay, or theme
selector -->`. I attacked the no-proof half three ways and it survived all three. First, the
only `replay` steps in `checks.yml` are gate 13 at lines 412 to 422, running
`replayRounds.test.ts`, whose component-side assertions read the shipped `ReplayMode.svelte`
source solely for legacy event-search regressions and the single `interpretEvents(` call.
Second, `grep -rln "isReplay\|replay=true\|start-replay" frontend/scripts scripts/qa` returns
exactly one file, `frontend/scripts/replay_blocker_proof.mjs`, and it appears nowhere in
`checks.yml`, so per the stated rule it cannot refute a no-proof finding. Third, the claim's
enumeration is COMPLETE rather than partial, which I checked rather than assumed: `isReplay`
occurs at exactly five sites in the whole tree, all in `App.svelte` (39 definition, 351
`warmMount`, 1170, 1598, 1689), and it appears in no other file. So there is no fourth
unguarded path the finding missed. Severity: the requirement is behaviourally MET and the
whole non-replay control tree is inside the `{:else}`, so nothing is rendered rather than
rendered-and-disabled. This is proof debt on a correct implementation, not a player-visible
defect, and it should be ordered accordingly.

**S2-C009, INCOMPLETE.** The quoted skip is verbatim at `vocabulary.ts:183`, the DOM scan does
list `currency` under `termsReportedNotFailed`, and its seven `surfacesCovered` do not include
replay. I then went looking for the proof path the finding might have missed, because
`vocabulary.test.ts` DOES read the shipped replay component at line 161 (`const replay =
readFileSync('src/lib/components/ReplayMode.svelte', 'utf8')`). It asserts one thing only, at
164-167: `checkThat('ReplayMode derives its first-paint mode from it, not from the flag alone',
/initialMode: GameMode = socialAtBoot/.test(replay) && ...)`. Nothing touches the label. So the
no-proof claim stands. Why INCOMPLETE rather than CONFIRMED: the finding names ONE label on
that surface, and the class has at least two members. `ReplayMode.svelte:321` renders
`{mode === 'social' ? 'Play' : 'Bet'}: <strong>{formatBalance(...)}</strong>` by the identical
hardcoded-ternary mechanism, on the identical unscanned surface. That one is arguably worse,
because `bet` is an ACTIVE term in the scanner's table rather than a NEVER_REWRITE one, so the
scanner would catch a regression there if any scan covered replay, and none does. The class is
"replay-surface social wording held only by hand-written ternaries on a surface no conformance
scan reaches", and the finding names half of it. Severity is right at proof-debt: behaviour is
correct today in both places, social mode renders `Token` and `Play`, so no player sees a
prohibited term.

**S2-C010, CONFIRMED.** I read the gate rather than trusting the finding's characterisation of
it, and the characterisation is accurate: `machine_tell_gate.mjs` holds `SYMBOL_BLOCKS`,
`ALLOWED_CODEPOINTS`, `PLACEHOLDER_WORDS`, `TITLE_TAG`/`PACKAGE_SHAPED`, `LOCALE_BLOCK`,
`MODE_BADGE_SELECTOR`, `TOKEN_DEF`/`FONT_FAMILY_DECL` and `CANONICAL_MONEY_MODULE`. Typography,
placeholders, title shape, locale completeness, badge selector, font tokens, money-module
canonicality. No brand class, no asset class, no content-standard class. I also checked the
whole workflow: 49 `- name:` steps, and the only asset-adjacent or brand-adjacent ones are
`machine tell gate` (three legs) and `asset references` (two legs). The nearest thing to
counter-evidence is `docs/records/reviews/sources/round2_review3.md`, which records `| No
Stake-branded assets | PASS | No Stake branding visible in the current capture groups. |` and
`| No offensive, explicit or underage-targeted content | PASS |`. I do not accept that as a
refutation: it is a human read of a capture set at a point in time, not committed output that
would visibly disagree if the requirement broke, and it explicitly scopes itself to what was
visible in captures rather than to shipped text. Severity is proof debt rather than a defect,
and the sharpest edge is the one the finding quotes: `COMPLIANCE_WATCH.md:21` asserts `**No
Stake branding:** verified.` The word "verified" is doing work no artefact supports.

**S2-C011, CONFIRMED_WRONG_CAUSE.** The gate half is true and I confirmed it from the script's
own header: `asset_reference_gate.mjs` checks that referenced paths EXIST in `dist`, nothing
more, and its stated exclusions are about unresolvable and non-shipping paths, not provenance.
The `docs/records/trademark/2026-07-18/au/` directory is exactly as described, one JSON plus
four IP Australia PNGs, and it is a NAME clearance. And `round2_review3.md:23` really does read
`| Original design and lawful IP | UNVERIFIABLE | Generation/provenance notes exist, but asset
ownership and third-party licence compliance cannot be independently established from the
repository alone. |`. But the sentence "The one committed clearance artefact covers the NAME,
not the art" is false, and it is the load-bearing sentence: `GENERATION_NOTE_background.md`
covers the art, records `| Origin | **Externally generated, commissioned by the owner** |`, a
source SHA-256 and a source byte count, and its own second paragraph says why it exists ("the
asset is externally designed, which the project's Assets convention now permits only WITH
recorded provenance"). `CLAUDE.md` names a second one for the tile. The review row the finding
cites even says so in the clause the finding did not quote: "Generation/provenance notes
exist". So the defect is real and the stated cause is not. THE REAL GAP, stated only as far as
I verified it: nothing MECHANICAL binds a recorded hash to what is in `dist`, so an asset
swapped after its generation record was written would leave every gate green and the record
still reading as clearance. Whether the in-house symbol set has equivalent records I did not
establish and will not guess. Severity drops accordingly: the records exist, the enforcement
does not.

**S2-C012, CONFIRMED, severity TOO_HIGH.** Every item is at HEAD verbatim, and the claim is a
word-for-word restatement of `docs/QUALITY_CHARTER.md:199` Q-27, which is already recorded
OPEN, so this cluster adds no new observation. The residue is real: `app.css:103` `color-scheme:
light dark;`, `:105` `background-color: #242424;`, the `a`/`a:hover` block at 113 to 120, the
`body { display: flex; place-items: center }` block at 122, and the `@media
(prefers-color-scheme: light)` block at 165 which re-paints `:root` white. The
`diagnosis_sound` verdict is YES because `git log -- frontend/src/app.css` shows the font work
landed as `eb357d9 feat(JOB 2): exactly two font stacks exist, as tokens`, and the token
declarations now sit at lines 97 to 99 immediately ABOVE the surviving scaffold properties in
the same `:root` block, which is precisely "took the font line out and left the rest". WHERE I
DEPART FROM IT is severity, and the departure is question 5 rather than question 3. Q-27's own
disposition sentence reads "The link colours are the sharpest, being a stock indigo in a cyan
and magenta game". That is not true: `grep -rnE "<a[ >]|createElement\('a'" frontend/src
frontend/index.html` returns zero hits, so no anchor element exists anywhere in the shipped
surface and the two rules the charter calls sharpest cannot paint a pixel. What remains is
committed-surface residue that a rival studio's engineer reading the repository would see and a
player would not, which is still worth clearing under the standing mandate but belongs low in
the order, not near the top.

**S2-C013, INCOMPLETE.** The observation is true and the requirement text is unambiguous:
REGISTER REQ-108 quotes `approval_guidelines_rgs_communication.md:51` requiring "the platform's
exact display token, on the platform's side of the amount ... suffix for DKK, PLN, VND, CLP,
ARS, SAR, ILS, AED, TND, OMR, QAR, XGC, XSC, XEC". `currency.ts:172-192` honours side and token
ONLY inside `if (display && (display.symbol || display.symbolAfter !== undefined ||
display.decimals !== undefined))`; absent that payload, fiat falls to `Intl.NumberFormat` with
`currencyDisplay: 'narrowSymbol'`, whose placement is decided by the LOCALE, not by the platform
table. The mirror's own Example column confirms the breach: `10.00 TND`, `10.00 OMR`, `10.00
QAR` are suffixed there and `en`-locale Intl prefixes them. The `rgsService.ts:582-584` quote is
verbatim, including `// UNKNOWN at the pin. Undefined here means "the platform sent nothing"`,
so the fallback path is the live path whenever the platform is silent, and the finding is right
that this is contingent rather than certain. Marked INCOMPLETE because the same line the finding
quotes breaks the requirement a SECOND way it does not name: `const decimals =
ZERO_DECIMAL.has(code) ? 0 : 2` forces two decimals on every non-zero-decimal code, while the
platform's own currency reference at `rgs.md:233` says `KWD: { symbol: 'KD', decimals: 3 }`. The
token, the side AND the decimal count all come from the platform table, and the finding covers
two of three.

**S2-C014, CONFIRMED.** All four limbs check out. The test really does assert only two and zero
places (`currency.test.ts:74` `eq('USD uses the narrow symbol', formatBalance(1.25 * S, 'USD',
'en'), '$1.25')`, `:80` `eq('JPY is zero-decimal', ...)`, and the metadata cases at 116 to 124
use decimals 2 and 0 only); `currency.ts:211` is the quoted line; `game_config.py:127` is
`            (5, "L3"): 0.65, (4, "L3"): 0.20, (3, "L3"): 0.08,`, so 0.08x is the minimum win
and it is below 0.1x. I checked the consumer rather than assuming it, because a separate
exact-win formatter would have refuted this outright: there is none. Both win readouts route
through the same two-decimal function, `HudOverlay.svelte:366` and `WinBanner.svelte:201`. With
`BET_LEVELS` starting at 0.10 (`gameStore.ts:7`, LOCKED file, quoted not edited), the minimum
win is 0.08 x 0.10 = $0.008 and renders as `$0.01`, against the mirror's `it is only a
requirement that wins in-game show exact win amounts`. On a $0.01 level, which the same page
recommends new submissions carry, it renders `$0.00`: a win displayed as nothing. ONE
QUALIFICATION on the claim as written, per question 5: it quotes the "4 points of precision"
sentence but not the next paragraph, `How these win values are displayed is at the discrecion of
the publisher, though it is reccomonded that the extra precions is only displayed with the base
bet-size is <$0.10`. That discretion clause weakens any argument built on the word "require"
alone. It does not save the finding, because the exact-win sentence is unconditional and is
breached at $0.10, but the finding should cite the exact-win sentence as its authority rather
than the precision count. Severity AGREE and player-visible.

## NEW FINDINGS FOUND WHILE VERIFYING

**N1. A second replay-surface social label with the same mechanism and the same absence of
coverage.** `frontend/src/lib/components/ReplayMode.svelte:321`:

    {mode === 'social' ? 'Play' : 'Bet'}: <strong>{formatBalance(Math.round(baseBet * CURRENCY_SCALE), params.currency, params.lang)}</strong>

Correct today, held by nothing. Unlike `currency`, `bet` is an active term in the scanner's
table, so this one WOULD be caught by a DOM scan, and the DOM scan's `surfacesCovered` does not
include replay. Same class as S2-C009, not named by it.

**N2. `vocabulary.ts:120`'s recorded justification for skipping `currency` is false at HEAD.**

    currency: 'appears only in code identifiers, never player-visible',

`ReplayMode.svelte:346` renders `{mode === 'social' ? 'Token' : 'Currency'}:` as a visible
label. This matters more than the skip itself: the skip is defensible as a policy, but its
stated reason is the thing a future reader will rely on when deciding whether the exemption is
still safe, and that reason is no longer true. The component's own comment two lines above even
says the opposite (`The word "currency" is itself on the stake.us prohibited-terms table
(currency -> token), so the label switches in social mode`), so the module and the component
disagree in writing.

**N3. `docs/QUALITY_CHARTER.md:199` (Q-27) overstates its own case.** Its disposition reads
`**OPEN.** The link colours are the sharpest, being a stock indigo in a cyan and magenta game.`
No anchor element exists in `frontend/src` or `frontend/index.html`, so `a { color: #646cff }`
and `a:hover { color: #535bf2 }` are unreachable rules and are the LEAST sharp items in the
list, not the most. This is a fourth instance of the class Q-29 already records about this
document.

clusters_assigned: 7
outcomes: CONFIRMED=3 REFUTED=0 CONFIRMED_WRONG_CAUSE=1 INCOMPLETE=2 UNKNOWN=0
new_findings: 3
tool_calls_used: 20
scripts_executed: NONE
tree_clean_after: ` M reports/qa/doc_currency/shards/D05.md` / ` M reports/qa/doc_currency/shards/D07.md` / ` M reports/qa/doc_currency/shards/D08.md` / `?? reports/qa/session2_audit/verify_shards/` (the three D0x modifications are NOT mine; this seat wrote exactly one file, `reports/qa/session2_audit/verify_shards/V06.md`, and executed no script)
