# V13: adversarial verification, COMPLIANCE batch 4 (8 clusters)

Read only. No project script was executed. All line citations re-derived at HEAD.

| cluster | claim_true | diagnosis_sound | severity_verdict | outcome | evidence_quote |
|---|---|---|---|---|---|
| S2-C046 | YES | YES | AGREE, HIGH (submission blocking, owner actionable only; not player visible) | CONFIRMED | `docs/stake-engine-live/2026-07-29/approval_guidelines_submission_checklist.md:26-28` "Login required / You must be logged in to view the approval guidelines. Log in →" ; `COMPLIANCE_WATCH.md:62-63` "the full criteria list is **login-gated** (portal auth) - capture the authenticated version on the owner's next portal login." |
| S2-C047 | YES | NO | TOO_HIGH, assign MEDIUM (duplicate of S2-C053 with no independent instance) | CONFIRMED_WRONG_CAUSE | `SUBMISSION_DOSSIER.md:71` "Verified and holding (evidence in audit/ and PR #1): stateless per platform" ; the pointer resolves: `audit/` holds `FS_PreSubmission_Compliance_Report.md`, `FS_FinalReview_Report.md`, `FS_MathSim_Audit_Report.md` and six more |
| S2-C048 | PARTLY | YES (for the malware half only) | AGREE, HIGH for the malware half; the second sentence is a foreign claim and carries its own KNOWN row | CONFIRMED | `frontend/package.json:6-13` scripts block exactly as quoted, and repo-wide grep for `malware\|anti-virus\|antivirus\|clamav\|virustotal\|npm audit` hits only `docs/stake-engine-live/2026-07-29/terms.md:371` and three Google Gemini licence texts: no scan target and no scan record exists |
| S2-C049 | YES | YES | AGREE, HIGH (contractual, cheap, player invisible) | CONFIRMED | `.github/workflows/checks.yml:183` `run: npm ci --ignore-scripts` with `:181` "# `npm ci` is reproducible and fails if the lockfile is out of sync." ; grep of the whole workflow for `audit\|sbom\|licen\|malware\|virus` returns only prose comments and unrelated step names |
| S2-C050 | YES | YES | TOO_HIGH, assign MEDIUM (values agree at HEAD, so nothing is player visible today; the exposure is ungated future drift) | INCOMPLETE | `frontend/src/lib/services/rgsService.ts:916-923` `const PAYTABLE: Record<string, Record<number, number>> = { H1: { 5: 22.00, 4: 6.00, 3: 1.50 }, ... L3: { 5: 0.65, 4: 0.20, 3: 0.08 }, }` a THIRD copy the finding does not name |
| S2-C051 | YES | YES | TOO_HIGH, assign MEDIUM (consistent with the MEDIUM already assigned to the same `round2_review3.md:23` row in V05) | CONFIRMED | `frontend/package.json:25-29` `"dependencies": { "@fontsource/orbitron": "^5.2.8", "pixi.js": "^7.4.3", "stake-engine": "github:StakeEngine/ts-client" }` ; no SBOM, licence inventory or copyleft scan is wired in `checks.yml` or present in `frontend/scripts/` |
| S2-C052 | YES | YES | TOO_HIGH, assign MEDIUM (evidentiary only: the sink is unreachable in a production build and telemetry.ts contains no network call at all) | CONFIRMED | `frontend/src/lib/services/telemetry.ts:83-84` `export function track(ev: DistributiveOmit<TelemetryEvent, keyof TelemetryBase> & Partial<TelemetryBase>): void {` / `  if (!sink) return` and `frontend/src/App.svelte:198-201` `if (import.meta.env.DEV) {` ... `setTelemetrySink(bufferSink(buf))` : both citations land exactly where the finding says, and `COMPLIANCE_WATCH.md:158-159` still names "`telemetry.ts:68-70`" and "`App.svelte:110-114`" |
| S2-C053 | YES | YES | AGREE, HIGH (it is the anchor two other requirements are evaluated against) | CONFIRMED | `docs/RGS_CONTRACT_REFERENCE.md:1-6` "# Stake Engine RGS wire-contract reference" ... "**cross-checked against our own `rgsService.ts`** so our development setup provably aligns." ; `SUBMISSION_DOSSIER.md:25-37` is thirteen rows and none is Game Documentation; every `Game Documentation` hit in the tree is `terms.md` itself or a register or shard row |

## PER CLUSTER REASONING

**S2-C046. CONFIRMED.** I tried to refute this with the strongest available counter: the
repository now holds a COMPLETE 64 page capture of the live docs dated today, so the obvious
hostile reading is that the criteria list has since been read. It has not. The capture of that
exact page exists at `docs/stake-engine-live/2026-07-29/approval_guidelines_submission_checklist.md`,
and at lines 26 to 28 it reads "Login required / You must be logged in to view the approval
guidelines. Log in →" sitting directly under line 24's "The checklist below reflects the exact
criteria your game will be reviewed against." So the page confirms both that the list is
authoritative and that we have never seen it. The finding is exactly right, and today's capture
strengthens rather than weakens it. Two things keep the severity honest rather than inflated.
First, nothing in the repository claims coverage of this list: `SUBMISSION_DOSSIER.md:19-20`
states the obligation ("the interactive approval checklist in the docs must be satisfied")
rather than asserting it is met, so this is a TRACKER_ROW over a genuinely PARKED requirement,
which is the one case where a tracker row is a legitimate proof form. Second, it is
unresolvable in the repository by anyone: only the owner's portal login can close it. I still
agree with HIGH, because the unread document is the precise criteria set the submission is
graded against, and an unknown of that shape belongs at the top of the owner's list rather than
in the middle of a builder's.

**S2-C047. CONFIRMED_WRONG_CAUSE.** The quotation is exact: `SUBMISSION_DOSSIER.md:75-79` is
verbatim what the finding reproduces, starting at line 75 with "the validated maths exactly;
spacebar spin;" and ending at 79 with "responsive verified at all six required viewports." So
the observation stands. The diagnosis does not. The row is filed NO_PROOF against REQ-135, the
requirement that the build not diverge from the documentation handed over, and it offers a bare
quotation with no divergence identified and no cause stated. I attacked its two available
supports and both held. Its own evidence pointer resolves: `audit/` exists and contains nine
committed reports including `FS_PreSubmission_Compliance_Report.md`, so the dossier's "(evidence
in audit/ and PR #1)" is not dangling. And I probed the quoted list for a false claim: the
responsive limb is conservatively true, not overstated, because `frontend/scripts/layout_fit_gate.mjs:66-74`
measures SEVEN platform presets, a superset of the six the dossier names. The one limb of the
list that genuinely has no proof, "static build, no external origins", is already a separate
ledger row: S2-C058 records that the network capture script is not a step in `checks.yml`, and
REQ-104 carries it in the register. **The real cause of the underlying gap is the one recorded
at S2-C053: there is no designated Game Documentation for the build to conform TO**, so
REQ-135 cannot be evidenced at all until C053 is closed. C047 as written adds no independent
instance to that, which is why I put it at MEDIUM: acting on it separately would produce a
second pass over the same missing artefact.

**S2-C048. CONFIRMED, and the row needs splitting before it can be actioned.** The
`frontend/package.json:6-13` quotation is exact, and the substantive half is true and survives
a hostile sweep: a repo-wide grep for `malware`, `anti-virus`, `antivirus`, `clamav`,
`virustotal` and `npm audit` hits only the platform's own warranty text at
`docs/stake-engine-live/2026-07-29/terms.md:371` ("the Developer has, in accordance with Good
Industry Practice, used all anti-virus and anti-malware software to screen the Game") and three
unrelated Google Gemini licence files. No scan target, no scan script and no scan record exists
anywhere. That is a real and cheaply fixable gap against a warranty we would be signing, so
HIGH is right. **But the claim as recorded is contaminated.** Its second sentence, "The mini
strip cuts the BALANCE with no ellipsis below about 390 css px: a player holding EUR 479,710.00
is shown `BAL €479`", has nothing to do with anti-malware screening, cannot be supported by the
cited path `frontend/package.json:6-13`, and belongs to a class that is already open and
already owned: `reports/qa/stream_test/KNOWN_OPEN.md:16` TR-115 / TR-086, "Money display fit
failures as one class: pods clipping, ellipsising or overflowing at various widths and
magnitudes. OPEN, HIGH, mapped to final-mile JOB 3". Left as one row this will send a fixer to
package.json to fix a HUD truncation. Split it: the malware half stays under REQ-136, the
balance half is KNOWN(TR-115).

**S2-C049. CONFIRMED.** `checks.yml:179-183` is quoted exactly, comments included. I tried two
refutations. The first: perhaps a security step exists elsewhere in the file. Grepping the
whole workflow for `audit`, `security`, `scan`, `sbom`, `licen`, `malware` and `virus` returns
only prose comments (line 23 "SECURITY POSTURE. This repository is PUBLIC.") and step names
belonging to other classes (`dead wiring scan`, `wallet float scan`, `dash gate, dist scan`).
There is no `npm audit`, no SCA, no dependency vulnerability check in either job. The second:
perhaps `--ignore-scripts` is being unfairly characterised. It is not; the flag is a security
positive (it blocks install-time script execution) and it supports rather than undercuts the
claim that this step is the only dependency-security-relevant one. One literal imprecision
worth recording without changing the verdict: there are TWO such steps, `:183` in the static
job and `:617` in the browser job, so "the only dependency-security-relevant step in either
job" is loose phrasing for a single step type appearing once per job. The substance holds.
HIGH is right for the same reason as C048: contractual, absent, and about a day's work for the
whole family.

**S2-C050. INCOMPLETE.** Both halves of the observation are true. I re-derived every value
rather than spot-checking the two the finding quotes, and all twenty-four agree:
`games/future_spinner/game_config.py:119-128` against `frontend/src/lib/components/PaytableModal.svelte:72-83`,
H1 22/6/1.5, H2 10/3/0.8, M1 5/1.5/0.45, M2 4/1/0.3, M3 2/0.6/0.2, L1 1.5/0.45/0.15,
L2 0.8/0.25/0.10, L3 0.65/0.20/0.08. And the NO_PROOF half survives: no gate in `checks.yml`
and no test among the twenty compares the two tables. The nearest candidate,
`frontend/src/lib/config/fsModes.drift.test.ts`, reads only mode costs from index.json
(`const index: { modes: Array<{ name: string; cost: number }> } = JSON.parse(readFileSync(indexPath, 'utf-8'))`
at line 20), and `paytable_card_fill_gate.mjs` is a card-fill layout gate, not a value gate.
**Where the finding falls short is its class sweep: it names two declarations of the paytable
and there are at least three.** `frontend/src/lib/services/rgsService.ts:916-923` holds a
complete third copy, and `:927` holds a third copy of the scatter table
(`const SCATTER_TABLE: Record<number, number> = { 3: 1, 4: 3, 5: 10 }`). A fix that gates
PaytableModal against game_config and stops there leaves the class open. Severity is TOO_HIGH:
REQ-140 is marked YES in the register, the numbers actually agree, and no player can see
anything today. This is drift insurance, which is MEDIUM.

**S2-C051. CONFIRMED, with its evidence sentence overstated.** The core claim holds against a
hostile sweep: nothing in `checks.yml`, nothing in `frontend/scripts/`, nothing under
`reports/` and no file in the tree constitutes an SBOM, a dependency licence inventory or a
copyleft scan. The `licence paths` phrase at `checks.yml:478` is about an audio generation-notes
README that once shipped in dist, not a licence gate. Two accuracy notes against the finding
rather than against the requirement. First, its citation `frontend/package.json:24-28` is off by
one: the `dependencies` block is lines 25 to 29, line 24 being a closing brace. Second, its
grep sentence ("returns only the platform's own clause text plus `round2_review3.md:23`") is
not quite true, because `docs/licences/google-gemini/2026-07-15/` holds three committed
licence documents. Those are asset-generation terms, not third-party software licences, so
they do not touch REQ-143's copyleft question, but a reader checking the finding's own grep
would find them and lose confidence in the row. Severity TOO_HIGH: the direct runtime
dependency set is three packages, all mainstream and permissive, the deliverable is one
generated manifest, and V05 already assigned MEDIUM to a sibling claim resting on the same
`round2_review3.md:23` row. MEDIUM keeps the two consistent.

**S2-C052. CONFIRMED on all three limbs, severity TOO_HIGH.** The citation-drift limb is the
one I expected to fail and it is exactly right: `COMPLIANCE_WATCH.md:158-159` still names
`telemetry.ts:68-70` and `App.svelte:110-114`, while at HEAD `track()` and its `if (!sink) return`
guard are at `telemetry.ts:83-84` and the DEV sink registration is at `App.svelte:198-201`.
Both of the finding's new citations land on the exact lines. The no-gate limb also holds: a grep
for `elemetry` across `frontend/src`, `frontend/scripts` and `scripts` returns production call
sites plus two comment mentions in `typecheck_baseline.mjs` and one in `soundService.ts`, and
no gate script mentions telemetry at all; `dist_hygiene_gate.mjs` asserts documentation absence
(`DOC_BASENAMES`, `isDoc`) and `data:` URI absence, nothing about endpoints. So there is no
CI_GATE, no UNIT_TEST and no EVIDENCE that would go red if a sink or an endpoint appeared. I
part company with the finding on severity. The registration is inside `if (import.meta.env.DEV)`,
which Vite statically replaces, so the block is eliminated from a production build; `track()`
returns on the first line without a sink; and `telemetry.ts` contains no `fetch`, no
`XMLHttpRequest`, no `WebSocket` and no `sendBeacon` at all, so there is no endpoint to leak to
in the first place. The defect is purely evidentiary. MEDIUM. Worth saying plainly, though: the
drifted citations are themselves a demonstration of the risk, since the prose was verified once
by hand and the code has moved twice underneath it.

**S2-C053. CONFIRMED.** Every limb checks out. The `RGS_CONTRACT_REFERENCE.md` quotation is
verbatim from lines 1 to 6. The inventory at `SUBMISSION_DOSSIER.md:23-37` is thirteen rows and
I read all thirteen: build, maths files, PAR sheet, blurb, tile BG, tile FG, provider logo,
staged bundle, portal facts sheet, compliance evidence pack, high-resolution asset link,
trademark position, portal team profile. No Game Documentation row. And the grep holds: every
`Game Documentation` occurrence in the tree is inside `docs/stake-engine-live/2026-07-29/terms.md`
or inside a register, walk or shard row. The hostile counter I tested is that documentation
fit for the purpose plainly exists (the PAR sheet, `RGS_CONTRACT_REFERENCE.md`, `GAME_FACTS.md`,
the dossier itself), so the requirement might be substantively met and merely unlabelled. That
counter fails on the wording of the requirement rather than on charity: `terms.md:227` defines
Game Documentation as what is handed over on the Effective Date, and `terms.md:353` and `:369`
make the build's conformance TO that named artefact a warranty. An undesignated pile of
documents cannot be the referent of a conformance warranty, and the finding says precisely
"no repository artefact is DESIGNATED", which is the careful and correct claim. HIGH agreed,
because REQ-135 and REQ-183 both hang off it: until one artefact is designated and assembled,
neither can be evidenced at all. Not player visible.

## NEW FINDINGS FOUND WHILE VERIFYING

**NEW-V13-01. A third declaration of the paytable, and of the scatter table, inside a LOCKED
money-path file, ungated.** Found while sweeping S2-C050's class. `frontend/src/lib/services/rgsService.ts:916-923`:

```
const PAYTABLE: Record<string, Record<number, number>> = {
  H1: { 5: 22.00, 4: 6.00, 3: 1.50 },
  ...
  L3: { 5:  0.65, 4: 0.20, 3: 0.08 },
}
```

and at `:927` `const SCATTER_TABLE: Record<number, number> = { 3: 1, 4: 3, 5: 10 }`. Both agree
with `games/future_spinner/game_config.py` at HEAD, so there is no live defect. Two reasons it
matters anyway. It is a third copy of numbers the register treats as a single truth under
REQ-140, REQ-061 and REQ-074, and no gate or test compares any of the three. And it is the
table the DEV mock path pays from (`_pickSymbol()` immediately below it uses `Math.random()`),
which is the path the browser gate fleet and much of the committed screenshot evidence runs
against, so a drift here would silently mispay every proof capture while the shipped maths
stayed correct. I searched `reports/qa/session2_audit/LEDGER.md` for an existing row on this and
found none. **LOCKED** (`rgsService.ts`), so it rides with the next sanctioned pass rather than
being fixed now; the gate that would catch it lives outside the locked file and can be written
today. Severity MEDIUM, evidence integrity, not player visible.

No other new defect was found. Two things I checked and deliberately did NOT raise, so the next
reader does not re-derive them: the "six required viewports" phrasing at `SUBMISSION_DOSSIER.md:79`
and `GAME_FACTS.md:306` against seven presets in `layout_fit_gate.mjs:66-74` is a gate exceeding
its documented claim, not a divergence; and the absence of any gate asserting no external
origins in `dist` is already recorded as S2-C058 and REQ-104.

clusters_assigned: 8
outcomes: CONFIRMED=5 REFUTED=0 CONFIRMED_WRONG_CAUSE=1 INCOMPLETE=1 UNKNOWN=0
new_findings: 1
tool_calls_used: 22
scripts_executed: NONE
tree_clean_after: NOT CLEAN, and none of it is mine except my own shard. Exact porcelain:
```
 M reports/qa/doc_currency/shards/D05.md
 M reports/qa/doc_currency/shards/D07.md
 M reports/qa/doc_currency/shards/D08.md
?? reports/qa/session2_audit/verify_shards/
```
I opened none of the three `reports/qa/doc_currency/shards/` files and executed no script. They
were already modified in the working tree when this seat started, so they belong to another
session or another seat; flagging them rather than touching them, per multi-track rule 11.
