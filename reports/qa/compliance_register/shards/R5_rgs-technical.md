# R5, rgs-technical: requirement register from the RGS technical captures

Read: `docs/stake-engine-live/2026-07-29/rgs.md`, `docs/stake-engine-live/2026-07-29/rgs_wallet.md`,
`docs/stake-engine-live/2026-07-29/rgs_example.md`. Enumeration only, no implementation check.
Quotes are verbatim from the capture bodies below each `capture_note` line, including upstream
typographic characters and upstream spelling errors.

| REQ | source_file:line | verbatim_quote | obligation_on | what_it_requires | player_visible |
| --- | --- | --- | --- | --- | --- |
| R5-01 | rgs.md:35 | "All flows require the /wallet/authenticate API to be called when the game first loads. This authorizes the sessionID to be used by the /wallet/play, /wallet/balance and /wallet/end-round endpoints." (trimmed from a longer single line; the sentence that follows states the 400 ERR_IS consequence) | ARTEFACT | Call /wallet/authenticate on first load and before any play, balance or end-round call; do not issue those three before authenticate has returned. | YES |
| R5-02 | rgs.md:37 | "This flow takes creates a round and will close the round after all animations have been complete. It accomplishes this by calling the /wallet/play API and then calling the /wallet/end-round API when the round is complete." | ARTEFACT | Every round opened by /wallet/play is closed by /wallet/end-round once the round, including its animations, has finished. | YES |
| R5-03 | rgs_wallet.md:26 | "The round returned may represent a currently active or the last completed round. Frontends should continue the round if it remains active." | ARTEFACT | On load, inspect the round object from authenticate and resume an active round rather than starting fresh; rgs.md:41 names round.event as the resume position. | YES |
| R5-04 | rgs.md:45 | "Games are hosted under a predefined URL. Providers should use the parameters below to interact with the RGS on behalf of the user and correctly display game information." | ARTEFACT | Read sessionID, lang, device and rgs_url from the launch URL query string and drive both RGS calls and display from them. | YES |
| R5-05 | rgs.md:51 | "sessionID Unique session ID for the player. Required for all requests made by the game." (table cells on one source line, tab rendered here as a single space) | ARTEFACT | Include the launch sessionID in the body of every RGS request the game makes. | NO |
| R5-06 | rgs.md:54 | "This URL should not be hardcoded, as it may change dynamically." (trimmed from the rgs_url table row on the same line) | ARTEFACT | Take the RGS base URL from the rgs_url query parameter at runtime; no build-time or constant RGS host anywhere in the shipped bundle. | NO |
| R5-07 | rgs.md:57 | "The lang parameter should be an ISO 639-1 language code." | ARTEFACT | Interpret lang as a two-letter ISO 639-1 code when selecting the display language (the page then lists 16 supported codes plus 'da'). | YES |
| R5-08 | rgs.md:80 | "Monetary values in the Stake Engine are integers with six decimal places of precision:" | ARTEFACT | Send and receive all money as integer micros at 1e6 scale, converting only for display; a $1 bet is the integer 1000000 (rgs.md:88). | YES |
| R5-09 | rgs.md:90 | "Currency impacts only the display layer; it does not affect gameplay logic." | ARTEFACT | Keep currency out of round logic, bet arithmetic and outcome selection; it may change only formatting. | NO |
| R5-10 | rgs.md:144 | "Here are some functions that will help you achieve the display format for the currencies." | ARTEFACT | Format displayed balances with the platform's per-currency symbol, decimal count and symbol placement (the CurrencyMeta table at rgs.md:204 to 257), falling back to code-after-amount for unknown codes. Normative force is weaker than the other rows here: the page offers the functions as help rather than mandating them, and the obligation is the display format they produce. | YES |
| R5-11 | rgs.md:288 | "The bet must fall between minBet and maxBet (returned from /wallet/authenticate)." (lead-in on rgs.md:286 reads "Although bet levels are not mandatory, bets must satisfy these conditions:") | ARTEFACT | Clamp every wager the game can submit to the authenticated minBet and maxBet, not to a hardcoded ladder. | YES |
| R5-12 | rgs.md:289 | "The bet must be divisible by stepBet." | ARTEFACT | Every submittable bet is an exact integer multiple of the authenticated stepBet. | YES |
| R5-13 | rgs.md:291 | "It is recommended to use the predefined betLevels to guide players." | ARTEFACT | Drive the player-facing bet ladder from the betLevels array in the authenticate response. | YES |
| R5-14 | rgs.md:295 | "New game submissions should incorporate small denomination bets, which are not yet industry standard, these are levels (in USD): [$0.01, $0.02, $0.05, $0.10, ....]." | ARTEFACT | Offer bet levels starting at $0.01, $0.02, $0.05 and $0.10 rather than a $0.10 floor. | YES |
| R5-15 | rgs.md:295 | "If the game has a minimum win of >= 0.1x, three points of precision are required: 0.1x * $0.01 = $0.001, while games with minimum wins <0.1x will require 4 points of precision." | ARTEFACT | Determine the game's minimum win multiplier and render win amounts with 3 decimal places if it is >= 0.1x, 4 if it is below. | YES |
| R5-16 | rgs.md:297 | "How these win values are displayed is at the discrecion of the publisher, though it is reccomonded that the extra precions is only displayed with the base bet-size is <$0.10." | ARTEFACT | Show the extra decimal places only while the base bet is under $0.10; above that, use normal currency precision. | YES |
| R5-17 | rgs.md:297 | "The ‘balance’ value displaying the players bankroll does not need to display more than 2 points of precision at any time, and it is only a requirement that wins in-game show exact win amounts." | ARTEFACT | In-game win readouts must show the exact win amount with no rounding; the balance readout may stay at 2 decimal places. | YES |
| R5-18 | rgs.md:319 and rgs.md:321 | "When making a play request: Player debit amount = Base bet amount × Bet mode cost multiplier" (joined from two source lines with a single space) | ARTEFACT | Compute and present the cost of a play as base bet times the selected bet mode's cost multiplier, and expect the wallet to debit that amount. | YES |
| R5-19 | rgs.md:341 | "When publishing math results, ensure that the file-format is abided by. These are strict conditions for successful math file publication." | MATHS | The published math files must match the platform's file-format specification exactly; format errors block publication. Both captures end on this same sentence (also rgs_wallet.md:151); the format itself is not stated on these pages. | NO |
| R5-20 | rgs_wallet.md:81-85 | "{ "amount": 100000, "sessionID": "xxxxxxx", "mode": "BASE" }" (joined from the request JSON block, one space per line break) | ARTEFACT | The play request body carries amount (integer micros), sessionID and mode, so the selected bet mode must reach the RGS on every play. Note the page gives an explicit POST path for authenticate, balance, end-round and /bet/event but none for play. | NO |
| R5-21 | rgs_example.md:33 | "If your win is greater than 0, you’ll need to manually call the /end-round API to finalize the bet—just like in a custom frontend implementation." | ARTEFACT | A winning round is not paid until the frontend itself calls /end-round; the game must call it rather than assume the RGS settles automatically. | YES |
| R5-22 | rgs_example.md:39-43 | "This will generate: A Zstandard-compressed set of simulation results A lookup table matching each result to its simulation The required index.json file" (joined from five source lines with a single space) | MATHS | The publication set must include the Zstandard-compressed simulation results, the matching lookup table, and index.json, which the page marks as required. | NO |
| R5-23 | rgs_example.md:57 | "Edit the vite.config.ts file: Make sure the defineConfig function includes: base: "./" (under plugins)," | ARTEFACT | A Vite-built frontend must set base to "./" so the bundle resolves its assets relative to the hosted game path. | YES |
| R5-24 | rgs_example.md:68 | "Upload the contents of the dist/ folder to the Stake Engine under frontend files" | PROCESS | At submission, upload the contents of dist/ (not the folder itself) into the platform's frontend files slot. | NO |

rows_enumerated: 24
pages_read: rgs.md, rgs_wallet.md, rgs_example.md
duplicates_noted: R5-01 is restated inside my own pages at rgs_wallet.md:22 ("Validates a sessionID with the operator. This must be called before using other wallet endpoints. Otherwise, they will throw ERR_IS (invalid session).") and is kept as one row; R5-19 and R5-22 will almost certainly restate the math-publication squad's rows on file format and index.json; R5-08 (integer micros at six decimals) and R5-10 (currency display format) are likely to appear in any currency or money-handling page another squad holds; R5-14, R5-15, R5-16 and R5-17 (small denominations and win precision) are the kind of clause the approval-guidelines or quality-ranking pages repeat; R5-24 overlaps whatever squad holds the portal or submission pages. Two deliberate non-rows, recorded so the gap is visible rather than looking like an omission: the 400 and 500 error-code tables (rgs.md:327 to 338, rgs_wallet.md:137 to 148) state what the API returns and place no obligation in their own words, and the jurisdiction flags in the authenticate response (rgs_wallet.md:47 to 52, socialCasino, disabledFullscreen, disabledTurbo) appear only as response fields on these pages with no clause saying the game must honour them, so the obligation to honour them is UNKNOWN from my pages and probably belongs to the jurisdiction squad. Not bounded; all three captures were read in full.
could_not_open: NONE
tree_clean_after: ?? reports/qa/compliance_register/ (the only entry; git collapses the new shard directory to its untracked parent, which is my own shard path and is expected)
