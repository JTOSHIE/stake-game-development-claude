# STC-DESKTOP-A, composition lens (desktop session, 1200x675, frames 001 to 026)

scope: Every frame of the `desktop` (en) session numbered 001 to 026 inclusive, which is the splash, the intro rules card, base idle, both spin transitions, the three dead spins, the standard win presentation, the big win triple, the HUD menu pair, the session panel, and the paytable from its opening view through section 06 Bet Modes. 26 frames, all opened once each with the Read tool. Judged as compositions and as a sequence. Geometry figures below were measured by decoding the PNGs directly (zlib, pure python, nothing written to disk) and every one is quoted with the frame it came from.
frames_read: 26

## STC-DESKTOP-A-01 STREAM The board goes see-through during spin-up, with the street scene visible through empty reel cells

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/007_desktop_transition_reels_accelerating.png (controls: 006_desktop_base_idle.png and 008_desktop_transition_reels_full_speed.png)
- Claim: At `about 250ms after spin press` (MANIFEST.json note for 007) the lower half of reels 2 and 3 carries no tile plate and no symbol, and the scene behind the board reads through the hole: the car's roofline, windscreen and a pink street sign are legible inside the reel window at roughly x400 to x660, y300 to y460. Measured over the identical region x460-640, y385-455: `006` idle reads mean RGB `(21.1, 23.2, 34.7)`, `008` at full speed reads `(29.7, 29.4, 47.5)`, and `007` reads `(36.3, 54.9, 74.5)`, which is the profile of the scene car sampled OUTSIDE the board in 006 at x180-290, y420-470, `(46.2, 76.4, 94.7)`. A dark idle tile in the same frame measures `(23.8, 24.5, 35.3)` (006, x360-440, y390-450), so the region is not a dark symbol, it is the backdrop. Reels 4 and 5 still hold their static tiles at that instant, so the board reads as three columns punched out beside two intact ones. This is on the path of every single spin, which is the most-repeated three seconds on any stream of this game.
- Where fixable: frontend/src/lib/components/GameGrid.svelte:69-74 (not locked). The strip geometry is `STRIP = ROWS + 3`, `7 slots: [buf, r0..r3, buf, buf]` with `REST_Y = -104`, that is ONE buffer tile above the window and two below, so the window is uncovered after slightly more than one tile of travel in the short direction. The re-seed point itself was not pinned in this pass.
- Proposed fix: give the strip a symmetric buffer (one more slot on the short side) or re-seed the wrap before the single buffer is consumed, so the window can never be uncovered mid-travel. Worth a seeded-violation gate per convention (p): a frame grab at 250ms that asserts no scene pixel inside the reel window.

## STC-DESKTOP-A-02 HIGH Paytable list markers are stranded at the left padding while every bullet line is centred

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/024_desktop_paytable_04_rules.png, 023_desktop_paytable_03_symbol_payouts.png, 025_desktop_paytable_05_overdrive_free_spins.png
- Claim: In 024 the chevron markers sit in a column at x80 to x82 for every bullet, while the text each one marks is centre-aligned and therefore starts at a different x on every line: `Wins pay left to right on adjacent reels starting from reel 1.` begins at x408, `Symbol values shown are per matching way; the total is that value times the number of ways times your bet.` at x234, and `3, 4, or 5 SCATTERs anywhere apply a 1×, 3×, or 10× multiplier to your total bet win.` at x326. The gap between a marker and its own text is therefore `326px`, `152px` and `244px` on three consecutive lines of one list. The same pattern runs down the RULES block in 023 and the Overdrive block in 025. A marker that does not travel with its text is not a list, and at this width it reads as two columns of unrelated content.
- Where fixable: frontend/src/lib/components/PaytableModal.svelte:667-669 (not locked): `.fs-rules li { font-size: 0.84rem; ... padding-left: 16px; position: relative; ... }` and `.fs-rules li::before { content: '›'; position: absolute; left: 0; ... }`. The li carries no `text-align` of its own, so the centring is inherited from an ancestor, and the fix site is the same rule either way.
- Proposed fix: add `text-align: left` to `.fs-rules li` (PaytableModal.svelte:668) so the text starts at its marker; that also closes STC-DESKTOP-A-11.

## STC-DESKTOP-A-03 HIGH The paytable panel jumps 23px up the moment it is scrolled

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/019_desktop_transition_paytable_opening.png and 020_desktop_paytable_top.png against 021_desktop_paytable_01_match_symbols_on_adjacent_reels_st.png, 022, 023, 024, 025 and 026_desktop_paytable_06_bet_modes.png
- Claim: The panel is the same size in both states and sits in two different places. In 019 and 020 its top border is the long light line at `y28` and its bottom border at `y646` to `y647`, so it is vertically centred with `28px` above and `28px` below in a 675px viewport. In 021 through 026 the top border is at `y5` and the bottom at `y623` to `y624`: `5px` above and `51px` below. Height is `619px` then `618px`, so nothing resized, the whole panel moved up `23px`. The yellow left accent bar confirms it independently: the same 307px element occupies `y323..630` at column x49 in 019 and 020 and `y300..607` in 021 through 026. The panel is `x48..1151` in every frame, so the shift is vertical only, and `92%` of 1200 is the measured 1104px width, matching the spec below.
- Where fixable: frontend/src/lib/components/PaytableModal.svelte:544-552 (`.fs-pt-panel { width: 92%; max-width: 1178px; height: 92%; max-height: 662px }`, not locked) sits inside the scrim at frontend/src/app.css:64-71 (`.fs-scrim { position: fixed; left: 50%; top: 50%; width: calc(100vw / var(--scrim-scale, 1)); height: calc(100dvh / var(--scrim-scale, 1)); transform: translate(-50%, -50%) }`), and PaytableModal.svelte:445-450 records the governing fact in the file's own comment: *App.svelte's transform:scale re-anchors position:fixed descendants to the stage*. A fixed element inside a transformed ancestor scrolls with the document, and frontend/src/app.css:127 sets `min-height: 100vh`, so the page has something to scroll.
- Proposed fix: lock the document while a modal is open (`overflow: hidden` on the root element for the life of the overlay) so the only thing that can scroll is `.fs-pt-body`, and the panel keeps its 28/28 centring throughout.

## STC-DESKTOP-A-04 MEDIUM The win ticker plate is drawn over the board's bottom neon edge, breaking the frame into two corner stubs

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/015_desktop_bigwin_settled.png, 014_desktop_transition_bigwin_countup_late.png, 016_desktop_transition_menu_opening.png, 017_desktop_hud_menu.png (controls: 009_desktop_dead_spin_1_settled.png and 012_desktop_win_presentation.png, which carry no ticker)
- Claim: Proven against a control rather than asserted. At column x400 the board's cyan bottom border occupies rows `y464..472` in both 009 (`(18, 222, 242)` at y470) and 012, and in 015 those rows read `(10, 34, 45)`, dark. The covering element's own edges are visible in the same column profile: silver plate edge at `y458..459` (`(199, 213, 222)`), dark face from `y460` to `y481`, silver edge again at `y482..483`, cyan glow resuming at `y484`. Horizontally the plate runs `x360` to `x842`, so scanning row y470 across the board the only lit runs are `x300..360` and `x843..899`, the two corner brackets, with the ticker's own glyphs (`M3  x5  8 ways  $16.00`) between them. The strip therefore erases roughly 480px of the board's 600px lower edge and leaves the neon burning at both corners. The board is the one object on screen with a deliberate frame, and a HUD strip that erases the middle of that frame reads as dropped on rather than docked to.
- Where fixable: frontend/src/lib/components/WinBreakdown.svelte:114-121 (not locked): `.win-breakdown { position: absolute; left: 50%; bottom: 6px; transform: translateX(-50%); z-index: 45; ... }`, mounted inside the scaled grid at frontend/src/App.svelte:1925. `bottom: 6px` measures from the grid's own bottom, which is exactly where the neon edge is drawn.
- Proposed fix: raise the offset so the plate clears the lit edge (the edge occupies about 9 screen px, so `bottom: 16px` inside the grid), or dock the plate below the frame entirely; one property either way.

## STC-DESKTOP-A-05 MEDIUM The HUD menu opens on top of the live win ticker and cuts the string in half

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/017_desktop_hud_menu.png, 016_desktop_transition_menu_opening.png
- Claim: The menu panel is an opaque dark slab from `x366` to `x596` (fill sampled at `(6, 7, 19)` at row y430, board tile immediately to its left at `(10, 34, 46)`) and from `y397` to `y536`. The win ticker underneath keeps running: at row y470 the first lit pixel to the right of the panel is at `x598`, so everything left of x596 is hidden and the viewer is left with the tail `ways  $0.20`, no symbol id and no multiplier. Two live surfaces are stacked and neither yields. Recorded honestly beside it, because I checked it and it is NOT a defect: the panel's bottom edge at y536 sits about 10px below the control bar's top border (measured `y525..526` in 006), which is simply the consequence of anchoring the menu 8px above a hamburger button that is itself inset in the bar.
- Where fixable: frontend/src/lib/components/HudOverlay.svelte:1598-1607 (`.hud-menu { position: absolute; bottom: calc(100% + 8px); left: 0; min-width: 200px; background: rgba(6, 6, 18, 0.96); ... }`, not locked), against frontend/src/lib/components/WinBreakdown.svelte:88 (`{#if visible && current}`, which has no menu-open condition).
- Proposed fix: suppress the ticker while any HUD dropdown is open, the same way it is already suppressed on a capped round via the `suppressed` prop at App.svelte:1925.

## STC-DESKTOP-A-06 MEDIUM The big win amount is not on the optical centre of its own band

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/014_desktop_transition_bigwin_countup_late.png and 015_desktop_bigwin_settled.png (013_desktop_transition_bigwin_countup_early.png is the same layout)
- Claim: In both 014 and 015 the amount `$16.20` occupies `x514..715`, centre `x614.5`, against a viewport centre of `x600`: it sits `14.5px` right. The band's outer margins are symmetric, `BIG WIN` at `x211..321` (211px in from the left edge) and `16x BET` at `x905..986` (214px in from the right edge), so a symmetric three-part band is plainly the intent. The gaps either side of the amount are equal, `193px` and `190px`, which is what fixes the fault: the amount is centred on the space BETWEEN the two labels rather than on the band, and those labels differ in width by `29px` (110 against 81), which is `14.5px` of half-difference, the exact offset measured. The hero figure of the game's loudest moment is therefore off axis by half a label-width difference, and it will move again in any locale where either label changes width.
- Where fixable: frontend/src/lib/components/WinBanner.svelte:362-364 (`.fs-plate > .fs-face { display: flex; flex-direction: row; align-items: baseline; justify-content: center; width: 100% }`) and :382 (`.tier-big .fs-face { min-height: 110px; padding: 18px 60px; gap: 32px }`), not locked.
- Proposed fix: take the amount out of the flow of the two labels, either by absolutely centring it on the band or by giving the two flanking labels equal flex basis, so the figure holds the centre whatever the labels say.

## STC-DESKTOP-A-07 MEDIUM The base game's control row is centred about 48px right of the board it sits under

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/006_desktop_base_idle.png, and identically 009_desktop_dead_spin_1_settled.png, 010, 011, 012_desktop_win_presentation.png
- Claim: The board is centred on the viewport: the reel frame spans `x301..911` at row y90 (centre `x606`) and `x299..905` at row y490 (centre `x602`), against a viewport centre of `x600`. The control row below it is not: measured at row y567 it runs from the turbo button's left arc at `x213` to the recall button's right edge at `x1086`, centre `x649.5`, and the HUD pod panel's own bottom border spans `x306..939`, centre `x622.5`. That leaves `213px` of empty viewport to the left of the row against `114px` to the right, an imbalance of about 1.9 to 1, and puts the row's axis about `48px` off the board's. The mascot art fills part of the left margin, which is why it does not read as broken today, but the two stacked primary blocks do not share a centre line and in a cropped stream overlay the row will read as sliding right.
- Where fixable: frontend/src/lib/components/HudOverlay.svelte, the desktop layout branch (not locked). The exact container rule was NOT pinned in this pass: the file carries four layout branches and their bottom-row class did not match the names I searched (`hud-bottom`, `d-hud`, `bottom-row`, `hud-row`).
- Proposed fix: centre the whole control row (turbo through recall) on the same axis as the reel frame, or state the offset as deliberate and move the board to match. PARK(the choice is an art call, and it is one measurement away from being decided).

## STC-DESKTOP-A-08 MEDIUM Four bet mode cards in one row do not share an internal baseline

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/026_desktop_paytable_06_bet_modes.png
- Claim: The four cards are identical boxes: all share a top border at `y104..106` and a bottom border at `y276..277`, with edges at `x78..329`, `x343..593`, `x607..857` and `x871..1121`, four 250px cards on 14px gutters. Their contents do not line up. Title glyphs occupy `y134..143` on `Normal`, `Cruise` and `Buy Overdrive`, and `y126..135` on `OVERBOOST`: the third card's title sits `8px` higher than its three neighbours, and its blurb follows it (`y226..248` on the three, `y218..256` on OVERBOOST). The lone `NITRO OVERDRIVE` card on row two repeats the higher position (card top `y292`, title glyphs `y314..323`, a 22px inset matching OVERBOOST's 22px against the other three cards' 30px). Four cards side by side with two different title baselines is what a rival studio's art director sees first.
- Where fixable: frontend/src/lib/components/PaytableModal.svelte:695-697 (not locked): `.fs-mode-card > .fs-face { flex-direction: column; align-items: center; text-align: center; gap: 10px; padding: 16px 14px }` overrides five properties of the base rule at :497-506 but NOT `justify-content: center`, which it inherits. The grid stretches every card to the tallest in the row (OVERBOOST, whose blurb runs to three lines), so every shorter card vertically centres its content and drops its title by half the surplus, which is the measured 8px.
- Proposed fix: add `justify-content: flex-start` to `.fs-mode-card > .fs-face` (PaytableModal.svelte:696), so all cards top-align and the extra blurb line hangs at the bottom where it belongs. One property.

## STC-DESKTOP-A-09 MEDIUM The paytable's two card grids break to ragged rows, leaving a third to two thirds of the last row empty

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/023_desktop_paytable_03_symbol_payouts.png, 022_desktop_paytable_02_ways_to_win.png, 026_desktop_paytable_06_bet_modes.png
- Claim: Symbol payouts run six cards per row on a 1043px content width (023, row one card edges measured at y200: `78/242`, `254/418`, `430/593`, `606/770`, `782/945`, `958/1121`), so the ten symbols break 6 plus 4 and row two ends at `x770` (023 at y400 shows edges only at `78/242`, `254/418`, `430/593`, `606/770`), leaving the last two slots, `351px` of panel, empty under a full row. Bet modes are worse: four cards fill row one and `NITRO OVERDRIVE` sits alone on row two (026 at y380 shows exactly two card edges, `78..79` and `327..328`), leaving `792px` of empty row beside it. At this viewport both grids read as unfinished rather than as breathing room, and five modes would sit as a balanced single row or 3 plus 2.
- Where fixable: frontend/src/lib/components/PaytableModal.svelte:650 (`.fs-sym-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 12px }`) and :690 (`.fs-mode-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(230px, 1fr)); gap: 14px }`), not locked. `auto-fill` on 1043px yields exactly 6 and exactly 4 columns, which is where 6+4 and 4+1 come from.
- Proposed fix: pick column counts that divide the sets at the wide breakpoint, `repeat(5, 1fr)` for the ten symbols and `repeat(5, 1fr)` (or 3 plus 2) for the five modes, keeping the existing `auto-fill` rules below the 799 media query at :798-799 for narrow sessions.

## STC-DESKTOP-A-10 MEDIUM The Overdrive scatter table hangs hard left under a centred heading, with 630px of empty panel beside it

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/024_desktop_paytable_04_rules.png, 025_desktop_paytable_05_overdrive_free_spins.png
- Claim: The heading `OVERDRIVE FREE SPINS` is centred on the panel (centred text in this modal measures to a centre of about `x607`, frame 024), while the three column table it heads, `SCATTERS` / `FREE SPINS` / `INSTANT AWARD` with its three rows, occupies only `x80` to `x490`. Heading and table therefore sit on two axes about 300px apart, and the right `630px` of panel beside the table is empty. It is the largest dead region in the whole 26 frame run, and it sits directly under a heading that promises the panel is full.
- Where fixable: frontend/src/lib/components/PaytableModal.svelte:677-678 is the section heading rule (`text-align: center; padding: 8px 0; font-weight: 700`), not locked. The scatter table's own rule was not pinned by name in this pass.
- Proposed fix: centre the table under its heading, or left-align the heading over the table; one rule either way, and the second choice also matches the STC-DESKTOP-A-02 fix.

## STC-DESKTOP-A-11 LOW The paytable's centred prose is centred 7.5px right of the panel's own axis

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/024_desktop_paytable_04_rules.png
- Claim: The panel spans `x48..1151` and its content box spans `x78..1121`, so both have a centre of `x599.5`. The centred bullet lines measure to a centre of `x607` on four separate lines (text spans `x408..806`, `x234..981`, `x326..889`, `x482..732`). That is exactly half of the `16px` marker gutter: the text is centred inside a box that starts at `x94` and ends at `x1121`, centre `x607.5`. The measurement and the stylesheet agree to half a pixel.
- Where fixable: frontend/src/lib/components/PaytableModal.svelte:668 (`.fs-rules li { ... padding-left: 16px; ... }`), not locked.
- Proposed fix: rides with STC-DESKTOP-A-02; left-aligning the list text removes the offset entirely, and if the centred look is kept instead, the gutter needs a matching `padding-right: 16px`.

## STC-DESKTOP-A-12 LOW The `WIN!` callout is wedged between the win path and a cell border with about 2px of clearance

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/012_desktop_win_presentation.png
- Claim: `WIN!` occupies `x549..653`, `y272..301`, so it is properly centred on the board horizontally (centre `x601`). Vertically it is jammed: sampling column x600, the cyan win path stroke ends at `y270` and the next cell border is at `y303`, so the callout has about `2px` of air above and below, with no plate or halo behind it, sitting on top of a highlighted winning cell. Nothing else in the frame is that tight, and the effect is that the game's win word looks placed rather than composed.
- Where fixable: frontend/src/lib/components/WinCelebration.svelte:50-53 (`.small-win-flash { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%) }`) with the animation at :67-70 lifting it to `-62%` then `-70%`, not locked.
- Proposed fix: give the callout a plate or a clear band, or park its rest position off the winning rows; 6 to 8px of breathing room either side is enough.

## Explicit absences, signed

- **No element collides with or is cut by the left, right or top viewport edge on any of the 26 frames.** I checked each frame's four margins. The mascot art is cropped at `x0` identically in 005, 006, 009, 010, 011, 012, 013, 014 and 015, which is the scene's intended crop, not a layout overflow. The paytable panel bleeds to `x48..1151` with equal 48px and 49px side margins by design.
- **Nothing overflows the bottom edge either, and my first read of frame 020 was wrong until I measured it.** The symbol cards look sliced at the foot of 020, but the panel's own bottom border is at `y646..647` with a 28px margin below it, so the slicing is the modal's internal scroll viewport at scroll top, which is correct behaviour. Recorded because the false version of this finding is an easy one to file.
- **No money pod, label or figure is clipped, ellipsised or overflowing at 1200x675 in any of the 26 frames.** `$50,000.00`, `$16.20`, `$15.95`, `$3.90`, `$1.00`, `$100.00`, `$400.00`, `1,024`, `96.35%` and `5,000×` all sit inside their containers with visible margin. That is the TR-115 / TR-086 class and it does not reproduce at this viewport.
- **The HUD menu panel is fully opaque and no board symbol bleeds through it.** I suspected a see-through and measured it: over x480-530, y400-450 in 017 the fill is mean `11.4`, min `10`, max `17`, flat. Not reported.
- **The big win band's vertical placement is correct and is not reported.** Its neon rules sit at `y237..241` and `y340..344`, centre `y290.5`, which is the board's centre and not the viewport's (`y337.5`), so the band is deliberately centred on the reels. Only the amount inside it is off axis, which is STC-DESKTOP-A-06.
- **The splash (001, 002) and the intro rules card (003, 004) are compositionally clean.** The logo and the `TAP TO CONTINUE` prompt read centred with balanced air above and below; the rules card is centred on both axes with its `Continue` button centred in it and top and bottom padding within a few pixels of each other. Nothing to report on the first two surfaces a viewer sees.
- **The session panel (018) is compositionally clean.** Modal centred on both axes, label column and value column consistent down all five rows, close button aligned to the value column's right edge, top and bottom padding even.
- **The mascot's idle bob between 009, 010 and 011 was checked and is not reported.** The character sits at slightly different heights across the three settled dead spins; that is an idle animation, it crops identically in each, and no interface element moves with it.
- **019 and 020 are layout-identical** (both panels at top border `y28`, accent bar `y323..630`), so the paytable's opening transition capture caught no intermediate state and no transition-specific composition claim can be made from it.
- **No HUD crowding at this viewport.** The pods, the bet stepper, the spin and recall buttons and the FEATURES pill all keep clear gaps; the crowding risk in this design belongs to the narrow sessions, which are not my frames.

## KNOWN matches

- KNOWN(MID-01): /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/013_desktop_transition_bigwin_countup_early.png, the banner reads `$10.29` while the HUD WIN pod reads `$15.95` in the same frame, settling at `$16.20` in 015_desktop_bigwin_settled.png. Seen, not opened as a new id.
- KNOWN(MID-02): 013, 014_desktop_transition_bigwin_countup_late.png and 015 all render the unit as `16x BET` with a letter x, in the same session as the paytable's `1×`, `3×`, `10×`.
- KNOWN(STV-01): fresh frame evidence for the hardcoded plural. 014_desktop_transition_bigwin_countup_late.png renders the ticker as `L3  x4  1 ways  $0.20`, and the same string survives half-covered in 016 and 017 as `ways  $0.20`. STV cited 030 and 039, so these are three more.
- KNOWN(STV-12): 014 and 015 render the ticker kind as `x4` and `x5` (prefix, letter x) while 020_desktop_paytable_top.png and 021 render `3×`, `4×`, `5×` (suffix, U+00D7) in the same session.
- KNOWN(STV-02): the scatter card is titled `SCAT` in 020, 021, 022_desktop_paytable_02_ways_to_win.png and 023, beside `Substitutes for all symbols except SCATTER` on the same row.
- KNOWN(Q-26): 026_desktop_paytable_06_bet_modes.png carries `about 1.6x`, `Debits 1.25x` and `pre-revved to 5x` with a letter x on the same cards that write `1.25×`, `100×`, `400×` and `5,000×`.
- KNOWN(Q-16 park): parked English strings visible on my frames are `Session` and `Mute` (017) and the paytable section headings `Symbol Payouts`, `Rules`, `Bet Modes` and `Interface Guide` (020 through 026).
- KNOWN(Q-34): only the lowercase side falls in my range (`Cruise`, 026); the `CRUISE` HUD badge is not in frames 001 to 026, so no fresh cross-surface pair from this shard.

tree_after: `git status --porcelain`, run from /Users/jt/math-sdk after this shard was written, verbatim:
```
?? reports/qa/stream_test/shards/STC-DESKTOP-A.md
?? reports/qa/stream_test/shards/STC-DESKTOP-B.md
?? reports/qa/stream_test/shards/STC-LAPTOP-A.md
?? reports/qa/stream_test/shards/STC-LAPTOP-B.md
?? reports/qa/stream_test/shards/STC-MOBILEL-A.md
?? reports/qa/stream_test/shards/STC-POPOUTL-A.md
?? reports/qa/stream_test/shards/STC-POPOUTL-B.md
?? reports/qa/stream_test/shards/STC-POPOUTS-A.md
?? reports/qa/stream_test/shards/STC-POPOUTS-B.md
```
Nine untracked shards, one of them mine and eight belonging to other squads, which are not mine and not my problem. **Nothing is MODIFIED and nothing is DELETED.** No committed file was touched by this run and no project script was executed: the pixel measurements above were taken by decoding the PNGs with zlib inside `python3 - <<'PY'` heredocs, which write nothing.
