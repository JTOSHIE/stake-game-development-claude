# STT-MOBILEL-2, TYPOGRAPHY (stream-test-2026-07-28, frames 226 to 242, 1600px upscaled)
supersedes: STT-MOBILEL-A.md (its 226 to 233 half) and STT-MOBILEL-B.md (its 234 to 242 half)
scope: the seventeen `mobile-l` frames numbered 226 to 242 inclusive, read from `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/`. Native viewport 425x812. Both superseded shards overlap this span: `STT-MOBILEL-A` covered 208 to 233 and `STT-MOBILEL-B` covered 234 to 259, so my range straddles the seam and I reconcile against both.
frames_read: 17

## STT-MOBILEL-2-01 STREAM The maths package's internal symbol codes are the player-facing symbol names, on the paytable and under the reels

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/230_mobile-l_paytable_03_symbol_payouts.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/229_mobile-l_paytable_02_ways_to_win.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/228_mobile-l_paytable_01_match_symbols_on_adjacent_reels_st.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/226_mobile-l_transition_paytable_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/227_mobile-l_paytable_top.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/237_mobile-l_transition_paytable_closing.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/238_mobile-l_autoplay_menu.png`
- Claim: the SYMBOL PAYOUTS grid labels its cards `WILD`, `SCAT`, `H1`, `H2`, `M1`, `M2`. Five of the six are the maths package's internal identifiers rather than names, and `SCAT` is a truncated one. The truncation collides inside a single view: the WILD card's body reads `Substitutes for all symbols except SCATTER` while the card 30 pixels to its right is titled `SCAT`, so one screen gives one symbol two names. The same class reaches the board: the win-line strip reads `M3   x5   8 ways   $16.00` in frame 237 and `L3   x4` in frame 238, so an internal code is painted under the reels for the whole of every winning spin. The codes are literal in source, not derived: `SYMBOL_IDS = { H1: 'H1', H2: 'H2', M1: 'M1', M2: 'M2', M3: 'M3', L1: 'L1', L2: 'L2', L3: 'L3' }`.
- Resolution note: NEW AT 1600PX for the paytable half. `STT-MOBILEL-B-02` caught the reel-strip half at native from frame 237 and named `L2` and `M3`; what native could not resolve is that the paytable's own symbol cards carry the same codes, which makes this a class across two surfaces rather than one leaky readout.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/components/PaytableModal.svelte:74-75` (the symbol table, `{ name: 'SCAT', file: 'scatter', ... }`, `{ name: 'H1', file: 'h1', pays: [null, null, 1.5, 6, 22] }` and siblings) rendered at `:226` as `{sym.name}`; and `/Users/jt/math-sdk/frontend/src/lib/components/WinBreakdown.svelte:15-24` (`SYMBOL_IDS` and `symbolLabel`) rendered at `:92`. Neither file is locked.
- Proposed fix: PARK(naming six symbols is an art call, convention l.6). The mechanical half should not wait for it: `SCAT` is wrong under every possible naming and must become `SCATTER` so it agrees with the copy beside it, which is a one-token edit at `PaytableModal.svelte:74`. Note that `WinBreakdown.svelte:21-22` already routes `W` and `S` through `$tr('symbolWild')` and `$tr('symbolScatter')`, so the display-name mechanism exists and the eight paying symbols were simply never given entries.

## STT-MOBILEL-2-02 HIGH The word "scatter" is set six different ways inside one modal, including `SCATTERs`

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/231_mobile-l_paytable_04_rules.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/232_mobile-l_paytable_05_overdrive_free_spins.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/233_mobile-l_paytable_06_bet_modes.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/230_mobile-l_paytable_03_symbol_payouts.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/241_mobile-l_transition_dialog_buy_overdrive_opening.png`
- Claim: one term, six renderings, four of them on one screen.
  - `SCAT`, the symbol card label (frames 226 to 230).
  - `SCATTER`, in the WILD card body and in the rules bullet `WILD substitutes for all symbols except SCATTER.` (frames 230, 231).
  - `SCATTERs`, in `3, 4, or 5 SCATTERs anywhere apply a 1×, 3×, or 10× multiplier to your total bet win.` (frame 231). An all-caps stem with a lowercase plural welded on is a machine-tell in itself: it is what you get when a plural is appended to an uppercased token rather than written out.
  - `SCATTERS`, the OVERDRIVE FREE SPINS table column header (frames 231, 232).
  - `Scatters`, in `3 or more Scatters during free spins award +5 free spins.` (frame 232) and in the buy dialog's `3, 4 or 5 Scatters award 8, 12 or 16 free spins` (frame 241).
  - `scatter` and `scatters` lowercase, in `The scatter build runs shorter on a retrigger` (frame 232) and `Standard play. Overdrive Free Spins trigger on 3+ scatters.` (frame 233).
  Charter class 4, capitalisation that changes between two screens showing the same word, except that four of the six are inside one scroll of one modal.
- Resolution note: NEW AT 1600PX. The lowercase terminal `s` of `SCATTERs` is about 5 device pixels tall at 425x812 and cannot be told from a cap; the header-versus-bullet case split likewise.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/i18n/prose.ts:112` (`rulesScatterMult`, real money) and `:195` (the social variant, which carries the identical `SCATTERs`), `:100` (`wildSubstitutes`), `:111` (`rulesWildSub`), and `/Users/jt/math-sdk/frontend/src/lib/components/PaytableModal.svelte:74`. Not locked.
- Proposed fix: `SCATTERs` is wrong under any house style and goes first, at `prose.ts:112` and `:195`. Then pick one form for the rest: `SCATTER` when naming the symbol, `Scatters` when counting them in prose, and sweep the remaining four sites.

## STT-MOBILEL-2-03 HIGH The same rule is punctuated two ways: `3, 4, or 5` on the paytable and `3, 4 or 5` in the buy dialog

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/231_mobile-l_paytable_04_rules.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/241_mobile-l_transition_dialog_buy_overdrive_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/242_mobile-l_dialog_buy_overdrive.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/228_mobile-l_paytable_01_match_symbols_on_adjacent_reels_st.png`
- Claim: the paytable rules bullet is `3, 4, or 5 SCATTERs anywhere apply a 1×, 3×, or 10× multiplier to your total bet win.`, with the serial comma on both lists. The buy dialog states the same rule as `3, 4 or 5 Scatters award 8, 12 or 16 free spins and pay an instant 1×, 3× or 10× total bet.`, with the serial comma on none of its three lists. Both strings transcribed verbatim from source, cited below. A third site, the ways-to-win caption on frame 228, reads `Reels 1, 2 and 3 hold the same symbol (highlighted)` and also omits it. So the house style is Australian everywhere except one bullet that is American, and the two forms of the identical sentence are two taps apart in one session. The project header mandates Australian English (`/Users/jt/math-sdk/CLAUDE.md:4`).
- Resolution note: NEW AT 1600PX. A comma is about 3 device pixels wide at 425x812. `STT-MOBILEL-A-09` reached this at native but only against frame 211, outside my range, and rated it LOW.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/i18n/prose.ts:112` and `:195` are the two outliers; `/Users/jt/math-sdk/frontend/src/lib/i18n/translations.ts:1540` (`rulesOverdriveTrigger`) and `:2021` are the majority form. Not locked.
- Proposed fix: drop the two serial commas at `prose.ts:112` and `:195` so both read `3, 4 or 5 ... a 1×, 3× or 10× multiplier`, matching `translations.ts:1540` and the stated house style. Two character deletions.

## STT-MOBILEL-2-04 HIGH The symbol payout grid uses three different decimal precisions in one column

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/230_mobile-l_paytable_03_symbol_payouts.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/229_mobile-l_paytable_02_ways_to_win.png`
- Claim: reading the payout column down the visible grid in frame 230, `1.5`, `6`, `22`, `0.8`, `3`, `10`, `0.45`, `1.5`, `0.3`, `1`. Zero, one and two decimal places in one right-aligned numeric column on one screen, so `22` sits above `1.5` sits above `0.45` with their right edges aligned and their decimal points not. The values are declared unformatted and reach the DOM raw: `pays: [null, null, 1.5, 6, 22]` at `PaytableModal.svelte:75`. `decimal or currency formats that disagree` is named verbatim in the standing mandate's machine-tell list (`/Users/jt/math-sdk/CLAUDE.md`, THE STANDING MANDATE, the inspection test), and a paytable is the one surface a reviewer reads as a table.
- Resolution note: NEW AT 1600PX. These numerals are about 8 device pixels tall at native; the presence or absence of a decimal fraction is not resolvable. `STT-MOBILEL-A-04` nonetheless claimed it at native and was right, which I record as a lucky call rather than a supported one.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/components/PaytableModal.svelte:74-82` (the `pays` arrays) and the cell that prints them. Not locked.
- Proposed fix: render every cell through one formatter. Two decimals throughout (`22.00`, `1.50`, `0.45`) is the genre convention for a per-way paytable and matches the currency formatting the rest of the game already uses. Held at HIGH rather than the native pass's MEDIUM: a mis-set paytable is what a rival studio's art director opens first.

## STT-MOBILEL-2-05 HIGH Bet-mode names mix Title Case and ALL CAPS in one list, and no `text-transform` can reconcile them

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/233_mobile-l_paytable_06_bet_modes.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/239_mobile-l_transition_features_menu_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/240_mobile-l_features_menu.png`
- Claim: the BET MODES stack on the paytable titles its cards `Normal`, `Cruise`, `OVERBOOST`, and the FEATURES menu stacks the same set plus `Buy Overdrive`. Three Title Case names and one shouted name in a single vertical list of identically styled cards. Source confirms the split is in the strings themselves, not in a style rule: `modeNormalLabel: 'Normal'`, `modeCruiseLabel: 'Cruise'`, `modeOverboostLabel: 'OVERBOOST'`, `modeBonusLabel: 'Buy Overdrive'`, `modeSuperLabel: 'NITRO OVERDRIVE'`. Three Title Case, two ALL CAPS. This is therefore NOT `Q-34`'s mechanism, which is one mode whose case changes because a `text-transform` is present on one surface class and absent on three; here the case differs BETWEEN modes on the SAME surface and no transform can fix it in either direction without breaking the other two.
- Resolution note: NEW AT 1600PX. At native the card titles carry about 10 device pixels of cap height and case is not separable from weight. `STT-MOBILEL-A-05` claimed it at native from frame 233 alone and rated it MEDIUM.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/i18n/prose.ts:85,87,89,91,93`, plus the social overrides at `:190`. Not locked.
- Proposed fix: PARK(the direction is an art call, and it is the same call `Q-34` is already parked on, so it should be decided once for both). Either all five go Title Case (`Overboost`, `Nitro Overdrive`) or all five go caps (`NORMAL`, `CRUISE`, `BUY OVERDRIVE`). Whichever wins, `Q-34` resolves with it, which is worth saying because `Q-34` is currently sized as a one-property fix and on this evidence it is a five-string one.

## STT-MOBILEL-2-06 HIGH The win-line strip writes the count multiplier as a prefix `x5` where every other surface writes it as a suffix

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/237_mobile-l_transition_paytable_closing.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/238_mobile-l_autoplay_menu.png`
- Claim: the strip under the reels reads `M3   x5   8 ways   $16.00` in frame 237 and `L3   x4` in frame 238 (partly occluded by the autoplay panel). The same quantity, the count of matching symbols, is written suffix-first everywhere else: `3x`, `4x`, `5x` on the paytable payout cards (frames 229, 230), `1.25× bet` and `100×` on the features menu (frames 239, 240). Source is unambiguous: `<span class="wb-count">x{current.kind}</span>`. One game, one quantity, the operator on opposite sides of the number depending on which surface is in front of you, and both are visible within the same session seconds apart. The strip is also the higher-exposure of the two, because it is painted for the whole of every winning spin.
- Resolution note: NEW AT 1600PX. The strip glyphs are about 10 device pixels tall at native and `x5` cannot be told from `5x`. `STT-MOBILEL-B-06` and `STT-MOBILEL-A-06` both reported this component's `x` as a GLYPH problem (letter `x` for `×`); neither could see that the operator is also on the wrong SIDE, which is the larger of the two defects and the one a viewer would actually notice.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/components/WinBreakdown.svelte:93`. Not locked.
- Proposed fix: change the template to `{current.kind}×`, which fixes the side and the glyph in one edit and brings the strip into line with `Q-26`'s target convention at the same time.

## STT-MOBILEL-2-07 HIGH The autoplay option labels are the only sentence-case, untracked control labels in the game, and sit directly under a tracked uppercase one

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/238_mobile-l_autoplay_menu.png`
- Claim: `Stop on win`, `Single win limit`, `Stop on feature` and `Loss limit` render sentence case, regular weight, at `letter-spacing: 0.02em`. Every other label in the same frame is uppercase and tracked: the panel's own `SPINS` separator immediately beneath them, and behind the panel `BALANCE`, `BET`, `SPIN`, `MAX` and `FEATURES`. The two treatments are eleven pixels apart inside one dropdown. Source gives the exact split, both rules in the same block: `.auto-menu-toggle { font-size: 0.82rem; letter-spacing: 0.02em; }` against `.auto-menu-sep { font-size: 0.66rem; letter-spacing: 0.08em; text-transform: uppercase; }`. The panel is also the only modal surface in my range with no heading at all: it opens straight onto a checkbox.
- **A claim I withdrew, recorded because withdrawing it is the point of this re-run.** My first read of this frame was that the four labels were rendering in a system fallback rather than the brand face, and I had written it up as STREAM. Source refutes it. `/Users/jt/math-sdk/frontend/src/main.ts:2-4` loads Orbitron 400, 700 and 900 and nothing else; `/Users/jt/math-sdk/frontend/src/app.css:97` declares `--fs-font-display: 'Orbitron', system-ui, sans-serif` and `:99` applies it at the root; and `grep -rn "font-family:" --include="*.svelte" --include="*.css" frontend/src/` returns no declaration anywhere in the tree that is not `var(--fs-font-*)` or `inherit`. `.auto-menu-toggle` sets no family, but it is a `<label>`, which inherits, so it gets Orbitron. (`.auto-menu-input` at `HudOverlay.svelte:1835` sets the family explicitly because `<input>` does NOT inherit, which is correct rather than suspicious.) The four labels are Orbitron 400 lowercase at 13px with almost no tracking, surrounded by Orbitron 700 and 800 uppercase at 0.08em to 0.14em, and that contrast is what read to me as a second typeface. **Not the brand face: REFUTED. The case, weight and tracking inconsistency: real, and restated above at HIGH.**
- Resolution note: NEW AT 1600PX. Neither superseded shard has any finding on frame 238 at all, although it falls inside `STT-MOBILEL-B`'s declared scope.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/components/HudOverlay.svelte:1800-1811` (`.auto-menu-toggle`), against `:1842-1850` (`.auto-menu-sep`). The markup is at `:503-509`, `:739-745` and `:945-951`, three mounts of the same block. Not locked.
- Proposed fix: bring `.auto-menu-toggle` and `.auto-menu-amount` onto the HUD's control-label treatment (uppercase, 0.08em tracking, 700 weight) so the panel reads as part of the game, and give the menu a heading. Note the block is mounted three times, so the fix must be made on the class and verified on all three mounts.

## STT-MOBILEL-2-08 MEDIUM The `WILD` and `SCAT` card labels sit on different baselines while every other row of the grid aligns

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/230_mobile-l_paytable_03_symbol_payouts.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/229_mobile-l_paytable_02_ways_to_win.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/228_mobile-l_paytable_01_match_symbols_on_adjacent_reels_st.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/227_mobile-l_paytable_top.png`
- Claim: in the top row of SYMBOL PAYOUTS the label `WILD` sits about 14 upscaled pixels (about 7 device pixels) above the label `SCAT` beside it. The rows below do not do this: `H1` and `H2` share a baseline exactly, and so do `M1` and `M2`. The cause is visible in the same frame: the two hero images in the top row have different intrinsic heights and the label is stacked under the image rather than positioned on a grid row, so only the row whose two images differ splits. Two labels of one class, one size, one weight (`.fs-sym-name { font-size: 0.82rem; font-weight: 800; letter-spacing: 0.08em; }`), on two baselines, at the top of the most-read panel in the game.
- Resolution note: NEW AT 1600PX. Seven device pixels of baseline offset is not resolvable in a 425px-wide frame, and neither superseded shard reports it.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/components/PaytableModal.svelte:659` is the label rule; the art container that lacks a fixed height is in the same `.fs-sym-*` block. Not locked.
- Proposed fix: give the symbol art container a fixed height with the image centred inside it, so every label in the grid lands on the same baseline regardless of which art is above it.

## STT-MOBILEL-2-09 MEDIUM Four labels for the cost of one thing, and four names for the purchase, across three surfaces two taps apart

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/232_mobile-l_paytable_05_overdrive_free_spins.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/233_mobile-l_paytable_06_bet_modes.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/239_mobile-l_transition_features_menu_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/240_mobile-l_features_menu.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/241_mobile-l_transition_dialog_buy_overdrive_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/242_mobile-l_dialog_buy_overdrive.png`
- Claim: the price of a spin or a buy is labelled `SPIN COST` on the features menu strip (frames 239, 240), `COST` on the paytable bet-mode cards (frame 233) and `PRICE` in the buy dialog stat strip (frames 241, 242). The purchase itself is called `BUY FEATURE` on the paytable inline button, `Bonus Buy` in the paytable prose immediately above that button (`Bonus Buy: pay 100× your bet to start the feature immediately.`, frame 232), `BUY FEATURES` as the features-menu section header, and `Buy Overdrive` as the card title and the dialog title (frames 239 to 242). Seven strings for two concepts, and a viewer walking paytable to features menu to dialog sees the same button renamed at every step.
- Resolution note: NEW AT 1600PX for the small grey column labels; `COST`, `PRICE` and `SPIN COST` carry about 7 device pixels of cap height at native. VISIBLE AT BOTH for `BUY FEATURE` beside `Buy Overdrive`.
- Where fixable: UNKNOWN for the three column labels specifically. `Bonus Buy` and the mode label `Buy Overdrive` are `/Users/jt/math-sdk/frontend/src/lib/i18n/prose.ts:91` and the buy-callout prose in the same file; the `.fs-buy-lbl` rule that renders `BUY FEATURE` is `/Users/jt/math-sdk/frontend/src/lib/components/PaytableModal.svelte:683`. I stopped at six source files before pinning the `SPIN COST` and `PRICE` literals.
- Proposed fix: settle on `COST` for the column label on all three surfaces and on the mode's own `label` for the purchase, then sweep. `Bonus Buy` is the odd one out and is not a mode name at all, so it is the first to go.

## STT-MOBILEL-2-10 MEDIUM One Interface Guide card calls its own subject `Features` in the title and `FEATURES` in the body

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/234_mobile-l_paytable_07_interface_guide.png`
- Claim: the card title is `Features` and the card body directly beneath it is `Open the FEATURES menu to pick a bet mode or buy the feature.` Same word, same card, about 20 upscaled pixels apart, two casings. The neighbouring card does the opposite: title `Menu`, body `Open the menu for the paytable and sound settings.`, all lowercase in the body including `paytable`, which is the modal's own shouted title. The guide therefore has no rule about when an interface noun is shouted, and applies both in adjacent cards.
- Resolution note: VISIBLE AT BOTH, marginally. `STT-MOBILEL-B-10` reported it at native and rated it LOW; at 1600px the case is unambiguous and I hold it at MEDIUM because the two cards are adjacent and contradict each other.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/i18n/prose.ts:129` (`guideFeaturesDesc: 'Open the FEATURES menu to pick a bet mode or buy the feature.'`) and the social variant at `:204`. Not locked.
- Proposed fix: lowercase the body reference to `the Features menu`, matching the `Menu` card's treatment, at both `:129` and `:204`. One rule applied to all seven guide cards.

## STT-MOBILEL-2-11 MEDIUM The buy dialog's centred lead-in breaks with a one-word widow

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/241_mobile-l_transition_dialog_buy_overdrive_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/242_mobile-l_dialog_buy_overdrive.png`
- Claim: the question under the dialog title sets as `Start Overdrive Free Spins now at 100× your` / `bet?`, leaving the single word `bet?` alone on a centred second line under a full-width first line. Under the game's most consequential confirm, the copy reads as though it broke rather than wrapped.
- Resolution note: NEW AT 1600PX. At native the second line is a 20-pixel smear and does not read as a widow; neither superseded shard reports it.
- Where fixable: UNKNOWN. The string is in the buy-dialog component, which I did not open within the six-file budget; `/Users/jt/math-sdk/frontend/src/lib/i18n/translations.ts` carries the sibling buy strings at `:1540` and `:2021`.
- Proposed fix: bind the last two words with a non-breaking space (`your bet?`), or set `text-wrap: balance` on the lead-in so the two lines share the words evenly.

## STT-MOBILEL-2-12 MEDIUM The max win cap is quoted against three different units, two of which are not synonyms

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/231_mobile-l_paytable_04_rules.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/233_mobile-l_paytable_06_bet_modes.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/235_mobile-l_paytable_08_responsible_play.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/241_mobile-l_transition_dialog_buy_overdrive_opening.png`
- Claim: `Maximum win per spin is capped at 5,000× your total bet.` (frame 231), `Maximum win 5,000× bet.` (frame 232), a bare `5,000×` with no unit at all in the MAX WIN pod (frame 235) and in the bet-mode cards (frame 233), and `5,000×` over `base bet` in the buy dialog stat strip (frames 241, 242). Four unit strings for one cap. Two of them are not synonyms: source has `rulesMaxWin: 'Maximum win per spin is capped at 5,000× your total bet.'` against `return social ? \`${FS_MAX_WIN_LABEL} base play\` : \`${FS_MAX_WIN_LABEL} base bet\``, and on OVERBOOST the spin debits 1.25x, so total bet and base bet differ by 25 per cent and the two sentences state different caps.
- Resolution note: NEW AT 1600PX. `base bet` carries about 8 device pixels of cap height at native and the words could not be read.
- Where fixable: `/Users/jt/math-sdk/frontend/src/lib/i18n/prose.ts:113` and `/Users/jt/math-sdk/frontend/src/lib/config/fsModes.ts:158` (with `FS_MAX_WIN_LABEL = '5,000×'` at `:139`). Not locked.
- Proposed fix: PARK(maths-adjacent, convention l.8). Which unit is correct is a statement about the cap and goes to the owner and Fable with the PAR sheet attached, not to a typography sweep. The typography half, that whichever unit wins should appear on all four surfaces, is mechanical and follows the ruling.

## STT-MOBILEL-2-13 MEDIUM The paytable panel slices non-money prose through the middle of its glyphs, with no mask and no scroll affordance

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/226_mobile-l_transition_paytable_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/227_mobile-l_paytable_top.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/231_mobile-l_paytable_04_rules.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/234_mobile-l_paytable_07_interface_guide.png`
- Claim: at four scroll positions the panel's lower edge cuts a line of prose through its x-height rather than between lines, with a hard edge and no gradient: `Substitutes for all` and `3 / 4 / 5 = 1× / 3× / 10× +` in frames 226 and 227, `3 or more Scatters during free spins award` in frame 231, and the card title `Turbo` in frame 234. Frames 235 and 236 show the reciprocal at the top, where the sticky header cuts the Turbo card. None of these are money strings, so this is not `TR-115` or `TR-086`. The half-glyphs read as a rendering fault rather than as scrollable content, because nothing on screen indicates there is more below.
- Resolution note: VISIBLE AT BOTH, but only at 1600px is it clear the cut lands mid-glyph rather than at a line boundary.
- Where fixable: UNKNOWN precisely; the scroll container is in `/Users/jt/math-sdk/frontend/src/lib/components/PaytableModal.svelte` and a previous squad recorded that `grep -rn "mask-image" frontend/src/` returns nothing tree-wide, so this is a class rather than an instance.
- Proposed fix: add a top and bottom `mask-image` fade to the scroll container plus a scroll affordance. Recorded here as typography's evidence for the mechanism several composition squads reported as their cluster 3, not as a competing claim: the fix belongs to whoever takes that cluster.

## STT-MOBILEL-2-14 LOW `1× bet` appears underlined on the active Normal card and not on the Cruise card beside it

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/240_mobile-l_features_menu.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/239_mobile-l_transition_features_menu_opening.png`
- Claim: in the SPIN MODES card the cost line under `Normal` renders `1× bet` with a hairline rule under the word `bet`, while the identical string under `Cruise` in the same card carries none. Two instances of one component, one with a decoration the other lacks. Stated at LOW and honestly: this is a one-device-pixel feature and I could not find a matching declaration in source.
- Resolution note: NEW AT 1600PX.
- Where fixable: UNKNOWN. `grep -rn "font-family:"` over the tree and a scan of the features-menu styles turned up no `text-decoration` anywhere; the most likely explanation is a sub-pixel artefact of the active card's inner border surviving the upscale, not a real decoration.
- Proposed fix: PARK(not reproducible from source, and plausibly an upscaling artefact). Recorded so a later squad seeing the same hairline knows it was looked at and not confirmed, rather than re-deriving it cold.

## Native pass reconciliation

Both superseded shards straddle my range. `STT-MOBILEL-A` declares scope 208 to 233 and `STT-MOBILEL-B` declares 234 to 259, so I reconcile the 226 to 233 tail of A and the 234 to 242 head of B. Findings whose frames all fall outside 226 to 242 belong to a sibling squad and I say so rather than judging frames I did not open.

**STT-MOBILEL-A**

- `A-01` HUD menu mixes upper and title case (frames 223, 224): OUT OF RANGE, sibling squad's.
- `A-02` The paytable rules print `SCATTERs` (frame 231): **CONFIRMED.** Source is verbatim, `prose.ts:112`: `'3, 4, or 5 SCATTERs anywhere apply a 1×, 3×, or 10× multiplier to your total bet win.'` A rated it HIGH; I agree, and I add that the identical defect is duplicated into the social string at `prose.ts:195`, which A did not have.
- `A-03` The scatter symbol is named four ways (frames 228, 230, 231 in range; 211 out): **REFINED.** The count is six, not four. A missed `SCATTERS` (the trigger table column header, frames 231 and 232) and the lowercase `scatter` / `scatters` in `The scatter build runs shorter on a retrigger` and `trigger on 3+ scatters`. Restated as STT-MOBILEL-2-02.
- `A-04` Payout figures carry zero, one and two decimals (frames 229, 230): **CONFIRMED**, and source-confirmed at `PaytableModal.svelte:74-82`. I raise it from MEDIUM to HIGH. Worth flagging for the marshal that A could not actually have resolved a decimal point at 425px; the call was right but it was not supported by what A could see.
- `A-05` Bet mode names mix title case and all caps (frame 233): **CONFIRMED**, and source-confirmed at `prose.ts:85,87,89,91,93`. REFINED in one respect that matters for costing: the split is in the string literals, so `Q-34`'s "one-property fix in either direction" does not apply to it.
- `A-06` The win detail strip reads `1 ways` and writes its match count with a letter `x` (frames 221 to 224): OUT OF RANGE for the `1 ways` half. On the `x` half, which reaches my frame 237, **REFINED**: source at `WinBreakdown.svelte:93` is `x{current.kind}`, so the operator is not only the wrong glyph, it is on the wrong SIDE of the number. Neither A nor B saw that.
- `A-07` The session panel heading is the only heading in sentence case (frame 225, contrasted against 227 and 231): OUT OF RANGE for its subject. I can confirm only the contrast half, that the paytable headings on frames 227 and 231 are uppercase display face, which is consistent with A's claim but does not test it.
- `A-08` The rules gate button is title case (frames 211, 209, 213, contrasted with 232): OUT OF RANGE for its subject. The contrast frame 232 is mine and its `BUY FEATURE` button is upper case, consistent with A's claim.
- `A-09` The same rule sentence is punctuated with and without a serial comma (frames 211, 231): **CONFIRMED and REFINED.** A had one of the two frames and rated it LOW. At full resolution I have a second and stronger pair entirely inside one session, `prose.ts:112` against `translations.ts:1540`, both quoted, and a third site at frame 228. Raised to HIGH as STT-MOBILEL-2-03.

**STT-MOBILEL-B**

- `B-01` The win-line readout prints `1 ways` (frames 245 to 259): OUT OF RANGE, sibling squad's.
- `B-02` Internal symbol ids render to the player (frame 237 in range, 245 to 259 out): **CONFIRMED** on frame 237, `M3`. **REFINED**: the same class is on the paytable's own symbol cards (`SCAT`, `H1`, `H2`, `M1`, `M2`), which B did not report, so this is two surfaces rather than one readout. Restated as STT-MOBILEL-2-01 and raised to STREAM.
- `B-03` The max win overlay says `HIT ENTER` (frames 256, 257): OUT OF RANGE, sibling squad's.
- `B-04` The two stacked HUD pod rows differ in size, weight and tracking (frames 245 to 259): OUT OF RANGE, sibling squad's.
- `B-05` `×BET` with no word space (frames 256, 257): OUT OF RANGE, sibling squad's.
- `B-06` The win-line readout writes the count multiplier with a letter `x` (frame 237 in range): **REFINED**, same refinement as `A-06`. The glyph claim stands; the side of the operator is the larger defect and neither shard has it. B rated it MEDIUM, I rate the combined defect HIGH.
- `B-07` `scatters` and `Scatters` one tap apart (frames 240, 241, 242 in range): **CONFIRMED**, and subsumed into the six-way count at STT-MOBILEL-2-02.
- `B-08` The two buy tiers are named in two different cases (frames 239 to 242 in range, 243 and 244 out): **CONFIRMED** for the half I can see, and source-confirmed: `modeBonusLabel: 'Buy Overdrive'` at `prose.ts:91` against `modeSuperLabel: 'NITRO OVERDRIVE'` at `:93`. Subsumed into STT-MOBILEL-2-05, which shows the split is three-to-two across all five modes rather than two buy tiers.
- `B-09` The FEATURES bet row overflows and drops the `+` onto its own line (frames 239, 240): **CONFIRMED.** Clearly visible at 1600px. Not restated as one of mine because it is a fit failure rather than a typographic one and the composition squads own it; recorded here so the marshal does not read my silence as a refutation.
- `B-10` `Features` and `FEATURES` inside one card (frame 234 in range): **CONFIRMED.** B rated it LOW; I hold it at MEDIUM, restated as STT-MOBILEL-2-10, because the adjacent `Menu` card applies the opposite rule.
- `B-11` The `MAX WIN` cell wraps and loses baseline alignment (frames 241, 242 in range): **CONFIRMED.** `5,000×` sits on the strip baseline with `base bet` on a second line while `PRICE` and `RTP` are single-line, so the third column's block hangs below the other two. B rated it LOW and that is right. Not restated; the same cell's UNIT problem is a separate matter at STT-MOBILEL-2-12.

**One claim of my own, refuted by me before it reached the ledger.** My first pass on frame 238 recorded the autoplay option labels as a system font leaking through where the brand face should be, at STREAM. Source refutes it: only Orbitron is loaded, the tree contains no font-family declaration that is not the brand token or `inherit`, and a `<label>` inherits. What I saw was Orbitron 400 lowercase untracked against Orbitron 700 to 800 uppercase tracked, eleven pixels apart. The real defect is case, weight and tracking, and it is written up at HIGH as STT-MOBILEL-2-07 with the withdrawal recorded inline.

## Explicit absences, signed

What I checked in order to be able to say these.

- **No `.fs-num` count-up frames in my range.** Frames 220 to 222 hold the big-win count-up and belong to a sibling squad, so I make no claim about `TR-089`'s per-digit boxes in either direction.
- **No numeral shimmy on any other numeric surface.** Frames 226 to 236 are eleven static paytable views and 237 to 242 hold the HUD at rest. `$50,000.00` is byte-identical in glyph positions between frames 237 and 238, and `$1.00`, `$100.00`, `96.35%` and `5,000×` are identical between 239 and 240 and between 241 and 242. Nothing in my range animates a number outside the win strip.
- **No money-display fit failure.** `$50,000.00`, `$16.20`, `$1.00`, `$16.00`, `$100.00`, `$1.25` all sit inside their containers with clearance on every frame that carries them. No clipping, no ellipsis, no overflow. So I record no `TR-115` or `TR-086` evidence from this range, which is a statement rather than an omission.
- **No mixed straight and curly quotes.** There is no apostrophe or quotation mark in any player-visible string across my seventeen frames. The paytable prose, the mode blurbs, the buy dialog and the disclaimer are all written without contractions or quoted matter.
- **No double spaces.** Checked on the two longest prose blocks, the eleven-line disclaimer and the six-line responsible-play paragraph, and confirmed against source: `prose.ts:118` contains no doubled space.
- **No em dash or en dash in player-visible prose.** Checked every prose block in frames 231 to 236 and 239 to 242. The only dash present is the hyphen in `Double-chance`, which is correct.
- **The disclaimer's punctuation is correct.** I read the mark after `Remote Game Server` as a possible semicolon in the frame and did not report it, because at 1600px it is genuinely ambiguous. Source settles it: `prose.ts:118` reads `returned by the Remote Game Server, not from events shown in the web browser.` A comma. **Not a finding**, and it is recorded here rather than dropped silently, because a later squad will see the same ambiguous mark.
- **The multiplication sign on the rules bullets and the scatter summary is correct.** I flagged these as candidate ASCII `x` from the frames and source refutes it: `prose.ts:112` is `1×, 3×, or 10×`, `:113` is `5,000×`, `:117` is `3 / 4 / 5 = 1× / 3× / 10× + 8 / 12 / 16 free spins`. All U+00D7. **Not findings.** The genuinely-ASCII instances are the mode blurbs, which are `Q-26`, below.
- **The `™` and `©` glyphs on frames 235 and 236.** I could not resolve confidently whether these fall back out of Orbitron, and I make no claim either way rather than guessing. Flagged for whoever runs a glyph-coverage check.
- **The `3x`, `4x`, `5x` match-count labels on the payout cards.** They render as the small raised form consistent with U+00D7 rather than the full-size ASCII form, but I did not locate the literal within my source budget and I do not claim them.

## KNOWN matches

- **KNOWN(Q-27)**, and its severity assessment is wrong. `KNOWN_OPEN.md:21` records the Vite scaffold remnants in `app.css` including "scaffold body centring" and judges them "Visible only if any link or unstyled surface reaches a frame." Frames `231`, `232`, `234`, `235` and `236` show what that centring actually reaches: `/Users/jt/math-sdk/frontend/src/app.css:143` is `#app { text-align: center; }`, and `PaytableModal.svelte` contains only five `text-align` declarations, none of which touches `.fs-rules`. So the scaffold rule is what sets the entire paytable prose layer centred, including an eleven-line legal DISCLAIMER paragraph (`Malfunction voids all wins and plays. ... All rights reserved.`) with both edges ragged, and the RULES bullets, whose `›` markers are absolutely positioned at `left: 0` inside an li that is itself centre-aligned (`PaytableModal.svelte:668-669`), so the gap between marker and first word changes on every item and the list has no left edge at all. The one section that reads correctly, INTERFACE GUIDE on frame 234, is the one place the tree overrides it, at `PaytableModal.svelte:778` `.fs-guide-text { text-align: left }`. The same rule also explains why the mode blurbs are centred on the paytable (frame 233) and left-aligned on the features menu (frames 239, 240). **This is not a new row. It is `Q-27` with the evidence that it is not cosmetic**: no studio sets a compliance paragraph centred, and it is the most reliable single tell that a text block landed in a container that inherited someone else's `text-align`. Recommend `Q-27` be re-rated and the `#app` rule removed before submission.
- **KNOWN(Q-26)**, with a stale citation. Frames `233`, `239` and `240` carry `Double-chance: about 1.6x the feature trigger rate. Debits 1.25x every spin while ON.` with ASCII U+0078. `KNOWN_OPEN.md:20` locates these in `fsModes.ts`; they are no longer there. They are at `/Users/jt/math-sdk/frontend/src/lib/i18n/prose.ts:90` and `:94` (`pre-revved to 5x`), duplicated into the social overrides at `:189` and `:192`, which is six live instances against the row's enumerated four. At 1600px the defect is sharper than the row implies: on the OVERBOOST card in frames 239 and 240 the blurb's full-size `1.25x` and the cost line's small `1.25×` sit about 60 upscaled pixels apart on ONE card, so the two glyphs are directly comparable to a viewer. Same shape as `MID-02`: a row written to catch an incomplete sweep is itself pointing at the wrong file.
- **KNOWN(Q-16)**, with the visibility evidence the row asks for. `KNOWN_OPEN.md:19` parks the hardcoded English autoplay labels and asks squads to list which parked strings are VISIBLE on stream frames. Frame `238` shows `Stop on win`, `Single win limit`, `Stop on feature`, `Loss limit` and `SPINS`, all four of the row's named autoplay strings plus the separator, on an open panel over live reels. Source at `/Users/jt/math-sdk/frontend/src/lib/i18n/prose.ts:139-142`. The urgency call stays with the de and ar squads; this is the en-session confirmation that the strings do reach a frame.
- **KNOWN(Q-07)**, reviewed and kept, not a finding. The `∞` at the foot of the SPINS list on frame `238` renders in a system fallback, which `KNOWN_OPEN.md:24` records as by design and allowlisted. Listed so my silence is not read as an oversight.
- **KNOWN(Q-34)**, partial fresh evidence. Frames `233`, `239` and `240` show `Cruise` in title case on both the paytable and the features menu, which is the row's stated majority. The `CRUISE` HUD badge is not in my range, so I confirm only the three-surface half. See STT-MOBILEL-2-05 for why the row's sizing needs revisiting.
- **NOT KNOWN(TR-115 / TR-086).** Signed above under absences: no money display in my range clips, ellipsises or overflows.
- **NOT KNOWN(MID-01), NOT KNOWN(MID-02).** Neither the win banner nor the big-win count-up appears in frames 226 to 242. The banner frames for this session are 220 to 222 and belong to a sibling squad.

tree_after:
```
?? reports/qa/stream_test/shards/STC-MOBILEL-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-2.md
?? reports/qa/stream_test/shards/STC-POPOUTS-3.md
?? reports/qa/stream_test/shards/STM-POPOUTS.md
?? reports/qa/stream_test/shards/STT-MOBILEL-1.md
?? reports/qa/stream_test/shards/STT-MOBILEL-2.md
?? reports/qa/stream_test/shards/STT-MOBILEL-3.md
?? reports/qa/stream_test/shards/STT-POPOUTS-1.md
?? reports/qa/stream_test/shards/STT-POPOUTS-2.md
?? reports/qa/stream_test/shards/STT-POPOUTS-3.md
```
Eleven untracked shards, ten of them other squads' and not mine. **Nothing shows as MODIFIED or DELETED: the tree is clean apart from new shard files, and I dirtied nothing.** My only write was `reports/qa/stream_test/shards/STT-MOBILEL-2.md`.
