# STM-MOBILEL, motion residue (mobile-l, frames 208 to 259, 1600px upscaled)
supersedes: STM-MOBILEL.md (retained at `reports/qa/stream_test/shards/superseded/STM-MOBILEL.md`)
scope: every `mobile-l` frame whose filename contains `transition_`, 18 frames: 208, 210, 212, 214, 215, 220, 221, 223, 226, 237, 239, 241, 243, 245, 247, 254, 256, 258. Viewport `425x812`, lang `en`, per `reports/screens/stream-test-2026-07-28/MANIFEST.json`. Read from `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/`, every file `837x1600`.
frames_read: 18 assigned, plus 11 settled endpoints opened for comparison and not counted against the set (209, 211, 213, 216, 222, 224, 227, 246, 255, 257, 259).

Reading note. Every frame in this set is `837x1600`, verified by reading the PNG IHDR of all 31 files opened. No frame in the assigned set is byte identical to its settled neighbour, so "indistinguishable" below always means indistinguishable to the eye at 1600px and never means byte equal.

## STM-MOBILEL-01 STREAM The HUD menu panel is translucent and the balance figure prints through its own menu labels

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/223_mobile-l_transition_menu_opening.png`. Settled endpoint, opened for comparison: `224_mobile-l_hud_menu.png`.
- Claim: the menu panel occupies roughly `x 22..512, y 1148..1442` of the 1600px frame and the `BALANCE` pod behind it occupies roughly `x 24..410, y 1150..1285`. The pod is therefore entirely inside the panel horizontally, and it is legible through it. The tail of the label `BALANCE` reads to the right of the menu item `PAYTABLE`, and the digits of `$50,000.00` read directly underneath and across the menu item `Session`, so the `S`, `e` and `s` of `Session` sit on top of the digit run `0,000.00`. Two independent strings occupy the same glyph boxes. The same overlap is present unchanged in the settled frame `224`, so this is not a fade artefact, it is the panel's resting opacity.
- Resolution note: NEW AT 1600PX. This is the one finding of this re-run that the native pass reached and got wrong in the other direction, so it is recorded as a refutation as well as a finding, see the reconciliation section below. At `425x812` the whole panel is about 240 by 145 device pixels and the ghost digits are sub-pixel; the native pass brightened a crop, read the digits as sitting outside the panel's right edge at about `x 281` native, and withdrew the finding. At 1600px the digit run measures `x 100..335`, which is native `x 51..170`, well inside a panel whose right edge is native `x 260`.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte:1603`. The base rule is `.hud-menu { ... background: rgba(6, 6, 18, 0.96); ... }` at `:1598-1607`, shared by all four layout branches (`:427`, `:545`, `:654`, `:816`); the mobile branch only repositions it, `.m-hud-menu { bottom: 44px; left: 0; }` at `:1115`. Not locked. Derived from the source and then matched to the frame, in that order: at alpha `0.96` a white `#fff` digit at 255 composites to about `0.04 * 255 + 0.96 * 6 = 16` against a panel body of `6`, which is a low but real delta on a dark screen, and the balance figure carries a text glow on top of that, which is why it reads rather than merely measures.
- Proposed fix: take the alpha to `1`, `background: rgb(6, 6, 18)`. One value. There is no `backdrop-filter` on the rule for the alpha to be serving, and the menu is a solid card in every other respect, so nothing is lost.

## STM-MOBILEL-02 HIGH At the frame labelled full speed no symbol carries motion blur and four of the five reels already hold the settled result

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/215_mobile-l_transition_reels_full_speed.png`. Settled endpoint, opened for comparison: `216_mobile-l_dead_spin_1_settled.png`.
- Claim: `MANIFEST.json` labels frame 215 `Reels at full speed`. All twenty tiles render pin sharp, with no directional streak, no smear and no ghosting on any symbol, and reels 1 to 4 register exactly with their cell borders rather than sitting between rows. Reading the twenty tiles against the settled frame `216`, reels 1 to 4 carry the identical symbols in the identical rows, and only reel 5 is mid travel, showing a clipped tile at the window's top edge and a clipped tile at its bottom edge. So the frame that is meant to show the fastest instant of the spin is already showing the answer on four fifths of the board, drawn as crisply as the settled frame. Taken with frame `214` at `about 250ms after spin press`, where reels 1, 2 and 3 carry three, two and one tiles over open scenery, the whole visible spin on this viewport is an empty window followed by the result. No frame in the set shows symbols travelling.
- Resolution note: NEW AT 1600PX. Sharpness against motion blur is not resolvable at roughly 460 image tokens, and neither is matching twenty small symbols one by one against the settled board.
- Where fixable: `frontend/src/lib/components/GameGrid.svelte:70-75` for the geometry, `:1224-1232` for the transparent column. Not locked. The strip is `STRIP = ROWS + 3`, seven slots (`:74`), on a `TILE` of `104` (`:72`), resting at `REST_Y = -104` (`:75`), and `.symbol-col` is `120px` by `412px` with `overflow: hidden` (`:1225-1232`) and declares no `background`, and `.grid-container` (`:1204-1214`) declares none either. Nothing in either rule carries a blur, a duplicate or a streak, so the absence of motion blur is by construction rather than by a capture accident.
- Proposed fix: PARK(whether the reels should render a travelling strip at all is an art call for the owner, and the fix differs completely between adding a blurred travel phase and lengthening the existing drop). What is not an art call is that the board must not be empty while it travels: `background: #1c1d2e` on `.symbol-col` at `GameGrid.svelte:1225`, the full alpha form of the tile tone already at `:1259`, closes that half in one property, which is the superseded shard's proposal and this shard confirms it.

## STM-MOBILEL-03 HIGH The feature entry hero prints `+16 FREE SPINS` straight through the gauge dial, and the card body passes the reel grid and its win lines through the heading

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/245_mobile-l_transition_feature_entry_fade.png`, `247_mobile-l_transition_feature_starting.png`. Settled endpoints, opened for comparison: `246_mobile-l_feature_entry_card.png`, `259_mobile-l_post_collect_base.png`.
- Claim: on the feature entry card the gauge graphic occupies roughly `x 300..540, y 435..660` and the yellow headline `+16 FREE SPINS` occupies roughly `x 222..615, y 615..665`. The headline's glyphs `6 FREE SP` cross the gauge's silver rim and its dark face, so the largest type on the card is drawn over the largest graphic on the card with no clearance and no plate behind it. Identical at `259`, where the same collision reads `+8 FREE SPINS`. Separately the card body is translucent: the reel grid's cell outlines, the dimmed symbols and the cyan win lines from the triggering round are all legible through it, and the heading `OVERDRIVE FREE SPINS` has a grid line and a `W` wild ghost behind its glyphs. Present identically in `245`, `246`, `247` and `259`.
- Resolution note: NEW AT 1600PX. At native the headline and the gauge are a single yellow and silver blob about 200 by 115 device pixels and the overlap cannot be separated from a deliberate badge.
- Where fixable: `frontend/src/lib/components/FreeSpinsPresentation.svelte:649-652`. Not locked. The mechanism is exact and it is worth stating precisely, because a previous audit round fixed the neighbouring collision and left this one. `.entry-gauge-wrap` is `240px` by `240px` in the stage's own coordinate system, centred, at `:581-583`. `.entry-bottom-group` is `position: absolute; left: 0; right: 0; bottom: 2%` with `display: flex; flex-direction: column; gap: 14px` at `:649-652`, and it holds `.entry-burst-text` at `font-size: 2.1rem; font-weight: 900` (`:654-655`) above `.entry-continue`. The comment at `:644-648` states that the flex column means the award text and the continue button *"can never overlap regardless of the button's own height or the viewport's aspect ratio"*, which is true and is OWNER AUDIT ROUND 3 item 3 working as designed. It guarantees a gap between the two members of the group. It guarantees nothing between the top of that group and the fixed `240px` gauge sitting above it, and on a `425x812` stage that is the gap that closes.
- Proposed fix: two edits. Put the gauge and the bottom group in one flex column so the same guarantee that separates the award text from the button also separates it from the dial, rather than anchoring one to the centre and the other to `bottom: 2%`. And give the entry card body an opaque background so the board it is gating does not read through it.

## STM-MOBILEL-04 HIGH A decorative ring belonging to the big win banner escapes the banner's band and is painted across the bottom symbol row

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/220_mobile-l_transition_bigwin_countup_early.png`. Compared against `221_mobile-l_transition_bigwin_countup_late.png` and the settled endpoint `222_mobile-l_bigwin_settled.png`.
- Claim: at `Big win count-up, early digits` a large ring, centred on the amount at roughly `(420, 545)` with a radius of about `205`, is drawn behind `$10.27`. The banner's own dark band runs `y 390..692`. The ring's lower arc therefore falls outside the band, and it is visible crossing the bottom symbol row between roughly `y 700` and `y 790`, over the cells that hold the hexagon, the bottle and the zap. The ring is absent from `221` and absent from the settled `222`, so it exists only while the count-up is running, and while it exists it paints on the board rather than on the banner.
- Resolution note: NEW AT 1600PX. The arc is a single low contrast grey line about two device pixels wide at native and is not separable there from the cell borders it crosses.
- Where fixable: `frontend/src/lib/components/WinBanner.svelte:450-462`. Not locked. Derived and then matched to the frame. The ring is the shared `shock_ring.png` particle at `:253-259`, styled `.c1-shockwave { position: absolute; top: 50%; left: 50%; ... z-index: 1; animation: c1-shockwave-burst 0.6s ease-out both; }` at `:450-454`, sized `.tier-big .c1-shockwave { width: 260px; height: 260px; }` at `:455`, and the keyframe ends at `transform: translate(-50%, -50%) scale(1.6)` at `:461`. Its maximum rendered diameter is therefore `260 * 1.6 = 416px`, centred on the plate. Its only ancestor is `.c1-plate-wrap { position: relative; ... }` at `:442`, which declares no `overflow`, so at `416px` on a `425` wide viewport the ring is wider than the screen and taller than the band and there is nothing to clip it. The `0.6s` duration is why it is in frame `220` and gone by `221`.
- Proposed fix: clip the ring rather than the wrapper. Putting `overflow: hidden` on `.c1-plate-wrap` at `:442` would also clip the coin fountain, which sets `overflow: visible` deliberately at `:468`, so the safe edit is to wrap `.c1-shockwave` in its own clipping element or to reduce the `tier-big` size at `:455` and the `1.6` end scale at `:461` until the burst fits the band. Not the banner's width: the band being full bleed while the reel window is inset by about eight pixels, so the reel frame's left and right neon borders are severed between `y 390` and `y 692`, is present identically in the settled `222` and belongs to the composition lens, which the superseded shard already assigned it to.

## STM-MOBILEL-05 HIGH The win line detail strip survives into surfaces it does not belong to and is drawn over the reel frame's bottom neon border for nearly its full width

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/221_mobile-l_transition_bigwin_countup_late.png`, `237_mobile-l_transition_paytable_closing.png`, `245_mobile-l_transition_feature_entry_fade.png`, `247_mobile-l_transition_feature_starting.png`, `254_mobile-l_transition_feature_exit.png`. Settled endpoints, opened for comparison: `222`, `246`, `255`, `259`.
- Claim: the strip is a black bar at roughly `y 822..862` spanning `x 22..818`, and the reel window's bottom neon border runs at roughly `y 840..862`. The strip is drawn on top of it, so the glowing frame that surrounds the board is broken for `796` of its `829` pixels of width and survives only as two short stubs at `x 0..22` and `x 818..837`. Beyond that the strip does not belong to the surface it appears on: at `245` and `247` it is painted below the feature entry gate, on a surface whose whole purpose is to stop play and be tapped, and at `237` it is still up after a full paytable open and close. It is the only element of the previous round's win presentation that outlives every surface put in front of it.
- Resolution note: NEW AT 1600PX. At native the strip is about 20 by 400 device pixels of dark grey and its relationship to the neon border underneath it cannot be read.
- Where fixable: `frontend/src/lib/components/WinBreakdown.svelte:114-121`. Not locked. `.win-breakdown { position: absolute; left: 50%; bottom: 6px; transform: translateX(-50%); z-index: 45; ... }`. `bottom: 6px` puts it inside its positioned ancestor's lower border band rather than clear of it, and `z-index: 45` puts it above the frame chrome, which is the two halves of the overlap. The persistence half has a precedent in the same file: `export let suppressed = false` at `:44`, added under MAX-WIN HOLD so the component stops behind the max win overlay, with the reasoning written out at `:26-43` and the parallel `WinCelebration` call quoted there. The feature entry gate was never given the same treatment.
- Proposed fix: two edits, both small and both with a precedent already in the file. Raise `bottom` at `:117` until the plate clears the frame's inner border, or move the element outside the reel window entirely. And extend the existing `suppressed` prop (`:44`) to the feature entry gate at its call site in `App.svelte`, exactly as it was extended to the max win overlay, so a blocking surface stops it rather than being drawn around it.

## STM-MOBILEL-06 MEDIUM The win line detail strip mixes a raw internal symbol code with a plain language one, writes the multiplier with a letter `x`, and reads `1 ways`

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/221_mobile-l_transition_bigwin_countup_late.png` reads `L3  x4  1 ways  $0.20`; `237_mobile-l_transition_paytable_closing.png` reads `M3  x5  8 ways  $16.00`; `245_mobile-l_transition_feature_entry_fade.png` reads `L2  x5  1 ways  $0.80`; `247_mobile-l_transition_feature_starting.png` and `254_mobile-l_transition_feature_exit.png` read `SCATTER  x5  5 ways  $10.00`. Settled endpoints carrying the same surface: `222` reads `M3  x5  8 ways  $16.00`, `255` reads `L2  x5  1 ways  $0.80`, `259` reads `M3  x3  1 ways  $0.20`.
- Claim: three separate machine tells on one player visible strip that is on screen for the whole of every win. `L2`, `L3` and `M3` are internal symbol identifiers from the maths package rendered to the player, and the same strip renders `SCATTER` in plain language, so the two conventions sit on the same surface across consecutive frames. The multiplier is written `x4`, `x5` and `x3` with a letter, against the `×` the paytable and the buy dialogs use. And the count reads `1 ways` on four of the eight readings transcribed above, where the singular is `1 way`.
- Resolution note: NEW AT 1600PX. This strip is about 20 device pixels tall at native and none of its text is legible there. Out of this squad's lens, transcribed because it is on this squad's frames and because the codes and the `x` are exactly what the standing mandate names as machine tells. A localisation shard reached the same surface from another direction, per `LEDGER.md`; that account is unverified and this transcription is independent of it.
- Where fixable: `frontend/src/lib/components/WinBreakdown.svelte:93`, `:94` and `:16-24`. Not locked. All three are one line each. `:93` is `<span class="wb-count">x{current.kind}</span>`, an ASCII `x` in markup. `:94` is `<span class="wb-ways">{current.ways} ways</span>`, hardcoded English with no plural rule and no `tr` call, on a component that routes every other word through `tr`. `:16-19` is `const SYMBOL_IDS: Record<string, string> = { H1: 'H1', ... L3: 'L3' }` and `:20-25` routes only `W` and `S` through `tr` as `symbolWild` and `symbolScatter`. So the mixing of `SCATTER` with `L3` is deliberate and its reason is recorded at `:12-15`: *the tier symbols are IDs, identical in every language*. The reason is about localisation and it is sound; what it does not address is that an identifier is not a player-facing word in any language, which is the standing mandate's machine-tell test rather than the locale gate's test.
- Proposed fix: three one line edits. `x` to `×` at `:93`. A `tr` routed, count aware label at `:94`. And a display name map at `:16-19` so `L3` reads as whatever the paytable calls that symbol, which also removes the `SCATTER` beside `L3` inconsistency in one move. Note for the marshal: the `x` at `:93` is a sixth survivor of the Q-26 class, alongside the fifth that `LEDGER.md` MID-02 records at `WinBanner.svelte:205`, and it is again not in `fsModes.ts`, which is where Q-26's enumeration looked.

## STM-MOBILEL-07 MEDIUM On the max win hero the multiplication sign binds to the unit rather than to the number, so it reads `5,000` then `×BET`

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/256_mobile-l_transition_maxwin_overlay_fade.png`. Settled endpoint, opened for comparison: `257_mobile-l_maxwin_celebration.png`.
- Claim: the hero renders `5,000`, then the sign, then `BET`. The final `0` ends at about `x 532`, the sign runs about `x 548..570`, and `BET` runs about `x 578..645`. The gap on the left of the sign is therefore about `16` pixels and the gap on its right about `8`, which at the 1.97 upscale factor is about `8.1` and `4.1` device pixels. The sign sits twice as close to the unit as to the quantity it multiplies, so the eye groups it as `×BET`. The sign is also set noticeably larger than `BET` and on a higher baseline, and it carries the numeral's glow while `BET` does not, which compounds the grouping. Identical in the settled `257`.
- Resolution note: NEW AT 1600PX. The superseded shard signed this one as explicitly unresolvable at native, which was the correct call there; the spacing is now measurable. This is the English sibling of the German `5,000×EINSATZ` collision the localisation shards report, so it is a class rather than a locale accident.
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:295-300`, with `:301-308`, `:309-315` and `:316-321`. Not locked. The three spans are `.c1-max-mult` `5,000`, `.c1-max-x` `×` and `.c1-max-betlabel` `BET` at `:155` and `:159`, in a `.c1-max-multwrap { display: flex; align-items: baseline; gap: 0.1em; }` at `:295-299`. **The `gap` is uniform, so the source spacing between the three is equal and the imbalance is entirely optical**: `.c1-max-mult` is `96px` falling to `50px` under the `max-width: 500px` rule at `:367`, so its final `0` carries a large right side bearing, while `.c1-max-betlabel` at `22px` falling to `13px` (`:369`) carries `letter-spacing: 0.2em` (`:320`), which adds space after each glyph and none before the first. A uniform `gap` between three spans of `50px`, `24px` and `13px` cannot produce even optical spacing, and it did not.
- Proposed fix: replace the uniform `gap: 0.1em` at `:298` with explicit margins so the sign sits closer to the number than to the unit, for example `gap: 0` plus `.c1-max-x { margin: 0 0.08em 0 0.02em; }`. **Source answer to the question this shard declined to answer from the frame: the glyph at `:155` is `×`, U+00D7, so the codepoint is already correct and only the spacing is wrong.** Recorded separately for the marshal, because it is a live trap: the comment at `:151-154` still reads *"A letter `x` here while the paytable, the mode cards and the feature menu all write the multiplication sign"*, describing the pre Q-12 state, and it now sits directly above a line that uses `×`. `LEDGER.md` MID-02 cites `MaxWinCelebration.svelte:151-155` as the project's recorded convention, so the next reader who opens those lines will find a comment that contradicts the code beneath it.

## STM-MOBILEL-08 MEDIUM A hairline strip of the feature scene shows between the reel frame and the top of the grid

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/245_mobile-l_transition_feature_entry_fade.png`, `247_mobile-l_transition_feature_starting.png`. Settled endpoints, opened for comparison: `246_mobile-l_feature_entry_card.png`, `259_mobile-l_post_collect_base.png`.
- Claim: a bright green horizontal strip about `9` pixels tall runs at roughly `y 283..292` across `x 30..800`, immediately inside the reel window's top border and above the first row of cells. It is the feature mode's green scene, which is the colour of the backdrop in exactly these frames, showing through a gap between the frame and the grid. It is absent from every base mode frame in the set, where the backdrop behind the same gap is dark and the leak has nothing to show. So the defect is present in base mode too and is only visible when the scene behind it is bright.
- Resolution note: NEW AT 1600PX. Four and a half device pixels at native, indistinguishable there from the reel frame's own inner glow.
- Where fixable: UNKNOWN, and stated as unknown rather than guessed, per convention (l.6). Two candidates were checked in the source and both were RULED OUT, so the next reader does not repeat them. It is not a vertical gap in the reel geometry: `.symbol-col` is `412px` tall against a `.grid-container` of `412px` (`GameGrid.svelte:1206`, `:1227`), and `REST_Y = -104` on a `TILE` of `104` (`:72`, `:75`) puts strip slot 1's top edge exactly at the window top, with the inter tile gap landing at `y -4..0`, above the window rather than inside it. And it is not a stray green element in either component: `grep -n` for `sig-green`, `4EFF91`, `meter-bar` and `overdrive-bar` across `GameGrid.svelte` and `FreeSpinsPresentation.svelte` returns nothing, and every plate tint in `GameGrid.svelte` defaults to `#00ffff` (`:104`, `:1140`, `:1261`). The remaining candidates, not checked within this shard's source budget, are the `border-radius: 8px` on `.symbol-col` (`:1231`) against the `border-radius: 12px` on `.grid-container` (`:1208`) leaking a sliver at the top corners, and a layer outside these two components.
- Proposed fix: PARK(the source was not located within budget and a guess would be worth less than the ruled out list above). What the fix must achieve is fixed either way: give `.grid-container` an opaque plate at `GameGrid.svelte:1204-1214`, which currently declares no `background`, so that any residual gap shows the board's own tone rather than whatever scene is behind it. That closes the symptom on every viewport regardless of which candidate is the cause, and it is the same one property edit STM-MOBILEL-02 needs.

## STM-MOBILEL-09 LOW The balance readout never moves across the whole session, including a 5,000x max win

- Frames: `BALANCE $50,000.00` is unchanged in every frame of the set that shows it: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/212`, `214`, `215`, `220`, `221`, `237`, `245`, `247` (partly occluded), `254`, `256` (through the overlay), `258`. Settled endpoints agree: `213`, `216`, `222`, `224`, `255`, `257`, `259`.
- Claim: three settled dead spins at `BET $1.00`, a `$16.20` big win, a full Overdrive feature and a `WIN $5,000.00` max win all pass with `BALANCE` reading `$50,000.00` throughout. At `258` and `259` the pods read `BALANCE $50,000.00` and `WIN $5,000.00` side by side.
- Resolution note: VISIBLE AT BOTH. The digits are large enough to read at native; the superseded shard did not report it.
- Where fixable: UNKNOWN, and it is the wrong question until the prior one is answered. The place to look FIRST is the capture harness, `frontend/scripts/stream_test_capture.mjs`, for a pinned balance, and only if it does not pin one does this become a frontend question. I did not read the harness, because the superseded shard already read it as text for its own findings and the answer to this does not change any other finding in this shard.
- Proposed fix: PARK(a fixed demo balance is a normal capture harness choice and cannot be distinguished from a broken readout by looking at stills). Recorded at LOW for that reason and handed to the marshal as a question rather than a claim: if the harness does not pin the balance, this is a STREAM defect, because a viewer watching a 5,000x win land on an unmoved balance will notice before anything else on this list.

## Native pass reconciliation

Every finding in `reports/qa/stream_test/shards/superseded/STM-MOBILEL.md` falls inside this squad's frame range, so none is deferred to a sibling. Six numbered findings plus the absences section are addressed.

- **STM-MOBILEL-01 (native), reel window transparent to the scene at 214: CONFIRMED.** At 1600px the frame reads exactly as described. Reel 1 carries three tiles, reel 2 two, reel 3 one, each column ending in a hard tile edge that lines up with no cell boundary, and below them the parallax scene is legible inside the window: the magenta chevron artwork at roughly `x 20..175, y 680..800`, the rain streaks, and a cyan smudge at about `(320, 780)`. Reels 4 and 5 are full. The endpoints hold: `213` and `215` both render twenty filled cells. Nothing about the claim shrinks at full resolution, and the derived 75.7 per cent figure is consistent with what the frame shows.

- **STM-MOBILEL-02 (native), max win overlay drawn before its backdrop covers the game: CONFIRMED on its main claim, REFINED on one sub-claim.** Confirmed: at `256` the celebration content is at full strength while `FUTURE SPINNER`, the reel grid with a `W` ghost, `BALANCE`, `$50,000.00`, `WIN`, `$5,000.00`, `⊞ FEATURES` and `$1.00` are all legible underneath, and the settled `257` still ghosts all of them, so the backdrop never reaches full cover. Refined: the sub-claim that `PRESS COLLECT OR HIT ENTER TO CONTINUE` is *painted over* the `FEATURES` label does not survive. At 1600px the two runs resolve as two separate lines, the hint at about `y 1035..1060` and the ghosted `⊞ FEATURES` at about `y 1068..1092`, with roughly four device pixels of clearance. They abut, they do not overlap. The native band `y 528..556` contained both lines and was read as one. The finding stands, on the see through backdrop; the text on text wording should not reach the ledger.

- **STM-MOBILEL-03 (native), `FEATURES` printed across the rules card's `Continue` button at 210: CONFIRMED.** At 1600px `⊞ FEATURES` reads clearly on the face of the cyan pill, immediately above the word `Continue`, and the settled `211` shows a clean opaque pill. The card body is translucent in the same frame: `$50,000.00`, `$0.00`, `BET`, `$1.00`, `MAX` and `SPIN` are all legible through it, and the reel window's cyan lower border crosses the body copy between `feature.` and `3 or more Scatters`. None of that is present in `211`.

- **STM-MOBILEL-04 (native), reel window empties to a featureless glow during the collect fade: CONFIRMED, and its UNKNOWN can be narrowed.** At 1600px `258` shows all twenty cells emptied to faint ghosts under a large amber bloom. The refinement full resolution adds is what else is in there: the cyan win line overlay from the previous board is still drawn across the emptied window, following the same geometry it had when the board was populated. Amber celebration bloom plus retained win lines over an emptied reel window is the exact shape of `LEDGER.md` Cluster 4, which three sibling squads report on other viewports, so this is a class instance rather than a mobile-l curiosity and the PARK for a video capture may no longer be needed.

- **STM-MOBILEL-05 (native), seven transition captures landed on a settled state: CONFIRMED for five, REFINED for two.** Confirmed for `208`, `212`, `223`, `226` and `237`: at 1600px each is indistinguishable to the eye from its settled neighbour, including `226` against `227` down to the yellow left hand rail and the identical clipping of `Substitutes for all` and `3 / 4 / 5 = 1x / 3x / 10x +` at the panel's bottom edge. Refined for two. `247` is not a landed settled capture: its win line strip reads `SCATTER  x5  5 ways  $10.00` against `L2  x5  1 ways  $0.80` in both `245` and `246`, and the symbol ghosts behind the card differ, so the board underneath advanced between `246` and `247` while the card above it did not move at all. `254` is not settled either: its `WIN` pod reads `$318.64` against `$363.89` in `255` and its strip differs, so it caught a live count up. The evidence gap the finding declares is real and is honestly declared; the roster of which frames are in it is the part that needed correcting.

- **STM-MOBILEL-06 (native), feature entry gate still up at 247: CONFIRMED on the gate, REFUTED on "nothing behind the card advanced either".** The gate is up at full opacity at `247` and is pixel comparable to `245` and `246`. But the accompanying claim that nothing behind it moved is wrong at full resolution: the strip changed as set out above, and the symbol ghosts changed with it. That makes the finding stronger rather than weaker, because a modal that is frozen while the state it gates churns underneath is a worse defect than a modal that is simply late. I did not open `248` or `249`, so the part of the claim that concerns those two frames is untested by this shard and is neither confirmed nor refuted here.

- **Native absences section, item on the whole view displacement at 258: CONFIRMED as correctly withdrawn.** The displacement is real and I saw it independently before reading the superseded shard: at 1600px the reel frame, the wordmark, both HUD pods and the whole button row in `258` sit a few pixels left of and larger than the same elements in `255` and `259`. I verified that all thirty one files opened are `837x1600`, so it is not a canvas size difference and it is a genuine in page transform. The native pass's derivation that this is the intended screen shake, with `259` confirming a trigger, is sound and I adopt it. Not reported as a finding.

- **Native absences section, item withdrawing the balance ghosting through the HUD menu at 223: REFUTED.** This is the substantive correction of the re-run and it runs the opposite way to the usual one: a real defect was seen at native, measured wrongly, and signed off as not a finding. See STM-MOBILEL-01 above. The panel spans about `x 22..512` at 1600px, native `x 11..260`; the ghost digit run spans about `x 100..335` at 1600px, native `x 51..170`. The digits are inside the panel by about ninety device pixels on the right, not outside it by twenty one. The stated measurement of "about `x 281`" cannot be the digits.

- **Native absences section, item declining to call the `x` against `×` glyph on the max win unit: CONFIRMED as the right call, and partly discharged.** Declining to read a codepoint from a native thumbnail was correct. At 1600px I still decline to call the codepoint, for the same reason in kind: a glyph that small carries antialiasing that can make either mark look like the other, and the source is the authority. What full resolution does discharge is the spacing, which is measurable and which is wrong, and that is STM-MOBILEL-07 above.

- **Native absences section, remaining items: CONFIRMED.** No element mid teleport, no ghost of a dismissed surface at `237` or `254` or `258`, no wrong z order and no game text landing on `CANCEL` or `BUY` at `241` or `243`, no backdrop blur on the wrong layer, no shadow or glow left behind by a moved element, and the big win band edges at `220` and `221` present identically in the settled `222` and therefore correctly assigned away from this lens. I re-checked each at 1600px and found nothing that changes them.

## Explicit absences, signed

Each line says what was checked, on which frames, so the absence is checkable rather than asserted. All 18 assigned frames were read once each at 1600px.

- **No element caught mid teleport, and no element moved without its shadow or glow.** Checked the reel frame's four edges, the two rails, the SPIN button's halo, the FEATURES bar's magenta rim and the modal card borders on all 18. The only displacement in the set is at `258` and it is the intended shake, reconciled above. The only glow that moves independently of its owner is the `TAP TO CONTINUE` halo, which pulses on a stationary button at `245` against `246`, which is a pulse and not a residue.
- **No wrong z order on the two buy confirm dialogs.** `241` and `243` both render the dialog body opaque, with `CANCEL` and `BUY` fully reachable and nothing from the FEATURES panel behind reading through the body. Checked the price rows specifically: `PRICE $100.00 / RTP 96.35% / MAX WIN 5,000× base bet` at `241` and `PRICE $400.00 / RTP 96.35% / MAX WIN 5,000× base bet` at `243` are clean. **This session does not reproduce `LEDGER.md` Cluster 2** on `mobile-l`: the action row is above the fold and both buttons are present.
- **No money display clipped, ellipsised or overflowed on any frame in the set**, checked at `$50,000.00`, `$5,000.00`, `$16.20`, `$318.64`, `$363.89`, `$100.00`, `$400.00`, `$1.00`, `$10.27` and `$15.95`. So KNOWN_OPEN TR-115 / TR-086 has no fresh mobile-l transition evidence from this squad.
- **No count up digit shimmy.** The `$10.27` of `220` and the `$16.20` of `221` sit on even digit boxes with no width jitter between them, consistent with TR-089 being closed for that surface. No other numeric surface in the set shimmies: `WIN $318.64` at `254` against `$363.89` at `255` holds its digit pitch.
- **No replay ghost pod (TR-114) on any frame in the set.**
- **Nothing was checked on frames 248 to 253.** They are settled feature frames outside this squad's set and outside the endpoints I opened, so the superseded shard's claims about them stand untested by this re-run.
- **Out of lens, recorded so it is not lost rather than claimed as this squad's finding.** At `239` the features menu bet stepper wraps: `SPIN COST $1.00  BET  [-]  $1.00` sits on one row and the `[+]` button alone on the row beneath it, at the far left under `SPIN COST`, so the two halves of one stepper are on different rows and different sides. Composition lens, and the settled `240` will carry it.

## KNOWN matches

- **KNOWN(MID-01)**: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/220_mobile-l_transition_bigwin_countup_early.png`. The banner reads `$10.27` while the HUD `WIN` pod reads `$15.95`, on a win that settles at `$16.20` in `222_mobile-l_bigwin_settled.png`. The two readouts disagree by `$5.68` on a `$16.20` win at that instant, and the pod has already revealed the figure the banner exists to reveal. Fresh mobile-l evidence for the ledger's row; the ledger's own derivation predicts `$15.96` against the `$15.95` printed.
- **KNOWN(MID-02)**: `220_mobile-l_transition_bigwin_countup_early.png` and `221_mobile-l_transition_bigwin_countup_late.png` both render the unit as `16x BET`. The settled `222` renders the same string. At 1600px the mark is a full height letter on the same baseline and cap height as the `1` and `6` beside it, which is what a letter `x` looks like and not what a raised multiplication sign looks like, but see STM-MOBILEL-07 for why this shard does not call codepoints from frames.
- **KNOWN(Q-26)**: the KNOWN_OPEN row sizes this as a Wave 3 fix candidate *if visible on frames*. It is, and worse than the row describes. `243_mobile-l_transition_dialog_nitro_overdrive_opening.png` carries `Buy a rich entry with the Overdrive meter pre-revved to 5x.` three lines above `3, 4 or 5 Scatters award 8, 12 or 16 free spins and pay an instant 1×, 3× or 10× total bet.` in the same body block of the same dialog, so both conventions are in one paragraph run at one glance. `239_mobile-l_transition_features_menu_opening.png` carries `Double-chance: about 1.6x the feature trigger rate. Debits 1.25x every spin while ON.` beside the badge `1.25× bet`, the same collision on the same card.
- **KNOWN(Q-34)**: `239_mobile-l_transition_features_menu_opening.png` renders the mode as `Cruise` in title case on the features menu. This squad's frames contain no HUD badge showing `CRUISE`, so this is one half of the pair only.
- **KNOWN(Q-16 park)**: the parked hardcoded English strings that are visible on this squad's frames are `Mute` at `223`, `PRESS COLLECT OR HIT ENTER TO CONTINUE` at `256`, and the paytable's `SYMBOL PAYOUTS` heading at `226`. This is an English session so the park's urgency is unchanged by these, and they are listed only because the protocol asks for the enumeration to be evidenced rather than abstract.

tree_after: `git status --porcelain` run at the end of this shard's work, verbatim below. **Every line is untracked (`??`) and every line is a re-run shard. Nothing shows as MODIFIED and nothing shows as DELETED, so this squad did not dirty the tree.** Mine is `reports/qa/stream_test/shards/STM-MOBILEL.md` and it is the only file this squad wrote. The other fourteen belong to sibling squads working at the same time.

```
?? reports/qa/stream_test/shards/STC-MOBILEL-1.md
?? reports/qa/stream_test/shards/STC-MOBILEL-2.md
?? reports/qa/stream_test/shards/STC-MOBILEL-3.md
?? reports/qa/stream_test/shards/STC-MOBILEM-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-2.md
?? reports/qa/stream_test/shards/STC-POPOUTS-3.md
?? reports/qa/stream_test/shards/STM-MOBILEL.md
?? reports/qa/stream_test/shards/STM-POPOUTS.md
?? reports/qa/stream_test/shards/STT-MOBILEL-1.md
?? reports/qa/stream_test/shards/STT-MOBILEL-2.md
?? reports/qa/stream_test/shards/STT-MOBILEL-3.md
?? reports/qa/stream_test/shards/STT-POPOUTS-1.md
?? reports/qa/stream_test/shards/STT-POPOUTS-2.md
?? reports/qa/stream_test/shards/STT-POPOUTS-3.md
```
