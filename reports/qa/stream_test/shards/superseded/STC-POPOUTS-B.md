# STC-POPOUTS-B, composition (popout-s, 400x225, en, frames 183 to 207)
scope: every frame of the `popout-s` session numbered 183 to 207 inclusive, 25 frames, covering paytable sections 07 to 09, the paytable close, the FEATURES menu, both buy confirm dialogs, the feature entry card, the six feature run intervals, the feature exit, the max win celebration and the post collect return.
frames_read: 25

**NOTE FOR THE MARSHAL, TWO INDEPENDENT PASSES ARE MERGED HERE.** A first pass over these 25 frames wrote this shard at 23:06. A second pass, run separately and without sight of the first until its own frame reading was complete, reached the same top five findings independently. The second pass has been merged in rather than overwriting, per multi-track rule 8 (resolve by concatenation, never by discarding). What the second pass changed is recorded explicitly and not silently folded in:
- it **added** STC-POPOUTS-B-12;
- it **corrected the direction of the centring error in STC-POPOUTS-B-07**, which the first pass had backwards, and replaced the eyeballed claim with pixel measurements;
- it **hardened** STC-POPOUTS-B-02 and STC-POPOUTS-B-05 with measurements;
- it **refuted three of its own candidate findings by measurement** before they were written, and those three are signed under absences so nobody re-opens them.

**A THIRD PASS IS NOW MERGED IN AS WELL.** It read the same 25 frames from scratch, wrote no other file, ran no project script, and reached findings 01 to 05, 08, 09 and 10 independently before opening this shard. It has NOT overwritten a single existing line. What it contributed:
- it **added** STC-POPOUTS-B-13, a misalignment inside the max win unit lockup that is distinct from the centring reported at STC-POPOUTS-B-07 and compatible with that entry's refutation 3;
- it **filled the `Where fixable: UNKNOWN`** left open at STC-POPOUTS-B-12, with the exact file and lines;
- it **corroborated STC-POPOUTS-B-02, STC-POPOUTS-B-07 and STC-POPOUTS-B-11 from independent pixel inputs**, which matters because convention (l.4) says two methods sharing an input are not corroboration;
- it **added one line of fresh evidence to KNOWN(Q-26)** from a component neither earlier pass named.

Pixel figures introduced by the second pass were read directly out of the PNG rasters and are quoted with the frame they came from. Third pass figures were decoded through ffmpeg to raw rgb24 and counted, an independent decode path from the second pass's, and are labelled as third pass wherever they appear. The 400x225 frame's geometric centre is x `199.5`.

## STC-POPOUTS-B-01 STREAM The buy confirm dialogs show a price and no visible way to accept or decline it

- Frames: `reports/screens/stream-test-2026-07-28/190_popout-s_dialog_buy_overdrive.png`, `reports/screens/stream-test-2026-07-28/192_popout-s_dialog_nitro_overdrive.png`, `reports/screens/stream-test-2026-07-28/189_popout-s_transition_dialog_buy_overdrive_opening.png`, `reports/screens/stream-test-2026-07-28/191_popout-s_transition_dialog_nitro_overdrive_opening.png`
- Claim: at 400x225 the dialog renders a symbol preview and its three column stat strip, and then a line of body text that is bisected horizontally through its letterforms. The visible fragment is `bet?` in frame `190` and `your bet?` in frame `192`, which is the tail of `buyConfirmBody`, `'Start Overdrive Free Spins now at {cost}× your bet?'` (`frontend/src/lib/i18n/translations.ts:1537`). **Neither the CANCEL button nor the CONFIRM button is legible in any of the four frames**, and both exist in the markup (`frontend/src/lib/components/BuyBonus.svelte:141-146`). **The dialog title is not legible either** (`<h2 class="buy-title">{featureName}</h2>`, `frontend/src/lib/components/BuyBonus.svelte:100`), so `190` and `192` are the same composition distinguished only by `$100.00` against `$400.00`: nothing in either frame names which mode is being bought. A modal that has taken over the screen, states a price, and shows no button is the worst composition in my 25 frames.
- Where fixable: `frontend/src/lib/components/BuyBonus.svelte:170-174`. `.buy-modal` is `width: min(94vw, 460px); max-height: 90dvh; overflow-y: auto; padding: 22px 24px 24px`. At a 225px viewport `90dvh` is about 202px, and the content stack it must hold is a title, a body paragraph, a four line what-you-get block (`:103-108`), a symbol preview row (`:111-117`), a three column stat strip (`:122-136`), a possible warning line (`:138-140`) and the action row (`:141-146`). Most of that is below the fold, and the only affordance saying so is the default scrollbar. Not locked.
- Proposed fix: hold `.buy-actions` out of the scroll region as a pinned footer of `.buy-modal`, so the two buttons are on screen at every viewport height, and clamp the `22px 24px 24px` padding and the block's fixed type steps below about 480px viewport height.

## STC-POPOUTS-B-02 STREAM The Overdrive instrument column is sliced by the right viewport edge for the whole feature, and it takes a money figure with it

- Frames: `reports/screens/stream-test-2026-07-28/193_popout-s_transition_feature_entry_fade.png`, `194_popout-s_feature_entry_card.png`, `195_popout-s_transition_feature_starting.png`, `196_popout-s_feature_run_1.png`, `197_popout-s_feature_run_2.png`, `198_popout-s_feature_run_3.png`, `199_popout-s_feature_run_4.png`, `200_popout-s_feature_run_5.png`, `201_popout-s_feature_run_6.png`, `206_popout-s_transition_maxwin_collect_fade.png`, `207_popout-s_post_collect_base.png` (all under `reports/screens/stream-test-2026-07-28/`)
- Claim: the free spins instrument column sits hard against the right edge of the 400x225 frame and every row of it is cut through. The label column renders as `OVERDRIVE F`, `TOTAL W` and `MULTIPL`, none of them complete. The value column is cut as well: in `194` the running total renders as `$10.6` and then the frame edge, in `206` as `$2.8` and in `207` as `$2.6`, against the same session's HUD writing `$363.89` in `203` to two decimals. Either a digit or a magnitude suffix is lost, so the figure a viewer reads is not the figure the game holds. The tachometer above the plates is bisected by the same edge, showing roughly its left half. This is a settled state and not a transition artefact: `194`, `196` through `201` and `207` are all `phase: state` in `MANIFEST.json`, and the clipping is identical in every one of the eleven frames.
- **Measured, second pass**: in frame `194` the final column of the raster, x `399`, carries 35 pixels brighter than 200 within y `20` to `175`, peaking at 632. Content is live in the last column of the viewport, so the column is genuinely cut off by the edge rather than merely tight against it.
- **Measured, third pass, independently and on a different band**: in frame `194`, counting pixels whose brightest channel exceeds 150, the last four columns of the raster carry live ink in BOTH bands, 9 lit rows at each of x `396`, `397`, `398` and `399` across the plate band y `95` to `170`, and 5 lit rows at x `399` across the tachometer band y `8` to `70`. The second pass measured the plate band only, so the tachometer's own clipping is now measured rather than described. Frame `207` reads the same to the last column; frame `206`, which is mid fade, falls to zero beyond x `394`, which is the dimming and not a different layout. Third pass transcription of the labels at 7x nearest neighbour magnification of the region x `320` to `400`: `OVERDRIVE FR`, `TOTAL W`, `MULTIPLI`, one or two characters further than the second pass read at 1x, and the value `$10.6` with the following glyph cut down its stroke rather than absent. The two transcriptions agree on the finding and differ only on where the cut lands, which is a resolution difference and not a disagreement.
- **The derivation and the frame disagree, and that is the finding under the finding.** `frontend/src/App.svelte:752` fixes the design surface at `const STAGE_W = 1280` and `:748` states the single stage factor as `S = min(vw/1280, vh/720)`. At 400x225 both terms are exactly `0.3125`. `frontend/src/lib/components/BonusInstrumentColumn.svelte:113-118` places the column at `position: absolute; left: 1000px; top: 96px; width: 262px`, so its right edge is design x `1262` of `1280`, which at `0.3125` maps to x `394.4` of `400`: it should clear the edge with about **5.6px to spare**. Eleven frames say it does not. So either the transform actually in force at this viewport is not the one `App.svelte:748` documents, or the column's contents overflow its declared `262px` box. Per convention (l.2) a measurement that disagrees with the specification is suspect, but eleven settled frames are not a measurement error, and both candidate causes are defects.
- Where fixable: `frontend/src/lib/components/BonusInstrumentColumn.svelte:113-118` and the stage transform in `frontend/src/App.svelte:747-752`. Neither locked.
- Proposed fix: PARK(the cause is not determined and guessing it would put a wrong mechanism into the ledger). Resolve the contradiction first with a live measurement of the effective stage transform and of the column's rendered box at 400x225. Only then choose between correcting the transform and relaying the column, which for a 400px viewport probably means moving it inside the cabinet bezel or collapsing it to a value only strip.

## STC-POPOUTS-B-03 HIGH The paytable body is bisected mid glyph by the panel edge, with no visible affordance saying it scrolls

- Frames: `reports/screens/stream-test-2026-07-28/185_popout-s_paytable_09_disclaimer.png`, `184_popout-s_paytable_08_responsible_play.png`, `183_popout-s_paytable_07_interface_guide.png`
- Claim: in `185` the last rendered line of the disclaimer, `plays and does not guarantee any result in a single`, is cut horizontally through the middle of its letterforms by the panel's lower edge, and the sentence has no visible ending. No scrollbar, no fade and no chevron appears in the frame to say the text continues. In `184` the body ends on the single word orphan `menu.` alone on its final line, sitting hard against that same edge with no effective bottom breathing space, against the generous clear space the header enjoys above `PAYTABLE`: top padding generous, bottom padding visually nil, on one panel. In `183` the second interface guide card is cut by the same edge, leaving exactly one complete card (`Spin`, `Start a spin at the current bet.`) beneath a header block that consumes roughly half of the 225px height.
- Where fixable: `frontend/src/lib/components/PaytableModal.svelte:593-603`. `.fs-pt-body` is `overflow-y: auto; padding: 20px 30px 30px` with `scrollbar-width: thin`, inside `.fs-pt-panel` at `:544-552` which is `width: 92%; height: 92%; max-height: 662px`, so at 400x225 the panel is about 207px tall and the scrolling body gets roughly half of that. A scroll box clips at its padding box, which is why the `30px` bottom padding does not save the cut line. Not locked.
- Proposed fix: add a bottom `mask-image` fade to `.fs-pt-body` sized to about one line height, and force a persistently visible scrollbar rather than the platform's overlay one, so a cut line reads as more below rather than as broken rendering.

## STC-POPOUTS-B-04 HIGH The FEATURES menu shows three of five modes and decapitates a section header at the fold

- Frames: `reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png`, `187_popout-s_transition_features_menu_opening.png`
- Claim: `MANIFEST.json` records frame `188` as `FEATURES menu, all five modes and their prices`. The frame shows three: `Normal`, `Cruise` and `OVERBOOST`. Buy Overdrive and Nitro Overdrive are not on screen at all. The section header that would introduce them renders as the top halves of its glyphs only, clipped at the bottom edge of the scrolling card list and sitting directly on the footer's rule, above `All modes · RTP 96.35%` and the `BET MODES` button. A visibly decapitated line of type resting on a horizontal rule reads as a rendering fault, not as scrollable content. `187` shows the identical clipping mid open, so it is not a transition artefact.
- Where fixable: `frontend/src/lib/components/FeatureMenu.svelte:873-880`. `.fm-cards` is `flex: 1 1 auto; min-height: 0; overflow-y: auto; padding: 14px 20px` with `scrollbar-width: thin`, and `.fm-foot` at `:1001-1005` is a `flex-shrink: 0` sibling carrying a `border-top`, so the list clips exactly at the footer rule. Not locked.
- Proposed fix: add a bottom `mask-image` fade on `.fm-cards` about one card gap deep and a persistently visible scrollbar, so the cut header reads as continuation. The comment at `:866-872` records that this list's clipping has been chased once before; the remaining gap is the affordance, not the flex maths.

## STC-POPOUTS-B-05 HIGH The bet stepper in the FEATURES menu is grossly asymmetric about its own value

- Frames: `reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png`, `187_popout-s_transition_features_menu_opening.png`
- Claim: the top row of the menu reads `SPIN COST $1.00` then `BET`, then the minus button, then a wide empty gap, then the value `$1.00`, then the plus button hard against it. The two stepper controls are an identical matched pair and plainly intend to flank the value; the gap on the minus side is several times the gap on the plus side. The row reads as a layout that ran out of instructions rather than as a control. The markup order is exactly as the frame shows it, `frontend/src/lib/components/FeatureMenu.svelte:334-344`: `.fm-spin-cost`, `.fm-betlabel`, the `-` button, `.fm-betval`, the `+` button.
- **Measured, second pass, frame `188`, glyph extents in the row band y `38` to `64`**: the minus glyph occupies x `165` to `171`, the value `$1.00` occupies x `287` to `319`, and the plus glyph occupies x `347` to `352`. The clear gap on the minus side is therefore **116px** and the clear gap on the plus side is **28px**, a ratio of **4.1 to 1** inside a single control, on a canvas only 400px wide. "Several times" is confirmed as literal.
- Where fixable: `frontend/src/lib/components/FeatureMenu.svelte:861-862`. `.fm-betval { margin-left: auto; min-width: 84px; text-align: right; }` pushes the value and everything after it to the right edge of the flex row, while the minus button stays packed left against the labels, so the two gaps are structurally unequal and the asymmetry grows as the row widens. The row's own `gap: 0.7rem` is at `:847`. Not locked.
- Proposed fix: wrap the minus, the value and the plus in one flex group with a single `gap`, move the `margin-left: auto` onto that group rather than onto the value, and keep the value's `min-width: 84px` with `text-align: center`.

## STC-POPOUTS-B-06 MEDIUM The whole HUD money row jumps sideways when the feature starts and again when it ends

- Frames: `reports/screens/stream-test-2026-07-28/202_popout-s_transition_feature_exit.png` and `203_popout-s_post_feature_base.png` and `186_popout-s_transition_paytable_closing.png` (two icon buttons at the left of the HUD) against `193_popout-s_transition_feature_entry_fade.png` through `201_popout-s_feature_run_6.png`, `206_popout-s_transition_maxwin_collect_fade.png` and `207_popout-s_post_collect_base.png` (one icon button)
- Claim: the HUD's left control cluster carries two icon buttons in the base frames and one in the feature and post collect frames. The row is packed from the left, so dropping a control drags `BAL`, its value, `WIN` and its value bodily leftwards by the width of the missing button plus its gap. `203` and `207` are the same surface family at the same viewport with the same two labels in two different horizontal positions. On stream the viewer's eye is parked on the win figure, and a lateral shift of the readout at the exact moment the feature ends is a visible discontinuity in a run of otherwise continuous frames. The second pass reached this independently and estimated the shift at roughly 50px, about an eighth of the frame's width.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte`, exact line UNKNOWN. The file carries several conditional guards around the left cluster (`{#if !$rgJurisdiction.autoplayDisabled}` at `:486` and `:722`, `{#if $isSpinning}` at `:629`, `{#if showMenu}` at `:426`, `:544`, `:653`, `:815`) and this shard's source budget did not isolate which one removes the button during the feature. Not locked.
- Proposed fix: reserve the slot with a fixed width placeholder when the control is hidden, or anchor the `BAL`/`WIN` group to the row centre so its position does not depend on how many controls sit to its left.

## STC-POPOUTS-B-07 MEDIUM The max win lockup is optically off centre: the number the surface exists to show is 28px left of everything else on it

- Frames: `reports/screens/stream-test-2026-07-28/205_popout-s_maxwin_celebration.png`, `204_popout-s_transition_maxwin_overlay_fade.png`
- **THIS FINDING WAS CORRECTED BY THE SECOND PASS AND THE CORRECTION IS THE POINT.** The first pass reported the composite as sitting *visibly right of the title above it*. That was backwards, and it contradicted the mechanism the same entry described. The measured direction is left, and the mechanism explains why.
- Claim, measured from the raster of frame `205`:

  | Element | x extent | centre |
  |---|---|---|
  | `MAX WIN REACHED!` (title block, y `7` to `76`) | `78` to `323` | **`200.5`** |
  | `5,000` (the numeral alone, bright yellow, y `104` to `145`) | `86` to `258` | **`172.0`** |
  | `COLLECT` button (y `169` to `208`) | `112` to `287` | **`199.5`** |

  The frame's geometric centre is `199.5`. The title lands on it to within 1px and the button lands on it exactly. **The numeral lands `28.5px` to the left of it**, which on a 400px canvas is 7.1 per cent of the full width. Three stacked elements, two of them agreeing to the pixel and the third, the largest and brightest object on the surface, visibly out of column with both.

  The mechanism is the one the first pass correctly identified: `frontend/src/lib/components/MaxWinCelebration.svelte:155-158` sets three siblings on one flex line, `<span class="c1-max-mult fs-num">5,000</span>`, `<span class="c1-max-x">×</span>` and `<span class="c1-max-betlabel">`. The flex line is centred, so the LINE is centred and the numeral inside it is not: the unit hangs to the right and pushes the numeral left by half the unit's width. The unit cluster measures x `265` to `320`, so half its width plus its leading gap accounts for the `28.5px` almost exactly.
- **Two claims the second pass made by eye and then withdrew on measurement**, recorded so they are not re-raised: the numeral and the unit do NOT touch (the numeral's yellow ends at x `258`, the unit's first ink is at x `265`, so there is about `6px` of clearance), and the unit is NOT set at mid height (the unit's ink runs y `122` to `146` against the numeral's `104` to `145`, so the two baselines agree to within about 1px). The first pass's reading of the baseline was the better one.
- **Third pass, independent corroboration of the two agreeing elements**: counting rows rather than columns, the first row of headline ink in frame `205` is y `7` and the last row of `COLLECT` ink is y `212` once the button's outer glow is included, against the second pass's `7` and `208` measured on the button's edge. The two decodes agree on the title's top to the row and on the button to within its glow, so the second pass's centre figures are corroborated from an independent input rather than merely repeated.
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:155-158`. Not locked.
- **Related but distinct, do not merge**: the unit cluster this entry treats as one object is itself misaligned internally, which is STC-POPOUTS-B-13. That finding is compatible with refutation 3 below rather than contradicting it: refutation 3 compares the CLUSTER to the numeral, B-13 compares `×` to `BET` inside the cluster.
- Proposed fix: centre the numeral rather than the line, by taking the unit out of flow (absolutely position `.c1-max-x` and `.c1-max-betlabel` against the numeral's right edge), or apply a left padding to the line equal to the unit cluster's width so the numeral's own centre lands on `199.5`. Either puts the figure back in column with the title and the button.

## STC-POPOUTS-B-08 MEDIUM The max win overlay's continue hint is not on screen at this viewport

- Frames: `reports/screens/stream-test-2026-07-28/205_popout-s_maxwin_celebration.png`, `204_popout-s_transition_maxwin_overlay_fade.png`
- Claim: `frontend/src/lib/components/MaxWinCelebration.svelte:166` renders `<p class="c1-hint hint" aria-live="polite">{t($locale, 'maxWinHint', localeMode)}</p>` below the collect button. No hint line is legible below `COLLECT` in either frame. The overlay at 400x225 therefore presents its celebration and its button with none of the instruction copy the component was written to carry, and the composition ends abruptly on the button with the remaining strip of frame empty. Corroborated independently by the second pass, which recorded the absence before reading any source: the `COLLECT` button's lower edge measures y `208` of `225` in frame `205`, leaving 16 rows of frame below it and nothing in them.
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:166` and the overlay's vertical stack, plus the small viewport branch at `:370` which already reduces `.c1-collect` to `padding: 12px 36px; font-size: 15px`. Not locked.
- Proposed fix: extend the existing small viewport branch to reduce the headline and lockup type steps as well, so the hint clears the bottom of the frame at 225px height.

## STC-POPOUTS-B-09 MEDIUM The buy dialog's stat strip has one column on two lines and two on one, so no value shares a baseline

- Frames: `reports/screens/stream-test-2026-07-28/190_popout-s_dialog_buy_overdrive.png`, `192_popout-s_dialog_nitro_overdrive.png`, `189_popout-s_transition_dialog_buy_overdrive_opening.png`, `191_popout-s_transition_dialog_nitro_overdrive_opening.png`
- Claim: the strip runs `PRICE` / `$100.00`, `RTP` / `96.35%`, `MAX WIN` / `5,000x` `base bet`. The three labels align on one baseline. The three values do not: columns one and two are a single line with a block of dead space beneath, while column three wraps and its second line `base bet` runs down to within a few pixels of the strip's lower border. The strip reads bottom heavy on its right third and empty on its left two thirds, and the row of figures a buyer is meant to compare is not on a shared baseline. Reached independently by both passes.
- Where fixable: `frontend/src/lib/components/BuyBonus.svelte:122-136` and the `.buy-stats-row` rule at `:224`. The three `.buy-stat` cells are equal width flex children with no shared baseline and no wrap control on `maxWinVsBaseBetLabel($isSocial)` at `:135`. Not locked.
- Proposed fix: set the value row to `align-items: baseline` and let the third value render on one line at a reduced step, or give all three columns a deliberate two line value pattern so the wrap is the design rather than one column's accident.

## STC-POPOUTS-B-10 MEDIUM The six feature run frames are one held composition, so the running feature has no evidenced layout at this viewport

- Frames: `reports/screens/stream-test-2026-07-28/196_popout-s_feature_run_1.png` through `201_popout-s_feature_run_6.png`, with `194_popout-s_feature_entry_card.png` and `195_popout-s_transition_feature_starting.png`
- Claim: `MANIFEST.json` describes frames `196` to `201` as `Overdrive free spins in flight, interval frame 1 of 6` through `6 of 6`. All six render the entry gate instead: the same `OVERDRIVE FREE SPINS` heading, the same tachometer, the same `+16 FREE SPINS`, the same `TAP TO CONTINUE` button and the same `WIN $0.00` in the HUD, across six sampled intervals. With `194` and `195` that is eight consecutive frames of one composition. Either the continue gate is never passed at this viewport or the capture never dismissed it, and the frames alone cannot tell me which, so I am not choosing. What is certain is that the running feature's composition at 400x225 is unevidenced by this session and must not be recorded as swept. The second pass reached the same conclusion independently and adds one observation: frame `207`, whose manifest note is `Back to base after collect, balance settled`, also shows the entry card, reading `+8 FREE SPINS` and `TAP TO CONTINUE` over a HUD reading `BAL $50K` and `WIN $5K`, so the mismatch between note and frame extends past the six run frames.
- Where fixable: UNKNOWN. If the gate is genuinely unpassable at 400x225 the cause is likely the same class as STC-POPOUTS-B-01; if it is a capture gap it belongs to the harness rather than to the frontend.
- Proposed fix: PARK(needs a re run of the popout-s feature leg before any layout claim about the running feature can be made). The re run should confirm first whether `TAP TO CONTINUE` responds at this viewport.

## STC-POPOUTS-B-12 MEDIUM The feature entry card ends on a stray base game win line reading `$0.00`, directly under its call to action

- Frames: `reports/screens/stream-test-2026-07-28/195_popout-s_transition_feature_starting.png` (most legible), `194_popout-s_feature_entry_card.png`, `196_popout-s_feature_run_1.png`, `197_popout-s_feature_run_2.png`, `198_popout-s_feature_run_3.png`, `199_popout-s_feature_run_4.png`, `200_popout-s_feature_run_5.png`, `201_popout-s_feature_run_6.png`, `207_popout-s_post_collect_base.png`
- Claim: a small orange strip reading `SCATTER ×3 4 ways $0.00` sits inside the entry card's bounds, below the `TAP TO CONTINUE` button and above the card's lower neon border, set at a type size far below anything else on the card and at a contrast that makes it read as leakage rather than as content. The card's designed sequence is heading, tachometer, `+16 FREE SPINS`, call to action. It instead terminates on base game telemetry whose last token is `$0.00`. **A `$0.00` set immediately beneath a free spins award, on the frame that announces the feature, is the worst possible last line for that card**, and it is present in nine of my 25 frames including eight settled states. Found by the second pass; the first pass had recorded the entry card over the reels as a deliberate occlusion and did not separate this strip out of it.
- Where fixable: **FILLED BY THE THIRD PASS**, `frontend/src/lib/components/WinBreakdown.svelte:89-95` and `:115-119`. The strip is `<div class="c1-win win-breakdown fs-plate">` at `:89`, whose four spans at `:92-95` are `{symbolLabel(current.symbol, $tr)}`, `` x{current.kind} ``, `` {current.ways} ways `` and `{payLabel}`, which composes exactly the `SCATTER x3 4 ways $0.00` the frames show. Its rule at `:115-119` is `position: absolute; ... bottom: 6px; ... z-index: 45`, so it is pinned to the bottom of the grid and painted at z-index 45; the feature entry overlay does not sit above it and nothing suppresses it while the overlay is up. Not locked.
- Proposed fix: suppress the win line label while a feature entry overlay is presented, or raise the overlay above `z-index: 45` in the stacking context. Small either way.

## STC-POPOUTS-B-13 MEDIUM Inside the max win unit lockup, `×` and `BET` share no baseline, no size and no bottom edge, because both opt out of the baseline alignment their own container declares

- Frames: `reports/screens/stream-test-2026-07-28/205_popout-s_maxwin_celebration.png`, `204_popout-s_transition_maxwin_overlay_fade.png`
- Claim: at 7x nearest neighbour magnification of the region x `180` to `350`, y `105` to `155` of frame `205`, the unit reads as a large raised `×` followed by a smaller, dimmer `BET` set lower, so that the `×` sits clear above `BET`'s cap height. Third pass extents from that magnification: `×` ink spans about y `120` to `132` and `BET` ink about y `126` to `136`. Two glyph runs of one lockup, at two sizes, on two baselines, with two different bottom edges, and no shared alignment of any kind between them. On the surface the whole game builds toward this is the detail an art director reads first.
- **The source says the same thing, and says why**, so this is derived and then confirmed rather than discovered by measurement, per convention (l.2). `frontend/src/lib/components/MaxWinCelebration.svelte:295-300` declares the container as `display: flex; align-items: baseline; gap: 0.1em`, which is the correct intent. Both unit spans then override it: `.c1-max-x` at `:309-315` is `font-size: 46px; align-self: flex-end; padding-bottom: 0.12em` and `.c1-max-betlabel` at `:316-323` is `font-size: 22px; align-self: flex-end; padding-bottom: 0.28em`. `align-self: flex-end` discards the baseline the container asked for and aligns bottom edges instead, and the two `padding-bottom` values are then expressed in `em`, so they resolve against different font sizes and the bottom edges do not align either. **At this viewport the narrow branch at `:367-369` is in force**, since `@media (max-width: 500px)` matches a 400px frame: `.c1-max-mult` drops to `50px`, `.c1-max-x` to `24px` and `.c1-max-betlabel` to `13px`. The two paddings therefore resolve to about `2.9px` and `3.6px` against a `24px` and a `13px` glyph, which is exactly the stagger the frame shows.
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:309-315`, `:316-323`, with `:295-300` as the container and `:367-369` as the narrow branch. Not locked.
- Proposed fix: drop `align-self: flex-end` from both spans and let the container's own `align-items: baseline` do the work, tuning with a single shared `padding-bottom` in `px` if an optical nudge is still wanted. One property removed from each of two rules.

## STC-POPOUTS-B-11 LOW The composition's optical centre sits left of the cabinet's geometric centre, because the scene's visual mass is all on the left

- Frames: `reports/screens/stream-test-2026-07-28/203_popout-s_post_feature_base.png`, `202_popout-s_transition_feature_exit.png`, `186_popout-s_transition_paytable_closing.png`
- Claim: the cabinet is centred by construction, which `frontend/src/App.svelte:801` states in terms (*the frame is horizontally centred in the 1280 stage by construction*), and I am not disputing the geometry. **The second pass verified this against the raster rather than taking it on trust**: in frame `203` the cabinet's outer bright bezel runs x `78` to `321`, giving a left margin of `78` and a right margin of `78`, and a centre of `199.5` against the frame's own `199.5`. The cabinet is centred to the pixel. The composition is nonetheless unbalanced at 400x225: the band of scene art to the left of the cabinet carries the character and the car, both high contrast and both reading as subjects, while the band to the right is empty gradient with light rays. The eye is pulled left of the element it is meant to be watching. It takes comparison across the three base frames to see, hence LOW, but a stream overlay that crops symmetrically about the frame centre will not crop symmetrically about the perceived subject.
- Where fixable: UNKNOWN. This is the scene layer's content weighting rather than the stage transform, and it was not isolated within this shard's source budget.
- Proposed fix: PARK(this is an art call, not a layout bug). The options are to lighten the left scene subjects at viewport widths under about 520px, to add a matching right hand subject, or to accept the weighting as deliberate.

*Numbering note: ids `01` to `11` are the first pass's and are held stable because they already carry source line numbers. `12` is the second pass's addition and `13` is the third pass's, and both are placed in severity order above `11` rather than renumbering anything.*

## Explicit absences, signed

- **No element collides with or is cut by the TOP edge of the viewport in any of my 25 frames.** I checked the `PAYTABLE` header in `183`, `184` and `185`, the `FEATURES` header in `187` and `188`, the magenta dialog border in `189` to `192`, and `MAX WIN REACHED!` in `204` and `205`, which is the tightest of them: its glyph tops clear the edge and its glow is not cut. Measured on `205`, the title block's ink starts at y `7`.
- **No element collides with or is cut by the LEFT edge of the viewport in any of my 25 frames.** The paytable and features panels both hold their yellow left accent bar clear of the edge, and the HUD's leftmost control keeps its margin in every base, feature and post collect frame. Every clipping finding above is on the right edge or the bottom edge.
- **No element is cut by the BOTTOM edge of the viewport.** The HUD bar is fully inside the frame on `186`, `193` to `203`, `206` and `207`, and the `COLLECT` button is fully inside on `204` and `205`, its lower edge measuring y `208` of `225`. Every clipping finding above is against a PANEL edge, not the viewport bottom.
- **No two pieces of text overlap or double draw anywhere in my 25 frames.** The only occlusions present are the deliberate footer rule in `187` and `188` and the entry card over the reels in `193` to `201` and `207`, and both are reported above under their own ids rather than as overlaps. The `5,000` and `×` pair on `205`, which one pass suspected of touching, measures about `6px` of clearance and is not an overlap.
- **No large dead region that reads as unfinished, beyond those already reported.** The dead space inside the `PRICE` and `RTP` columns is STC-POPOUTS-B-09, the paytable's header to content ratio is STC-POPOUTS-B-03, and the empty upper half of the buy dialog is part of STC-POPOUTS-B-01. The scene bands either side of the cabinet are breathing room by design and are reported only for their optical weighting, at LOW, as STC-POPOUTS-B-11. Nothing else in the 25 frames is a dead region.
- **No HUD element crowds or overlaps another HUD element at this viewport.** The bottom bar in `186`, `193` to `203`, `206` and `207` keeps clear separation between the icon cluster, the `BAL` pod, the `WIN` pod, the bet stepper and the spin control in every frame. The HUD's fault on this run is the lateral jump reported as STC-POPOUTS-B-06, not crowding.
- **No layout jump between `183`, `184` and `185`.** The three paytable sections hold the header block, the accent bar and the close button in identical positions, so the paytable's chrome is stable across sections.
- **No layout jump across `196` to `201`.** They are compositionally identical, which is itself reported as STC-POPOUTS-B-10.
- **No misalignment found in the HUD bet stepper.** Unlike the FEATURES menu stepper reported as STC-POPOUTS-B-05, the HUD's down arrow, `$1.00` and up arrow in `186`, `202`, `203`, `206` and `207` read as an evenly spaced group.
- **MID-01 is not observable in this frame range** and is not reported. The popout-s big win count up pair is frames `169`, `170` and `171`, which belong to another squad. I did not open them.
- **TR-114, the replay ghost pod, does not appear.** No replay surface is present in frames `183` to `207`.
- **Q-27, the Vite scaffold CSS remnants, is not observable from these frames.** No link and no unstyled surface reaches any of the 25, so neither the indigo link colour nor the `#242424` body background can be judged from them.

### Three candidate findings the second pass raised by eye and then REFUTED by measurement, recorded so nobody re-opens them

Written out because a refuted candidate is worth more to the marshal than a silent one, and because all three would have been plausible in prose.

1. **"The reel cabinet is not horizontally centred."** Estimated by eye at roughly 68px left against 78 to 80px right. **Refuted.** Frame `203`, outer bright bezel x `78` to `321`: margins `78` and `78`, centre `199.5` against the frame's `199.5`. The eye was reading the scene glow, not the bezel. This is what STC-POPOUTS-B-11 correctly separates.
2. **"The paytable header is not symmetric about its panel."** Estimated by eye from the close button appearing inset from the content column. **Refuted.** Frame `183`: the `PAYTABLE` wordmark runs x `45` to `210` and the close ring x `318` to `355`, inside a panel spanning about x `16` to `383`. Left inset `29`, right inset `28`. Symmetric.
3. **"The unit `×` touches the final `0` of `5,000`, and sits at mid height rather than on the baseline."** **Both halves refuted**, see the note under STC-POPOUTS-B-07: about `6px` of clearance, and baselines agreeing to about 1px.

## KNOWN matches

- KNOWN(TR-115 / TR-086): `reports/screens/stream-test-2026-07-28/206_popout-s_transition_maxwin_collect_fade.png` and `207_popout-s_post_collect_base.png` show the HUD as `BAL $50K` beside `WIN $5K`, while `203_popout-s_post_feature_base.png` shows `BAL $50K` beside `WIN $363.89` and `202_popout-s_transition_feature_exit.png` shows `WIN $319.45`. The same pod switches between an abbreviated and a two decimal format inside one session, four frames apart, and the abbreviation lands on the game's largest possible win: the 5,000x cap on a `$1.00` bet renders as `$5K` in the same frame family where the celebration overlay is announcing it in full. Money display fit class, mapped to final mile JOB 3.
- KNOWN(Q-26): `reports/screens/stream-test-2026-07-28/190_popout-s_dialog_buy_overdrive.png` and `192_popout-s_dialog_nitro_overdrive.png` render the max win stat as `5,000x` with a letter `x`, while `205_popout-s_maxwin_celebration.png` renders the same quantity as `5,000×` with the multiplication sign at `frontend/src/lib/components/MaxWinCelebration.svelte:155`. Two surfaces, one session, two glyphs for one quantity. Fresh frame evidence for the multiplication glyph class; no new id opened. `188_popout-s_features_menu.png` additionally carries the `fsModes.ts` blurbs the row enumerates, rendering `1x bet` twice and `1.25x bet`; at 400x225 the glyph on that surface is not resolvable, so that frame is recorded as surface presence only and not as independent confirmation.
- KNOWN(Q-16 park): `reports/screens/stream-test-2026-07-28/205_popout-s_maxwin_celebration.png` is the max win overlay and shows no continue hint at all. Recorded against the park because the park's urgency assessment lists `Press COLLECT or hit Enter to continue` as a visible hardcoded English string on this surface, and at 400x225 it is not visible. Note for the marshal: the source now reads `t($locale, 'maxWinHint', localeMode)` at `frontend/src/lib/components/MaxWinCelebration.svelte:166`, so the park's description of that particular string may be stale; the visibility gap is reported separately as STC-POPOUTS-B-08.
- KNOWN(Q-34): `reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png` renders the mode name as `Cruise`, which is the features menu half of the row. The HUD badge half is not in this frame range, so this confirms one side only.
- KNOWN(MID-02): `reports/screens/stream-test-2026-07-28/205_popout-s_maxwin_celebration.png` and `204_popout-s_transition_maxwin_overlay_fade.png` are two of the max win frames MID-02 claims (*every session's big-win triple plus its max-win frames*). Recorded as fresh evidence that the surface is present at this viewport. No new id opened. STC-POPOUTS-B-07 on the same frames is a distinct claim, about centring, and does not depend on which glyph is used.

tree_after: `git status --porcelain` run from the repository root after this write, verbatim:
```
?? reports/qa/stream_test/shards/STC-DESKTOP-A.md
?? reports/qa/stream_test/shards/STC-DESKTOP-B.md
?? reports/qa/stream_test/shards/STC-LAPTOP-A.md
?? reports/qa/stream_test/shards/STC-LAPTOP-B.md
?? reports/qa/stream_test/shards/STC-POPOUTL-A.md
?? reports/qa/stream_test/shards/STC-POPOUTL-B.md
?? reports/qa/stream_test/shards/STC-POPOUTS-A.md
?? reports/qa/stream_test/shards/STC-POPOUTS-B.md
```
Eight untracked shard files: mine, plus seven other squads' shards which are not mine and not my problem. **Nothing shows as MODIFIED and nothing shows as DELETED.** No tracked file in the repository was touched by this squad, and no project script was run.
