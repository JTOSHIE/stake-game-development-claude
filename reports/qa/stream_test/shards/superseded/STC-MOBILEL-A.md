# STC-MOBILEL-A, composition (mobile-l, frames 208 to 233)
scope: every `mobile-l` frame numbered 208 to 233 inclusive, viewport `425x812`, lang `en`, 26 frames, each opened exactly once
frames_read: 26

note on numbering: findings 01 to 11 keep the ids and order of this squad's first pass, which reached disk before it was cut off. 12 and 13 were found on the completing pass and are appended rather than interleaved, so no existing id moves; both carry their own severity tag, and 12 is a MEDIUM sitting after three LOWs for that reason. The completing pass also re-tested one candidate of its own (the Overdrive table's centring) and refuted it, recorded under absences.

## STC-MOBILEL-A-01 STREAM The reel window empties into visible holes on spin start, with the scene background reading straight through the lit frame
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/214_mobile-l_transition_reels_accelerating.png`
- Claim: the manifest note for this frame is `Reels accelerating, about 250ms after spin press`. At that moment the reel window inside the cyan frame is not full. Reel 1 carries three tiles and its bottom row is empty; reel 2 carries two tiles and its bottom two rows are empty; reel 3 carries exactly one tile, the gold hex in row 1, with rows 2, 3 and 4 empty; reels 4 and 5 still carry the full idle board of piston tiles from `213_mobile-l_base_idle.png`. The result is a single contiguous empty region of roughly 170 by 170 device pixels in the lower left and centre of the reel window, through which the parallax city plate and its magenta chevron are plainly visible inside the lit frame. Compare the immediately following frame `215_mobile-l_transition_reels_full_speed.png`, where the window is full. The outgoing board is therefore cleared before the incoming strip has filled the window, and every spin passes through this state.
- Where fixable: `frontend/src/lib/components/ReelsCanvas.svelte:404-433` (the `visibleSymbols` accessor that returns `strip.slice(startIdx, startIdx + VISIBLE_ROWS)` per reel while `reelOffsets` is animating), not locked
- Proposed fix: hold the settled board underneath the incoming strip until the strip covers the window, or pad the strip so the window is never short of four tiles per reel.

## STC-MOBILEL-A-02 HIGH The HUD menu panel slices a live bet control in half and covers the balance pod, with no backdrop scrim to explain it
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/223_mobile-l_transition_menu_opening.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/224_mobile-l_hud_menu.png`
- Claim: the menu panel is anchored bottom left and its right edge lands mid control. The bet row's decrease chevron is bisected: only its right portion is visible, emerging from behind the panel edge immediately to the left of `$1.00`, while the increase chevron beside it is whole. The `BALANCE` pod is entirely covered. Every other HUD element (`WIN` pod with `$16.20`, `$1.00`, `FEATURES`, `SPIN`, `MAX`) stays at full brightness, so nothing marks the panel as a layer and it reads as a rectangle punched through the interface. The two frames are pixel identical, so this is the settled state and not a mid transition artefact.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte:1495-1515` (`.hud-menu-panel` is `position: absolute; bottom: calc(100% + 10px); left: 0; min-width: 250px` with no scrim element and no `max-width` against the viewport), not locked
- Proposed fix: dim the HUD behind the panel, and size or anchor the panel so its edge falls in the gutter between controls rather than across one.

## STC-MOBILEL-A-03 HIGH Balance digits read through the menu panel and sit inside its own text rows
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/223_mobile-l_transition_menu_opening.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/224_mobile-l_hud_menu.png`
- Claim: the panel fill is translucent enough that the covered balance pod is legible through it. The label `BALANCE` shows through on the `PAYTABLE` row, and the figure `$50,000.00` shows through on the `Session` row, immediately to the right of the word `Session` and on the same baseline, so a menu item and a currency figure occupy one line of text. This is separate from A-02: A-02 is where the panel sits, this is what it lets through.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte:1499` (`background: rgba(10, 14, 22, 0.92)` over an unblurred backdrop), not locked
- Proposed fix: raise the panel's background opacity, or add a blur or solid plate behind it, so nothing beneath it is legible.

## STC-MOBILEL-A-04 HIGH Paytable prose bullets pin the marker to a fixed left column while centring the text, so markers float unattached
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/231_mobile-l_paytable_04_rules.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/232_mobile-l_paytable_05_overdrive_free_spins.png`
- Claim: in the `RULES` list every `›` marker sits at the same left position, but each item's text is centre aligned in the panel, so the distance from marker to first word changes with the length of the line. On `231` the gap runs from roughly 17 device pixels on `Symbol values shown are per matching way;` to roughly 46 on `Wins pay left to right on adjacent reels`; on `232` it runs from roughly 21 on `The Overdrive meter starts at 1x and rises +1x` to roughly 40 on `Base game and Bonus Buy both return`. Several markers therefore stand alone in white space with no text beside them. The same `›` marker on the intro rules card, `211_mobile-l_intro_rules.png`, is set against left aligned text with a constant gap, so the two surfaces that use this bullet disagree with each other.
- Where fixable: `frontend/src/lib/components/PaytableModal.svelte:1042-1050` (`.rule-item` has `text-align: center` inherited from `.pt-section`, while the `::before` marker is `position: absolute; left: 0`), not locked
- Proposed fix: left align the bullet text in the paytable list to match the intro rules card, or drop the hanging marker and centre the text alone.

## STC-MOBILEL-A-05 MEDIUM The `COST` column on every bet mode card is left aligned under a centred header, while `RTP` and `MAX WIN` are centred
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/233_mobile-l_paytable_06_bet_modes.png`
- Claim: on all three visible cards, `Normal`, `Cruise` and `OVERBOOST`, the three column headers `COST`, `RTP` and `MAX WIN` are centred over their columns. Under `RTP` the value `96.35%` is centred on the header to the pixel, and under `MAX WIN` the value `5,000×` likewise. Under `COST` the two stacked values (`1x` over `$1.00` on `Normal` and `Cruise`, `1.25x` over `$1.25` on `OVERBOOST`) are flush left and sit roughly 10 device pixels left of the header's centre. One column of three behaves differently from the other two on a card whose whole point is a three column comparison.
- Where fixable: `frontend/src/lib/components/PaytableModal.svelte:1176-1200` (`.mode-stat` block: the cash sub-line is emitted as a separate element that does not inherit the cell's `text-align: center`), not locked
- Proposed fix: centre the cost cell's contents to match the other two columns.

## STC-MOBILEL-A-06 MEDIUM The win breakdown strip collides with the reel frame's bottom border
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/221_mobile-l_transition_bigwin_countup_late.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/222_mobile-l_bigwin_settled.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/223_mobile-l_transition_menu_opening.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/224_mobile-l_hud_menu.png`
- Claim: the pale breakdown strip that carries `L3  x4  1 ways  $0.20` on `221`, `223` and `224` and `M3  x5  8 ways  $16.00` on `222` is pinned to the bottom of the reel window with no clearance. Its lower edge runs into the cyan frame stroke rather than clearing it, so the strip and the frame share pixels, and the strip's text sits closer to the frame than to the symbol row above it. The strip's own text is also the smallest type anywhere in the interface at this viewport, several steps below the `BET` and `BALANCE` labels beside it.
- Where fixable: `frontend/src/lib/components/WinLineInfo.svelte` (the strip component; its wrapper is positioned against the reel frame's bottom rather than inset from it), not locked
- Proposed fix: give the strip bottom clearance inside the reel frame, or float it below the frame as its own row.

## STC-MOBILEL-A-07 MEDIUM Session modal close button overhangs the panel's own right margin
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/225_mobile-l_session_panel.png`
- Claim: the modal is well centred, but its header row is not. The title `Session information` is inset from the left edge by roughly 23 device pixels, while the `✕` button's right edge is inset from the right edge by roughly 14, so the header's two ends do not share a margin. The button also overhangs the right aligned value column below it (`00:00:21`, `5`, `$5.00`, `$20.10`, `+$15.10`), whose right edge sits roughly 8 pixels further in, so the strongest vertical edge in the panel is broken by the one element above it.
- Where fixable: `frontend/src/lib/components/SessionPanel.svelte` (header row padding against the stat row padding), not locked
- Proposed fix: set the close button's right inset equal to the value column's, so the header and the data share one right edge.

## STC-MOBILEL-A-08 MEDIUM The `WIN!` caption is drawn bare over a lit symbol and its own win line
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/219_mobile-l_win_presentation.png`
- Claim: the standard win presentation renders the caption `WIN!` with no plate, pill or outline, directly over the reel window at about the grid centre. At that position it overlaps the lit tile of reel 3 row 2 and is crossed by the cyan win line that runs from that tile down to reel 4, so the glyphs and the line share pixels. Every other text element on this surface (`BALANCE`, `WIN`, `BET`, `FEATURES`) sits on a plate.
- Where fixable: UNKNOWN (searched the component tree for the caption's literal without locating its owner within this pass's file budget)
- Proposed fix: give the caption a plate or a contrast shadow, or place it clear of the winning cells.

## STC-MOBILEL-A-09 LOW The paytable scroll region fades at the bottom and hard clips at the top, so cards under the sticky header are beheaded
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/227_mobile-l_paytable_top.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/228_mobile-l_paytable_01_match_symbols_on_adjacent_reels_st.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/230_mobile-l_paytable_03_symbol_payouts.png`
- Claim: at the bottom of the panel the last line of content is dimmed into a fade, for example `3 / 4 / 5 = 1x / 3x / 10x +` on `227`. At the top there is no equivalent treatment: on `228` the `Match symbols on adjacent reels` card has its left, right and bottom borders drawn but its top border and both top corners cut flush against the sticky header's rule, and on `230` the `SYMBOL PAYOUTS` heading meets the same hard line. The two ends of one scroll region are finished differently.
- Where fixable: `frontend/src/lib/components/PaytableModal.svelte:944-958` (`.pt-body` carries a bottom-only `mask-image` gradient), not locked
- Proposed fix: mirror the bottom fade mask at the top of the scroll region.

## STC-MOBILEL-A-10 LOW The `BUY FEATURE` bar's price is closer to the bar's right edge than its label is to the left
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/232_mobile-l_paytable_05_overdrive_free_spins.png`
- Claim: on the highlighted bar the label `BUY FEATURE` is inset from the bar's left edge by roughly 21 device pixels while the price `$100.00` is inset from the right edge by roughly 11, on a bar whose two ends are otherwise identical in treatment.
- Where fixable: `frontend/src/lib/components/PaytableModal.svelte:1148-1166` (`.buy-row` padding), not locked
- Proposed fix: equalise the bar's horizontal padding.

## STC-MOBILEL-A-11 LOW The bottom control row is optically unbalanced: three of its four secondary controls carry ring chrome and `MAX` carries none
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/213_mobile-l_base_idle.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/216_mobile-l_dead_spin_1_settled.png`
- Claim: the row is menu, turbo, `SPIN`, `MAX`, autoplay. The menu, turbo and autoplay controls are each drawn as a bordered dark circle of matching diameter; `MAX` is set as bare yellow type with no comparable container, in the one slot that is the mirror of the turbo button on the left. The left pair therefore reads as two buttons and the right pair as one button plus a word, so the row's visual weight sits left of the `SPIN` axis it is meant to be symmetrical about.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte` (the `.hud-btn` ring styling is applied to the menu, turbo and autoplay buttons and not to the max-bet button), not locked
- Proposed fix: PARK(giving `MAX` matching chrome is an art call about control hierarchy, not a padding tweak).

## STC-MOBILEL-A-12 MEDIUM At full spin speed four reels sit on the settled row grid and only the fifth is off it, clipped at both ends
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/215_mobile-l_transition_reels_full_speed.png`
- Claim: on the frame the manifest calls `Reels at full speed`, reels 1 to 4 sit on exactly the row grid of the settled boards (`216_mobile-l_dead_spin_1_settled.png`), with four whole tiles each and clean boundaries shared across all four columns, while reel 5 alone is offset downward by roughly half a tile, so it is the only column showing a part tile at the top of the window and a part tile at the bottom. Judged as a composition, four columns share a set of horizontal edges and the fifth breaks every one of them, and the only clipped art on the surface is in that column. Read as a sequence with A-01, the reel bank does not present as one coherent object at any point between the press and the settle.
- Where fixable: `frontend/src/lib/components/ReelsCanvas.svelte` (per-reel spin phase and offset, the same `reelOffsets` mechanism named in A-01), not locked
- Proposed fix: PARK(the cause is the spin animation's per-reel phase rather than layout, and the motion squad's reading of the same frame should sit beside this before anything is changed).

## STC-MOBILEL-A-13 LOW The bet pod carries a dead gap of roughly 190 device pixels between its label and its controls
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/213_mobile-l_base_idle.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/216_mobile-l_dead_spin_1_settled.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/217_mobile-l_dead_spin_2_settled.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/218_mobile-l_dead_spin_3_settled.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/219_mobile-l_win_presentation.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/222_mobile-l_bigwin_settled.png`
- Claim: inside the bet pod the label `BET` ends at roughly the same x as the left edge of the `$50,000.00` figure in the `BALANCE` pod directly above it, and the decrease chevron does not begin until roughly 190 device pixels further right, so a little under half the pod's width is empty. The two pods immediately above it fill their width with a centred label over a centred figure, so at this viewport the row below them reads as the one HUD element with a hole in it. Recorded as LOW and with the fix parked because a label-left, control-right split is a conventional pattern and the gap is a consequence of it rather than a mistake in it.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte` (the bet row's `justify-content: space-between`), not locked
- Proposed fix: PARK(centring the value and stepper cluster in the pod versus keeping the label-left split is an art call about how the row reads beside the two pods above it).

## Explicit absences, signed

Signed for the 26 frames listed under scope, each opened once with the Read tool.

- **Viewport edge collisions and clipping, other than those reported.** Checked every frame for content touching or crossing the `425x812` bounds. The reel frame comes closest, sitting a few pixels inside both side edges on `213` through `222`, but it is inset on both sides by the same amount and nothing crosses. The paytable modal on `226` through `233` is inset from the top and bottom by what reads as the same margin and its side margins match. No text, pod, button or badge is cut by a viewport edge anywhere in this run. Nothing found beyond A-01, which is a hole inside a frame rather than a clip at the viewport.
- **Splash composition, `208` and `209`.** Checked the logo and `TAP TO CONTINUE` as a group against the viewport centre. The group is horizontally centred and sits marginally below the vertical centre, which is the conventional optical placement. Re-tested on the completing pass with the opposite hypothesis, that the surface is dead rather than airy: the mark is under half the 425px width and the fields above and below the group run to roughly 270 and 250 device pixels of near-flat black, so the hero does not scale to the portrait viewport. Judged deliberate on both passes and NOT raised as a finding, since a centred mark over a wide black field is the genre's standard splash. Recorded here so the judgement is visible rather than silent, and so an art direction call can overturn it without re-deriving it.
- **Intro rules card, `210` and `211`.** Checked the card's centring, its internal padding and the `Continue` button. The card is centred both ways, the button is centred in the card, and the heading, bullet markers and bullet text share one left edge. This is the surface the paytable list of A-04 fails to match. No finding.
- **Session modal centring, `225`.** The modal itself is centred horizontally and vertically to within a pixel or two; only its header row misaligns, which is A-07. No finding on the modal box.
- **Symbol payout card grid, `229` and `230`.** Checked the two column grid's outer margins, gutter and the alignment of the `3x`/`4x`/`5x` label column against the right aligned value column across `H1`, `H2`, `M1` and `M2`. Consistent. No finding.
- **Overdrive award table, `231` and `232`.** Checked the three columns `SCATTERS`, `FREE SPINS`, `INSTANT AWARD` and their values `3`/`8`/`1x`, `4`/`12`/`3x`, `5`/`16`/`10x`. Headers and values are centred on the same axes. No finding. The completing pass raised its own candidate here, that the table's block centre sits left of the panel's, and REFUTED it: the columns are content sized, so the centre to centre spacings differ (roughly 104 then 128), but the block's outer extent still centres on the panel to within a few pixels. The measure that matters is the block's extent, not the midpoint of the column centres, and taking the second for the first is how this candidate looked real. Not a finding. Note also that this is the same three column pattern that A-05 gets wrong one card later, so that failure is local to the bet mode card rather than to the pattern.
- **Sequence jumps across adjacent frames.** Compared each frame against its neighbour for a layout that moves when it should not. The HUD block (`FEATURES`, `BALANCE`/`WIN`, `BET`, control row) holds the same geometry across `213`, `214`, `215`, `216`, `217`, `218`, `219`, `220`, `221` and `222`, including through the big win banner, so nothing reflows when the banner arrives. The paytable header and gold rail hold position across `227` to `233` while the content scrolls. The sequence discontinuities found are A-01 (`213` to `214`) and A-12 (`215` against the settled grid of `216`); there are no others.
- **Big win banner geometry, `220`, `221`, `222`.** Checked whether the banner band is placed off centre or crops the grid unevenly. It covers the middle two of the four symbol rows, leaving one row visible above and one below, and `BIG WIN`, the amount and the unit line are centred on the same axis as the ring behind them. Judged deliberate. No composition finding. The amount disagreement and the glyph are recorded as KNOWN below and are not mine to open.
- **Money fit at this viewport.** No currency figure is clipped, ellipsised or overflowing on any of the 26 frames: `$50,000.00` sits inside the `BALANCE` pod with clear room both sides on every base frame, `$16.20` inside the `WIN` pod, `$100.00` inside the buy bar, and the banner's `$16.20` well inside the reel width. So this run adds no fresh mobile-l evidence to TR-115 / TR-086, and the absence of evidence is stated rather than left to be read as an oversight.
- **Out of lens, recorded because a later squad should not have to rediscover it.** `216`, `217` and `218` are captioned `Dead spin 1 of 3`, `2 of 3` and `3 of 3` in `MANIFEST.json` and show an identical symbol board in all three, and `BALANCE` reads `$50,000.00` unchanged across all three plus `219` (`WIN` `$3.90`), `220`, `221` and `222` (`WIN` `$16.20`). Not a composition claim and not numbered here; flagged for whichever squad holds state and money. Likewise `223` against `224` and `226` against `227`: each `transition_` frame is compositionally identical to the settled frame it precedes, which is material for the motion lens rather than this one.

## KNOWN matches
- KNOWN(MID-01): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/220_mobile-l_transition_bigwin_countup_early.png`, banner reads `$10.27` while the HUD `WIN` pod already reads `$15.95`, on a win that settles at `$16.20` in `222_mobile-l_bigwin_settled.png`. The mobile-l instance of the same three frame pattern the ledger records for desktop, laptop, popout-l and popout-s.
- KNOWN(MID-02): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/220_mobile-l_transition_bigwin_countup_early.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/221_mobile-l_transition_bigwin_countup_late.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/222_mobile-l_bigwin_settled.png`, all render `16x BET` with the letter `x`, while `231_mobile-l_paytable_04_rules.png` writes `5,000×` and `233_mobile-l_paytable_06_bet_modes.png` writes `5,000×` and `1.25x` on the same run of frames.
- KNOWN(Q-26): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/233_mobile-l_paytable_06_bet_modes.png` shows the `fsModes.ts` survivors on stream at this viewport: the `OVERBOOST` card's cost reads `1.25x` with a letter `x` in the same three column row as `5,000×` with the sign, and its blurb reads `about 1.6x the feature trigger rate. Debits 1.25x every spin while ON.` `232_mobile-l_paytable_05_overdrive_free_spins.png` carries `pay 100× your bet` with the sign, so the two glyphs appear within one scroll of each other.
- KNOWN(Q-34): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/233_mobile-l_paytable_06_bet_modes.png` renders the mode name as `Cruise` on the paytable card, the surface the row names as the non uppercased side.
- KNOWN(Q-16 park): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/224_mobile-l_hud_menu.png` shows parked hardcoded English on stream at this viewport: `PAYTABLE`, `Session`, `Mute`, `MUSIC`, `SOUND`. Recorded as visibility evidence only; the row stays parked.

tree_after: pending, recorded at the end of the run
