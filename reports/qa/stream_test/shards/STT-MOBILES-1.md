# STT-MOBILES-1, typography (mobile-s, frames 312 to 329, 1600px upscaled)
supersedes: STT-MOBILES-A.md (partially: only its findings that fall inside frames 312 to 329. Frames 330 to 337 of that shard belong to a sibling re-run squad and are left to it. STT-MOBILES-B.md covers frames 338 to 363 and contains no finding citing a frame in my range, so nothing in it is superseded here.)
scope: the 18 `mobile-s` frames numbered 312 to 329 inclusive, viewport 320x568, lang `en`, read from `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/`. Frame paths below are given in the upscaled directory because that is what was read; the committed originals at `reports/screens/stream-test-2026-07-28/` carry the same numbering.
frames_read: 18

## STT-MOBILES-1-01 HIGH The HUD menu runs three type treatments across five consecutive rows

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/327_mobile-s_transition_menu_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/328_mobile-s_hud_menu.png`
- Claim: the menu stacks, top to bottom, `PAYTABLE`, `Session`, a divider, `Mute`, then the slider labels `MUSIC` and `SOUND`. That is three treatments inside one list a viewer takes in at a glance:
  - `PAYTABLE`, all caps, `font-size: 0.8rem`, no tracking. The string is the translated value `'PAYTABLE'` rendered by `{$tr('paytable')}` at `frontend/src/lib/components/HudOverlay.svelte:428`.
  - `Session` and `Mute`, title case, same class, same size, no tracking. Both are hardcoded English literals: `Session` at `frontend/src/lib/components/HudOverlay.svelte:429` and `{$isMuted ? 'Unmute' : 'Mute'}` at `:432`.
  - `MUSIC` and `SOUND`, all caps at `font-size: 0.5rem` with `letter-spacing: 0.12em`, so they disagree with the three rows above on size and tracking as well as sitting in the same list.
  The mechanism is that `.hud-menu-item` (`frontend/src/lib/components/HudOverlay.svelte:1609-1619`) declares neither `text-transform` nor `letter-spacing`, so each row shows exactly the case its author typed, while `.audio-label` (`:1696-1703`) sets its own size and tracking. The pattern repeats in all four layout variants of the component, at `:428-432`, `:546-575`, `:655-659` and `:817-821`.
- Resolution note: NEW AT 1600PX. At the native 320x568 capture the whole frame is about 242 image tokens and these rows are about 14 device pixels tall; `Session` against `SESSION` is not resolvable there. The native pass reported two conventions at MEDIUM on what it could see.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte:1609` (the shared `.hud-menu-item` rule), with the literals at `:429,432`, `:547,575`, `:656,659` and `:818,821` as the alternative site.
- Proposed fix: add `text-transform: uppercase` and a matching `letter-spacing` to `.hud-menu-item` at `:1609`. One property pair normalises all four layout variants, survives future rows, and does not touch a locale file. `Session` and `Mute`/`Unmute` remain on the Q-16 park as hardcoded English; the CSS fix does not have to wait for that park.

## STT-MOBILES-1-02 HIGH The win-line strip prints a raw internal symbol id, an ASCII letter as a multiplication sign, and an unconditional plural

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/325_mobile-s_transition_bigwin_countup_late.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/326_mobile-s_bigwin_settled.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/327_mobile-s_transition_menu_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/328_mobile-s_hud_menu.png`
- Claim: the strip across the foot of the reel frame carries four tokens. On frames `325`, `327` and `328` they resolve as `L3`, `x4`, `1 ways` and an amber money figure; on frame `326` as a different, `H`-grade id, `x5`, `8 ways` and an amber money figure. Three defects in one line, and the strip is on screen for the whole of every win.
  - The first token is a maths-package symbol id shown to the player. `frontend/src/lib/components/WinBreakdown.svelte:15-24` holds `SYMBOL_IDS` mapping `H1..L3` onto themselves and returns the raw id for everything except `W` and `S`, which do get translated words. So eight of the ten symbols print their internal id and two print a name.
  - `x4` is the ASCII letter `x` (U+0078): `frontend/src/lib/components/WinBreakdown.svelte:93` renders `x{current.kind}`. The same session's intro rules card writes the same operator as `×` (U+00D7) five times, at `frontend/src/lib/i18n/translations.ts:1540,1541,1554,1555`. Two conventions for one operator, four screens apart in one session. This is the class charter row Q-26 enumerates, and it is a further instance outside the `fsModes.ts` list Q-26 names, exactly as MID-02 records that enumeration being incomplete.
  - `1 ways` is `{current.ways} ways` with no plural handling, at `frontend/src/lib/components/WinBreakdown.svelte:94`. Not viewport dependent: it renders on every viewport on every one-way win.
- Resolution note: NEW AT 1600PX for the `L3` and `x4` tokens. The native pass could resolve only the first token and its own absences section states that no glyph-identity claim is made from pixels anywhere in that shard, so it could not read the operator off a frame. VISIBLE AT BOTH for `1 ways`, which it reported as A-06.
- Where fixable: `frontend/src/lib/components/WinBreakdown.svelte:15-24` (the id map), `:93` (the operator) and `:94` (the plural).
- Proposed fix: `:93` to `×{current.kind}`, a one-character edit. `:94` to `{current.ways} {current.ways === 1 ? 'way' : 'ways'}`, or a plural-aware key if the strip is to be localised; the string is hardcoded English either way. `:15-24` PARK(naming the eight tier symbols is an art call and adds sixteen locale values per name).

## STT-MOBILES-1-03 HIGH The win-line strip is set smaller than any other string in the game, and does not become legible even at 1600px

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/325_mobile-s_transition_bigwin_countup_late.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/326_mobile-s_bigwin_settled.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/327_mobile-s_transition_menu_opening.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/328_mobile-s_hud_menu.png`
- Claim: at 1600px the strip's cap height measures under a third of the cap height of `BET` in the pod beneath it in the same frame, and under a quarter of the `$1.00` beside it. The amber money token at the right, which is the only figure on the strip a player would want, does not resolve into separate digits even at 1600px: on frame `325` it reads as a smear consistent with `$0.20` and on frame `326` as a smear consistent with `$16.20`, and neither can be transcribed with confidence. The plate is also shallower than the text it holds, so the glyph bottoms are cut across the whole row on all four frames.
  The mechanism is a scale, not a font size. `frontend/src/lib/components/WinBreakdown.svelte:136` sets `font-size: 0.7rem` on the plate face and `:146` sets `0.62rem` on `.wb-ways`, which are 11.2px and 9.9px, entirely reasonable. The strip lives inside the fixed 1280px stage that `App.svelte` scales inline, so at a 320px viewport those become roughly 2.8 and 2.5 device pixels.
  This is not the TR-115 / TR-086 money-fit class: the strip is not clipped by its container's width, and three of its four tokens are non-money strings.
- Resolution note: VISIBLE AT BOTH, and hardened by the re-run. The native pass caught it as A-05. What is new is that the failure survives a roughly 5x linear resolution increase, which rules out capture resolution as the explanation and leaves the render itself.
- Where fixable: `frontend/src/lib/components/WinBreakdown.svelte:136,146`, and the scaled stage in `frontend/src/App.svelte`.
- Proposed fix: PARK(moving the strip out of the scaled canvas is a layout change, not a small one, and the same move was already made for the HUD region). The cheap interim is to size this component's text in inverse-scaled units, for example `calc(0.7rem / var(--S, 1))` with a cap, so it holds a legible floor at narrow widths.

## STT-MOBILES-1-04 HIGH Two adjacent lines of one locale block put the same verb on two consecutive screens in two cases, and the modal titles disagree the same way

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/313_mobile-s_splash.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/315_mobile-s_intro_rules.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/317_mobile-s_base_idle.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/329_mobile-s_session_panel.png`
- Claim: two conventions run side by side across the first four surfaces a player meets.
  - Buttons. The splash call to action is `TAP TO CONTINUE`, all caps and widely tracked (frame `313`). The very next screen's primary button is `Continue`, title case and untracked (frame `315`). The two strings are on **adjacent lines of the same locale block**: `frontend/src/lib/i18n/translations.ts:1556` reads `introContinue: 'Continue',` and `:1557` reads `splashPressAnywhere: 'TAP TO CONTINUE',`. The controls behind them are `SPIN` and `MAX`, both all caps (frame `317`), and the band above the HUD is `FEATURES`, all caps and tracked. `Continue` is the outlier, and it is on the game's second screen.
  - Modal titles. The intro card is titled `OVERDRIVE FREE SPINS`, all caps, tracked (frame `315`, `frontend/src/lib/i18n/translations.ts:1539`). The session panel is titled `Session information`, first word capitalised only, untracked (frame `329`, `frontend/src/lib/i18n/translations.ts:246`, rendered by `frontend/src/lib/components/SessionPanel.svelte:100`). Its style rule `frontend/src/lib/components/SessionPanel.svelte:194` sets `letter-spacing: 0.08em` and no `text-transform`. Every other title string in the same block is caps: `buyConfirmTitle: 'BUY OVERDRIVE FREE SPINS'` (`:1536`), `buyConfirm: 'BUY'` and `buyCancel: 'CANCEL'` (`:1538`), `totalWin: 'TOTAL WIN'`, `featureComplete: 'FEATURE COMPLETE'` (`:1535`).
- Resolution note: NEW AT 1600PX. Casing and tracking of a 16px button label and a 16px modal title are not judgeable in a 242-token thumbnail. The native pass asserted this at MEDIUM; the adjacent-source-line evidence and the consecutive-screen sighting raise it.
- Where fixable: `frontend/src/lib/components/SessionPanel.svelte:194` and the intro button's own rule in `frontend/src/lib/components/IntroSplash.svelte` (string at `:35`, `t($locale, 'introContinue', mode)`). String-side alternatives are `frontend/src/lib/i18n/translations.ts:1556` and `:246` plus their fifteen locale siblings.
- Proposed fix: `text-transform: uppercase` on the session-panel `h2` at `SessionPanel.svelte:194` and on the intro button's class. Both are CSS, so neither touches a locale file, and `rgSessionTitle` already has correct sentence-case values in all sixteen locales that would upper-case cleanly.

## STT-MOBILES-1-05 HIGH The big-win banner centres a narrow leading digit in a wide fixed box, so `$ 16.20` on the banner disagrees with `$16.20` in the HUD pod on the same frame

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/324_mobile-s_transition_bigwin_countup_early.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/325_mobile-s_transition_bigwin_countup_late.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/326_mobile-s_bigwin_settled.png`
- Claim: on frame `326` the banner reads `$ 16.20` with a visible gap between the currency symbol and the first digit, while the HUD WIN pod on the same frame reads `$16.20` with the symbol effectively touching the `1`. Measured off the 1600px render of `326`: banner `$` right edge to `1` left edge is about 15 render px against a digit advance of about 28 render px; the HUD equivalent is about 5 render px against about 30. The same gap is on `324` (`$ 10.28` banner against `$15.95` pod) and on `325`.
  Derived from source rather than diagnosed from the pixels, per convention (l.1). `frontend/src/lib/components/WinBanner.svelte:414` sets `.c1-amount .c1-digit { display: inline-block; width: 0.834em; text-align: center; }`, and `:409-413` records that 0.834em is Orbitron's widest digit advance while `:296` records the measured advances as `834 391 830 826 730 830 820 660 834 828`. A `1` is 391 of 1000, so a leading `1` centred in a 0.834em box carries `(834 - 391) / 2 = 0.222em` of empty space on its left. Add `.c1-amount`'s `letter-spacing: 2px` and the predicted gap is about 13 render px at this size, against the 15 measured. The measurement confirms the derivation, per (l.2).
  The component's own comment at `:299-300` states that the currency symbol is deliberately left unboxed so it is not spaced oddly. It is spaced oddly anyway, because the slack is on the digit's side of the pair, not the symbol's.
  This is **not** the shimmy TR-089 closed and the brief carves out. The count-up digits are steady; the defect is horizontal spacing between the symbol and the number, and it is present on the settled frame as well as the two count-up frames.
- Resolution note: NEW AT 1600PX. A 0.222em gap on a 50px numeral is about 5 CSS px, under two device pixels of the native thumbnail. The native pass did not report it and could not have.
- Where fixable: `frontend/src/lib/components/WinBanner.svelte:414`, with the markup at `:301-302`.
- Proposed fix: give the box `text-align: right` for the first character only, or drop the leading box and let the first digit set naturally, so the symbol and the number stay a pair. Either is one declaration and neither weakens TR-089, whose guarantee is that a digit cannot change the string's width mid-roll.

## STT-MOBILES-1-06 MEDIUM The in-reel `WIN!` callout and the HUD `WIN` label set the same word two ways on one frame

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/323_mobile-s_win_presentation.png`
- Claim: the callout drawn over the reels reads `WIN!` with its letters effectively touching and at a visibly heavier weight than any other uppercase string on the frame. The HUD pod label about 570 render px below it reads `WIN`, tracked at `letter-spacing:.18em` (`frontend/src/lib/components/HudOverlay.svelte:1328`, which also carries `text-transform:uppercase`). Two settings of one word, both on screen at once. The callout also carries the only exclamation mark anywhere in the eighteen frames, so it is the one surface writing in an excited register while `BALANCE`, `WIN`, `BET`, `FEATURES`, `SPIN` and `MAX` are all flat.
  No family claim is made: at 1600px the callout still carries enough glow bloom that the face cannot be identified from pixels. What is measurable is the tracking and the weight.
- Resolution note: VISIBLE AT BOTH. The native pass caught the tracking as A-12 at LOW. Raised to MEDIUM because at full resolution the weight difference is as large as the tracking difference and the exclamation mark resolves.
- Where fixable: `frontend/src/lib/components/WinCelebration.svelte` (`.small-win-flash`, the rule the native pass located at `:40`; not re-verified by this squad, which spent its source budget elsewhere).
- Proposed fix: add `letter-spacing: 0.12em` to `.small-win-flash` so the callout matches the HUD treatment of the same word, and settle the exclamation mark as an art call.

## STT-MOBILES-1-07 MEDIUM The intro rules card capitalises its game terms three ways inside four bullets

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/315_mobile-s_intro_rules.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/314_mobile-s_transition_splash_to_rules.png`
- Claim: within one card the player reads, verbatim from `frontend/src/lib/i18n/translations.ts:1540,1541,1542,1554`:
  - `3, 4 or 5 Scatters award 8, 12 or 16 free spins and pay an instant 1×, 3× or 10× total bet.`
  - `The Overdrive meter starts at 1× and rises +1× after every winning free spin, multiplying all later wins. It never resets during the feature.`
  - `3 or more Scatters during free spins award +5 free spins.`
  - `Bonus Buy: pay 100× your bet to start the feature immediately.`
  So `Scatters` is capitalised while `free spins` is lower case, in the same sentence, twice. The card's own heading is `OVERDRIVE FREE SPINS` in caps (`:1539`), so `free spins` appears in three cases on one screen. `Overdrive` is capitalised as a noun and `meter` is not. Capitalisation that changes between two views of one word is named in the standing mandate's inspection test; here it changes inside one card.
- Resolution note: NEW AT 1600PX. Body prose at a 320px capture width is roughly 12 device pixels; individual capital letters are not resolvable in a 242-token image.
- Where fixable: `frontend/src/lib/i18n/translations.ts:1539,1540,1541,1542,1554`.
- Proposed fix: pick one convention for the two game terms and sweep the English block. PARK(the sixteen-locale half: each of these keys has fifteen sibling values, so the sweep leaves English immediately and is larger than small).

## STT-MOBILES-1-08 HIGH Every animating HUD numeral relies on `tabular-nums`, which this project has already measured to be inert against Orbitron

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/323_mobile-s_win_presentation.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/324_mobile-s_transition_bigwin_countup_early.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/326_mobile-s_bigwin_settled.png`
- Claim: the HUD WIN pod runs its own rAF count-up over 400 to 800 ms on every win (`frontend/src/lib/components/HudOverlay.svelte:302-315`), and the frames capture it mid-roll: `$3.90` on `323`, `$15.95` on `324`, `$16.20` on `326`. The pod's value class sets `font-family: var(--fs-font-numeric); ... font-variant-numeric:tabular-nums` (`frontend/src/lib/components/HudOverlay.svelte:1332-1335`), and `frontend/src/app.css:98` resolves that token to `'Orbitron', 'Courier New', monospace`. `frontend/src/lib/components/WinBanner.svelte:293-296` records, from a measurement of the shipped woff, that **`tabular-nums` is inert against Orbitron because Orbitron ships no GSUB features at all**, and that its digit advances are `834 391 830 826 730 830 820 660 834 828` of 1000. So the declaration on the HUD buys nothing, a `1` is under half the width of a `0`, and the pod's readout is centred with `white-space:nowrap`, which means the number shifts horizontally as its digit VALUES change and not only as digits are added. That is the defect TR-089 was raised for, still live on the numeric surface a viewer watches on every single win rather than only on the big ones. The same inert declaration appears at `:1157`, `:1511`, `:1708`, `:1936` and `:2265`.
  Stated plainly, per (l.3): this is DERIVED from source and from the project's own measurement of its own font. It is not measured from my frames. Eighteen single-moment captures cannot show motion, and the two same-length pod captures I hold (`$15.95` on `324` and `$16.20` on `326`) differ by a predicted 0.196em, which is about 3 CSS px and below what I can read off a still. The finding is offered as a derivation with a named confirming experiment, not as a pixel claim.
- Resolution note: NEW AT 1600PX in the sense that the surface it lives on could not be read at native at all. The native pass signed `numeral width shimmy outside .fs-num: none observed` on the strength of four still frames, which is a claim four stills cannot support in either direction.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte:1332-1335` and its five siblings at `:1157`, `:1511`, `:1708`, `:1936`, `:2265`.
- Proposed fix: apply the TR-089 remedy to the HUD readouts: box the digits at `0.834em` as `frontend/src/lib/components/WinBanner.svelte:414` already does, and delete the inert `font-variant-numeric` declarations so nothing else is written against a feature this font does not have. The confirming experiment before the fix is a two-frame capture of the pod at `$11.11` and `$88.88` at one viewport; if the widths differ, the shimmy is real. `win_countup_steady_gate.mjs` already holds the equivalent property for `.fs-num` and is the obvious place to widen.

## STT-MOBILES-1-09 MEDIUM The 100x purchase is given a third name on the intro card

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/315_mobile-s_intro_rules.png`
- Claim: the intro card names the purchase `Bonus Buy`, twice: `Bonus Buy: pay 100× your bet to start the feature immediately.` (`frontend/src/lib/i18n/translations.ts:1554`) and `Base game and Bonus Buy both return 96.35% RTP. Maximum win 5,000× bet.` (`:1555`). The confirm dialog for the same purchase is titled `BUY OVERDRIVE FREE SPINS` (`:1536`) and its button is `BUY` (`:1538`), and the bet-mode list names the same mode `Buy Overdrive` (`frontend/src/lib/i18n/prose.ts:91`, cited from the superseded shard's A-08 rather than re-read here). Three names for one purchase, in three cases.
  Honest scope limit: only the `Bonus Buy` sighting is inside my eighteen frames. The other two surfaces are on frames held by sibling squads, so the source citation is the evidence for those and the frame is the evidence for mine.
- Resolution note: NEW AT 1600PX. `Bonus Buy` is body prose on the intro card and is not readable at native.
- Where fixable: `frontend/src/lib/i18n/translations.ts:1536,1554,1555` and `frontend/src/lib/i18n/prose.ts:91`.
- Proposed fix: pick one player-facing name for the purchase and sweep. PARK(sixteen locales carry every one of these values, so the sweep is not small, and the choice of name is the owner's).

## Native pass reconciliation

Against `reports/qa/stream_test/shards/superseded/STT-MOBILES-A.md`, which covered frames 312 to 337. Findings whose evidence lies wholly in 330 to 337 belong to the sibling re-run squad and are marked OUT OF RANGE without a verdict, because refusing to rule on evidence I did not look at is the point of the exercise.

- **A-01**, `SCATTERs` on frame 335: OUT OF RANGE.
- **A-02**, five spellings of the scatter symbol across the paytable: OUT OF RANGE for its paytable evidence. **CONFIRMED** for the one frame inside my range: frame `315` does read `Scatters` in title case, twice, at 1600px, and `frontend/src/lib/i18n/translations.ts:1540,1542` holds both.
- **A-03**, ways-to-win strip slicing `1` and `5`, frames 330 to 333: OUT OF RANGE.
- **A-04**, centred paytable bullets, frames 335 and 336 against 315: OUT OF RANGE for its paytable evidence. **CONFIRMED** for the control it leans on: frame `315` at 1600px sets its bullets flush left with a hanging `›` marker and every continuation line on one left edge, and `frontend/src/lib/components/IntroSplash.svelte:97` does set `text-align: left` and `:109` does set the marker `content: '›'`.
- **A-05**, win strip about 3 device pixels and unreadable: **CONFIRMED** and hardened. Carried forward as STT-MOBILES-1-03. At roughly 5x the linear resolution the money token still does not resolve into digits, which removes capture resolution as an explanation. Added at full resolution: the glyph bottoms are also cut by the plate.
- **A-06**, `1 ways`: **CONFIRMED**. Carried forward and widened as STT-MOBILES-1-02, which adds the raw symbol id and the ASCII `x` now that both are readable off the frame.
- **A-07**, HUD menu case mixing, MEDIUM: **CONFIRMED and REFINED**. Carried forward as STT-MOBILES-1-01. Refinement: the native pass saw two conventions, and at full resolution there are three, because `MUSIC` and `SOUND` are caps at `0.5rem` with `letter-spacing: 0.12em` (`HudOverlay.svelte:1696-1703`) while the rows above are `0.8rem` untracked. Severity MEDIUM to HIGH.
- **A-08**, bet mode names on frame 337: OUT OF RANGE. Its source citations are used in my 1-09 and are credited to it.
- **A-09**, payout decimal precision on frame 334: OUT OF RANGE.
- **A-10**, modal title and button casing drift: **CONFIRMED and REFINED** for the four frames inside my range (`313`, `315`, `317`, `329`); the paytable titles it also cites are OUT OF RANGE and are not verified here. Carried forward as STT-MOBILES-1-04. Refinement: the shard led with the title comparison and left the session-panel title's fix site as UNKNOWN. Both are now located (`SessionPanel.svelte:100,194`, `translations.ts:246`), and the sharper statement of the defect is the button pair, whose two strings sit on adjacent lines `translations.ts:1556` and `:1557`. Severity MEDIUM to HIGH.
- **A-11**, serial comma disagreement between frames 315 and 335: **CONFIRMED** for frame `315`, which reads `3, 4 or 5 Scatters award 8, 12 or 16 free spins` with no serial comma at 1600px and matches `translations.ts:1540` exactly. The other half of the comparison is a sibling squad's frame, so the finding as a whole cannot be closed from my range and is not carried forward here.
- **A-12**, `WIN!` callout untracked, LOW: **CONFIRMED and REFINED**. Carried forward as STT-MOBILES-1-06. Refinement: at 1600px the weight difference against the HUD `WIN` is as large as the tracking difference, and the exclamation mark resolves. Severity LOW to MEDIUM. The native pass's refusal to make a family claim from the glow-bloomed callout is upheld and repeated.

**Nothing in the native shard was REFUTED inside my frame range.** Every claim it made about frames 312 to 329 survives at full resolution.

**One of its signed ABSENCES does not survive, and that matters as much as a refuted finding.** Its absences section states *Numeral width shimmy outside `.fs-num`: none observed*, resting on four still frames of a settled balance. Four stills of a static readout cannot support a claim about an animated one, and the animated one is the HUD WIN pod, which counts up on every win. My STT-MOBILES-1-08 is that claim re-opened from source. **A second of its absences is wrong on the facts**: it states *two faces are in use, a squarish display face ... and a rounded techno face for body prose*. There is one face. `frontend/src/app.css:97-99` declares `--fs-font-display: 'Orbitron', system-ui, sans-serif` and applies it at the root, and `:98` sets the numeric token to the same family; what reads as a second face is Orbitron's own lowercase. I made the identical misreading from the frames and it was source, not pixels, that corrected it.

**STT-MOBILES-B.md** (frames 338 to 363) carries no finding citing a frame in 312 to 329, so there is nothing in it for me to reconcile. Its B-08 covers the same win-strip class as my 1-02 and 1-03 from its own frames.

## Explicit absences, signed

Each is a claim I am signing, with what was actually checked to be able to say it.

- **Money display fit failures (TR-115 / TR-086): none in this range.** Every money string in the eighteen frames was measured against its container at 1600px: `$50,000.00` in the balance pod (frames `314`, `316` to `328`), `$0.00`, `$3.90`, `$15.95` and `$16.20` in the win pod, `$1.00` in the bet pod (all in-play frames), `$5.00`, `$20.10` and `+$15.10` in the session panel (frame `329`). None is clipped, ellipsised or overflowing. The widest string this session produced is `$50,000.00`, so the class is unexercised at this viewport rather than proven absent from the product.
- **Non-money string clipping: one instance only**, the win-strip glyph bottoms at 1-03. Every other string terminates cleanly: the four intro-card bullets, `TAP TO CONTINUE`, the six HUD labels, the five menu rows, the five session-panel row labels, the banner tier label and unit. No ellipsis appears anywhere in the eighteen frames.
- **Curly and straight quote mixing: not observable, not merely absent.** No apostrophe and no quotation mark of any kind appears in any of the eighteen frames, so the class cannot be exercised here. The only punctuation in player-visible prose is the full stop, comma, colon, plus sign and the `›` bullet marker.
- **Em dashes and en dashes in player-visible prose: none.** Checked splash (`312`, `313`), intro rules card (`314`, `315`), HUD in every state (`316` to `328`), big-win banner (`324` to `326`), win strip (`325` to `328`), HUD menu (`327`, `328`) and session panel (`329`). No horizontal stroke of any length appears in any string. The only near-miss is the `+` in `+1×`, `+5` and `+$15.10`, a plus in every case.
- **Double spaces: none detected.** Word gaps compared at 1600px across the intro card's four bullets, the splash line, the five menu rows and the five session-panel labels. All uniform.
- **A font visibly not the brand face: none found, and my first reading of the frames was wrong.** From pixels alone I recorded two families, a squared display face and a rounded body face. Source refutes it: `frontend/src/app.css:97-99` declares one family, `'Orbitron', system-ui, sans-serif`, applied at the root, with `--fs-font-numeric` at `:98` resolving to the same face. What reads as a second family is Orbitron's lowercase against its caps. No system default was seen leaking anywhere, with the one honest exception at 1-06, where glow bloom means no family claim can be made in either direction.
- **The `×` in body copy is NOT a fallback.** I logged it during the frame pass as a suspected fallback because it sets at roughly half the cap height of the digits beside it and at a thinner stroke. Source resolves it against the suspicion: the sign is U+00D7 in `frontend/src/lib/i18n/translations.ts:1540,1541,1554,1555`, the surface renders in Orbitron, and Orbitron carries U+00D7 (recorded independently at `frontend/src/lib/components/MaxWinCelebration.svelte:151-155` and in ledger MID-02). The small light `×` is how the face draws the glyph. **Recorded as a refuted suspicion rather than dropped silently**, because a re-run that only adds findings is not doing half its job.
- **The `›` bullet marker is deliberate, not an escaped character.** `frontend/src/lib/components/IntroSplash.svelte:109` sets `content: '›'` on the list marker. Not a finding.
- **Decimal and currency format disagreement: none beyond 1-05's spacing.** Every money figure in the range is two decimal places with a full stop separator and a comma thousands group (`$50,000.00`). The percentages in the menu (`50%`, `80%`) are integers with no space before the sign. The session timer reads `00:00:21`, zero padded. The session panel's five values right-align cleanly to one edge.
- **Localisation casing: not testable.** All eighteen frames are `lang: en`, so TR-104 and the Q-16 park cannot be exercised beyond the visibility record below.
- **What I did NOT check, named so it is not mistaken for swept.** I did not open `frontend/src/lib/components/WinCelebration.svelte`, `frontend/src/lib/i18n/prose.ts` or `frontend/src/App.svelte`; the citations to those three in 1-03, 1-06 and 1-09 are carried from the superseded shard and are credited to it, not re-verified. I did not run any script or gate. I did not compare against the committed native PNGs pixel for pixel; the native-resolution statements in the resolution notes are reasoned from the recorded image-token budget in `LEDGER.md`, not remeasured.
- **Out of lens, recorded because it is stream-visible and I do not know which squad holds it.** The balance pod reads `$50,000.00` unchanged on every frame from `314` to `328`, while frame `329` reports `Spins 5`, `Total wagered $5.00`, `Total won $20.10` and `Net result +$15.10`. A balance that never moves across five wagered spins is not a typography finding and is not claimed as one. Noted so it is not lost.

## KNOWN matches

- **KNOWN(MID-01)**: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/324_mobile-s_transition_bigwin_countup_early.png`, the banner reads `$10.28` while the HUD WIN pod already reads `$15.95`, on a win that settles at `$16.20` on `326_mobile-s_bigwin_settled.png`. Fresh evidence that the desktop `013` / `015` pattern reproduces at `mobile-s`, now readable to the cent rather than inferred.
- **KNOWN(MID-02)**: `324_mobile-s_transition_bigwin_countup_early.png`, `325_mobile-s_transition_bigwin_countup_late.png` and `326_mobile-s_bigwin_settled.png` all render the banner unit as `16x BET`, the mark set at x height and lighter than the adjacent caps, consistent with the ASCII `x` the ledger derives from `WinBanner.svelte:205`.
- **KNOWN(Q-26)**: `frontend/src/lib/components/WinBreakdown.svelte:93` renders `x{current.kind}` with an ASCII `x`, and it is frame-evidenced on `325`, `326`, `327` and `328`. Q-26's enumeration names four instances in `fsModes.ts`; this is outside that file, as is MID-02's. Reported in full as STT-MOBILES-1-02 because the raw symbol id beside it is not part of Q-26.
- **KNOWN(Q-16 park)**: `327_mobile-s_transition_menu_opening.png` and `328_mobile-s_hud_menu.png` show the parked hardcoded strings `Session` (`HudOverlay.svelte:429`) and `Mute` (`:432`) on a stream-visible surface, and `329_mobile-s_session_panel.png` adds `Session information`, `Time played`, `Spins`, `Total wagered`, `Total won` and `Net result`. This is an `en` session so the park's urgency is unchanged by it; recorded so the de and ar squads have a matching English reference for which parked strings actually reach a frame.
- **KNOWN(TR-089)**: the banner count-up on `324` to `326` is the fixed per-digit mechanism and is not reported as shimmy. The currency-symbol spacing at 1-05 is reported separately and deliberately, because it is spacing and not shimmy, and it is present on the settled frame too. The row's closing sentence, that any OTHER numeric surface which shimmies is a new finding, is what STT-MOBILES-1-08 answers.

tree_after: `git status --porcelain`, verbatim below. All 24 lines are `??`, untracked. **Nothing shows as MODIFIED or DELETED, so this squad did not dirty the tree.** One line is this squad's shard, `reports/qa/stream_test/shards/STT-MOBILES-1.md`; the other 23 are sibling squads' shards and are not mine. `.evidence-scratch/` does not appear because it is ignored, and `reports/screens/` does not appear, which is the check that the write-once evidence directory was not touched.

```
?? reports/qa/stream_test/shards/STC-MOBILEL-1.md
?? reports/qa/stream_test/shards/STC-MOBILEL-2.md
?? reports/qa/stream_test/shards/STC-MOBILEL-3.md
?? reports/qa/stream_test/shards/STC-MOBILEM-1.md
?? reports/qa/stream_test/shards/STC-MOBILEM-2.md
?? reports/qa/stream_test/shards/STC-MOBILEM-3.md
?? reports/qa/stream_test/shards/STC-MOBILES-2.md
?? reports/qa/stream_test/shards/STC-POPOUTS-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-2.md
?? reports/qa/stream_test/shards/STC-POPOUTS-3.md
?? reports/qa/stream_test/shards/STM-MOBILEL.md
?? reports/qa/stream_test/shards/STM-MOBILEM.md
?? reports/qa/stream_test/shards/STM-POPOUTS.md
?? reports/qa/stream_test/shards/STT-MOBILEL-1.md
?? reports/qa/stream_test/shards/STT-MOBILEL-2.md
?? reports/qa/stream_test/shards/STT-MOBILEL-3.md
?? reports/qa/stream_test/shards/STT-MOBILEM-1.md
?? reports/qa/stream_test/shards/STT-MOBILEM-2.md
?? reports/qa/stream_test/shards/STT-MOBILEM-3.md
?? reports/qa/stream_test/shards/STT-MOBILES-1.md
?? reports/qa/stream_test/shards/STT-MOBILES-2.md
?? reports/qa/stream_test/shards/STT-POPOUTS-1.md
?? reports/qa/stream_test/shards/STT-POPOUTS-2.md
?? reports/qa/stream_test/shards/STT-POPOUTS-3.md
```
