# STC-DESKTOP-B, composition lens (desktop, 1200x675, frames 027 to 052)

scope: The desktop (en) session frames 027 to 052 inclusive, 26 frames, opened once each with the Read tool. Surfaces covered: paytable sections 07 to 09 and the paytable close transition, the autoplay menu, the FEATURES menu and its opening transition, both buy confirm dialogs and their opening transitions, the feature entry card and its fade and start transitions, the six feature-run interval frames, the feature exit transition, post-feature base, the max win overlay fade, the held max win celebration, the collect fade, and post-collect base. Judged as compositions and as a sequence. Pixel figures were measured on the committed PNGs with Pillow through the repository's own `env/` interpreter; no project script was run and no frame was re-opened after its single read.

frames_read: 26

PROVENANCE, because it changes how this file should be read. This squad ran twice against the same 26 frames. Run 1 wrote a complete fourteen-finding shard with sources located. Run 2 read all 26 frames independently before opening this file, reached the same fourteen (bar the two it could not have reached without run 1's measurements), and found five that run 1 did not. **Nothing has been discarded.** Every run 1 finding is kept with its measurements and its located sources; run 2's five are merged in at their severity positions, each carrying its own measurement. Findings are renumbered into strict severity order and every internal cross-reference has been renumbered with them. Two places where the runs disagreed are recorded rather than smoothed over:

- The clipped wordmark (finding 02). Run 2 judged it STREAM by eye. Run 1 measured the loss at three pixels. The measurement governs and it is filed HIGH.
- The max win unit glyph. Run 2 read the frame and was about to file KNOWN(MID-02). Run 1 derived from source that this component already uses the multiplication sign. The derivation governs, per convention (l.1), and the note at the foot of the KNOWN list stands. At the raster size involved the two glyphs are not separable by eye, which is the whole reason (l.1) exists.

RUN 3, appended without disturbing anything above it. A third pass read all 26 frames and measured them before opening this file, using an independent decoder (pure `zlib`/`struct` PNG decode rather than Pillow) and its own thresholds, so its agreement is worth something under convention (l.4): the two sides share the PNGs and share nothing else. What it adds:

- **Corroborated to the pixel, independently**: the pods' right border at `x=1182` on 039; the `×` and `BET` glyph boxes 13 px apart on 050; the paytable modal's four borders at `y=5`, `y 623..625`, `x 48..54`, `x 1150..1152`; the FEATURES pill's label run stopping at `x=961` on 031 where 030 continues to `x=1020`; and the 23 px stage displacement, seen on a different element again, the cabinet's metal frame moving from `y 64..78` to `y 87..101` down column `x=600` while its horizontal extent holds at `x 307..321` and `x 878..893`.
- **One new finding, number 20**, the interface guide's `Turbo` row. It is a MEDIUM sitting after the LOWs, which breaks this file's strict severity order deliberately: renumbering again would invalidate the cross-references the earlier merge has just finished repairing, and stable ids are worth more to the marshal than a tidy sequence. It also corrects a signed absence, which is noted in place on that absence rather than only here.
- **One severity dissent, recorded and not acted on**: run 3 would file the stage displacement (the 23 px jump between the two settled base frames) at STREAM rather than MEDIUM, because frame 051 sits in neither position (left metal at `x 302..317`), so the move is animated across the collect fade and an audience watches the whole scene slide at the moment the celebration clears. The finding's text and evidence are untouched; the marshal has both readings.
- **One observation left unnumbered on purpose**: on 050 the base HUD pods, the bet arrows and the spin button stay legible through the celebration overlay. It is not written as a finding by run 3 because the sampled luminance there is dominated by the overlay's own gradient and run 3 could not separate ghosting from vignette by measurement. Run 1's finding on the same surface covers it from the other side.
- **The base-game control cluster, figures for the park below.** The absence list parks it for want of numbers, so here are run 3's: on frame 048, row `y=544`, the cluster runs from the turbo circle at `x=213` to the autoplay circle at `x=1086`, centre `x=649.5`, against the cabinet's `x 307..893`, centre `x=600`, and clearances of `213 px` left against `114 px` right. The park stands, because a number does not settle whether the weighting is deliberate; but the park now has its measurement attached and the owner can rule on it.

## STC-DESKTOP-B-01 STREAM The autoplay panel opens on top of the FEATURES button and cuts the label to `FE`

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/031_desktop_autoplay_menu.png (compared against /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/030_desktop_transition_paytable_closing.png for the unoccluded button)
- Claim: The autoplay panel's left border is at **x=961** (luminance step `15` to `68` at x=961 on row 300, frame 031). The FEATURES pill's right edge is at **x=1039** (last pixel above threshold on rows 210 and 221, frame 030). The panel therefore covers **78 px** of the pill: on row 221 the pill's bright run ends at x=961 in frame 031 against x=1039 in frame 030, and the label renders as `FE`. Two live controls hold the same pixels, with the button neither hidden, dimmed nor moved out from under the panel, so the frame shows a half-eaten button carrying half a word. Autoplay is opened constantly on stream and this is what a viewer sees for as long as it is open.
- Where fixable: frontend/src/lib/components/HudOverlay.svelte:1750-1761 is the positioning wrapper the panel anchors to (`.autoplay-wrapper`, with `.autoplay-wrapper .fs-auto { position: static; left: auto; top: auto; }` at :1761); the panel's own rule was not pinned down inside this file's four layout branches, so that half is UNKNOWN. Not locked.
- Proposed fix: give the right rail one stacking rule at this viewport, either hiding the FEATURES pill while the autoplay panel is open or anchoring the panel to start below the pill's bottom edge.

## STC-DESKTOP-B-02 HIGH The `FUTURE SPINNER` wordmark is clipped by three pixels at the top of the viewport in the standard layout

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/030_desktop_transition_paytable_closing.png, 031_desktop_autoplay_menu.png, 038_desktop_transition_feature_entry_fade.png, 039_desktop_feature_entry_card.png, 040_desktop_transition_feature_starting.png, 041_desktop_feature_run_1.png, 042_desktop_feature_run_2.png, 043_desktop_feature_run_3.png, 044_desktop_feature_run_4.png, 045_desktop_feature_run_5.png, 046_desktop_feature_run_6.png, 047_desktop_transition_feature_exit.png, 048_desktop_post_feature_base.png (and dimmed under the dialog scrim on 034 to 037). Counter-example, uncut: 052_desktop_post_collect_base.png
- Claim: Proved by profile match rather than by eye. Counting pixels above luminance 190 per row in the window `x 470..745`, frame 048 gives rows 0 to 8 as `44, 47, 52, 67, 80, 80, 74, 75, 83` and frame 052 gives rows 23 to 31 as `44, 46, 52, 67, 80, 80, 74, 75, 83`. That is the same wordmark. Frame 052 additionally renders rows **20, 21 and 22** with counts `96, 59, 47`, and frame 048 does not have them; row 20's count of 96 is the flat cap line, the top edge of the mark. So in the standard base and feature layout the wordmark starts at row 0 with its top three rows outside the 675 px viewport, while the identical element on frame 052 starts at y=20 with twenty clear rows above it. The mark spans `x 487..711` in both. Zero top clearance on the studio's own logo, in most of the frames a stream would show, and the first thing an overlay crop reaches.
- Where fixable: PARTIAL. The intent is `frontend/src/App.svelte:811` (`const LOGO_TOP_Y = 18`) and `frontend/src/App.svelte:2459-2462` (`.logo-box { position: absolute; left: 450px; top: 18px; width: 380px; height: 60px; }`) on the 1280x720 design surface at `frontend/src/App.svelte:2196-2201` (`.game-wrapper { width: 1280px; height: 720px; transform: scale(var(--S, 1)); transform-origin: center center; }`). `computeS()` at `frontend/src/App.svelte:754-757` returns `Math.min(1200/1280, 675/720)` = **0.9375** at this viewport, which places the logo box at y=16.9 and matches frame 052, not frame 048. The displacement that puts it at y=0 is therefore NOT in these three sites and its source is UNKNOWN. Not locked.
- Proposed fix: find and remove the extra vertical displacement before touching `LOGO_TOP_Y`; changing the constant would paper over a stage offset that is also moving everything else (see finding 11).

## STC-DESKTOP-B-03 HIGH The FEATURES menu slices the Buy Overdrive card in half and never shows NITRO OVERDRIVE, with no scroll affordance drawn

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/033_desktop_features_menu.png, 032_desktop_transition_features_menu_opening.png
- Claim: The Buy Overdrive card's top border is at y=484 (luminance step `14` to `229` on column x=400) and its left and right borders stop at **y=544** (x=359) and **y=546** (x=840). The card is cut about 61 px into its own height, immediately under the pink `ACTIVATE` button, and the fifth mode `NITRO OVERDRIVE` is entirely off-screen. The manifest's own note for this frame is `FEATURES menu, all five modes and their prices`; four are on screen and one of those four is sliced. Nothing marks the cut as scrollable: over rows 200 to 560 the pixel standard deviation at columns x=848 to x=859, the strip just inside the content area's right edge, is **2** at every column, so no scrollbar, thumb or fade is rendered. A hard slice through a card with no affordance reads as a broken panel, not as a scroll region. The panel also ends at about y=595 in a 675 px viewport, so roughly 80 px of empty stage sits below a panel whose own content is being cut.
- Where fixable: frontend/src/lib/components/FeatureMenu.svelte:706-713 (the panel's `max-height: 88%` / `88dvh` cap) and frontend/src/lib/components/FeatureMenu.svelte:874-880 (`.fm-cards { flex: 1 1 auto; min-height: 0; overflow-y: auto; padding: 14px 20px; ... scrollbar-width: thin; }`). Not locked. The comment at :868-873 records that this list was made to shrink and scroll under OWNER AUDIT REMEDIATION A2; what it was not given is a visible edge.
- Proposed fix: add a bottom fade or a persistent scrollbar thumb to `.fm-cards` so the cut reads as scroll, and check the card rhythm at 675 px of height so a card cannot be cut mid-body.

## STC-DESKTOP-B-04 HIGH The bet stepper's minus and plus do not flank the value: 171 px of gap on one side, 17 px on the other

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/033_desktop_features_menu.png, 032_desktop_transition_features_menu_opening.png
- Claim: On frame 033 the row reads `SPIN COST $1.00  BET`, then the minus control, then a void, then the value and the plus control. Measured on row 124 (the button boxes) and row 137 (the glyph runs): the minus button box spans **x 526..557**, the value `$1.00` spans **x 728..771**, and the plus button box spans **x 788..819**. The clear gap between the minus button and the value it decrements is **171 px**; between the value and the plus button it is **17 px**, a ratio of ten to one. A plus and a minus either side of a figure is the one arrangement in this row whose symmetry is not a matter of taste, and the minus has been left against the `BET` label at the far end.
- Where fixable: frontend/src/lib/components/FeatureMenu.svelte:334-344 is the markup order (`fm-spin-cost`, then `fm-betlabel`, then the decrement button, then `fm-betval`, then the increment button, all direct siblings) and frontend/src/lib/components/FeatureMenu.svelte:847 is the row (`.fm-betbar > .fs-face { flex-direction: row; align-items: center; gap: 0.7rem; padding: 8px 16px; flex-wrap: wrap; }`). The rule that opens the 171 px void is inherited from the shared `.fs-face` plate and I did not locate it, so that half is UNKNOWN. Not locked.
- Proposed fix: wrap the minus, value and plus in one stepper element so they cannot be separated, and let the spin-cost text take the free space instead.

## STC-DESKTOP-B-05 HIGH The `+16 FREE SPINS` headline is drawn across the bottom of the Overdrive gauge

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/038_desktop_transition_feature_entry_fade.png, 039_desktop_feature_entry_card.png, 040_desktop_transition_feature_starting.png, 041_desktop_feature_run_1.png, 042_desktop_feature_run_2.png, 043_desktop_feature_run_3.png, 044_desktop_feature_run_4.png, 045_desktop_feature_run_5.png, 046_desktop_feature_run_6.png, 052_desktop_post_collect_base.png (there as `+8 FREE SPINS`)
- Claim: Measured on frame 039 in the column band `x 555..645`. Silver gauge-rim pixels (`|R-B| < 30` and `R > 150`) continue down to **y=345**, while the first yellow headline pixels (`R > 190`, `G > 140`, `B < 110`) appear at **y=317**. The two overlap over **28 rows**. The gauge is centred near (600, 281) with a radius of about 66, so its lowest point is y=347 and the headline's cap tops sit 30 px above it. This is the feature entry moment, the most replayed frame in any stream of this game, and the award figure is sitting on the dial rather than under it.
- Where fixable: frontend/src/lib/components/FreeSpinsPresentation.svelte:649-652 (`.entry-bottom-group { position: absolute; left: 0; right: 0; bottom: 2%; display: flex; flex-direction: column; align-items: center; gap: 14px; }`) against frontend/src/lib/components/FreeSpinsPresentation.svelte:581-584 (`.entry-gauge-wrap { position: relative; width: 240px; height: 240px; ... }`). Not locked. Worth recording for the marshal: the comment at :645-648 states that the flex column means the award text and the gate button *"can never overlap regardless of the button's own height or the viewport's aspect ratio, unlike two independent bottom:N% elements"*. That is true of those two elements and it is exactly why the defect survived: the gauge is not in that flex column, it is an in-flow sibling, and the absolutely positioned group rides up into it at a short viewport. A fix that closed a class for two of three elements was recorded as closing the class.
- Proposed fix: bring the gauge into the same flex column so one gap rule governs all three, rather than anchoring the lower group to the card's bottom edge.

## STC-DESKTOP-B-06 HIGH The backdrop changes focus between two modals one click apart: the FEATURES panel blurs the scene, the buy dialog it opens does not

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/033_desktop_features_menu.png and 032_desktop_transition_features_menu_opening.png (backdrop soft) against /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/034_desktop_transition_dialog_buy_overdrive_opening.png, 035_desktop_dialog_buy_overdrive.png, 036_desktop_transition_dialog_nitro_overdrive_opening.png, 037_desktop_dialog_nitro_overdrive.png (backdrop sharp)
- Claim: Pressing `ACTIVATE` on the FEATURES panel swaps one modal for another and the scene behind snaps from blurred to sharp in that one step. Measured as mean absolute adjacent-pixel luminance difference over the identical backdrop patch `x 60..300, y 180..460`, which holds the robot and the car on every one of these frames, and normalised by the patch's mean luminance so that dimming cannot be mistaken for blur:

  | Frame | gradient | mean luminance | gradient / mean |
  |---|---|---|---|
  | 048, no modal, undimmed | 3.595 | 36.82 | **0.0976** |
  | 035, buy dialog | 1.447 | 14.09 | **0.1027** |
  | 033, FEATURES panel | 0.243 | 6.57 | **0.0370** |

  The buy dialog's backdrop retains the same relative detail as the undimmed scene, within 5 per cent, so it is dimmed and **not** blurred. The FEATURES panel's is down by a factor of **2.8**, so it is dimmed and blurred. Both carry a scrim, so a scrim-coverage check passes on both and cannot see this. Compositionally the sharp one is the worse of the two: the dialog is only about 433 px wide, so the live HUD survives at full contrast on both sides of it, with the hamburger button sliced vertically by the dialog's left border and the `BET` `$1.00` pod by its right.
- Where fixable: UNKNOWN. Not locked. The FEATURES panel side is `frontend/src/lib/components/FeatureMenu.svelte`, the dialog side is `frontend/src/lib/components/BuyBonus.svelte`, and the two overlay backdrops are declared separately; I did not open the backdrop rules in either file, so the exact pair of declarations is not cited and no line number is claimed.
- Proposed fix: apply one backdrop treatment, dim plus blur, to every modal layer, so the scene cannot change focus between two frames of one flow.

## STC-DESKTOP-B-07 HIGH The paytable modal has 5 px above it and 50 px below it, against 48 px on both sides

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/027_desktop_paytable_07_interface_guide.png, 028_desktop_paytable_08_responsible_play.png, 029_desktop_paytable_09_disclaimer.png
- Claim: Measured on frame 027. The modal's top border is at **y=5** (luminance step `26` to `180` on column x=600) and its bottom border at **y=623 to 625**; the left border is at **x=48 to 54** and the right at **x=1150 to 1152**. Horizontal margins are `48` and `48`, matched to the pixel; vertical margins are `5` and `50`, a factor of ten apart. The panel is jammed against the top of the frame with a 50 px band of scene left under it. The symmetry was clearly intended on one axis and lost on the other.
- Where fixable: PARTIAL. The panel's own size is correct and is at frontend/src/lib/components/PaytableModal.svelte:544-551 (`.fs-pt-panel { width: 92%; max-width: 1178px; height: 92%; max-height: 662px; }`): 92 per cent of 1200 is 1104, which is exactly the measured `1152 - 48`, and 92 per cent of 675 is 621, which is exactly the measured `625 - 5`. So the size is right and the CENTRING is what is wrong. The centring container was not located, so that half is UNKNOWN. Not locked.
- Proposed fix: fix the overlay's vertical centring rather than the panel's height; at 92 per cent the correct inset is 27 px top and bottom.

## STC-DESKTOP-B-08 HIGH On the max win frame the unit is set at two sizes on two baselines with no optical space

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/050_desktop_maxwin_celebration.png, 049_desktop_transition_maxwin_overlay_fade.png
- Claim: Measured glyph boxes on frame 050. The numeral `5,000` occupies `y 356..429, x 400..710`. The multiplication sign occupies `y 389..406, x 721..737`, height **18 px**. `BET` occupies `y 405..419, x 746..795`, height **15 px**. The sign and `BET` therefore sit on different baselines, **13 px** apart at the foot, and are separated from the numeral and from each other by only `11 px` and `9 px` against a 78 px numeral, so the line reads as one jammed string in three mismatched sizes. This is the cap-win frame, the one image that leaves a stream as a clip.
- Where fixable: frontend/src/lib/components/MaxWinCelebration.svelte:155 (`<span class="c1-max-mult fs-num">5,000</span><span class="c1-max-x">×</span>`) with the `BET` span at :156-158; the sizes live in the `.c1-max-mult`, `.c1-max-x` and BET rules, which I did not open. A note at MaxWinCelebration.svelte:362 already records that the *"5,000x" numeral were fixed desktop sizes with no narrow-viewport rule*, so this surface is known to be size-brittle. Not locked.
- Proposed fix: set the sign and the unit as one run at one size on the numeral's baseline with a single word space before it.

## STC-DESKTOP-B-09 MEDIUM The buy dialog's bottom padding collapses from 22 px to 6 px between two dialogs one click apart

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/035_desktop_dialog_buy_overdrive.png against 037_desktop_dialog_nitro_overdrive.png (same pair in the transition frames 034 and 036)
- Claim: Measured on column x=700, which runs through the `BUY` button. In frame 035 the button's bottom edge is at **y=578**; in frame 037 it is at **y=594**. The dialog's own outer boundary is at **y=599 to 600** in both. So the visible clear space under the action row falls from **22 px** to **6 px** between the two dialogs, while the top is unchanged (boundary at y=31 and first content at y=65 in both). The NITRO dialog is the same component with one more paragraph of blurb, and the space it was designed with has been eaten by the extra text instead of the dialog growing or the text scrolling. Buttons all but touching their own frame read as overflow.
- Where fixable: frontend/src/lib/components/BuyBonus.svelte:174-175 (`.buy-modal { width: min(94vw, 460px); max-height: 90dvh; overflow-y: auto; padding: 22px 24px 24px; ... }`) and frontend/src/lib/components/BuyBonus.svelte:226-233, where the stat strip is `position: sticky; bottom: 0; z-index: 2`, so once the body overflows the strip pins to the bottom of the scroll box and crowds the action row beneath it. Not locked.
- Proposed fix: give the action row its own reserved band outside the scrolling body, so the blurb scrolls and the buttons keep their 24 px whatever the mode.

## STC-DESKTOP-B-10 MEDIUM `5,000x base bet` wraps inside the MAX WIN stat cell, orphaning `bet` and knocking the three stat values off a shared line

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/034_desktop_transition_dialog_buy_overdrive_opening.png, 035_desktop_dialog_buy_overdrive.png, 036_desktop_transition_dialog_nitro_overdrive_opening.png, 037_desktop_dialog_nitro_overdrive.png
- Claim: The three-column stat strip renders `PRICE` `$100.00`, `RTP` `96.35%` and `MAX WIN` `5,000x base` with the word `bet` alone on a second line. On frame 035 the strip runs `y 447..522` on column x=700 and the value text band is `y 477..487`, so `$100.00` and `96.35%` sit on the first line with about 25 px of empty cell under them while the third column fills the space. A three-up stat row whose values do not share a line, with visible dead space in two of three cells. Possibly adjacent to the TR-115 / TR-086 fit class, but the value here is a static label rather than a money figure, so it is filed on its own rather than as a KNOWN.
- Where fixable: frontend/src/lib/components/BuyBonus.svelte:135 (`<span class="buy-stat-val">{maxWinVsBaseBetLabel($isSocial)}</span>`), with the equal-thirds cell at frontend/src/lib/components/BuyBonus.svelte:235-237 (`.buy-stat { flex: 1; display: flex; flex-direction: column; align-items: center; gap: 3px; padding: 0 6px; ... }`) and the value size at :238. Not locked.
- Proposed fix: centre the row's cells against each other (`align-items: center` on the strip) so a wrap in one cell cannot leave the other two hanging, and consider shortening the label to `5,000x base`.

## STC-DESKTOP-B-11 MEDIUM The same feature entry card is drawn at two different stage geometries inside one session

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/039_desktop_feature_entry_card.png against 052_desktop_post_collect_base.png (both captured as settled `state` frames), with the same jump visible across the adjacent pair 048_desktop_post_feature_base.png and 051_desktop_transition_maxwin_collect_fade.png
- Claim: The right-hand feature HUD pods sit at rows **322 / 390 / 458** on frame 039 and at rows **345 / 412 / 480** on frame 052, 23 px lower. The control bar's top edge is at **y=488** on 039 and **y=482** on 052, 6 px higher. The wordmark's first rendered row is **y=0** on 039 and **y=20** on 052 (finding 02). The stage is about 29 px shorter on 052 while the same surface is shown, so the same HUD stack lands in two different places. A viewer who triggers the feature twice in one session watches the whole scene resize between the two entries. The two frames also bracket the difference in finding 02, so the same displacement is behind both.
- Where fixable: PARTIAL. The stage is `frontend/src/App.svelte:2196-2201` (`.game-wrapper { width: 1280px; height: 720px; transform: scale(var(--S, 1)); transform-origin: center center; }`) scaled by `computeS()` at `frontend/src/App.svelte:754-757`, which is a pure `Math.min(vw/1280, vh/720)` and returns the same 0.9375 for both frames, so it cannot by itself produce two geometries at one viewport. The extra transform is UNKNOWN. Not locked.
- Proposed fix: PARK(the displacement has to be found before it can be fixed, and it is shared with finding 02; one investigation closes both).

## STC-DESKTOP-B-12 MEDIUM The held max win celebration leaves the HUD legible behind it, with the hint line sitting on the ghosted pod row

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/050_desktop_maxwin_celebration.png (the held state, per the manifest note `MAX WIN celebration, the 5,000x cap, held`), 049_desktop_transition_maxwin_overlay_fade.png
- Claim: At the held state, not mid-fade, the base layout is still resolvable through the overlay. Measured on frame 050 in the HUD band `y 545..600`: local pixel standard deviation is **10.19** across `x 420..900`, where the three readout pods sit, against **2.20** across `x 200..380` on the same rows, which is clear overlay. A factor of **4.6**. The pod borders are recoverable as luminance steps at **x=420** and **x=778** on rows 545 and 555, and glyph strokes at **x=514, 525, 554 and 569** on row 575, so it is the pods and their digits producing the variance rather than a gradient. The hint line `PRESS COLLECT OR HIT ENTER TO CONTINUE` sits at about `y=519`, immediately above that row, so the game's most-clipped single frame carries a row of ghost boxes under its call to action. This is a separate defect from finding 17: that one is the lit field not reaching the frame edges, this one is the scrim not hiding the game.
- Where fixable: UNKNOWN. Not locked. The backdrop declaration at frontend/src/lib/components/MaxWinCelebration.svelte:194-198 is the vignette (see finding 17) rather than the scrim opacity, and I did not locate the scrim layer itself, so no line is claimed for it.
- Proposed fix: raise the scrim opacity for the max win layer, or blur behind it, so no part of the base layout is legible at the held state.

## STC-DESKTOP-B-13 MEDIUM The feature entry card stops short of the win ticker, so the previous spin's breakdown keeps cycling underneath it

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/038_desktop_transition_feature_entry_fade.png and 039_desktop_feature_entry_card.png (`L2  x5  1 ways  $0.80`), 041_desktop_feature_run_1.png, 042 and 043 (`SCATTER  x5  5 ways  $10.00`), 044, 045 and 046 (back to `L2  x5  1 ways  $0.80`), 052_desktop_post_collect_base.png (`M3  x3  1 ways  $0.20`)
- Claim: The entry overlay dims the board behind it but its lower bound stops above the ticker strip, which stays at full brightness and keeps cycling. The strip's text band is rows **444 to 452** on frame 039 (bright-pixel counts of 38, 24, 42, 35, 28 and 60 in `x 520..700`, against 0 on rows 433 to 443). Comparing frames 041 and 044, both held on the same gate: the entry card itself is **pixel-identical**, 0 differing pixels in the band `x 500..700, y 200..300`, while the ticker band `x 500..700, y 440..462` differs in **872 pixels**. So the card is frozen and the strip beneath it is not, showing a win from a spin the feature award has already superseded. It also takes the bottom of the card's box away, which is part of why the award headline has nowhere to go but onto the dial (finding 05).
- Where fixable: UNKNOWN. Not locked. The overlay is `frontend/src/lib/components/FreeSpinsPresentation.svelte` (its entry card rules are cited at finding 05) and the strip is `frontend/src/lib/components/WinBreakdown.svelte`; which of the two owns the bound was not established, so no line is claimed.
- Proposed fix: extend the entry overlay's cover to include the ticker strip, or clear the ticker when the feature entry gate mounts.

## STC-DESKTOP-B-14 MEDIUM The autoplay panel mixes two alignments and carries none of the chrome its sibling panels have

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/031_desktop_autoplay_menu.png
- Claim: Measured on frame 031. The panel's left border is at **x=961**, its content runs to about x=1170, so its centre is about **x=1065**. The four option rows all begin at exactly **x=977** and end ragged at 1085, 1102, 1113 and 1067, so their own centres are 1031.0, 1039.5, 1045.0 and 1022.0: left-aligned, as intended. The five spin-count values below them centre on **1064.5, 1063.5, 1068.5 and 1065.0**, which is the panel's centre: centre-aligned. Two alignment systems inside one 209 px panel, switching halfway down it. The panel is also a plain dark rectangle with no header, no title and no close control, while both other panels in this same session carry a titled header, a divider rule and a yellow left accent bar (`PAYTABLE` on frames 027 to 029, `FEATURES` on 033). Beside them it reads as a surface that never received the game's panel treatment.
- Where fixable: UNKNOWN. Not locked. The panel is rendered from `frontend/src/lib/components/HudOverlay.svelte` (the wrapper is cited at finding 01) and its rules are repeated across that file's four layout branches; I did not resolve which branch serves 1200x675, so no line is claimed.
- Proposed fix: settle one alignment for the whole panel and give it the shared panel header treatment.

## STC-DESKTOP-B-15 MEDIUM Paytable sections 08 and 09 render an identical view, so the last section jump moves nothing

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/028_desktop_paytable_08_responsible_play.png, 029_desktop_paytable_09_disclaimer.png
- Claim: The two frames differ in **876 pixels** at a summed RGB delta above 10, and every one of them lies in the box `y 308..476, x 0..47`, which is outside the modal (the modal's left border is at x=48, per finding 07). Inside the modal the two frames are identical. `RESPONSIBLE PLAY` and `DISCLAIMER` are both already visible at the foot of frame 028 with the content at its scroll limit, so selecting the ninth section cannot move the view. One of nine navigation targets is a dead step and a reviewer clicking through the paytable meets it.
- Where fixable: UNKNOWN (the section navigation in frontend/src/lib/components/PaytableModal.svelte; the specific handler was not located). Not locked.
- Proposed fix: reserve trailing space in the scroll container so every section header can reach the top of the content area, or merge the two trailing sections into one target.

## STC-DESKTOP-B-16 MEDIUM The feature exit transition holds an empty reel window as the largest region of the frame

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/051_desktop_transition_maxwin_collect_fade.png
- Claim: The reel window renders flat black with only faint ghost symbols, roughly 520 by 330 px, about 38 per cent of the frame's area, while everything around it is fully lit: the neon frame, the wordmark, the gauge, the three free-spins pods reading `OVERDRIVE FREE SPINS` `8`, `TOTAL WIN` `$2.80` and `MULTIPLIER` `1x`, and the control bar reading `$50,000.00`, `$5,000.00` and `$1.00`. The whole centre of the composition is dead while its periphery is at full brightness, which reads as a failed load rather than as a transition.
- Where fixable: UNKNOWN. Not locked.
- Proposed fix: hold the last settled board under the overlay collapse instead of clearing the reels before the incoming state is ready.

## STC-DESKTOP-B-17 LOW The lit celebration field stops about 190 px short of each side of the max win frame

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/050_desktop_maxwin_celebration.png, 049_desktop_transition_maxwin_overlay_fade.png
- Claim: Luminance sampled along row y=337, the vertical centre, on frame 050: `x=0` is `9`, `x=60` is `12`, `x=120` is `10`, `x=180` is `13`, then `x=240` is `32`, rising to `55` at `x=600`, and falling back to `12` at `x=1020`, `11` at `x=1080` and `10` at `x=1140`. The lit field spans roughly `x 200..1010` of a 1200 px frame, about 67 per cent, leaving a near-black band about 190 px wide down each edge. The same row on frame 049 still reads `43` at `x=1080`, so the boundary also moves between the fade frame and the held frame. Filed at LOW and stated carefully because the backdrop itself is a DELIBERATE vignette, `frontend/src/lib/components/MaxWinCelebration.svelte:194-198` (`background: radial-gradient(ellipse at center, rgba(20, 8, 50, 0.97) 0%, rgba(6, 4, 20, 0.99) 100%)`); the claim is only that at a wide short viewport the lit celebration reads as an ellipse floating in a dark frame rather than filling it, which is an art call rather than a defect I can rule on.
- Where fixable: frontend/src/lib/components/MaxWinCelebration.svelte:194-198, plus whichever halo and glow layers set the lit field's radius. Not locked.
- Proposed fix: PARK(size the lit field against the viewport diagonal instead of a fixed ellipse if the owner wants it to reach the edges; the current vignette is deliberate, so the direction is an art call).

## STC-DESKTOP-B-18 LOW The right-hand feature HUD stack sits 18 px from the viewport edge, against the 48 px margin used elsewhere in the same session

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/039_desktop_feature_entry_card.png, 038, 040 to 046, 051_desktop_transition_maxwin_collect_fade.png, 052_desktop_post_collect_base.png
- Claim: The pods' right border is at **x=1182** on frame 039, confirmed by colour as the pod's own magenta border (`(203, 89, 202)` at row 322, x=1182), giving a right margin of **18 px**. The paytable modal in the same session keeps **48 px** on that edge (finding 07). Three stacked pods 18 px from the frame edge is the tightest gutter in the layout and the first thing a right-hand overlay crop reaches. Recorded as LOW because it is a margin choice and not a collision: I checked the pixels at x=1196 to 1198 on the same rows and they are green scene glow, `(41, 82, 13)`, not the pod, so the stack does not touch the edge.
- Where fixable: UNKNOWN. Not locked.
- Proposed fix: match the right gutter to the 48 px the modals use, or declare 18 px the HUD gutter and apply it consistently.

## STC-DESKTOP-B-19 LOW The Overdrive dial is not centred on the pod stack it heads

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/039_desktop_feature_entry_card.png, 038, 040 to 046, 051_desktop_transition_maxwin_collect_fade.png, 052_desktop_post_collect_base.png
- Claim: On frame 039 the dial's silver rim spans **x 989..1147** on rows 175, 178 and 180, giving a centre of **x=1068.0**. The three pods below it span **x 938..1182** on their own rows 322, 390 and 458, giving a centre of **x=1060.0**. The two elements read as one right-rail column and their centres are **8 px** apart, which is visible once the eye has the pods' straight left edge to measure against. The dial shares no edge with them either: the pods align left at x=938 and the dial begins 51 px inboard of that.
- Where fixable: UNKNOWN. Not locked.
- Proposed fix: centre the dial on the pod column, or align it to the pods' left edge, so the right rail reads as one column.

## STC-DESKTOP-B-20 MEDIUM The interface guide's `Turbo` row breaks the list's shared title column by 70 px, on the two frames where that row is fully visible

- Frames: /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/028_desktop_paytable_08_responsible_play.png, /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/029_desktop_paytable_09_disclaimer.png (fully visible), and cut by the scroll fold on /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/027_desktop_paytable_07_interface_guide.png
- Claim: The interface guide is eight rows and seven of them start their title at **x=161**, measured on frame 028 as the ink start of `Menu` (`x=161`) and the ink box of `Max Bet` (`x 161..219`). The `Turbo` row starts its title at **x=231**, a **70 px** break in a column the other seven hold. The icon cell is the cause and the raster shows it directly: on row 133, the `Menu` row, the cell carries one bright run at `x 115..125`; on row 215, the `Turbo` row, it carries three, at `x 116..119`, `x 145..161` and `x 182..204`. Derived from the specification per convention (l.1) and it agrees: `.fs-guide-icon` is a fixed `width: 56px` box (`frontend/src/lib/components/PaytableModal.svelte:736-746`), and `.fs-guide-icon--set` overrides it to `width: auto; gap: 6px; padding: 0 8px` with `34px` images (`:754-759`), applied whenever an entry has more than one file (`:356`). Exactly one entry does, `Turbo`, with three (`:131`). That gives `3 × 34 + 2 × 6 + 2 × 8 = 130 px` against `56 px`, a **74 px** widening, against **70 px** measured in the title ink; the 4 px residual is the different left side bearing of `T` against `M`, so measurement confirmed the derivation rather than discovering it (l.2). A nine-item list with one item's text column kicked out is the machine tell the mandate's inspection test names: it reads as two authors.
- Where fixable: `frontend/src/lib/components/PaytableModal.svelte:754-759` (not locked). Worth recording for the marshal: the widening is DELIBERATE and its comment at `:749-753` states the reason (three speed captures, with the row `min-width: 0` on its text side so *the description reflows instead of pushing the panel wider*, verified at Popout S). The reasoning is about the panel's width and is sound; what it does not consider is the list's own left rhythm, which is a different property. A fix that solved fit and cost alignment.
- Proposed fix: give every `.fs-guide-icon` the widened cell's width so the column stays one width for all eight rows, or move the three speed swatches into the description line and leave the icon cell at 56 px.

## Explicit absences, signed

- **Horizontal centring is correct everywhere I could measure it, and I measured rather than assumed.** The paytable modal's borders are at x=48 and x=1152 (margins 48 and 48, frame 027); the FEATURES modal's are at x=338 and x=862 (margins 338 and 338, frame 033); the buy dialogs centre on x=600 to within a pixel (frames 034 to 037). No left-versus-right imbalance on any surface in my range.
- **The max win celebration's content block is optically centred and I am NOT reporting it as off-centre.** On frame 050 the headline box is `y 191..319`, the value line `y 356..429`, the `COLLECT` button `y 451..501` and the hint sits at about y=519, so the block runs roughly y=150 to y=527 for a centre of 338.5 against a viewport centre of 337.5. Horizontally the headline centres on x=601, the value line on x=597.5 and the button on x=599.5. That is deliberate work, not drift.
- **`CANCEL` and `BUY` are equal in width and equally spaced** on frames 034 to 037, and the `COLLECT` button on frame 050 is symmetric about the block's centre (box `x 493..706`). No button-row asymmetry to report.
- **The five symbol chips and the three stat columns in the buy dialogs are even.** On frames 034 to 037 the chips are equally spaced and centred on the dialog and the stat columns are within about 6 px of equal width. The only fault in that strip is the wrapped value, finding 10.
- **The interface guide rows on frame 027 share one left edge and one baseline rhythm** across all seven rows: icon box, title and body all start from the same two x positions and the row heights are even. Nothing to report on that surface. **SECOND PASS CORRECTION, and the marshal should take the finding over this absence: that is true of frame 027 and only of frame 027, because the eighth row, `Turbo`, is cut by the scroll fold there and only the top sliver of its icon cell renders. On frames 028 and 029, where the same row is fully visible, it does NOT share the left edge; it is 70 px out. See finding 20. The absence was correctly measured on the frame it names and the frame it names is the one frame in the set that hides the defect.**
- **The feature entry card's own contents are centred and I am NOT reporting their placement.** On frame 039 the title, the dial, the award headline and the `TAP TO CONTINUE` button all centre within about 2 px of x=600, and the block's vertical centre is within about 11 px of the reel window's. The faults on that card are the dial collision (finding 05) and the uncovered ticker (finding 13), not where the block sits.
- **No HUD crowding at this viewport, and nothing is cut off by the LEFT, RIGHT or BOTTOM viewport edge.** At 1200x675 the `BALANCE`, `WIN` and `BET` pods, the bet arrows, the spin button and the turbo and autoplay controls all keep clear space; nothing touches and nothing overlaps. The left scene art bleeds off the left edge by design and its silhouettes are complete where they carry meaning. The top edge is the only one with a collision and it has two, finding 02 and the cabinet lights in the next line. The three readout pods do differ in width and inter-pod gap (roughly 186, 134 and 120 px wide, with gaps of roughly 20 and 13 px), which I judged as sized-to-content rather than as a defect; recorded here so the marshal can overrule me.
- **The green cabinet lights above the reel frame are clipped by the top edge on the feature frames** (038 to 046). Recorded here rather than as its own row because it is the same stage displacement as findings 02 and 11 and will move when that moves; on frame 052, where the stage sits lower, those lights are whole.
- **The base-game control cluster's horizontal balance was checked and is NOT reported.** The lone turbo button on the left and the spin plus autoplay pair on the right put the cluster's bounding centre right of the reel stage's centre on frames 030, 031, 047 and 048. I could not separate deliberate functional weighting from drift from the frames alone, and convention (l.6) says an unsolved item is parked rather than filled in, so it is recorded here instead of raised.
- **The win breakdown strip's right edge was checked and is NOT reported.** On frames 030 and 048 the strip's dark run ends at x=842 while the reel window's inner border peaks at about x=857, and its left end is flush with reel one. Thirteen pixels of one-sided inset, and I could not separate a designed inset from a misalignment without the source, so no claim is written. Flagged for whoever holds the source: if the strip is meant to match the grid, it is wrong at one end only.
- **No large dead region reads as unfinished on the base frames.** The right-hand third between the cabinet and the viewport edge holds only the `FEATURES` pill on frames 030, 047 and 048, but the scene art (rain, hoardings, light shafts) carries it and it reads as composed negative space. The two dead regions that ARE reported are the roughly 80 px below the FEATURES panel (finding 03) and the blank reel window on 051 (finding 16), and in both of those something is being clipped or emptied at the same moment.
- **THE IN-FLIGHT FEATURE LAYOUT AT THIS VIEWPORT IS NOT SWEPT, AND NOBODY SHOULD READ THIS SHARD AS HAVING SWEPT IT.** All six frames the manifest calls `Overdrive free spins in flight, interval frame N of 6` (041 to 046) render the entry gate instead: the `OVERDRIVE FREE SPINS` card with `+16 FREE SPINS` and `TAP TO CONTINUE` is still up in every one, the `WIN` pod reads `$0.00` throughout, and the six differ from each other only in the character's idle pose, the pulsing gate button and the cycling win ticker (all six have distinct md5 hashes, so they are six genuine captures of one unchanged state; the card region itself is pixel-identical across them, per finding 13). Frames 038, 039 and 040, labelled entry fade, settled and dismissing, are the same card again, and 052, whose manifest note is `Back to base after collect, balance settled`, shows a second entry card rather than base. So the feature running, the meter climbing, and the spin-to-spin layout during free spins have **not** been judged at 1200x675 by this squad. Whether a gate persisting through six interval captures is a harness gap or a real game state is not something a frame can settle, so per convention (l.6) it is parked and named rather than guessed at; it needs a re-capture that dismisses the gate.

## KNOWN matches

- KNOWN(Q-26): /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/033_desktop_features_menu.png and 032_desktop_transition_features_menu_opening.png carry `Double-chance: about 1.6x the feature trigger rate. Debits 1.25x every spin while ON.` directly above the same card's `1.25× per spin while ON`; 037_desktop_dialog_nitro_overdrive.png carries `Buy a rich entry with the Overdrive meter pre-revved to 5x.` beside `1×, 3× or 10×` in the paragraph below it.
- KNOWN(STV-01): the singular-as-plural win ticker on /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/030_desktop_transition_paytable_closing.png (`L3  x4  1 ways  $0.20`), 039_desktop_feature_entry_card.png (`L2  x5  1 ways  $0.80`), 044, 045 and 046 (same string) and 052_desktop_post_collect_base.png (`M3  x3  1 ways  $0.20`). Six further frames of evidence.
- KNOWN(STV-04): `TAP TO CONTINUE` renders on the feature entry button on ten of my frames (038 to 046 and 052) in a mouse-driven desktop session, while 050_desktop_maxwin_celebration.png reads `PRESS COLLECT OR HIT ENTER TO CONTINUE` two surfaces later.
- KNOWN(STV-05): `5,000x base bet` renders as the MAX WIN stat on /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/035_desktop_dialog_buy_overdrive.png and 037_desktop_dialog_nitro_overdrive.png. Fresh frames for the base-bet half of STV-05's pair; finding 10 above is that string's wrap, not its wording.
- KNOWN(STV-08): /Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/033_desktop_features_menu.png shows the `SPIN MODES` section heading and the `BET MODES` footer button in one frame.
- KNOWN(STV-12): 030, 039, 041, 044 to 046, 047, 048 and 052 render the ticker's n-of-a-kind as `x3`, `x4` and `x5`, prefixed and with a letter x.
- KNOWN(Q-16 park): parked hardcoded English visible on my frames: `Stop on win`, `Single win limit`, `Stop on feature`, `Loss limit` and `SPINS` on 031_desktop_autoplay_menu.png; `PRESS COLLECT OR HIT ENTER TO CONTINUE` on 050_desktop_maxwin_celebration.png; the `INTERFACE GUIDE`, `RESPONSIBLE PLAY` and `DISCLAIMER` section headers on 027, 028 and 029.
- KNOWN(Q-07): the infinity glyph on the autoplay spin list, 031_desktop_autoplay_menu.png, allowlisted and not a finding. Noted only because finding 14's alignment measurement enumerates that list.
- **NOT a KNOWN(MID-02), and the marshal should read this rather than assume the opposite.** MID-02's frame sentence claims the surface is carried by *"every session's big-win triple plus its max-win frames"*. My max win frames, 049 and 050, are NOT instances: the max win overlay builds its unit at `frontend/src/lib/components/MaxWinCelebration.svelte:155` as `<span class="c1-max-mult fs-num">5,000</span><span class="c1-max-x">×</span>`, which is the multiplication sign U+00D7, consistent with the Q-12 change that LEDGER.md itself cites for this component at `MaxWinCelebration.svelte:151-155`. Derived from the source per convention (l.1) rather than read off the raster, and worth stating plainly that run 2 of this squad DID read it off the raster and got the opposite answer, because at 18 px the two glyphs are not separable by eye. MID-02's finding stands on the big-win banner; its stated frame COUNT of 60 needs re-deriving, because the max-win frames it counts in belong to a different component that was already fixed. Finding 08 above is the typographic setting of that same line and is independent of the glyph question.

tree_after:
```
?? reports/qa/stream_test/shards/STC-DESKTOP-A.md
?? reports/qa/stream_test/shards/STC-DESKTOP-B.md
?? reports/qa/stream_test/shards/STC-LAPTOP-A.md
?? reports/qa/stream_test/shards/STC-LAPTOP-B.md
?? reports/qa/stream_test/shards/STC-MOBILEL-A.md
?? reports/qa/stream_test/shards/STC-POPOUTL-A.md
?? reports/qa/stream_test/shards/STC-POPOUTL-B.md
?? reports/qa/stream_test/shards/STC-POPOUTS-A.md
?? reports/qa/stream_test/shards/STC-POPOUTS-B.md
```
Recorded verbatim from `git status --porcelain` run at the end of run 3's work, from the repository root. **Nothing is MODIFIED and nothing is DELETED.** All nine lines are untracked and all nine are squad shards under the expected `reports/qa/stream_test/shards/` path. `STC-DESKTOP-B.md` is this squad's; the other eight belong to other squads and are neither ours nor our problem. Note for the marshal, since it is the one thing in this run that could mislead: this squad's shard was found ALREADY PRESENT at its path, so run 3 appended to it under the read-before-write check rather than overwriting it. No other squad's file was opened or touched at any point.
