# STC-MOBILEL-3, COMPOSITION (mobile-l, 425x812, frames 243 to 259, 1600px upscaled)
supersedes: STC-MOBILEL-B.md (partially: frames 243 to 259 of its 234 to 259 range). STC-MOBILEL-A.md covers 208 to 233 and is entirely outside this shard's range.
scope: the `mobile-l` frames numbered 243 to 259 inclusive, 17 frames, from `243_mobile-l_transition_dialog_nitro_overdrive_opening.png` through `259_mobile-l_post_collect_base.png`, read from `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/`.
frames_read: 17

**Units.** Every pixel figure below is in pixels of the UPSCALED frame unless it
says otherwise. The upscaled frames are 837x1600 against a native viewport of
425x812, so the scale factor is exactly `1600 / 812 = 1.9704`: one CSS pixel is
1.9704 upscaled pixels and one upscaled pixel is 0.5075 CSS pixels. Where a
figure matters to a person rather than to a diff, the CSS equivalent is given
beside it. Measurements were taken by decoding the PNGs (`sips` to BMP, then a
pure-python reader) and sampling rows and columns, not by eye, except where the
text says otherwise. Strings are transcribed verbatim in backticks.

**Two of this shard's own first-pass claims were refuted by reading the source
and have been withdrawn rather than shipped.** They are recorded in full under
"Explicit absences, signed" rather than deleted, because a withdrawn claim is
evidence about the instrument. Five findings survive.

## STC-MOBILEL-3-01 STREAM The feature entry caption is painted across the speedometer's chrome bezel, and the fix that was applied here guaranteed the wrong gap

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/246_mobile-l_feature_entry_card.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/245_mobile-l_transition_feature_entry_fade.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/247_mobile-l_transition_feature_starting.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/248_mobile-l_feature_run_1.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/249_mobile-l_feature_run_2.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/250_mobile-l_feature_run_3.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/251_mobile-l_feature_run_4.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/252_mobile-l_feature_run_5.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/253_mobile-l_feature_run_6.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/259_mobile-l_post_collect_base.png`. Ten of my seventeen frames.
- Claim: on the Overdrive entry card the caption `+16 FREE SPINS` is drawn on top of the speedometer illustration rather than below it. Measured in `246`: the caption's yellow ink occupies the box `x=227 to 611, y=620 to 656`; the gauge's neutral chrome bezel occupies `x=305 to 533, y=443 to 671`, a circle of radius about 114 centred on `(419, 557)`. At the caption's top row `y=620` that circle spans `x=324 to 514`, and at its bottom row `y=656` it spans `x=362 to 475`, so roughly **190 of the caption's 384 px width, about 49 per cent, is set over the chrome bezel**, and the letters of `FREE SP` cross the gauge's brightest, highest-contrast band. The caption is bare glowing type with no plate of its own. `259` renders the identical collision with `+8 FREE SPINS`.

  **The mechanism, and why the existing guard does not catch it.** The dial is `.entry-gauge-wrap`, a `240px` square laid out in normal flow and centred by `.fs-entry-stage { display: flex; align-items: center; justify-content: center; }` (`frontend/src/lib/components/FreeSpinsPresentation.svelte:566`, `:581-584`). The caption is inside `.entry-bottom-group`, which is `position: absolute; left: 0; right: 0; bottom: 2%` (`frontend/src/lib/components/FreeSpinsPresentation.svelte:649-652`). The two are in different layout systems, so **nothing in the tree relates the caption's position to the dial's**: as the card gets shorter the flex-centred dial descends toward the middle while the absolutely positioned caption rises from the bottom, and at 425x812 they meet. The comment at `frontend/src/lib/components/FreeSpinsPresentation.svelte:461-466` records an OWNER AUDIT ROUND 3 fix that replaced two independent `bottom:N%` elements with this flex column so they "can never overlap regardless of viewport, by construction". That guarantee is real and it is between the caption and the `TAP TO CONTINUE` button. **The element the caption actually collides with is the dial, which was not brought into the group.**
- Resolution note: VISIBLE AT BOTH. The overlap is gross enough to read at thumbnail scale. NEW AT 1600PX are the measurement and the fact that the type crosses the bezel's brightest band rather than merely touching its edge.
- Where fixable: `frontend/src/lib/components/FreeSpinsPresentation.svelte:649-652` (`.entry-bottom-group`), against `:566` (`.fs-entry-stage`) and `:581-584` (`.entry-gauge-wrap`).
- Proposed fix: bring the dial into the same flex column as the caption and the button, so one `gap` governs all three and the Round 3 guarantee extends to the element that actually collides. Failing that, give `.entry-burst-text` its own darkened plate so it is composed against a field rather than against chrome; if the overlap is to stay, it needs to look intended.

## STC-MOBILEL-3-02 MEDIUM A 4 CSS px green band sits between the reel frame and the board for the whole of the feature

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/250_mobile-l_feature_run_3.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/259_mobile-l_post_collect_base.png`, and visible across `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/245_mobile-l_transition_feature_entry_fade.png` through `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/253_mobile-l_feature_run_6.png`
- Claim: the board's dark field does not reach the inside of the reel frame, and in feature mode the gap shows the green feature scene through it as a hairline across the full width of the board. Sampled straight down two independent columns in `250`:

  | Column | `y=276 to 280` (frame) | `y=282 to 288` (the band) | `y=290` (board) |
  |---|---|---|---|
  | `x=418` | `(224,110,101)` red | `(54,115,66)`, `(41,112,65)`, `(20,55,35)` green | `(7,10,21)` |
  | `x=120` | `(225,110,102)` red | `(54,115,74)`, `(42,112,73)`, `(20,53,38)` green | `(7,10,18)` |

  The band is about **8 px tall, 4.1 CSS px**, and its hue matches neither the red frame above it nor the near-black board below it: it is the green feature backdrop. The control frame `255` (base mode, cyan frame) sampled at the same two columns gives `(9,111,143)`, `(10,102,134)`, `(5,72,95)`, a clean falloff of the frame's own cyan. So the gap exists in both modes and is only VISIBLE in the feature, because that is the mode whose page background behind the board is bright green. That is also the mode a stream audience watches most closely.
- Resolution note: NEW AT 1600PX. 4.1 CSS px of foreign colour between two edges cannot be separated from the frame's own glow at native scale.
- Where fixable: UNKNOWN, and the elimination is recorded so the next reader does not repeat it. `frontend/src/lib/components/GameGrid.svelte:1204-1216` (`.grid-container`) is a fixed `616x412` box with `overflow: hidden` and **no background fill of its own**, only box-shadows, so it cannot be the opaque field; its `.symbol-col` children (`:1225-1232`) tile it exactly (`5 x 120px` plus `4 x 4px` gap = 616). The board's opaque field and the neon frame therefore come from the scaled canvas layer rather than this component, and pinning which one owns the 4 px was outside this run's source budget.
- Proposed fix: extend the board's background fill under the frame chrome by several pixels so the page can never show between them, rather than tuning the frame's position, which would only move the seam.

## STC-MOBILEL-3-03 MEDIUM On the max win hero the `×` and the `BET` of the unit are told to align on a baseline and then both opt out of it

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/257_mobile-l_maxwin_celebration.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/256_mobile-l_transition_maxwin_overlay_fade.png`
- Claim: the hero reads `5,000` in large numerals followed by the unit `×BET`, and the two glyph groups of that unit sit on different baselines. Sampled in `257`: the `×` glyph (columns `x=548 to 572`) occupies rows `y=833 to 852`; `BET` (columns `x=575 to 650`) occupies rows `y=843 to 862`. That is a **10 px vertical offset, 5.1 CSS px, against a `BET` cap height of 19 px**, so the two halves of one two-character unit are out of register by 53 per cent of their own cap height. The `×` is optically centred against the numerals (`5,000` occupies `y=798 to 880`) and `BET` is centred against nothing. The horizontal gap between the numerals and the `×` is 20 px (10.2 CSS px), so this is **not** the collided `5,000×EINSATZ` shape recorded as `STL-DE-B-02` on the German session; the English string keeps its space and loses its baseline instead.

  **The mechanism is visible in three consecutive declarations.** `.c1-max-multwrap` sets `display: flex; align-items: baseline` (`frontend/src/lib/components/MaxWinCelebration.svelte:295-300`), which is the correct instruction. Then `.c1-max-x` overrides it with `align-self: flex-end; padding-bottom: 0.12em` (`:309-315`) and `.c1-max-betlabel` overrides it with `align-self: flex-end; padding-bottom: 0.28em` (`:316-322`). **Both children opt out of the baseline the parent declares, and are then re-registered by hand with two different em-relative paddings at two different font sizes** (46px and 22px at full size, 24px and 13px under the narrow-viewport branch at `:367-369`, which is the branch mobile-l takes). Hand-tuned optical registration at one size does not survive a second size, which is exactly what the frame shows.
- Resolution note: NEW AT 1600PX. A 5.1 CSS px baseline offset on roughly 10 CSS px of ink is below what a 425 px wide thumbnail resolves.
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:309-322` (`.c1-max-x` and `.c1-max-betlabel`), against `:295-300` (`.c1-max-multwrap`).
- Proposed fix: delete both `align-self: flex-end` rules and both `padding-bottom` values and let the `align-items: baseline` already declared on the wrapper do the work. The register then holds at every font size and in every locale by construction instead of by tuning.

## STC-MOBILEL-3-04 MEDIUM Two identically sized feature pods, one label at 92 per cent of its content box and the other at 38 per cent, with the source's own safety valve one word from firing

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/245_mobile-l_transition_feature_entry_fade.png`, and the same row in `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/246_mobile-l_feature_entry_card.png` through `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/253_mobile-l_feature_run_6.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/258_mobile-l_transition_maxwin_collect_fade.png` and `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/259_mobile-l_post_collect_base.png`
- Claim: the feature pod row is two equal cells carrying labels of wildly unequal fit. Sampled in `245` at row `y=1041` (the cells' top border) and `y=1063` (the label row): the left cell spans `x=27 to 410` (383 px, 194.4 CSS px) and its label `OVERDRIVE FREE SPINS` spans `x=47 to 384` (337 px, 171 CSS px). The right cell is the same 383 px (`x=429 to 812`) and its label `TOTAL WIN` spans `x=549 to 694` (145 px, 73.6 CSS px). `.pm-cell` declares `padding: 6px 4px` (`frontend/src/lib/components/BonusInstrumentColumn.svelte:231-246`), so the content box is 186.4 CSS px and the long label fills **171 of it, 92 per cent, clearing its own content box by about 6.2 CSS px on the left and 9.2 on the right**, against 38 per cent for its twin.

  **The source anticipated this and its guard has not engaged.** `.pm-label` at `frontend/src/lib/components/BonusInstrumentColumn.svelte:247-259` carries the comment that the longer `OVERDRIVE FREE SPINS` label should "wrap onto a second line rather than force-nowrap/overflow; the cell has no fixed height so it grows", and sets `white-space: normal`. At 425 px it has **not** wrapped: it fits on one line with under 8 CSS px to spare across both margins combined. So the row is one word, one locale or one narrower handset away from the left cell going to two lines while the right cell stays at one, and `.pm-cell` sets `min-height: 48px` with no fixed height, so at that point the two cells stop matching in height as well as in fill. Nothing clips today, which is why this is not `TR-115` / `TR-086`; the finding is that an 8 CSS px margin is being relied on and it is on screen for the whole feature.
- Resolution note: NEW AT 1600PX. Telling a 6 CSS px pad from a clipped terminal needs the resolution to see the cell border and the label's last glyph in the same read.
- Where fixable: `frontend/src/lib/components/BonusInstrumentColumn.svelte:247-259` (`.pm-label`) and `:231-246` (`.pm-cell`).
- Proposed fix: step `.pm-label` down a size below about 480 px, or give `.pm-cell` a fixed two-line label height so the wrap when it comes is absorbed instead of desynchronising the pair. Whichever is chosen, the pair should be verified against the longest of the sixteen locales rather than against English.

## STC-MOBILEL-3-05 MEDIUM The BET row is 46 per cent empty, corroborating STC-MOBILEL-A-13 to one pixel from a different frame

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/255_mobile-l_post_feature_base.png`, and the same row in `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/245_mobile-l_transition_feature_entry_fade.png` through `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/254_mobile-l_transition_feature_exit.png`, `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/258_mobile-l_transition_maxwin_collect_fade.png` and `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/259_mobile-l_post_collect_base.png`
- Claim: sampled in `255` at row `y=1357`, the BET row's content is `BET` at `x=54 to 101`, then nothing until the decrement chevron's glyph at `x=507 to 520`, then `$1.00` at `x=579 to 675` and the increment chevron at `x=735 to 746`. The row's own border samples `x=24` and `x=812`. The ink-free run between the label and the first control glyph is **406 px, 206 CSS px**; allowing for the stepper's grey plate, which begins near `x=473` and falls below the sampling threshold, the genuinely empty band is about **372 px, 189 CSS px, roughly 46 per cent of the row's 788 px width**. The mechanism is `.p-bet-stat { width: 100%; display: flex; justify-content: space-between; }` (`frontend/src/lib/components/HudOverlay.svelte:1946-1963`) driving a row that has exactly two children, the label and `.p-bet-row` (markup at `:402-411`), so `space-between` pushes them to opposite ends of a 425 px viewport. The comment at `:396-400` records why the row is full width (a three-column stats row could not hold two 44 px steppers plus a stress-test bet figure), so the full-width row is deliberate and the void is its unexamined consequence.

  **Recorded as corroboration, not as discovery.** `STC-MOBILEL-A-13` states the same defect at "roughly 190 device pixels" on frames 208 to 233; my 189 CSS px reproduces that to one pixel from an independent frame in a different session phase. **The marshal should merge this into A-13 rather than counting it twice.** What my range adds is that the void is present in every frame of the second half of the session, so it is the row's steady state and not a transient, and that the source location is now pinned.
- Resolution note: VISIBLE AT BOTH. A-13 saw it at native. The value here is agreement to one pixel from a genuinely different input, which is the kind convention (l.4) asks for, plus the `file:line`.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte:1946-1963` (`.p-bet-stat`), markup at `:402-411`.
- Proposed fix: drop `justify-content: space-between` for `justify-content: center` with a fixed gap, or let the label sit immediately left of the stepper group, so the row does not distribute two small clusters across the full 425 px.

## Native pass reconciliation

`STC-MOBILEL-A.md` covers frames 208 to 233. **Every one of its thirteen findings
falls outside my range and belongs to a sibling squad; I reconcile none of them.**
The one exception I record rather than reconcile is `STC-MOBILEL-A-13`, whose
defect recurs on my frames and which `STC-MOBILEL-3-05` corroborates to one pixel.

`STC-MOBILEL-B.md` covers 234 to 259. Its `B-01` (frames `239`, `240`), `B-02`
(frame `238`) and `B-05` (frames `234`, `235`, `236`, `240`) cite no frame in 243
to 259 and are a sibling squad's; **I make no claim about them in either
direction.** The nine that reach my range:

- **`STC-MOBILEL-B-03` (max win overlay lets the HUD through, `FEATURES` label 11 px under the overlay's subtitle): CONFIRMED and REFINED.** Confirmed on `257` and `256`, and strengthened three ways. First, the bleed-through is now measured rather than read: the ghosted `$50,000.00` glyph pixels at row `y=1235` sample `(14,60,81)` against a local background of `(11,5,30)` at row `y=1290`, so the HUD is carrying real signal through the backdrop. Second, **the two text lines are much closer than "an 11 px gap" conveys**: across columns `x=300 to 540` the celebration's own line `PRESS COLLECT OR HIT ENTER TO CONTINUE` and the ghosted `FEATURES` label form one continuous run of ink from `y=1020 to y=1109`, and at a stricter threshold across `x=60 to 780` the only break between them is **3 px, 1.5 CSS px**. At full resolution this is a collision, not a gap. Third, **B-03's source citation is correct and I re-verified it**: the backdrop is `background: radial-gradient(ellipse at center, rgba(20, 8, 50, 0.97) 0%, rgba(6, 4, 20, 0.99) 100%)` at `frontend/src/lib/components/MaxWinCelebration.svelte:194-198`, and the `0.97` centre stop is what lets the HUD through. I read `FEATURES` and `$50,000.00` directly off the frame.
- **`STC-MOBILEL-B-04` (the celebration fills about a third of the portrait viewport): CONFIRMED.** The content group runs from the stars' top at `y=540` to the helper line's last ink at `y=1067`, which is **527 of 1600 rows, 32.9 per cent**, against B-04's "about 33 per cent". Its PARK is the right disposition and I do not disturb it.
- **`STC-MOBILEL-B-06` (reel machine and control stack inset differently, edges step by about 4 px left and 5 px right): CONFIRMED, to the pixel.** Sampled in `255`: the reel frame's neon reaches `x=16` and `x=822` (8.1 and 417.2 native), the BALANCE pod's border samples `x=24` and `x=812` (12.2 and 412.1 native), and the BET row's border samples the same `x=24` and `x=812`. The step is **8 px, 4.1 CSS px on the left and 10 px, 5.1 CSS px on the right**, against B-06's "about 4 px" and "about 5 px". Recorded so the agreement is not mistaken for two independent methods: both squads used the neon's outer visible extent as the reel landmark.
- **`STC-MOBILEL-B-07` (no safe area: 5 px above the wordmark, 7 px below SPIN): CONFIRMED.** In `255` the wordmark's topmost lit row is `y=9` (**4.6 CSS px** from the top edge) and the SPIN button's lowest lit row is `y=1583` on a last row of 1599 (**8.1 CSS px** of clearance). The measurement holds at full resolution.
- **`STC-MOBILEL-B-08` (frame `258` is the documented screen shake, not a defect; the finding is the amplitude against the gutter): CONFIRMED, and I record that I would have raised the shake itself as a defect without it.** I measured `258` before reading B and found the whole interface translated left by exactly **10 px, 5.07 CSS px**, on three independent landmarks: the bottom control row's bright extent moves from `x=53 to 769` in `245`, `246`, `250`, `255` and `259` to `x=43 to 759` in `258`, width unchanged at 716 px; the `FUTURE SPINNER` wordmark moves from `x=278 to 558` to `x=270 to 547`; and the reel frame's left glow shrinks from `x=0 to 17` to `x=0 to 7` while its right glow grows from `x=821 to 836` to `x=812 to 836`. That is B-08's 5 px to within 0.07 CSS px from different landmarks. **B-08's gutter claim also holds**: the left gutter closes from 8.6 CSS px to 3.6 CSS px at the displacement actually captured, against B-08's "from 8 px to 3 px". B-08 declining to call the shake a defect was correct and it saved this shard a false positive.
- **`STC-MOBILEL-B-09` (a 63 px empty band between the reel machine and the FEATURES button in base mode): REFINED.** The band is real, the observation that only feature mode decorates it is right, and its PARK is right. The **figure does not survive.** Sampled down the same landmark column `x=30` in `255`, the reel machine's lower chrome ends at `y=956` and the FEATURES pill's top border begins at `y=1036`, a gap of **80 px, 40.6 CSS px, not 63**. The difference is entirely where "lower chrome ends" is placed: B-09 put it at native `y=466` (`y=918` upscaled) and the chrome's lit rows continue to `y=956`. The neighbouring gap B-09 gives as 24 px (FEATURES to BALANCE) measures **32 px, 16.2 CSS px** on the same column, so the ratio to its neighbours is about **2.5x, not 3x**. Shape confirmed, magnitude overstated by about half.
- **`STC-MOBILEL-B-10` (both buy dialogs orphan `bet?` onto its own centred line): CONFIRMED.** `243` and `244` set `Start Overdrive Free Spins now at 400× your` across the full first line with `bet?` alone and centred beneath it, exactly as described.
- **`STC-MOBILEL-B-11` (the `PRICE` / `RTP` / `MAX WIN` row loses its shared baseline because only the third cell wraps): REFINED, and the headline half of it is REFUTED.** The wrap is real and the uneven padding is real and larger than stated. **The lost baseline is not.** Sampled in `244` down three column windows: all three labels occupy `y=1139 to 1152`, and the three first value lines occupy `y=1176 to 1197` (`PRICE`), `y=1176 to 1197` (`RTP`) and `y=1176 to 1201` (`MAX WIN`). **The first value line is on a shared baseline in all three cells**, and the 4 px difference on `MAX WIN` is the `×` glyph's own descent, not a row misalignment. What actually differs is what B-11's title does not say: `MAX WIN` carries a second line `base bet` at `y=1219 to 1241`, so the space between the last value ink and the strip's bottom edge at `y=1273` is **76 px (38.6 CSS px) under the two single-line cells against 32 px (16.2 CSS px) under the wrapped one, a 44 px, 22.3 CSS px difference in internal bottom padding**. The mechanism B-11 names is right: `.buy-stat { flex: 1; ... }` at `frontend/src/lib/components/BuyBonus.svelte:234-237` with the string from `maxWinVsBaseBetLabel()`, rendered at `:130-136`. **The finding should be re-titled to the padding, which is what a viewer sees. The baseline claim as written would not survive a verifier.**
- **`STC-MOBILEL-B-12` (the win info strip is squeezed into a band about 16 px tall, tight and off centre): REFINED, half CONFIRMED and half REFUTED.** Sampled in `255` across columns `x=330 to 520`: the bottom symbol row's last lit rows are `y=810 to 822`, the strip's type occupies `y=836 to 849`, and the reel frame's bottom border's first rows above threshold are `y=861 to 879`. Clearance above the type is **14 px, 7.1 CSS px**, matching B-12's "roughly 7 px", so the squeeze is confirmed. Clearance below is **12 px, 6.1 CSS px**, against B-12's "roughly 3 px". **The line is not meaningfully off centre**: the two clearances differ by 1 CSS px, not by 4. That half is sensitive to exactly where the frame's inner border is placed, and at full resolution I could not reproduce it in either direction from B-12's own landmarks, so it should not reach the ledger as stated.

## Explicit absences, signed

- **WITHDRAWN CLAIM 1, and the refutation is the point of writing it down. This shard's first pass raised, at HIGH, that the buy dialog's stats strip carries a four-stroke magenta accent frame in which no corner closes.** The measurements were correct and the conclusion was wrong. Sampled in `244` by isolating magenta pixels: top rule `y=1112 to 1114` spanning `x=93 to 743`; bottom rule `y=1269 to 1272` spanning `x=93 to 761`; left vertical `x=76` spanning `y=1131 to 1254`; right vertical `x=760` spanning `y=1131 to 1272`. Three separate 18 px (9.1 CSS px) mismatches, which read as one systematic inset applied to two sides and not the other two. **The source shows it is one stroke, not four, and every figure is the design.** `frontend/src/lib/components/BuyBonus.svelte:224-233` sets a single `box-shadow: inset 0 0 0 1.5px rgba(255, 43, 214, 0.85)` and then clips the element with `clip-path: polygon(0 12%, 3% 0, 97% 0, 100% 12%, 100% 100%, 3% 100%, 0 88%)`, a deliberately chamfered panel with notches at top-left, top-right and bottom-left and a square bottom-right. Against the measured panel box (`x=74 to 763`, `y≈1112 to 1273`), that polygon predicts the rules at `x=94.7 to 742.3` and `x=94.7 to 763`, the left edge from `y=1131.3 to 1254.6` and the right edge from `y=1131.3 to 1273`. **Every one of my four strokes lands within 2 px of its predicted vertex, and the left edge's terminus at `y=1254` matches `88%` exactly.** The corners do close, at chamfer vertices my row-and-column sampling could not see. Raised, measured, refuted, withdrawn.
- **WITHDRAWN CLAIM 2. This shard's first pass raised, at LOW, that `CANCEL` is 4 px shorter than `BUY`.** Measured in `244`: `CANCEL`'s outline runs `y=1307 to 1384` at column `x=80` (77 px) and `BUY`'s fill runs `y=1305 to 1386` at column `x=440` (81 px). **The source refutes the direction as well as the size.** `frontend/src/lib/components/BuyBonus.svelte:248` gives both buttons one shared rule, `flex: 1; padding: 10px; border-radius: 8px`, and the only difference is `border: 1px solid` on `.buy-cancel` (`:249`) against `border: none` on `.buy-confirm` (`:250`). Under `border-box` they are identical; under `content-box` `CANCEL` would be 2 CSS px **taller**, which is the opposite of what I measured. The measurement is therefore anti-aliasing on a solid fill against a crisp 1 px stroke, exactly the bias I caveated, and there is no layout difference to report. Withdrawn.
- **Viewport edge collisions and clipping.** Checked in all seventeen frames. Nothing in this range is cut off by a viewport edge. The reel frame's neon glow reaches `x=0` and `x=836` in every base and feature frame, but that is falloff rather than the frame, and it is symmetric within 1 to 2 px (left run `x=0 to 16/17`, right run `x=821/822 to 836`); the buy dialog's magenta glow reaches both edges the same symmetric way. The one asymmetric case is `258`, and that is the documented screen shake, reconciled above under `STC-MOBILEL-B-08`.
- **Modal overflow of the viewport it opens in.** Checked in `243` and `244`. The buy dialog's magenta card border samples `y=175` at the top and `y=1419 to 1423` at the bottom at column `x=32`, clearing the top edge by 175 px and the bottom by about 177 px, and its content column is a clean `x=73 to 763` in all three blocks (the `WHAT YOU GET` panel, the stats strip and the button row). It does not exceed the viewport, it does not scroll, and both `CANCEL` and `BUY` are fully on screen with the entire stats strip above them. **This session's Cluster 2, the buy confirm dialog with no reachable CONFIRM or CANCEL, does NOT occur at `mobile-l`**, and I say so explicitly because six squads reported it at four other viewports.
- **Money display fit, `TR-115` / `TR-086`.** Checked every money readout in my seventeen frames: `$400.00`, `$50,000.00`, `$5,000.00`, `$363.89`, `$318.64`, `$10.80`, `$10.00`, `$2.80`, `$1.00`, `$0.80`, `$0.20`, `$0.00`, plus `96.35%` and `5,000×`. **Every one sits inside its container with visible clearance on both sides; nothing clips, ellipsises or overflows.** No `TR-115` / `TR-086` instance in this range.
- **Controls too small to hit.** Measured rather than judged, and **not raised**, per convention (l.6). `COLLECT` on the max win overlay is the smallest primary control here: its amber fill measures `x=232 to 601` by `y=926 to 1006` (187 x 40.6 CSS px) and its outline and glow reach `y=912 to 1022` (55.8 CSS px). The fill alone is 3.4 CSS px under the 44 px convention and the outlined bounds are comfortably over it, so it straddles the line and the measurement cannot settle it. `CANCEL` and `BUY` measure about 171 and 170 CSS px wide by 39 to 41 CSS px tall, the same straddle. The SPIN button, the four secondary HUD controls and the bet steppers are all clearly above 44 px. Separately, `frontend/src/lib/components/FreeSpinsPresentation.svelte:686-708` shows the `TAP TO CONTINUE` gate already carries a derived `::after` hit extension (`inset: -28px 0`) with the landscape-scale arithmetic recorded, so that control is above the floor by construction.
- **Bottom control row symmetry.** Checked by sampling the band `y=1480 to 1550` in `245`, `246`, `250`, `254`, `255` and `259`: the row's bright extent is `x=53 to 769` in every one, a 716 px run centred on `x=411` against a viewport centre of `x=418.5`. That 7.5 px offset is the `MAX` label being text where the other controls are circles, which `STC-MOBILEL-B`'s own absence note already judged not clean enough to support a finding. **I agree, independently, and raise none.**
- **Panels that have become scrolling boxes.** No scroll region appears in frames 243 to 259; the paytable and the FEATURES panel are all below 243. **Cluster 3 is neither evidenced nor refuted by my range.**
- **THE FEATURE ITSELF IS NOT EVIDENCED AT `mobile-l`, AND MY SILENCE ON IT IS NOT A PASS.** Frames `246` through `253`, eight consecutive frames including all six the manifest names `Overdrive free spins in flight, interval frame N of 6`, render the identical undismissed Overdrive entry card with `+16 FREE SPINS` and `TAP TO CONTINUE`. The only thing that changes between them is the cycling win info strip (`L2  x5  1ways  $0.80` in `245`, `246`, `250`, `251`, `252`; `SCATTER  x5  5 ways  $10.00` in `247`, `248`, `249`, `253`). **I therefore make no composition claim about the in-flight free spins board at 425x812, because no frame in my range shows it.** Frame `259`, whose manifest note reads `Back to base after collect, balance settled`, is likewise not a base frame: it is another entry card reading `+8 FREE SPINS`. This reproduces `STC-MOBILEL-B`'s absence note independently. It is a capture gap and not a render defect: the gate is deliberate and explicit per the manifest note on `246` and per `frontend/src/lib/components/FreeSpinsPresentation.svelte:469-478`, and the harness never tapped it.
- **Transition frames that caught no motion.** `243` is indistinguishable from `244` and `245` from `246`. Of my five transition frames only `254`, `256` and `258` caught a distinct intermediate state. **I could not judge the composition of the buy dialog's opening animation or of the feature entry fade at all**, and that is stated rather than left as silence.
- **Cluster 1, the reel window going transparent mid-spin, is not evidenced in my range**, because no spin frame falls in 243 to 259. Neither confirmed nor refuted here.
- **Cluster 4, max win collect leaving residue on the board, IS present at `mobile-l`.** Frame `258` shows the board emptied of every symbol to a flat amber wash while the cyan win lines from the settled board are still drawn across it and the reel frame is still lit. Recorded as an observation rather than as a finding of mine, because it is a motion-residue claim and this is the composition lens; `258` is fresh evidence for the squad that owns it.

## KNOWN matches

- **KNOWN(Q-26), with a new instance and a new surface.** The `NITRO OVERDRIVE` dialog body in `243` and `244` mixes both glyphs **inside one block of copy**: `Start Overdrive Free Spins now at 400× your bet?` and `3, 4 or 5 Scatters award 8, 12 or 16 free spins and pay an instant 1×, 3× or 10× total bet.` use U+00D7, while `Buy a rich entry with the Overdrive meter pre-revved to 5x.` and `The Overdrive meter starts at 1x and rises +1x after every winning free spin` use the ASCII letter. The `5x` matches Q-26's own enumeration. **The new part is the win info strip under the reels**, which renders `x5` and `x3` with the ASCII letter in `245` through `255` and `259`, on screen for the whole of every win. `STL-AR-A-01` reached the same surface from the Arabic session, and `MID-02` records that Q-26's enumeration was built from the config and prose layers and missed the components. This is a third confirmation of that gap, from the English `mobile-l` frames.
- **KNOWN(Q-16 park), visibility recorded.** The parked hardcoded English string `PRESS COLLECT OR HIT ENTER TO CONTINUE` is on screen in `256` and `257`. My session is `en`, so the park's urgency is unchanged by my frames; the frame numbers are recorded so the de and ar squads have the equivalent surface.
- **No other KNOWN row and neither MID finding matched, checked rather than assumed.** `TR-104` needs a localised session and mine is `en`. `TR-115` / `TR-086` is signed absent above with the full list of readouts checked. `TR-114` needs a replay surface and none appears. `Q-34` needs a `Cruise` / `CRUISE` pair in one range and my frames carry neither surface. `Q-27` needs a link or an unstyled surface and none appears. `TR-089` is about count-up shimmy and no numeric surface in my range is mid count-up. `MID-01` needs a big win count-up frame and none falls in 243 to 259. `MID-02` needs the win banner, which does not appear in my range; the only banner-style unit here is `×BET` on the max win overlay in `256` and `257`, which correctly uses U+00D7 and is the surface already fixed under Q-12.

tree_after: `git status --porcelain`, verbatim, at the close of this run. Nothing MODIFIED, nothing DELETED. Every line is an untracked shard; only `STC-MOBILEL-3.md` is mine.

```
?? reports/qa/stream_test/shards/STC-MOBILEL-1.md
?? reports/qa/stream_test/shards/STC-MOBILEL-2.md
?? reports/qa/stream_test/shards/STC-MOBILEL-3.md
?? reports/qa/stream_test/shards/STC-MOBILEM-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-2.md
?? reports/qa/stream_test/shards/STC-POPOUTS-3.md
?? reports/qa/stream_test/shards/STM-MOBILEL.md
?? reports/qa/stream_test/shards/STM-POPOUTS.md
?? reports/qa/stream_test/shards/STT-MOBILEL-1.md
?? reports/qa/stream_test/shards/STT-MOBILEL-2.md
?? reports/qa/stream_test/shards/STT-MOBILEL-3.md
?? reports/qa/stream_test/shards/STT-POPOUTS-1.md
?? reports/qa/stream_test/shards/STT-POPOUTS-2.md
?? reports/qa/stream_test/shards/STT-POPOUTS-3.md
```
