# STT-POPOUTS-B, typography (popout-s, frames 183 to 207)
scope: every `popout-s` frame numbered 183 to 207 inclusive, lang en, 25 frames, each opened once with the Read tool. Manifest declares viewport `400x225` for all 25.
frames_read: 25

Note on method, because it changed three of my answers. Every glyph-level claim below was re-checked against a magnified crop of the frame AND against the source literal. Three findings I had written from the frames alone did not survive that check and are recorded as signed absences rather than quietly dropped.

## STT-POPOUTS-B-01 STREAM The buy confirm dialog renders a horizontal slice of its own prose and nothing else, and its buttons are below the fold
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/190_popout-s_dialog_buy_overdrive.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/189_popout-s_transition_dialog_buy_overdrive_opening.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/192_popout-s_dialog_nitro_overdrive.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/191_popout-s_transition_dialog_nitro_overdrive_opening.png`
- Claim: the string is `Start Overdrive Free Spins now at {cost}× your bet?` (`frontend/src/lib/i18n/translations.ts:1537`). What reaches the player at this viewport is `bet?` on 190 and `your bet?` on 192, one text line rendered in a roughly 28px strip below the stat row, with the tops of its glyphs cut by that row's opaque magenta bottom edge. Nothing of the sentence's head is visible on either frame.
  The mechanism is exact. `.buy-modal` is `max-height: 90dvh; overflow-y: auto` with `padding: 22px 24px 24px` (`frontend/src/lib/components/BuyBonus.svelte:174-175`), so at a 225px viewport the scroll box is about 202px, which is what the frames measure. `.buy-stats-row` is `position: sticky; bottom: 0; z-index: 2` with an opaque `background` and a `clip-path` (`frontend/src/lib/components/BuyBonus.svelte:224-233`). Pinned to the bottom of a 202px scroll box, the row plus the header art consume nearly all of it, and the container's own bottom padding leaves a narrow strip UNDER the sticky row through which the scrolling prose shows as a slice. `.buy-actions` (`:141-146`, `buyCancel` and `buyConfirm`) is further down the scroll and never renders: 190 and 192 show only the top rounded edges of the two buttons at the frame's bottom border, with no labels.
  The sticky was added deliberately and the reason is recorded at `frontend/src/lib/components/BuyBonus.svelte:212-223`: the price, RTP and max win disclosure was measured below the fold at 360x600 and at 812x375. The disclosure is now always visible; the sentence it is meant to accompany, and the buttons that act on it, are the things that went below the fold instead.
- Where fixable: `frontend/src/lib/components/BuyBonus.svelte:174` and `:230-232` (not locked)
- Proposed fix: cancel the modal's bottom padding beneath the sticky row so no content strip can exist under it, for example move `padding-bottom` off `.buy-modal` onto an inner wrapper that ends above `.buy-stats-row`, and add `scroll-padding-bottom` equal to the row's height so scrolling never parks a text line in that band. One rule each, no reordering of the owner-approved B4 presentation.

## STT-POPOUTS-B-02 STREAM The free spins instrument column is cut by the right edge of the composition, truncating every label mid-word with no ellipsis, for the whole feature
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/194_popout-s_feature_entry_card.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/196_popout-s_feature_run_1.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/197_popout-s_feature_run_2.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/198_popout-s_feature_run_3.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/199_popout-s_feature_run_4.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/200_popout-s_feature_run_5.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/201_popout-s_feature_run_6.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/193_popout-s_transition_feature_entry_fade.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/195_popout-s_transition_feature_starting.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/206_popout-s_transition_maxwin_collect_fade.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/207_popout-s_post_collect_base.png`
- Claim: the three instrument plates render their labels as `OVERDRIVE FR`, `TOTAL W` and `MULTIPLI`, each cut mid-word at the hard right edge of the frame, on eleven of my twenty-five frames and continuously through the entire feature. There is no ellipsis on any of the three.
  The absence of an ellipsis is the diagnostic detail: `.plate-value` and `.pm-value` DO carry `white-space: nowrap; overflow: hidden; text-overflow: ellipsis` (`frontend/src/lib/components/BonusInstrumentColumn.svelte:211-214` and `:265-269`), so if the text were outgrowing its own plate an ellipsis would appear. None does, which means the cut is happening OUTSIDE the component, at the edge of the composition, and no in-component fit rule can reach it. `.instrument-column` is `position: absolute; left: 1000px; top: 96px; width: 262px` (`frontend/src/lib/components/BonusInstrumentColumn.svelte:113-117`), design coordinates in the fixed 1280x720 surface described at `frontend/src/App.svelte:747-752`. At the mini-player profile the column's right portion falls outside the visible area.
  I did NOT locate the line in `App.svelte` that produces the mini-player crop, so the second half of the fix location is honestly unknown rather than guessed.
- Where fixable: `frontend/src/lib/components/BonusInstrumentColumn.svelte:113-117` (not locked), plus the mini-player composition branch in `frontend/src/App.svelte` around `:2003` (`.native-hud-slot ... class:mini-player`), exact line UNKNOWN
- Proposed fix: PARK(the column is positioned in absolute design coordinates and the mini-player profile has its own native composition; deciding whether the column is re-laid-out, docked or dropped at 400x225 is a composition call, not a CSS tweak). For the owner: the same profile already drops the mode blurbs and OVERBOOST's secondary line by explicit decision (`frontend/src/lib/components/FeatureMenu.svelte:731-737`), so a recorded decision for this column is the matching move.

## STT-POPOUTS-B-03 HIGH Two mini-profile type styles are set at 0.52rem, below the 11px legibility floor the same file names
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/187_popout-s_transition_features_menu_opening.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png`
- Claim: `.fm-panel--mini .fm-section-label { font-size: 0.52rem; ... }` (`frontend/src/lib/components/FeatureMenu.svelte:771`) and `.fm-panel--mini .fm-cost { font-size: 0.52rem; }` (`frontend/src/lib/components/FeatureMenu.svelte:780`). `0.52rem` is `8.32px` at the browser default 16px root, and nothing in `frontend/src/app.css` overrides the root font size, so 8.32px is the rendered value.
  The project states its own floor as `11px` in this same file: *"would either sit below the 11px legibility floor or overflow the 76px strip height"* (`frontend/src/lib/components/FeatureMenu.svelte:227`). Two styles at 8.32px are 24 per cent under it.
  The frames confirm the consequence rather than discovering it. At 11x magnification the OVERBOOST cost line resolves to `1.25` plus a unit mark plus `bet` occupying about four pixels of digit height, and at native size it reads as a smudge; the Normal and Cruise cost lines are worse. The affected strings are the per-mode cost, which is the platform disclosure this panel exists to carry, and the section labels `SPIN MODES` and `BUY FEATURES` (`frontend/src/lib/i18n/translations.ts:275`).
- Where fixable: `frontend/src/lib/components/FeatureMenu.svelte:771` and `:780` (not locked)
- Proposed fix: raise both to at least `0.6875rem` (11px) to meet the file's own floor, and let the mini panel drop a row rather than shrink type below it. If 11px will not fit the strip, that is a composition decision to record, not a size to quietly undercut.

## STT-POPOUTS-B-04 MEDIUM The `BUY FEATURES` section label is knife-cut through its letterforms at the scroll edge, immediately above the footer rule
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/187_popout-s_transition_features_menu_opening.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png`
- Claim: the second section label, `BUY FEATURES` (`frontend/src/lib/i18n/translations.ts:275`, rendered at `frontend/src/lib/components/FeatureMenu.svelte:459`), comes to rest with roughly its top two pixels showing and the rest below the scroll boundary of `.fm-cards` (`overflow-y: auto`, `frontend/src/lib/components/FeatureMenu.svelte:874-876`), directly against the `border-top` of `.fm-foot` (`:1001-1005`). At 9x magnification the surviving band is a row of disconnected yellow fragments that carries no word.
  This IS the scroll boundary behaving as declared, and the file says so at `:736`. The finding is that there is no mask, fade or snap, so the boundary lands mid-glyph on a section header, beside a fully rendered `SPIN MODES` on the same frame; two instances of one component, one legible and one reduced to fragments. The manifest note for 188 is `FEATURES menu, all five modes and their prices`, and three of the five mode rows are legible on it.
- Where fixable: `frontend/src/lib/components/FeatureMenu.svelte:874-876` (not locked)
- Proposed fix: add a bottom scroll fade to `.fm-cards`, one property, `mask-image: linear-gradient(to bottom, #000 calc(100% - 14px), transparent)`, so partial content reads as continuing rather than as cut.

## STT-POPOUTS-B-05 MEDIUM The paytable DISCLAIMER ends mid-clause, cut through its descenders at the panel border, with no continuation affordance in frame
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/185_popout-s_paytable_09_disclaimer.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/183_popout-s_paytable_07_interface_guide.png`
- Claim: on 185 the last rendered line is `plays and does not guarantee any result in a single`, and the panel's bottom border passes through the descenders of `p`, `y` and `g` on that line. The sentence stops there. At 8x magnification the descenders of the line above are also visible as clipped fragments. No scrollbar, fade, chevron or rule indicates the text continues. On 183 the second interface-guide card is cut the same way.
  CAVEAT, signed: the paytable is a scrolling surface, so a partially visible last line is not by itself a fault, and I did not read `PaytableModal.svelte`, so I cannot say whether a scrollbar exists and simply did not render in the capture. The claim I am making is what the frame shows: at this viewport the compliance disclaimer terminates mid-clause with nothing on screen telling the reader there is more.
- Where fixable: UNKNOWN. `frontend/src/lib/components/PaytableModal.svelte` is the surface; it uses a 500px breakpoint per the cross-reference at `frontend/src/lib/components/MaxWinCelebration.svelte:362-365`. Not locked.
- Proposed fix: same bottom scroll fade as STT-POPOUTS-B-04, applied to the paytable body, so a clipped line reads as continuing. Whether the disclaimer should instead be guaranteed to fit at 400x225 is a compliance question for the owner, per convention (l.8).

## STT-POPOUTS-B-06 LOW The max win unit sets `×` and `BET` at 24px and 13px and bottom-aligns them, so the two halves of one unit share no baseline
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/205_popout-s_maxwin_celebration.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/204_popout-s_transition_maxwin_overlay_fade.png`
- Claim: the unit is two spans, `<span class="c1-max-x">×</span>` and `<span class="c1-max-betlabel">{t($locale, 'bet', localeMode)}</span>` (`frontend/src/lib/components/MaxWinCelebration.svelte:155,158`). Under `@media (max-width: 500px)`, which applies at 400px wide, they are `font-size: 24px` and `font-size: 13px` beside a `50px` numeral (`frontend/src/lib/components/MaxWinCelebration.svelte:366-369`). Both spans are `align-self: flex-end` with different `padding-bottom` values, `0.12em` and `0.28em` (`:311-315` and `:317-323`), so they are aligned by their bottom edges rather than by a shared baseline. At 7x magnification on 205 the `×` sits a full cap height above the optical centre of `BET` and reads as detached from it.
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:311-323` and `:366-369` (not locked)
- Proposed fix: replace `align-self: flex-end` on the two unit spans with a shared baseline, for example `align-items: baseline` on `.c1-max-multwrap` with the `×` given a small `position: relative; top` if the optical lift is wanted deliberately.

## STT-POPOUTS-B-07 LOW Mode names in one list mix title case and full capitals, and the source records only one side of it as deliberate
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/187_popout-s_transition_features_menu_opening.png`
- Claim: the SPIN MODES list renders `Normal` and `Cruise` in title case and `OVERBOOST` in full capitals, same weight, same size, three rows of one component. Sources: `modeNormalLabel: 'Normal'` and the sibling Cruise entry (`frontend/src/lib/i18n/prose.locales.ts:115` and its block), against `modeOverboostLabel: 'OVERBOOST'` (`frontend/src/lib/i18n/prose.ts:89`).
  COUNTER-EVIDENCE, recorded so the marshal can dispose of this in one read: `frontend/src/lib/i18n/prose.locales.ts:22` instructs translators to *keep the case shape, never translate the product nouns (Overdrive, OVERBOOST, ...)*, so OVERBOOST's capitals are a recorded intention, not a drift. Graded LOW for that reason. It remains true that a viewer of that one list gets no rule explaining why one of three names shouts.
- Where fixable: `frontend/src/lib/i18n/prose.ts:89` and the `modeNormalLabel` / `modeCruiseLabel` entries across `frontend/src/lib/i18n/prose.locales.ts` (not locked)
- Proposed fix: PARK(an art call, and a sixteen-locale one). It is the same direction question as KNOWN row Q-34 and should be ruled on with it, not separately.

## STT-POPOUTS-B-08 NOT A PRODUCT FINDING, self-reported: this squad damaged a committed evidence frame and restored it
- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png`
- Claim: **I overwrote this committed frame in the working tree and then restored it.** Reported in full rather than quietly reverted, because a silently repaired evidence file is exactly the artefact convention (h.1) exists to prevent.
  What happened. Probing which image tools were available on the machine, I ran `sips -c 330 2340 --cropOffset 0 0 <path to 188>` **without an `--out` argument**. `sips` writes IN PLACE when no `--out` is given, so the probe cropped the committed PNG from `400 x 225` to `2340 x 330` and `git status` then showed ` M reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png`, `Bin 59295 -> 91803 bytes`. Restored with `git checkout --` on that one path; the file is back to `400 x 225` and `git status --porcelain` is clean of it, as recorded under `tree_after` below.
  What it did NOT affect, checked rather than assumed. The damage happened AFTER my frame-reading pass, so the visual read of 188 in step 1 was of the intact file. Every claim in this shard that cites 188 also cites `187_popout-s_transition_features_menu_opening.png`, an untouched 400x225 capture of the same surface, and all magnified verification of the features menu was done on 187. No finding here rests on the damaged bytes. I briefly wrote up the wrong dimensions as a capture-set defect before tracing them to my own command; that draft is superseded by this entry.
  The generalisable lesson, since it is the same shape as the near-miss recorded in `CLAUDE.md` under LOCKED_FILE_DEBTS: **a read-only intent does not make a command read-only.** `sips` mutates by default. Any tool invoked against a committed evidence path needs its output target stated explicitly, and a capability probe should be run against a scratch copy, never against the artefact.
- Where fixable: not source. A process note for the audit method, `docs/skills/FULL_AUDIT_METHOD.md` section 4.2 (the scratch-rename rule) is where it belongs.
- Proposed fix: extend the scratch rule so that squads copy any frame to scratch before invoking a non-Read tool on it, and never name a committed path as the operand of an image tool.

## Explicit absences, signed

Three of these are findings I had written from the frames and then withdrew. They are recorded in full because a withdrawn finding is evidence about the instrument.

- **WITHDRAWN, the abbreviated money pods are sanctioned, not a defect.** I had opened a HIGH finding on `BAL $50K` and `WIN $5K` rendering beside `$1.00` on frames 206 and 207, and on the WIN pod changing format across `$16.20` (186), `$319.45` (202), `$363.89` (203), then `$5K` (206, 207). It is refuted at source: `frontend/src/lib/utils/currency.ts:257-268` records Fable's ruling closing TR-066, *"the 400x225 mini-player profile alone, for BALANCE and WIN alone, and only when the fully formatted value cannot fit its MEASURED slot at the legible floor"*, with the decision taken by measurement in `frontend/src/lib/actions/fitMoney.ts`, not by a magnitude threshold. `popout-s` IS the 400x225 profile, the two pods ARE balance and win, and the bet pod correctly keeps full precision. Truncation toward zero is deliberate and documented at `:236-247`. Nothing to fix.
- **WITHDRAWN, the features menu unit is `×`, not a letter `x`.** I had logged a KNOWN(Q-26) match on `1x bet` and `1.25x bet` from frames 187 and 188. The literal is `{m.cost}× {$isSocial ? 'per spin' : 'bet'}` at `frontend/src/lib/components/FeatureMenu.svelte:372`, `:427` and `:480`, U+00D7 at all three. The frames cannot resolve a glyph at four pixels and I should not have read one off them. Worth noting for the marshal: the prose comment at `frontend/src/lib/components/FeatureMenu.svelte:420` still writes `"1.25x bet"` with a letter `x` when describing the rendered string, which is how a reader gets the wrong impression from the source too.
- **WITHDRAWN, the Overdrive meter unit is `×`, not a letter `x`.** I had opened a MEDIUM finding claiming the meter's `1x` was a further instance outside Q-26's enumeration. The literal is `<span class="plate-value">{multiplier}×</span>` at `frontend/src/lib/components/BonusInstrumentColumn.svelte:105`, U+00D7. No new instance of the MID-02 class exists on my frames.
- **No system font leakage found on any of the 25 frames.** Two families are in use and the split is consistent: a wide display face on every heading, label, chip, button and numeral (`PAYTABLE` on 183 to 185, `FEATURES` on 187 and 188, `PRICE`/`RTP`/`MAX WIN` on 190 and 192, `COLLECT` on 205, every money pod), and a narrower squared-terminal text face on running prose and mode names (the RESPONSIBLE PLAY and DISCLAIMER paragraphs on 184 and 185, `Start a spin at the current bet.` on 183, `Normal` and `Cruise` on 187 and 188, `your bet?` on 192). Checked at magnification on 184, 187, 190 and 205. No heading used the text face and no paragraph used the display face.
- **No missing-glyph box on any frame.** The only candidate was the max win unit on 204, captured mid-fade. Magnified 9x it resolves to `×` and letterspaced `BET`, and 205 shows the same element settled. It is small type, not a tofu, and the glyph-coverage claim is not made.
- **No mixed straight and curly quotes, and no apostrophe at all, in this range.** The three surfaces carrying enough prose to contain one are 183, 184 and 185. Read at 5x, none uses a possessive or a contraction: the RESPONSIBLE PLAY paragraph and the DISCLAIMER paragraph are both written without them.
- **No em dash or en dash in player-visible prose in this range.** Checked the two full paragraphs (184, 185) at 5x and 8x, the interface guide row copy (183), and every label and button on 186 through 207. The only non-alphanumeric separators seen are the middle dot in `All modes · RTP 96.35%` (187, 188) and the minus glyph on the bet decrement control (187, 188).
- **No double space detected, and the limit of that claim is stated.** The body faces render at roughly six pixels of x-height at this viewport; a doubled word space is at the edge of what a rendered frame can prove even at 5x. Word spacing in the 184 paragraph looked uniform across three full lines. This absence is signed against what the frames can show, not against the source.
- **No numeral shimmy on a non-count-up surface.** The bet pod holds `$1.00` at identical glyph width across 186, 193 to 201 and 202 to 207; `96.35%` holds identical width across 187, 188, 190 and 192; the free spins count holds its box across 194 to 201. The only figures that move are the HUD WIN pod and the banner, which are the `.fs-num` mechanism allowlisted by KNOWN row TR-089. `.buy-stat-val` carries `font-variant-numeric: tabular-nums` (`frontend/src/lib/components/BuyBonus.svelte:239`) and `.pm-value` carries it too (`frontend/src/lib/components/BonusInstrumentColumn.svelte:264`), which is consistent with what the frames show.
- **No letter-spacing or weight mismatch between two instances of one component**, beyond the case difference reported as STT-POPOUTS-B-07 and the legibility floor reported as STT-POPOUTS-B-03. The three stat column headers on 190 and 192 match each other in tracking and weight; the section headers `INTERFACE GUIDE`, `RESPONSIBLE PLAY` and `DISCLAIMER` match each other across 183, 184 and 185; `PAYTABLE` is identical on all three.
- **No ellipsised string anywhere in the range.** Every truncation found is a hard cut with no ellipsis character, which is itself part of STT-POPOUTS-B-01, B-02 and B-04.
- **No placeholder string, no lorem text and no untranslated key** on any of the 25 frames.

## KNOWN matches
- KNOWN(TR-115 / TR-086): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/194_popout-s_feature_entry_card.png`, the instrument column's TOTAL WIN value is cut to `$10.6` at the right edge; the same on 195 through 201, and cut to `$2.8` on 206 and 207. Fresh evidence of the money-fit class at the 400x225 profile, on a surface the fit mechanism does not currently reach.
- KNOWN(Q-34): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png` and `187_popout-s_transition_features_menu_opening.png`, the features menu writes `Cruise` in title case. Fresh evidence for the row's cross-surface casing claim, and the direction question it raises should be ruled on together with STT-POPOUTS-B-07.
- No match found for MID-01 or MID-02 on my frames. Neither the win banner nor the HUD big-win count-up appears in 183 to 207; the big-win triple for this session is 169 to 171, outside my range.

tree_after:
```
?? reports/qa/stream_test/shards/STC-MOBILEL-B.md
?? reports/qa/stream_test/shards/STC-MOBILEM-B.md
?? reports/qa/stream_test/shards/STC-MOBILES-A.md
?? reports/qa/stream_test/shards/STC-MOBILES-B.md
?? reports/qa/stream_test/shards/STC-STRETCH-A.md
?? reports/qa/stream_test/shards/STC-STRETCH-B.md
?? reports/qa/stream_test/shards/STL-DE-A.md
?? reports/qa/stream_test/shards/STM-DESKTOP.md
?? reports/qa/stream_test/shards/STM-LAPTOP.md
?? reports/qa/stream_test/shards/STM-MOBILEM.md
?? reports/qa/stream_test/shards/STM-MOBILES.md
?? reports/qa/stream_test/shards/STM-POPOUTS.md
?? reports/qa/stream_test/shards/STM-STRETCH.md
?? reports/qa/stream_test/shards/STT-DESKTOP-A.md
?? reports/qa/stream_test/shards/STT-DESKTOP-B.md
?? reports/qa/stream_test/shards/STT-LAPTOP-B.md
?? reports/qa/stream_test/shards/STT-POPOUTS-A.md
?? reports/qa/stream_test/shards/STT-POPOUTS-B.md
```
All entries untracked. `STT-POPOUTS-B.md` is mine; the other seventeen are other squads' shards, not mine and not my concern. The count rose by one between my first and last status call as another squad landed `STL-DE-A.md`, which is expected in a concurrent wave.

**LOUD, and it is mine.** The first `git status --porcelain` of this run was NOT clean. It read:

```
 M reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png
```

plus the seventeen untracked shards. The MODIFIED entry was caused by this squad, by an unguarded `sips` call with no `--out`, and is written up in full as STT-POPOUTS-B-08 above. It was restored with `git checkout --` on that single path, the file is back to its HEAD bytes at `400 x 225`, and the status shown above is the state I am leaving the tree in. No other file was written by this squad except this shard. Nothing else showed as MODIFIED or DELETED at any point.
