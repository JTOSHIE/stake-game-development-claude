# STM-MOBILEM, motion residue (mobile-m, frames 260 to 311, 1600px upscaled)
supersedes: STM-MOBILEM.md (retained at `reports/qa/stream_test/shards/superseded/STM-MOBILEM.md`)
scope: every frame of the `mobile-m` session whose filename contains `transition_`, 18 of 18. Read from `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/`, the same frames as `reports/screens/stream-test-2026-07-28/` resampled to 1600px height (900x1600 from a native 375x667, a factor of 2.398). Settled frames 263, 265, 274, 276, 279, 292, 294, 298, 300, 307, 309 and 311 were opened for comparison and do not count against the set.
frames_read: 18

**Measurement note.** Where this shard states a pixel figure it was measured, not estimated, by decoding the PNG and scanning rows and columns; the figure is given in the 1600px render and again at native. Where a claim touches source it was checked against source, and three of my own first readings were refuted that way and are recorded as refuted rather than quietly dropped, per convention (l.2) and (l.5).

## STM-MOBILEM-01 STREAM The collect transition drops the board into an olive wash, translates the whole page, and shows two win figures that differ by `$4,997.20`

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/310_mobile-m_transition_maxwin_collect_fade.png`. Endpoints opened: `309_mobile-m_maxwin_celebration.png` and `311_mobile-m_post_collect_base.png`.
- Claim: the manifest note for 310 is `Collect pressed, overlay mid-fade-out`. There is no max win overlay in the frame at any opacity: no `MAX WIN REACHED!`, no `5,000`, no `COLLECT` button, no particle field. What occupies the reel window instead is a yellow-olive radial wash covering all twenty cells, under which every symbol is a discernible near-black ghost, and the win-line strip that 311 carries (`M3  x3  1 ways  $0.20`) is absent entirely. Three further things are wrong only in this frame and they compound.
  1. **The page is translated, and it is a pure translation.** Measured off the red reel frame at three columns and off the SPIN button centre: 310's frame runs y=151 to y=839 and 311's runs y=141 to y=829, an identical 688px tall, so the surface is moved and not scaled. The offset is **12px left and 10px down in the 1600px render, which is 5.0px left and 4.2px down at the native 375x667.** The consequence is visible without measuring: the reel frame's right edge sits at x=886 in 310 against x=898 in 311, so a **12px strip (5px native)** of raw scene background is exposed down the right edge of the viewport for the length of the transition, and the frame's left border is pushed to x=0.
  2. **Two win readouts disagree by three orders of magnitude.** `TOTAL WIN` reads `$2.80` in the feature pod while `WIN` reads `$5,000.00` in the HUD pod, in the same view, at the instant a 5,000x max win is collected. Both figures are large, well lit and a pod apart.
  3. **The feature chrome has already returned.** Red reel frame, green scene, magenta SPIN button and `OVERDRIVE FREE SPINS` `8`, none of which 309 shows, while the entry card those pods belong to (`+8 FREE SPINS`, `TAP TO CONTINUE`, which 311 does show) has not yet arrived. The feature HUD precedes its own card.
- Resolution note: VISIBLE AT BOTH for the wash and the translation. NEW AT 1600PX for the exposed right-edge background strip, for the translation being pure rather than a scale, and for the wash leaving the twenty symbols individually discernible rather than blanking the board.
- Where fixable: UNKNOWN for the wash; grep found no yellow flash rule on the grid, and the native pass had already checked and excluded the brightness flash at `frontend/src/lib/components/GameGrid.svelte:1571-1589`. The dismissal transform is on the celebration host, `frontend/src/lib/components/MaxWinCelebration.svelte` (the surface is `.c1-max-*`, markup `frontend/src/lib/components/MaxWinCelebration.svelte:148-166`). The `$2.80` against `$5,000.00` pairing is the feature pods against the HUD win pod, both in `frontend/src/lib/components/HudOverlay.svelte`. None locked.
- Proposed fix: PARK(the wash has to be identified in source before anything is proposed, and if it turns out to be an intended collect flash it wants a shorter, gold, whole-screen treatment rather than a reel-window-only olive one, which is an art call). The translation is separable and is not an art call: whatever transform runs on dismissal should run on the overlay, not on a container that carries the game surface with it.

## STM-MOBILEM-02 STREAM The reel window has no opaque backing, so six of twenty cells are voids showing the scene art straight through the board

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/266_mobile-m_transition_reels_accelerating.png`. Endpoints opened: `265_mobile-m_base_idle.png` and `267_mobile-m_transition_reels_full_speed.png`.
- Claim: about 250ms after the spin press the grid is holed. Reel 1 carries three symbols and one empty cell, reel 2 carries two and two empty, reel 3 carries one and three empty, and reels 4 and 5 still show the complete pre-spin board of identical pistons, untouched and perfectly on grid. **Six of the twenty cells are voids.** At 1600px what shows through those voids is identifiable scene artwork rather than a dark placeholder: the magenta chevron stripes of the city backdrop across reel 1 rows 3 and 4 and reel 2 rows 3 and 4, and the vertical rain streaks and building lights across reel 3. That is the difference between a cell that is empty and a board that has no backing, and it settles which of the two this is. Neither endpoint has an empty cell. Eleven squads across two lenses and seven viewports have reported this surface; the mobile-m evidence is here.
- Resolution note: NEW AT 1600PX for the diagnosis. The native pass could see that cells were dark; it could not see that the specific magenta chevron and rain artwork of the backdrop is what fills them, which is what makes this a missing-backing defect rather than a missing-symbol one.
- Where fixable: `frontend/src/lib/components/GameGrid.svelte`, the reel strip surface at `frontend/src/lib/components/GameGrid.svelte:1235` (`.reel-strip`). Not locked. Recorded so the next reader does not repeat the search: grep over `frontend/src/lib/` found no opaque backing rule behind the strips, which is consistent with the frames.
- Proposed fix: give the reel window an opaque backing layer beneath the strips so no cell can ever composite the scene, and fill each spinning strip from a buffer populated before the reel starts.

## STM-MOBILEM-03 STREAM The max win overlay does not hide the HUD: the collect instruction is printed across the live `BALANCE` and `WIN` labels

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/308_mobile-m_transition_maxwin_overlay_fade.png`. Same state settled: `309_mobile-m_maxwin_celebration.png`.
- Claim: the caption `PRESS COLLECT OR HIT ENTER TO CONTINUE` wraps to two lines under the COLLECT button, and its first line shares a band with the ghosted pod labels `BALANCE` and `WIN` showing through the celebration backdrop. One line lower, `$50,000.00` and `$5,000.00` are both individually readable through the overlay; lower again, `$1.00`; lower again, the word `SPIN`. Five HUD strings are legible through the game's largest celebration surface and one of them overprints the caption the celebration exists to give the player. The source gives the figure: the backdrop is `radial-gradient(... rgba(20, 8, 50, 0.97) 0%, rgba(6, 4, 20, 0.99) 100%)` at `frontend/src/lib/components/MaxWinCelebration.svelte:194-198`, so one to three per cent of a bright cyan money string comes through, and there is no blur to make what comes through unreadable. Present in the transition frame and the settled frame alike, so a scrim defect rather than a fade defect; reported because 308 is an assigned frame and because the most-watched surface in the title is the wrong place to find it.
- Resolution note: VISIBLE AT BOTH that figures ghost through. NEW AT 1600PX that the ghosted strings are individually transcribable and that the caption line and the pod labels occupy the same band.
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:194-198`. Not locked.
- Proposed fix: take both alphas to `1`, or add `backdrop-filter: blur(8px)` so what shows through is unreadable by design rather than by accident.

## STM-MOBILEM-04 HIGH The rules card composites as one subtree, so HUD money prints through the card body and the control bar stays lit under a modal that has fully arrived

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/262_mobile-m_transition_splash_to_rules.png`. Endpoint opened: `263_mobile-m_intro_rules.png`.
- Claim: in the settled frame 263 nothing behind the rules card is legible: the board is dimmed and blurred and the bottom control bar is almost black. In 262 the card's own copy is at full strength while the surface behind it is barely dimmed in its lower half, and these strings are all readable inside or beside the card at 1600px: `FEATURES` (its right half, as `URES`, behind the third bullet), `BALANCE` and `$50,000.00` and `WIN` and `$0.00` in the band between the last bullet `the feature immediately.` and the `Continue` button, and `BET` with `$1.00` across the card's lower border. Separately, the whole bottom control bar (menu, turbo, SPIN, MAX and refresh) is at **full brightness** in 262 and dimmed nearly to black in 263, so for the length of the fade a fully formed modal sits over an apparently live and pressable SPIN button. Text over text on the first gated screen of the game.
  The mechanism is two declarations. `.intro-backdrop` carries `background: rgba(0, 0, 0, 0.86)`, `backdrop-filter: blur(3px)` and `animation: intro-fade-in 0.35s ease both` at `frontend/src/lib/components/IntroSplash.svelte:46-49`, where `intro-fade-in` is `from { opacity: 0 } to { opacity: 1 }` at `frontend/src/lib/components/IntroSplash.svelte:147`. `.intro-card` is its child (`frontend/src/lib/components/IntroSplash.svelte:66`) with its own opacity animation at `:79`. Animating opacity on the parent composites the whole subtree, so the card's opaque `linear-gradient(160deg, #0c0c22 0%, #08081a 100%)` (`:72`) is multiplied by the scrim's own alpha and cannot be opaque while the scrim is not. The blur behaves the same way: a `backdrop-filter` layer drawn at partial opacity over an unblurred real backdrop reads as sharp, which is why the HUD text is legible rather than smeared.
- Resolution note: VISIBLE AT BOTH that money reads through. NEW AT 1600PX for the control-bar brightness lag, which at thumbnail scale is a dark row either way.
- Where fixable: `frontend/src/lib/components/IntroSplash.svelte:46-49`. Not locked.
- Proposed fix: animate `background-color` and `backdrop-filter` on `.intro-backdrop` instead of the element's opacity, so the card underneath is never composited below full opacity; or hold the backdrop at its settled values from frame one and fade only `.intro-card`.

## STM-MOBILEM-05 HIGH The HUD menu is translucent: `PAYTABLE` overprints `BALANCE`, `Session` overprints `$50,000.00`, and the panel edge bisects two money pods

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/275_mobile-m_transition_menu_opening.png`. Same state settled: `276_mobile-m_hud_menu.png`.
- Claim: two strings are simultaneously legible in the same band. The menu row `PAYTABLE` and the ghosted pod label `BALANCE` sit about twelve 1600px rows apart, close enough that their glyphs interleave; the menu row `Session` sits directly on the ghosted `$50,000.00`, with the `i` and the `o` of `Session` falling inside the `0` glyphs. The panel does not cover the pods, it cuts them: the WIN pod is left reading `16.20` with its `$` behind the panel edge at x=620, and the bet row is left reading `1.00`. The source gives the figure: `.hud-menu` is `background: rgba(6, 6, 18, 0.96)` at `frontend/src/lib/components/HudOverlay.svelte:1603`, with no `backdrop-filter` in the rule at `:1598`. Identical in the settled frame 276, so a scrim defect and not a fade defect; reported because 275 is an assigned frame.
- Resolution note: VISIBLE AT BOTH.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte:1603`. Not locked.
- Proposed fix: take the alpha to `1` or add a backdrop blur, and size or place the panel so it does not stop mid-pod.

## STM-MOBILEM-06 HIGH Neither buy dialog shows CONFIRM or CANCEL, and the price is the last thing on screen

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/293_mobile-m_transition_dialog_buy_overdrive_opening.png` and `295_mobile-m_transition_dialog_nitro_overdrive_opening.png`. Endpoint opened: `294_mobile-m_dialog_buy_overdrive.png`.
- Claim: on all three frames the dialog runs icon, title, `Start Overdrive Free Spins now at 100× your bet?` (`400×` on the NITRO variant), the `WHAT YOU GET` box, a five symbol row, then the stat bar `PRICE` `$100.00` / `RTP` `96.35%` / `MAX WIN` `5,000× base bet`, and then the panel border closes. There is no confirm control, no cancel control and no close cross anywhere on screen. The last thing a player is shown before the panel ends is a price of `$100.00` or `$400.00`. The source says exactly why, and I verified each line rather than inheriting it: `.buy-modal` is `max-height: 90dvh; overflow-y: auto` (`frontend/src/lib/components/BuyBonus.svelte:174`), which is 600px in a 667px viewport and is where the frames put it; `.buy-stats-row` is `position: sticky` (`frontend/src/lib/components/BuyBonus.svelte:230`, markup at `:122`), so the disclosure row is pinned to the bottom of that scroll box and is guaranteed on screen; `.buy-actions` comes AFTER it in the markup (`frontend/src/lib/components/BuyBonus.svelte:141`) and is a plain `display: flex` with no sticky (`frontend/src/lib/components/BuyBonus.svelte:247`), so the two buttons sit in the overflowed remainder underneath the pinned row. Present in transition and settled alike, so a fit failure rather than a motion one; reported because these are assigned frames and because a purchase confirmation with no visible confirm control is the most consequential thing on them. Six squads across two lenses and four viewports have reported this surface.
- Resolution note: VISIBLE AT BOTH.
- Where fixable: `frontend/src/lib/components/BuyBonus.svelte:247`, with the markup order at `frontend/src/lib/components/BuyBonus.svelte:122-141`. Not locked.
- Proposed fix: give `.buy-actions` the same `position: sticky; bottom: 0` treatment the stats row already has and move it into or directly below that pinned block, so the disclosure and the two controls form one pinned footer at every supported height.

## STM-MOBILEM-07 HIGH The feature entry card holds at full opacity while its own backdrop dim lifts, and the card's display type is printed over the gauge art

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/297_mobile-m_transition_feature_entry_fade.png` and `299_mobile-m_transition_feature_starting.png`. Endpoints opened: `298_mobile-m_feature_entry_card.png` and `300_mobile-m_feature_run_1.png`.
- Claim: two things, and the second is the one the native pass could not see.
  1. **The scrim leaves without the card.** The card (heading `OVERDRIVE FREE SPINS`, the chrome gauge, `+16 FREE SPINS`, the `TAP TO CONTINUE` pill) is identical across 297, 298, 299 and 300. What changes is only the layer behind it: in 297 and 298 the board beneath is barely present, in 299 and 300 the same twenty symbols are clearly readable through the overlay, and the win-line strip behind advances from `L2  x5  1 ways  $0.80` to `SCATTER  x5  5 ways  $10.00`. So the dim lifts out from under a card that does not move, and the card's copy ends up printed over live board content and a cycling win readout. **300 is indistinguishable from 299**, and the manifest calls 300 `Overdrive free spins in flight, interval frame 1 of 6`, so the card is still fully painted after the feature is said to be running. Two readings survive the frames, and both are defects: either the card is a ghost of a surface already dismissed, or the explicit continue gate is not gating while the round underneath runs on.
  2. **`+16 FREE SPINS` is drawn on top of the gauge.** The yellow display line crosses the gauge's lower chrome ring; the ring's bevel and its `0` tick are visible between and through the letters of `FREE` and `SPINS`. Text over graphic, on all four frames.
- Resolution note: NEW AT 1600PX for point 2, which at thumbnail scale reads as one yellow blob on a dial. VISIBLE AT BOTH for point 1, strengthened here by opening 300.
- Where fixable: `frontend/src/lib/components/FreeSpinsPresentation.svelte` is the surface by name; not opened, source budget spent on the eight findings above and below that could be closed to a line. Not locked.
- Proposed fix: drive the card's own opacity from the same transition as its backdrop so they leave together, and hold the board and the win-line cycler until the gate is actually dismissed; and lift `+16 FREE SPINS` clear of the gauge, or shrink the gauge, so no glyph overlaps the dial.

## STM-MOBILEM-08 MEDIUM The big win count-up ring is not clipped to its band and paints across the live symbol rows

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/272_mobile-m_transition_bigwin_countup_early.png`. Endpoints opened: `273_mobile-m_transition_bigwin_countup_late.png` and `274_mobile-m_bigwin_settled.png`.
- Claim: on 272 only, a dashed grey ring about 430px across (about 180px native) is centred on the banner band and extends roughly 55px (23px native) above and below it, so its arcs cross the top symbol row and the bottom symbol row of a live board. It is gone by 273. The source names it and explains the escape: `.c1-shockwave` is `position: absolute; top: 50%; left: 50%; z-index: 1` with `animation: c1-shockwave-burst 0.6s ease-out both` (`frontend/src/lib/components/WinBanner.svelte:451-454`), sized `260px` square for this tier (`frontend/src/lib/components/WinBanner.svelte:455`), and its parent `.c1-plate-wrap` is `position: relative` with no `overflow` (`frontend/src/lib/components/WinBanner.svelte:442`). A 260px ring centred on a band roughly 131px tall therefore overhangs it by about 65px at each end with nothing to clip it. A decorative element escaping its container onto live symbols, for the length of the count-up.
  **RECORDED AS A CORRECTION, because it is the point of checking source.** My first reading of these frames raised two further claims at MEDIUM: that the banner band overhangs the reel window horizontally so the window's neon border is severed between y=335 and y=650, and that rows 2 and 3 of the board are hidden for the whole celebration. Both observations are accurate and **both are deliberate**. `.big-win-banner` is `position: absolute; left: 0; right: 0; width: 100%` at `frontend/src/lib/components/WinBanner.svelte:342-352`, under a comment at `frontend/src/lib/components/WinBanner.svelte:339-341` that states the intent in terms: *stage edge to edge, vertically centred on the grid at stage y=310, no longer a centred box, so reels stay visible above and below*. The band covering the middle two rows and leaving the outer two is the stated design, not a defect, and the severed border is its direct consequence. They are withdrawn as findings and recorded here so no later squad raises them cold.
- Resolution note: NEW AT 1600PX. The ring is low-contrast grey on near-black and is not resolvable at thumbnail scale.
- Where fixable: `frontend/src/lib/components/WinBanner.svelte:442` (the wrapper) or `:451-457` (the ring). Not locked.
- Proposed fix: add `overflow: hidden` to `.c1-plate-wrap`, or move `.c1-shockwave` into the band element so it is clipped by the surface it is bursting from.

## STM-MOBILEM-09 MEDIUM After the feature the base game repaints the trigger spin's win lines and breakdown under a total that includes the feature

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/306_mobile-m_transition_feature_exit.png`. Endpoint opened: `307_mobile-m_post_feature_base.png`.
- Claim: on feature exit the base game restores the **trigger** board, the five scatter bursts and the five cyan win-line polylines that go with them, and restores the breakdown strip with them. 306 reads `SCATTER  x5  5 ways  $10.00` under a `WIN` pod counting through `$324.86`; 307 reads `L2  x5  1 ways  $0.80` under `WIN` `$363.89`. So for the whole of the exit the on-board illustration and the strip enumerate a `$10.00` line and then a `$0.80` line while the pod shows a figure two orders of magnitude larger, and the breakdown can never sum to the total because it is the breakdown of a different spin.
- Resolution note: NEW AT 1600PX. The strip is about 8px tall at native and its content is not transcribable at thumbnail scale, which is what the claim rests on.
- Where fixable: UNKNOWN for the restore behaviour. The breakdown surface itself is `frontend/src/lib/components/WinBreakdown.svelte`; what re-drives it on feature exit is elsewhere and was not traced. Not locked.
- Proposed fix: suppress the breakdown cycler and the win lines while the round total is a feature total, or label the strip with the spin it describes.

## STM-MOBILEM-10 MEDIUM The win-line breakdown pluralises unconditionally and prints raw internal symbol codes

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/273_mobile-m_transition_bigwin_countup_late.png` (`L3  x4  1 ways  $0.20`), `275_mobile-m_transition_menu_opening.png` (same string), `297_mobile-m_transition_feature_entry_fade.png` (`L2  x5  1 ways  $0.80`). Endpoints carrying the correct plural: `274_mobile-m_bigwin_settled.png` (`M3  x5  8 ways  $16.00`), `299` and `300` (`SCATTER  x5  5 ways  $10.00`), `307` (`L2  x5  1 ways  $0.80`), `311` (`M3  x3  1 ways  $0.20`).
- Claim: two defects in one 8px-tall bar under the reels.
  1. **The unit is unconditional**, so a line paying on a single way reads `1 ways`. `<span class="wb-ways">{current.ways} ways</span>` at `frontend/src/lib/components/WinBreakdown.svelte:94`. The multi-way cases are correct, so the singular alone is wrong. It reaches a frame only while the cycler is holding a one-way line, which is why it belongs on a motion sweep: the settled frame of a multi-way win does not contain it.
  2. **The symbol column prints the engine's internal codes.** `L2`, `L3` and `M3` are book symbol identifiers, not player-facing names, and they sit beside `SCATTER`, which is a word. The count is written `x5`, `x4`, `x3` with the ASCII letter, against `×` on the rules card, the buy dialogs and the max win hero in the same session. Independently reached from the Arabic session by the localisation lens, which is corroboration from a genuinely different input.
- Resolution note: VISIBLE AT BOTH as defects. **RECORDED AS A CORRECTION:** the first version of this shard claimed that at full resolution the singular renders with its space closed, as `1ways`. That is wrong. `frontend/src/lib/components/WinBreakdown.svelte:94` has a literal space, and a 3x crop of the bar on 273, 297 and 307 shows the space present. The native shard's transcription `1 ways` is correct and my refinement of it is withdrawn. What does survive as a correction is the amount: the native shard reads the 297 line as `$0.60`; at 1600px it is `$0.80`, which the same line on 307 confirms.
- Where fixable: `frontend/src/lib/components/WinBreakdown.svelte:94`. Not locked.
- Proposed fix: select the singular or plural on the count and route it through the locale table rather than a literal; map the symbol code to its display name; and write the count with `×`.

## STM-MOBILEM-11 MEDIUM The max win hero collides the multiplier with its unit: `5,000×BET`, three colours and two sizes, effectively no gap

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/308_mobile-m_transition_maxwin_overlay_fade.png`. Same state settled: `309_mobile-m_maxwin_celebration.png`.
- Claim: the hero line renders `5,000` in yellow at roughly 72px cap height in the 1600px render, then `×` in orange at roughly 28px, then `BET` in grey at roughly 34px, with no perceptible gap between the `×` and the `B`, so the compound reads as one collided token. Three colours and two sizes inside a single unit, on the title's biggest surface. Derived from source rather than eyeballed: the three spans are adjacent at `frontend/src/lib/components/MaxWinCelebration.svelte:155-159`; the wrapper is `display: flex; align-items: baseline; gap: 0.1em` at `frontend/src/lib/components/MaxWinCelebration.svelte:295-299`. **`gap` resolves `em` against the flex container's own font size, not against its children's**, so a `0.1em` gap sits beside a `.c1-max-mult` of `50px` and a `.c1-max-x` of `24px` at this viewport (`frontend/src/lib/components/MaxWinCelebration.svelte:367-368`), and is a couple of pixels. The colours are `--sig-gold` on the number (`:305`) and `--sig-orange` on the sign (`:312`), and being a flex container the whitespace between the spans is discarded as well.
  This is the English twin of the localisation squad's `5,000×EINSATZ` finding on the German max win hero, which means the defect is in the composition of the hero rather than in the German string, and fixing the German string alone would leave it.
- Resolution note: NEW AT 1600PX. The native shard read this same frame and cited `5,000×` from it as an example of CORRECT multiplication-sign usage; the collision and the size and colour breaks are below thumbnail resolution.
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:298` (the gap) and `:301-315` with `:367-368` (the sizes and colours). Not locked.
- Proposed fix: set the gap in a unit that does not depend on the wrapper's inherited size, for example `gap: 0.12em` moved onto a wrapper whose own `font-size` matches the numeral, or a plain `gap: 8px` with a mobile step; and set the `×` and the unit at one size and one colour so the compound reads as one quantity.

## STM-MOBILEM-12 MEDIUM Two sibling buy dialogs disagree on casing: `Buy Overdrive` against `NITRO OVERDRIVE`

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/293_mobile-m_transition_dialog_buy_overdrive_opening.png` (`Buy Overdrive`) and `295_mobile-m_transition_dialog_nitro_overdrive_opening.png` (`NITRO OVERDRIVE`). Endpoint opened: `294_mobile-m_dialog_buy_overdrive.png`, same string.
- Claim: the same component slot, on two dialogs a player reaches from the same menu one tap apart, renders one title in title case and the other in all caps, in the same magenta at the same size. It is the shape KNOWN_OPEN row **Q-34** describes for `Cruise`/`CRUISE`, on a pair Q-34 does not enumerate. The strings are literals one line apart: `modeBonusLabel: 'Buy Overdrive'` at `frontend/src/lib/i18n/prose.ts:91` and `modeSuperLabel: 'NITRO OVERDRIVE'` at `frontend/src/lib/i18n/prose.ts:93`, with `modeCruiseLabel: 'Cruise'` at `:87` in the same block; `fsModes.ts` reaches them by `labelKey` (`frontend/src/lib/config/fsModes.ts:101` and `:111`). So this is not a `text-transform` drift like Q-34's, it is two source literals written in two conventions, and no gate reads it.
- Resolution note: NEW AT 1600PX. At thumbnail scale both titles are a magenta bar.
- Where fixable: `frontend/src/lib/i18n/prose.ts:87,91,93`, and the same three keys across the other fifteen locales. Not locked.
- Proposed fix: PARK(which direction to normalise is an art call and it is the same call Q-34 is already waiting on; settle both together rather than separately, since a decision on one that is not applied to the other leaves the same defect with a different pair).

## STM-MOBILEM-13 MEDIUM Nothing on the board carries motion blur at full speed, and the window clips a spinning symbol with a hard straight edge

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/267_mobile-m_transition_reels_full_speed.png`. Endpoints opened: `265_mobile-m_base_idle.png` and `266_mobile-m_transition_reels_accelerating.png`.
- Claim: on the frame the manifest calls `Reels at full speed`, reels 1 to 4 sit on exactly the row pitch of the settled grid in 265, each showing four whole symbols, pin sharp, with no streak or blur of any kind. Only reel 5 is off that pitch, and it is the only reel whose symbols are cut by the window: a piston is clipped to its lower third at the top edge and another is clipped at the bottom edge, in both cases by a **hard straight line directly against the neon frame**, with no mask, no fade and no feather. A studio reel window fades or masks its top and bottom few pixels precisely so a symbol never terminates on a hard edge. `grep -rn "mask-image\|-webkit-mask" frontend/src/lib/` returns nothing, so this is a class rather than an instance, which is the same result the composition lens reached from a different surface.
- Resolution note: NEW AT 1600PX. The pitch offset is a few native pixels and the hard clip is a one-pixel boundary; neither is resolvable at thumbnail scale.
- Where fixable: `frontend/src/lib/components/GameGrid.svelte:1235` (`.reel-strip`) and its containing window. Not locked.
- Proposed fix: mask the reel window's top and bottom edges so a partial symbol fades out rather than terminating on the frame. Whether to add motion blur is an art call and is not proposed here.

## STM-MOBILEM-14 LOW The buy dialog open overshoots asymmetrically, which a centred scale cannot produce

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/293_mobile-m_transition_dialog_buy_overdrive_opening.png` against endpoint `294_mobile-m_dialog_buy_overdrive.png`.
- Claim: measured by scanning column x=450 for the magenta border, 293's panel spans y=61 to y=1527 and 294's spans y=79 to y=1520 in the 1600px render. So 293 is **1466px tall against 1441px, a scale of 1.0173**, and its centre sits **5.5px higher** (2.3px at native). The native shard derived the peak scale as `1.0137` from `animation: buy-pop 0.28s cubic-bezier(0.34, 1.56, 0.64, 1)` (`frontend/src/lib/components/BuyBonus.svelte:180`) over `@keyframes buy-pop { from { transform: scale(0.86) } to { transform: scale(1) } }` (`:182`), which I re-read and confirm as the only transform on the panel. Two things follow. The measured scale is a little above the derived peak, which is within what a single frame near the peak can explain. **The 2.3px centre shift is not**: a `scale()` with a default `transform-origin` is symmetric about the element's centre and cannot move it, so something other than the keyframe is moving this panel, and the frames alone cannot say what. Recorded at LOW because nothing leaves the viewport and the whole event is about 100ms; recorded at all because a measurement that a derivation does not predict is the useful kind (convention l.2).
- Resolution note: NEW AT 1600PX as a measurement. A 4px against 10px distinction, and a 2px centre shift, are not available at thumbnail scale.
- Where fixable: `frontend/src/lib/components/BuyBonus.svelte:180-182`. Not locked.
- Proposed fix: nothing is required for the overshoot itself. The unexplained centre shift wants one look at what else transforms or re-lays-out the panel during `buy-pop`, since `max-height: 90dvh` (`:174`) puts the panel at its cap and a scaled child of a flex-centred parent that also scrolls is the usual source of a shift like this.

## STM-MOBILEM-15 LOW Five panel and overlay transitions are indistinguishable from their settled endpoints

- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/275_mobile-m_transition_menu_opening.png` against `276`, `278_mobile-m_transition_paytable_opening.png` against `279`, `291_mobile-m_transition_features_menu_opening.png` against `292`, `297_mobile-m_transition_feature_entry_fade.png` against `298`, and `289_mobile-m_transition_paytable_closing.png`, which shows base chrome with no trace of the paytable at all.
- Claim: at 1600px I can say more than the native pass could. Each of those four pairs differs **only** in the pulse phase of the wheel and burst symbols on the board behind, and in no property of the panel itself: position, size, opacity, border glow, the yellow rail on the paytable and features panels, and every string are identical. The same capture pass DID land inside animations elsewhere on this session (the buy dialogs mid-scale on 293 and 295, the reels mid-acceleration on 266, the count-up mid-figure on 272), so the harness is capable of catching a mid-state here. The reading consistent with all of it is that these four panels appear with no entrance motion and the paytable leaves with no exit motion. Stated as an inference and not a measurement: one frame per transition cannot establish a duration.
- Resolution note: NEW AT 1600PX for the strength of the claim rather than for the claim.
- Where fixable: UNKNOWN
- Proposed fix: PARK(settling this needs the capture delays out of the harness and the transition durations out of each component; if the panels genuinely have no transition then adding one is an art call about house feel rather than a repair).

## Native pass reconciliation

Every finding in `reports/qa/stream_test/shards/superseded/STM-MOBILEM.md` falls inside my frame range, because that shard covered the same 18 frames. There are no sibling-squad findings to ignore.

- **STM-MOBILEM-01 (native), collect wash: CONFIRMED, with one component REFUTED.** The wash, the total absence of any overlay, the feature chrome arriving before its card and the whole-page translation are all confirmed at full resolution. I measured the translation independently at 12px left and 10px down in the 1600px render, **5.0px left and 4.2px down at native**, against the shard's "roughly 5px left and 4px down": agreement to within a pixel, from a different method. I add that the frame height is identical either side (688px), so it is a pure translation and not a scale, and that a 5px-native strip of scene background is exposed at the right edge. **REFUTED within it:** the shard describes endpoint 311 as "a normally lit board". It is not. Frame 311 shows the feature entry card at full opacity (`OVERDRIVE FREE SPINS`, `+8 FREE SPINS`, `TAP TO CONTINUE`) over a dimmed board, with the strip `M3  x3  1 ways  $0.20`. The shard contradicts itself on this inside one paragraph and the second reading is the correct one.
- **STM-MOBILEM-02 (native), buy dialogs with no CONFIRM or CANCEL: CONFIRMED.** Carried forward as my 06. I re-read every source line it cites rather than inheriting them and all four hold. I did not open 296 so I make no claim on it.
- **STM-MOBILEM-03 (native), rules card composites as one subtree: CONFIRMED and REFINED.** Every string the shard names is legible at 1600px and the mechanism it derives is right, `intro-fade-in` at `frontend/src/lib/components/IntroSplash.svelte:147` being a plain opacity keyframe on the parent of the card. I add two observations it does not have: `FEATURES` reading through as `URES` behind the third bullet, and the whole bottom control bar at full brightness in 262 against nearly black in 263, so the modal is fully formed over an apparently live SPIN button. Carried forward as my 04.
- **STM-MOBILEM-04 (native), overlay panels 96 to 99 per cent opaque: CONFIRMED, and split.** Both halves survive and both source figures are correct (`HudOverlay.svelte:1603`, `MaxWinCelebration.svelte:194-198`). I have separated them because they are two components with two severities: the HUD menu is my 05 at HIGH, the max win overlay is my 03 at STREAM, since it is the game's biggest surface and the overprint lands on the caption itself.
- **STM-MOBILEM-05 (native), entry card at full opacity over a board that has moved on: CONFIRMED and STRENGTHENED.** The shard declined to claim anything about 300 because it did not open it. I opened it: **300 is indistinguishable from 299**, so the card is still fully painted at the frame the manifest calls "in flight, interval frame 1 of 6". I add `+16 FREE SPINS` printed over the gauge art, which the native pass could not have resolved. Carried forward as my 07.
- **STM-MOBILEM-06 (native), `PRESS COLLECT OR HIT ENTER TO CONTINUE` on a phone viewport: CONFIRMED as an observation.** The string renders on 308 and 309 exactly as transcribed and the source line is right. I have not carried it as a numbered finding of mine because it is a copy-fit question rather than a motion one and the string is already a KNOWN(Q-16 park) item; it is recorded under KNOWN matches below with the mobile-specific objection intact, so nothing is lost.
- **STM-MOBILEM-07 (native), empty cells mid-acceleration: CONFIRMED, and severity REFINED upward from MEDIUM to STREAM.** The cell counts are exactly right: 1 plus 2 plus 3 equals six voids. What changes the severity is what full resolution shows inside those voids, identifiable magenta chevron and rain artwork from the scene backdrop, which makes this a board with no backing rather than a board with missing symbols, on the surface a viewer watches for the whole session. Carried forward as my 02.
- **STM-MOBILEM-08 (native), `1 ways`: CONFIRMED, and my own attempt to refine it REFUTED.** The defect is real and the source line is right. I initially recorded that the space closes up at full resolution, to `1ways`; that is wrong, `frontend/src/lib/components/WinBreakdown.svelte:94` carries a literal space and a 3x crop shows it rendered. **The native transcription stands and mine is withdrawn.** One transcription in the native shard does not survive: the 297 line is `$0.80`, not `$0.60`, confirmed against the identical line on 307. Carried forward as my 10, with the raw symbol codes and the ASCII `x` added.
- **STM-MOBILEM-09 (native), buy dialog overshoot of about 4px: REFINED.** The derivation is sound as far as it goes and the two source lines are right. Measured, the panel is 1.0173 of its resting size against the derived peak of 1.0137, and its centre is 2.3px native higher. **A `scale()` about the default origin cannot move a centre**, so the derivation does not account for the shift and something else is moving the panel. Recorded as an open thread rather than a solved one, still LOW. Carried forward as my 14.
- **STM-MOBILEM-10 (native), five transitions indistinguishable from their endpoints: CONFIRMED and STRENGTHENED.** At full resolution the pairs differ in exactly one property, the pulse phase of the wheel and burst symbols behind the panel, and in no property of the panel itself. Carried forward as my 15.
- **The native shard's "Explicit absences": one is REFUTED.** It signs "No z-order inversion found", having "specifically checked ... the COLLECT button and its glow in 308". At 1600px, 308 carries celebration particles rendered **in front of** the `MAX WIN REACHED!` headline: a magenta dot in the gap between the `X` and the `W`, a peach dot on the lower stroke of the `I` of `WIN`, and a yellow dot over the middle of the three stars above. That is a z-order relationship on the celebration's own primary message. I have not raised it as a numbered finding because the same relationship holds in the settled 309 and the dots are small, so it may be the intended depth; it is recorded here so the absence is not signed a second time without being looked at.
- **My own first readings, refuted by source and withdrawn, recorded so they are not re-raised:** the big win band's edge-to-edge overhang and its covering of the middle two symbol rows are deliberate and commented at `frontend/src/lib/components/WinBanner.svelte:339-352`; and the `1ways` spacing claim above. Two withdrawals and one native transcription corrected is the return on checking source before signing, and it is the reason the shard is written in this order.
- **Not in the native shard at all, and new here:** my 08 (the unclipped shock ring), 09 (post-feature breakdown against the round total), 11 (`5,000×BET` collision and its `gap: 0.1em` mechanism), 12 (`Buy Overdrive` against `NITRO OVERDRIVE`), 13 (no mask at the reel window edge).

## Explicit absences, signed

Signed for the 18 `transition_` frames of the `mobile-m` session, each opened once at 1600px, each compared against at least one settled frame beside it.

- **No element caught mid-teleport.** On every transition frame each surface was at a position on the path between its two endpoints, never elsewhere. The two positional differences found are bounded, measured and recorded: the whole-page 5.0px-left, 4.2px-down native translation on 310 (my 01) and the 2.3px native centre shift on 293 and 295 (my 14). Checked by naming, for each frame, which surface had moved against which endpoint.
- **No ghost of a dismissed surface found on the close transitions.** 289 (`paytable_closing`) shows base chrome with no paytable remnant at any opacity: no panel border, no yellow rail, no residual scrim, and the reel window, `FEATURES` bar and control row are all at their settled brightness. 310 shows no max win overlay remnant of any kind. Checked at full resolution specifically for a low-alpha remainder, which is the form a thumbnail would hide, and which is why this absence is worth more at 1600px than at native.
- **No orphaned shadow or glow found.** Specifically checked for an element that had moved with its shadow left behind, on every pair where something did move: the SPIN button and its ring on 266 and 267, the reel frame and its neon glow on 306 and 310, the dialog panel and its magenta rim on 293 and 295, the COLLECT button and its glow on 308, the banner band and its `band-edge` glow rules (`frontend/src/lib/components/WinBanner.svelte:372-378`) on 272 and 273. In every case the glow travelled with the element. The reel frame's outer bloom on 310 sits 12px off where 311 puts it, and that is inside my 01's page translation rather than a separate orphaning.
- **No wrong-layer blur.** The only blurred surfaces anywhere in the eighteen frames are the base game behind the rules card on 262 and behind the paytable on 278. On 262 the blur is composited below full opacity, which is my 04; it is not applied to the wrong layer. No frame showed a foreground surface blurred.
- **No z-order inversion between panels and chrome.** Checked on 262, 275, 278, 289, 291, 293, 295, 297, 299, 308 and 310 by naming which surface occluded which and comparing against the settled endpoint. No HUD control painted over a modal, no reel symbol over a banner, no bottom bar over the paytable. The one z-order question found is within a single surface, the particles over the max win headline, and it is recorded in the reconciliation above rather than buried here.
- **No pop found on the surfaces that do animate.** The buy dialogs (293, 295), the reels (266, 267) and the count-up (272, 273) all show genuine intermediate states. The pop claim is confined to the five surfaces in my 15 and is stated there as an inference, not a measurement.
- **Not claimed, and why.** The `BALANCE` pod reads `$50,000.00` on all eighteen transition frames and on every settled frame I opened, including 307 after `$363.89` and 311 after `$5,000.00`. Frames alone cannot separate a frozen mock wallet from a defect, so I am not raising it; a squad with the harness configuration settles it in one look.
- **Not claimed, and why.** The elliptical light field on 308 and 309 has a visible curved boundary that sweeps across the viewport and is in a different position in each frame. `frontend/src/lib/components/MaxWinCelebration.svelte:194-198` is a `radial-gradient(ellipse at center, ...)`, which is consistent with an intended soft field rather than a hard mask, and two frames cannot tell a rotating gradient from a mask edge. Named rather than guessed at.
- **Not audited, and named.** I did not open 261, 268, 269, 270, 271, 277, 280 to 288, 290, 296, or 301 to 305. They are settled frames belonging to the composition and typography lenses and none is an endpoint of a transition I judged, except 271 and 296, which I name explicitly so nobody reads my 06 or my 08 as covering them.

## KNOWN matches

- **KNOWN(MID-01)**: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/272_mobile-m_transition_bigwin_countup_early.png`. Fresh `mobile-m` evidence for the same divergence: the banner reads `$10.28` while the HUD WIN pod reads `$15.95`, on a win that settles at `$16.20` in `274_mobile-m_bigwin_settled.png`. The ledger's desktop row records `$10.29` and `$15.95` on frame 013; the mobile-m banner is one cent lower at the captured instant and the pod figure is identical, which is what one shared easing over two different durations predicts. At 1600px both figures are unambiguous, which they were not at 334 tokens.
- **KNOWN(MID-02)**: `272_mobile-m_transition_bigwin_countup_early.png` and `273_mobile-m_transition_bigwin_countup_late.png`, both rendering `16x BET`, against `1×`, `3×`, `10×` and `100×` on the rules card in 262, `100×` and `5,000×` in the buy dialogs on 293 and 294, and `5,000×` on the max win hero in 308, all in the same session. Recorded beside it as a widening of MID-02's own class: the win-line breakdown bar writes the count as `x5`, `x4`, `x3` with the ASCII letter too (my 10), on a surface `WinBanner.svelte` does not own.
- **KNOWN(Q-26)**: `291_mobile-m_transition_features_menu_opening.png` and `295_mobile-m_transition_dialog_nitro_overdrive_opening.png`. The row asks whether the `fsModes.ts` survivors reach a frame, and they do. On 291: `1× bet` on the Normal and Cruise cards, `1.25× bet` on the OVERBOOST card and `1.25× per spin while ON · $1.25` in its footer all use the correct sign, while `Double-chance: about 1.6x the feature trigger rate. Debits 1.25x every spin while ON.` in the body of that same card uses the ASCII letter twice. **The two forms are eight lines apart inside one card.** On 295: `Buy a rich entry with the Overdrive meter pre-revved to 5x.` is the fourth survivor. Both are surfaces a player opens in order to spend money. The park's urgency changes accordingly: these are not config strings that never surface.
- **KNOWN(Q-16 park)**: `308_mobile-m_transition_maxwin_overlay_fade.png` carries `PRESS COLLECT OR HIT ENTER TO CONTINUE`, one of the park's enumerated strings, on a stream frame, rendered from `maxWinHint` via `frontend/src/lib/components/MaxWinCelebration.svelte:166`. This session is English so the park's language urgency is unchanged by it. Recorded beside it, because it is a separate objection to the same string: on a 375x667 session there is no Enter key, so half the instruction is unfollowable on the device this viewport represents.
- **KNOWN(Q-34), partial**: `291_mobile-m_transition_features_menu_opening.png` renders `Cruise` in title case on the features menu, which is the half of Q-34's pair this frame set can see (`modeCruiseLabel: 'Cruise'`, `frontend/src/lib/i18n/prose.ts:87`). No frame in my set shows the HUD badge, so I confirm one side of the row and not the other. My finding 12 is a second instance of the same class on a pair Q-34 does not enumerate, and unlike Q-34's it is two source literals rather than a `text-transform` difference.

tree_after:
```
?? reports/qa/stream_test/shards/STC-MOBILEL-1.md
?? reports/qa/stream_test/shards/STC-MOBILEL-2.md
?? reports/qa/stream_test/shards/STC-MOBILEL-3.md
?? reports/qa/stream_test/shards/STC-MOBILEM-1.md
?? reports/qa/stream_test/shards/STC-MOBILEM-2.md
?? reports/qa/stream_test/shards/STC-MOBILEM-3.md
?? reports/qa/stream_test/shards/STC-MOBILES-2.md
?? reports/qa/stream_test/shards/STC-POPOUTS-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-2.md
?? reports/qa/stream_test/shards/STC-POPOUTS-3.md
?? reports/qa/stream_test/shards/STM-MOBILEL.md
?? reports/qa/stream_test/shards/STM-MOBILEM.md
?? reports/qa/stream_test/shards/STM-POPOUTS.md
?? reports/qa/stream_test/shards/STT-MOBILEL-1.md
?? reports/qa/stream_test/shards/STT-MOBILEL-2.md
?? reports/qa/stream_test/shards/STT-MOBILEL-3.md
?? reports/qa/stream_test/shards/STT-MOBILEM-1.md
?? reports/qa/stream_test/shards/STT-MOBILEM-2.md
?? reports/qa/stream_test/shards/STT-MOBILEM-3.md
?? reports/qa/stream_test/shards/STT-MOBILES-1.md
?? reports/qa/stream_test/shards/STT-MOBILES-2.md
?? reports/qa/stream_test/shards/STT-POPOUTS-1.md
?? reports/qa/stream_test/shards/STT-POPOUTS-2.md
?? reports/qa/stream_test/shards/STT-POPOUTS-3.md
```

**Clean. Nothing is MODIFIED and nothing is DELETED.** Every entry is `??`, untracked: twenty-three other squads' shards, which are theirs and not my concern, and `reports/qa/stream_test/shards/STM-MOBILEM.md`, which is mine and is the only file I wrote. I ran no project script, wrote nothing into `reports/screens/`, and touched no source file; the only source access was `grep -n` and `sed -n` reads.

Recorded because it closes a loop: the superseded native shard signed off with `M reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png` in its tree and raised it loudly as a committed evidence frame that something had rewritten. **It is no longer modified.** Whoever restored it, the audited artefact set now matches HEAD again, so the marshal's blocker on consolidating any `popout-s` finding against frame 188 is cleared. Stated as an observation of the tree, not a claim about who restored it or when.
