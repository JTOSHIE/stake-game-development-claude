# STM-MOBILEM, motion residue (mobile-m, frames 260 to 311)
scope: every `transition_` frame of the `mobile-m` session (375x667, lang en), 18 of 18 covered. Settled endpoints 261, 263, 265, 271, 274, 276, 279, 290, 292, 294, 296, 298, 300, 307, 309, 311 were opened for comparison and do not count against the set.
frames_read: 18

## STM-MOBILEM-01 STREAM The collect transition passes through a full-board mustard wash that neither endpoint contains

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/310_mobile-m_transition_maxwin_collect_fade.png`. Endpoints: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/309_mobile-m_maxwin_celebration.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/311_mobile-m_post_collect_base.png`.
- Claim: the manifest calls frame 310 `Collect pressed, overlay mid-fade-out`. There is no overlay in it at all: the MAX WIN surface has already gone completely, with no partial opacity anywhere. What is there instead is a solid yellow-green radial wash filling the entire reel window, under which all twenty symbols are reduced to near-black ghosts and the win-line bar has disappeared. Neither endpoint contains a wash: 309 is the celebration, 311 is a normally lit board. Three further things are wrong only in this frame, and they compound: the feature chrome has already switched to `OVERDRIVE FREE SPINS` / `8` and `TOTAL WIN` / `$2.80`, which 309 does not show, while the free-spins entry card that 311 shows (`OVERDRIVE FREE SPINS`, `+8 FREE SPINS`, `TAP TO CONTINUE`) is absent, so the feature HUD arrives before its own card; and the whole page is translated roughly 5px left and 4px down against 311, read off the SPIN button centre and the `BALANCE` pod left edge, so the reel frame's left border reaches x=0 in 310 where it sits at x=8 in 311. The wash could be a deliberate collect flash, and it is recorded here with that possibility named: what argues against it is that the flash is confined to the reel window while the HUD, the feature pods and the bottom bar stay at normal brightness, the hue is olive rather than the gold the celebration itself uses, and it lands on a board that is otherwise mid-rebuild. What an audience sees at the single most-watched moment in the game is the board turning to unreadable mustard.
- Where fixable: UNKNOWN. The surface is `frontend/src/lib/components/GameGrid.svelte`. One candidate was checked and EXCLUDED so the next reader does not repeat it: the celebratory beat `escalate-flash` at `frontend/src/lib/components/GameGrid.svelte:1571-1589` is `filter: brightness(var(--flash-peak))` peaking at `2.0`, which brightens the existing symbols and cannot produce a yellow field over darkened ones.
- Proposed fix: PARK(the wash has to be identified in source before anything is proposed; if it turns out to be the intended collect flash it wants a shorter, gold, whole-screen treatment rather than a reel-window-only olive one, and that is an art call).

## STM-MOBILEM-02 HIGH Neither buy dialog shows CONFIRM or CANCEL at 375x667, because the sticky fix pinned the disclosure row and left the actions below the fold

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/293_mobile-m_transition_dialog_buy_overdrive_opening.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/295_mobile-m_transition_dialog_nitro_overdrive_opening.png`. Endpoints: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/294_mobile-m_dialog_buy_overdrive.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/296_mobile-m_dialog_nitro_overdrive.png`.
- Claim: on all four frames the dialog runs icon, title, `Start Overdrive Free Spins now at 100x your bet?` (`400x` on the NITRO variant), the `WHAT YOU GET` box, the symbol row, then the stat bar `PRICE` `$100.00` / `RTP` `96.35%` / `MAX WIN` `5,000x base bet`, and then the panel closes. No confirm button, no cancel button and no close cross is anywhere on screen. The source says exactly why. `.buy-modal` is `max-height: 90dvh; overflow-y: auto` (`frontend/src/lib/components/BuyBonus.svelte:174`), which is `600px` in a 667px viewport, and the frames show the panel spanning about `y=33` to `y=632`, so it is at its cap and its content is overflowing. `.buy-stats-row` is `position: sticky; bottom: 0; z-index: 2` (`frontend/src/lib/components/BuyBonus.svelte:224-233`), so the disclosure row is pinned to the bottom of that scroll box and is guaranteed on screen. `.buy-actions` comes AFTER it in the markup (`frontend/src/lib/components/BuyBonus.svelte:141-146`) and is not sticky (`frontend/src/lib/components/BuyBonus.svelte:247`), so the two buttons sit in the overflowed remainder, underneath the pinned row, and a player who does not scroll never sees them. The R12 comment at `frontend/src/lib/components/BuyBonus.svelte:212-223` records this exact defect being measured and fixed for the disclosure row at 360x600, and notes `(390x664, the height the brief named, was already fine.)`. The fix moved the below-the-fold problem from the disclosure row onto the confirm row, and the height that was checked was 15px wider than this one. This is present in the transition and settled frames alike, so it is a fit failure rather than a motion one; it is reported because it is on my assigned frames and because a purchase confirmation with no visible confirm control is the most consequential thing in this shard after finding 01.
- Where fixable: `frontend/src/lib/components/BuyBonus.svelte:247` (`.buy-actions`), with the markup order at `frontend/src/lib/components/BuyBonus.svelte:138-146`. Not locked.
- Proposed fix: give `.buy-actions` the same `position: sticky; bottom: 0` treatment the stats row already has and move it into or directly below that pinned block, so the disclosure and the two controls are one sticky footer that is on screen at every supported height.

## STM-MOBILEM-03 HIGH The rules card fades in as one element with its own scrim, so HUD money reads through the card body

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/262_mobile-m_transition_splash_to_rules.png`. Endpoints: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/261_mobile-m_splash.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/263_mobile-m_intro_rules.png`.
- Claim: in the settled frame 263 the base game behind the rules card is dimmed and blurred and nothing behind it is legible. In the transition frame the base game renders sharp and bright behind AND THROUGH the card: `$50,000.00`, `$0.00`, `BET` and `$1.00` are all legible inside the card's own lower region, printed across it between the last bullet and the `Continue` button, and `WIN`, `MAX`, `SPIN` and the title `FUTURE SPINNER` are sharp outside it. That is text rendered over text on the game's gated first-run surface, and it is a state neither endpoint contains: endpoint one is the splash on black, endpoint two is the card over a blurred board. The mechanism is in one declaration. `.intro-backdrop` carries `background: rgba(0, 0, 0, 0.86)`, `backdrop-filter: blur(3px)` and `animation: intro-fade-in 0.35s ease both` (`frontend/src/lib/components/IntroSplash.svelte:46-48`), and `.intro-card` is its child (`frontend/src/lib/components/IntroSplash.svelte:66`). Animating opacity on the parent composites the whole subtree, so the card's own opaque `linear-gradient(160deg, #0c0c22 0%, #08081a 100%)` background is faded by exactly the same factor as the scrim and cannot be opaque while the scrim is not. The blur behaves the same way: a `backdrop-filter` layer drawn at partial opacity over the unblurred real backdrop reads as sharp, which is why the HUD text is legible rather than smeared.
- Where fixable: `frontend/src/lib/components/IntroSplash.svelte:46-48`. Not locked.
- Proposed fix: animate `background-color` and `backdrop-filter` on `.intro-backdrop` instead of the element's opacity, so the card underneath it is never composited below full opacity; or hold the backdrop at its settled values from frame one and fade only `.intro-card`.

## STM-MOBILEM-04 HIGH Overlay panels are 96 to 99 per cent opaque, so HUD money figures ghost through them and collide with overlay copy

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/275_mobile-m_transition_menu_opening.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/308_mobile-m_transition_maxwin_overlay_fade.png`. Same state settled: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/276_mobile-m_hud_menu.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/309_mobile-m_maxwin_celebration.png`.
- Claim: on the HUD menu the balance pod's `$50,000.00` is visible through the panel and lands on the same baseline as the menu row `Session`, so the two strings overprint. The panel also cuts the pods rather than covering them: `$16.20` in the WIN pod is left reading `16.20` with the dollar sign behind the panel edge, and the bet row is left reading `1.00`. On the max win overlay the same class: `$50,000.00` and `$1.00` read through the celebration and sit directly under the caption `PRESS COLLECT OR HIT ENTER TO CONTINUE`. The source gives the figures. `.hud-menu` is `background: rgba(6, 6, 18, 0.96)` with no `backdrop-filter` (`frontend/src/lib/components/HudOverlay.svelte:1598-1608`), so four per cent of a bright cyan money string comes through. The celebration backdrop is `radial-gradient(ellipse at center, rgba(20, 8, 50, 0.97) 0%, rgba(6, 4, 20, 0.99) 100%)` (`frontend/src/lib/components/MaxWinCelebration.svelte:194-198`), one to three per cent. Both are present in the settled endpoints as well as the transition frames, so this is not a transition-only artefact; it is reported because it is on my assigned frames and because the fade in finding 03 is the same defect made worse.
- Where fixable: `frontend/src/lib/components/HudOverlay.svelte:1603` and `frontend/src/lib/components/MaxWinCelebration.svelte:194-198`. Neither locked.
- Proposed fix: take both alphas to `1`, or add `backdrop-filter: blur(8px)` to `.hud-menu` so what shows through is unreadable by design rather than by accident; and size or place the menu panel so it does not bisect the balance, win and bet pods.

## STM-MOBILEM-05 HIGH The free-spins entry gate card is still painted at full opacity over a board that has moved on

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/299_mobile-m_transition_feature_starting.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/297_mobile-m_transition_feature_entry_fade.png`. Endpoints: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/298_mobile-m_feature_entry_card.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/300_mobile-m_feature_run_1.png`.
- Claim: the manifest calls 299 `Feature starting, entry card dismissing` and 300 `Overdrive free spins in flight, interval frame 1 of 6`. In both the entry card is at full opacity with no dismissal in progress: gauge, `+16 FREE SPINS` and `TAP TO CONTINUE` all fully painted. What changes between 297/298 and 299/300 is only the layer behind it: the dim under the card lightens so the reel symbols become legible through the overlay, and the win-line bar behind advances from `L2  x5  1 ways  $0.60` to `SCATTER  x5  5 ways  $10.00`. So the scrim fades out from under a card that stays at full opacity, and the card's copy ends up printed over live, readable board content and a still-cycling win-line readout. Either the card is a ghost of a surface already dismissed, or the explicit continue gate is not gating while the round underneath runs on; both readings are defects and the frames cannot separate them. I did not open 301 and beyond, which are outside the endpoints of my transitions, so I make no claim about how long the card persists.
- Where fixable: UNKNOWN. `frontend/src/lib/components/FreeSpinsPresentation.svelte` is the surface by name; not opened, source budget spent on findings 02, 03 and 04.
- Proposed fix: drive the card's own opacity from the same transition as its scrim so they leave together, and hold the board and the win-line cycler until the gate is actually dismissed.

## STM-MOBILEM-06 HIGH The max win overlay tells a phone player to hit a key that is not there

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/308_mobile-m_transition_maxwin_overlay_fade.png` and endpoint `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/309_mobile-m_maxwin_celebration.png`.
- Claim: the caption under the COLLECT button reads `PRESS COLLECT OR HIT ENTER TO CONTINUE` on a 375x667 session. There is no Enter key on the device this viewport represents, so half the instruction is unfollowable, and it is the instruction printed on the game's biggest celebration. The string is `maxWinHint:  'Press COLLECT or hit Enter to continue'` at `frontend/src/lib/i18n/prose.ts:83`, uppercased by the surface. KNOWN_OPEN records this same string in the Q-16 park as hardcoded English; the mobile problem is a different one, that its CONTENT is wrong for the viewport rather than its language.
- Where fixable: `frontend/src/lib/i18n/prose.ts:83`, rendered by `frontend/src/lib/components/MaxWinCelebration.svelte`. Not locked.
- Proposed fix: split the hint so a coarse-pointer session renders `Press COLLECT to continue` and only a keyboard session names the key; the keyboard shortcut keeps working either way and does not need to be on screen.

## STM-MOBILEM-07 MEDIUM Mid-acceleration the board renders empty cells beside untouched pre-spin symbols

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/266_mobile-m_transition_reels_accelerating.png`. Endpoints: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/265_mobile-m_base_idle.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/267_mobile-m_transition_reels_full_speed.png`.
- Claim: about 250ms after the spin press the grid is holed. Reel 1 carries three symbols and an empty fourth cell, reel 2 carries two and two empty, reel 3 carries one and three empty, while reels 4 and 5 still show the complete pre-spin board of pistons untouched. The empty cells are flat background, not a blurred strip, so the effect is a board with rectangular voids in it rather than reels in motion. The symbols on the three moving reels also sit at a visibly tighter vertical pitch than the four static rows beside them, so the moving and still columns do not line up. Neither endpoint has an empty cell.
- Where fixable: UNKNOWN. The surface is `frontend/src/lib/components/GameGrid.svelte`, the per-reel strip refs at `frontend/src/lib/components/GameGrid.svelte:182`; source budget spent elsewhere.
- Proposed fix: feed the spinning strip from a buffer that is filled before the reel starts, so no cell is ever drawn empty between the outgoing and the incoming symbol.

## STM-MOBILEM-08 MEDIUM The win-line readout writes `1 ways`

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/273_mobile-m_transition_bigwin_countup_late.png` (`L3  x4  1 ways  $0.20`) and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/297_mobile-m_transition_feature_entry_fade.png` (`L2  x5  1 ways  $0.60`). Also on endpoints `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/307_mobile-m_post_feature_base.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/311_mobile-m_post_collect_base.png`.
- Claim: the per-line breakdown bar under the reels pluralises unconditionally, so a line paying on a single way reads `1 ways`. The same bar reads `8 ways` and `5 ways` correctly on the multi-way cases (`M3  x5  8 ways  $16.00` in `274_mobile-m_bigwin_settled.png`, `SCATTER  x5  5 ways  $10.00` in `299`), so it is the singular case alone that is wrong. The source is one line: `<span class="wb-ways">{current.ways} ways</span>` at `frontend/src/lib/components/WinBreakdown.svelte:94`. It belongs on a motion sweep because it only exists while the cycler holds a one-way line: the settled frame of a multi-way win does not contain it.
- Where fixable: `frontend/src/lib/components/WinBreakdown.svelte:94`. Not locked.
- Proposed fix: `{current.ways} {current.ways === 1 ? 'way' : 'ways'}`, and route it through the locale table rather than the literal if the surrounding row is localised.

## STM-MOBILEM-09 LOW The buy dialogs overshoot their resting size on open by about 8px of height

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/293_mobile-m_transition_dialog_buy_overdrive_opening.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/295_mobile-m_transition_dialog_nitro_overdrive_opening.png`. Endpoints: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/294_mobile-m_dialog_buy_overdrive.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/296_mobile-m_dialog_nitro_overdrive.png`.
- Claim: derived from the specification first, per convention (l.1). `.buy-modal` opens with `animation: buy-pop 0.28s cubic-bezier(0.34, 1.56, 0.64, 1)` (`frontend/src/lib/components/BuyBonus.svelte:180`) over `@keyframes buy-pop { from { opacity: 0; transform: scale(0.86); } to { opacity: 1; transform: scale(1); } }` (`frontend/src/lib/components/BuyBonus.svelte:182`). The first control point's y value of `1.56` puts the curve above 1, so the scale passes through values LARGER than its resting value. That bezier peaks at `1.098` of its range at about t=0.58, giving a peak scale of `0.86 + 0.14 x 1.098 = 1.0137`. With `max-height: 90dvh` (`frontend/src/lib/components/BuyBonus.svelte:174`) the modal rests at `600px` in a 667px viewport, centred, so its borders rest at `y=33.5` and `y=633.5` and peak at `y=29.4` and `y=637.6`: about 4px past the resting edge at each end, for roughly 100ms.
  RECORDED AS A CORRECTION, because it is the point of a derivation. My first reading of frame 293 was that the overshoot had pushed the dialog's bottom border out of the 667px viewport entirely, and I wrote that into the first version of this shard at MEDIUM. The derivation refutes it: `637.6` is on screen, and the same derivation predicts the top border moving up about 4px, which is what the frame actually shows against 294. The specification is the authority (convention l.2), so the finding is the 4px overshoot and nothing more, and its severity is LOW.
- Where fixable: `frontend/src/lib/components/BuyBonus.svelte:180-182`. Not locked.
- Proposed fix: nothing is required. If the overshoot is unwanted on a panel that already fills 90 per cent of a short viewport, drop the ease to `cubic-bezier(0.34, 1.2, 0.64, 1)` or start the scale above `0.86` so the absolute overshoot in pixels is smaller.

## STM-MOBILEM-10 LOW Five panel and overlay transitions are indistinguishable from their settled endpoints

- Frames: `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/275_mobile-m_transition_menu_opening.png` against `276`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/278_mobile-m_transition_paytable_opening.png` against `279`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/291_mobile-m_transition_features_menu_opening.png` against `292`, `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/297_mobile-m_transition_feature_entry_fade.png` against `298`, and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/289_mobile-m_transition_paytable_closing.png`, which shows base chrome with no trace of the paytable at all.
- Claim: the frames the manifest labels `mid-open`, `mid-fade` and `mid-close` for the HUD menu, the paytable, the FEATURES menu and the feature entry overlay show those surfaces fully arrived or fully gone, at full opacity, at final position. In the same pass and the same session the harness DID land inside animations elsewhere: the buy dialogs are mid-scale in 293 and 295, the reels mid-acceleration in 266, the count-up mid-figure in 272. So the capture is capable of catching a mid-state on this session, and on these five surfaces it caught the endpoint every time. The reading consistent with all of it is that these panels appear and disappear with no entrance or exit motion, which is a pop. Stated as an inference and not a measurement: timing is approximate and one frame per transition cannot establish a duration.
- Where fixable: UNKNOWN
- Proposed fix: PARK(settling this needs the capture delays read out of the harness and the transition durations read out of each component; if the panels genuinely have no transition then adding one is an art call about house feel rather than a repair).

## Explicit absences, signed

Signed for the 18 `transition_` frames of the `mobile-m` session, each opened once, each compared against the settled frame either side.

- **No z-order inversion found.** On every transition frame the layering order matched the settled endpoints: no HUD control painted over a modal, no reel symbol over a banner, no bottom bar over the paytable. Checked on 262, 275, 278, 289, 291, 293, 295, 297, 299, 308 and 310, in each case by naming which surface occluded which and comparing to the endpoint. The defects found are opacity and scrim defects, not order defects.
- **No element caught mid-teleport.** No surface appeared at a position it does not occupy in one endpoint or the other. The two positional differences found are small and bounded: the 4px dialog overshoot (finding 09, derived) and the whole-page 5px translation in 310 (finding 01), and both move a surface along the path between its endpoints rather than to somewhere else.
- **No orphaned shadow, glow or backdrop found.** Specifically checked for an element that had moved with its shadow left behind: the SPIN button and its ring in 266 and 267, the reel frame and its neon glow in 306 and 310, the COLLECT button and its glow in 308, the dialog panel and its magenta rim in 293 and 295. In every case the glow travelled with the element.
- **No backdrop blur applied to the wrong layer.** `backdrop-filter` appears on exactly one surface in the frames, the base game behind the rules card, and the finding against it (03) is that it is composited below full opacity, not that it is on the wrong layer. No frame showed a blur applied to a foreground surface.
- **No ghost of a dismissed surface found on the close transitions.** 289 (`paytable_closing`) and 310 (`maxwin_collect_fade`) both show their dismissed surface completely gone with no partial remainder. Finding 05 is the inverse case, a surface that has not left, and 310's total absence of the max win overlay in a frame labelled mid-fade-out is recorded under finding 01.
- **Not claimed, and why.** The `BALANCE` pod reads `$50,000.00` on every frame of this session, including 307 and 311 after wins of `$363.89` and `$5,000.00`. I cannot tell a frozen mock wallet from a defect out of frames alone, so I am not raising it; a squad with the harness configuration settles it in one look.
- **Not claimed, and why.** The `WIN` pod reads `$5,000.00` while `TOTAL WIN` reads `$2.80` in 310 and 311. Same reason: the tail of this session is a fresh round starting over a max win, and the frames do not establish which figure each pod is meant to hold.

## KNOWN matches

- KNOWN(MID-01): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/272_mobile-m_transition_bigwin_countup_early.png`. Fresh mobile-m evidence for the same divergence: banner `$10.28`, HUD WIN pod `$15.95`, settling at `$16.20` in `274_mobile-m_bigwin_settled.png`. The desktop row records `$10.29` and `$15.95` on frame 013; the mobile-m banner is one cent lower at the captured instant and the pod figure is identical, which is what one shared easing over two different durations predicts.
- KNOWN(MID-02): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/272_mobile-m_transition_bigwin_countup_early.png` and `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/273_mobile-m_transition_bigwin_countup_late.png`, both rendering `16x BET` with the ASCII letter, against `1×`, `3×`, `10×` and `100×` on the rules card in `262` and `5,000×` on the max win overlay in `308`, same session.
- KNOWN(Q-26): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/291_mobile-m_transition_features_menu_opening.png`. The row asks whether the `fsModes.ts` survivors are visible on frames, and they are: `1x bet` twice on the Normal and Cruise cards, `1.25x bet`, `about 1.6x the feature trigger rate`, `Debits 1.25x every spin while ON` and `1.25x per spin while ON · $1.25`, all ASCII, on the surface a player opens to buy, on the same screen a player reaches from the `1×`/`100×` rules card. The park's urgency changes: these are not config-file strings that never surface, they are a purchase screen.
- KNOWN(Q-16 park): `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/308_mobile-m_transition_maxwin_overlay_fade.png` carries `PRESS COLLECT OR HIT ENTER TO CONTINUE`, one of the park's enumerated strings, on a stream frame. This session is English so the park's own urgency is unchanged by it; finding 06 above is a separate problem with the same string.

tree_after:
```
 M reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png
?? reports/qa/stream_test/shards/STC-MOBILEL-B.md
?? reports/qa/stream_test/shards/STC-MOBILES-A.md
?? reports/qa/stream_test/shards/STC-MOBILES-B.md
?? reports/qa/stream_test/shards/STC-STRETCH-A.md
?? reports/qa/stream_test/shards/STM-DESKTOP.md
?? reports/qa/stream_test/shards/STM-MOBILEM.md
?? reports/qa/stream_test/shards/STM-MOBILES.md
?? reports/qa/stream_test/shards/STT-DESKTOP-A.md
?? reports/qa/stream_test/shards/STT-DESKTOP-B.md
?? reports/qa/stream_test/shards/STT-LAPTOP-B.md
?? reports/qa/stream_test/shards/STT-POPOUTS-A.md
?? reports/qa/stream_test/shards/STT-POPOUTS-B.md
```

**LOUD, AND IT IS NOT A SHARD.** `reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png` is
**MODIFIED** in the working tree. It is a committed evidence frame in the capture set this
whole wave is auditing, and something in this session's window has rewritten it. That is the
SA-012 failure verbatim, the one convention (h.1) exists to prevent: *evidence that a casual
re-run can overwrite is not evidence.* It is not mine. I opened no `popout-s` frame, I ran no
project script, and I wrote exactly one file, my own shard, which shows correctly as
untracked. The other eleven untracked entries are other squads' shards and are expected.

Two consequences for the marshal, and the first one is urgent. **The audited artefact set is
no longer the committed one**, so any finding raised against frame 188 by whichever squad
holds `popout-s` is now a finding against a file that does not match HEAD, and it cannot be
consolidated until the frame is restored with `git checkout -- reports/screens/stream-test-2026-07-28/188_popout-s_features_menu.png`
and that squad re-reads it. **Restoring it is not mine to do**: this shard is read-only against
the repository bar its own path, and a squad quietly reverting another squad's working tree is
the same class of mistake as the write that caused this. Second, whatever wrote it wrote one
PNG in a directory of 519, so the cause is worth finding before the next wave rather than
after: a capture or proof script pointed at the committed directory is the shape that has
done this before.
