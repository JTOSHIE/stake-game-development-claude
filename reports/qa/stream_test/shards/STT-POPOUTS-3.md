# STT-POPOUTS-3, typography (popout-s, frames 191 to 207, 1600px upscaled)
supersedes: STT-POPOUTS-B.md (frames 183 to 207; my range 191 to 207 is its tail. STT-POPOUTS-A.md covers 157 to 182 and shares no frame with me)
scope: the `popout-s` frames numbered 191 to 207 inclusive, lang en, viewport `400x225` per `reports/screens/stream-test-2026-07-28/MANIFEST.json`, 17 frames, each opened exactly once
frames_read: 17

Frames were read from `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/`, which is the committed set at `/Users/jt/math-sdk/reports/screens/stream-test-2026-07-28/` resampled to 2844x1600. Every path below is the upscaled one, because that is what my eyes were on. The committed twin has the identical filename.

**The stage factor, because five findings below need it and it is one line of specification.** `frontend/src/App.svelte:746-756` lays the whole stage out at a fixed 1280x720 design surface and scales it by `S = min(vw/1280, vh/720)`. At Popout S that is `min(400/1280, 225/720)`, so **S = 0.3125**. Every `rem` figure quoted below is therefore multiplied by 0.3125 before it reaches a viewer. That conversion is what makes several of these findings arithmetic rather than opinion.

**Two of my own seven candidate findings did not survive the source pass and are withdrawn below rather than shipped.** They are in the signed absences, with the reason each is wrong.

## STT-POPOUTS-3-01 STREAM Every label on the feature instrument column is cut mid-word at the right edge, with no ellipsis, for the whole feature
- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/193_popout-s_transition_feature_entry_fade.png`, `194_popout-s_feature_entry_card.png`, `195_popout-s_transition_feature_starting.png`, `196_popout-s_feature_run_1.png`, `197_popout-s_feature_run_2.png`, `198_popout-s_feature_run_3.png`, `199_popout-s_feature_run_4.png`, `200_popout-s_feature_run_5.png`, `201_popout-s_feature_run_6.png`, `206_popout-s_transition_maxwin_collect_fade.png`, `207_popout-s_post_collect_base.png` (11 of my 17)
- Claim: the three instrument plates render `OVERDRIVE FR`, `TOTAL W` and `MULTIPLI`, each ending at the hard right edge of the frame with no ellipsis character and no fade. The first of the three is the feature's own name, and the entry card on the same screen renders that name in full as `OVERDRIVE FREE SPINS`, so one frame carries the complete string and its truncation at once. The values are cut on the same line: `16` survives, `$10.6` does not (193 to 201), `$2.8` does not (206, 207), `1x` survives. Eleven of my seventeen frames, and continuously for the whole feature.
  The source states the property that these frames disprove, which is why this is worth more than a layout row. `frontend/src/App.svelte:748-752` records that the single stage factor exists so that *every element (frame, grid, HUD, instrument column, scene group) shrinks or grows in lockstep and never overflows or clips at small popout sizes (for example Popout S, 400x225)*. The instrument column is named in that sentence and Popout S is named in that sentence, and it is clipped on eleven frames of the Popout S session. Something downstream of the stage factor is moving or resizing this column, because at S = 0.3125 the column's own bounds (`left: 1000px; width: 262px`, `frontend/src/lib/components/BonusInstrumentColumn.svelte:113-117`) resolve to a right edge at 394 device px, inside a 400px viewport. I did not find what does it and I am not guessing.
- Resolution note: VISIBLE AT BOTH
- Where fixable: `frontend/src/lib/components/BonusInstrumentColumn.svelte:113-117` (not locked), plus whatever overrides the stage transform for the mini-player profile in `frontend/src/App.svelte`, exact line UNKNOWN
- Proposed fix: PARK(the arithmetic says the column should fit and the frames say it does not, so the first job is diagnosis, not a CSS change). For the marshal: the in-component `text-overflow: ellipsis` on `.plate-value` cannot fire because the cut is outside the component, so no fit rule inside `BonusInstrumentColumn.svelte` can ever reach this.

## STT-POPOUTS-3-02 STREAM The buy confirm dialog puts nothing on screen but the last two words of its own question
- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/191_popout-s_transition_dialog_nitro_overdrive_opening.png`, `192_popout-s_dialog_nitro_overdrive.png`
- Claim: the only prose on either frame is `your bet?`, in the band below the opaque stats card. The head of the sentence is behind that card and the confirm and cancel controls are not in the frame at all. At 1600px the surviving fragment is fully formed: its glyphs are whole top and bottom. That matters, because the defect is not a sliced line, it is a question of which only the tail is on screen, sitting above a price of `$400.00` with no visible way to accept or decline it.
- Resolution note: VISIBLE AT BOTH
- Where fixable: `frontend/src/lib/components/BuyBonus.svelte:174` and `:224-233` (not locked)
- Proposed fix: remove the modal's bottom padding beneath the sticky `.buy-stats-row` so no content band can exist under it, and add `scroll-padding-bottom` equal to the row height so a text line can never park in that band.

## STT-POPOUTS-3-03 HIGH The max win unit renders as one collided token, `5,000xBET`, with its second half knocked back to 72 per cent opacity
- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/204_popout-s_transition_maxwin_overlay_fade.png`, `205_popout-s_maxwin_celebration.png`
- Claim: the hero line is three adjacent spans with no word space in any of them: `<span class="c1-max-mult fs-num">5,000</span><span class="c1-max-x">×</span>` on one source line and `<span class="c1-max-betlabel">{t($locale, 'bet', localeMode)}</span>` on the next (`frontend/src/lib/components/MaxWinCelebration.svelte:155,159`). The first two are butted together in the markup, and the whitespace before the third is stripped because the wrapper is `display: flex` (`:295-300`). The only separation is the wrapper's `gap: 0.1em` (`:298`), which resolves against the wrapper's inherited font size, not against the 50px numeral, so at Popout S it is roughly half a device pixel. The rendered string is `5,000xBET`.
  Full resolution adds the part the native pass could not see. `.c1-max-x` is `color: var(--sig-orange)` at full strength (`:309-315`) while `.c1-max-betlabel` is `color: color-mix(in srgb, var(--sig-orange) 72%, transparent)` (`:316-323`), so the second half of one unit is deliberately set at 72 per cent alpha over a bright celebration bloom, plus `letter-spacing: 0.2em` that the multiplication sign does not have, plus `font-size: 24px` against `13px` under `@media (max-width: 500px)` (`:368-369`). On frame 204, mid-bloom, `BET` is very nearly gone. Both spans also carry `align-self: flex-end` with different `padding-bottom` values, `0.12em` and `0.28em`, which overrides the wrapper's own `align-items: baseline`, so the two halves are bottom-aligned rather than sharing a baseline.
  This is the most-watched string in the game, and the German sibling shard reports the same collision as `5,000×EINSATZ` (STL-DE-B-02, ledger), which proves the collision is the English construction and not a translated string-length artefact.
- Resolution note: NEW AT 1600PX
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:298`, `:309-323` and `:368-369` (not locked)
- Proposed fix: put a real word space into the unit (raise the wrapper `gap` to a value that resolves against the numeral, or emit the space in markup), drop `align-self: flex-end` and the two `padding-bottom` values so the container's `align-items: baseline` actually applies, and bring `.c1-max-betlabel` to full `var(--sig-orange)` so both halves of one unit are the same colour.

## STT-POPOUTS-3-04 STREAM The win detail strip renders at 3.5 device pixels and prints raw internal symbol codes to the player
- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/193_popout-s_transition_feature_entry_fade.png`, `194_popout-s_feature_entry_card.png`, `195_popout-s_transition_feature_starting.png`, `196_popout-s_feature_run_1.png`, `197_popout-s_feature_run_2.png`, `198_popout-s_feature_run_3.png`, `199_popout-s_feature_run_4.png`, `200_popout-s_feature_run_5.png`, `201_popout-s_feature_run_6.png`, `202_popout-s_transition_feature_exit.png`, `203_popout-s_post_feature_base.png`, `207_popout-s_post_collect_base.png` (12 of my 17)
- Claim: the strip across the foot of the reel window is `WinBreakdown.svelte`, four tokens: symbol, count, ways, pay. Its face is `font-size: 0.7rem` (`frontend/src/lib/components/WinBreakdown.svelte:136`) which is `11.2px` at the 16px root, and the ways token is `font-size: 0.62rem` (`:146`) which is `9.92px`. Multiplied by the stage factor S = 0.3125 those become **3.5 device pixels and 3.1 device pixels**. The project names its own legibility floor as `11px` in `frontend/src/lib/components/FeatureMenu.svelte:227`. This surface is at 32 per cent and 28 per cent of that floor. It is not a matter of taste: at 2844x1600, four times the shipped pixel count, the strip still does not resolve into words, and at the shipped 400x225 it is a smear of coloured pixels sitting under the reels for the whole of every win.
  The second half is a content defect the frames pointed at and the source settles. The symbol token is `symbolLabel(current.symbol, $tr)` (`:92`), and `symbolLabel` (`:19-24`) routes only `W` and `S` through the translator; every other symbol falls through to `SYMBOL_IDS`, which is an identity map of the internal codes, `H1: 'H1', H2: 'H2', M1: 'M1', M2: 'M2', M3: 'M3', L1: 'L1', L2: 'L2', L3: 'L3'` (`:15-17`). So for eight of the ten symbols the player is shown a raw internal code, in every one of the sixteen locales. My frames show exactly that alternation: a long alphabetic token on 195, 197 and 201 (the scatter name) and a two-character token on 193, 194, 198, 199, 200, 202, 203 and 207.
  The comment immediately above that map (`:12-14`) states that these strings *now come from the tr layer like every other player-visible word*. For `W` and `S` that is true. For the other eight it is not, and the comment reads as though the sweep was complete.
- Resolution note: NEW AT 1600PX
- Where fixable: `frontend/src/lib/components/WinBreakdown.svelte:136` and `:146` for the size, `:15-24` for the symbol names (not locked)
- Proposed fix: two separate changes. Size: either raise the strip above the 11px floor after the stage factor (which means about 2.24rem at this profile, so realistically it must be given a mini-player rule of its own or suppressed at Popout S, an owner call). Names: give `SYMBOL_IDS` real translated display names through `t()` for all ten symbols, the same route `W` and `S` already take, so no internal code reaches a player.

## STT-POPOUTS-3-05 HIGH The win detail strip writes its multiplier with a letter `x`, a further survivor of the class Q-26 and MID-02 already enumerate
- Frames: same twelve frames as STT-POPOUTS-3-04, the surface is on screen for the whole of every win
- Claim: `frontend/src/lib/components/WinBreakdown.svelte:93` is `<span class="wb-count">x{current.kind}</span>`. That `x` is ASCII U+0078. The project's convention is recorded rather than inferred: `FS_MAX_WIN_LABEL = '5,000×'` (`frontend/src/lib/config/fsModes.ts:139`) and `MaxWinCelebration.svelte:151-154` carry a comment stating that *the paytable, the mode cards and the feature menu all write the multiplication sign `×` (U+00D7, which Orbitron carries)*.
  **What makes this worth a row rather than a character.** KNOWN row Q-26 enumerates the survivors of the Q-12 sweep as four instances in `fsModes.ts`. Ledger MID-02 already found a fifth at `WinBanner.svelte:205` and recorded that Q-26's list *was itself incompletely swept*. This is a sixth, in a third file, and it is a prefix (`x5`) rather than a suffix, which is why a search shaped for `}x` would miss it. The Arabic sibling shard independently reports *an ASCII `x` under the reels* (STL-AR-A-01, ledger), which is this same span reached from a different session and a different lens.
  **Stated honestly: this glyph is NOT resolvable on my frames.** At 3.5 device pixels nothing about it could be. The frames establish only that the surface reaches the player on twelve of seventeen frames; the glyph claim is from the source literal, cited above. The superseded native pass withdrew two `x` claims it had read off frames, correctly, and the lesson it recorded (do not read a glyph off four pixels) is followed here.
- Resolution note: NEW AT 1600PX
- Where fixable: `frontend/src/lib/components/WinBreakdown.svelte:93` (not locked)
- Proposed fix: change the literal to `×`. Separately, and this is the more valuable half: widen Q-26's enumeration by grepping the whole `frontend/src` tree for a letter `x` adjacent to an interpolation in BOTH directions, since the two known misses were a suffix in a component and a prefix in a component while Q-26's list searched the config layer.

## STT-POPOUTS-3-06 MEDIUM The MAX WIN value is the only stat that wraps, and it wraps onto a lower-case unit inside a strip whose every other string is capitals
- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/191_popout-s_transition_dialog_nitro_overdrive_opening.png`, `192_popout-s_dialog_nitro_overdrive.png`
- Claim: the value is `maxWinVsBaseBetLabel($isSocial)` (`frontend/src/lib/components/BuyBonus.svelte:135`), which returns `` `${FS_MAX_WIN_LABEL} base bet` `` (`frontend/src/lib/config/fsModes.ts:157-159`), so the literal is `5,000× base bet`. At this viewport it wraps to two lines, `5,000×` then `base bet`, making the third column two lines tall beside `$400.00` and `96.35%` at one line each, and unbalancing a three-column strip that is otherwise symmetrical. The register also breaks: `.buy-stat-label` carries `text-transform: uppercase` (`frontend/src/lib/components/BuyBonus.svelte:239`) so all three headers are capitals, the two other values contain no letters, and `base bet` is the single lower-case string in the strip.
  Recorded so it is not re-derived: I had also written this up as a WEIGHT mismatch between the two lines. That half is wrong and is withdrawn in the absences below. Both lines are one span at `font-weight: 700` (`:240`).
- Resolution note: NEW AT 1600PX
- Where fixable: `frontend/src/lib/config/fsModes.ts:157-159` (the literal) or `frontend/src/lib/components/BuyBonus.svelte:240` (the fit), neither locked
- Proposed fix: an art call between two one-line changes. Either uppercase the unit so the strip has one register (`BASE BET`, matching `PRICE` / `RTP` / `MAX WIN`), or drop the unit from the value and put it in the label (`MAX WIN vs BASE BET`) so the value is a single line like its two siblings. Note that the same helper feeds social mode with `base play`, so whichever is chosen has to apply to both.

## STT-POPOUTS-3-07 MEDIUM The max win overlay's only instruction to the player does not reach the frame
- Frames: `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/204_popout-s_transition_maxwin_overlay_fade.png`, `205_popout-s_maxwin_celebration.png`
- Claim: `frontend/src/lib/components/MaxWinCelebration.svelte:166` renders `<p class="c1-hint hint" aria-live="polite">{t($locale, 'maxWinHint', localeMode)}</p>` beneath the COLLECT button. It is on neither frame. Its style is `font-size: 11px; letter-spacing: 0.18em; color: rgba(255, 255, 255, 0.3)` (`:352-358`), and 11px times the stage factor S = 0.3125 is **3.4 device pixels**, at 30 per cent white over a bright celebration bloom. Whichever of the two mechanisms is operating, off the bottom of the composition or invisible against the bloom, the result on the frames is the same: the overlay tells the player nothing about how to proceed, and the button is the only affordance left.
  Marshal note attached to the same line, because it corrects a parked row rather than adding to it. KNOWN row Q-16 lists *`Press COLLECT or hit Enter to continue` on the max win overlay* among the hardcoded-English strings in its park. That item is stale: this string is routed through `t($locale, 'maxWinHint', localeMode)` at `:166`. The park's count should drop by one.
- Resolution note: NEW AT 1600PX
- Where fixable: `frontend/src/lib/components/MaxWinCelebration.svelte:352-358`, and the `@media (max-width: 500px)` block at `:365-371` which sizes the headline, the numeral, the unit spans and the button but not the hint (not locked)
- Proposed fix: give `.c1-hint` a rule in the same narrow-viewport block its four siblings already have, at a size and an opacity that survive the bloom, or accept that it cannot fit at this profile and say so.

## Native pass reconciliation

Against `STT-POPOUTS-B.md`, findings B-01 to B-08. `STT-POPOUTS-A.md` covers frames 157 to 182 and shares no frame with me; all ten of its findings belong to a sibling squad and none is reconciled here.

- **B-01 CONFIRMED, with two refinements, both of which make it worse.** The dialog does put only the tail of its question on screen and my 191 and 192 show `your bet?` exactly as claimed. REFINED: (i) B-01 says the surviving line has *the tops of its glyphs cut by that row's opaque magenta bottom edge*; at 1600px the line is not cut at all, its glyphs are whole, and the defect is purely that the head of the sentence is hidden. (ii) B-01 says 192 shows *only the top rounded edges of the two buttons at the frame's bottom border*; at 1600px there is no button edge on either 191 or 192, the space below the dialog border is empty. The buttons are not partly visible, they are absent.
- **B-02 CONFIRMED verbatim.** `OVERDRIVE FR`, `TOTAL W` and `MULTIPLI`, eleven frames, no ellipsis on any of the three. Re-reported as STT-POPOUTS-3-01, with the added observation that `App.svelte:748-752` claims in writing that this cannot happen at Popout S.
- **B-03 out of my range** (frames 187, 188). Sibling.
- **B-04 out of my range** (frames 187, 188). Sibling.
- **B-05 out of my range** (frames 183, 185). Sibling.
- **B-06 CONFIRMED as to the mechanism, REFUTED as to the severity, REFINED as to the content.** The `align-self: flex-end` plus two different `padding-bottom` values do defeat the wrapper's `align-items: baseline`, exactly as B-06 says, and the 24px against 13px sizes are correct. But B-06 grades it LOW and describes only a baseline problem. At full resolution the unit is also collided into `5,000xBET` with no word space, and `BET` is set at 72 per cent alpha against a full-strength multiplication sign, so half of the unit disappears into the bloom on 204. That is not LOW on the game's most-watched surface. Re-reported at HIGH as STT-POPOUTS-3-03.
- **B-07 out of my range** (frames 187, 188). Sibling.
- **B-08 not a product finding.** B's self-report of damaging and restoring committed frame 188, which is outside my range. Read, not dropped. My own tree check is at the foot of this shard and is clean.
- **B's withdrawal of the money-pod finding: UPHELD, and re-verified rather than taken on trust.** `frontend/src/lib/utils/currency.ts:260-268` records Fable's ruling closing TR-066: *the 400x225 mini-player profile alone, for BALANCE and WIN alone, and only when the fully formatted value cannot fit its MEASURED slot at the legible floor*. `BAL $50K` (193 to 203, 206, 207) and `WIN $5K` (206, 207) are that sanctioned case, beside a bet pod that correctly keeps `$1.00` in full. Not a defect and I did not re-open it.
- **B's withdrawal of the `1x` glyph findings: UPHELD, re-verified.** `frontend/src/lib/components/BonusInstrumentColumn.svelte:105` is `{multiplier}×`, U+00D7, and `frontend/src/lib/config/fsModes.ts:139` is `'5,000×'`, U+00D7, which is the dialog value on 191 and 192. Neither is a Q-26 instance. B was right to withdraw both, and right about why.
- **B's signed absence "no system font leakage ... two families are in use" is REFUTED, and so is my own first reading of the same thing.** There is exactly ONE family in the build. `frontend/src/app.css:97-98` declares `--fs-font-display: 'Orbitron', system-ui, sans-serif` and `--fs-font-numeric: 'Orbitron', 'Courier New', monospace`, `frontend/src/main.ts:2-4` self-hosts Orbitron at 400, 700 and 900, and every `font-family` declaration in `frontend/src` resolves to one of those two tokens or to `inherit`. What B described as *a narrower squared-terminal text face* and what I described as a second family on the max win overlay are both Orbitron: `.c1-max-headline` and `.c1-collect` are byte-for-byte the same declaration, `font-family: var(--fs-font-display); font-weight: 900` (`MaxWinCelebration.svelte:279-281` and `:326-331`). The apparent difference is size, `42px` against `15px` at this breakpoint, `letter-spacing: 0.22em` on the button, and a four-layer `text-shadow` bloom on the headline that fattens and rounds its strokes. **B's conclusion (no system font leaked) is right; its description of how many faces exist is wrong, and a marshal reading it would come away believing this game has two brand faces.** My candidate finding on the same thing is withdrawn below.

## Explicit absences, signed

Each is a claim I am signing, with what I checked in order to be able to sign it.

- **WITHDRAWN: my "two numeral sets on one row" finding.** I had opened a MEDIUM on frames 191, 192, 203, 205, 206 and 207 claiming that some zeros carry a diagonal slash (`5,000` on the max win hero, `$1.00` on the HUD, `5,000×` in the dialog) and some do not (`$400.00` in the same dialog strip, `$50K` on the same HUD row), and that a slashed zero is a property of a typeface so two forms means two families. **The specification refutes it.** One family is loaded, Orbitron, and both font tokens lead with it (`frontend/src/app.css:97-98`), so a family split is not available as an explanation. Per convention (l.2) a measurement that disagrees with the specification is a broken measurement until proven otherwise, and I could not prove otherwise: `fontTools` is not installed on this machine so I could not open the glyph outline in `frontend/node_modules/@fontsource/orbitron/files/` and count its contours. The likely mechanism is that Orbitron's zero carries the diagonal at every weight and the small readouts fill it in under their own glow (`.buy-stat-val.gold` has `text-shadow: 0 0 8px`, `BuyBonus.svelte:241`), which would make the 50px `5,000` the only place it survives. **Recorded rather than deleted, and handed on as one bounded question**: open the Orbitron 900 outline and confirm the zero design, because if it does differ between weights this becomes real.
- **WITHDRAWN: my "two families on the max win overlay" finding.** Refuted at source, in full, in the reconciliation section above. `.c1-max-headline` and `.c1-collect` carry identical `font-family` and `font-weight` declarations. What I read as two faces is one face at 42px with a bloom and at 15px without one.
- **WITHDRAWN, in part: the weight half of STT-POPOUTS-3-06.** I had written that `base bet` is set at a heavier weight than the `5,000×` above it. It is not. Both are one span, `.buy-stat-val`, `font-weight: 700` (`BuyBonus.svelte:135, 240`), and the two lines are a wrap of one string. The case and the wrap halves survive and are reported; the weight half is wrong.
- **No em dash and no en dash in any player-visible string on my 17 frames.** The strings in range are the three stat headers and their values on 191 and 192, the fragment `your bet?`, the entry card title and its free spins line and button, the three instrument labels and values, the four bottom HUD readouts and their labels, the max win heading, hero and button. The only non-alphanumeric separators present are the comma in `5,000`, the decimal points, the `+` on `+16 FREE SPINS` and `+8 FREE SPINS`, the `%` on `96.35%`, the `?` closing the dialog fragment and the `!` closing `MAX WIN REACHED!`.
- **No apostrophe, therefore no mixed straight and curly quotes, in range.** Not one string on 191 to 207 carries a possessive or a contraction. `your bet?` is the only prose fragment on screen at all.
- **No double space observable, and the limit of the claim is stated.** The only multi-word strings large enough to judge are `MAX WIN REACHED!`, `OVERDRIVE FREE SPINS`, `TAP TO CONTINUE`, `+16 FREE SPINS`, `MAX WIN` and `base bet`; word spacing in each is uniform at 1600px. The win detail strip is not legible enough to make any spacing claim about, which is STT-POPOUTS-3-04, so this absence is signed against the frames and not against the source.
- **No placeholder string, no lorem, no untranslated key and no missing-glyph box** on any of the 17 frames.
- **No numeral shimmy on a non count-up surface.** The bet pod holds `$1.00` at identical glyph positions across 193 to 203 and 206 to 207. `96.35%` is identical between 191 and 192, and so is `$400.00`. The free spins count holds `16` in the same box across 193 to 201 and `8` across 206 and 207. The only figures that change are the WIN pod between `$319.45` (202) and `$363.89` (203), which is a real value change and not shimmy. No feature meter, balance tick or instrument value shimmied. Consistent with source: `.buy-stat-val` carries `font-variant-numeric: tabular-nums` (`BuyBonus.svelte:240`) and `.fs-num` carries it too (`WinBreakdown.svelte:142`).
- **No letter-spacing mismatch between two instances of one component.** `PRICE`, `RTP` and `MAX WIN` match each other in tracking on both dialog frames and match across the two frames; the three instrument plate labels share tracking across all eleven frames carrying them; `TAP TO CONTINUE` is identical on 194 through 201 and 207.
- **No ellipsis character anywhere in range.** Every truncation on these frames is a hard cut.
- **No case drift between two renderings of one word in range.** `MAX WIN` is capitals on 191, 192, 204 and 205. `OVERDRIVE` is capitals on the entry card and on the instrument plate. The one register break is `base bet` against a strip of capitals, reported as STT-POPOUTS-3-06 rather than counted here.
- **Out of my lens, passed on rather than dropped.** (i) `TAP TO CONTINUE` (194 to 201, 207) is a touch verb on a desktop popout profile, a voice-lens question. (ii) `frontend/src/lib/components/MaxWinCelebration.svelte:155` hardcodes the hero numeral as the literal `5,000`, so the thousands separator is a comma in every locale; on a German session that string reads as five, which is the same class as STL-DE-A-01. I have no localised frame and I am not claiming it from one; it is handed to the localisation squads as a source-derived lead. (iii) Frames 196 to 201 are manifested as six interval frames of the feature in flight and all six show the entry card still up with `TAP TO CONTINUE` unpressed and `WIN $0.00`; frame 207 is manifested `post_collect_base` and shows a fresh entry card reading `+8 FREE SPINS`. The feature is never observed running in this session. That is for the composition and motion squads; no typography claim of mine rests on it.

## KNOWN matches

- **KNOWN(TR-115 / TR-086)**: the instrument column's TOTAL WIN value is cut to `$10.6` on `/Users/jt/math-sdk/.evidence-scratch/stream-test-upscaled-1600/193_popout-s_transition_feature_entry_fade.png` through `201_popout-s_feature_run_6.png`, and to `$2.8` on `206_popout-s_transition_maxwin_collect_fade.png` and `207_popout-s_post_collect_base.png`. Fresh evidence of the money-fit class at the 400x225 profile, on a surface the fit mechanism does not reach.
- **No match for MID-01.** The banner and HUD count-up disagreement needs the big-win triple, frames 169 to 171 for this session, outside my range. No frame of mine carries the win banner.
- **No match for MID-02, and a supporting observation.** The win banner does not appear on 191 to 207. The max win hero on 204 and 205 uses U+00D7 correctly (`fsModes.ts:139`), which supports MID-02's account that `WinBanner.svelte:205` is the outlier rather than the celebration surfaces. My STT-POPOUTS-3-05 is a further member of the same class, in a third file.
- **KNOWN(Q-26), extended**: STT-POPOUTS-3-05 above is a new instance of this row's class at `WinBreakdown.svelte:93`, outside the two files the row's enumeration searched.
- **No match for TR-104, TR-114, TR-089, Q-07, Q-16 (as a frame match), Q-27, Q-28, Q-34** on frames 191 to 207. No localised session, no replay surface, no infinity glyph, no autoplay panel, no paytable section header, no link or unstyled surface, and no mode name rendered twice in two registers appears in this range. Q-16 gets a correction rather than a match, recorded under STT-POPOUTS-3-07.

tree_after:
```
?? reports/qa/stream_test/shards/STC-POPOUTS-1.md
?? reports/qa/stream_test/shards/STC-POPOUTS-2.md
?? reports/qa/stream_test/shards/STC-POPOUTS-3.md
?? reports/qa/stream_test/shards/STM-POPOUTS.md
?? reports/qa/stream_test/shards/STT-POPOUTS-1.md
?? reports/qa/stream_test/shards/STT-POPOUTS-2.md
?? reports/qa/stream_test/shards/STT-POPOUTS-3.md
```
**Clean. Nothing MODIFIED and nothing DELETED.** Every entry is untracked. `STT-POPOUTS-3.md` is mine and is the only file this squad wrote; the other six are sibling squads' shards, not mine and not my concern. `reports/screens/` was read from and never written to, and the upscaled set under `.evidence-scratch/` is gitignored so it does not appear here. No image tool was invoked against any committed path: every frame was opened with the Read tool only, which is the discipline STT-POPOUTS-B-08 earned the hard way.
