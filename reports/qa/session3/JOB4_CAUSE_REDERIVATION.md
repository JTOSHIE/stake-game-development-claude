# JOB 4: THE 27 UNSOUND CAUSES, RE-DERIVED FROM SOURCE

**JOB 4 of `reports/briefs/FS_SESSION3_REMEDIATION_Prompt.md`.** The brief was explicit that
these 27 rows *are not actionable as written* and that the cause must be re-derived from source
before anything is touched, per Fable's claim-type ruling now at
`docs/skills/FULL_AUDIT_METHOD.md` 2.7.

4 squads, 4 COMPLETED, 0 LOST, 741,810 subagent tokens, 338 tool calls.

Australian English, no em dashes or en dashes.

## THE HEADLINE: the ruling was right, and the recorded causes were mostly wrong

| Verdict on the RECORDED cause | Count |
|---|---|
| **PARTIALLY_RIGHT** | 13 |
| **REFUTED** | 13 |
| **UNTESTABLE** | 1 |

| Is the SYMPTOM still real at HEAD | Count |
|---|---|
| YES | 18 |
| PARTIAL | 9 |

| Cause status after re-derivation | Count |
|---|---|
| DERIVED | 27 |

| Fix size | Count |
|---|---|
| SMALL | 24 |
| PARK | 2 |
| ONE_LINE | 1 |

**NOT ONE of the 27 recorded causes survived a re-derivation from source.** Thirteen were
REFUTED outright, thirteen were PARTIALLY_RIGHT, one was UNTESTABLE, and the count of
ACTUALLY_CORRECT is **zero**. Meanwhile every symptom is real: 18 fully, 9 in part.

**That is the claim-type split measured a third time, and the sharpest reading of it yet.**
Session 1 verified diagnoses and 19 per cent survived. Session 2 verified observations and 94
per cent survived. Here, taking findings whose SYMPTOMS an adversarial panel had already
upheld, and re-deriving only their CAUSES, the survival rate is **0 of 27**.

**The defects were real and the explanations were worthless.** That is exactly what Fable's
ruling predicted, exactly why the brief forbade acting on the recorded causes, and a strong
argument that the ruling should bind future sessions rather than being treated as advice about
one bad batch.

**One of the 27 needs a lock sanction (S2-C062, `gameStore.ts`)** and it is named rather than
touched, per the brief. The other 26 do not, which was not guaranteed going in.

**Not one of the 27 needs a lock sanction**, which was not guaranteed going in: three of the
rows sit in or beside locked paths.

## THE DISPOSITIONS

| Cluster | Sev | Recorded cause | Symptom now | Fix size | The derived cause, abbreviated |
|---|---|---|---|---|---|
| S2-C001 | STREAM | **PARTIALLY_RIGHT** | YES | SMALL | The row fuses two unrelated causes and cites a line that references no asset. (1) The player-visible defect is GEOMETRIC, not an asset defect. `GameGrid.svelte:499` `DROP_H = 520` against a 724px strip (`:72,74`) in a 412px window (`:67`, `:1228`): coverage re |
| S2-C011 | STREAM | **PARTIALLY_RIGHT** | YES | SMALL | Two of the recorded cause's three parts fail. (a) The citation is wrong at HEAD: .github/workflows/checks.yml:494-496 is the comment block for "Gate 13b: the dash gate's dist scan"; asset_reference_gate.mjs is actually wired at checks.yml:536-542. (b) "The one |
| S2-C016 | STREAM | **PARTIALLY_RIGHT** | YES | SMALL | The historical half of the recorded cause is corroborated by the file itself (the Q-13 comment at `app.css:91-96` sits inside an otherwise untouched scaffold `:root` block), but the OPERATIVE half, Q-27's ranking, is refuted at HEAD: `docs/QUALITY_CHARTER.md:1 |
| S2-C029 | HIGH | **REFUTED** | PARTIAL | SMALL | The recorded cause reasons backwards. It argues that because the harness drives the full six-parameter shape and then waits for `.start-replay` to become visible, "the mandatory-parameter throw would prevent" it, and files REQ-080 as unproven on that basis. Th |
| S2-C032 | HIGH | **REFUTED** | PARTIAL | SMALL | The recorded cause blames the CI gate's fixture coverage ("bonus and super carry only cap and feature"). That is a true description of frontend/src/lib/services/__fixtures__/replay_rounds.json but it is not the cause, and it points away from the answer, becaus |
| S2-C033 | HIGH | **REFUTED** | PARTIAL | SMALL | The recorded cause says the rare-feature half "is not dischargeable as written, because the base-mode trigger event ID is unpinned at REPLAY_TEST_EVENTS.md:31". Refuted by the same source as S2-C032: base feature = book id 1 at frontend/src/lib/services/__fixt |
| S2-C045 | HIGH | **REFUTED** | PARTIAL | SMALL | The recorded cause is "NONE (no CI step, no test, no tracker row)". The tracker-row half is false: TR-064, TR-065, TR-066 and TR-067 all exist at docs/records/reviews/REVIEW_TRACKER.md:202-205 and :309 and all four are CLOSED. The real cause is that the self-a |
| S2-C047 | HIGH | **UNTESTABLE** | YES | SMALL | The row's cause column is a verbatim re-quotation of the paragraph itself and asserts no mechanism, so there was nothing to test. Derived: `git blame -L 71,79 -- SUBMISSION_DOSSIER.md` returns commit 271a2b03, 2026-07-03, for EVERY line of the COMPLIANCE EVIDE |
| S2-C057 | HIGH | **REFUTED** | YES | SMALL | "The string '1 August 2026' occurs in no project record" is false at HEAD: it occurs at docs/stake-engine-live/2026-07-29/giveaway_terms.md:25 and in four audit artefacts including reports/qa/compliance_register/REGISTER.md:262 and reports/qa/compliance_regist |
| S2-C062 | HIGH | **REFUTED** | YES | PARK | `gameStore.ts:7` is not the artefact that decides what the game OFFERS, so acting on the recorded cause would produce a green gate over an unchanged submission. What the game offers is declared in the published maths package: `games/future_spinner/game_config. |
| S2-C066 | HIGH | **PARTIALLY_RIGHT** | PARTIAL | SMALL | Not "the record is stale" as a whole. The supersession IS recorded, at COMPLIANCE_WATCH.md:118-128 ("### 2026-07-07: SUPERSEDED by FeatureMath v2 - five-mode package / The two entries above (the base-only decision at 2026-07-03's game-quality-rankings note ... |
| S2-C068 | HIGH | **REFUTED** | YES | SMALL | The recorded cause's consequences do not hold. "The committed tile cannot be regenerated by anyone else" and "the source path recorded here is unreachable [so CLAUDE.md asset test point 4 fails]" are both wrong, because the script is a BYTE COPY, not a derivat |
| S2-C072 | HIGH | **PARTIALLY_RIGHT** | YES | SMALL | "The header line is stale against its own file" describes the state; it is not the cause, and it misattributes the number. Two derivations. (1) THE MECHANISM. `git show e0fd0b6 -- reports/qa/stream_test/LEDGER.md` ("test(stream): marshal the 47 shards to one l |
| S2-C076 | HIGH | **REFUTED** | YES | PARK | The recorded cause is "No root document addresses it". A root document DOES address it, and that is the actual defect: README.md:102-103 states "No Stake branding appears in any shipped asset or string. The repository carries no / open-source licence grant: it |
| S2-C081 | HIGH | **PARTIALLY_RIGHT** | YES | SMALL | The falsity is real, but "the live docs do publish one" is an observation, not a cause, and STALE_NUMBER is the wrong family. The cause is an unstruck supersession, the same class the document itself names. The 2026-07-29 docs-watch entry at COMPLIANCE_WATCH.m |
| S2-C085 | HIGH | **PARTIALLY_RIGHT** | YES | SMALL | The recorded text restates the observation (31 rounds, correct: 12+12+7) rather than giving a cause, and miscounts the cells 15 against 16. The cause is that the document was TRUE when written and nothing re-reads it. REPLAY_TEST_EVENTS.md was last committed a |
| S2-C095 | MEDIUM | **REFUTED** | YES | SMALL | The recorded cause is not a cause; it is an eyeball CONCLUSION offered as the proof, and the shipped asset refutes it. I opened frontend/public/assets/themes/future-spinner/ui/scene_character.png, the file SceneGroup.svelte:71 actually renders (`<img class="ch |
| S2-C098 | MEDIUM | **PARTIALLY_RIGHT** | PARTIAL | ONE_LINE | THE UNGUARDED THING IS livePart(), AND ITS PLACEMENT, NOT THE REFUSALS. scripts/kit_build.mjs:309 `const PART = livePart(readFileSync(join(clone, 'docs/records/upload-kit/00_READ_ME_FIRST.md'), 'utf-8'))` is called AFTER :280 `rmSync(KIT, { recursive: true, fo |
| S2-C100 | MEDIUM | **REFUTED** | PARTIAL | SMALL | Two separate defects, both derivable; the recorded row states neither, because it is a bare quote with no cause attached. (1) MAPPING. REQ-137's source is reports/qa/compliance_register/REGISTER.md:207 -> `terms.md:363+373`, warranty (v) "does not incorporate, |
| S2-C101 | MEDIUM | **PARTIALLY_RIGHT** | YES | SMALL | The recorded text is the symptom restated ("the register returns ZERO hits") plus a true but non-causal observation about WAYS_OF_WORKING, and it points at the wrong surface. COMPLIANCE_WATCH.md:32-37 is a PRE-SUBMISSION reminders block: both bullets are about |
| S2-C104 | MEDIUM | **REFUTED** | YES | SMALL | kit_build.mjs:14-18 is convention (o), which governs how a bundle is BUILT reproducibly. REQ-181 governs WHEN a built bundle is DELIVERED to Carrot after acceptance or after a modification. They are about different events, and "not in checks.yml" cannot be the |
| S2-C105 | MEDIUM | **PARTIALLY_RIGHT** | YES | SMALL | Both greps in the recorded cause reproduce exactly at HEAD and checks.yml:93-94 is as quoted, so the row is factually accurate. But it describes absences rather than a cause, and it never established the one fact that decides the finding. VERIFIED 2026-07-29 b |
| S2-C107 | MEDIUM | **REFUTED** | YES | SMALL | The recorded row is a verbatim quote plus a negative observation and names no cause at all, which is why it failed. The real cause is one line and it is a dropped clause. Source, verbatim at docs/stake-engine-live/2026-07-29/approval_guidelines.md:34: "Once a  |
| S2-C109 | MEDIUM | **PARTIALLY_RIGHT** | PARTIAL | SMALL | Two things, both checkable. (1) WRS_MASTER_DOCUMENT.md:24 is the only DONE row from the 2026-07-28 sitting that cites no evidence. Its neighbours all do: :23 cites `reports/screens/live-portal-2026-07-28/082628_frame.png`; :26 cites frames `082429` and `082546 |
| S2-C117 | MEDIUM | **REFUTED** | YES | SMALL | The load-bearing sentence of the recorded cause, carried from `frontend/src/lib/components/BetSelector.svelte:25-31`, is false at HEAD. It says the assertion "is NOT possible today" because `stepBet` "is read by `rgsService.authenticate()` into its `AuthRespon |
| S2-C123 | MEDIUM | **PARTIALLY_RIGHT** | PARTIAL | SMALL | The gate half of the recorded cause is refuted and the floor half is a category error. (a) "`validate_math.py` appears nowhere in `.github/workflows/checks.yml`" is true of that one file and false as a diagnosis: .github/workflows/validate-math.yml:37 is `run: |
| S2-C216 | LOW | **PARTIALLY_RIGHT** | YES | SMALL | The recorded cause "in-repo proof is impossible by design, see WRS_MASTER_DOCUMENT.md:6-10" is the half that produces the wrong action. The privacy rule does not say proof is impossible; it supplies the proof form, at :10: "Where a value exists but must not be |

---

## FULL RE-DERIVATIONS

Every claim below carries its own `file:line`. Nothing here was taken from the Session 2 ledger.

### S2-C001 (STREAM, frontend/src/lib/components/GameGrid.svelte)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** BOTH halves real at HEAD. (a) Geometry: `frontend/src/lib/components/GameGrid.svelte:499` `const DROP_H = 520`; `:501` `const startY = REST_Y - DROP_H`; `:75` `const REST_Y = -VIS_OFFSET * TILE  // -104`; `:72` `const TILE = CELL_H + GAP  // 104`; `:74` `const STRIP = ROWS + 3  // 7 slots`; `:67` `const CANVAS_H = ROWS * CELL_H + (ROWS - 1) * GAP  // 412`. Strip pixel height = 6*104 + 100 = 724 (from `.symbol-cell { height: 100px }` :1249 and `.reel-strip { gap: 4px }` :1242). startY = -104 - 520 = -624, so the strip's bottom edge sits at y=100 inside a 412px window (`.symbol-col { height: 412px; overflow: hidden }` :1225-1232): 312 of 412 px, 75.7 per cent, uncovered at the start of every drop. NOTHING paints behind it: neither `.symbol-col` (:1225-1232) nor `.grid-container` (:1204-1214) declares a background, and the only fill in the column is `.symbol-cell { background: rgba(28, 29, 46, 0.88) }` (:1259), i.e. the tiles themselves. Reachable on the shipping path: `frontend/src/lib/stores/reelMode.ts:39` returns `'drop'` by default, `GameGrid.svelte:942` `if (get(reelMode) === 'drop') await _dropAll(boardRows)`, and `:1054` says of that branch "THIS IS THE SHIPPING PATH". (b) Proof gap: the wired gate is `.github/workflows/checks.yml:538,542` running `scripts/asset_reference_gate.mjs` (+ `--self-test`), whose own header `frontend/scripts/asset_reference_gate.mjs:26-30` limits it to theme-derived and literal `assets/...` strings "required to exist in `dist/`"; `:11` hands the runtime half to `build_diet_verify.mjs`, and that script's ONLY occurrence in `.github/`, `frontend/package.json` and `Makefile` is a prose comment at `checks.yml:519`. Requirement text verbatim: `docs/stake-engine-live/2026-07-29/approval_guidelines_front_end_communication.md:23` "Ensure the game is free of visual bugs, including broken or missing assets or animations."

**Derived cause.** The row fuses two unrelated causes and cites a line that references no asset. (1) The player-visible defect is GEOMETRIC, not an asset defect. `GameGrid.svelte:499` `DROP_H = 520` against a 724px strip (`:72,74`) in a 412px window (`:67`, `:1228`): coverage requires startY + 724 >= 412, i.e. DROP_H <= 208, so at 520 the window is bare from the top of the fall and, solving -624 + 520f^2 >= -312, is not fully covered until f >= 0.775 of `FALL` (`:500`, 400ms, 260ms turbo). Per reel, per spin, on the shipping choreography. (2) The CI-coverage cause is real and stronger than recorded: `asset_reference_gate.mjs` asserts reference-to-existence only (`:26-30`, `existsSync(join(DIST, p))` at `:149`), it cannot see a corrupt file, an animation state or reachability, and self-hosted `@fontsource` fonts are outside its `assets/...` literal scan entirely; the runtime half its own header defers to (`build_diet_verify.mjs`, `:11`) is wired in no CI step. Neither cause is an ASSET being broken or missing, so the recorded NOT_MET framing points the fix at hunting a defect nobody has seen.

**Fix (SMALL).** Two, independent. (1) `frontend/src/lib/components/GameGrid.svelte:499`: reduce `DROP_H` from 520 to at most 208 (the strip's real bottom buffer: (STRIP - ROWS - VIS_OFFSET) * TILE = 2 * 104), or raise `STRIP` at `:74` so (STRIP-1)*104 + 100 - 104 - DROP_H >= 412 still holds at the current drop height. Backstop only, not a substitute: an opaque fill on `.symbol-col` (`:1225`) hides the band but, at `.symbol-cell` alpha 0.88 (`:1259`), also changes the look of a fully painted board, so that is a design call and not a mechanical fix. (2) Wire `frontend/scripts/build_diet_verify.mjs` into the browser-gates job of `.github/workflows/checks.yml`, or amend `asset_reference_gate.mjs:11` so its header stops claiming a runtime half that nothing runs.

**Re-proof.** Add a unit assertion derived from GameGrid's own constants and run it in CI: `(STRIP - 1) * TILE + CELL_H - VIS_OFFSET * TILE - DROP_H >= CANVAS_H` (today 724 - 104 - 520 = 100 < 412, RED; at DROP_H <= 208 it is GREEN). Separately, `grep -n build_diet_verify .github/workflows/checks.yml` returns a `run:` line rather than only the comment at :519.

### S2-C011 (STREAM, design-system/brand/GENERATION_NOTE_background.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** The "no gate checks asset provenance or hashes" half is real at HEAD. `grep -rln "GENERATION_NOTE" scripts/ frontend/scripts/ .github/` returns only scripts that WRITE notes (frontend/scripts/tile_master_ingest.mjs, frontend/scripts/tile_delivery_build.mjs) plus scripts/qa/doc_currency_baseline.json; nothing in .github/workflows/checks.yml reads one. Seven GENERATION_NOTE files are committed (git ls-files) and no gate compares any declared hash to any file. Reproduced the consequence fresh at HEAD: frontend/public/assets/themes/future-spinner/ui/hero_icon_96.png is sha256 97ed2fe0abe14a4244872b89ebab07935156a0df78a3de9859d72a7017a094a6 / 23,135 bytes while design-system/brand/hero_icon/hero_icon_96.png is 2affa2250bcb4a14d923424ac6576e79f0d7b9ab51b5ca38c9b197b81691de20 / 24,547 bytes, and hero_icon has no generation note at all.

**Derived cause.** Two of the recorded cause's three parts fail. (a) The citation is wrong at HEAD: .github/workflows/checks.yml:494-496 is the comment block for "Gate 13b: the dash gate's dist scan"; asset_reference_gate.mjs is actually wired at checks.yml:536-542. (b) "The one committed clearance artefact covers the NAME, not the art" is REFUTED: design-system/brand/GENERATION_NOTE_background.md:12-23 is a committed provenance record for the ART, and both its truncated hashes resolve. Source `65ef44c1ce96d351e96f69831bde8146...` (:15) resolves at reports/qa/background_candidate_ingest.json:109 to the full 65ef44c1ce96d351e96f69831bde8146d6cb77f4f7f86ad1b56bca1e0340f467; shipped `c7ecfa15dde8db42...` (:22) is a correct prefix of the file I hashed at HEAD, c7ecfa15dde8db421f3c9428211810c24cf292db6ba49fd149973a6fbffd9491.

THE REAL CAUSES, two, neither recorded. (1) The declared SOURCE is not in the repository and the note's own recovery pointer does not lead to it. GENERATION_NOTE_background.md:14 records `Found at | ~/Downloads/slot_background_assets/bg_improved_v1.jpg` at 569,573 bytes (:16), and :107 says "the candidate files themselves are recoverable at commit `6eaea1a`". `git show --stat 6eaea1a` holds `.../candidates/bg_base_candidate_v1.jpg  Bin 0 -> 273173 bytes` and `bg_base_candidate_v2.jpg  Bin 0 -> 259918 bytes`, i.e. the q80 RE-ENCODES, not the 569,573-byte source, and `git ls-files | grep bg_base_candidate` returns nothing at HEAD. So the source-to-shipped step is checkable only by hash, never re-runnable, and only from one machine's Downloads. (2) The one gate that reads generation records refuses their hashes BY DESIGN. scripts/qa/doc_currency_gate.mjs:404 `const SHA_RE = /\b(?=[0-9a-f]*\d)(?=[0-9a-f]*[a-f])(?:[0-9a-f]{7,12}|[0-9a-f]{40})\b/g`, whose own comment at :401-406 states the 7-to-12-or-40 length restriction exists precisely so that "`65ef44c1ce96d351e96f...` is the background's source hash and `c7ecfa15dde8db42` is a truncated one" are not matched. The project's only reader of these documents treats the hashes as noise to skip rather than claims to verify.

**Fix (SMALL).** Add scripts/qa/asset_provenance_gate.mjs: parse every design-system/**/GENERATION_NOTE_*.md provenance table, resolve each declared `Landed as` path, and assert the file's sha256 begins with the declared `Shipped SHA-256`; ship a convention (p) seeded violation (one flipped byte in a scratch copy) and wire it beside the other sixteen self-tested gates in checks.yml. Separately correct design-system/brand/GENERATION_NOTE_background.md:107, which currently points a reader at commit 6eaea1a for the source and delivers the re-encodes, and state plainly there that the 569,573-byte source is off-repo so the chain is hash-only.

**Re-proof.** `node scripts/qa/asset_provenance_gate.mjs --self-test` goes RED on the seeded byte and green on the clean tree; `node scripts/qa/asset_provenance_gate.mjs` confirms design-system/brand/GENERATION_NOTE_background.md:22's `c7ecfa15dde8db42` against the shipped bg_base.jpg; and `grep -n "6eaea1a" design-system/brand/GENERATION_NOTE_background.md` no longer claims the source is recoverable there.

### S2-C016 (STREAM, frontend/src/app.css)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** `frontend/src/app.css` lines 73-176 are the `create-vite` svelte-ts template `src/app.css`, item for item, with only the `font-family` declaration replaced (tokens at `:97-99`, whose comment at `:91-96` names QUALITY_CHARTER Q-13). At HEAD: `:103` `color-scheme: light dark`; `:105` `background-color: #242424`; `:113-117` `a { font-weight: 500; color: #646cff; text-decoration: inherit }`; `:118-120` `a:hover { color: #535bf2 }`; `:122-128` `body { margin: 0; display: flex; place-items: center; min-width: 320px; min-height: 100vh }`; `:130-133` `h1 { font-size: 3.2em }`; `:135-137` `.card { padding: 2em }`; `:139-144` `#app { max-width: 1280px; margin: 0 auto; padding: 2rem; text-align: center }`; `:146-156` `button { ... background-color: #1a1a1a }`; `:157-159` `button:hover { border-color: #646cff }`; `:160-163` `button:focus, button:focus-visible { outline: 4px auto -webkit-focus-ring-color }`; `:165-176` the whole `@media (prefers-color-scheme: light)` branch. It ships: `frontend/src/main.ts:7` `import './app.css'`. Charter text verbatim at `docs/QUALITY_CHARTER.md:199` (Q-27) and the B1-B7 bar at `:66-72`; requirement source `docs/stake-engine-live/2026-07-29/approval_guidelines_game_quality_rankings.md:23`.

**Derived cause.** The historical half of the recorded cause is corroborated by the file itself (the Q-13 comment at `app.css:91-96` sits inside an otherwise untouched scaffold `:root` block), but the OPERATIVE half, Q-27's ranking, is refuted at HEAD: `docs/QUALITY_CHARTER.md:199` and `reports/qa/stream_test/KNOWN_OPEN.md:21` both say "The link colours are the sharpest", and `grep -rn '<a[ >]' frontend/src frontend/index.html` returns ZERO. The only `href` in the tree is the favicon at `frontend/index.html:7`. So `a`, `a:hover` (`app.css:113-120`) and the light-scheme `a:hover` (`:170-172`) style an element the game never renders. The finding's own cited path `:160-163` is the focus ring, which is S2-C017's subject, not the colour block the claim describes. THE SOUND CAUSE IS ONE LINE: `app.css:143` `text-align: center`, inside the scaffold `#app` rule. `frontend/index.html:22` `<div id="app"></div>` is the mount root (`main.ts:11`); the `#app` ID selector (specificity 1,0,0) outranks App.svelte's universal reset `:global(*, *::before, *::after) { box-sizing: border-box; margin: 0; padding: 0 }` at `frontend/src/App.svelte:2135-2139` (0,0,0), and nothing else in the tree targets `#app`. `text-align` then inherits the whole way down, because no ancestor of the paytable rules list resets it. At `frontend/src/lib/components/PaytableModal.svelte:667-669`: `.fs-rules { list-style: none; ... padding: 0; margin: 0 }`, `.fs-rules li { ... padding-left: 16px; position: relative }`, `.fs-rules li::before { content: '›'; position: absolute; left: 0; ... }`. The chevron marker is absolutely pinned to the li's left edge while the inherited centring pulls the text away from it. That is cluster C-07 verbatim (`reports/qa/stream_test/CLUSTERS.md:43`, "Paytable bullet markers pinned left while their text is centred"), which `KNOWN_OPEN.md:21` records as the one cluster both Session 1 panel seats CONFIRMED outright, and it is still open at HEAD. Nothing gates any of it: `frontend/scripts/machine_tell_gate.mjs` does read `.css` (`:121`), but its `orphan-placeholder` class matches only a package-name-shaped `<title>` and authoring-marker words (`:182-215`), never a CSS declaration. SECOND, UN-ENUMERATED REACHING BRANCH: `app.css:165-176`'s `@media (prefers-color-scheme: light)` sets `button { background-color: #f9f9f9 }` at `:173-175`, which beats the base `#1a1a1a` at `:153` on a light-preference device for any button that declares no background of its own. Presence CONFIRMED; visibility NOT established here (it needs a browser probe I did not run) and I am not asserting it.

**Fix (SMALL).** Delete the dead members outright: `frontend/src/app.css:113-120` (`a`, `a:hover`), `:130-133` (`h1`), `:135-137` (`.card`), and `:165-176` (the whole light-scheme branch). Replace `:139-144` with `#app { width: 100%; height: 100% }`, dropping `text-align: center`, `padding: 2rem` and `max-width: 1280px`; that single deletion closes C-07. Leave `:160-163` to S2-C017, where it should be replaced with a brand focus style rather than removed bare. Then correct the two documents that carry the refuted ranking: `docs/QUALITY_CHARTER.md:199` and `reports/qa/stream_test/KNOWN_OPEN.md:21`.

**Re-proof.** `grep -n 'text-align' frontend/src/app.css` returns nothing, and a Playwright probe with the paytable open asserts `getComputedStyle(document.querySelector('.fs-rules li')).textAlign` is `start` and that the `::before` marker's box left edge and the text's first-line left edge differ by exactly the 16px `padding-left`.

### S2-C029 (HIGH, frontend/src/lib/services/replayService.ts)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** The REQUIREMENT is MET in source; only the proof half survives. `frontend/src/lib/services/replayService.ts:59` `if (params.get('replay') !== 'true') return null`; `:61-65` reads `game`, `version`, `mode`, `event`, `rgs_url`; `:67-71` `if (!game || !version || !mode || !event || !rgsUrlRaw) { throw new Error('Replay mode requires game, version, mode, event, and rgs_url query parameters.') }`; `:73-75` normalises `rgs_url`; `:125` `const url = `${p.rgsUrl}/bet/replay/${p.game}/${p.version}/${p.mode}/${p.event}``. That matches the platform text segment for segment: `docs/stake-engine-live/2026-07-29/approval_guidelines_game_replay_requirements.md:68-73` lists exactly those six as Required=Yes, and `:81` documents `GET {rgs_url}/bet/replay/{game}/{version}/{mode}/{event}`. That capture is byte-identical to the 2026-07-04 one (sha256 789dcfd5... in both headers), so the spec has not moved. WHAT IS REAL: the only proof is `frontend/scripts/replay_blocker_proof.mjs` (`:68-72` `const LIVE_QS = (mode, event) => `replay=true&game=...&version=1` + `&mode=${mode}&event=${event}&rgs_url=rgs.stake-engine.com` + `&currency=EUR&amount=10000000&lang=en&device=desktop&social=false``; `:100` `await page.locator('.start-replay').waitFor({ state: 'visible', timeout: 15000 })`), and it is wired NOWHERE: grep for `replay_blocker_proof` across `.github/`, `frontend/package.json`, `Makefile` and `scripts/` returns only a prose row in `scripts/qa/doc_currency_baseline.json:1734`. No test file imports `parseReplayParams`.

**Derived cause.** The recorded cause reasons backwards. It argues that because the harness drives the full six-parameter shape and then waits for `.start-replay` to become visible, "the mandatory-parameter throw would prevent" it, and files REQ-080 as unproven on that basis. The throw is exactly what makes the harness a proof: `replayService.ts:67-71` throws when any of the five mandatory parameters is missing, so `ReplayMode` never mounts and `replay_blocker_proof.mjs:100` times out RED. The finding's own evidence describes a working proof and then records the requirement as having none. THE REAL CAUSE OF THE RESIDUAL GAP IS A SCOPE POLICY, not a code defect and not a missing assertion: `.github/workflows/checks.yml:14-17` states the family-wide decision that "The Playwright suites that need a real dev server and a real environment ... stay local and in the external audit". `replay_blocker_proof.mjs` starts its own static server (`:52-65`) and launches chromium, so it fell inside that exclusion and was never wired. `replayService.ts:59-71` and `:125` are correct as written and need no change.

**Fix (SMALL).** Either (a) wire `frontend/scripts/replay_blocker_proof.mjs` into the existing browser-gates job of `.github/workflows/checks.yml` (it serves `dist` itself, so it needs only the chromium install that job already does), or, cheaper and CI-native, (b) add a new replayService params test at frontend/src/lib/services/ (to be created, so it is deliberately not written as a citation): stub `window.location.search`, assert `parseReplayParams()` throws for each of the five mandatory parameters removed in turn, returns `null` without `replay=true`, and returns a `ReplayParams` whose five fields reappear in the exact `:125` URL for the full live shape. (b) also closes the standing gap that no test file references `parseReplayParams` at all.

**Re-proof.** A named step in `.github/workflows/checks.yml` runs the new test (`npx tsx src/lib/services/replayService.params.test.ts`) and it goes RED when any one of the five conditions in `replayService.ts:67` is deleted (convention (p) seeded-defect leg); or `grep -n replay_blocker_proof .github/workflows/checks.yml` returns a `run:` line.

### S2-C032 (HIGH, REPLAY_TEST_EVENTS.md)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** REPLAY_TEST_EVENTS.md:31 still reads `| Feature trigger | *see method* | **11** | **1** | n/a | n/a |` and :38-39 reads "The curated local trigger samples (below) are the stand-in for base until a real trigger sim ID is pinned on staging." So one cell of the five-mode/five-scenario table is genuinely unfilled (base Feature trigger); the other 24 are complete or justifiably `none` (bonus/super have no zero-payout outcome, :30). The parallel capture sheet is worse: docs/REVIEW_EVENTS_PLAN.md:14-25 has EVERY `Normal ID` and `Social ID` cell empty, and its Mode column names only `base` and `bonus`, i.e. two of the five shipped modes.

**Derived cause.** The recorded cause blames the CI gate's fixture coverage ("bonus and super carry only cap and feature"). That is a true description of frontend/src/lib/services/__fixtures__/replay_rounds.json but it is not the cause, and it points away from the answer, because the SAME fixture file already contains the missing base ID. frontend/scripts/extract_replay_fixtures.mjs:40-48 implements exactly the scan REPLAY_TEST_EVENTS.md:33-37 says requires an external tool (mnemoo or stake-dev-tool): `const triggered = round.events.some((e) => e.type === 'freeSpinTrigger')`. Its committed output at frontend/src/lib/services/__fixtures__/replay_rounds.json:1371-1376 records base feature = `"id": 1`, `"payoutMultiplier": 36389`, `"criteria": "freegame"`. 36389 centibets is 363.89x, which is the same round REPLAY_TEST_EVENTS.md:28 already lists as base Big win **1** (~363.89x): a trigger-plus-big-win round, the exact pattern the doc itself records for cruise sim 11 and antelite sim 1 at :31. So the base trigger ID is 1 and it is pinned in the repository, asserted by CI gate 13 on every run (.github/workflows/checks.yml:466-468). The fixture landed 2026-07-25 in commit 3319529; REPLAY_TEST_EVENTS.md has not been edited since commit c23404c on 2026-07-08. The document went stale because nothing re-derives it when the tool that answers it is written. Separately and independently: docs/REVIEW_EVENTS_PLAN.md was last committed at aee3d56 on 2026-07-04, three days BEFORE FeatureMath v2 shipped the five-mode package on 2026-07-07 (CLAUDE.md, True game facts), which is why it enumerates only base and bonus. It is superseded by REPLAY_TEST_EVENTS.md but never marked so, and COMPLIANCE_WATCH.md:28 still names it as the capture sheet.

**Fix (SMALL).** Two documentation edits, no code. (1) REPLAY_TEST_EVENTS.md:31, replace `*see method*` in the BASE column with `**1** (~363.89x)` and a note that it is the same round as the Big win cell, a genuine trigger-plus-big-win round; replace the sentence at :38-39 with a citation of frontend/src/lib/services/__fixtures__/replay_rounds.json:1371-1376 and frontend/scripts/extract_replay_fixtures.mjs:40-48 as the derivation, keeping the plus-one offset caveat at :42-44 intact since it applies to the deployed RGS id, not to the sim id. (2) docs/REVIEW_EVENTS_PLAN.md, either extend to five modes or mark SUPERSEDED by REPLAY_TEST_EVENTS.md at the head, and repoint COMPLIANCE_WATCH.md:28.

**Re-proof.** node -e "const f=require('./frontend/src/lib/services/__fixtures__/replay_rounds.json');console.log(f.base.feature.id,f.base.feature.payoutMultiplier,f.base.feature.criteria,f.base.feature.events.some(e=>e.type==='freeSpinTrigger'))" prints `1 36389 freegame true`; and `grep -c 'see method' REPLAY_TEST_EVENTS.md` returns 0.

### S2-C033 (HIGH, REPLAY_TEST_EVENTS.md)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** Max-win half IS discharged and recorded: reports/screens/live-round2-2026-07-26/01_replay_22975_celebration_multiplier_5000x_win_3750000.png and 02_MAX_WIN_REACHED_overlay_5000x_bet_collect.png exist, catalogued at reports/screens/CAPTURE_CATALOGUE_2026-07-27.md:247. Rare-feature half is NOT: the only other replay capture in the tree is reports/screens/dtt-live-2026-07-26/37_REPLAY_WORKING_event_52121_with_disclaimer.png, described at reports/qa/screenshot_deep_audit_2026-07-26.md:94 as `Mode: base, Bet: $1.00, Currency: USD` with no feature content, and `find reports/screens -iname "*replay*"` returns no `08-replay-<mode>-<scenario>.png`, the filenames DTT_PROTOCOL.md:270 requires. REPLAY_TEST_EVENTS.md:85-87 still carries the undischarged instruction verbatim.

**Derived cause.** The recorded cause says the rare-feature half "is not dischargeable as written, because the base-mode trigger event ID is unpinned at REPLAY_TEST_EVENTS.md:31". Refuted by the same source as S2-C032: base feature = book id 1 at frontend/src/lib/services/__fixtures__/replay_rounds.json:1371-1372, cruise 11 and antelite 1 confirmed in the same file. The step is dischargeable today. Two real causes, neither of which is a missing ID. FIRST, the instruction sits in the wrong artefact. The owner performs this on a portal visit working from the Desktop kit, and scripts/kit_build.mjs:302-303 copies exactly ONE document into the kit (docs/records/upload-kit/00_READ_ME_FIRST.md -> 00_READ_ME_FIRST_SECOND_VISIT.md) alongside dist and branding. That walkthrough's Check 8 at docs/records/upload-kit/00_READ_ME_FIRST.md:311-312 reads "We have a list of test replay IDs in `REPLAY_TEST_EVENTS.md`. Open a replay for each of the five game modes." `ls ~/Desktop/FS_UPLOAD_KIT_V10/` returns only 00_READ_ME_FIRST_SECOND_VISIT.md, 02_frontend_upload, 03_branding, BUILD_INFO.json, README.md; REPLAY_TEST_EVENTS.md is not in the kit, and the kit copy is byte-identical to the repo source (diff empty). The step points at a document the operator does not have in front of him. The contrast is the proof: the max-win half is the one step whose id is INLINE in the kit, docs/records/upload-kit/00_READ_ME_FIRST.md:475 "Set **Game Mode** to `super` and **Event ID** to **22975**", and it is the half that got done. SECOND, the method both documents describe is superseded. reports/qa/dtt_live_session_2026-07-26.md:518-522 records that the platform's Bets panel now exposes a per-round "Replay this bet" button ("Event ID 52121 was replayed successfully", "DTT check 8 is largely solved"), so a feature round can be replayed by playing one and clicking it, with no derived ID at all. Neither REPLAY_TEST_EVENTS.md (frozen 2026-07-08) nor DTT_PROTOCOL.md:240-270 was updated to say so.

**Fix (SMALL).** Inline the per-mode feature-trigger IDs into the kit walkthrough's Check 8 exactly as the max-win step already inlines 22975: base **1**, cruise **11**, antelite **1**, bonus and super guaranteed-trigger so any round serves. Add one sentence recording the Bets-panel "Replay this bet" fallback from reports/qa/dtt_live_session_2026-07-26.md:518-522. Edit docs/records/upload-kit/00_READ_ME_FIRST.md:311-312, then rebuild the kit (scripts/kit_build.mjs), since kits are single use per OWNER_CHECKLIST.md:64-65.

**Re-proof.** `grep -c "REPLAY_TEST_EVENTS.md" docs/records/upload-kit/00_READ_ME_FIRST.md` returns 0 and `grep -n "Event ID" docs/records/upload-kit/00_READ_ME_FIRST.md` shows a feature-trigger id per mode; the rebuilt kit's 00_READ_ME_FIRST_SECOND_VISIT.md carries them; the next portal visit lands reports/screens/<pass>/08-replay-<mode>-feature.png.

### S2-C045 (HIGH, docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** The REQUIREMENT is still unmet: the platform shows 0 of 58 ticked (OWNER_CHECKLIST.md:36 "once the count moves off 0/58"; STAKE_GUIDELINES_SELF_ASSESSMENT.md:4-6). But the specific defects the document records have INVERTED since Session 2, so the document now overstates the outstanding work. docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md:160-170 still reads `| **FAIL** | **3** (items 15, 25, 46) |` and `| **CONFLICT** | **1** (item 12) |`. Item 25 at :96 says "`touch-action` appears nowhere in `frontend/src`": false, frontend/src/app.css:16 is `touch-action: manipulation;` under a comment at :1 naming "guideline item 25"; TR-066 CLOSED (docs/records/reviews/REVIEW_TRACKER.md:204). Item 46 at :137 says "Nothing forces English": false, frontend/src/lib/stores/socialLocale.ts:91 `if ($social && get(locale) !== SOCIAL_LOCALE) locale.set(SOCIAL_LOCALE)`, wired at App.svelte:191; TR-067 CLOSED (:205). Item 15 at :76 cites App.svelte:1821: at HEAD the rule is App.svelte:2265, TR-065 (:203) records the root cause as "two stale constants, not the overflow property", and reports/qa/layout_fit_gate_2026-07-26.json reports `vScroll: false` at all seven presets with the gate wired at .github/workflows/checks.yml:617-618. Item 12 at :68 cites rgsService.ts:797: at HEAD :797 is a comment and the gate is :799, and TR-064 (REVIEW_TRACKER.md:309) is CLOSED, "the guideline is SATISFIED by the current code, and no code changes".

**Derived cause.** The recorded cause is "NONE (no CI step, no test, no tracker row)". The tracker-row half is false: TR-064, TR-065, TR-066 and TR-067 all exist at docs/records/reviews/REVIEW_TRACKER.md:202-205 and :309 and all four are CLOSED. The real cause is that the self-assessment is a single-commit snapshot that has never been revised: `git log --format=%h%ad -- docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md` returns exactly one commit, c4b53ae, 2026-07-26. Every defect it records was closed within the next two days, TR-065/066/067 on 2026-07-26 and TR-064 on 2026-07-28, and each tracker row records its own fix while the document that summarises them was never touched. The link is one-directional: the tracker rows cite the guideline items, nothing cites back, so closing a row cannot prompt the document. And no external source can correct it either: the 58-item list is login-gated on the Future Spinner Math page and exists only as the owner's transcription (STAKE_GUIDELINES_SELF_ASSESSMENT.md:2-4). It is absent from all 64 mirrored pages under docs/stake-engine-live/2026-07-29/, so no convention (d) capture pass can ever refresh it.

**Fix (SMALL).** Re-derive the four rows against HEAD and restate the summary as PASS 35 / OBSERVE 14 / OWNER 8 / FAIL 0 / CONFLICT 0 / N/A 1 (sums to 58), each changed row citing the tracker row that closed it. Correct the two stale line citations: App.svelte:1821 becomes :2265, rgsService.ts:797 becomes :799. Add a back-reference in TR-064 to TR-067 naming the guideline item and this document, so the next closure has somewhere to point. Note for the owner: OWNER_CHECKLIST.md:32 says "Nine of them are OWNER items" while this document's own tally at :169 says 8, already recorded as D04-20/D04-21 and out of scope here.

**Re-proof.** `grep -n '\*\*FAIL\*\*' docs/records/compliance/STAKE_GUIDELINES_SELF_ASSESSMENT.md` returns only the status-vocabulary legend row at :18, and the summary table's counts sum to 58.

### S2-C047 (HIGH, SUBMISSION_DOSSIER.md)

- **Recorded cause:** UNTESTABLE
- **Symptom still real at HEAD:** YES
- **Evidence:** SUBMISSION_DOSSIER.md:78 reads "Bet Replay implemented for base rounds" and :79 reads "responsive verified at all six required viewports". Both are wrong against HEAD. Replay is five-mode and CI-gated: frontend/src/lib/services/__fixtures__/replay_rounds.json carries base, cruise, antelite, bonus and super, and frontend/src/lib/services/replayRounds.test.ts is gate 13, wired at .github/workflows/checks.yml:466-468. The platform's own screen list is SEVEN, not six: frontend/scripts/layout_fit_gate.mjs:21-23 "The seven presets are the platform's own, read off the DTT Screen menu", and reports/qa/layout_fit_gate_2026-07-26.json measures seven (Desktop 1200x675, Laptop 1024x576, Popout S 400x225, Popout L 800x450, Mobile L 425x812, Mobile M 375x667, Mobile S 320x568). The file also contradicts itself: SUBMISSION_DOSSIER.md:34 reads "replay event IDs derived for all five modes".

**Derived cause.** The row's cause column is a verbatim re-quotation of the paragraph itself and asserts no mechanism, so there was nothing to test. Derived: `git blame -L 71,79 -- SUBMISSION_DOSSIER.md` returns commit 271a2b03, 2026-07-03, for EVERY line of the COMPLIANCE EVIDENCE MAP paragraph. It is a 2026-07-03 snapshot that predates all three things it now misstates: FeatureMath v2 shipped the five-mode package on 2026-07-07 (CLAUDE.md, True game facts); the five-mode replay fixtures landed 2026-07-25 in commit 3319529; the platform's seven-preset Screen menu was read off the DTT on 2026-07-26 (reports/screens/dtt-live-2026-07-26/09_dtt_screen_presets_popout_s_400x225.png). Every subsequent capability change was recorded ELSEWHERE in the same file, at :34 and in the five-mode table at :92 onward, and the paragraph was left untouched, which is why one document disagrees with itself. It is unguarded because it is prose: it names no file and no gate reads it. "Six required viewports" is additionally not traceable to any platform source: `grep -rn -i viewport docs/stake-engine-live/2026-07-29/` over the 64-page mirror returns nothing that requires a count, so the only checkable figure in the repository is seven.

**Fix (SMALL).** Edit SUBMISSION_DOSSIER.md:78 to "Bet Replay implemented and CI-gated for all five modes (frontend/src/lib/services/replayRounds.test.ts, checks.yml gate 13)" and :79 to "responsive verified at all seven platform Screen presets (frontend/scripts/layout_fit_gate.mjs, checks.yml browser gate)". Then re-derive the remaining twelve claims in the same 2026-07-03 paragraph against HEAD rather than trusting the two that happened to be caught, since they share the freeze date and therefore the risk. Under REQ-135 (terms.md:363, 369) this paragraph is the document handed to Carrot, so a stale claim here is a divergence between documentation and build by definition.

**Re-proof.** `grep -n 'base rounds\|six required viewports' SUBMISSION_DOSSIER.md` returns nothing, and each replacement claim names a gate that `grep -n` locates in .github/workflows/checks.yml.

### S2-C057 (HIGH, OWNER_CHECKLIST.md)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** YES
- **Evidence:** `grep -n August OWNER_CHECKLIST.md SUBMISSION_DOSSIER.md WRS_MASTER_DOCUMENT.md COMPLIANCE_WATCH.md README.md GAME_FACTS.md DTT_PROTOCOL.md` returns NOTHING, and `grep -rn 'giveaway\|Giveaway\|competition\|Competition\|deadline\|Closing Date'` over the same five root documents also returns nothing. OWNER_CHECKLIST.md:25 is the heading `# THE FIVE OUTSTANDING ITEMS`, and none of the five items below it, including item 3 "Upload kit V10" at :61-78, carries a date of any kind. The obligation is docs/stake-engine-live/2026-07-29/giveaway_terms.md:25 "Closing Date: 11:59pm AEDT on 1 August 2026." Today is 2026-07-29, so this is live: under three days remain and item 3 is not done.

**Derived cause.** "The string '1 August 2026' occurs in no project record" is false at HEAD: it occurs at docs/stake-engine-live/2026-07-29/giveaway_terms.md:25 and in four audit artefacts including reports/qa/compliance_register/REGISTER.md:262 and reports/qa/compliance_register/shards/R9_commercial-legal-triage.md:55. The true statement is narrower and worse: no DRIVING document carries it, only the mirror and the audit's own paperwork. The cause is that convention (d) docs watch reports DIFFERENCES, and a page captured for the first time has no prior version to differ from. giveaway_terms.md has exactly one mirror, first captured 2026-07-29 in commit 9fe3c48, whose message enumerates "TWO REAL CHANGES" (a math-verification file-size section, and 13 new currencies) and names no closing date, because the diff was computed only over the pages that overlapped a prior capture ("Eight of ten overlapping pages match on sha256"). That capture added 64 pages; the roughly 56 with no prior version produced no delta, so every obligation new to them entered the tree silently and nothing propagated to the owner-facing records. This is protocol rule 16's mechanism one step earlier in the chain: there a stale line was never struck, here a new line was never read.

**Fix (SMALL).** Time-critical, escalate today. (1) Put a dated header line at the top of OWNER_CHECKLIST.md quoting giveaway_terms.md:25 verbatim per convention (l.7), with the capture date, and record giveaway_terms.md:30's escape clause ("Stake Engine may extend or shorten the Competition Period by publishing an updated Closing Date on the Stake Engine website, effective immediately upon publication") so a later capture that moves the date reads as expected rather than as a contradiction. (2) Amend the convention (d) docs-watch procedure so a page with NO prior capture is reported IN FULL as NEW rather than omitted from the delta list, which is the mechanism that lost this. (3) Raise with the owner now: the deadline is 2026-08-01 and OWNER_CHECKLIST.md item 3 (upload kit V10 to future-spinner-3) and item 1 (58 Guidelines, still 0/58) are both outstanding.

**Re-proof.** `grep -n '1 August 2026' OWNER_CHECKLIST.md` returns a line quoting the mirror with its file:line and date; and the next docs-watch commit message lists NEW pages as their own section separately from CHANGED pages.

### S2-C062 (HIGH, frontend/src/lib/stores/gameStore.ts)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** YES
- **Evidence:** The absence is real; the cited artefact is not the binding one. No sub-$0.10 level exists anywhere in the tree: `frontend/src/lib/stores/gameStore.ts:7` `export const BET_LEVELS = [0.10, 0.20, 0.50, 1.00, 2.00, 5.00, 10.00, 20.00, 50.00, 100.00]`; `games/future_spinner/game_config.py:106` `self.bet_levels = [0.10, 0.20, 0.50, 1.00, 2.00, 5.00, 10.00, 20.00, 50.00, 100.00]`; `games/future_spinner/library/publish_files/game_metadata.json` `"betLevels": [0.1, 0.2, 0.5, 1.0, 2.0, 5.0, 10.0, 20.0, 50.0, 100.0]`, `"minBet": 0.1`, `"stepBet": 0.1`. Requirement source verbatim, `docs/stake-engine-live/2026-07-29/rgs.md:295`: "New game submissions should incorporate small denomination bets, which are not yet industry standard, these are levels (in USD): [$0.01, $0.02, $0.05, $0.10, ....]". NOT AN ALREADY-KNOWN LOCKED_FILE_DEBT: `CLAUDE.md`'s LOCKED_FILE_DEBTS section (:137-221) lists exactly four `gameStore.ts` items, `canBuyBonus`, the bet-ladder ACTIONS, four em dashes, and four dead stores. The `BET_LEVELS` ARRAY is not among them, and unlike the actions it is NOT unreachable from production: `frontend/src/lib/stores/betLadder.ts:32` imports it and `:39-41` serves it whenever `rgsBetLevels` is empty.

**Derived cause.** `gameStore.ts:7` is not the artefact that decides what the game OFFERS, so acting on the recorded cause would produce a green gate over an unchanged submission. What the game offers is declared in the published maths package: `games/future_spinner/game_config.py:106`, surfaced as `games/future_spinner/library/publish_files/game_metadata.json`'s `betLevels`/`minBet 0.1`/`stepBet 0.1`. The ladder a player actually sees comes from the platform: `frontend/src/lib/services/rgsService.ts:568` `betLevels: (config.betLevels ?? []).map(microsToDisplay)` into `:735` `rgsBetLevels.set(auth.betLevels)`, consumed by `betLadder.ts:39-41`; `gameStore.ts:7` is only the fallback when that array is empty. Widening `gameStore.ts:7` alone would also falsify `gameStore.ts:6`'s own comment ("matches game_config.py bet_levels"), which nothing gates. THE ACTUAL CAUSE IS AN UNTAKEN DECISION WITH A COUPLED DEPENDENCY. Nobody ever decided whether to ship a sub-dime ladder, and it cannot be decided in isolation, because the SAME paragraph carries REQ-125/126/127: the game's minimum way-win is 0.08x (`games/future_spinner/game_config.py:127` `(3, "L3"): 0.08`, mirrored at `frontend/src/lib/components/PaytableModal.svelte:82`), which is below the 0.1x threshold, so `rgs.md:295` requires FOUR points of win precision at a $0.01 level, while `frontend/src/lib/utils/currency.ts:211` `const decimals = ZERO_DECIMAL.has(code) ? 0 : 2` fixes fiat at two. Adding $0.01 without that renders a true $0.0008 win as `$0.00`. Note the obligation strength: `rgs.md:287` "Although bet levels are not mandatory" and `:295` "should incorporate", so this is a recommendation twice over, not a mandate.

**Fix (PARK, NEEDS LOCK SANCTION).** PARK. Owner decision, one line: ship a sub-$0.10 ladder or record the decision not to, citing `rgs.md:287`/`:295`'s "not mandatory"/"should" wording. If YES, it is a single coordinated sanctioned pass, never a frontend-only edit: `games/future_spinner/game_config.py:106` plus a regenerated `games/future_spinner/library/publish_files/game_metadata.json` (LOCKED prefix), `frontend/src/lib/stores/gameStore.ts:7` to preserve the parity its own `:6` comment asserts (LOCKED), and the win-precision half in `frontend/src/lib/utils/currency.ts:211` for REQ-125/126/127. Do NOT widen `gameStore.ts:7` alone: it turns any ladder gate green while the submitted package still declares `minBet 0.1`.

**Re-proof.** Whichever way the decision goes, a wired frontend test asserts the three declarations are identical lists: `gameStore.ts:7` BET_LEVELS === `game_metadata.json` `betLevels` === `game_config.py:106` `bet_levels`, and `game_metadata.json` `minBet` === list[0]. Today that test passes at a 0.10 floor and would go red on any one-sided widening, which is the property the recorded cause would have broken.

### S2-C066 (HIGH, COMPLIANCE_WATCH.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** COMPLIANCE_WATCH.md:91-93 at HEAD, verbatim: "...and warns against 'over-reliance on generic AI-generated assets'. Our canonical / decision is deliberately base-only single-mode, which may cap the star rating even though / it is fully compliant." That sentence is false at HEAD: COMPLIANCE_WATCH.md:9 and :16-18 in the same file say "**Five modes** (base 1.0x, cruise 1.0x, OVERBOOST 1.25x, Buy Overdrive 100.0x, NITRO OVERDRIVE 400.0x...)". `grep -n "base-only" COMPLIANCE_WATCH.md` returns :92, :107 and :119, and only :92 is an unmarked present-tense assertion. The second half of the recorded symptom is PARTIAL: the CI steps quoted are verbatim correct (.github/workflows/checks.yml:331-337 `machine tell gate, seeded-violation self-test` / `machine tell gate, source scan`, plus a dist scan at :506), and of the four 1-star causes at docs/stake-engine-live/game-quality-rankings.md:32-36 the visual-design one has three CI proxies (checks.yml:619 `browser: contrast`, :617 `browser: layout fit`, :627 `browser: scrim coverage`), so "the other three are gated nowhere" is overstated; shallow gameplay and missing engaging features are genuinely ungated and are not mechanically gateable.

**Derived cause.** Not "the record is stale" as a whole. The supersession IS recorded, at COMPLIANCE_WATCH.md:118-128 ("### 2026-07-07: SUPERSEDED by FeatureMath v2 - five-mode package / The two entries above (the base-only decision at 2026-07-03's game-quality-rankings note ...) are both superseded"). The cause is that it was recorded 26 lines BELOW the sentence it supersedes, under a heading naming FeatureMath v2 rather than the quality-rankings item, with NO marker on the superseded sentence or on its own heading at COMPLIANCE_WATCH.md:76 ("### 2026-07-03: first live docs refresh"). The document states the correct convention itself, later, at COMPLIANCE_WATCH.md:451-452: "**When adding an entry that resolves an earlier one, strike the earlier one in the same edit.**", and shows the applied form at :434 ("### 5. New payments documentation page. SUPERSEDED BY ENTRY 7 BELOW, 2026-07-29.") plus the STRUCK paragraph at :439. A reader arriving at :87-95 by citation, which is exactly what the Session 2 walk squad did, gets no signal at all. Compounding it, the Watch log is not in date order: the two 2026-07-04 headings at :41 and :65 sit ABOVE the two 2026-07-03 headings at :76 and :103, so position in the file is not a reliable staleness signal here either.

**Fix (SMALL).** COMPLIANCE_WATCH.md, one edit. (1) Amend the heading at :76 to "### 2026-07-03: first live docs refresh. The game-quality-rankings bullet below is SUPERSEDED by the 2026-07-07 entry", and the heading at :103 the same way. (2) Replace the clause at :91-93 "Our canonical decision is deliberately base-only single-mode, which may cap the star rating even though it is fully compliant" with a STRUCK note in the :439 form pointing at :118-128 and at the five-mode posture at :9-18. (3) While the bullet is open, quote all four 1-star causes verbatim from docs/stake-engine-live/game-quality-rankings.md:32-36 rather than the two it currently paraphrases, per convention (l.7).

**Re-proof.** `grep -n "base-only single-mode" COMPLIANCE_WATCH.md` returns no line outside a STRUCK or SUPERSEDED marker, and `grep -n "^### 2026-07-03" COMPLIANCE_WATCH.md` returns two headings both carrying SUPERSEDED. `node scripts/qa/doc_currency_gate.mjs` stays green.

### S2-C068 (HIGH, frontend/scripts/tile_master_ingest.mjs)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** YES
- **Evidence:** Literally real at HEAD. frontend/scripts/tile_master_ingest.mjs:20 `const SOURCE = '/Users/jt/Downloads/we-roll-spinners_future-spinner_tile.png'`, :21 `const TILE = '/Users/jt/math-sdk/design-system/brand/tile'`, and :27-30 `if (!existsSync(SOURCE)) { console.error(...); process.exit(2) }`. The script cannot run on any other machine.

**Derived cause.** The recorded cause's consequences do not hold. "The committed tile cannot be regenerated by anyone else" and "the source path recorded here is unreachable [so CLAUDE.md asset test point 4 fails]" are both wrong, because the script is a BYTE COPY, not a derivation: :80 `writeFileSync(join(TILE, MASTER), srcBuf)`, verified at :81-85 (`if (landedSha !== srcSha) ... process.exit(3)`), and the note it emits carries the FULL untruncated source hash at :98. Measured at HEAD: design-system/brand/tile/tile_composed_master.png is sha256 741e77face74f7e93fb29790eb16dfc22f15b7a13d3d9f47a00a682e7c99434e, which is character-for-character the `Source SHA-256` recorded at design-system/brand/tile/GENERATION_NOTE_composed_master.md:10. The source bytes ARE in the repository. There is nothing to regenerate and the provenance chain is fully checkable.

THE REAL DEFECT IS ONE LINE LOWER, at :21, and it is the WRITE target, not the read. `const TILE = '/Users/jt/math-sdk/design-system/brand/tile'` hardcodes one machine's checkout as the destination. The comparable script that must work from a clone derives its root instead: scripts/kit_build.mjs:48-49 `const HERE = dirname(fileURLToPath(import.meta.url))` / `const REPO = resolve(HERE, '..')`. Run from any other checkout, tile_master_ingest.mjs writes OUTSIDE the working tree, which is a reproducibility failure convention (o) cannot see because kit_build clones and never invokes it. It is a class, not a one-off: frontend/scripts/tile_layer_derivation.mjs:39 and :41, provider_mark_build.mjs:47-49, provider_mark_ingest_e.mjs:28, provider_mark_ingest_f.mjs:36-37, provider_mark_ingest_g.mjs:36, background_local_testing_verify.mjs:28, evidence_menu.mjs:8, evidence_feature_flow.mjs:16, plus fsmenu_proof.mjs:9, hud_reskin_proof.mjs:6 and scene_proof.mjs:13 which `import` playwright through `/Users/jt/math-sdk/frontend/node_modules/`. 25 tracked files under scripts/, frontend/scripts/ and tools/ contain a `/Users/jt/` literal. The gate that could see it declines by an exemption written for the wrong population: scripts/qa/doc_currency_gate.mjs:355-358 returns false for any token starting with `/` or `~`, reasoning "`/Users/jt/math-sdk/...` appears in this repository's reports; it is one person's checkout, not a repository fact" - the reason names REPORTS and the rule silently covers SCRIPTS.

**Fix (SMALL).** In frontend/scripts/tile_master_ingest.mjs replace :21 with a derived root, `const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '../..')` and `const TILE = join(ROOT, 'design-system/brand/tile')`, and make :20 a `--source <path>` argument defaulting to the recorded path so the machine-local read is declared rather than compiled in. Then add a gate step asserting no `/Users/` literal appears in any tracked *.mjs or *.py under scripts/, frontend/scripts/ or tools/ (reports/ stays exempt, which is what doc_currency_gate.mjs:355-358 actually meant), with a seeded violation. The gate enumerates the remaining 24 files rather than this pass rewriting them.

**Re-proof.** `grep -rn "/Users/" frontend/scripts/tile_master_ingest.mjs` returns nothing; running the script from a clone at an arbitrary path writes tile_composed_master.png into THAT clone's design-system/brand/tile/ and its landed sha still equals 741e77face74f7e93fb29790eb16dfc22f15b7a13d3d9f47a00a682e7c99434e; the new gate's self-test goes red on a seeded `/Users/` literal.

### S2-C072 (HIGH, reports/qa/stream_test/LEDGER.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** Both halves verbatim at HEAD. reports/qa/stream_test/LEDGER.md:8-10 still reads "JOB 4 consolidates the shards into this file. Until then this ledger holds only / the two entries below, and `shards/SHARD_INDEX.md` is the authority on what has / and has not been swept.", while :77 `## Consolidated shard findings` proves JOB 4 ran. reports/qa/stream_test/KNOWN_OPEN.md:41-43 reads "**The stream test ledger is at `reports/qa/stream_test/LEDGER.md`, its clusters at / `CLUSTERS.md`, and it holds 571 findings of which 506 are HIGH, MEDIUM or LOW and are / UNCLUSTERED AND UNVERIFIED.**"

**Derived cause.** "The header line is stale against its own file" describes the state; it is not the cause, and it misattributes the number. Two derivations.

(1) THE MECHANISM. `git show e0fd0b6 -- reports/qa/stream_test/LEDGER.md` ("test(stream): marshal the 47 shards to one ledger, discovery complete") has exactly ONE diff hunk, `@@ -76,5 +76,115 @@`, 112 insertions and 2 deletions. The two deleted lines are "Not yet populated. JOB 4 consolidates `shards/*.md` into this section after the / JOB 3 discovery squads complete." So the consolidating commit removed the not-yet-populated claim where it sat at the SECTION and never touched the identical claim in the document HEADER at :7-10, because its hunk begins at line 76. The claim was written in two places; one copy was updated. This is the two-sources-of-truth failure kit_build.mjs:58-59 states in this same repository: "Two constants that must agree are two constants that eventually will not."

(2) THE NUMBER IS NOT THE LEDGER'S. 571 is CLUSTERS.md's, not LEDGER.md's, and the two count DIFFERENT POPULATIONS. LEDGER.md:132 states "**540 findings: 43 STREAM, 160 HIGH, 228 MEDIUM, 106 LOW.**" over the 47-shard discovery wave, and its own shard table at :126-130 sums to exactly 540 (236+163+77+42+22). CLUSTERS.md:7 and :13-15 state "Marshalled ... over 55 active shards" and "**571 active findings: 60 STREAM, 183 HIGH, 242 MEDIUM, 81 LOW.** Superseded native shards at `shards/superseded/` are excluded". The population difference is derivable and exact: SHARD_INDEX.md:16-20 records 20 provisional shards moved to superseded/ and replaced by 28 re-run upscaled shards, and 47 - 20 + 28 = 55, which matches the filesystem (55 files in shards/*.md, 20 in shards/superseded/*.md). KNOWN_OPEN.md:42's "506" is arithmetically CLUSTERS.md's HIGH+MEDIUM+LOW (183+242+81 = 506), confirming it is quoting CLUSTERS while naming LEDGER. Neither document states which population its total describes, which is why a squad cannot reconcile 540, 571 and "two".

FOUND WHILE DERIVING, and it is independent of the above: neither total reconciles with its own class breakdown. LEDGER.md:132's classes sum to 537 (43+160+228+106) against a stated 540 that its shard table does support; CLUSTERS.md:13's classes sum to 566 (60+183+242+81) against a stated 571. The severity scale at LEDGER.md:15-19 defines exactly four tiers, so there is no fifth class absorbing the gaps.

**Fix (SMALL).** Three edits, all in reports/qa/stream_test/. (a) Delete or rewrite LEDGER.md:7-10 so the header states what the file holds now and names CLUSTERS.md rather than shards/SHARD_INDEX.md as the current authority. (b) Rewrite KNOWN_OPEN.md:41-43 to attribute each number to the file that holds it: LEDGER.md holds 540 over 47 discovery shards, CLUSTERS.md holds 571 over 55 active shards after the sight-gate re-run, and the two are different populations, not a discrepancy. (c) Reconcile the two class breakdowns against their totals, or state which findings the totals include that the tiers do not.

**Re-proof.** `grep -n "holds only" reports/qa/stream_test/LEDGER.md` returns nothing; the class figures at LEDGER.md:132 and CLUSTERS.md:13 sum to their stated totals; and KNOWN_OPEN.md:41-43 names 540 against LEDGER.md and 571 against CLUSTERS.md with the 47-vs-55 shard difference stated.

### S2-C076 (HIGH, WRS_MASTER_DOCUMENT.md)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** YES
- **Evidence:** LICENSE:1 "MIT License", LICENSE:3 "Copyright (c) 2025 Stake Engine", LICENSE:8 "to use, copy, modify, merge, publish, distribute, sublicense, and/or sell". Tracked: `git ls-files --error-unmatch LICENSE` succeeds; `git log -- LICENSE` shows one commit, 46b3525, 2025-11-27, "Add MIT License to the project", i.e. unchanged since the upstream math-sdk fork. The tree it sits over is public (`git remote -v` -> github.com/JTOSHIE/stake-game-development-claude) and holds WRS's own maths, art and frontend.

**Derived cause.** The recorded cause is "No root document addresses it". A root document DOES address it, and that is the actual defect: README.md:102-103 states "No Stake branding appears in any shipped asset or string. The repository carries no / open-source licence grant: it is published for platform review and audit, not for reuse." That flatly contradicts the tracked LICENSE:1-8, which IS an open-source licence grant permitting reuse and sale. The defect is CONTRADICTION, not silence, and the recorded cause would have sent a fixer to add prose to a document that already has prose. Provenance: README.md was rewritten on 2026-07-25 in commit 472c38f ("chore(repo): professional README, root reduced to its functional set") and asserted the intended outbound posture without touching the inherited LICENSE file. WRS_MASTER_DOCUMENT.md:28 is correctly NOT the owner of this: its "Licence archive" row is scoped to INBOUND tool licences ("Holds: Stability AI Community License ... audio provenance ... Add every future tool licence at adoption time"). So no register row anywhere owns the repository's OUTBOUND licence. Independently corroborated from a different squad's read at reports/qa/compliance_register/walk_shards/W13.md:41-42: "I opened the root `LICENSE` and it is 'MIT License / Copyright (c) 2025 Stake Engine', i.e. the upstream `math-sdk` licence, which covers the fork rather than the dependency set."

**Fix (PARK).** PARK. Owner and Fable decision, not a builder one, per convention (l.8). The one-line owner decision: does this repository grant MIT over We Roll Spinners' original maths, art and frontend, yes or no. If NO, replace LICENSE with a review-only / all-rights-reserved notice and keep README.md:102-103, checking first what the inherited upstream code under src/, utils/ and optimization_program/ requires be retained. If YES, delete the README.md:102-103 sentence. Either way add one WRS_MASTER_DOCUMENT.md section 1 row that owns the outbound licence, since section 1 currently owns only inbound tool licences; note that terms.md:355 (register R9-11) separately warrants third-party and copyleft licence compliance.

**Re-proof.** After the owner's ruling: README.md's licence paragraph and the root LICENSE file state the same position, checkable by `grep -n "licence grant" README.md` read beside `head -3 LICENSE`; and a WRS_MASTER_DOCUMENT.md section 1 row names which of the two is authoritative.

### S2-C081 (HIGH, COMPLIANCE_WATCH.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** COMPLIANCE_WATCH.md:308-310 at HEAD, verbatim: "- **SC display format contradiction, unresolved.** The only first-party currency table / found (repository only, live no longer publishes one) documents `XSC` as symbol `SC` / with `symbolAfter: true`, example `10.00 SC`." The parenthetical is false. docs/stake-engine-live/rgs-communication.md header records "resolved_url: https://stake-engine.com/docs/approval-guidelines/rgs-communication", "fetched: 2026-07-04", "looks_real: true", and holds the table at :47 ("Currency\tAbbreviation\tDisplay\tExample") and :83 ("Stake Cash\tXSC\tSC\t10.00 SC"). False again on the newer capture, docs/stake-engine-live/2026-07-29/approval_guidelines_rgs_communication.md:99, same row.

**Derived cause.** The falsity is real, but "the live docs do publish one" is an observation, not a cause, and STALE_NUMBER is the wrong family. The cause is an unstruck supersession, the same class the document itself names. The 2026-07-29 docs-watch entry at COMPLIANCE_WATCH.md:658-681 settles this item explicitly ("### Delta 2. `rgs-communication`: 13 new currencies, and XEC SETTLES THE OPEN CONTRADICTION" ... "**A third independent first-party source, agreeing with what we ship.**"), and at :662 it names and updates ONE earlier record ("Section 2 of the 2026-07-25 platform delta above") while striking NEITHER of the two. So at HEAD one document carries three live unmarked positions on one question: :308-313 "unresolved" with a false reason; :407, heading still reading "### 2. CONTRADICTION: SC display format. NEEDS A RULING, affects XSC as well as XEC."; and :658-681, settled. That is precisely the failure the same file diagnoses at :445-452 about entry 5 and entry 7, with the rule stated there: "**When adding an entry that resolves an earlier one, strike the earlier one in the same edit.**" That rule landed 2026-07-29 in commit 89dc510 and the 2026-07-29 docs-watch entry did not apply it to its own siblings. Riding with it, and it must be corrected in the same edit: WRS_MASTER_DOCUMENT.md:64 says "we ship the brief's form behind a single flip constant pending a ruling", the brief's form being leading `SC 1,000`. That is false at HEAD and contradicts COMPLIANCE_WATCH.md:410 ("We ship **trailing**, `1,000.00 SC`"). The shipped behaviour is trailing: frontend/src/lib/utils/currency.test.ts:46 asserts `formatBalance(1000 * S, 'XSC', 'en')` equals `'1,000.00 SC'`.

**Fix (SMALL).** One edit, three locations. (1) COMPLIANCE_WATCH.md:309: delete "(repository only, live no longer publishes one)" and cite docs/stake-engine-live/rgs-communication.md:83 and docs/stake-engine-live/2026-07-29/approval_guidelines_rgs_communication.md:99 instead. (2) Mark the :308-313 bullet and the :407 heading "SUPERSEDED BY THE 2026-07-29 DOCS WATCH, DELTA 2 (:658)" in the :434/:439 form. (3) WRS_MASTER_DOCUMENT.md:64: correct "we ship the brief's form" to trailing, citing frontend/src/lib/utils/currency.test.ts:46, and log in WRS section 9 per WRS_MASTER_DOCUMENT.md:2. Whether the comms item CLOSES stays with Fable per convention (l.8); this fix only stops the document contradicting itself and the source.

**Re-proof.** `grep -n "live no longer publishes one" COMPLIANCE_WATCH.md` returns nothing; `grep -n "NEEDS A RULING" COMPLIANCE_WATCH.md` returns only a line carrying a SUPERSEDED marker; the CI step "currency static assertions" (.github/workflows/checks.yml:439) stays green; `node scripts/qa/doc_currency_gate.mjs` stays green with an added `<!--CHECK: grep "XSC" docs/stake-engine-live/2026-07-29/approval_guidelines_rgs_communication.md-->`.

### S2-C085 (HIGH, REPLAY_TEST_EVENTS.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** REPLAY_TEST_EVENTS.md:66-71 is unchanged at HEAD, e.g. :66 `| Loss | `?mockCategory=base_loss` | n/a - no curated sample yet | n/a - no curated sample yet | n/a | n/a |`, and :73-78 still asserts "Cruise/antelite/super have no curated entries in `sample_rounds.json` yet". Against it, a node tally of frontend/src/lib/mock/sample_rounds.json returns antelite 12, cruise 12, super 7, i.e. 31 curated rounds across exactly those three modes. The recorded cell count is also one short: `grep -o "n/a - no curated sample yet" REPLAY_TEST_EVENTS.md | wc -l` returns 16, not 15.

**Derived cause.** The recorded text restates the observation (31 rounds, correct: 12+12+7) rather than giving a cause, and miscounts the cells 15 against 16. The cause is that the document was TRUE when written and nothing re-reads it. REPLAY_TEST_EVENTS.md was last committed at c23404c, 2026-07-08; `git show c23404c:frontend/src/lib/mock/sample_rounds.json` holds only 27 base and 31 bonus entries, no cruise, antelite or super. The three modes were curated afterwards in three separate later commits, none of which touched the doc: super at 6364384 (2026-07-15), cruise at f703dd5 (2026-07-24), antelite at b3df381 (2026-07-24). Nothing binds the two: `grep -n sample_rounds .github/workflows/checks.yml` is empty, so no gate reads the pool against the prose. The same stale fact therefore survives in two further places for the same reason, frontend/src/lib/mock/roundProvider.ts:57 and frontend/src/App.svelte:655-659. A SECOND defect in the same table that no row has recorded: the table gives a bare `?mockCategory=` value, but frontend/src/lib/mock/roundProvider.ts:84-90 filters by MODE as well as category (`(await samplesFor(mode)).find((s) => s.category === category)`) and frontend/src/App.svelte:1394 sets `const mode = $standingMode`, so `?mockCategory=cruise_loss` serves nothing unless Cruise is already the selected standing mode. A tester following the table verbatim would get the generic random board and read it as the documented fallback, i.e. the doc would appear to confirm itself.

**Fix (SMALL).** Replace REPLAY_TEST_EVENTS.md:64-78 from the measured pool. Twelve of the sixteen cells are fillable: cruise/antelite Loss (`cruise_loss`, `antelite_loss`), Normal win (`*_win_small` / `*_win_mid`), Big win (`*_win_large`), Bonus trigger (`trigger_3` / `trigger_4` / `trigger_5`); super Normal win (`super_win_small` / `super_win_mid`), Big win (`super_win_large`), Win cap (`super_wincap`), Retrigger and high meter (`super_retrigger` / `super_high_meter`). FOUR cells stay honestly n/a: cruise and antelite have no wincap and no retrigger/high-meter sample. Add one sentence stating the mode must be SELECTED as well as the parameter passed, citing roundProvider.ts:84-90. Correct the same stale prose at roundProvider.ts:57 and App.svelte:655-659.

**Re-proof.** node -e "const d=require('./frontend/src/lib/mock/sample_rounds.json');const m={};for(const e of d){(m[e.mode]??={})[e.category]=1}console.log(JSON.stringify(m))" matches every non-`n/a` cell in the rewritten table, and `grep -o 'no curated sample yet' REPLAY_TEST_EVENTS.md | wc -l` drops from 16 to 4.

### S2-C095 (MEDIUM, design-system/DESIGN_SYSTEM.md)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** YES
- **Evidence:** The NO_PROOF symptom for REQ-006 (no child or child-like characters, no art appealing to minors) is real at HEAD. `grep -rn "underage\|minor\|child" .github/workflows/checks.yml` returns exactly one hit, :644 "that the post-run assertion goes RED on a seeded leaked child", which is a leaked child PROCESS in preview_server_gate.mjs, not a character. And the cited specification says nothing about the question: design-system/DESIGN_SYSTEM.md:56-59 reads "## SCENE, BRAND AND INSTRUMENT MASTERS (non-symbol) / - Scene group (scene_character_car.svg): the racer plus hover car, the game's / identity character. The car is required (this is a car game). Placed left of / the frame; idle breathing only until Motion Polish."

**Derived cause.** The recorded cause is not a cause; it is an eyeball CONCLUSION offered as the proof, and the shipped asset refutes it. I opened frontend/public/assets/themes/future-spinner/ui/scene_character.png, the file SceneGroup.svelte:71 actually renders (`<img class="char-img" src="{$themeAssets.assetBase}/ui/scene_character.png" ... />`), 680x1344 read from the PNG IHDR. It is not "an adult-proportioned helmeted racer figure in an armoured suit standing beside the hover car". It is a stylised non-human mascot robot: a large dome helmet occupying roughly the top quarter of the figure, a bobble-tipped antenna, short limbs, no human anatomy, and no car in the frame at all (the car is a separate sprite, scene_car.png, per SceneGroup.svelte:6-11). Head-to-figure ratio reads at roughly 3.7 head heights against the 7-to-8 of adult figure canon. Acting on the recorded finding would have certified REQ-006 as met on a premise the artefact contradicts, which is the worst available outcome on a compliance requirement.

THE REAL CAUSE OF THE NO_PROOF: DESIGN_SYSTEM.md:56-59 is the only specification of the scene character in the repository, and it specifies the character by ROLE ("the racer plus hover car, the game's identity character") and by a master filename, with no age-coding, proportion, or figure-canon requirement stated anywhere. There is nothing for a gate, a test, or a reviewer to check against, which is why REQ-006 has no proof path in its current form. Compounding it, the line names a master the build no longer uses: SceneGroup.svelte:4-5 records "The old single baked scene_character_car.png (character + car together, pushed off-screen left) is replaced by two separately rendered sprites", and what ships is the externally enhanced 680x1344 raster adopted under CLAUDE.md's 2026-07-25 Assets amendment. design-system/masters/scene_character_car.svg still exists at HEAD but its only live reader is tools/brand/build_tile_hero.py:20. So the single line a compliance reader would open describes a retired asset and answers a different question.

**Fix (SMALL).** Two parts, and the second is not a builder call. (1) In design-system/DESIGN_SYSTEM.md, update the scene-group entry to name the shipped rasters (ui/scene_character.png 680x1344 and ui/scene_car.png) rather than the retired scene_character_car.svg, and add one explicit age-coding line stating the intended reading and the property that makes it checkable (non-human mascot, no human child anatomy, or a stated head-height figure). Only then can any gate or reviewer check it. (2) PARK the REQ-006 judgement to the owner and Fable with the measurement attached: the shipped figure reads at roughly 3.7 head heights as a big-domed mascot with a bobble antenna, which is a compliance reading, not a builder ruling. Owner decision: does a non-human toy-proportioned mascot engage "no art appealing to minors", or is the requirement satisfied because there is no child and no human?

**Re-proof.** `grep -n "scene_character.png" design-system/DESIGN_SYSTEM.md` returns the scene-group entry, `grep -n "scene_character_car.svg" design-system/DESIGN_SYSTEM.md` no longer does, and the file carries one stated age-coding property. The owner/Fable ruling on the mascot proportions is recorded in reports/FABLE_COMMS.md as a numbered item.

### S2-C098 (MEDIUM, scripts/kit_build.mjs)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** The literal observation is real: `grep -rn "kit_build\|kit-build" .github/` returns nothing (exit 1), and frontend/package.json's scripts are dev, build, preview, check, assets, qa:soak, owner:preview with no kit entry. The finding's stated method is wrong, though: there is no package.json at the repository root (`find . -maxdepth 2 -name package.json` returns only frontend/package.json). The DEFECT half does not hold for the two tree refusals: judgeTree at kit_build.mjs:86-104 takes only headOnRemote, frontendDirty and otherDirty, and treeFacts at :120-124 derives the dirty lists from `git status --porcelain -z`, which is empty on any CI checkout, so the refusal at :94 cannot fire in CI and a wired `--check` would be a gate that can only print PASS, the exact thing convention (p) forbids.

**Derived cause.** THE UNGUARDED THING IS livePart(), AND ITS PLACEMENT, NOT THE REFUSALS. scripts/kit_build.mjs:309 `const PART = livePart(readFileSync(join(clone, 'docs/records/upload-kit/00_READ_ME_FIRST.md'), 'utf-8'))` is called AFTER :280 `rmSync(KIT, { recursive: true, force: true })` has already deleted the existing kit, and after :281-307 have populated 02_frontend_upload/ with the full bundle, copied in 00_READ_ME_FIRST_SECOND_VISIT.md and built 03_branding/. livePart THROWS by design when the walkthrough does not name exactly one unsuperseded `# PART 9x:` heading (:160-163), and its own doc comment at :151-154 states why: "A kit whose README cannot name the visit it belongs to is worse than no kit." But at :309 the throw PRODUCES that: a Desktop folder holding the full bundle with no README.md (written at :361) and no BUILD_INFO.json (:363-366), so no commit SHA, no "This kit is SINGLE USE", no "~/Desktop/FS_UPLOAD_KIT/ is DEAD" - the unlabelled stale kit on the Desktop that is precisely the TR-062 shape this script exists to prevent. The tmpdir clone leaks too, because `rmSync(work, ...)` at :368 is never reached.

The trigger is an ordinary docs commit, not an exotic one: adding `# PART 9j:` without marking `# PART 9i:` SUPERSEDED gives two live parts. That exact mistake is recorded as having shipped: TR-100 in docs/records/reviews/REVIEW_TRACKER.md:275 records that kit_build wrote the literal `PART 9e` for three kit versions while the walkthrough had moved on. Today the invariant holds - `grep -n "^# PART 9" docs/records/upload-kit/00_READ_ME_FIRST.md` shows eight headings of which only `1101:# PART 9i: THE v10 VISIT` is unsuperseded - and eight superseded headings across ten kit versions is the rate at which this line gets edited.

The secondary point the recorded finding gestures at without reaching: selfTest() at :167-212 reads the REAL walkthrough at :191 and its case at :193-194 asserts the resolved part matches `/^PART 9[a-z]$/`, so `--self-test` would catch a two-live-parts state before any build. Nothing runs it, while sixteen other gates run `--self-test` in checks.yml (:147, :198, :259, :303, :333, :538, :557, :622, :624, :626, :628, :630, :636, :641, :646, :655). It is CI-safe by construction: :215 `if (argv.includes('--self-test')) process.exit(selfTest() ? 0 : 1)` runs BEFORE :218 `const facts = treeFacts()`, so it never reaches the refusals that cannot fire in CI.

**Fix (ONE_LINE).** Move line 309 (`const PART = livePart(...)`) up to immediately after the clone-HEAD verification at :242, before `npm ci` (:246), before `npm run build` (:248) and before the destructive `rmSync(KIT)` at :280, reading the walkthrough from the clone as it already does. The throw then costs nothing and destroys nothing. Then add one step to checks.yml beside the other sixteen: `- name: kit build, seeded-violation self-test` / `run: node scripts/kit_build.mjs --self-test`. It needs no browser, no build and touches no file. Do NOT wire `--check`: the tree refusals cannot go red in CI.

**Re-proof.** Append `# PART 9j: TEST` to docs/records/upload-kit/00_READ_ME_FIRST.md, then `node scripts/kit_build.mjs` exits with the two-live-parts error BEFORE the clone and `~/Desktop/FS_UPLOAD_KIT_V10/` is untouched; and `node scripts/kit_build.mjs --self-test` prints `KIT BUILD SELF-TEST: PASS` as a green checks.yml step.

### S2-C100 (MEDIUM, COMPLIANCE_WATCH.md)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** The two bullets are verbatim at HEAD. COMPLIANCE_WATCH.md:21 "- **No Stake branding:** verified. No Stake trademark or themes in any shipped asset or text." and :22 "- **No underage appeal:** verified. No child or child-like characters." Neither carries a date, a method or a citation; nor does :19 ("- **Original IP:** verified."). But the symptom as framed, that these lines are the unproven evidence for REQ-137, rests on a mis-mapping, so PARTIAL.

**Derived cause.** Two separate defects, both derivable; the recorded row states neither, because it is a bare quote with no cause attached. (1) MAPPING. REQ-137's source is reports/qa/compliance_register/REGISTER.md:207 -> `terms.md:363+373`, warranty (v) "does not incorporate, contain or refer to any Prohibited Content" (docs/stake-engine-live/2026-07-29/terms.md:373), bound by the definition at terms.md:112: "means advertising or content that: (a) promotes pornographic material or is lewd, profane, obscene, unlawful; (b) is defamatory, libellous, discriminatory or constitutes \"hate speech\"; (c) infringes the rights (including the Intellectual Property Rights) of third parties; and/or (d) incites or encourages racism." "No Stake branding" and "no underage appeal" are approval-guidelines postures, sourced at docs/stake-engine-live/2026-07-29/approval_guidelines.md:26 and :28 respectively, a different instrument, and neither falls in any of the four categories. The walk squad picked the nearest-looking posture bullets, which is exactly the error REGISTER.md:18-22 warns against, and its own note at reports/qa/compliance_register/walk_shards/W12.md:151-158 already reached the honest answer. Re-run at HEAD: no gate, test or committed audit addresses any of the four terms.md:112 categories. (2) THE WORD "verified". COMPLIANCE_WATCH.md:19, :21 and :22 each assert "verified" with neither date nor method, which CLAUDE.md protocol rule 16 (transcribed 2026-07-29, commit 7dae3ac) forbids: "**VERIFIED**, with the DATE it was checked AND the METHOD used ... A date without a method is not VERIFIED." And no method exists to cite for the Stake-branding half: `grep -in "brand\|trademark\|underage\|minor" .github/workflows/checks.yml` returns zero, so nothing in CI would go red if Stake branding arrived.

**Fix (SMALL).** Two parts, different files. (1) COMPLIANCE_WATCH.md:19-22: give each posture bullet a date and a named method per protocol rule 16, or downgrade "verified" to REPORTED with its source. (2) reports/qa/compliance_register/REGISTER.md:207: remove COMPLIANCE_WATCH.md:21-22 as REQ-137's proof path; the honest entry is NO_PROOF with no implementation path, which walk_shards/W12.md:151-158 already concluded, and citing a project posture as proof is what REGISTER.md:18-22 exists to forbid.

**Re-proof.** `grep -n "verified\." COMPLIANCE_WATCH.md` shows each posture bullet carrying a date and a method; `grep -riE "profan|obscene|hate speech|pornograph|defamator|racis" .github/workflows/checks.yml frontend/scripts scripts` still returns zero, which is the statement REGISTER.md:207 should then carry rather than a false proof path.

### S2-C101 (MEDIUM, COMPLIANCE_WATCH.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** Re-run at HEAD: `grep -rniE "notify (carrot|the platform)|written notice|defect notification|notify .* in writing" --include=*.md .` excluding docs/stake-engine-live/, reports/qa/compliance_register and reports/qa/session* returns ZERO. COMPLIANCE_WATCH.md:32 "## Process reminders" holds exactly two bullets, :34-35 (Portal Developer Testing Tool, pre-review) and :36-37 (upcoming platform features, roadmap only). The obligation is real: docs/stake-engine-live/2026-07-29/terms.md:293 "i. The Developer shall immediately notify Carrot in writing of any Errors it has detected in the services.", registered at REGISTER.md:248 as REQ-178.

**Derived cause.** The recorded text is the symptom restated ("the register returns ZERO hits") plus a true but non-causal observation about WAYS_OF_WORKING, and it points at the wrong surface. COMPLIANCE_WATCH.md:32-37 is a PRE-SUBMISSION reminders block: both bullets are about getting to review. The surface that actually owns this exists and is elsewhere: WRS_MASTER_DOCUMENT.md:221 "## 6. POST-RELEASE OPERATIONS". Its three bullets model the relationship as INBOUND ONLY: :222 "Change policy: cosmetic-only; anything else requires Stake's request." and :223 "Weekly: check Friday re-rank position; monitor approval-thread/messages; record payments per cycle against the payments register." Every duty recorded there waits for Stake to move first. terms.md:293 puts the first move on us, and terms.md:295 and :297 add continuing correction duties ("correct any Errors discovered during acceptance testing", "correct any Errors arising throughout the term of the Agreement"). So the cause is not a missing bullet in a reminders list; it is that the one section owning post-release conduct is written on an inverted model of who initiates, and therefore had no slot for an outbound duty to omit.

**Fix (SMALL).** Add to WRS_MASTER_DOCUMENT.md section 6 (after :222) a bullet quoting terms.md:293 verbatim per convention (l.7), with the capture date, naming who sends the notice (JOSH), the route (the approval thread, or support@stake-engine.com per docs/stake-engine-live/2026-07-29/privacy.md:30) and where the sent copy is filed; add a second bullet for the continuing correction duties at terms.md:295 and :297. Log in WRS section 9 per WRS_MASTER_DOCUMENT.md:2. Cross-reference from COMPLIANCE_WATCH.md:32-37 rather than duplicating the text, so there is one owner.

**Re-proof.** `grep -rn "immediately notify Carrot in writing" WRS_MASTER_DOCUMENT.md` returns a hit, and the grep in symptom_evidence stops returning zero.

### S2-C104 (MEDIUM, scripts/kit_build.mjs)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** YES
- **Evidence:** REQ-181 is docs/stake-engine-live/2026-07-29/terms.md:189: "Within three (3) days of the successful acceptance by Carrot pursuant to clause 3.2b), to the extent it has not already done so, the Developer shall deliver to Carrot one copy of the object code for the Game, as it exists as of that date. During the term of this Agreement, the Developer shall provide to Carrot any updated object code within ten (10) days after completion of any modifications or revisions to the Game or creation of any New Versions." A tree-wide grep for "object code" and "within (three|ten|3|10) days" outside docs/stake-engine-live/ and reports/qa/compliance_register/ returns only the register's own derived artefacts (REGISTER.md:251, shards/R9_commercial-legal-triage.md:40, walk/W14_commercial-legal-triage_p3.md:10 and :35-36, walk_shards/W14.md:50-55) and the Session 3 park list. Neither OWNER_CHECKLIST.md (five outstanding items at :27-116) nor WRS_MASTER_DOCUMENT.md:36-40 names the obligation, an owner, a clock or a done-when.

**Derived cause.** kit_build.mjs:14-18 is convention (o), which governs how a bundle is BUILT reproducibly. REQ-181 governs WHEN a built bundle is DELIVERED to Carrot after acceptance or after a modification. They are about different events, and "not in checks.yml" cannot be the cause of an obligation no CI gate could ever observe, because delivery is an upload to a third party's portal.

THE REAL CAUSE: the repository records kit BUILDS and never records kit UPLOADS, so the ten-day clock has an observable START and no observable STOP. The start is repo-observable ten times over: VERSION holds `10`, scripts/kit_build.mjs:65-76 reads it and stamps it into KIT_NAME (:76) and into the kit README (:314-320), and frontend/vite.config.ts stamps the same value into build-info.json, so "completion of any modifications" is a dated committed fact. The stop is recorded nowhere: docs/records/upload-kit/ holds only 00_READ_ME_FIRST.md, 01_maths_HASHES.txt and 03_branding_HASHES.txt, no upload log; and the walkthrough states the gap in its own voice at :1114-1115, "**What you are working from:** `~/Desktop/FS_UPLOAD_KIT_V10/`. Delete every older kit, including V9, which was never confirmed uploaded." OWNER_CHECKLIST.md:61 carries "Upload kit V10 and screenshot the v10 console line" as an outstanding action with no date field and no successor row. The obligation has no done-when because the act it turns on is never written down when it happens.

SCOPE NARROWED BY THE CLAUSE'S OWN TEXT: the 3-day half carries the carve-out "to the extent it has not already done so", and the portal upload documented step by step at SUBMISSION_DOSSIER.md:228-249 IS that delivery and happens before acceptance, so the 3-day window is discharged by the existing flow. Only the 10-day half is genuinely unrecorded, and it interacts with the post-approval lockdown at WRS_MASTER_DOCUMENT.md:37.

**Fix (SMALL).** Add docs/records/upload-kit/UPLOAD_LOG.md: one row per kit (version, commit SHA, built date, uploaded date, confirming screenshot path), seeded with what is known - V10 pending, V9 never confirmed uploaded per 00_READ_ME_FIRST.md:1115 - and make writing the row a numbered step of the live PART beside the existing delete-the-old-kits step. Then add one OWNER_CHECKLIST.md row naming the ten-day clock, the owner as its owner, and "row written in UPLOAD_LOG.md" as its done-when. PARK the legal reading for the owner and Fable: does the ten-day clock start at kit build, at merge to main, or at the owner's upload? That is a contract reading, not a builder call.

**Re-proof.** `ls docs/records/upload-kit/UPLOAD_LOG.md` exists and its V10 row names a commit SHA that `git cat-file -e` resolves; `grep -n "ten (10) days\|10-day" OWNER_CHECKLIST.md` returns a row with a named owner and a done-when; and `grep -rn "object code" OWNER_CHECKLIST.md WRS_MASTER_DOCUMENT.md` is no longer empty.

### S2-C105 (MEDIUM, OWNER_CHECKLIST.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** OWNER_CHECKLIST.md:52-54: "**Never send that address to us and never put it in the repository.** This repository is public. We record that the wallet is configured and nothing else". That sentence is load-bearing for a money-security rule, and nothing anywhere records or defends it. `grep -n 'github.com' WRS_MASTER_DOCUMENT.md README.md COMPLIANCE_WATCH.md OWNER_CHECKLIST.md SUBMISSION_DOSSIER.md` returns nothing; `grep -rn 'Carrot'` over the same five returns nothing. The obligation it would satisfy is docs/stake-engine-live/2026-07-29/terms.md:179, clause 3.6a: "Carrot and/or its Nominee will have continuous, real-time access to the Developer's development platform or environment where the Game and any New Games are being developed." .github/workflows/checks.yml:93-94 is `push:` / `branches: [main]` and reads no repository visibility.

**Derived cause.** Both greps in the recorded cause reproduce exactly at HEAD and checks.yml:93-94 is as quoted, so the row is factually accurate. But it describes absences rather than a cause, and it never established the one fact that decides the finding. VERIFIED 2026-07-29 by direct API read (`gh repo view JTOSHIE/stake-game-development-claude --json isPrivate,visibility` returns `{"isPrivate":false,"visibility":"PUBLIC"}`; remote is git@github.com:JTOSHIE/stake-game-development-claude.git): the premise at OWNER_CHECKLIST.md:53 is TRUE and clause 3.6a is in fact satisfied today. So the defect is not an unmet grant, it is an unrecorded one, and the shape of the omission is what makes it dangerous. WRS_MASTER_DOCUMENT.md:8-17 argues from publicness only as a HAZARD ("a payout address in a public repository is permanent, is trivially linked to revenue, and cannot be withdrawn once indexed"), and nowhere is publicness recorded as a contractual REQUIREMENT. A future session tidying "why is a compliance repository public" therefore finds every reason to flip the GitHub setting to private and no reason not to; the flip is one click, leaves no commit, and no gate reads it. That asymmetry, hazard documented and obligation undocumented, is the cause.

**Fix (SMALL).** Add a row to WRS_MASTER_DOCUMENT.md section 1 recording: repository https://github.com/JTOSHIE/stake-game-development-claude, visibility PUBLIC, the verification date and method, and terms.md:179 clause 3.6a as the reason it must remain so. Cross-reference it from OWNER_CHECKLIST.md:53 so the wallet-address security rule and the Carrot access grant read as two consequences of one recorded fact rather than one unsourced assertion. PARK for the owner: whether to add a CI assertion that visibility equals PUBLIC. It is cheap (`gh repo view --json visibility`) but adds a token-scoped API call to every run, and the owner should decide whether a contractual setting belongs in a build gate.

**Re-proof.** `grep -n 'github.com/JTOSHIE' WRS_MASTER_DOCUMENT.md` returns the recorded row citing terms.md:179, and `gh repo view JTOSHIE/stake-game-development-claude --json visibility` returns PUBLIC.

### S2-C107 (MEDIUM, WRS_MASTER_DOCUMENT.md)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** YES
- **Evidence:** WRS_MASTER_DOCUMENT.md:37 verbatim at HEAD: "- Post-approval lockdown: only minor cosmetic updates after approval; NO math changes, NO new modes, NO gameplay changes. Everything ships final." The second half holds too: `grep -iE "corrective action|rectify|deactivat|remediat"` over WRS_MASTER_DOCUMENT.md, COMPLIANCE_WATCH.md, SUBMISSION_DOSSIER.md, OWNER_CHECKLIST.md and docs/records/WAYS_OF_WORKING.md returns nothing on point; WRS:27's phrase "removal conditions bind us" is the only gesture at it and nowhere says what they are.

**Derived cause.** The recorded row is a verbatim quote plus a negative observation and names no cause at all, which is why it failed. The real cause is one line and it is a dropped clause. Source, verbatim at docs/stake-engine-live/2026-07-29/approval_guidelines.md:34: "Once a game has been approved for publication on Stake/Stake-US, only minor updates to address visual issues are permitted, **unless otherwise requested by the Stake Engine team**. Changes to the underlying math model, the addition of new game modes, or modifications to gameplay mechanics will not be allowed." WRS_MASTER_DOCUMENT.md:37 renders that as an absolute and drops the carve-out, ending "Everything ships final." The dropped clause IS the remediation route REQ-189 asks for: REGISTER.md:259 sources REQ-189 at terms.md:385, clause 7.1b.ii, "Carrot will notify the Developer and the Developer will take immediate corrective action, in good faith and in cooperation with Carrot and Stake ... to rectify that issue as soon as reasonably practicable at no cost to Carrot", with terms.md:387 (7.1c) deactivation and licence termination if it is not remedied. Read literally, WRS:37 forbids the change the contract compels. That this is a local paraphrase error rather than a project-wide misreading is provable from the same document: WRS_MASTER_DOCUMENT.md:222 gets it right, "Change policy: cosmetic-only; anything else requires Stake's request." Two other root documents drop the same clause the same way, COMPLIANCE_WATCH.md:82-83 and SUBMISSION_DOSSIER.md:13. This is a convention (l.7) breach: "Compliance text is quoted, never paraphrased."

**Fix (SMALL).** Replace WRS_MASTER_DOCUMENT.md:37 with the verbatim approval_guidelines.md:34 sentence and its capture date. Add a second section 2 bullet quoting terms.md:385 and :387 verbatim, so the notified-breach remediation route and its consequence are on the register. Apply the same correction at COMPLIANCE_WATCH.md:82-83 and SUBMISSION_DOSSIER.md:13. Log in WRS section 9.

**Re-proof.** `grep -n "unless otherwise requested by the Stake Engine team" WRS_MASTER_DOCUMENT.md COMPLIANCE_WATCH.md SUBMISSION_DOSSIER.md` returns a hit in each file, and `grep -n "Everything ships final" WRS_MASTER_DOCUMENT.md` returns nothing.

### S2-C109 (MEDIUM, WRS_MASTER_DOCUMENT.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** WRS_MASTER_DOCUMENT.md:24 verbatim at HEAD: "| **Payout wallet** | **DONE, CONFIGURED 2026-07-28** | JOSH | Configured on the platform. **ERC-20, USDT.** The address is **held offline only and is never recorded in this repository**, per the privacy rule above. Two facts from the platform's own wallet page that are worth carrying: payouts run at the beginning of each month for the previous month, and **failed payments cannot be recovered** ...". The recorded "the one-wallet-per-team count is verified nowhere" is only half true: WRS_MASTER_DOCUMENT.md:32 already carries "One wallet per team." and OWNER_CHECKLIST.md:147 records "| **The payout wallet, configured** | 2026-07-28 | Configured on the platform, ERC-20 USDT." The requirement text itself (REGISTER.md:264 -> payments.md:81 "Add a wallet on your team settings - one wallet per team.") is recorded. What is genuinely absent is any evidence pointer, plus a source for one quoted platform claim.

**Derived cause.** Two things, both checkable. (1) WRS_MASTER_DOCUMENT.md:24 is the only DONE row from the 2026-07-28 sitting that cites no evidence. Its neighbours all do: :23 cites `reports/screens/live-portal-2026-07-28/082628_frame.png`; :26 cites frames `082429` and `082546`; :21 gives ASIC dates. The privacy rule at :6-10 does not explain the absence, because it forbids publishing the ADDRESS, not citing evidence that a wallet exists, and :10 in fact prescribes the substitute: "the register records **that it exists and where it is held**, and nothing more". (2) One of the two "facts from the platform's own wallet page" has no source anywhere in the repository. "Payouts run at the beginning of each month for the previous month" IS in the mirror, uncited and paraphrased, at docs/stake-engine-live/2026-07-29/payments.md:82 "Payouts run on the 1st of every month. A paid invoice appears first, then funds are sent within 12 hours." But "failed payments cannot be recovered" is in NO capture: `grep -rnE "cannot be recovered|not be recovered|recovered" docs/` returns only terms.md:470, :472 and :660, all about recovering costs and damages, and there is no capture of a team payout-wallet page at all (docs/stake-engine-live/2026-07-29/ holds payments.md and rgs_wallet.md, the latter being the RGS wallet API). The same unsourced sentence is load-bearing elsewhere: OWNER_CHECKLIST.md:44-45 makes it the entire stated reason checklist item 2 exists ("The platform's own wallet page says it plainly: **failed payments cannot be recovered.**"). That is a convention (l.3) and (l.7) breach, not a privacy-rule consequence.

**Fix (SMALL).** (1) At WRS_MASTER_DOCUMENT.md:24, cite docs/stake-engine-live/2026-07-29/payments.md:82 and requote the payout-timing sentence verbatim. (2) For "failed payments cannot be recovered": either mirror the platform wallet page that carries it into docs/stake-engine-live/<date>/ and cite it, or mark it UNKNOWN per protocol rule 16 at BOTH WRS:24 and OWNER_CHECKLIST.md:44-45, since the checklist item rests on it. (3) Record the wallet in the privacy rule's own prescribed form (:10, "that it exists and where it is held") with an address-cropped frame as the evidence pointer, so the row matches its neighbours. Log in WRS section 9.

**Re-proof.** `grep -rn "cannot be recovered" docs/stake-engine-live/` returns a hit, OR the phrase no longer appears in WRS_MASTER_DOCUMENT.md and OWNER_CHECKLIST.md as an asserted platform quote; and WRS_MASTER_DOCUMENT.md:24 carries a `reports/screens/...` or `docs/stake-engine-live/...` citation like every other DONE row in the same sitting.

### S2-C117 (MEDIUM, frontend/src/lib/stores/betLadder.ts)

- **Recorded cause:** REFUTED
- **Symptom still real at HEAD:** YES
- **Evidence:** REQ-122's source is `docs/stake-engine-live/2026-07-29/rgs.md:289` "The bet must be divisible by stepBet." Nothing at HEAD asserts it. `grep -rn stepBet` over `frontend/src`, `frontend/scripts`, `scripts` and `.github` returns only: the AuthResponse declaration (`frontend/src/lib/services/rgsService.ts:170`, `:267` `stepBet:   number       // dollars`), its population (`:567` `stepBet: microsToDisplay(config.stepBet ?? 0)`), a prose comment (`:615`), the BetSelector comment (`frontend/src/lib/components/BetSelector.svelte:26`), six Playwright fixtures that merely hand a stub `stepBet: 100_000`, two `featureResume.test.ts` fixtures (`:63`, `:197`), and `frontend/src/lib/services/rgsService.contract.test.ts:99` `check('stepBet comes from config, in micros', auth.stepBet, 0.1)` plus `:173`'s test NAME string `'amount is a number, not a string (the platform checks amount % stepBet)'`. No assertion anywhere of the form "every offerable level is a whole multiple of stepBet". The two cited blocks are verbatim at HEAD: `frontend/src/lib/stores/betLadder.ts:105-112` (the "MINSTEP IS RESPECTED BY CONSTRUCTION" comment) and `:123-129` (`setBetLevel`, `if (!levels.includes(level)) return false`).

**Derived cause.** The load-bearing sentence of the recorded cause, carried from `frontend/src/lib/components/BetSelector.svelte:25-31`, is false at HEAD. It says the assertion "is NOT possible today" because `stepBet` "is read by `rgsService.authenticate()` into its `AuthResponse` and then dropped, because only `betLevels` is published to a non-locked store", so "Adding the passthrough is a one-line change to a LOCKED file and this brief carries no lock exception". Three refutations from source: (i) `stepBet` is a DECLARED PUBLIC FIELD of `AuthResponse` (`rgsService.ts:267`) and is populated on every authenticate (`:567`), so it is returned, not dropped; (ii) the NON-LOCKED `frontend/src/lib/stores/sessionRecovery.ts:201` `const auth = await platform.authenticate(params)` already receives that whole object on every production boot, reached at `frontend/src/App.svelte:1210` `await recoverSession(import.meta.env.DEV, undefined, presentRecoveredRound, offerResume)`, and its only early return is `if (isDev)` at `sessionRecovery.ts:194`; (iii) a non-locked test already reads the field, `rgsService.contract.test.ts:99`. No lock sanction is required to write this assertion, so the parked disposition that followed from the stated cause is unfounded. REAL CAUSE, TWO PARTS. (1) Nobody wrote the assertion; there is no mechanical obstacle. (2) The "by construction" argument at `betLadder.ts:105-112` has a production hole it does not record. `betLadder.ts:39-41` `derived(rgsBetLevels, ($levels) => $levels.length > 0 ? $levels : BET_LEVELS)` guards on LENGTH, not on environment, and its feed is `rgsService.ts:568` `betLevels: (config.betLevels ?? []).map(microsToDisplay)`. An authenticate response that omits or empties `config.betLevels`, which `rgs.md:287` expressly permits ("Although bet levels are not mandatory"), silently serves the hardcoded `gameStore.BET_LEVELS` in PRODUCTION, not just in the "mock and dev runs" the source claims at `betLadder.ts:109` and `BetSelector.svelte:12-13`. In that state every value the panel offers is one WE chose, `setBetLevel`'s refusal guarantees nothing about the platform's increments, and the requirement is not held by construction at all.

**Fix (SMALL).** Two non-locked changes, no sanction needed. (a) Publish `auth.stepBet`, `auth.minBet` and `auth.maxBet` to a new non-locked store from `frontend/src/lib/stores/sessionRecovery.ts:201`, which already holds them, exactly as `rgsBetLevels` is published today; then add a new betLadder stepBet test beside frontend/src/lib/stores/betLadder.test.ts (to be created, so it is deliberately not written as a citation) asserting for every entry of `activeBetLevels` that `Math.abs(level / stepBet - Math.round(level / stepBet)) < 1e-9` and `minBet <= level <= maxBet`, and wire it into `.github/workflows/checks.yml` beside the existing `wallet contract` step. (b) Correct the two comments that record the refuted diagnosis: `BetSelector.svelte:25-31` (the named sanction request is not needed) and `betLadder.ts:105-112` (the "mock and dev" description of the fallback at `:109` misstates a length guard as an environment guard). Whether the empty-`rgsBetLevels` case should instead become a blocked-session state rather than a silent local ladder is an owner call and collides with REQ-121, so it is not folded into this fix.

**Re-proof.** `npx tsx src/lib/stores/betLadder.stepBet.test.ts` passes with a stubbed auth of `stepBet 0.1` and the ladder from `rgsBetLevels`, and goes RED under a convention (p) seeded off-step level; and `grep -n 'NOT possible today' frontend/src/lib/components/BetSelector.svelte` returns nothing.

### S2-C123 (MEDIUM, reports/qa/math_selfaudit_2026-07-14.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** PARTIAL
- **Evidence:** The disclosure defect is real; the "ungated" half is not. Real: the reviewer question at docs/stake-engine-live/2026-07-29/approval_guidelines_math_verification.md:38, "List the number of non-zero weight payouts. Zero-weight payouts should not dominate the provided simulations", is answered only at reports/qa/math_selfaudit_2026-07-14.md:119-125 (base 10,930; cruise 5,837; antelite 14,814; bonus 37,193; super 46,049). `grep -n "non-zero\|unique payout" SUBMISSION_DOSSIER.md` returns nothing, and `grep -rn "math_selfaudit" SUBMISSION_DOSSIER.md COMPLIANCE_WATCH.md` returns nothing, so the figure never reaches the document a reviewer reads. WRS_MASTER_DOCUMENT.md:85 compounds it, still recording "Math self-audit vs approval criteria | reports/qa/math_selfaudit_*.md | OPEN (JOB 3b, addendum ... still not started)" for a document that exists and is complete. Also confirmed literally: scripts/validate_math.py:56 `MIN_UNIQUE_PAYOUTS = 10` and :163 `check(r["unique_payouts"] >= MIN_UNIQUE_PAYOUTS, ...)`.

**Derived cause.** The gate half of the recorded cause is refuted and the floor half is a category error. (a) "`validate_math.py` appears nowhere in `.github/workflows/checks.yml`" is true of that one file and false as a diagnosis: .github/workflows/validate-math.yml:37 is `run: python3 scripts/validate_math.py`, and it has fired, not merely existed - the workflow landed at 60ddf17 (2026-07-04) and `git log 60ddf17..HEAD` over its trigger paths returns 63fa1c0 and 05b6407, two later commits that match. (b) `MIN_UNIQUE_PAYOUTS = 10` is a compliance-rubric floor doing a different job from the disclosure; treating it as the proof the disclosure lacks compares a threshold with a report.

THE REAL CAUSE IS A DISCLOSURE-ROUTING GAP, NOT A GATE GAP. The numbers are already derived, exactly reproducible and printed by a committed script. scripts/validate_math.py:156 prints `unique payouts` per mode; I re-ran it at HEAD and it emitted base 10,930, cruise 5,837, antelite 14,814, bonus 37,193, super 46,049, digit for digit the table at math_selfaudit_2026-07-14.md:121-125, with `MATH VALIDATION: ALL COMPLIANCE CHECKS PASS`. What is missing is that the answer never travels: SUBMISSION_DOSSIER.md's platform-limits table at :533-537 lists RTP band, outcomes per mode and max win, and has no row for non-zero weight payouts, and the dossier never cites the self-audit that holds it.

A SECOND, SHARPER GATE FINDING THAN THE RECORDED ONE, worth naming because it is not what the finding said: validate-math.yml:11-22 is PATH-FILTERED to games/future_spinner/library/publish_files/index.json, lookUpTable_*.csv, scripts/validate_math.py and the workflow file. Every one of those data paths sits inside games/future_spinner/**, which CLAUDE.md locks and whose published lookup tables it calls "frozen truth ... never regenerated outside an owner-sanctioned pass". So the workflow can only fire on a change that is itself forbidden without a lock sanction. It is not ungated; it is a gate that by construction cannot fire on ordinary work.

**Fix (SMALL).** Two documentation edits and one gate scoping note. (1) Add a row to SUBMISSION_DOSSIER.md section 8c carrying the per-mode non-zero unique payout counts with `scripts/validate_math.py` named as the computation and reports/qa/math_selfaudit_2026-07-14.md:117-131 as the source, so the reviewer's question is answered in the document the reviewer opens. (2) Correct WRS_MASTER_DOCUMENT.md:85 from "OPEN ... still not started" to DONE with the path, since the artefact exists and its figures reproduce. (3) Record beside validate-math.yml that its path filter means it fires only on a lock-sanctioned maths change, so nobody later reads a green history as continuous coverage.

**Re-proof.** `grep -n "non-zero weight payouts" SUBMISSION_DOSSIER.md` returns the new row; `python3 scripts/validate_math.py | grep "unique payouts"` prints the same five figures the row states; and `grep -n "Math self-audit" WRS_MASTER_DOCUMENT.md` no longer reads OPEN.

### S2-C216 (LOW, WRS_MASTER_DOCUMENT.md)

- **Recorded cause:** PARTIALLY_RIGHT
- **Symptom still real at HEAD:** YES
- **Evidence:** WRS_MASTER_DOCUMENT.md:27 verbatim at HEAD: "| Stake Engine developer ToS acceptance | DONE | JOSH | Accepted at account creation. Record date if known. Re-read before submission; post-approval lockdown and removal conditions bind us. |" Status DONE, and the Notes field still carries the unexecuted instruction "Record date if known" with no date supplied. Separately and more importantly, this is not REQ-190's row: REGISTER.md:260 sources REQ-190 at `privacy.md:34+36-41`, and `grep -rnE "18 years or older|individual developer|authoriz(ed|ised) company representative" --include=*.md` over the tree outside docs/stake-engine-live/ returns hits only in the register shards themselves, never in a WRS_MASTER_DOCUMENT.md row.

**Derived cause.** The recorded cause "in-repo proof is impossible by design, see WRS_MASTER_DOCUMENT.md:6-10" is the half that produces the wrong action. The privacy rule does not say proof is impossible; it supplies the proof form, at :10: "Where a value exists but must not be published, the register records **that it exists and where it is held**, and nothing more." The register used that form for the ABN (:21) and the wallet (:24) and did not use it here. The actual cause is a missing row plus a mis-mapped citation. REQ-190 is the platform's registration-data set, quoted at docs/stake-engine-live/2026-07-29/privacy.md:34 and :36-41: name, email, "A link to your GitHub repository", a description of intended use, "Confirmation that you're 18 years or older", and "Confirmation of your status as either an individual developer or an authorized company representative". WRS_MASTER_DOCUMENT.md:27 records ToS ACCEPTANCE, a different obligation, and was cited because it is the nearest account-creation row in section 1. Section 1 has no row for the six declarations; the only one recorded anywhere is the GitHub link, and it lives outside the register at CLAUDE_PROJECT_INSTRUCTIONS_v7.md:3. The developer-status declaration is now materially live rather than formal: WRS_MASTER_DOCUMENT.md:21 records ASIC registration of We Roll Spinners on 2026-07-27 with the note that a lapsed business name is "a live compliance problem on a platform that requires the publisher name to match", so which of the two statuses was declared matters. Independently reached by the originating squad at reports/qa/compliance_register/walk_shards/W15.md:69-71: "the correct proof would be a register row saying the registration items are held off-repo, which is exactly what the rule's own pattern asks for and what is missing."

**Fix (SMALL).** Two edits in WRS_MASTER_DOCUMENT.md section 1. (1) Add a row in the :10 prescribed form: "Stake Engine account registration declarations | <DONE/OPEN> | JOSH | The six items at docs/stake-engine-live/2026-07-29/privacy.md:34,36-41 were supplied at account creation; held on the platform account and deliberately not recorded here, per the privacy rule at :6-10. Declared status: <individual developer | authorised company representative>." (2) On :27, either fill the acceptance date or replace "Record date if known" with an explicit UNKNOWN per protocol rule 16, because a DONE row whose own note says its evidence is missing is exactly the shape rule 16 exists to stop. Log both in section 9 per WRS_MASTER_DOCUMENT.md:2.

**Re-proof.** `grep -n "18 years or older" WRS_MASTER_DOCUMENT.md` returns a hit, and `grep -n "Record date if known" WRS_MASTER_DOCUMENT.md` returns nothing.

