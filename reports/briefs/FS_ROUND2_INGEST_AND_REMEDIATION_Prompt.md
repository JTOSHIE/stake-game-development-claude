FS_ROUND2_INGEST_AND_REMEDIATION_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Explicit paths only, never git add -A. Australian English, no em or en dashes. Commit per job. Work the jobs in order and stop honestly when the session runs short; every job is independently valuable and a resume picks up cleanly. One sanctioned lock exception exists in JOB 4 and nowhere else; outside it, if anything appears to require games/future_spinner/**, gameStore.ts or .claude/settings.json, park it with options instead.

JOB 1, INGEST THE ROUND TWO REVIEWS. The owner's review document is on the desktop: /Users/jt/Desktop/FUTURE_SPINNER_reviews_round_two__.docx (the desktop is in your additionalDirectories). Extract its text (textutil -convert txt, or pandoc, or python-docx, whichever exists on this machine) and split it into the three reviews it contains: reviewer 1 (score 2.00), reviewer 2 (score 1.67), reviewer 3 (score 0.67, begins "Future Spinner, Round-Two Approval Review", reviewed HEAD 78d02cc). Save each verbatim and unedited to docs/records/reviews/sources/ as round2_review1.md, round2_review2.md, round2_review3.md with SHA-256 recorded in the sources README, per the round one convention.

JOB 2, TRACKER ROWS WITH FABLE'S DISPOSITIONS. Append rows for every distinct round two finding. Fable has verified the following first-hand at git level; record these dispositions with the evidence stated, do not re-litigate them: (a) ALL reviewer 2 maths and path findings are HALLUCINATED: the shipped tables have 100,000 rows each, every maximum is exactly 500000 centibets, super row id 40992 is 40992,1131181606,10550, super RTP recomputes to 96.3500%, games/future_spinner/index.json does not exist, and all eight cited frontend paths are absent; no remediation work follows from that review. (b) R3 blocker 1 CONFIRMED: App.svelte lines 676 and 692 read only the session parameter while the official launch uses sessionID, and rgsService.ts models flat authenticate, play and end-round responses while the pinned official client (stake-engine: github:StakeEngine/ts-client, types at the pinned ref) defines nested balance {amount, currency}, config {minBet, maxBet, stepBet, defaultBetLevel, betLevels}, jurisdictionFlags with twelve named flags, round {betID, active, mode, state}, PlayResponse {balance, round}, EndRoundResponse {balance} with no roundId; the docs mirror independently corroborates authenticate/config/minStep. (c) R3 blocker 2 CONFIRMED: ReplayMode.svelte routes only freeSpinTrigger rounds through roundInterpreter and searches ordinary rounds for board, win and scatter events that occur zero times in the shipped books. (d) R3 blocker 3 CONFIRMED at minimum for the paytable rules strings, the interface guide entries and WinBanner's unconditional BET. (e) R3 major on jurisdiction flags CONFIRMED: responsibleGambling.ts consumes minSpinMs, realityCheckMs, maxAutoplaySpins and mandatorySessionDisplay, which do not exist in the official JurisdictionFlags, and ignores minimumRoundDuration, disabledSpacebar, disabledSlamstop, displayRTP and displaySessionTimer. (f) R3 major on the verifier CONFIRMED by execution: with books absent the tool prints zero rounds, zero assertions and PASS. (g) R1's four MAJORs map to existing rows TR-012c, TR-031, TR-035b and TR-036; add source references to those rows rather than new rows. (h) The popout finding is recorded as R3's reading of the committed capture versus R1's modal-dismissal-only gate; disposition CONFIRMED pending the dedicated HUD in JOB 8.

JOB 3, APP LAUNCH FIX, unlocked and immediate. In App.svelte accept sessionID with session as legacy fallback in both the token read and the live-guard launch check, matching parseSessionParams. Add a regression test asserting an official-shaped launch URL (sessionID plus rgs_url) enables the live path. Commit separately; this one line is the difference between playable and dead on an official launch.

JOB 4, WALLET CONTRACT REWRITE UNDER SANCTIONED LOCK EXCEPTION. Granted by the owner pasting this brief: lift exactly Edit(frontend/src/lib/services/rgsService.ts) and Write(frontend/src/lib/services/rgsService.ts) for this job only. Rewrite the raw layer of rgsService.ts to the pinned official contract exactly as defined by the stake-engine package's types: nested authenticate (balance object, config object, jurisdictionFlags object, round with betID, active, mode, state), play returning {balance, round} with the round's events extracted from the official round shape, end-round returning balance only with round identity handled per the official model, and rgs_url host normalisation matching the official client for normal play as it already does for replay. Transcribe the official types into our own declarations rather than importing the package at runtime (its licence file is absent; the pin remains our reference). Preserve every downstream export signature where possible so consumers keep working; where a consumer signature must change, list every touched consumer in the session report. All currency arithmetic stays in integer micros through the existing helpers; document explicitly what unit the official balance amount arrives in, citing the evidence (ts-client source, docs mirror, or marked UNKNOWN pending DTT with both paths implemented behind one constant). Rebuild the mock to emit OFFICIAL-shaped responses so mock and service no longer share the invented schema, and rewrite the service tests against official-shaped fixtures, including jurisdictionFlags delivery to the RG store and active-round recovery retyped to {betID, active, state}. Wire currencyDisplay metadata through if the official shape carries it, closing TR-012c's gap, and mark it UNKNOWN otherwise. This work lands on branch fix/R2R-wallet-contract as an isolated PR that is NOT self-merged: lock proof in the PR body (exact deny lines lifted, settings diff zero bytes, SHA identical before and after, gameStore.ts and games/future_spinner untouched), full contract mapping table old to new, and the PR held for Fable's line-by-line review per the PR #103 precedent. Restore the deny lines before any commit. If gameStore.ts requires changes to complete the plumbing, park that portion with options; its deny lines are not lifted here.

JOB 5, REPLAY THROUGH THE CANONICAL INTERPRETER, unlocked. Route every replay round through roundInterpreter, delete the board, win and scatter legacy search, and add replay assertions for a loss, an ordinary win, a big win, a cap round and a feature round using real book fixtures per mode where available.

JOB 6, SOCIAL VOCABULARY LAYER, unlocked. Route every player-visible and accessibility string through one social-aware vocabulary function, covering the paytable rules strings, the interface guide, WinBanner, banners, overlays and replay first paint (derive social from currency as well as the flag before mount). Replace the narrow conformance script with a full-rendered-DOM scan against the complete prohibited-term table from the jurisdiction mirror, run in both modes, asserting zero hits in social and committing the JSON.

JOB 7, VERIFIER FAIL-CLOSED, unlocked and small. verify_books_lookup_equality.py must fail on missing input files, non-zero decompressor exit status, zero rounds processed, duplicate or missing IDs, and row counts that do not exactly match the lookup; add expected-count parameters and print FAIL loudly. Re-commit the tool with a self-test that runs it against absent input and asserts a non-zero exit.

JOB 8, MINI-PLAYER HUD, unlocked. Build a dedicated 400x225 layout profile rather than scaling the desktop HUD, and commit active-state proofs: idle, spinning, result, feature and modal, each legible and operable, replacing the current popout capture group in EVIDENCE_INVENTORY.md per the partial-progress rule.

JOB 9, PURPOSE-DRAWN PROVIDER MARK, per Fable's art spec. Build an original vector SVG master drawn at 48 first and scaled up, not down: an outer wheel ring of two concentric circles with stroke never below 3px at 48, three rounded-rectangle reel windows across the centre carrying 7 glyphs at no less than 10px height at 48, no text ring, exactly two colours from the brand palette (cyan ring, magenta reel accents) on transparency, deterministic build script committed with provenance. Export 512, 96 and 48 plus one delivery-named set per the platform tile and branding convention, and rename the tile exports to that convention at the same time. Commit a refreshed 48px comparison adding this candidate beside b and c for the owner eye-call.

JOB 10, RETRIGGER ESCALATION, TR-036 option (b) as already ratified: reduced ladder capped at level 3 on retrigger, its own test fixture, and one added line in the retrigger scatter rules text routed through the JOB 6 vocabulary layer.

JOB 11, CLOSE. Full typecheck command green and added to CI alongside the Svelte baseline and a production build. Session report with dated section and archive copy: the ingest hashes, the tracker rows, the contract mapping table, every gate result, and a FOR THE NEXT SESSION block stating exactly what remains for DTT. The statement to make plainly if reached: round three of external review should wait until the wallet-contract PR is merged after Fable's review and the DTT has confirmed the live payload shapes.
