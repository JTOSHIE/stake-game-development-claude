# R107: FINAL FX CLOSURE + SHADOW PLACEMENT + MINIMAL BURST OVERLAY

Sole live brief. Unattended. Review lane. Maximum useful effort. Long-running autonomous session.

## THE FENCE

- No kit packaging.
- output/ read-only.
- Do not weaken asset guards.
- Working-tree-only raster intake by default.
- Commit rasters only when a paired CI-safe path is clearly justified.
- Do not disturb the committed banner pair.
- Do not illegally animate external character parts.
- If blocked, record exact evidence and continue.

## PRECONDITIONS

- On main, up to date.
- Confirm kit exists: `.scratch/art-review/chatgpt-fx-final-closure-kit/`
- Confirm current dirty FX/placeholder state is preserved.
- Confirm prior runtime findings remain available: jet flame must be green-source for hue-rotate
  colourways; particle paths are individual sprites, not atlas; contact shadows must sit outside
  `.char-layer` / `.car-layer`; outstanding ledger / FX specs.

## GOAL

This is an implementation session, not just an audit.

Close the remaining FX rows this kit truly unlocks, place the contact shadows correctly, and add
only the minimum burst-overlay support needed to make the new micro-FX usable.

## WORKSTREAM 1 - FULL KIT AUDIT AGAINST REAL CONTRACTS

Inventory every asset. For each file record: path, dimensions, alpha, subject, measured hue if
relevant, intended consumer, status: READY / WRONG-SPEC / WEAK-VS-INCUMBENT / NO-ROW / HOMELESS /
REVIEW-ONLY.

Especially verify: FX-03 5-frame green jet strip; frame 3 export if present; gold coin / bright
spark / bold cyan ring / smoke; win micro-bursts; residual chips; contact shadows.

Confirm the jet sheet is actually green-source near hue 110 before intake.

## WORKSTREAM 2 - CLOSE FX-03 / FX-04

### 2.1 Jet sheet validation
Validate against the live `FlameJets` contract: 5 frames; correct strip geometry; green-source hue
behaviour under the existing hue-rotate colourways; no regression versus incumbent readability.

### 2.2 Intake
If valid: intake FX-03 working-tree-only onto the real target; if FX-04 is defined as frame 3 of
the same sheet, derive/intake that too; verify the live consumer can use it without code changes
if possible.

### 2.3 If invalid
Refuse with exact numbers: hue; frame geometry; resulting colourway outcomes.

## WORKSTREAM 3 - PARTICLE CLOSURE

### 3.1 Map incumbents
Identify current particle targets and compare each new candidate on: size; subject; contrast /
saturation / punch; readability at runtime size.

### 3.2 Intake winners only
Replace only when the new particle is clearly as strong or stronger and subject-correct: gold coin
must read gold; ring must read bold cyan; spark must read bright; smoke may win on form/density.

### 3.3 Refuse weak art
Do not ship quieter replacements by accident.

### 3.4 Result
State exactly which particle rows are now closed.

## WORKSTREAM 4 - CONTACT SHADOW IMPLEMENTATION

### 4.1 Confirm safe insertion point
Shadows must not sit inside the breathing / hovering animated wrappers. Use the non-transforming
scene wrapper path identified earlier.

### 4.2 Implement minimally
If the shadow art matches widths and alpha is clean: add the minimum markup/CSS needed; position
by measurement and visual iteration; keep robot and car shadows independent; do not alter existing
idle animations.

### 4.3 Verify
Confirm: shadows stay grounded while character/car idle motion plays; no layout shift in the reel
area; no clipping disasters.

If visual placement cannot be confidently completed headlessly, leave a precise residual with the
exact snippet and measured targets.

## WORKSTREAM 5 - MINIMAL BURST OVERLAY SUPPORT

### 5.1 Determine the smallest useful component change
Using the new micro-bursts, determine whether a tiny isolated overlay layer can be added for:
small/medium win spark feedback; scatter-style accent hits.

### 5.2 Implement only if truly minimal
Acceptable: one overlay host; one or two sprite references; simple show/hide or short animation
hook into an existing win path.

Not acceptable in this session: redesigning the whole win presentation system; large architecture
rewrites; locale-unsafe baked text.

### 5.3 If not minimal
Write the exact next component brief and stop at intake + docs.

## WORKSTREAM 6 - PROGRAMME RECOMPUTE

Update: REPLACE coverage; FX rows closed vs open; particle rows closed vs open; homeless art
remaining; law-blocked items remaining; owner decisions remaining. Update the outstanding ledger.

Rank the next work as: remaining safe art intakes; remaining component work; owner decisions.

## CLOSE

- Safe working-tree intakes
- Minimal safe implementation for shadows / burst overlay where justified
- No accidental placeholder sweeps
- Guards remain active
- PR on review lane
- Continue until all workstreams are complete
