FS_UPLOAD_KIT_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Explicit paths only, no em or en dashes, no lock exceptions; the books are read from the local machine, never committed.

PURPOSE. The owner is doing the portal uploads for the first time and the dossier's 5b assumes portal familiarity. Build a complete, pre-verified upload kit at ~/Desktop/FS_UPLOAD_KIT/ so the owner's task is drag and drop from labelled folders while following one plain-language document. Everything in the kit is verified before packaging; nothing is left for the owner to check.

JOB 1, THE MATHS SET. Create 01_maths_upload/ containing the eleven upload files: the seven repo-committed publish files plus the five books_<mode>.jsonl.zst from the local math-sdk output. Verify every file's SHA-256 against the dossier 5c table and BOOKS_MANIFEST.md before copying; a mismatch stops the job loudly. Write HASHES.txt into the folder with the verified values.

JOB 2, THE FRONTEND SET. Per convention (o), clone the repository fresh into a temporary directory, build the production bundle there, run the network-hygiene gate and the size gate against that build, and copy the dist contents into 02_frontend_upload/. Record the byte total and file count and confirm they match the committed reproducible figure.

JOB 3, THE BRANDING SET. Copy the three delivery assets from design-system/brand/delivery/ into 03_branding/ with their hashes: WeRollSpinners-Logo.png (goes to Team Settings, Branding), FutureSpinner-BG.jpg and FutureSpinner-FG.png (go to the Tile Editor).

JOB 4, THE WALKTHROUGH. Write 00_READ_ME_FIRST.md in the kit root, in plain Australian English for a first-time portal user, phone friendly, numbered steps only, no jargon without a one-line explanation. It must open with the safety facts: nothing before Submit for review is visible outside the team or irreversible, every upload is replaceable, and Submit for review is NOT part of this session. Then the exact sequence with the portal page names and button labels: log in; Team Settings then Branding, upload the logo from 03; open the Future Spinner game entry; Files page, upload the contents of 01, then the contents of 02; Publish Game, Math, then Publish Game, Front End (explain in one line that publish means make my uploads runnable for me, nothing public); Tile Editor, compose from 03; then the game's Developer page, Start game session, Launch in New Tab. Close with the ten DTT_PROTOCOL.md observations restated in owner language, each as look at this, expect this, screenshot this, and where to put the screenshots. Cross-reference rather than duplicate the protocol's technical detail.

JOB 5, CLOSE. Commit the brief, the walkthrough source (a copy under docs/records/), and a session report with dated section and archive copy stating the kit's location, contents and verified hashes. FOR THE NEXT SESSION: the owner runs 00_READ_ME_FIRST.md end to end; Fable verifies the observation captures; round three follows.
