FS_MULTITRACK_PROTOCOL_V2_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Supersedes the never-run FS_MULTITRACK_PROTOCOL_Prompt.md, which is DEAD. Explicit paths only, no em or en dashes, no lock exceptions. This session is the INTEGRATOR and runs on main.

JOB 1, RECORD THE PROTOCOL. Add to CLAUDE.md beside the standing mandate, mirrored in WRS_MASTER_DOCUMENT.md, the MULTI-TRACK PROTOCOL: (1) main is single-writer; exactly one session at a time holds the integrator role and only it merges or pushes main. (2) A track is one branch plus one committed scope manifest (docs/records/tracks/<track>.manifest, path globs) declared in its brief; tracks deliver by pull request only. (3) Parallel tracks require provably disjoint scopes; overlap forces sequence. (4) Default one job per session, fresh context; multi-job sessions justify themselves in the session report. (5) Model policy: Sonnet High for mechanical and suite work, Opus for judgement, xHigh and above reserved for single hard bounded problems in short surgical sessions; the twice-failed escalation rule stands. (6) Hard problems are extracted into their own surgical brief rather than solved mid-flow. (7) Fable verifies every PR before merge; the scope gate enforces manifests in CI. (8) Session reports on tracks are dated AND track-tagged sections; the integrator merges PRs one at a time and resolves report conflicts by concatenation, never by discarding a section.

JOB 2, THE SCOPE GATE. Generalise the locked-paths CI gate: branches named track/<name> are checked against docs/records/tracks/<name>.manifest; any diff outside the manifest fails CI. Seeded self-test per convention (p): a planted out-of-scope change must fail before the gate's PASS counts.

JOB 3, JOB 7 OF THE LIVE PARENT, ON MAIN, NOW. The owner's second visit section in the walkthrough, exactly as previously ruled: delete math/HASHES.txt; upload FS_UPLOAD_KIT_V3/02_frontend_upload as Front End and publish; confirm Front V3, the pilot, no dashes in the paytable, the FEATURE PRICE line on a bought round, the mini-player BALANCE and WIN readouts and the menu button rendering at Popout S; compose the tile via Design Thumbnail; the five observations (zero-win end-round, events location in round.state, currencyDisplay scan, Gold Coin decimals, Language menu plus da fallback); screenshots saved loose on the Desktop; then the Guidelines ticks with the self-assessment open; never Start Approval. This lands on main immediately so the owner's portal visit is unblocked by nothing.

JOB 4, OPEN TWO PARALLEL TRACKS. (a) track/docs-reskin, manifest: docs/RESKIN_BOUNDARY.md, docs/records/reviews/FIX_LIST_2026-07-26.md, SUBMISSION_DOSSIER.md, GAME_FACTS.md, COMPLIANCE_WATCH.md, WRS_MASTER_DOCUMENT.md, docs/records/tracks/. Its brief: JOB 6 and JOB 10 of the live parent, plus bringing the fix list up to this session's commits. (b) track/quality-sweep, manifest: frontend/src/**, frontend/scripts/**, .github/workflows/**, docs/QUALITY_CHARTER.md, docs/records/tracks/. Its brief: JOB 9 of the live parent, the charter, the sweep, the fixes, the CI gate with seeded self-test. Write both track briefs as complete fenced prompts into docs/records/tracks/ so the owner can paste each into its own fresh session; each brief opens with its boot list, its manifest, and the single-writer rule that it must NOT push main.

JOB 5, THE RETRO MECHANISM. Record in WRS_MASTER_DOCUMENT.md under process: after the owner's second portal visit, Fable's benchmark polish review nominates up to three surfaces for focused redo sessions, one specialist session each on its own track, selected by measured weakness against the professional bar. Session report with dated section and archive copy; FOR THE NEXT SESSION names the two track briefs awaiting the owner's paste and the integrator's return once their PRs are up.
