FS_PLAYER_EXPERIENCE_PASS_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh session, Ultra allowance, on main, explicit paths, commit per job, no em or en dashes, no lock exceptions; anything appearing to need rgsService.ts, gameStore.ts, games/future_spinner/** or settings is parked with a named sanction request, never touched. Jobs ordered so an honest stop leaves whole work; stop between jobs, never inside one.

JOB 1, V7 RECONCILIATION CROSS-CHECK, first because it gates the owner's trust in everything after. Build the definitive audit-to-shipped ledger: every finding from the audit arc (Q-01 through Q-29 in QUALITY_CHARTER 4.2x, TR-086 through TR-092, the 19 formerly frozen locale entries, and the visual fixpack rows) reconciled against the V7 bundle by evidence, not by memory: for each, the fixing commit, whether that commit is an ancestor of the kit V7 build SHA (read it from the kit README or the shipped build-info.json), and a verdict SHIPPED IN V7, FIXED POST-V7 (ships next kit), or OPEN. Commit as docs/records/V7_RECONCILIATION.md with a short owner section on top listing only the FIXED POST-V7 and OPEN items so the owner knows exactly what V7 cannot show. Any finding whose fix is not on main at all becomes a tracker row on the spot.

JOB 2, MAX-WIN HOLD, owner-ordered player behaviour. The max-win celebration must hold indefinitely: nothing auto-advances, nothing progresses behind it, no timer dismisses it; it leaves only on the player's COLLECT or ENTER. Audit the celebration flow for any auto-dismiss, background progression, autoplay continuation or idle-attract behind the overlay while it holds; fix what violates the rule; extend the same guarantee to the big and epic win banners only if they already gate on input, otherwise leave standard banners as they are and record the distinction. Prove with a measured assertion: celebration mounted, thirty seconds elapsed, zero state transitions, wallet and round state unchanged, then a real COLLECT click proceeds cleanly. Seeded per convention (p).

JOB 3, THE BET SELECTOR, owner-specified to industry convention. Clicking or tapping the BET window opens a denomination picker panel listing every level from the authenticate ladder (never a hardcoded list), laid out for one-tap jumps from minimum to maximum, current level highlighted, minStep respected, closing on selection or dismissal, keyboard and touch accessible with a compliant hit target, styled to the HUD design language, routed through the vocabulary and locale layer, and honouring the effective-cost display so OVERBOOST shows ante-adjusted figures beside levels if shown at all (or plain levels with the effective BET readout updating on selection, choose the cleaner and record why). The stepper arrows remain. Prove at desktop, mobile portrait and Popout S with captures, ladder-driven test against a mock authenticate carrying an unusual ladder, and the existing conformance suites green.

JOB 4, FEATURE RESUME, the heavy one, designed before built. Owner intent: leave mid-spin or mid-bonus, come back, continue where you left off rather than being fast-settled. Design constraints stated in the repo already: the RGS round is one determined round; presentation is ours. Implement presentation-resume: persist presentation progress locally keyed by betID (position within the event sequence, accumulated win shown, free spins remaining display state) at safe checkpoints; on recovery of an active round whose betID matches the persisted key, offer RESUME, playing the presentation forward from the checkpoint through the canonical interpreter, with the existing replay-then-settle flow as the fallback for no checkpoint or mismatch; never diverging displayed totals from the round's true figures; storage failure degrades silently to the existing flow. Mid-spin exit on an ordinary round keeps the existing resume-and-settle. Test matrix: reload mid-feature at three checkpoints, reload mid-ordinary-spin, checkpoint for a different betID, storage unavailable; all through official-shaped fixtures. Record in the row that live confirmation at the portal is the owner's next-visit item. If this job cannot complete to that definition, park the design document and partial branch per protocol rather than shipping half.

JOB 5, close per rule 10 with the run link; session report; FOR THE NEXT SESSION: kit V8 assembles in the records pass after this merges.
