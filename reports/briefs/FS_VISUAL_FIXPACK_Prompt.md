FS_VISUAL_FIXPACK_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh session on main, explicit paths, commit per job, no em or en dashes, no lock exceptions; anything appearing to need a locked path is parked with options. Owner-reported visual defects, each fixed at root with a measured proof, per the standing mandate: no minor-defer, fixed or owner-parked.

JOB 1, SPLASH MOTION REGRESSION. The load screen's We Roll Spinners logo now jumps around and starts spinning. The owner's ruling: the splash is calm, black screen, logo sitting still with its gentle pulse, raindrops, TAP TO CONTINUE, nothing else moving. Find what introduced the motion (likely the logo asset swap interacting with an animation keyed to the old asset, or an idle-attract behaviour reaching the splash), restore the calm presentation, and prove it: a capture sequence across the first ten seconds showing logo position variance of zero beyond the pulse, committed, with a gate assertion on the splash logo's transform stability.

JOB 2, TURBO CONTROL REDESIGN, owner-specified. The speed button currently shows the bolt with a tiny 1x, 2x, 4x figure the owner calls too small and silly. New design: the bolt symbol only, no numeral, with the three speeds expressed by the control itself intensifying, brighter glow and stronger highlight at each step, clearly distinguishable at a glance at all seven presets. Keep exactly three speeds and their existing behaviour and jurisdiction gating; change presentation only. Update the paytable Interface Guide row for the control to match the new visual, routed through the vocabulary layer. Proofs: the control at all three speeds at desktop, mobile portrait and Popout S, committed side by side; contrast measured between adjacent states so the steps are provably distinguishable, not just intended.

JOB 3, PAYTABLE CARD FILL CLIP. In Symbol Payouts, the WILD and SCAT cards' background fill stops short so the bottom of their text cuts off the blue panel, unlike every other card; the owner's screenshot is committed as the spec. Root-cause the card sizing (likely fixed-height fill against taller multi-line content), fix so the fill always encloses the content on every card regardless of text length or locale, and assert it: a check across all locales that no payout card's text exceeds its fill box.

JOB 4, BLACKOUT COVERAGE. The full-screen dark overlay behind dialogs and transitions does not always cover the whole screen; at some sizes the corners show through. Find every full-screen scrim in the app, give them one shared implementation covering the true visual viewport at every preset including safe-area insets, and assert coverage in the composition gate at all seven presets plus three swept window sizes: zero uncovered pixels at the edges while a scrim is active.

JOB 5, KIT V6 after all gates green: fresh clone, all dist gates in the clone, frontend only, single use, walkthrough updated to the short flow: upload and publish V6; eyeball the calm splash, the new turbo control, the two fixed paytable cards, and a dialog blackout at a stretched window; the remaining owner list is in OWNER_CHECKLIST.md. JOB 6, close per rule 10 with the run link; session report, dated section, archive copy.
