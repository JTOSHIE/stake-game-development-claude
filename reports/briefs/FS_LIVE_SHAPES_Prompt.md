FS_LIVE_SHAPES_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh session on main, explicit paths, commit per job, no em or en dashes, NO lock exception granted here; if any fix requires rgsService.ts, design the exact one-line change, prove it against the captured live response, and PARK it as a named sanction request for the owner's next paste instead of touching the file.

JOB 1, INGEST AND RECONCILE. Commit the owner's new Desktop captures (four devtools frames plus everything else loose) per the analyst pattern, then reconcile the FULL live authenticate and play responses field by field against the parser: confirmed already from the frames: state is an array of events directly, end-round returns balance only, amounts are micros, plays without end-round exist. Open questions to settle from the captures and code: (a) the live jurisdiction block appears TOP LEVEL beside balance rather than at config.jurisdiction; determine what our parser actually reads on the live shape, whether flags currently arrive empty, and what one tolerant line fixes it (parked as sanction request if locked); (b) the bet-level and mode config shape versus our model; (c) the RED authenticate request and the four console errors: identify cause from the captures and reproduce locally at the live parameter shape. Record every confirmation in the tracker rows (events location, end-round shape, micros, zero-win behaviour) with the capture paths as evidence.

JOB 2, WALKTHROUGH REWRITE. Replace PART 9c's remaining content with a ONE PAGE update flow reflecting reality: differential sync (upload only changes, deletions are sync-driven, skips are normal), one Publish button, maths untouched, tile already composed by the owner and done, HASHES.txt accepted as harmless residue. Keep only the still-open items as a short list: replay-live confirmation ideally event 22975 with the celebration capture, twenty bracketed cruise spins, the Gold Coin decimals check, the language list scroll and Danish fallback, and the Guidelines ticks. Nothing else.

JOB 3, CLOSE per rule 10 with the run link; session report, dated section, archive copy; FOR THE NEXT SESSION: any parked sanction line for the jurisdiction read, the owner's short remaining list, then Fable's polish review and round three.
