FABLE BRIEF R058: REPLAY DESKTOP POLISH, OWNER EYE-CALL (2026-08-13)
Sole live brief. Owner re-test on the published build at 9504c610, with
screenshots: fit, feature results and sub-cent display all CONFIRMED
working; two desktop replay findings remain. Judgement tier, one session,
Australian English, no em or en dashes. Save and commit verbatim.
Explicit-path commits, remote CI green per rule 10, comms folded per (t).
TASK 1, THE CLIPPED BANNER. The win-tier banner (MEGA WIN class) truncates
its amount at desktop replay size ("CA$39.(" in the owner's capture). The
amount must fit at every size and every realistic length: fit-to-width or
stepped font sizing, proven against the worst case string, a wincap win at
the maximum bet in the widest currency format the ladder allows. This is
the shared banner component, so verify the live-game path renders
identically before and after everywhere the worst case cannot occur is
untouched.
TASK 2, OWNER DESIGN RULING: REPLAY POD REMOVED ON DESKTOP. In the REPLAY
VIEW ONLY, the right-hand multiplier and win pod is removed at all sizes,
and the end-of-replay banner above REPLAY AGAIN carries both values
inline: the amount in its existing pink treatment with the multiplier in
the blue treatment beside it (amount then multiplier, e.g. CA$39.64 and
39.6x). Desktop and mobile replay become one layout. SCOPE GUARD: the
live game's Overdrive meter panel during features is NOT part of this
ruling and must be pixel-untouched; the proof asserts the live feature
meter still renders.
TASK 3, PROOF. The replay proof gains: no pod element in the replay DOM
at any size; the end banner carries amount and multiplier matching the
envelope; the tier banner amount fits with zero clipping at the three
reference sizes including the worst-case string; the live-game meter
assertion per the scope guard. Seeded per (p) with the clipped state and
a leaked pod as the negative controls; frames committed.
CLOSE. Tracker row, comms folded, full matrix green, rebuild at the tip
for the owner's single dist sync, tree clean. FOR THE NEXT SESSION: the
owner's final re-test (these two plus the XEC glance), then the
fifty-one walk, one-timers, Start Approval on the owner's word.
