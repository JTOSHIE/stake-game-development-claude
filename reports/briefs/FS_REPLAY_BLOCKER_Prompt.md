FS_REPLAY_BLOCKER_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh session on main, explicit paths, commit per job, no em or en dashes, no lock exceptions; if the fix appears to need rgsService.ts or any locked path, stop and park with options. Multi-job justified: one substantial job plus three small hygiene items, recorded per protocol.

JOB 1, THE REPLAY BLOCKER, surgical. New tracker row, BLOCKER: on the live platform the Replay panel (game mode, event ID, amount, currency, Play Event) launches the game, the board renders static, START REPLAY is visible as an unclickable shadow at the bottom, and nothing plays; reproduced by the owner on super event 22975 (EUR) and on a fresh bet. Reproduce locally first with the EXACT live replay parameter shape (the owner's frame shows the math page launcher with launch=true; derive the game iframe's real query string, and use the DTT Local Testing redirect against the real RGS if needed). Suspects in order: an overlay layer intercepting input above the replay CTA (the HeroSplash click-interception class, third appearance); z-index or DOM layering placing the CTA behind the stage; replay initialisation failing silently on the live parameter shape and leaving the CTA inert. Fix at root, then prove with a Playwright proof that performs a REAL click on START REPLAY at the live parameter shape and asserts the replay presentation actually runs (events animate, win counts up); seed the failure per convention (p) by re-introducing the interception in a scratch copy. Then, with replay working, replay event 22975 and capture whether MaxWinCelebration presents at the wincap, closing TR-075 with the capture, or filing the missing celebration as its own defect with the same evidence. Include EUR display correctness in the replay proof.

JOB 2, RULE 9. Fill the empty slot with its originally intended content: seeded-failure proofs run locally where possible; a genuinely required red run against origin uses a branch named test/expected-fail-<topic>, commit message opening EXPECTED FAIL, branch deleted after, session report naming the run BEFORE the owner can meet the notification; an unexplained red on any other branch is treated as real. Record beside it that the slot was skipped because the rule's brief was issued but never executed.

JOB 3, WORKING-TREE RELOCATIONS, per Fable's rulings, nothing discarded: scripts/assets/backgrounds.py committed to a branch chore/wip-backgrounds and the working tree restored; games/future_spinner_super/ committed to the existing claude/fs-super-prototype branch (horizon material, must not live loose on the capture machine) and removed from the working tree; reports/screens/cohesion-pass/char-enhanced-closeup.png committed as evidence if anything references it, otherwise moved to scratch per (h.1); sideproject/ left untouched and listed for the owner to say LUMEN-branch or off-repo, since its nature is not determinable from the tree.

JOB 4, CLOSE per rule 10 with the remote run link; session report, dated section, archive copy; FOR THE NEXT SESSION: the owner re-tests replay on the live platform after the next frontend upload, the cruise bracketed run and the remaining PART 9b observations stand, and Fable's polish review follows the owner's visit.
