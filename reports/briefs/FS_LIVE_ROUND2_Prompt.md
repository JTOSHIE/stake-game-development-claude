FS_LIVE_ROUND2_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh session on main, explicit paths, commit per job, no em or en dashes. ONE sanctioned lock exception exists in JOB 1 and nowhere else, granted by the owner pasting this brief.

JOB 1, SANCTIONED ONE-LINE JURISDICTION FIX. Lift exactly Edit(frontend/src/lib/services/rgsService.ts) and Write(frontend/src/lib/services/rgsService.ts) solely to make the jurisdiction read tolerant of both live shapes: ...(config.jurisdiction ?? raw.jurisdiction ?? {}). One line changed; prove with the captured-live-body harness that flags now populate from the top-level shape AND still populate from the config-nested shape; git diff --stat shows one file one insertion one deletion (or minimal equivalent); settings diff empty; deny lines restored; gameStore and games untouched. Update the parked row to MERGED with the harness output.

JOB 2, LIVE CONFIRMATIONS INTO THE RECORD. Ingest the owner's new captures (Desktop and this batch): close TR-073 and TR-076 as CONFIRMED LIVE with the max-win celebration frame; record the Danish fallback PASS, Gold Coins unavailable in this environment (TR-057 stays parked, interim stands), and the red authenticate as observed-only. Add to GAME_FACTS a plain-language PLATFORM DISPLAY CONVENTION note: COST columns and round.amount carry bet level on every mode; effective debits are bet level times the mode cost multiplier, proven live to the cent for all modes except cruise; our HUD, FEATURES card and confirm dialog state effective prices.

JOB 3, POPOUT S RECOMPOSITION, owner-specified. The live mini player renders the reel stage small and right-anchored with FEATURES stranded left; the owner's screenshots are the defect spec. Redesign the 400x225 composition as a purpose-built layout: stage centred and scaled to fill the height between title and strip, FEATURES reachable from the strip (it already has the trigger; make the panel present usably at this size), and re-derive the same treatment at Mobile S/M/L where the owner reports the reels reading too small inside their borders, widening the stage to its true available width per the TR-065 method (measured, never overflow-hidden). Prove at all seven presets with before and after captures committed, plus the existing fit and mini-money gates green.

JOB 4, BACKGROUND CANDIDATE. Ingest ~/Downloads/bg_improved_v2.jpg through the owner-art pipeline as background candidate v2 with provenance; produce in-game side-by-side proofs (current versus v2 behind the live frame at desktop and mobile portrait) for the owner eye-call; no adoption in this pass; record bundle-size impact at web-appropriate compression.

JOB 5, KIT V5 after JOB 3 passes its gates: fresh clone, all dist gates in the clone, frontend-only, single use, walkthrough updated to the short update flow with the remaining list (upload V5, eyeball Popout S and mobile stages, bracketed cruise spins if still unrun, background eye-call reply BG: V2 or BG: KEEP, Guidelines ticks). JOB 6, close per rule 10 with the run link; FOR THE NEXT SESSION: Fable verifies this and the prior session at git level, then the benchmark polish review, then round three.
