FS_MARK_ADOPT_AND_DTT_PREP_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Explicit paths only, commit per job, no em or en dashes. This supersedes FS_DTT_PREP_Prompt.md from the earlier reply, which was never executed and is now dead; do not run it. One sanctioned lock exception exists in JOB 3 and nowhere else.

Go with F

JOB 1, ADOPT. Regenerate design-system/brand/delivery/WeRollSpinners-Logo.png from provider_mark_e-owner-supplied-transparent at the platform's required resolution via the deterministic ingest pipeline, update the delivery README and provenance with hashes, mark candidates a through d superseded in the provider_mark README per convention (h), and set TR-031 to MERGED, pending only the one-time portal upload which is the owner's.

JOB 2, DTT PROTOCOL. Write DTT_PROTOCOL.md at the repository root: a scripted checklist for the owner's Developer Testing Tool session, each item a single observation with its expected value and its flip-point in code. Cover at minimum: the authenticate payload (nested balance {amount, currency}; config with minBet, maxBet, stepBet, defaultBetLevel, betLevels; config.jurisdiction with the twelve flags; round null or active), the play payload ({balance, round}) and where the events sit inside round.state, the end-round payload (balance only), whether any currencyDisplay field arrives (TR-012c), XGC decimals as the platform displays them (TR-057, interim 2, flip is one line in the display table), resume-and-settle against a real open round (reload mid-round, observe the replay-then-settle banner, confirm wallet agreement), the replay IDs including the documented possible plus-or-minus-one offset, live spacebar and jurisdiction flag behaviour, and the mini-player HUD in the real popout. For each item state what to screenshot and where the capture lands in the evidence inventory. Cross-reference the dossier's section 5 staging protocol so the owner runs one document. Formalise from the FOR THE NEXT SESSION block that already lists the seven confirmations; nothing here is new substance, only its owner-usable form.

JOB 3, SANCTIONED PAR CORRECTION, granted by the owner pasting this brief: lift exactly Edit(games/future_spinner/**) and Write(games/future_spinner/**) solely to correct the scatter-count line in games/future_spinner/FUTURE_SPINNER_PAR_SHEET.md so it states the true maximum of five visible scatters, matching the corrected facts (CLAUDE.md convention (l) worked example). One file, one line changed, no table, weight, config or code byte; git diff --stat must show exactly one file under games/future_spinner/ with one insertion and one deletion; SHA-256 of every file under games/future_spinner/library/ recorded identical before and after; deny lines restored and settings diff verified empty before the commit. Update TR-047's PAR component to MERGED with this commit as evidence.

JOB 4, TR-047 REMAINDER. Verify the dossier nowhere still records tile or provider work as pending, refresh the stale bundle-size figure from a fresh production build, and close the row's unlocked components with evidence.

JOB 5, CLOSE. Session report with dated section and archive copy, the sanction proof, and FOR THE NEXT SESSION stating plainly: the build and its documents are DTT-ready; the owner runs DTT_PROTOCOL.md on the portal along with the one-time uploads (provider logo from the delivery set, tile layers in the Tile Editor); external review round three follows DTT confirmation of the payload shapes.
