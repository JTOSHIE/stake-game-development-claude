FS_V3_CONTINUATION_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. FS_V3_CONSOLIDATED_Prompt.md remains the live parent; this note continues it from JOB 3(d), folds in the owner's mandate and two new sub-jobs, and supersedes the never-run FS_V3_CONSOLIDATED_ADDENDUM block, which is DEAD. Explicit paths only, commit per job, no em or en dashes, no lock exceptions; the gameStore.ts comment-dash debt stays parked per the recorded rule.

STANDING MANDATE, owner's words, binding on every session from now to submission, to be recorded verbatim in CLAUDE.md beside convention (p): this title is the studio's flagship and the template for every future build. Whatever is found gets fixed now, not later; before submission there is no minor-defer category, only fixed or explicitly owner-parked with reasons. The bar is that a player, a reviewer, or a rival studio inspecting any surface concludes this was made by a professional outfit adhering to industry conventions. Nothing player-visible may read as machine-generated.

JOB 3 REMAINDER: (d) social sessions force the en locale before first paint regardless of the language parameter, flag or currency derived, with a test for a social da launch rendering English. (e) TR-066 properly: fix the mini-player WIN clipping AND the assertion that passed while it was clipped, diagnose why noStatValueIsTruncated missed a cut glyph, repair it, seed the failure per convention (p), and close the row only on a rebuilt-bundle Popout S capture compared against the owner's original. (f) TR-068 per the ruling: the FEATURE PRICE line on bought rounds' result banner, driven by the same integer-micros cost source as the confirm dialog, routed through the vocabulary layer, with a test at both a real and a social session. (g) TR-070: the FEATURES bar label contrast at mobile portrait raised to a measured contrast ratio, asserted. (h) NEW, the locked-paths CI gate: a workflow step that diffs the push range for the four locked paths and fails unless the commit message carries an owner-sanction token; ship its seeded self-test proving it fails on a planted locked-path change; document the token format in CLAUDE.md beside the locks. (i) NEW, no documentation ships: the build prunes all .md and other doc files from dist, the dash and hygiene gates assert none ship, and the bundle figure is re-recorded.

JOBS 4 THROUGH 8 exactly as written in the live parent brief, with JOB 7's already-ruled amendment (screenshots saved loose on the Desktop, builder files them) and one addition to JOB 7: the second visit's confirmation list now includes the FEATURE PRICE line on a bought round and the mini player WIN readout at Popout S.

JOB 9, QUALITY CHARTER AND SWEEP. Write docs/QUALITY_CHARTER.md: the standing mandate, the benchmark (Valkyrie-class layout and finish), and the sweep list of machine-tells and unprofessional-finish markers: dash typography, straight-versus-curly quote inconsistency, double spaces, inconsistent capitalisation across surfaces, mixed decimal or currency formats, orphaned placeholder strings, inconsistent button casing, mismatched iconography, default-font leakage. Run the sweep across every player-visible string and committed surface, source and dist; fix everything found in this pass; commit a repeatable script for the sweepable subset and add it to CI as a gate with a seeded self-test.

JOB 10, THE RESKIN BOUNDARY. Write docs/RESKIN_BOUNDARY.md: the definitive engine-versus-skin inventory, directory by directory. ENGINE: mode logic, wallet layer, interpreters, stores, gates, conformance suites, maths pipeline. SKIN: symbol art and masters, palette tokens, vocabulary and locale strings, audio rows, splash and celebration assets, layout theme constants, brand marks, tile layers. For each skin element: where it lives, format and dimensions, generation pipeline and seed convention, and which gates re-run after swapping it. Close with the honest gaps: everything currently hard-coupled that a clean reskin would need decoupled, ranked, explicitly for post-submission work with no refactoring now. Cross-reference WRS_MASTER_DOCUMENT.md's LUMEN template section so the two documents tell one story.

JOB 8 CLOSE, as written, plus: the session report records the locked-paths CI gate's self-test output, the quality sweep's findings ledger, and FOR THE NEXT SESSION: kit V3 is on the Desktop, the owner runs the second visit, Fable verifies, then Fable's benchmark polish review of the live build, then round three.
