FS_RULE11_MERGE115_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. INTEGRATOR session, on main, explicit paths, no em or en dashes, no lock exceptions. JOB 1: append rule 11 to the MULTI-TRACK PROTOCOL in CLAUDE.md, mirrored in WRS_MASTER_DOCUMENT.md: concurrent sessions never share a working tree; every track session creates its own git worktree at boot (worktrees/<track>/, gitignored) and removes it at close; the primary checkout belongs to the integrator alone; a session finding the primary checkout on an unexpected branch touches nothing and reports. JOB 2: merge PR #115 (verified and approved by Fable), un-squashed, then promote the ledger findings to REVIEW_TRACKER rows per protocol: the parked MaxWinCelebration evidence gap, SA-019 (one settled-state frame settles it), SA-020 as a positive closure note on the bet-ladder row, and the cruise wallet-delta gap. JOB 3: append two lines to the walkthrough's PART 9 observation list: (a) on the Bets panel, open the €3,750,000.00 super round and press Replay this bet, screenshot whether the max-win celebration overlay presents during the replay; (b) the twenty cruise spins now bracket with a session panel screenshot before AND after so the wallet delta can be differenced. JOB 4: close per rule 10 with the remote run link. Session report, dated section, archive copy. ADDENDUM to FS_RULE11_MERGE115, one extra job. JOB 5: complete the (h.1) migration the CI triage session logged as open work: every proof and gate script that currently writes screenshots or JSON into committed evidence directories (anticipation_proof.mjs, the layout and contrast gates, and any others a sweep of write paths finds) is redirected to scratch output under a gitignored path, with committed evidence written only by jobs that explicitly regenerate evidence per convention (h.1); re-run each touched script once to prove the working tree stays clean afterwards, and record the sweep's findings in the session report. Also note the pre-existing backgrounds.py working-tree edit and untracked directories to the owner for a keep-or-discard call rather than deciding either way.
