FS_STREAM_TEST_RECOVERY_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh Opus Ultra session on main, explicit paths, commit per job, no em or en dashes, no lock exceptions. This RESUMES the stream test after the prior session hit its allowance mid-checkpoint; per convention (q), nothing that exists is re-run. JOB 1, SALVAGE SURVEY: inventory the working tree: the Wave 1 capture directory, every Wave 2 shard on disk, the voice shard, WAVE2_SQUAD_PROMPT.md, KNOWN_OPEN.md, the harness with its locale-agnostic fix, and the state of the failed 220MB push (git log local versus origin); report exactly what exists before touching anything. JOB 2, DURABLE COMMIT, IN BATCHES: push the local brief and harness commits first (small), then commit the capture set in preset-sized batches so no single push exceeds what the connection handled today, then every completed shard verbatim, then a shard index marking each squad COMPLETE or PARTIAL with last known coverage; the two mid-flight findings (the banner count-up racing the settled WIN pod, and the 16x letter-x unit) are recorded as ledger entries credited to the trial session. JOB 3, COMPLETE DISCOVERY VIA THE CONTAINER: finish the partial squads' remaining surfaces using the workflow-container orchestration per the harness lesson, not chat-spawned agents; squads read the existing shards' coverage notes and audit only what remains. JOB 4, MARSHAL AND VERIFY per the audit method: consolidate shards to the single ledger, then the shared-nothing adversarial verification pass, then the fix batch for small unlocked items with re-proof from fresh frames, larger items parked with owner lines. JOB 5, METHOD SKILL AMENDMENTS, the two lessons paid for today: mass-agent waves run under workflow-container orchestration with per-agent retry, chat-spawned squads only for small counts; sessions rename their own regeneratable scratch aside rather than rm so the safety layer never has to ask; plus the model-trial record written into the session report with the burn figures and the roster ruling. JOB 6, close per rule 10 with the run link; FOR THE NEXT SESSION: Fable verifies the arc, the STREAM-severity ledger goes to the owner, and the total audit's remaining waves schedule on the fresh allowance.
