FS_LOCALE_AND_TYPE_PASS_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. ONE COHERENT JOB, fresh session, Opus, on main, explicit paths, no em or en dashes, no lock exceptions. Implement Fable's rulings 1 to 3. (1) TR-091, all nineteen frozen entries: replace every hand-rolled social ternary and duplicated constant with the existing $tr layer per the comms 020 mapping (four reuse existing keys, about six need new keys translated across all sixteen locales with social variants where the override table applies, one dev-only allowlist), deleting the duplicate layer as you go; burn each fixed entry out of the frozen-debt list in the same commit as its fix so the ratchet visibly empties; full locale and social conformance suites green at the end, gate reading zero debt. (2) TR-092: remove the uppercase transform so all four surfaces render the specification's spelling, asserted across surfaces. (3) TR-089 per the ruling: per-digit fixed-width boxes on the win count-up number only, measured proof that digits no longer shift as it rolls at the shipped font's real advances, remaining static tabular-nums declarations recorded as accepted in the row. Convention (p) seeded self-tests wherever a gate changes. Session report, rule 10 run link. If the session cannot finish all nineteen, it stops between entries with the ratchet counting the remainder, never mid-entry.
