FS_V3_CONSOLIDATED_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. This brief supersedes FS_V3_FIXPACK_Prompt.md from the prior Fable reply, which was never executed and is DEAD; if any copy of it exists, do not run it. This is the single live instruction set. Explicit paths only, commit per job, no em or en dashes, no lock exceptions anywhere; anything appearing to need games/future_spinner/**, rgsService.ts, gameStore.ts or .claude/settings.json is parked with options. Manifest: JOB 1 screenshot deep audit, JOB 2 rulings into tracker, JOB 3 the fix pass, JOB 4 build provenance, JOB 5 kit V3, JOB 6 documentation, JOB 7 owner walkthrough second visit, JOB 8 close. Verify all eight are present before starting.

JOB 1, SCREENSHOT DEEP AUDIT, BEFORE ANY FIX IS BUILT. Every capture tells a story and the owner has asked for the full read. Enumerate every screenshot available: all committed sets under reports/screens/dtt-live-2026-07-26/ and every other portal capture set, plus everything in ~/Desktop/DTT_SCREENSHOTS/ and any other capture folder on the Desktop not yet in the repository (commit any uncommitted ones first with content-assigned names per the established pattern). For each capture record in one audit table: filename, device or viewport if determinable, what surface it shows, and every observable anomaly across these lenses: layout (clipping, overlap, scrollbars, dead space, misalignment), text (spelling, punctuation including dashes, truncation, wrong locale bleed, placeholder strings), money display (every visible balance, bet, win and price cross-checked for internal consistency within the frame), state (broken images, missing controls, wrong mode indicators), and platform chrome observations worth banking. Reconcile the table against FIX_LIST_2026-07-26.md: every anomaly is either already a fix-list item, a new tracker row, or explicitly recorded as not a defect with one line of reasoning. Commit the audit as reports/qa/screenshot_deep_audit_2026-07-26.md. New small unlocked frontend defects found here join JOB 3's scope; anything larger is parked with options. This job gates JOB 3: the fix pass does not start until the audit is committed, so one build carries everything.

JOB 2, RULINGS INTO THE TRACKER. Record: TR-068 ruled, gross WIN retained, secondary FEATURE PRICE line on bought rounds' result banner, headline multiplier unchanged against bet level; TR-062 ruled option (a) with (b), Desktop kit dead, kits single use; TR-063 ruled option (c) with (b) plus the seeded-violation convention; TR-059 parked pending the full language capture, with the da fallback observation added; item 12 conflict held for observation, no code change; guideline items 15, 25, 46 approved; the mini-player WIN clipping approved. Add the seeded-violation convention to CLAUDE.md under the next free letter: every gate that claims a class closed ships a self-test that plants a violation and must FAIL on it before its PASS counts.

JOB 3, THE FIX PASS, ONE BUILD. In one frontend pass, no locked files: (a) the dash class per TR-063, gate moved onto built dist text scanning every emitted byte for U+2014 and U+2013 with a reviewed allowlist, whole-text source scan including App.svelte behind it, every source instance fixed including the document title, seeded-violation self-test proving the gate; (b) item 15, the frame fits without scrollbars at mobile portrait, Mobile S and Popout S by re-deriving the layout maths, not by hiding overflow; (c) item 25, double-tap zoom disabled via touch-action manipulation, not maximum-scale; (d) item 46, social sessions force the en locale before first paint regardless of the language parameter, with a test for a social da launch; (e) the mini-player WIN readout clipping fixed with a measured assertion; (f) TR-068 per the ruling, with the price line driven by the same integer-micros cost source as the confirm dialog and routed through the social vocabulary layer; (g) every small unlocked defect the JOB 1 audit adds. Re-run every conformance suite including the new dash gate and the layout suites at all seven real platform presets; npm run check green; all gates green in the clone, not just locally.

JOB 4, BUILD PROVENANCE, TR-062. Emit build-info.json into dist (commit SHA, build date, bundle byte total) plus one console boot line; the network-hygiene gate asserts it adds no request. The kit builder refuses a dirty tree or non-HEAD checkout and names the SHA in the kit README.

JOB 5, KIT V3, FRONTEND ONLY, SINGLE USE. Fresh clone at post-fix HEAD, build there, run every dist gate in the clone, assemble ~/Desktop/FS_UPLOAD_KIT_V3/ containing only 02_frontend_upload/ and a README: upload this folder as Front End, publish, maths stays at V1 untouched, delete this kit after upload. 

JOB 6, DOCUMENTATION, SAME SESSION. Dossier evidence section: the platform's independent maths corroboration (96.3500, 17.2841, 100000, 5000, 0.00% variance, all constraints green at both star tiers) with capture pointers; the production money-path proof (the four to-the-cent HUD reconciliations across base, 100x and 400x, and Event 63's 400x debit); the item 12 conflict recorded as observation-pending with both first-party citations. GAME_FACTS: the 5.00% wincap RTP band fact. COMPLIANCE_WATCH: the platform-stated 96.70% ceiling with its capture reference. FIX_LIST updated so every row carries its disposition and commit. Tracker rows updated in the same commits as their fixes.

JOB 7, THE OWNER'S SECOND VISIT, IN THE WALKTHROUGH. Replace any prior second-visit section with one authoritative version: 1 delete math/HASHES.txt from the portal Files listing; 2 upload FS_UPLOAD_KIT_V3/02_frontend_upload as Front End and publish; 3 confirm Front V3, the pilot rendering, no dashes in the paytable, and the FEATURE PRICE line on a bought round; 4 compose the tile via Design Thumbnail from 03_branding (the two tile images are re-provided inside kit V3 for convenience); 5 the five observations, look-at-this, expect-this, screenshot-this: a zero-win spin's Network tab (expect no end-round request, note what active reads), the play response with state expanded (where events sits), the authenticate response scanned for any currencyDisplay information (expect none), Gold Coin decimals via the Currency selector, and the full Language menu scrolled end to end plus a da session confirming clean English fallback; 6 then tick the portal Guidelines checklist with STAKE_GUIDELINES_SELF_ASSESSMENT.md open, the 8 OWNER items being yours; 7 do not press Start Approval. Keep the safety facts at the top.

JOB 8, CLOSE. Session report with dated section and archive copy: the audit inventory, every fix with its gate result, the kit SHA, and FOR THE NEXT SESSION: owner runs the second visit; Fable verifies the observations; the round three reviewer prompt follows from Fable; Start Approval follows round three.
