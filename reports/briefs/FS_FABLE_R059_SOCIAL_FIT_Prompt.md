FABLE BRIEF R059: SOCIAL MONEY FIT PASS, OWNER SWEEP FINDINGS (2026-08-14)
Sole live brief. Owner screen-by-screen sweep at maximum values, with
screenshots: real-money displays autofit correctly at every size (CAD
100,000,000.00 renders perfectly at Mobile S), social GC displays do not.
Judgement tier, one session, Australian English, no em or en dashes. Save
and commit verbatim. Explicit-path commits, CI green per rule 10, comms
folded per (t).
GOVERNING RULE, enforce estate-wide: every money-bearing element renders
through the one proven fitting mechanism (the autofitText action) in BOTH
vocabularies, the currency or token marker is always visible, and
text-overflow ellipsis on money is banned. A gate scans money-class
elements for the ellipsis property and for overflow at render, seeded per
(p) with the current dotted state and a stripped-suffix popout as the
negative controls.
TASK 1, HUD AND PANELS. Coins, balance, prize, win, play and bet boxes,
and the feature side panels (TOTAL PRIZE, MULTIPLIER), all fit their
social GC strings at every reference size exactly as the CAD path already
does; the popout compact form keeps its token suffix inside the fitted
string (149.5K GC, never 149.5K clipped).
TASK 2, REPLAY BANNER SOCIAL STRING. The end banner's social form
(amount, token, multiplier) runs the same fit; the R058 worst case
extends to the widest social string.
TASK 3, BUY DIALOG SMALL SCREENS. The PRICE, RTP and MAX WIN strip stops
floating over the scrolling copy: it docks in the scroll flow at the
bottom of the content. The three cells fit their text at Mobile S
including the worst-case GC price, MAX WIN never clipped or pushed out.
TASK 4, PAYTABLE WAYS DIAGRAM. The ways sequence renders whole at 320
wide, the leading 1 visible.
TASK 5, PROOF. Frames in social GC at Desktop, Popout S and Mobile S with
the owner's own maximum-value scenarios plus the worst-case buy price;
assertions: no ellipsis character in any money element, token present,
no horizontal clipping on money nodes; the real-money path is
regression-guarded pixel-unchanged where it already renders correctly.
CLOSE. Record the owner's confirmations: the R058 replay display verified
correct live, and the buy-entry instant award confirmed working as
designed. Tracker rows, full matrix green, rebuild at the tip for one
dist sync, tree clean. FOR THE NEXT SESSION: the owner re-runs the same
sweep in social at maximum values, plus the still-open XEC glance, then
the fifty-one walk, one-timers, Start Approval on the owner's word.
