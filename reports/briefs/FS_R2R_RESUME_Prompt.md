FS_R2R_RESUME_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. This resumes reports/briefs/FS_ROUND2_INGEST_AND_REMEDIATION_Prompt.md, which remains the live instruction set; jobs 1 to 3 and 5 to 7 are complete and verified by Fable, PR #114 is APPROVED. Explicit paths only, commit per job, no em or en dashes. NO lock exception exists in this session; the JOB 4 sanction is spent and closed. Anything appearing to need games/future_spinner/**, rgsService.ts, gameStore.ts or .claude/settings.json is parked with options.

JOB A, MERGE PR #114. Fable has approved it after line-by-line review and a clean test merge against main. Merge it (no squash that would lose the lock-proof commit message), update tracker rows TR-038 through TR-046 as applicable to MERGED with the merge commit as evidence, and re-run the contract, responsible gambling and session recovery suites plus the parse suite on merged main, recording results.

JOB B, TR-035b RESUME-AND-SETTLE, per Fable's re-ruling. On an active round reported by authenticate, extract the round's events (same tolerant extraction the service uses), play the round back through roundInterpreter so the player sees the true outcome, then settle via endRound and show one plain banner: the previous round has been completed and its result applied. No forfeit path. Test with official-shaped fixtures for an active feature round and an active ordinary round, capture the banner state as the TR-049 screenshot into the evidence inventory, and update both rows to reflect DTT as the remaining confirmation, not the design.

JOB C, JOB 8 FROM THE LIVE BRIEF: the dedicated 400x225 mini-player HUD with active-state proofs (idle, spinning, result, feature, modal), replacing the popout capture group in EVIDENCE_INVENTORY.md in the same commit.

JOB D, JOB 9 FROM THE LIVE BRIEF: the purpose-drawn provider mark to Fable's spec exactly as written there (48-first geometry, two colours, no text, deterministic build with provenance), delivery-named exports, tile exports renamed to the platform convention in the same pass, and the refreshed 48px comparison sheet adding this candidate beside b and c for the owner eye-call.

JOB E, JOB 10 FROM THE LIVE BRIEF, done whole as you correctly insisted: TR-036 option (b), reduced escalation ladder capped at level 3 on retrigger, wired through GameGrid so nothing is dead code, its own test fixture, and the retrigger rules line routed through the vocabulary layer.

JOB F, CLOSE. Make npm run check fully green and add it plus a production build to CI alongside the existing gates. Session report with dated section and archive copy, TR-057 ruling recorded (interim 2 decimals, two sources versus one, DTT flips one line), full job inventory, and a FOR THE NEXT SESSION block listing exactly what DTT must confirm: authenticate, play and end-round payloads live, the events location inside round.state, currencyDisplay presence or absence, XGC decimals, and resume-and-settle against a real open round. State plainly that external review round three waits for DTT confirmation of those shapes.
