FS_OWNER_PREVIEW_RULE_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh session on main, explicit paths, no em or en dashes, no lock exceptions. Purpose, owner's order: whenever main changes, the owner's local copy is already fresh, never stale, never his job to refresh.

JOB 1, THE CANONICAL OWNER PREVIEW. Create scripts/owner_preview.mjs plus an npm script owner:preview: it targets one designated checkout (the primary main checkout only, never a worktree), and on every run: stops ONLY the previous owner-preview instance it itself started (tracked by a pidfile under a gitignored .owner-preview/ directory, never by guessing at processes, so nothing belonging to anyone else is ever touched), then git fetch and hard-syncs that checkout to origin/main refusing to proceed if the tree is dirty (report instead, never discard), installs if the lockfile changed, starts the dev server bound to the LAN host on the fixed known port (the one the owner already uses, 5173), waits for ready, and prints one line the owner can trust: the version label, commit SHA, build date and the URL. Failure at any step prints loudly and leaves nothing half-started.

JOB 2, THE STANDING DUTY, INTO PROTOCOL. Append to the MULTI-TRACK PROTOCOL in CLAUDE.md, mirrored in WRS_MASTER_DOCUMENT.md, rule 12: any session that lands a change on main (integrator or main-line) runs owner:preview as part of its close, before the session report, and records the printed version line in the report; track sessions never touch the owner preview (single writer applies to it as to main); if the preview cannot be refreshed (machine constraints, dirty tree), the session report says so in its own line rather than silently leaving it stale. Add the same line to the session-close checklist wherever it is enumerated.

JOB 3, FIRST RUN AND OWNER NOTE. Deal with the current stale state per the rule's own discipline: the long-running vite dev server from the background-test session is the owner's designated preview's ancestor, so fold it in: stop it via the new script's adoption path (record its pid as the previous instance), then run owner:preview fresh so the owner's next glance at 192.168.4.92:5173 shows current main with the version line. Add two lines to OWNER_CHECKLIST.md item zero area: your local preview at that address is now always latest main after any session closes; if the printed SHA ever disagrees with the newest session report, tell the builder rather than debugging it yourself.

JOB 4, close per rule 10 with the run link; session report includes the first printed version line as proof the mechanism works.
