IFS_BRANDING_CLEANUP_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Explicit paths only, no em or en dashes. No lock exception exists in this session.

OWNER APPROVAL ON ENTRY: the untracked directory frontend/public/assets/themes/future-spinner/branding/ is approved for deletion from the build machine.

JOB 1. Delete the directory, add the path to frontend/.gitignore with a one-line comment naming the TR-047 finding, and rebuild: record the fresh production bundle size and confirm it matches a clean-clone build within rounding.

JOB 2. Record in CLAUDE.md's conventions, one sentence: the staging bundle uploaded to the platform is always built from a fresh clone, never from a working machine, so the uploaded artefact is reproducible by definition.

JOB 3. Session report with dated section and archive copy; FOR THE NEXT SESSION states the repository is fully DTT-ready and the remaining items are the owner's portal session per DTT_PROTOCOL.md and the two one-time uploads.
</br>
