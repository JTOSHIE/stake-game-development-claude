FS_KIT_V4_AND_BRAND_Prompt.md (v2, supersedes the v1 block from the previous Fable reply, which is DEAD unrun). Save this verbatim to reports/briefs/ and commit it with the work. Fresh session on main, explicit paths, commit per job, no em or en dashes, no lock exceptions. No owner lines are needed; both decisions below are derived or already ruled.

JOB 1, BRAND INGEST. Both owner-supplied external art deliveries are in ~/Downloads: (a) the We Roll Spinners logo variant pack (transparent PNGs 32 through 1254, webp, jpg, ico, pdf, and its README) and (b) we-roll-spinners_future-spinner_tile.png, the composed Future Spinner tile. Ingest both through the deterministic pipeline with provenance (source hashes, external origin, date, found-at path): the logo pack as candidate g under design-system/brand/provider_mark/, the tile under design-system/brand/tile/ as the composed master.

JOB 2, LOGO DECISION, DERIVED NOT ASKED. Re-read the captured provider-logo requirements and any rendered-size evidence in docs/stake-engine-live/ and the portal captures (how small Stake actually renders the provider mark). Build a true-size comparison of candidate f versus candidate g at exactly those rendered sizes, smoothing disabled. The delivery WeRollSpinners-Logo.png goes to whichever candidate is measurably more legible at the SMALLEST size the platform renders; record the derivation (sizes, measurements, verdict) in the provenance and supersede the other in the README per convention (h). Either way, candidate g's pack is recorded as the studio brand set for non-portal use (favicon, site, print) per its own README.

JOB 3, TILE DELIVERY. Re-read the captured tile requirements and produce what the Tile Editor actually needs from the composed master: flat portrait export at documented dimensions with delivery naming if a flat image is accepted; if layers are required, derive background and foreground where cleanly possible and record honestly what could not be derived so the owner composes with the existing BG and FG plus the new master as reference. Update 03_branding accordingly.

JOB 4, SIDEPROJECT OFF-REPO, owner ruled. Archive sideproject/ out of the repository entirely: zip it to ~/Desktop/WRS_ARCHIVE/sideproject_2026-07-26.zip with a one-line README inside noting it is concept material, possible future theme inspiration, not project code; verify the zip, remove the directory from the working tree, and record the archive path in the session report and in WRS_MASTER_DOCUMENT.md's horizon section.

JOB 5, KIT V4. Fresh clone at HEAD, build, all dist gates in the clone, assemble ~/Desktop/FS_UPLOAD_KIT_V4/: 02_frontend_upload (carrying the replay fix, sizing fixes, dash purge, FEATURE PRICE line) and 03_branding (logo per JOB 2's derivation, tile deliverables), single use, README naming the SHA. Then update the walkthrough to PART 9c: upload and publish V4; replay any bet live from the Bets panel, ideally event 22975, confirming the celebration presents; twenty cruise spins bracketed by session panel screenshots; the remaining observations; the tile in Design Thumbnail from the new deliverables; the Guidelines ticks with STAKE_GUIDELINES_SELF_ASSESSMENT.md open; never Start Approval.

JOB 6, CLOSE per rule 10 with the remote run link; session report, dated section, archive copy; FOR THE NEXT SESSION: the owner runs PART 9c, then Fable's benchmark polish review, then external review round three.
