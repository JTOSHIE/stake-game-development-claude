CAPACITY DIRECTIVE, owner's order: this session runs under open allowance. Deploy parallel agent squads at the scale the work supports per docs/skills/FULL_AUDIT_METHOD.md: discovery squads on disjoint surfaces, shared-nothing adversarial verification after, ledger shards, one marshal, this session as sole writer. Full tool inventory authorised: task agents, Playwright with browsers installed, every committed gate and conformance script, the local preview and RGS harness, image tooling and OCR. Finish the brief; stop only at wave boundaries with resume state written and the exhausted resource named.

FS_STREAM_TEST_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh session on main, explicit paths, commit per job, no em or en dashes, no lock exceptions; anything appearing to need a locked path is parked with options. ONE COHERENT JOB: the stream test, the owner's lens made operational. Context, binding: this game will be shown on stream to tens of thousands of viewers; the standard is that nothing on screen, at any moment, in any transition, reads as less than a top studio's work.

WAVE 1, CAPTURE SQUADS. One squad per preset (the seven platform presets plus one stretched desktop window), each simulating the watched session end to end against the local harness: splash, entry, base spins including a dead streak, feature trigger, feature run, big win, max win celebration via the wincap fixture, collect, menu, paytable end to end, a buy flow, and back to base. Capture every state AND every transition midpoint, catalogued per the analyst pattern.

WAVE 2, THE WATCHED-FRAME AUDIT. Fresh squads, shared-nothing, audit every captured frame under the stream lens across five channels: composition (anything off-balance, cramped, or dead), typography (any inconsistency of case, weight, spacing, or family, any number that shimmies or clips at any magnitude), motion residue (any frame where two surfaces overlap wrongly, any pop, any element caught mid-teleport), localisation (run one full watched session in German and one in Arabic; any English word or LTR artefact on a localised frame), and voice (any string that reads machine-made against docs/QUALITY_CHARTER.md). Every finding severity-tagged with STREAM reserved for anything a watching audience would notice, each with its frame path and, where fixable in unlocked frontend code, the proposed fix.

WAVE 3, FIX AND RE-PROVE. Apply every small unlocked fix in one pass; re-run the affected squads' captures; adversarially verify each fix against the fresh frames, not the old ledger; anything larger than small is parked with a design note and the owner's decision framed in one line. Gates green, seeded per convention (p) for any gate touched.

CLOSE per rule 10 with the run link: the ledger fully dispositioned, the STREAM-severity count before and after stated plainly, the frame evidence committed, and FOR THE NEXT SESSION naming what remains. The session report additionally records, for the owner's model trial: total agent count deployed, waves completed, self-caught errors, and deviations resolved in the open, so this run can be compared like for like against the Opus sessions.
