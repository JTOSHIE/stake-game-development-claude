FS_SESSION1_STREAM_CLOSE_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh Opus Ultra session on main, drafted per reports/briefs/_TEMPLATE.md, container orchestration only, no lock exceptions, explicit paths, no em or en dashes.

BUDGET: 14.5M usable, 4.2h. Reserve 1.5M. Main loop 2.0M. Agents 11.0M.
SCALE: squads sized per AGENT_BUDGET_AND_SCHEDULING.md; 20 to 25 artefacts each; small-viewport frames upscaled to 1600px before any squad judges them (the sight gate); compute verification cost at launch, not after; post the Plan of Record before the first wave.
STOP LINES: no new waves at 4.0M left; no new agents at 2.5M left; close at 1.5M left.
DEGRADATION ORDER: mobile re-run, then marshal clustering, then cluster verification with STREAM panels, then the fix batch, then re-proof; anything below the current tier at a stop line is parked with resume state, never attempted.
DONE MEANS: a verified STREAM-severity ledger reading zero or owner-parked, with every applied fix re-proven from fresh frames.

JOB 1: re-run the six mobile squads on 1600px-upscaled frames per Fable's ruling; replace the PROVISIONAL mobile shard sections. JOB 2: marshal the full ledger into clusters by defect identity (grep-level, main loop). JOB 3: cluster verification per Fable's ruling: two representative instances from different squads per cluster, two-verifier panels on every STREAM cluster, divergent instances reopen their cluster; container-orchestrated verifier agents, shared-nothing. JOB 4: fix batch for verified small unlocked findings, including MID-01 per Fable's clock ruling (banner and WIN pod driven from one shared count-up source, frame-level equality asserted) and the 16x letter-x unit unified to the multiplication glyph; each fix re-proven from freshly captured frames, gates green, seeded per convention (p); larger items parked with one-line owner decisions. JOB 5: close per rule 10 with the run link, the ledger fully dispositioned, the before and after STREAM counts stated, and FOR THE NEXT SESSION handing Session 2 its parameters from Fable's map.
