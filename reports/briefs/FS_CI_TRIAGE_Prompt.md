FS_CI_TRIAGE_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. INTEGRATOR session, on main. Explicit paths, commit per job, no em or en dashes, no lock exceptions.

JOB 1, MAKE MAIN GREEN. Root cause, reproduced by Fable: layout_fit_gate.mjs and contrast_gate.mjs import chromium and were added to the browser-free static job; they crash at chromium.launch() on every CI run since. Fix per Fable's ruling: move both into a new second job "browser gates" in checks.yml that installs the chromium binary (npx playwright install chromium --with-deps) after npm ci, blocking, same security posture (no secrets, contents read only); update the workflow's header to record the two-job policy and why. Push, then VERIFY THE REMOTE RUN GREEN via the API or gh before proceeding, and record the run number and link in the session report. Re-run or confirm superseded the failed runs 117 to 120.

JOB 2, RULE 10 INTO PROTOCOL. Append to the MULTI-TRACK PROTOCOL in CLAUDE.md, mirrored in WRS_MASTER_DOCUMENT.md: (10) a red run on main stops the line: no new job starts until main is green; every session verifies its own final push's remote CI result before closing and records the run link in the session report; local gate results never substitute for the remote run. Alongside it, record the corrected account of runs 117 to 121 so the history is honest.

JOB 3, UNBLOCK THE ANALYST. Amend docs/records/tracks/quality-sweep.manifest to exclude reports/qa/live_stats/** (narrow its reports/qa glob), resolving SA-013; re-run the disjointness check and record it. Do NOT merge PR #115; Fable verifies it first-hand next check-in and rules on merge then.

JOB 4, SA-012, THE MODIFIED EVIDENCE. On the build machine, the four uncommitted changed files under reports/screens/scatter-anticipation/ are restored from HEAD, with the cause identified if cheaply possible (which script re-rendered them at 01:11) and this rule recorded beside convention (h): proof and gate scripts write to scratch paths only; committed evidence directories are never written outside a job that explicitly regenerates evidence.

JOB 5, CLOSE. Session report, dated section, archive copy, remote run link per rule 10. FOR THE NEXT SESSION: Fable verifies PR #115 and the green CI; the owner's second visit gains one line, twenty cruise spins with a before and after balance frame for the analyst; the two prepared track briefs remain ready to paste; the polish review follows the owner's visit.
