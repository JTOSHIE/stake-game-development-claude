FS_RECORDS_KIT_V9_Prompt.md (v2). Save this verbatim to reports/briefs/ and commit it with the work. SUPERSEDES both FS_RECORDS_AND_KIT_V8_Prompt.md and the v1 of this brief, both DEAD unrun. Fresh session on main, explicit paths, commit per job, no em or en dashes, no lock exceptions.

JOB 1, THE 403 BACKGROUNDS, FIRST. The owner's console capture shows the live build on game entry future-spinner-2 requesting three background files under assets/themes/future-spinner/backgrounds/ and receiving 403 from the platform file API. Determine the root cause with evidence: are the three files in the current dist (check the build-diet prune rules and the kit manifest), were they in the uploaded set, and is the entry fully published; fix at root (unprune and re-gate if pruned, or record the publish-state explanation if the paths were scratch), and add a dist gate assertion that every asset path the code references exists in dist, so a missing-asset class can never ship silently, seeded per convention (p). Record as fact, owner-ruled: future-spinner-2 IS the submission entry (fresh clean upload; the original entry awaits deletion after the platform cooldown, which is an owner checklist item); the approvals 404 on the new entry is expected until Start Approval and is recorded as such.

JOB 2, VERSION LABEL. Add a human version, v9 now and incremented per kit, to build-info.json, the console boot line and the kit README beside the SHA, so the owner reads v9 rather than a hash.

JOB 3, INTAKE AND CLOSURES. Commit all new Desktop captures: the Arabic and Danish locale frames showing the locale pass live, the currency and language menu scrolls, the bracketed cruise session, the observed-once red authenticate, and the 403 console frame. Analyst-pattern catalogue and money reconciliation on all of them; difference the bracketed cruise frames and close TR-075 if the arithmetic holds at exactly 1x, recording the wallet proof for the fifth and final mode; the red authenticate recorded observed-once with frames, escalating only if it recurs.

JOB 4, HARNESS LEAK, PER FABLE'S RULING. Tracker row for the gate-family preview-server leak, fixed by option three: an in-process node:http static server serving dist inside the gate process, built once as a shared helper beside lib/evidencePaths.mjs and adopted across the entire gate family, deleting the orphanable child rather than managing it; port-reaping permitted only as a temporary guard during the migration and removed in the same pass that completes it; post-run assertion that zero owned processes survive, seeded per convention (p). Also close out the two-day-old corpse processes from the earlier sessions per that discipline, touching nothing that is not provably ours, and note the owner's old vite dev server is handled by the owner-preview rule if FS_OWNER_PREVIEW_RULE has landed, or left for it if not.

JOB 5, RECORDS TO HEAD. OWNER_CHECKLIST updated: ticks recorded with evidence paths (cruise spins DONE, V7 and V8 era uploads done, submission entry decided as future-spinner-2); open remainder: Guidelines ticks on the submission entry, USPTO confirmation, payments model choice, accountant conversation, delete the old future-spinner entry once the cooldown allows, and next portal visit live-verifies the bet denomination picker, the max-win hold and feature resume if the player-experience pass shipped them. Dossier and FIX_LIST reconciled to HEAD including the V7 reconciliation, the player-experience pass outcomes, and this session's findings; MONEY_DISPLAY_EXPLAINED pinned as checklist item zero if not already; the dossier's staging references updated to name future-spinner-2 as the submission entry.

JOB 6, KIT V9. Fresh clone at HEAD, all dist gates in the clone including the new asset-reference gate, frontend only, single use, v9 labelled. Short walkthrough: upload to future-spinner-2, publish, screenshot the console line showing v9 and the SHA (which also proves the 403 fix live when the backgrounds load), then the eyeball list including the recomposed surfaces, the bet picker if shipped, and one max-win hold check if reachable.

JOB 7, CLOSE per rule 10 with the run link; session report, dated section, archive copy; FOR THE NEXT SESSION: Fable verifies this arc at git level, takes the benchmark polish review against the committed capture packs, ratifies the round three reviewer prompt, and round three runs.
