FS_FINAL_MILE_Prompt.md. Save this verbatim to reports/briefs/ and commit it with the work. Fresh session, Ultra, on main, explicit paths, commit per job, no em or en dashes. One conditional sanction in JOB 4 and nowhere else. Jobs ordered heaviest first; stop honestly between jobs, never inside one.

JOB 1, INGEST ROUND THREE AND THE OWNER'S SESSION. Commit the three round-three reviews verbatim to sources with hashes; tracker rows with Fable's dispositions: R3R1 scored its own environment failure, both maths claims REFUTED by the platform's live ingestion and the ×5000.00 wincap; R3R2 and R3R3 findings CONFIRMED as mapped below. Ingest all the owner's Desktop captures from today; close TR-057 (platform prints XGC at two decimals, interim confirmed) and rule TR-064 from the owner's zero-win network frames if they show no end-round emitted.

JOB 2, LOCALISATION COMPLETION, the heavy one. Key every remaining player-visible English string, the roughly thirty sentence-case prose strings, mode descriptions, paytable prose, rules text, and the celebration words (BIG WIN, MEGA WIN, MAX WIN REACHED, TAP TO CONTINUE and kin), across all sixteen locales with social variants where the override table applies, routed through the existing layer; replace the ALL-CAPS-only conformance scan with a rendered-DOM prose scan across every locale in both modes, seeded per convention (p); the German capture that failed becomes the regression fixture.

JOB 3, MONEY DISPLAY, THE CLASS, per Fable's ruling. One shared fit-or-abbreviate behaviour for every money readout in every profile: render full when it fits at the floor, abbreviate when it cannot, never clip, never overflow during count-up; fixes TR-086, the owner's GC 7,684,200 top-clip, and the count-up zoom overflow in one mechanism; proof sweeps a continuous width range and a magnitude ladder from cents to hundreds of millions GC, seeded per convention (p).

JOB 4, TR-087 AND THE CONFIG QUESTION. Fix the replay ghost pod at root (the legacy multiplier and win widget rendering over the stripped replay view, owner-confirmed live) with a replay-mode DOM assertion that only replay UI mounts. Then verify R3R3's claim that rgsService.ts parses minBet, maxBet, stepBet then discards them before consumption: if consumption genuinely happens elsewhere, record the refutation with the wiring traced; if the claim is true, the owner's paste of this brief grants the narrow sanction to lift Edit and Write on rgsService.ts solely to wire the parsed config through to its consumers, with the full lock proof ritual, restored deny lines, and the PR-style diff summary in the session report.

JOB 5, PROVENANCE AND EVIDENCE CURRENCY. Regenerate the books equality proof from the local books under the current fail-closed verifier, commit the run log with counts and hashes, refresh BOOKS_MANIFEST so its date, blob and figures agree; refresh the dossier's stale statements reviewers named (tile status, soundtrack blurb) to current fact; then regenerate the polish capture pack at post-fix HEAD so evidence and build carry the same version for the first time in the programme.

JOB 6, KIT V11 AND CLOSE. Fresh clone, all gates in the clone, v11 labelled, single use; walkthrough: upload to future-spinner-3, v11 console screenshot, one glance each at a German session's prose, a huge GC win, and a replay; then the owner ticks the remaining Guidelines with the self-assessment open (53 recorded as satisfied by platform Replay, 15 as frame-fits by design). Close per rule 10 with the run link; FOR THE NEXT SESSION: Fable verifies, and the Start Approval decision goes to the owner with everything green.
