# Future Spinner sound files, provenance

Generated by `tools/audio_forge/` (Stable Audio 3 open weights), mastered by
`tools/audio_forge/master.py`, both at the repo root. All twelve shipped rows below use
seed `20260707` and model `stabilityai/stable-audio-3-medium`, no fallback to
small-sfx/small-music occurred for any of them (the pipeline's probe stayed under the
120s limit and used medium throughout; see `reports/audio/GENERATION_LOG_2026-07-13.md`
for the full run record, and `reports/SESSION_REPORT.md`'s Job 1 section for the
mastering pass itself, silence trim, loop-point/bar alignment, loudness, encoding).

| Shipped file(s) | Row / prompt pointer |
|---|---|
| `bgm_loop.{mp3,webm}` | `bgm_loop`, dark cyberpunk synthwave bed, 100 BPM |
| `bgm_tension.{mp3,webm}` | `bgm_tension`, Overdrive/tension bed, 140 BPM |
| `anticipation_build.{mp3,webm}` | `anticipation_build`, rising tension loop |
| `spin.mp3` | `spin`, turbo whoosh + ignition snap |
| `reel_stop.mp3` | `reel_stop`, mechanical reel clunk |
| `reel_stop_anticipation.mp3` | `reel_stop_anticipation`, heavy metallic slam |
| `win_small.mp3` | `win_small`, bright synth chime |
| `win_medium.mp3` | `win_medium`, rising two-note chime |
| `win_big.mp3` | `win_big`, ascending fanfare + coin cascade |
| `win_epic.mp3` | `win_epic`, euphoric fanfare + bass drop |
| `scatter_land.mp3` | `scatter_land`, futuristic ping |
| `ui_click.mp3` | `ui_click`, minimal digital tap |

Full prompts (including the shared SFX palette prefix and negative prompt) are in
`tools/audio_forge/generate.py`'s `MANIFEST`.

## Licence

Generated audio is subject to the **Stability AI Community License Agreement** (the
model's licence, not this repo's), see `tools/audio_forge/LICENSE.md` and
`tools/audio_forge/NOTICE` for the full text and attribution line.

## Legacy files

NONE, as of 2026-08-09. This section used to say that scatter.mp3 and win.mp3 were
unreferenced leftovers from an earlier sound-tier refactor, "left in place rather than
removed as out of scope for this pass". They were removed from THIS directory some time
ago, and the paragraph was never updated.

It kept resolving only because copies survived in the ROOT frontend/public/assets/sounds/
tree, which was a full duplicate nothing could reach: audio paths are built under the theme
base in `frontend/src/lib/stores/themeStore.ts`. That root tree was deleted on 2026-08-09,
1,955,845 bytes across 14 files, which is what finally made this claim fail out loud.
