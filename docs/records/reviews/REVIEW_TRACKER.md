# Review tracker

One row per distinct finding across all external reviews, plus Fable's dispositions.
Built before any fix, per the CONSOLIDATED REVIEW REMEDIATION PROGRAMME (2026-07-27).

**Rules.** Every merge updates its row in the same commit. Every future review appends
rows rather than starting a new document. Nothing is ever answered twice from scratch:
if a finding recurs in a later review, it gets a source reference added to the existing
row, not a new row.

Australian English, no em dashes or en dashes.

## Status of the source ingest

### Round two (2026-07-25)

**RESOLVED.** All three round two reviews saved verbatim to `sources/` as
`round2_review1.md`, `round2_review2.md` and `round2_review3.md`, with SHA-256, the source
document's own hash, the extraction method and a proof that the three-way split is
byte-lossless all recorded in `sources/README.md`. All three reviewed HEAD `78d02cc`.

Scores as submitted: reviewer 1 **2.00 / 3.00**, reviewer 2 **1.67 / 3.00 (reject)**,
reviewer 3 **0.67 / 3.00 (reject)**.

Round two rows are the `### Round two rows` block inside the tracker below and run from
TR-038. Coverage is exhaustive against the three ingested files: reviewer 1's F-01 to F-08,
reviewer 2's findings 1 to 7 and reviewer 3's findings 1 to 10 each have a row or a source
reference on an existing row, with none omitted.

### Round one

**RESOLVED 2026-07-27.** All three review documents supplied by the owner and saved
verbatim to `sources/`, unedited, with SHA-256 recorded:

| File | Lines | SHA-256 (truncated) |
|---|---|---|
| `review1_claude.md` | 285 | `6fac7e82...` |
| `review2.md` | 295 | `97b6a339...` |
| `review3_openai.md` | 425 | `69c5b170...` |

**Coverage is now exhaustive**: every distinct finding across all three reviews has a
row below, deduplicated where two reviewers found the same thing.

Scores as submitted: review 1 **0.67 / 3.00 (reject)**, review 2 no numeric score given,
review 3 **2.00 / 3.00**.

## Disposition vocabulary

| Value | Meaning |
|---|---|
| CONFIRMED | Real, reproduced, needs a fix |
| REFUTED | Not true of this codebase, with the evidence recorded |
| ALREADY COVERED | Real concern, already guarded by an existing assert or gate |
| STALE SNAPSHOT | Was true when the reviewer looked, fixed on `main` before receipt |
| HALLUCINATED | The cited file, path, symbol or behaviour does not exist |

## Tracker

| ID | Finding | Source | Disposition | Severity | Branch | Status | Fix evidence |
|---|---|---|---|---|---|---|---|
| TR-001 | Build loads fonts from an external CDN (google-fonts), breaching the static-build rule | Review 2 | **REFUTED** | Blocker as filed | n/a | CLOSED | **Independently re-verified 2026-07-27, not accepted on relay.** Zero hits for `fonts.googleapis`/`fonts.gstatic` in `frontend/src`, in `frontend/index.html`, across the whole `frontend` tree excluding `node_modules`, **and in the built `frontend/dist` bundle**. Fonts load via `@fontsource/orbitron` imported in `src/main.ts` (400/700/900), which is bundled locally. The reviewer's blocker does not exist in this codebase. |
| TR-002 | Missing `sweeps_en.json` language file required for social mode | Review 2 | **REFUTED** (HALLUCINATED path) | Blocker as filed | n/a | CLOSED | **Independently re-verified 2026-07-27.** `find` for `sweeps*` across the repo excluding `node_modules` and `.git` returns **zero** matches: no such file, and no reference to one. Social strings are implemented via the `socialLabel`/`socialBlurb` overrides in `frontend/src/lib/config/fsModes.ts` plus the `isSocial` store, not via a `sweeps_<lang>` file. The platform docs *recommend* that pattern; we did not adopt it. The reviewer asserted a missing file for an approach we never took. |
| TR-003 | NITRO/super wincap frequency disagrees with our stated figure | Review 2 | **REFUTED**, pending the reviewer's arithmetic | High as filed | n/a | OPEN pending reviewer | **Independently recomputed 2026-07-25 and re-checked 2026-07-27.** `super` P(>=5000x) = `4.000000e-03`, which is **exactly 1 in 250.0**, from `reports/qa/bet_level_compliance_raw_2026-07-25.json`, itself produced by `scripts/qa/bet_level_compliance.py` reading the frozen publish tables with exact integer ratios and no sampling. This is the third independent agreement (Fable, the Part 2 recomputation, and this re-check). Row stays OPEN only because the reviewer's own working has not been seen; if they used the raw cap probability without the documented 0.8 cost scale, or divided by a different denominator, that would explain the gap. Not a code change either way. |
| TR-004 | R3 F3: prohibited "Buy" language in `fsModes.ts` blurb/label for bonus and super, no social branching | R3 (BLOCKER for stake.us) | **STALE SNAPSHOT** | was Blocker | n/a | CLOSED | Fixed before receipt. Verified on current `main`: `fsModes.ts` carries **9** `socialLabel`/`socialBlurb` occurrences and both consumers branch on `$isSocial`. Landed in the 2026-07-14b wording ruling and extended 2026-07-26. |
| TR-005 | R3 F5: game tile assets (BG, FG hero, provider logo) not produced, hard blocker for portal upload | R3 (BLOCKER) | **STALE SNAPSHOT** | was Blocker | n/a | CLOSED | Fixed before receipt. Verified on disk: `design-system/brand/tile/` holds `tile_background_master.jpg`, `tile_hero_full.png` plus generation notes. **Review 1 independently confirms these as PASS** ("2048x1152 and well under 3 MB", "transparent and under the combined size ceiling"), which is the stronger evidence since two reviewers disagreed. Provider-logo naming and 48px legibility remain open, see TR-031. |
| TR-006 | R3 F7: `CLAUDE.md` compliance line names `ControlBar`/`AutoPlayModal`, which do not exist | R3 (MINOR) | **STALE SNAPSHOT** | was Minor | n/a | CLOSED | Fixed before receipt. Reworded to describe behaviour rather than component names in the cleanup pass (PR #90, merged). The remaining textual occurrence is the parenthetical that explains the reword. |
| TR-007 | R3 F8: `CURRENCY_SCALE` defined three times, money path drift risk | R3 (MINOR) | **STALE SNAPSHOT** | was Minor | n/a | CLOSED | Fixed before receipt. Verified: `replayService.ts` now **0** local definitions (imports canonical), `utils/currency.ts` 1 (canonical), locked `rgsService.ts` 1 (recorded in `LOCKED_FILE_DEBTS` and held by `currency_scale_drift.test.mjs`). PR #90. |
| TR-008 | R3 F10: `endRound()` not wrapped in `_withRetry` | R3 (MINOR) | **CONFIRMED, was live** | Minor | `fix/R1a-rgs-locked-pass` | **MERGED** (PR #103) | Disposition was corrected on evidence against Fable's original STALE listing, and the correction held: `endRound()` really did call `_post` directly while `_withRetry` wrapped `play` only. Closed inside the first lock sanction, scope (b). The severity was understated as MINOR: the unprotected leg was the **credit** leg, so a single transient network failure left the round open with the wallet having taken the bet and the player not paid. Retry is safe because end-round is idempotent on the round id. |
| TR-009 | Live path parses legacy `board`/`win`/`scatter` in locked `rgsService.ts` while the canonical `roundInterpreter` and shipped books use `reveal`/`winInfo` | Consensus (per programme brief) | CONFIRMED | Critical | `fix/R1a-rgs-locked-pass` | **MERGED** (PR #103) | Confirmed, and TOTAL rather than partial. Measured across the first 300 rounds of the shipped `books_base.jsonl.zst`: `reveal` 724, `winInfo` 499, `setWin` 499, `setTotalWin` 774, and `board` **0**, `win` **0**, `scatter` **0**. Every branch of the parser was dead on a live round, so `_emptyBoard()` was returned unchanged and a live player would have watched an empty grid with no wins and no scatter. Fixed in the first lock sanction by **delegating to `roundInterpreter`** rather than reimplementing the schema, since a second implementation is the duplication that caused it. The six-row `reveal` board is stripped to the visible 5x4, verified against the book ([6,6,6,6,6]) rather than assumed, because counting padding as real is what produced the retracted six-scatter claim. Held by `rgsService.parse.test.ts`, 46 assertions against real decoded book rows including totals reconciling to each round's own `payoutMultiplier` (CI gate 11). **Method note:** TR-009's recorded method preferred an adapter where raw events are exposed, and they are; the sanction enumerated the at-source fix and was ratified, establishing convention (n), the sanction governs over an earlier recorded method. |
| TR-010 | Production build can reach the dev mock; a failed live auth silently falls through to fabricated rounds | Consensus | CONFIRMED | Critical | `fix/R2-mock-containment` | **MERGED** | Confirmed, and the severity was in the failure mode rather than the code size. `rgsService.initRGS()` sets `_rgsMode = false` on **both** branches of its error handling, the dev no-params case and a genuine authenticate failure, and `spin()` then falls through to `_mockSpin()`. A production player whose session failed to authenticate would have been served the mock: fabricated boards, fabricated wins, a balance moving on screen, and nothing reaching the wallet. `rgsService.ts` is locked and out of scope for this run, so the fallthrough is made **unreachable** rather than removed. New non-locked `stores/liveGuard.ts` states the rule positively: in a production build betting is enabled only when launch parameters are present AND authenticate reported no error; absence of evidence that we are live is treated as evidence that we are not. Every bet route is gated, including the spacebar, which reaches `handleSpin` by its own path. A blocked session shows a non-dismissible player-visible banner, translated in all 16 locales, because silently disabling the spin button would read as a bug and invite reloading into the same state. `mock_containment_check.mjs` asserts both halves: every mock import is `import.meta.env.DEV`-guarded in source, and the built bundle contains no mock marker. **The bundle half was vacuous on first write** and reported PASS while scanning one file, because it reused the source walker whose filter is `.svelte`/`.ts`; it now recurses all of `dist` and refuses to pass on fewer than two files. Negative control run: injecting a marker into `dist/index.html` makes the gate FAIL, removing it makes it PASS, so the gate can genuinely fail. The 3.5 MB curated sample payload stays out of the 16 MB bundle. |
| TR-035 | Session recovery: `authenticate.round` is discarded, so a mid-round reload abandons an open round | Consensus / R11 | CONFIRMED | High | `fix/R11-session-recovery` | **MERGED** | `authenticate` returns `round?: { roundId, state }` and `rgsService` maps it faithfully; `initRGS` then discards it, publishing balance, currency, bet levels and jurisdiction flags and never looking at `round`. A player who reloaded mid-round came back to a fresh game while the RGS still held their round. **On a `pending_end` round that is money**: the win is decided and waiting to be credited, and nothing was ever going to ask for it. Fixed entirely outside the lock, since `parseSessionParams`, `authenticate` and `endRound` are all exported: `stores/sessionRecovery.ts` settles a `pending_end` round through the platform's own `endRound` and adopts the authoritative balance. Proven by 17 assertions against an injected stub platform (`sessionRecovery.test.ts`) - ES module exports are read-only bindings so the calls are injected rather than patched, and a `pending_end` round cannot be produced on demand against a live RGS. Recovery never throws: a failure must not stop a player reaching the game. |
| TR-035b | An `open` round cannot be resumed from `authenticate` alone, and the platform's expectation is unverified | R11 spin-off; **R2R1 F-03 (MAJOR)** | **RE-RULED and RESOLVED (Fable, 2026-07-26)** | Medium | `main` | **MERGED. RESUME AND SETTLE, no forfeit path.** DTT confirms the payload, not the design | **The premise the park rested on is gone.** The row was parked because "recovering an open round needs the round's events, and authenticate does not return them". Under the official contract it DOES, at `round.state`, which JOB 4 established and JOB B acts on. Recovery now extracts the events with the service's own reader, interprets them through `roundInterpreter`, **replays the round in front of the player**, settles via `endRound`, then shows one plain dismissible banner. There is no forfeit path and none is needed: the only reason to forfeit was not knowing what the round contained. A round whose events cannot be read still settles, because the money is not held hostage to the presentation, and the outcome records `presented: false` so the case is visible. Held by gate 10b (21 assertions, including that PRESENT runs BEFORE endRound in one recorded call sequence) and by a production-build Playwright proof, `reports/qa/session_recovery_proof_2026-07-26.json`. **What DTT still confirms is the payload, not the behaviour**: that a real active round carries its events at `round.state.events`. | `authenticate` reports `state: 'open'` but does not return the round's events, so the presentation cannot be rebuilt from it. The build surfaces the open round and **stops**: it does not settle it, because settling could forfeit a feature the player has not seen, and it does not fabricate a presentation, because that would be inventing an outcome. **Options:** (a) call `endRound` on an open round too, simplest and guarantees no uncredited money, but risks forfeiting an unseen feature; (b) leave it open and start fresh, current behaviour, safe for the player but leaves platform-side state the game never closes; (c) re-request the round if the platform exposes a call that returns its events, which is the correct answer if such a call exists. **Cannot be chosen from documentation**: which is right depends on what the RGS actually does. `docs/staging/DTT_SESSION_RECOVERY_VERIFICATION.md` step 3 asks the four questions that settle it, and step 4 tests the double-credit risk on repeated reloads. |
| TR-011 | Books/lookup equality unproven; dossier does not distinguish repo-committed artefacts from the local upload set | Consensus | CONFIRMED | High | `fix/R3-books-equality` | **MERGED** | **Proven, and it holds.** `tools/verify_books_lookup_equality.py` decodes every row of every mode and makes five independent reconciliations per round: book-vs-lookup on the same id, the closing `finalWin`, the last `setTotalWin`, the per-spin `winInfo.totalWin` values summing to the declared payout, and each spin's individual symbol wins summing to its own total. The last two are the substantive claim; the first alone would only prove two files agree on a number. **500,000 rounds, 4,455,829 assertions, 0 failures** (`reports/qa/books_lookup_equality_2026-07-25.json`). Read-only, streams via the `zstd` CLI so it needs no third-party package and a reviewer can run it on their own copy. **Two findings from the run.** (1) The 5,000x cap truncates recorded totals: the first draft asserted plain equality and reported 22 failures across 2,500 rounds, every one at exactly 500,000 centibets and none anywhere else. They were the cap working, not defects; reporting them would have been badly wrong, and the rule was derived from the data before being written. (2) Raw capped-row counts are not probabilities, because the books are a sample set and the lookup carries the weights. Weighted, `base` is 0.0000100000 (1 in 100,000) and `bonus` 0.0010000000 (1 in 1,000), matching the published facts **exactly**; `cruise` 1 in 250,000, `antelite` 1 in 80,000 and `super` 1 in 250 are now measured for the first time. |
| TR-012 | Social/currency incomplete: XEC absent from the SC family; social mode not derived from currency code; accessibility strings carry real-money terms | Consensus | CONFIRMED | High | `fix/R4-social-currency` | **MERGED (XEC parked, see TR-012b)** | Two of three parts closed with proof. **(1) Accessibility strings.** 14 hardcoded English `aria-label`s carried the restricted phrase "bet" (`Increase bet`, `Decrease bet`, `Max bet`, `Features and bet modes`) across `HudOverlay` and `FeatureMenu`. Invisible to every existing check, because the visible-text sweep reads rendered DOM text and screen-reader text is not rendered DOM text: a blind player in a social jurisdiction heard the vocabulary a sighted player was protected from. Three translated keys added across all 16 locales plus SOCIAL_OVERRIDES, `Max bet` routed to the existing `betMax`. Runtime proof `reports/qa/a11y_social_proof_2026-07-25.json`: social mode 0 restricted hits, real-money mode correctly retains bet wording. New permanent gate `a11y_social_terms_check.mjs` (CI gate 6a) checks both restricted phrases and hardcoded a11y text. **(2) Currency-derived social mode.** `parseReplayParams` derived `social` from the flag alone, so a replay URL with `currency=XSC` and no flag rendered full real-money vocabulary beside an SC balance. Now `socialFlag || isVirtualCurrency(currency)`, which can only turn social ON, never off, so it cannot suppress real-money language where that language is correct. **(3) XEC: parked, see TR-012b.** |
| TR-012a | RG wording: panel and reality-check strings were hardcoded English, incomplete, and the reality check had no stop control | R4 spin-off, ruled by Fable 2026-07-25 | CONFIRMED | Medium | `feat/rg-wording` | **MERGED** | Built on Fable's English masters, taken verbatim. Scope was wider than the original a11y note: the panel showed **three of the five** rows the masters require, `Total wagered` and `Total won` did not exist at all, `NET` was hardcoded English in every locale, and the reality check offered only an acknowledge button. **`Stop playing` is now a real control**: it clears `isAutoPlay` and `autoPlayCount`, so App cancels the pending continuation and the game returns to idle. A reality check that dismisses itself while autoplay keeps spinning is a notification, not a control. 10 keys x 16 locales, translated with the standard casino RG lexicon per the ruling rather than literal rendering. One social override only, `Total wagered` to `Total played`, since "wager" is on the platform's restricted list and "play" is its own stated replacement; the rest of the masters are already social-safe and are deliberately not restated, because an override that repeats its master is a second copy to drift. Proof `reports/qa/rg_wording_proof_2026-07-25.json`: English rows match the masters in order, German renders genuinely translated rather than falling back, the body interpolates `{time}` and `{amount}`, and Stop takes autoplay from `true/25` to `false/0`. **Translation confidence, recorded as directed:** HIGH for de, es, fr, pt, pl, ru, zh, ja, ko (established casino RG terminology with settled conventional equivalents); MEDIUM for fi, id, tr, vi, hi, ar (correct and idiomatic, but RG phrasing is less standardised in these markets and "reality check" in particular has no single settled rendering). Per the ruling, the DTT language pass validates rendering, and professional review triggers only if a mandatory-display jurisdiction ever activates. |
| TR-012c | SC display placement: announcement shows leading, we ship trailing | First-party sources in conflict; **R2R1 F-01 (MAJOR)**, **R2R3 finding 7 (MAJOR)** | **RESOLVED by dissolving it** (Fable, 2026-07-25b) | Medium | `chore/docs-refresh` | **MERGED, DTT-verified later** | Neither side of the argument had to be right. The announcement's own words are the instruction: "Your game should use the provided display information rather than showing the raw currency code to players." Placement is therefore not a constant we choose, it is a value the session hands us. `formatBalance` now accepts optional `CurrencyDisplay` metadata using the **documented** field names (`symbol`, `symbolAfter`, `decimals`) rather than a shape of our own invention, and renders per payload. Partial payloads degrade field by field rather than being discarded whole. **Absent metadata, behaviour is byte-identical to before**, asserted, which is what makes this safe to ship before the DTT confirms which placement is live. 10 new assertions covering both placements, fiat as well as virtual, every partial combination, and the no-metadata equivalence. **WIRING GAP, named not hidden:** locked `rgsService.authenticate()` builds a typed object and drops any field not in its list, so a real payload's display metadata cannot reach this consumption layer yet. The mechanism is complete and proven; connecting it needs either a lock sanction adding the field to that mapping, or confirmation at the DTT that the platform sends it inside a field the mapping already carries. Empirical confirmation happens at the OFFICIAL on-platform tool during staging. |
| TR-012b | XEC currency code not implemented | R1, then first-party platform announcement 2026-07-25 | **RESOLVED: XEC IS REAL** | High | `feat/xec-currency` | **MERGED** | The HOLD is superseded by evidence, which is the discipline working rather than failing. A first-party Stake EU announcement states: "To be eligible for release on Stake EU, your games must support the XEC currency. Games that don't support it won't be released on the platform." That is the source the hold was waiting for. Implemented as **byte-identical to XSC by construction**, because the announcement's operative instruction is "Just like Stake US": the test asserts XEC equals XSC rather than asserting hardcoded output, so the two cannot drift apart if SC presentation ever changes. Also added to the social-currency set, since "games released on Stake EU will have social set to true" - defence in depth, so an XEC session presents social vocabulary even if the flag were absent. 8 new assertions, including that a formatted XEC balance never contains the string "XEC". **Note the cost of the hold was zero**: implementation took one line in each of two tables plus tests, exactly the afternoon Fable costed it at, so waiting for evidence lost nothing. |
| TR-013 | Bet-changing surfaces do not all drive from the authenticated bet-level model | Consensus | CONFIRMED | High | `fix/R5-bet-levels` | **MERGED** | Confirmed and worse than described. `HudOverlay` drove from `rgsBetLevels` correctly; `FeatureMenu` imported `increaseBet`/`decreaseBet`/`canIncreaseBet` from locked `gameStore.ts`, which index the hardcoded `BET_LEVELS`. **Derived arithmetically, not measured:** off the authenticated ladder `BET_LEVELS.indexOf($bet)` is -1, so `increaseBet` evaluates `BET_LEVELS[-1+1]` = `BET_LEVELS[0]` = 0.10. Pressing "+" **dropped the bet to the minimum**, "-" silently did nothing, and `canIncreaseBet` returned true so the control stayed enabled. Reproduced as the first three assertions of `betLadder.test.ts`. Fixed by a shared non-locked model, `stores/betLadder.ts`, driving from `rgsBetLevels` with `BET_LEVELS` only as fallback; both surfaces now use it, so the duplication that let them diverge is gone. `gameStore.ts` untouched (locked); its now-unreferenced bet actions recorded in `LOCKED_FILE_DEBTS` and `canIncreaseBet` allowlisted in the dead-wiring gate with the reason that it was wrong, not merely dead. **A second defect the unit test could not see, caught by the runtime proof:** `$:` aliases in `HudOverlay` latched the transient `false` that is correct for the instant between the ladder arriving and the bet snapping onto it, leaving both arrows disabled with a valid bet on screen. Markup now binds the stores directly. Proof `reports/qa/bet_ladder_proof_2026-07-25.json`: both surfaces step 100 to 200 to 100 on a JPY-shaped ladder sharing no value with the hardcoded one, never collapsing to 0.10. New CI gate 8. **CONFIRMED LIVE 2026-07-26, closure note promoted from `track/screenshot-analyst` ledger SA-020.** Until now this row was held by a unit test (`betLadder.test.ts`) and by reasoning; there was no evidence from a real session. The track's stats intake supplies it. The bet levels observed in live play are `EUR 450.00`, `EUR 500.00`, `EUR 750.00` and `EUR 1,000.00` (`reports/qa/live_stats/2026-07-26b_bets_page_all_modes_126_rows.json`, `reports/screens/screenshot-analyst-2026-07-26/11_bets_panel_super_5000x_wincap_row_and_buy_tiers.png`). **None of those is in the hardcoded fallback array**, which is `[0.10, 0.20, 0.50, 1.00, 2.00, 5.00, 10.00, 20.00, 50.00, 100.00]` at `gameStore.ts:7` and tops out at 100. A ladder the hardcoded array cannot express is therefore driving the game, which is only possible if both bet-changing surfaces are reading `rgsBetLevels` through the non-locked `stores/betLadder.ts`, as this row's fix intended. The `indexOf` returning -1 failure recorded in CLAUDE.md's LOCKED_FILE_DEBTS is not reachable in these sessions. Recorded because a fix confirmed only by its own test is a fix confirmed by its author. |
| TR-014 | Launch `lang` parameter not applied to the locale store before first render | Consensus | CONFIRMED | High | `fix/R6-locale` | **MERGED** | Gap confirmed by derivation, not guesswork: `rgsService.parseSessionParams()` read `lang` at line 298 into `SessionParams` and sent it to the RGS, but nothing published it to the `locale` store (`writable<Locale>('en')`, locked `gameStore`), which was never set at boot. `?lang=ja` rendered the whole game in English: parsed, carried, dropped. Fixed in `App.svelte`'s script body, so it applies **before first render** rather than flashing English. Validated against the **shipped `locales` map**, not a hardcoded list, so the check cannot drift from what ships. Spec quoted from the dated mirror: "lang | No | Language code". New `locale_launch_conformance.mjs`: **16/16 locales render their own strings, 10/10 malformed values fall back to English silently, zero page errors.** Proofs for `ja` and `ar`. |
| TR-014a | 40 player-facing strings hardcoded English, untranslated in all 16 locales | Discovered during R6 | **CONFIRMED, FIXED** | High | `fix/R24-locale-completeness` | **MERGED** | **18 new keys x 16 locales = 288 translations added**, wired into FeatureMenu, HudOverlay, BuyBonus, SessionPanel, BonusInstrumentColumn and ReplayMode, each using that file's existing translation idiom rather than introducing a second pattern. **Three of the 40 were misclassified as translation gaps and were something else**, established by derivation before any translation was written: `RTP` is a universal industry acronym left untranslated (the platform's own docs do the same); ThemeSelector's `SELECT THEME`/`BACK`/`COMING SOON` are **dev-only** (`import.meta.env.DEV` gated at App.svelte:1343) and never reach a player; and `OVERBOOST`/`CRUISE` were a **duplication bug, not a translation gap**, re-typed in three HudOverlay branches while `fsModes.ts` already declared them, and **already diverging in case** ('CRUISE' versus 'Cruise'). Those now read from `fsModes` via `modeLabel()`, which also gives them the social override for free. **New permanent gate** `locale_completeness_check.mjs`: every required interface key must exist in all 16 locales, and no component may render an unexplained uppercase literal. It caught a real missed string on its first run (`FeatureMenu` h2 title) and also produced a false positive on French that was verified and traced to the gate's own regex, not the translations. Verified after: locale launch conformance 16/16 plus 10/10 fallbacks, typecheck 0, social conformance PASS, owner_audit_v4 PASS. |
| TR-015 | RG jurisdiction flags not enforced: autoplay cap, `turboDisabled`, `minSpinMs` | Consensus | CONFIRMED | High | `fix/R7-rg-enforcement` | **MERGED** | Confirmed exactly. The RG model in `responsibleGambling.ts` was already complete and correct; the defect was that nothing consumed it. **`turboDisabled` had ZERO readers anywhere in the codebase**, verified by search: the flag was derived correctly, including the UKGC-shaped `minSpinMs > 0` implication, and every consumer ignored it, so a market that bans fast play still ran rounds at 2x and 4x. Now enforced in `cycleSpeed()` itself rather than only in markup, so no future caller can reach a banned speed, plus a subscription that resets a speed already chosen when the ban arrives late with the authenticate response. **`maxAutoplaySpins` gated only the infinite option**, so a market capping autoplay at 25 still offered and started 50 and 100; the offered list is now filtered AND the count clamped at `startAuto`, and a cap below every option still offers the cap so autoplay stays reachable at a legal count. `minSpinMs` already reached the autoplay gap via `rgSpinDelay`; the turbo ban it implies now covers manual spins too. Enforced at the store so all three layout branches inherit it. `responsibleGambling.test.ts` 14 to 33 checks, and **it was not in CI at all** until this pass, now gate 8. Runtime proof `reports/qa/rg_enforcement_proof_2026-07-25.json` drives the real controls: unrestricted cycles to turbo and offers 10/25/50/100/infinity; `disabledTurbo` and `minSpinMs: 2500` each force normal and disable all three speed buttons; capped at 25 the menu offers exactly 10 and 25. |
| TR-016 | Modal safety and buy affordability: spacebar/autoplay ignore blocking modals; affordability computed from the wrong tier cost | Consensus + owner observation 2026-07-26 | CONFIRMED | High | `fix/R8-modal-affordability` | **MERGED** | Both halves confirmed. **Modal safety.** `App.svelte`'s spacebar handler named six surfaces, and every blocking surface whose open state is COMPONENT-LOCAL was missing, because a local `let` in another component cannot be named from `App.svelte` at all: the buy confirm dialog, the FEATURES menu, the autoplay menu, the HUD menu, the session panel and the reality check. Spacebar span the reels underneath all six, and autoplay kept firing through a reality check, which exists precisely to be acknowledged. Fixed by inverting the dependency: surfaces REGISTER with `stores/modalGuard.ts`, so a new modal suppresses the spacebar by announcing itself rather than by being added to a list that cannot see it. Autoplay re-checks at FIRE time, not only at schedule time, and re-arms rather than cancelling so it resumes once the surface is dismissed. **Affordability.** `canBuyBonus` in locked `gameStore.ts` is `bal >= bet * 100` for every tier; at the 400x NITRO tier that enabled CONFIRM beside a correctly displayed 400x price (bet 1.00, balance 150: old gate true, real cost 400). Compensated today by the FEATURES menu's own guard running first, exactly as `LOCKED_FILE_DEBTS` recorded, but it is a money-path check one refactor from being live. New non-locked `stores/buyAffordability.ts` is now the single truth used by BOTH the card and the dialog, so they cannot disagree, and it also exposes `shortfallFor` so the UI can say why a tier is unavailable, which is the owner's observation that NITRO was 'not always selectable'. Locked file untouched. `modalGuard.test.ts` 25 assertions; runtime proof `reports/qa/modal_safety_proof_2026-07-25.json` shows SPACE spins with nothing open (control) and does NOT spin under the FEATURES menu or the buy dialog. New CI gate 8. |
| TR-017 | Scatter rules under-disclosed: scatter3/4/5 strings do not state free spins AND instant pay; no 6-plus scatter rule | Consensus | CONFIRMED | High | `fix/R9-scatter-disclosure` | **MERGED** | All 48 scatter strings (16 locales x 3) rewritten from `"3 SCATTERS - 1x MULTIPLIER"`, which **misdescribed the mechanic**, to `"3 SCATTERS: 8 FREE SPINS + 1x BET"`, disclosing both awards. Composed from **each locale's own already-shipped `freeSpins` and `bet` vocabulary**, never invented translations. Dash sweep: 35 instances inside string literals, now zero, enforced by the new `player_string_dash_check.mjs`. Social overrides added ("bet" to "play"). Social conformance re-run: ALL CHECKS PASS. |
| TR-017a | ~~6-plus scatters reachable and undisclosed~~ **RETRACTED** | Claimed by me, 2026-07-27 | **REFUTED, my own error** | n/a | `fix/R9-scatter-disclosure` | **CLOSED** | **I was wrong and the owner was right.** I reported 352 base rounds reaching 6 scatters and 12 reaching 7. The owner pushed back that 6 scatters is not a thing. It is not. The `reveal` event emits a **6-row board per reel**: the visible 5x4 grid plus one padding row above and below, used for the spin animation and never shown to a player. I counted all six rows. Re-measured across the same 100,000 rounds counting only the visible window (rows 1 to 4): **maximum 5 scatters, zero rounds at 6 or 7**. Distribution on the visible board: 3 scatters 11,461; 4 scatters 3,008; 5 scatters 731. The `rulesScatterSixPlus` disclosure added on the strength of the bad measurement has been **removed** from all 16 locales, the interface, the key union and the social overrides, because it documented a state a player cannot reach and would have invited exactly the question the owner asked. Lesson recorded: the engine's `scatter: true` flag appears on padding cells too, so it is not a safe proxy for what the player sees. Any future board-derived measurement must slice the visible window first. |
| TR-018 | 11 outstanding `svelte-check` errors; CI ratchets to a non-zero baseline | Review(s) + internal | CONFIRMED | Medium | `fix/R10-type-zero` | **MERGED** | **Baseline is ZERO and CI enforces it.** All 11 fixed, not tolerated, and three were single defects rather than noise: (a) `RainLayer.svelte` carried a literal style tag inside a script COMMENT, which `svelte.compile` respects but `svelte2tsx` does not, so it misparsed to EOF: 3 errors AND type-checking silently disabled for that whole component; (b) `telemetry.ts` used `Omit<Union, K>`, which is not distributive and collapses a union to its common properties, deleting every per-variant field from `track()`'s parameter type: 5 errors from one operator; (c) the last 3 were literal-union widening plus one dead markup branch left by Round 3 item 5. Gates after: typecheck 0/36 PASS, owner_audit_v4 PASS, currency + social PASS, dead wiring PASS, wallet floats PASS, scale drift PASS, fsModes drift PASS. |
| TR-019 | No session recovery: active round not hydrated from `authenticate.round`; no idempotent `endRound` retry | Consensus | **DUPLICATE of TR-035** | High | `fix/R11-session-recovery` | **CLOSED as duplicate** | Same finding, same branch. TR-035 carries the work and is MERGED: `stores/sessionRecovery.ts` hydrates `authenticate.round` and settles a `pending_end` round through the platform's own `endRound`, proven by `sessionRecovery.test.ts`'s 17 assertions, and the idempotent-retry half was closed separately inside the lock sanction as TR-008. **Row retained rather than deleted**, per this tracker's own dedup lesson: a de-duplicator that matches on id alone destroyed a real row once already, and a closed duplicate is auditable where a missing one is not. |
| TR-020 | Evidence hygiene: stale failing `audio_verify` JSON committed; Vite starter residue; favicon not the hero icon | Consensus | CONFIRMED | Medium | `fix/R12-evidence-hygiene` | **MERGED** (except TR-020a) | Favicon now the 32px hero icon, `vite.svg` deleted, zero starter residue. Stale audio JSON explicitly marked SUPERSEDED per convention (h) with a dated successor recording today's real state. Splash and buy-dialog items below. |
| TR-020a | `audio_verify`: `bedSwapFiredOnBonusBuy` and `bedRevertedAfterFeature` fail; `loopSeamsWithinTolerance` fails | Internal, carried since 2026-07-13 | CONFIRMED | Medium | `fix/audio-bed-swap` | **MERGED** | Resolved under ruling 23 by instrumenting first. `setOverdriveBed()` now carries a dev-only `__bedSwapTrace` counter, kept permanently as the assert. **The early-return theory (option b) is REFUTED and the crossfade is correct:** on a real bought feature the counters read `crossfadeToTension` 0 to 1 and `crossfadeToBase` 0 to 1, `earlyReturnMuted` 0, and `bgm_tension` genuinely plays. All three failures were harness artefacts, and both root causes are the same mistake. **(1) `mockCategory` is global, not per-spin.** `baseUrl` pinned `?mockCategory=base_win_small` and `App.svelte` re-reads `window.location.search` on every mock round, the buy included, so the buy called `serveCategory('bonus','base_win_small')`, which searches the bonus pool, found nothing, returned null, and presented no feature at all. The bed had nothing to swap for. Repointed to `bonus_win_mid` via `history.replaceState` (no reload, so the trace survives). **(2) The seam check never fetched any audio.** `runSeamChecks` built `${baseUrl}/assets/...` with the query string already attached, yielding `http://host:port?mockCategory=base_win_small/assets/.../bgm_loop.webm`; the dev server answered index.html and `decodeAudioData` failed on HTML for all six rows in both formats. Now built from a bare `origin`. Real measurements follow: `bgm_loop` delta 0.038 dB webm / 0.153 dB mp3, `bgm_tension` 0.224 / 0.020, `anticipation_build` 0.695 / 0.495, all inside tolerance. The assert also moved off `__playedSounds`, which records the preload burst (every clip, `bgm_tension` included) and so could read a preload as a crossfade. `AUDIO VERIFY: ALL CHECKS PASS`, exit 0, twice consecutively. Report: `reports/qa/audio_verify_2026-07-13.json`. |
| TR-020b | Buy-dialog RTP and max-win disclosure clipped at short viewport heights | Programme brief + measured | CONFIRMED | **High** | `fix/R12-evidence-hygiene` | **MERGED** | Measured, and broader than the brief assumed: **390x664 already passed**; the disclosure was out of view at **360x600** and at compact landscape **812x375**. The modal already scrolled, so it was reachable but below the fold, which a reviewer who does not scroll never sees. Per-mode cost, RTP and max win are a stated review requirement. Fixed by making `.buy-stats-row` sticky to the bottom of the modal's scroll box, so the disclosure is on screen at every height without reordering the owner-approved B4 layout. Verified in view at 390x844, 390x664, 360x600 and 812x375. |
| TR-021 | Mini-player popout: `IntroSplash` Continue button renders outside the viewport at 400x225 | R2 (MAJOR, though citing a non-existent file), internal since Round 3 | **CONFIRMED, FIXED** | High | `fix/R14-popout` | **MERGED** | **Reproduced by measurement before fixing.** At 400x225 the card was **399px tall in a 225px viewport**, top `-87` and bottom `312`, overflowing BOTH directions because `.intro-backdrop` centres with `align-items: center` and nothing capped the card height. The Continue button sat at 242 to 282, **entirely below the fold**. A player whose first session opened directly in mini-player mode could not dismiss the rules and could not proceed. 800x450 and 320x568 were unaffected. **Fixed** by capping the card to the viewport with scroll, pinning Continue sticky to the bottom of that scroll box (same pattern as the R12 buy-dialog disclosure), and compacting padding below 400px height so the common popout case fits outright rather than merely being reachable. After: card **209px** in the 225px viewport, Continue at 161 to 202, inside. **The decisive proof is a REAL Playwright click.** Playwright's out-of-viewport check is geometric and is NOT bypassed by `{force:true}`; only a DOM-level click evades it, which is exactly what the harness needed before. A genuine `.click()` now succeeds at 400x225 and dismisses the modal. **New permanent gate** `popout_conformance.mjs`, three viewports, asserting geometry, the 44px touch target, and a real click at each. It deliberately does NOT use the `clickViaDom` helper, since that helper exists to work around this very class and using it would hide what the gate is for. Note this also partially answers review 2's popout finding, which was otherwise unactionable because it cited a screenshot path that does not exist. |
| TR-022 | Splash shows only once per browser profile, so a returning player never sees the brand screen | Owner observation 2026-07-26 | CONFIRMED | Low | `fix/R12-evidence-hygiene` | **MERGED** | `introSeen()` read **localStorage first**, making the flag effectively permanent per browser profile: exactly why the owner had to use incognito to see the splash again and never saw it on desktop. Now **sessionStorage only**, so it returns on every cold load and is still skipped for the rest of the session. Any legacy localStorage flag is actively **cleared on first read**, otherwise every existing player would stay permanently opted out and the fix would be invisible to precisely the people who hit the bug. |

| TR-023 | All five event books absent from the repository | R1 (BLOCKER) | **RESOLVED BY DESIGN** (Fable ruling 2026-07-28) | was Blocker | `fix/R3-books-manifest` | **MERGED** | **Git LFS REJECTED and the books never enter the public repository.** Three reasons on the record: LFS burns bandwidth quota on every clone of a public repo including Fable's daily verification clones; the platform receives books by **ACP upload, not GitHub**, so committing satisfies no submission requirement; and decisively, **the books ARE the complete outcome distribution of all five modes**, so publishing them pre-release hands the entire product to anyone who clones. A fourth mechanical point makes the alternative impossible regardless: `books_bonus` (144.9 MB) and `books_super` (142.4 MB) exceed GitHub's hard 100 MB per-file limit. **Delivered instead:** `BOOKS_MANIFEST.md` in the repo root as the public fingerprint of the private set (per-book SHA-256, byte size, row count, provenance), and `SUBMISSION_DOSSIER.md` 5c reworded to define the two artefact sets explicitly with the manifest as the bridge. **Verified while building it:** all five books hold exactly 100,000 rows, matching their lookup tables; the mode set matches `index.json`; and **all five SHA-256 values are byte-identical to those already in dossier 5c**, so the dossier's hash record was always correct and only its wording was defective. Row-by-row payout equality remains R3's work and is stated as not-yet-proven in the manifest. |
| TR-024 | Dossier claims eleven files present while the repository holds seven | R1 (BLOCKER, same root) | **CONFIRMED, FIXED** | was Blocker | `fix/R3-books-manifest` | **MERGED** | Section 5c reworded with a two-set table and an explicit heading warning that the sets differ. The old wording was true of the build machine and false of the repository, which is exactly how three reviewers cloned it and one raised a BLOCKER. The artefacts were never wrong; the sentence was. |
| TR-025 | `frontend/dist` committed to the repository | R1 | CONFIRMED | Minor | `chore/pre-review` | **MERGED** | Removed from tracking and added to `.gitignore`. The ruling recorded with it: the build is reproducible via `npm run build` and size-gated by `build_diet_verify.mjs`, so committing its output adds no verifiable evidence, and a two-file `dist` invites a reviewer to conclude the shipped bundle is two files. The bundle's real size is asserted by the gate, not by an artefact in the tree. |
| TR-026 | Antelite tail concentration: top 1% of weighted mass supplies ~75.4% of RTP | R1 (observation) | **ACCEPTED BY DESIGN** (Fable ruling 2026-07-28) | Minor | n/a | **CLOSED** | No maths change. It passes every platform gate (ETL 0.6654 against 0.9 at 3-star, 0.8 at 2-star); **the tail weighting IS the OVERBOOST product promise**, since an ante paid for boosted feature access rides the feature tail by construction; post-approval lockdown would make any change permanent in the wrong direction; and the player-facing disclosures already carry the truth. Recorded in `COMPLIANCE_WATCH.md` 2026-07-28 rather than the PAR sheet, because the PAR is inside the `games/future_spinner/**` deny rules and the ruling named no deny lines to lift. **Owner decision 2026-07-28: `COMPLIANCE_WATCH.md` is the permanent location. No lock lift sought** - unlocking the shipped maths package to relocate an existing, dated, findable note is more risk than the tidiness is worth. **PAR POINTER ADDED 2026-07-25** under a sanctioned lock exception (JOB 4d): section 11 of `FUTURE_SPINNER_PAR_SHEET.md` now carries one line pointing at `COMPLIANCE_WATCH.md` for antelite operational detail. This closes the narrow question left open when the PAR was parked: the analysis stays in `COMPLIANCE_WATCH.md`, which remains its permanent home, but the artefact a regulator opens first now says where to look. Proof recorded: exactly one file changed with one insertion, all 188 files under `games/future_spinner/library/` byte-identical before and after, and `git diff .claude/settings.json` 0 bytes with the settings SHA-256 unchanged at `a0c8f149...`. |
| TR-033 | Scatter anticipation: only the final reel held, and the near-miss branch read the final board | Owner audit round 4 item 7, built under the integrity ruling | CONFIRMED | Major for score | `feature/scatter-anticipation` | **MERGED** | Built to `docs/design/SCATTER_ANTICIPATION_SHIP_SPEC.md`. **The ruling dissolved the proposal's hardest question** rather than answering it: escalation is a function of scatters VISIBLY LANDED plus whether reels are still moving, so a level-3 build states "three are down and reels are still turning", which is true 100% of the time. It never promises a fourth, so there is no lie to soften and no reason to read the board. `_checkAnticipation(finalBoard)` deleted: it fabricated a near-miss from a decided outcome and was turbo-gated so fast play never saw it. **Measured from 40,000 shipped rounds before designing:** anticipation opens in 23.18% of base rounds, 64.5% of those convert, 24.0% of triggers go past three, the third scatter lands on the final reel 46.2% of the time, and **0.5% land two scatters on one reel** - so levels are computed from state, never incremented, because an incrementing ladder is wrong on one round in two hundred. 44 assertions including replays of real book rounds. **OWNER SIGN-OFF, 2026-07-25**, reviewed live at `?anticipationDemo=5`: "this seems to be the cleaner look... I saw the flames. That looks really good. I like how that's tied in." Recorded as evidence because two parts of it are not things a gate can establish. **(1) The flame gauge tie-in is an owner eye-call** and it was the owner's own idea, so acceptance of the escalation-to-height mapping closes proposal open question 3. **(2) "It seems to spin well, even when you go through the different turbos" is a human confirmation of turbo-shortens-never-skips.** The automated gate proves only the arithmetic, that every hold stays at or above 300ms and every pulse at or above 180ms at Super Turbo; whether a beat compressed to its floor still READS is a judgement no assertion can make, and it has now been made by eye across all three tiers. **HOLD LENGTHS RULED, 2026-07-25.** The builder raised the fixed 1000ms level-3 hold as a possible drag, since 76% of triggers stop at three. Owner ruled it stands: "you still want to see if you're getting the four or the five... people don't mind waiting that additional time... **it's when it's dead spins that people just want it to go quicker**." The general principle, recorded in the ship spec so it is not re-litigated: **attention is earned by the round.** The ladder already implements it, since 0 or 1 scatter opens nothing at all and runs at full speed. Explicitly NOT licensed: shortening a hold because the outcome is known to be poor, which is the lookahead the integrity ruling forbids. Dead spins are fast because they never open the ladder, a property of visible state rather than of the outcome. |
| TR-034 | **The whole anticipation system was unreachable in the shipped configuration** | Found 2026-07-25 while proving TR-033 | **CONFIRMED, pre-existing** | Major for score | `feature/scatter-anticipation` | **MERGED** | `reelMode` ships as `'drop'` (owner eye-call, 2026-07-07) and `_spinSequence()` returns early on the drop path, before any anticipation code. The final-reel hold, the tremble, the sparks and the neighbour dim therefore ran **only** in `'strip'`, which a player reaches solely through a dev URL parameter. On the evidence this is the most likely reason review 1 called the animation "serviceable, not exceptional": the best of it was never on screen. `_dropAll` now releases reels one at a time with the stagger awaited between them, visually identical when no anticipation fires and leaving a seam to hold open when it does. `_scatterAnticipation` and `_chargeLandedScatters` also had strip-only guards (`reels[].state !== 'rest'`) that excluded every reel in drop mode; both are now mode-agnostic. |
| TR-036 | Does the escalation run for a retrigger inside free spins? | Proposal section 5 item 3, ruling silent; **R2R1 F-04 (MAJOR)**, **R2R1 F-07 (MINOR)** | **RATIFIED option (b), and BUILT** | Low | `main` | **MERGED** | **Built whole, R2R-R JOB E, 2026-07-26.** The same ladder, capped at level 3: levels 0 to 3 behave exactly as in the base game and 4, 5 and 6 collapse onto 3. A SUSTAINED level above the cap clamps to 3, because a hold has to be some value while reels are still turning; a PULSE above the cap becomes NOTHING rather than a clamp, because clamping 4 to 3 would turn a one-shot beat into a sustained level and clamping it to 2 would fire SECURED twice in one spin. **It is wired where the player can see it.** The free-spins overlay renders its own board and GameGrid's reel-stop loop is behind it and not running, so wiring the ladder into GameGrid would have produced a beat nobody could see. The ladder runs in `FreeSpinsPresentation`, which reveals a retriggering spin reel by reel so "visibly landed" means something inside the overlay, and `App.svelte` lifts the flame jets above the overlay for the duration of the beat. The integrity property is untouched: `escalationFor` is still fed only landed scatters and reels still to come, and still cannot be passed a board. **R2R1 F-07's disclosure half is closed too**: the paytable now carries a line explaining that the build is shorter on a retrigger because the feature is already secured, in all sixteen locales, routed through the JOB 6 vocabulary layer. Held by gate 10c, which gained 22 assertions including that nothing exceeds the cap across the whole state space, that the base game is untouched, and that the component wiring exists rather than only the store function. | The ship spec covers the base-game trigger only. The stakes genuinely differ once the bonus is secured, and the proposal's own instinct was "no, or heavily reduced". **Options:** (a) no escalation on retrigger, simplest and keeps the gauge meaning "how good is this entry"; (b) a reduced ladder capped at level 3, acknowledging the +5 spins without competing with the feature's own presentation; (c) the full ladder, on the basis that a retrigger is genuinely exciting and consistency is its own virtue. **Recommend (b).** |
| TR-027 | Art direction: assembled rather than unified (R1) versus high end for a debut (R3) | R1 (MARGINAL), R3 (positive) | **RULED: R1's bar governs** (Fable as art director, 2026-07-28) | Major for score | `feature/cohesion-pass` | **MERGED** | Both reviewers saw truly; they applied different bars, and for a three-star target review 1's bar governs. The answer is a **targeted cohesion pass, not a rebuild**, scheduled with the final polish phase after Wave 3: (a) integrate the character layer into the cinematic backdrop with atmosphere, depth haze and scene-matched colour grading so the flat art sits IN the scene, extending the existing rim-light work; (b) one global grade unifying backdrop, frame, symbols and celebrations to a single palette temperature; (c) the animation half is already answered by `feature/scatter-anticipation`. Sequenced alongside it. **PROGRESS 2026-07-25.** (b) the global grade is BUILT and works, applied as a blended overlay at z40 rather than a filter, deliberately: `.tile-inner` already carries the anticipation filter chain and a second filter would multiply rather than blend. (a) the depth haze **does not work and will not**: even at maximum it barely moves, because the pilot and car are flat vector illustrations against a rendered backdrop, which is a style mismatch rather than an atmosphere deficit. The real lever is the art itself. **Owner-approved enhanced character art**, reviewed in situ: "I really like the hero... the 3D is popping". Converted to a drop-in 680x1344 RGBA sprite by `frontend/scripts/greenscreen_to_sprite.mjs`; subject bounding boxes match the original to 0.7%, and the despill was tuned because the green key sits exactly where the new rim light does. **Staged, NOT adopted**: `CLAUDE.md` prohibits externally sourced art, so adoption needs a ruling and an explicit amendment rather than a silent exception. Car art pending and may be dropped; the owner's read is that the hero carries it. **CLOSED 2026-07-25.** Resolved by improving the ART, not by more CSS. The enhanced character and car are ADOPTED under an owner ruling amending the assets convention: external ENHANCEMENT of art we already own is permitted, externally DESIGNED art is not, and symbols are never externally designed. Owner chose the UNGRADED look, so the global grade ships at `neutral` and remains available as `?grade=warm|cool|deep` for future comparison. The depth haze is recorded as ineffective and is not the lever. **Verified, not assumed:** all scene motion is CSS on the layer rather than baked into the sprite, confirmed live (`.char-layer` transform animating translateY -6.95 to -0.10, scale 1.0148 to 1.0002, plus sway); `.antenna-light` and `.visor-glint` are percentage-positioned so the 0.7% subject match keeps them aligned, confirmed by capture; the anticipation proof still passes unchanged with the new assets; portrait correctly excludes the scene group by design. Owner reviewed the layered visor highlight (baked specular plus the 6s CSS sweep) and accepted it. |
| TR-028 | Animation quality "serviceable, not exceptional"; several captures begin in dialogs or artificial test states | R1 (MARGINAL) | CONFIRMED as evidence weakness | Major for score | `chore/pre-review` | **MERGED** | **The capture half is closed.** All seven groups regenerated 2026-07-25 with the route recorded per group in a `PROVENANCE.md` beside the captures: layouts (223d1c4, six viewports, zero open dialogs asserted in the run), feature flow (0944a5f, a **natural** base-game trigger, no forced category and no injected board), rules and paytable (0a7674c, opened by clicking as a player does), buy dialogs (a288bfe, including the affordability boundary that proves R8), celebrations (9ab4cf2, ordinary win played for and the higher tiers served from **curated shipped-book rounds** rather than by faking `winAmount`), menus (a695203, each panel opened by clicking), brand (4308447). Inventory at `reports/screens/EVIDENCE_INVENTORY.md`. **The animation half was closed separately** by `feature/scatter-anticipation`, merged and owner-signed-off. |
| TR-029 | Evidence filenames carry dates ahead of their commit | R1 | CONFIRMED | Minor | `chore/pre-review` | **MERGED for new evidence; historical files deliberately unchanged** | Every artefact produced by this pass carries the true commit date, 2026-07-25. The pre-existing future-dated files under `reports/qa/` are **left exactly as they are**, per the standing ruling that commit dates are authoritative and nothing is relabelled retroactively: renaming them would falsify the record of what each file was called when it was written. They are listed by name in `reports/screens/EVIDENCE_INVENTORY.md` so the discrepancy is accounted for rather than ambient, which is the same treatment convention (h) gives a superseded artefact. |
| TR-030 | Buy dialog drops the "what you get" and RTP/max-win detail at short viewport heights | R1 (MARGINAL) | **CONFIRMED, and FIXED** | High | `fix/R12-evidence-hygiene` | **MERGED (PR #93)** | Review 1 independently found what R12 measured: at 390x664 the buy dialog showed less information than the taller layout. Our measurement went further, finding the disclosure fully out of view at 360x600 and at compact landscape 812x375. Fixed by making `.buy-stats-row` sticky; verified in view at all four heights. Per-mode cost, RTP and max win are a stated review requirement. |
| TR-037 | Paytable Bet Modes cards TRUNCATE the max-win value to `5,000x ba...` on every mode | Found by the 2026-07-25 evidence regeneration | CONFIRMED | Major, platform requirement | `chore/pre-review` | **MERGED** | Fixed and verified by re-capture, not by assumption. **The first attempt only moved the truncation**: putting the qualifier in the stat label rendered the value in full at last and then clipped the label to `MAX WIN (BASE BE`. A four-column stat row has no width for a qualified label, and repeating the same six words in five columns was never the right shape. Resolved by keeping the label short and putting the qualifier once, as a footnote under the grid, where it cannot clip. All five cards now read COST / RTP / MAX WIN with every value complete (`1x`, `96.35%`, `5,000x`), and the footnote reads "Max win is quoted against the base bet", with a social variant routed through the same function so the pair cannot drift. Evidence: `reports/screens/rules-paytable-2026-07-25/06-bet-modes.png`, regenerated after the fix. Gates: TYPECHECK BASELINE and the fsModes drift gate both PASS. |
| TR-031 | Provider mark: delivery naming does not match convention and the 48px proof is poorly legible | R1 (MARGINAL); **R2R1 F-02 (MAJOR)**, **R2R1 F-08 (POLISH)**, **R2R3 finding 8 (MAJOR)** | CONFIRMED | Major | `main` | **MERGED. Candidate f ADOPTED 2026-07-26; only the one-time portal upload remains, and that is the owner's** | **The owner chose f** ("Go with F"). `design-system/brand/delivery/WeRollSpinners-Logo.png` is now candidate f at its **native 1024 master**, SHA-256 `66c692da...`, byte-identical to the master because the master is a full-frame crop of the owner's file at its own resolution: the submitted asset has been through **no resampling at all**. **A contradiction in the adopting brief is recorded rather than resolved silently**: it opens with "Go with F" and then names `provider_mark_e-owner-supplied-transparent` in its JOB 1 body. "Go with F" was taken as the decision, being unambiguous, standalone and a direct answer to the question the previous session asked; the JOB 1 filename reads as carried over from when e-transparent led. Reversing it is one path in `frontend/scripts/tile_delivery_build.mjs` plus a re-run. **Why f was the right answer on the evidence too**: it closes both questions e left open, and closes them by its own construction rather than by anything done to the art. It carries a real alpha channel, so the platform's transparent-background rule is met by the artwork rather than by a keying step we performed on it, and it has no wordmark, so e's measured 0.81 px per stroke at 48px cannot arise. Exactly three colours, `#00FFFF` and `#FF00FF` verbatim from the brand palette plus `#0A0A14`, and at 48px it is the most legible candidate produced: the 777 reads as three digits where d's outlined windows and e's smaller 7s go to texture. a, b, c, d and e are superseded and **kept**, and stay in the seven-way comparison sheet. **Nothing in this row is a code or asset task any longer.** The provider logo is a one-time square upload in Team Settings Branding; DTT_PROTOCOL.md carries it as an owner action. Previously: **candidates e and f delivered, awaiting the owner eye-call** | **Candidate f, 2026-07-26, the owner's second supplied file, closes both questions candidate e left open**, and closes them by its own construction rather than by anything done to it: it carries a REAL ALPHA CHANNEL, so the platform's transparent-background requirement is met by the file rather than by a keying step we performed on the owner's art, and it has NO WORDMARK, so e's measured 0.81 px per stroke at 48px does not arise. Source SHA-256 `61a2258d...`, 1024x1024 RGBA, centring skew +1 px on both axes. It uses **exactly three colours**: `#00FFFF` and `#FF00FF` verbatim from the brand palette plus `#0A0A14` structural near-black. **At 48px it is the most legible candidate produced**: the 777 reads as three digits where d's outlined windows and e's smaller 7s do not. **Two things recorded rather than corrected**, since correcting either would be redrawing the owner's art inside an ingest: the structural near-black is OPAQUE, so on a dark portal surface it sits at very low contrast and the lines holding the mark together lose definition, rendered at true size over four surfaces at `reports/screens/provider-mark/candidate-f-on-surfaces.png`; and the alpha is HARD-EDGED with **zero partially transparent pixels**, so the source has no antialiasing and the exports rely on a high-quality downscale to supply it, with the outer silhouette also deviating 5.1% from a true circle. Previously: **candidate e delivered (owner-supplied), awaiting the owner eye-call; a to d superseded as non-preferred** | **2026-07-26: the owner commissioned a purpose-made mark externally and supplied it.** Ingested as candidate **e** through the same deterministic canvas pipeline every other candidate used, by `frontend/scripts/provider_mark_ingest_e.mjs`, which measures and reports rather than retouches: source SHA-256 `fba98ff4...`, 1254x1254 8-bit RGB with **no alpha channel**, ink bounding box and four-sided margins recorded, centring skew measured at **0 px on both axes** so squaring was a 1 px correction rather than a recomposition. Externally generated art is permitted here: CLAUDE.md bars externally DESIGNED art and never externally designed SYMBOLS, and a provider logo is neither a symbol nor an animation-pipeline asset; the obligation it does set, recording provenance, is met by `PROVENANCE_e.md`. **TWO FORMS are committed because the artwork and the delivery requirement disagree**: the mark on its designed dark rounded-corner field, and the same mark with the field keyed out at full resolution before downscaling, because the platform states the provider logo wants "a transparent background" (`game-tile-requirements.md:36`). The rounded corners are the evidence that the field is part of the design, so the choice is genuinely the owner's and is not made here. **The text ring was measured, not assumed**: **0.81 px per stroke at 48px**, below one whole pixel, which is the same structural finding this row recorded against the original master and the reason candidate d dropped its text. That is not an argument against e, whose ring, windows and 7s read cleanly at 48 where no earlier candidate did; it is the fact the owner needs in order to choose. Evidence: the six-way sheet at `reports/screens/provider-mark/48px-legibility-comparison.png` and a true-size strip at `reports/screens/provider-mark/candidate-e-true-size.png` showing actual 512, 96 and 48 with no upscaling. **NOT ADOPTED, and the delivery set is deliberately unchanged**: `WeRollSpinners-Logo.png` still carries candidate d until the owner confirms. Previously: **purpose-drawn mark delivered (candidate d), owner eye-call open across four** | **R2R-R JOB D, 2026-07-26: the purpose-drawn mark this row asked for exists.** Built to Fable's spec by `frontend/scripts/provider_mark_build.mjs`, which ASSERTS every line of that spec and fails the build rather than documenting a miss: 48-unit viewBox drawn first and scaled up, two concentric rings at stroke exactly 3, three rounded-rectangle reel windows carrying 7 glyphs at exactly 10px, no `<text>` element anywhere, and exactly the two brand colours checked against the generated markup rather than against intent. The window geometry FAILED its own assertion on the first attempt (corners at 15.50 against a 14.83 limit, so they would have crossed the inner ring) and the windows were narrowed rather than the assertion loosened. No font is involved at all: the 7s are paths, so rasterising is deterministic across machines. Provenance and hashes at `design-system/brand/provider_mark/PROVENANCE_d.md`. The refreshed sheet at `reports/screens/provider-mark/48px-legibility-comparison.png` is now FOUR-way and shows the true 48px beside each 8x blow-up with smoothing off, so it cannot flatter any candidate. **Still no adoption: the eye-call is the owner's.** Previously: three deterministic derivations from the committed master, re-runnable to identical bytes by `frontend/scripts/provider_mark_derive.mjs`, exported at 512, 96 and 48 with delivery-style names under `design-system/brand/provider_mark/`. Side-by-side 48px comparison at `reports/screens/provider-mark/48px-legibility-comparison.png`. **The cause is structural, not a sharpening problem:** the master carries arched WE ROLL / SPINNERS text around a detailed chrome wheel, and at 48px that text is about two pixels per stroke, so it cannot resolve at any contrast. Candidates b and c drop the text ring and keep the wheel and reels; both are markedly more legible than the control. **Stated plainly: none is ideal.** A purpose-drawn mark built simple at small size would beat all three; these are the best available derivation from the committed master, not the best possible mark. No adoption in this pass, the eye-call is the owner's. |
| TR-032 | Review 2 cites `frontend/src/locales/sweeps_en.json`, `frontend/src/index.html` google-fonts link, `Controls.svelte`, `RulesModal.svelte`, `soundManager.ts`, `rgsApi.ts`, `i18n.ts`, `Board.svelte`, `reports/screens/mini_player_preview.png` | R2 | **HALLUCINATED** | n/a | n/a | CLOSED | **None of these paths exist in the repository.** Verified individually: zero `sweeps*` files; zero `fonts.googleapis`/`fonts.gstatic` hits in source, in `index.html`, across the whole `frontend` tree, or in the built `dist` bundle; `mini_player_preview.png` absent. Our actual files are `HudOverlay.svelte`, `PaytableModal.svelte`, `soundService.ts`, `rgsService.ts`, `translations.ts`, `GameGrid.svelte`. Review 2's two BLOCKERs and its MAJOR all rest on files that do not exist, so its findings cannot be actioned and its verdicts carry no weight against this build. Recorded in full rather than discarded, because the source document is retained verbatim and a future reader must know why it was set aside. |
| TR-033 | Review 2 finding 4: super-mode cap frequency discrepancy, claims 106 cap outcomes | R2 (MINOR) | **REFUTED** | n/a | n/a | CLOSED | Recomputed from the shipped table: `super` P(>=5000x) = **4.000000e-03 = exactly 1 in 250.0** (`reports/qa/bet_level_compliance_raw_2026-07-25.json`). Review 1 independently computes **1 / 250.000** and review 3 records the same. Three independent agreements against review 2's outlier. Review 2 also states a cross-mode RTP delta of 0.061% (bonus 96.382%, cruise 96.321%); reviews 1 and 3 and our own computation all find the spread **0.0000pp** with every mode at 96.35%. Review 2's arithmetic is not reproducible. |
| TR-034 | Review 2 finding 5: win count-up audio 450ms shorter than the 4,000ms rollup | R2 (POLISH) | **HALLUCINATED** | n/a | n/a | CLOSED | Cites `sfx_win_countup.mp3` and durations in files that do not exist under those paths. No such asset name is in the shipped sound set. |

### Round two rows (ingested 2026-07-25)

Sources are `sources/round2_review1.md` (score 2.00), `sources/round2_review2.md` (1.67)
and `sources/round2_review3.md` (0.67), all reviewing HEAD `78d02cc`, SHA-256 recorded in
`sources/README.md`. They are referenced below as **R2R1**, **R2R2** and **R2R3** so they
cannot be confused with round one's R1, R2 and R3.

Dispositions marked *(Fable, verified at git level)* were established by Fable first-hand
and are recorded, not re-litigated, per the round two brief. Where this session could
confirm the same fact cheaply and independently it did so, and says which check it ran;
that is corroboration, not a second opinion, and convention (l.4) applies to it: the
independent checks below read the shipped files directly, not Fable's summary of them.

Four round two findings map onto rows that already exist. Per the tracker's own rule they
gained a source reference rather than a new row: **TR-012c** (R2R1 F-01, R2R3 finding 7),
**TR-031** (R2R1 F-02, R2R1 F-08, R2R3 finding 8), **TR-035b** (R2R1 F-03) and **TR-036**
(R2R1 F-04, R2R1 F-07).

| ID | Finding | Source | Disposition | Severity | Branch | Status | Fix evidence |
|---|---|---|---|---|---|---|---|
| TR-038 | Launch reads only the `session` URL parameter; the official launch sends `sessionID`, so the live guard disables betting on a correct launch | R2R3 finding 1 (BLOCKER), first half | **CONFIRMED** *(Fable, verified at git level)* | Blocker | `main` | **MERGED (`37c8c1f`)** | `App.svelte` lines 676 and 692 at `78d02cc` read `params.get('session')` only, while `parseSessionParams` in `stores/launchParams.ts` already resolved `sessionID` first. The token fell through to `dev-mock-token`, `authenticate` failed, and the R2 live guard then correctly refused to take a bet: dead on arrival on an official URL, perfect on every dev one, which is why it survived. Both reads now take `sessionID` with `session` as a legacy fallback, in `parseSessionParams` order. `launchParams.test.ts` pins the order and deliberately also asserts the PRE-FIX behaviour, so a refactor cannot quietly reinstate it. CI gate 10d. |
| TR-039 | `rgsService.ts` models flat authenticate, play and end-round responses; the pinned official client defines nested ones, and `rgs_url` host normalisation, play events, currency, jurisdiction flags, round identity and end-round semantics are all incompatible | R2R3 finding 1 (BLOCKER), second half | **CONFIRMED** *(Fable, verified at git level)* | Blocker | `fix/R2R-wallet-contract` | **MERGED (`c35fac8`, PR #114)** | Approved by Fable after line-by-line review and a clean test merge; merged with a merge commit rather than a squash so `da4826f`'s lock proof survives in history. Re-run on merged `main`: contract, responsible gambling, session recovery and parse suites all PASS, svelte-check 0 errors 36 warnings. The pinned reference is `stake-engine: github:StakeEngine/ts-client`, types at the pinned ref, which defines `balance {amount, currency}`, `config {minBet, maxBet, stepBet, defaultBetLevel, betLevels}`, `jurisdictionFlags` with twelve named flags, `round {betID, active, mode, state}`, `PlayResponse {balance, round}` and `EndRoundResponse {balance}` with no `roundId`. The docs mirror independently corroborates `authenticate`, `config` and `minStep`, which matters under convention (l.4): the mirror and the package are separate inputs, so their agreement is real corroboration rather than two readings of one file. Lands as an isolated PR under the JOB 4 lock sanction, held for Fable's line-by-line review per the PR #103 precedent, **not self-merged**. |
| TR-040 | Replay routes only `freeSpinTrigger` rounds through `roundInterpreter` and searches ordinary rounds for `board`, `win` and `scatter` events that occur zero times in the shipped books | R2R3 finding 2 (BLOCKER) | **CONFIRMED** *(Fable, verified at git level)* | Blocker | `main` | **MERGED (`3319529`)** | Independently located this session: `ReplayMode.svelte:121` gates the canonical interpreter on `ev.type === 'freeSpinTrigger'`, and lines 144, 150 and 159 then search for `'board'`, `'win'` and `'scatter'`. Those three event types are the same dead schema that `rgsService.ts` carried before PR #103 removed it, recorded in CLAUDE.md's LOCKED_FILE_DEBTS as 0, 0 and 0 occurrences across the first 300 rounds of `books_base.jsonl.zst` against `reveal` 724 and `winInfo` 499. Normal wins and losses therefore replay as an empty or static board. Mandatory replay is a platform requirement, so this is correctly filed as a blocker. |
| TR-041 | stake.us social wording still contains prohibited terms across the paytable rules, the interface guide and the win banner, and replay's first paint can show the real-money disclaimer before mount | R2R3 finding 3 (BLOCKER) | **CONFIRMED at minimum for the three named surfaces** *(Fable, verified at git level)* | Blocker | `main` | **MERGED**, and the new scan found one more | Independently located this session: `PaytableModal.svelte:44` and `:52` carry "Malfunctions void all pays and plays." in both rules blocks; `:98`, `:99`, `:100` and `:108` hardcode "Increase Bet", "Decrease Bet", "buy the feature" and "Max Bet" in the interface guide; `WinBanner.svelte:248` renders `{multLabel} BET` unconditionally. R2R3 also observes that the existing conformance script inspects mode-card text only and checks only "Buy" and "Debits", so its PASS does not cover the surface it appears to cover. The fix is one social-aware vocabulary function plus a full rendered-DOM scan in both modes, not per-string patching. |
| TR-042 | The responsible-gambling store consumes invented jurisdiction flag names and ignores several real ones; the tests pass because they inject the same non-platform properties | R2R3 finding 4 (MAJOR) | **CONFIRMED** *(Fable, verified at git level)* | Major | `fix/R2R-wallet-contract` | **MERGED (`c35fac8`, PR #114)** | `responsibleGambling.ts` lines 19 to 38 declare and read `minSpinMs`, `realityCheckMs`, `maxAutoplaySpins` and `mandatorySessionDisplay`, none of which exists in the official `JurisdictionFlags`, and it reads none of `minimumRoundDuration`, `disabledSpacebar`, `disabledSlamstop`, `displayRTP` or `displaySessionTimer`. A test that injects the property the code invented proves only that the code reads its own invention. Typing the store from the transcribed official flags is part of JOB 4 because the flags arrive through `authenticate`. |
| TR-043 | The committed 400x225 mini-player proof shows an unusable active HUD, and the popout gate proves only that the rules modal's Continue button can be clicked | R2R3 finding 5 (MAJOR); R1 (round one) popout gate | **CONFIRMED, and CLOSED** | Major | `main` | **MERGED** | **A dedicated 400x225 profile, not the compact strip scaled down.** The popout previously fell into the compact-landscape layout: a 76px strip carrying seven controls, which is a third of a 225px viewport before a control is placed and about 50px each across 400px. The mini profile is a 44px row with FOUR controls; turbo, AUTO and MAX are not dropped but MOVE into the menu at full menu-item size, which they never had as crammed icons. Stats read inline rather than stacked, which is what was overlapping. The breakpoint requires BOTH dimensions, so a 400px-wide portrait phone does not get it. **Legibility and operability are measured, not judged from a picture**: pairwise overlap across every control, 44px targets including pseudo-element extensions, and no truncated stat value. Three defects were caught by those measurements and fixed before commit, the worst being that the first run captured a UI that had never spun, because without launch parameters the live guard correctly blocks betting; the proof now runs a real session and asserts the balance moved. Five states committed at `reports/screens/mini-player-2026-07-26/`, superseding the popout capture group per the partial-progress rule. | Recorded exactly as Fable framed it: this is R2R3's reading of the committed capture `reports/screens/layouts-2026-07-25/popout-400x225.png` set against round one R1's modal-dismissal-only gate. The two are not in conflict; the gate simply never claimed to prove an operable HUD, and the capture shows balance and bet compressed into overlapping fragments with an unlabelled feature control. The answer is a dedicated 400x225 layout profile rather than a scaled desktop HUD, with idle, spinning, result, feature and modal proofs replacing the current capture group in `EVIDENCE_INVENTORY.md` under the partial-progress rule. |
| TR-044 | `tools/verify_books_lookup_equality.py` prints PASS with no books present: zero rounds, zero assertions, five `zstd: can't stat` errors | R2R3 finding 6 (MAJOR) | **CONFIRMED by execution** *(Fable)* | Major | `main` | **MERGED (`d6d921c`)** | The books are gitignored (`.gitignore:9`, `**/library/**`), so a reviewer working from the GitHub tree has no `books_*.jsonl.zst` at all and gets exactly this result. That is what makes it serious rather than theoretical: the committed 4.45-million-assertion report cannot be reproduced by anyone outside this machine, and the tool as written would report PASS to them. The fix is to fail closed on missing input, non-zero decompressor exit, zero rounds, duplicate or missing IDs and any row count that is not exactly the lookup's, with a self-test that runs the tool against absent input and asserts a non-zero exit. |
| TR-045 | Submission tile delivery names do not follow the published convention, and `SUBMISSION_DOSSIER.md` still records tile and provider work as pending | R2R3 finding 8 (MAJOR), tile half | **CONFIRMED, and the naming half is CLOSED** | Major | `main` | **MERGED for the delivery set; the dossier reconciliation is TR-047 and remains open** | `design-system/brand/delivery/` now holds `FutureSpinner-BG.jpg`, `FutureSpinner-FG.png` and `WeRollSpinners-Logo.png`, which are the platform's own three rules quoted verbatim from the dated mirror (`game-tile-requirements.md` lines 29, 33 and 37). Built by `frontend/scripts/tile_delivery_build.mjs`, which also CHECKS the 3 MB background-plus-foreground ceiling rather than assuming it: 796 KB of 3072 KB, with the provider logo correctly excluded from that sum because the platform's sentence names only the two. **They are copies, not in-place renames**, because two committed GENERATION_NOTE files describe the masters by their existing names and renaming would orphan that provenance to save a duplicate of a file we already hold; `delivery/` is the submission set and `tile/` stays the working set. | Split from TR-031 deliberately. TR-031 is the provider mark's legibility and adoption; this row is the delivery naming of the tile exports and the dossier's own contradiction at lines 29 to 31 and 164. JOB 9 renames the tile exports to the platform convention in the same pass that delivers the purpose-drawn mark, so the two land together rather than leaving a half-renamed asset set. |
| TR-046 | `npm run check` fails and CI runs only the Svelte baseline, so TR-018's "baseline is zero and CI enforces it" is incomplete | R2R3 finding 9 (MINOR) | **CONFIRMED, reproduced exactly** | Minor | `main` | **MERGED** | **Fixed and gated, R2R-R JOB F.** The TS7006 was one untyped parameter, `dirSize(absPath)` in `vite.config.ts`; annotated, and `npm run check` now exits 0 with 0 errors. CI gains gate 1a, which runs the COMPLETE command rather than `typecheck_baseline.mjs`'s svelte-check half, and gate 1b, a production build. The build gate matters beyond the finding: it catches broken import chains and plugin failures no type-check sees, and every Playwright proof that serves `dist` depends on it existing. Originally reproduced at the previous HEAD, and it matches R2R3 figure for figure: `COMPLETED 491 FILES 0 ERRORS 36 WARNINGS 5 FILES_WITH_PROBLEMS`, then `vite.config.ts(33,18): error TS7006: Parameter 'absPath' implicitly has an 'any' type.` The warnings R2R3 singles out are also there: `SessionPanel.svelte:93` carries `role="dialog"` without a tabindex, and the bulk of the rest are unused animation selectors in `GameGrid.svelte` and `HudOverlay.svelte`. JOB 11 makes the complete command green and runs it, plus a production build, in CI beside the Svelte baseline. |
| TR-047 | Evidence and source-of-truth documents remain internally inconsistent: the dossier treats tile assets as both pending and complete, the PAR still allows six-plus visible scatters, the bundle-size measurement is stale | R2R3 finding 10 (MINOR) | **CONFIRMED in part, one component blocked** | Minor | unassigned | **MERGED, all three components** | **All three components are closed.** **(1) The dossier**: rows 5, 6 and 7 read DELIVERED rather than "To design", the staging step that called the tile assets "still design-pending" now names the three delivered files and points at the delivery README and DTT_PROTOCOL.md, and a grep for "To design" or "design-pending" against tile or provider work returns nothing. **(2) The bundle-size figure**: re-measured from a fresh production build at **21.87MB** against the 25MB budget, superseding the 13.59MB recorded on 2026-07-13. **The drift has a specific cause and it is a finding in its own right**: 7.06MB of the total is `frontend/public/assets/themes/future-spinner/branding/`, which is UNTRACKED in git and referenced by NO source file, and which Vite copies verbatim out of `public/` into `dist/`. Without it the build is 14.81MB, consistent with the July figure. So the bundle is **not reproducible from the repository**: a reviewer cloning and building gets 14.81MB where this machine gets 21.87MB, and a committed size a reviewer cannot reproduce is worse than none. **RESOLVED 2026-07-26 on owner approval**: the directory is deleted, the path is in `frontend/.gitignore` with a comment naming this row, and the build now strips stray `.DS_Store` too. **A clean clone taken from origin and this machine then produced byte-identical builds: 108 files, 15,510,083 bytes, identical file lists.** The adopted logo's source lived in that directory, so before deleting it the committed master was compared PIXEL BY PIXEL against it and found identical across all 4,194,304 RGBA bytes. Recorded as CLAUDE.md convention (o): the staging bundle is always built from a fresh clone, so this cannot recur by forgetting. **A second defect surfaced in the same pass**: `build_diet_verify.mjs`, the network-hygiene proof the dossier cites, had been broken since 2026-07-16 by the HeroSplash intercepting its clicks, and its committed result was dated 2026-07-14, two days before the splash existed. Both of its pages now use the shared dismissal helper and a fresh result is committed: zero 404s, zero pruned-path requests, zero console errors, 14.79MB against the 25MB budget. Original options preserved: three were recorded in the dossier with a recommendation to delete. **(3) The PAR line is corrected.** `games/future_spinner/FUTURE_SPINNER_PAR_SHEET.md` said "6+ scatters can appear on a free-spin board (scatters stack)"; it now says "FIVE scatters is the maximum on the visible 5x4 board". **Re-measured before writing, not taken from the earlier finding**: 20,000 rounds of every one of the five books, counting only rows 1 to 4 of each six-row reel so the padding is excluded, gives a maximum of **5 in all five modes** and **zero rounds at 6 or 7**. Distributions recorded in the session report. **Sanction proof**: deny lines lifted were exactly `Edit(games/future_spinner/**)` and `Write(games/future_spinner/**)`, working-tree only, restored before the commit; `.claude/settings.json` SHA-256 `a0c8f149806a4e0a074a4d1ba10bc29346ae188ed7bf88e4d79bdc46e30570cc` identical before and after with an empty diff; all **188 files under `games/future_spinner/library/` byte-identical** before and after (manifest hash `3f7da94d...`); `git diff --stat` shows exactly one file under `games/future_spinner/` with **1 insertion and 1 deletion**. No table, weight, config or code byte was touched. Original text of this component follows. **The PAR component was blocked:** the PAR sheet lives at `games/future_spinner/FUTURE_SPINNER_PAR_SHEET.md`, inside `games/future_spinner/**`, whose deny lines are NOT lifted by this brief. Options: **(a)** carry it to the next sanctioned maths-package pass, which is where CLAUDE.md's LOCKED_FILE_DEBTS convention says compensated locked-file findings should ride; **(b)** request a narrow lock sanction for the single PAR line, on the grounds that it is documentation rather than maths and the corrected fact is already established (`num_reels = 5`, five visible scatters maximum, per the worked example in CLAUDE.md convention (l)); **(c)** record the correction in an unlocked errata document that the dossier points to, leaving the PAR itself untouched. Recommended: **(b)**, because a PAR sheet that contradicts the shipped maths is a submission-facing document and (c) leaves two documents disagreeing. Owner and Fable call, not the builder's, per THE FACTS DISCIPLINE item 8. |
| TR-048 | Two `audio_verify` artefacts sit at HEAD and a reviewer cannot tell which is current without reading the conventions | R2R1 F-05 (MINOR) | **CONFIRMED as a legibility gap, not a defect** | Minor | unassigned | **OPEN, JOB 11** | R2R1 states the treatment is within documented convention (h) and TR-029, and it is: `reports/qa/audio_verify_2026-07-13.json` is marked SUPERSEDED and the future-dated filename is deliberately unchanged under the standing ruling that nothing is relabelled retroactively. The finding is that a reviewer unfamiliar with convention (h) must read `EVIDENCE_INVENTORY.md` to work that out. The fix is prominence in the inventory, not a rename, and a rename would falsify the record. |
| TR-049 | The open-round stall state has no committed screenshot, so it is unconfirmed that it renders a comprehensible player-facing state rather than a frozen UI | R2R1 F-06 (MINOR) | **CONFIRMED, and CLOSED** | Minor | `main` | **MERGED** | **There is no stall state left to photograph, which is the better answer to the finding.** TR-035b's re-ruling replaced the stall with resume-and-settle, so the captures committed at `reports/screens/session-recovery/` show the round being replayed, then settled with the balance moving from $100.00 to $103.90, then the banner dismissed. R2R1 asked whether the state was comprehensible rather than frozen; it is now neither frozen nor a state the player has to interpret. **The capture itself found a defect**: the first run showed the replay had already played out behind the intro splash, so the player was told their round was complete and never saw it. Recovery now waits for every boot splash to clear, and the proof asserts the banner is absent while a splash is up. | Directly downstream of TR-035b, which is PARKED needing live observation. Capturing the stall state is cheap and worth doing regardless of how TR-035b resolves, because the capture is evidence either way: if the state is comprehensible it closes this row, and if it is not it strengthens TR-035b. Sequenced after JOB 4, since the open-round shape is part of the official contract being transcribed there. |
| TR-050 | R2R2 finding 1 (BLOCKER): super-mode RTP and variance band violation, claiming a 0.60% drift against base | R2R2 | **HALLUCINATED** *(Fable, verified at git level; independently recomputed this session)* | n/a | n/a | CLOSED | Recomputed from the shipped tables this session, not accepted on relay: every one of the five modes returns **96.3500%** to four decimal places, so the cross-mode spread is **0.0000pp**, not 0.60%. Method: `RTP = sum(weight x payout) / 100 / (cost x sum(weight))` with the published costs 1.0, 1.0, 1.25, 100.0, 400.0, run over all 100,000 rows of each `lookUpTable_<mode>_0.csv`. This is the same figure CLAUDE.md's true game facts record and the same one round one's reviews 1 and 3 reached independently. |
| TR-051 | R2R2 finding 2 (BLOCKER): raw sweepstakes currency codes exposed, requiring `rgsResponse.displayMeta.symbol` | R2R2 | **HALLUCINATED as filed** *(Fable)* | n/a | n/a | CLOSED | `rgsResponse.displayMeta.symbol` is not a field of the official contract; the official metadata question is the `currencyDisplay` one tracked at **TR-012c**, which is a genuine open gap and is not this. R2R2's remedy therefore names a payload that does not exist. The underlying concern is real and already has a row; the finding as written is not actionable. |
| TR-052 | R2R2 finding 3 (MAJOR): maximum win cap breach, claiming `lookUpTable_super_0.csv` row 40,993 carries an invalid 5050x payout | R2R2 | **HALLUCINATED** *(Fable, verified at git level; independently recomputed this session)* | n/a | n/a | CLOSED | Checked directly this session. The maximum payout in **every** shipped table is exactly **500000** centibets, which is exactly the 5,000x cap, with no entry above it in any mode. Super's row at id 40992 is `40992,1131181606,10550`, that is 105.5x, not 5050x. R2R2 appears to have misread a 10550 as 5050 and attached it to the wrong row index. There is no cap breach to fix. |
| TR-053 | R2R2 finding 4 (MAJOR): `games/future_spinner/index.json` is malformed by a trailing comma and will fail ACP deployment | R2R2 | **HALLUCINATED (path does not exist)** *(Fable, verified at git level; independently checked this session)* | n/a | n/a | CLOSED | There is no file at `games/future_spinner/index.json`. The real index ships at `games/future_spinner/library/publish_files/index.json`, which is tracked and valid. A finding about a trailing comma in a file that does not exist cannot be actioned, and correcting the file that does exist would be a change made on no evidence. |
| TR-054 | R2R2 finding 5 (MAJOR): player-facing rules text contradicts the RGS mathematics on the antelite feature cost | R2R2 | **HALLUCINATED (cited paths do not exist)** *(Fable)* | n/a | n/a | CLOSED | Cited against `frontend/src/components/Paytable/Information.svelte` and `frontend/src/components/Paytable/AnteliteDescription.svelte`, neither of which exists. Our paytable is a single component, `frontend/src/lib/components/PaytableModal.svelte`, and mode costs come from `frontend/src/lib/config/fsModes.ts`, which the fsModes drift gate already pins against the maths package. The same claim was raised and closed in round one at **TR-032**. |
| TR-055 | R2R2 finding 6 (MINOR): mini-player aspect-ratio clipping caused by absolute `vh` sizing in `frontend/src/styles/mini-player.css` | R2R2 | **HALLUCINATED (path does not exist)**; the underlying 400x225 concern is real and lives at TR-043 | n/a | n/a | CLOSED | No `mini-player.css` exists anywhere in the repository. Recorded rather than discarded because R2R3 finding 5 reaches a genuinely serious conclusion about the same 400x225 surface from evidence that does exist, the committed capture. That row is **TR-043** and it is JOB 8. R2R2's version of it cites a file that is not there and prescribes a CSS change to it, so it is not actionable as filed. |
| TR-056 | R2R2 finding 7 (POLISH): leftover placeholder developer text in the antelite description | R2R2 | **HALLUCINATED (path does not exist)** *(Fable)* | n/a | n/a | CLOSED | Same absent component as TR-054. Independently checked this session: all eleven distinct frontend and maths paths R2R2 cites are absent from the repository, including `frontend/src/stores/betStore.ts`, `frontend/src/components/UI/WinPresentation.svelte`, `frontend/src/components/UI/SweepstakesBalance.svelte`, `frontend/src/components/Menu/AudioToggle.svelte`, `frontend/src/managers/KeyboardManager.ts`, `frontend/src/rendering/ReelSpin.ts`, `frontend/public/assets/symbols.json` and `games/future_spinner/index.json`. |

| TR-057 | The official client's `CurrencyMeta` gives XGC 0 decimals; our `VIRTUAL_CURRENCIES` gives it 2, and the official client CONTRADICTS ITSELF on which is right | Found by JOB 4 while transcribing the pinned contract | **ESCALATED, not ruled on** | Medium, player money display | unassigned | **PARKED for owner and Fable** | `node_modules/stake-engine/src/helpers.ts` at the pin has `XGC: { symbol: 'GC', decimals: 0, symbolAfter: true }`. Its OWN documentation table, in the same file, reads `\| Stake Gold Coin \| XGC \| GC \| 10.00 GC \|`, which is two decimals, and the docs mirror at `docs/stake-engine-live/rgs-communication.md:82` says the same. So our value agrees with both prose sources and differs from the executable one. **Options:** (a) follow the client code, 0 decimals, on the grounds that the executable beats the comment; (b) hold at 2 decimals, on the grounds that two independent first-party prose sources agree and the client's own comment is one of them; (c) resolve empirically at DTT against a real GC session, which is the only input none of the three sources can be wrong about. **RULED 2026-07-26 (R2R-R JOB F): option (c) with (b) as the interim, exactly as recommended.** We hold at 2 decimals now, and DTT flips one line if a real GC session disagrees. The reasoning, recorded so the ruling does not have to be re-argued: TWO first-party sources say 2 (the official client's own documentation table, and the docs mirror at `rgs-communication.md:82`) against ONE that says 0 (the client's `CurrencyMeta` code), and the one that disagrees contradicts a table sitting in the same file. Holding also matches what a reviewer opening the published docs would expect to see. Changing player money display on a self-contradicting source would be the worse call, and it is reversible in a single value either way. Original options preserved: recommended **(c) with (b) as the interim**, because holding matches the documentation a reviewer would check and changing on a self-contradicting source is not evidence. This is player money display, so per THE FACTS DISCIPLINE item 8 it goes to the owner and Fable with the evidence rather than being decided by the builder. **XSC agrees on all three sources at two decimals and trailing placement**, which independently corroborates TR-012c's placement ruling. |
| TR-058 | The paytable's social rules said "Prizes pay left to right"; "pay" is itself a restricted phrase | Found by JOB 6's new full-DOM scan | **CONFIRMED, and FIXED** | Medium | `main` | **MERGED** | Not filed by any reviewer. It was found by the replacement conformance scan on its first run, which is the point of replacing the old one: the previous rewrite of this block changed "Wins" to "Prizes" at the front of the sentence and left "pay" in the middle, and a scan checking two words could never have seen it. Fixed by authoring rather than substituting, because `sv('pay')` is "win" and "Prizes win left to right" is not English. |

**Verdict on R2R2, recorded once rather than repeated in seven rows.** Every maths finding
it makes is refuted by direct recomputation from the shipped tables, and every path finding
it makes cites a file that is not in the repository. **No remediation work follows from that
review.** Its rows above exist so that a future reader can see each finding was examined and
why it was set aside, which is the same treatment round one gave the same reviewer at
TR-032, TR-033 and TR-034. The pattern repeating across two rounds is itself worth recording:
this reviewer describes a different codebase each time, with confident specifics.

**What was checked independently this session, and how.** Convention (l.4) requires stating
what each side relies on before agreement counts as corroboration. These checks read the
shipped artefacts directly rather than any report about them: row counts and maxima by
scanning all 100,000 rows of each of the five `lookUpTable_<mode>_0.csv`; per-mode RTP by
recomputing the weighted sum against the published mode costs; super id 40992 by locating
the row itself; the eleven R2R2 paths and `games/future_spinner/index.json` by filesystem
test; the `ReplayMode.svelte`, `PaytableModal.svelte`, `WinBanner.svelte` and
`responsibleGambling.ts` line references by opening those files; and `npm run check` by
running it. Not independently re-derived, and taken on Fable's stated first-hand
verification per the brief: the contents of the pinned `ts-client` types, and the verifier's
zero-round PASS (the books are present on this machine, so reproducing that specific
condition belongs to JOB 7, where the self-test creates it deliberately).

| TR-059 | Player-facing prose is NOT localised: in a German session the chrome translates and the body stays English | Owner's first live portal session, 2026-07-26 | **CONFIRMED, seen on the real platform** | Major | unassigned | **OPEN** | The owner's DTT screenshots show a `lang=de` session rendering `DREHKOSTEN`, `DREHMODI`, `WÄHLEN`, `AKTIV`, `AKTIVIEREN` and `GEWINNTABELLE` beside fully English body copy: "Standard play. Overdrive Free Spins trigger on 3+ scatters.", "Match symbols on adjacent reels starting from reel 1", "Substitutes for all symbols except SCATTER", and every mode blurb. **Measured: 19 sentence-case player-facing literals in component markup**, plus the `rulesList` arrays inside `PaytableModal.svelte`'s script block and every `label`/`blurb`/`socialLabel`/`socialBlurb` in `config/fsModes.ts`, which are English-only by design. **Why no gate caught it**: `locale_completeness_check.mjs`'s hardcoded-literal scan matches `>([A-Z][A-Z0-9 &'.-]{2,})<`, i.e. ALL-CAPS text between tags only, so it sees shouty labels like `SELECT THEME` and is blind to sentence-case prose, which is most of what a player actually reads. The keyed layer is complete across all 16 locales and works; what is missing is that most prose was never keyed. This is a real localisation gap, not a broken translation, and closing it is roughly 30 new keys times 16 locales plus routing the fsModes strings, which is a pass of its own rather than a patch. **EXTENDED 2026-07-26 from the DTT toolbar captures**: the platform's own Language menu lists `ar de en es fi fr hi id ja ko pl pt ru tr zh vi da` and was still scrolling at the capture edge. We ship exactly sixteen locales, verified from `lib/i18n/translations.ts`: `ar de en es fi fr hi id ja ko pl pt ru tr vi zh`. **`da` (Danish) is offered by the platform and is not one of ours**, and the tail of the list is unread, so the true coverage gap is not yet known. Parked per (l.6) rather than estimated: the owner scrolls and captures the full menu before this work is sized. **PARKED 2026-07-26 by ruling, pending the full language capture**, which is observation 5 of the JOB 7 second visit. Not deferred out of reluctance: sizing ~30 keys times an unknown locale count is guesswork until the platform's real list is read end to end, and (l.6) prefers an honest park to a confident estimate. **Added to the observation**: the `da` session doubles as a fallback test, since `da` is a locale we do not ship, so it must degrade cleanly to English rather than blank. That single capture therefore settles both the size of the gap and the safety of the fallback. |
| TR-060 | Em dashes rendered to the player, and the gate that forbids them scanned two files | Owner's first live portal session, 2026-07-26 | **CONFIRMED, and FIXED** | Minor | `main` | **MERGED** | Visible in the owner's `GEWINNTABELLE` screenshot: "All matching symbol positions count — no fixed paylines." and "Reels 1, 2 and 3 hold the same symbol (highlighted) — a match". `player_string_dash_check.mjs` reported PASS throughout because its `FILES` list was exactly `translations.ts` and `fsModes.ts`, while the convention it enforces says "no em dashes or en dashes anywhere". Same shape as the social conformance script R2R3 dismantled: a gate whose name implies breadth and whose scope is a couple of hand-listed files. The gate now scans **all 25 files** including every component, which immediately surfaced two more (the paytable's `—` placeholders and a GameGrid console string). All fixed; gate green at 25 files. **AMENDED 2026-07-26 by the DTT live-session self-audit: this closure note was half right and is superseded by TR-063.** The two strings are gone and the gate is green, but the widening fixed only the file-coverage half. The gate reads single-quoted JavaScript literals only, and the two strings it was written to catch were markup prose between tags, so it still cannot see the form of defect that produced it. Status stays MERGED because the fix landed; the recurrence guard did not. |
| TR-061 | Four frontend files never reached the portal, and one of them is a live player-visible asset: the hero pilot renders as a broken image in the published build | Owner's file listing versus the upload kit, 2026-07-26 | **CONFIRMED, seen on the real platform** | Major while live, trivial to remedy | unassigned | **OPEN** | Portal FRONTEND panel reads **13.0 MB, 104 files**; `~/Desktop/FS_UPLOAD_KIT/02_frontend_upload/` holds **108 files, 15,510,083 bytes**. Diffing the portal's own listing against `find` output: nothing on the portal is absent from the kit, and four kit files are absent from the portal. `assets/themes/future-spinner/ui/scene_character.png` (629,245 B) is live at `SceneGroup.svelte:71` and is the broken image the owner reported; the 05.11.48 capture shows the placeholder glyph with `scene_car.png` rendering correctly behind it. The other three have no live reader, verified by grep: `assets/sounds/bgm_loop.mp3` (955,254 B) is shadowed by the themed copy `themeStore.ts:71` actually resolves to, `frames/frame-1.png` (169,689 B) was superseded by `frame-2.png` at `themeStore.ts:57`, `ui/brand_mark_base.png` (107,674 B) by `hero_icon_96.png` at `LoadingScreen.svelte:22`. **Corroborated by byte arithmetic**: the four total 1,861,862 B, and 15,510,083 minus that is 13,648,221 B, i.e. 13.015 MB against the panel's 13.0 MB. **Cause not determined and not guessed**: ruled out size (169 KB failed while 1,012 KB succeeded), content de-duplication (no shared MD5 among the 108), position (indices 11, 28, 75, 101) and a reported skip (the maths dialog read `Skip 0 Files`). **Missing input named per (m)**: the frontend upload's sync dialog was never captured, so we cannot tell whether the portal was handed 108 and stored 104 or only ever handed 104. Capture it before the next Start Sync. Remedy is a re-upload, but see TR-062 first. **RESOLVED 2026-07-26.** The owner re-dragged the same folder and the sync dialog, captured this time, read **`Upload 4 Files, Skip 104 Files, Delete 0, Move 0`** and named `bgm_loop.mp3`, `scene_character.png`, `brand_mark_base.png`, `frame-1.png`: **the portal independently identified exactly the four files the diff identified**, from its own content comparison rather than from our listing. Republished to Front V2; the 05.50.11 capture shows the pilot rendering. **Cause now determined**: the sync is content-aware, so re-dragging the same folder proves it held 108 on both runs, which means the first upload handed the portal 108 and the portal stored 104. The four were **dropped platform-side with no error surfaced**. Stated as strongly supported rather than proven, since the first run's dialog was never captured. Worth reporting upstream: a sync that silently drops files is a platform defect, and it was caught only because the walkthrough told the owner to compare the panel count against 108. |
| TR-064 | The official client's end-round rule and the platform's own testing guideline give OPPOSITE instructions for zero-win rounds | Stake Engine testing guidelines, item 12, read 2026-07-26 | **ESCALATED, not ruled on** | High, it is a named guideline item with a pass or fail | unassigned | **PARKED for owner and Fable** | Guideline: *"Zero-win bets do not send an end-round request to the RGS"*. Our gate is `rgsService.ts:797`, `const needsEndRound = playResp.round ? playResp.active : playResp.winMicros > 0`. That was **deliberately changed** from `winMicros > 0` to the round's `active` flag under the JOB 4 sanction, and the comment records why: the official client says *"Only call this API if Play() has returned an Active result"*, and a `winMicros > 0` gate is wrong in both directions against that contract (a zero-win round the RGS still holds open would never settle; a settled winning round would settle twice). **So the two first-party sources conflict.** If the RGS returns `active: true` on a zero-win round we breach the guideline; if it returns `active: false` both are satisfied and there is nothing to do. **This must not be "fixed" before it is observed**, because reverting to `winMicros > 0` would reintroduce the defect the sanction closed. **Options:** (a) observe one zero-win spin in the Network tab and follow the evidence; (b) revert to `winMicros > 0` to satisfy the checklist literally, reintroducing the unsettled-round bug; (c) gate on `active && winMicros > 0`, which satisfies both texts but silently leaves a round open if the RGS ever holds a zero-win round active. **Recommend (a)**, and it is a thirty-second observation. Per (l.8) this is money-path and goes to the owner and Fable with the evidence rather than being decided by the builder. **RULED 2026-07-26: option (a), observe first, and NO CODE CHANGE in this pass.** The conflict is held open deliberately. Reverting to a win-based gate would reintroduce the defect the JOB 4 sanction closed, and neither first-party source can be preferred on argument alone. Observation 1 of the JOB 7 second visit captures a zero-win spin's Network tab: expect no end-round request, and note what `active` reads either way. The code moves only after that capture exists. |
| TR-065 | The game frame scrolls on every responsive layout, against guideline item 15 | Stake Engine testing guidelines item 15, plus the owner's captures | **CONFIRMED, reproduced from code and three captures** | Medium | unassigned | **OPEN** | Guideline: *"Main game frame should not be scrollable"*. `:global(body)` is correctly `overflow: hidden; height: 100dvh` (`App.svelte:1709`), and desktop is clean. But `App.svelte:1821` gives `.game-wrapper.portrait`, `.game-wrapper.compact-landscape` and `.game-wrapper.mini-player` `overflow-y: auto`. A scrollbar is visible in the owner's captures at **mobile portrait, Mobile S (320x568) and Popout S (400x225)** and absent at Desktop, Laptop and Popout L, which is exactly the three-class list. Derived from code first, then corroborated by three captures at three sizes, independently. Fix is one rule; the reason `auto` was chosen (small viewports where content might exceed the box) needs checking so the fix does not clip instead of scroll. **APPROVED 2026-07-26 for the JOB 3 fix pass**, item (b), with the method constrained by the ruling: the frame must **fit** at mobile portrait, Mobile S and Popout S by re-deriving the layout maths, **not** by hiding overflow. Hiding it would trade a visible scrollbar for an invisible unreachable control, which is exactly what TR-069 turns out to be. Layout suites run at all seven real platform presets. **FIXED 2026-07-26, JOB 3(b).** Root cause was two stale constants, not the overflow property. Measured at Mobile M (375x667): the portrait wordmark was assumed 28px and is actually **42**; the HUD slot was assumed 290px and its content needs **287**, but `.p-hud` inside it ran to y=678 against a 667 viewport. 42+287=329 needed above the canvas where the old maths reserved 28+290=318, so the canvas got 349px into 338px of space and overshot by exactly the 11px observed. **Both constants are now runtime measurements** (`hudSlotEl.scrollHeight` and the wordmark's own rect, re-measured on resize and by a ResizeObserver so a mid-round feature strip or a longer translated label cannot reintroduce it); the constants survive only as first-frame fallbacks. A first draft clamped the measurement back up with `Math.max(287, 290)`, which returned 290 and changed nothing, and that is recorded because it is the same estimate-drift trap one level down. Separately `.canvas-slot` gains `overflow: hidden`, which is NOT the forbidden fake fit: what it clips is decorative scene bleed (car and character layers extend past the frame by design, and desktop already clipped them via `.stage`), and the gate proves it by asserting reachability against clipping ancestors independently. **`layout_fit_gate.mjs` PASSES at all seven presets: no scroll, no offscreen control, no clipped readout.** |
| TR-066 | Double-tap zoom is not disabled on mobile, against guideline item 25; and the mini-player WIN readout clips mid-glyph | Stake Engine testing guidelines item 25, plus the Popout S capture | **CONFIRMED** | Minor | `main` | **CLOSED, both halves** | Guideline: *"Double tap to zoom is disabled on mobile"*. `frontend/index.html` viewport meta is `width=device-width, initial-scale=1.0, viewport-fit=cover`, with no `maximum-scale` and no `user-scalable=no`, and **`touch-action` appears nowhere in `frontend/src`** (grepped). Nothing disables double-tap zoom. Note `user-scalable=no` alone is ignored by modern iOS Safari, so the durable fix is `touch-action: manipulation` on the interactive surfaces, not the meta tag alone. **Filed together with a second cosmetic defect from the same capture**: at Popout S the mini strip's WIN readout renders `$0.0` plus a clipped partial character, and the balance drops its cents (`BAL $1,040` where the true balance is $1,040.06). The dropped cents may be intentional at that width; the mid-glyph clipping is not. Both live in the mini HUD sizing that TR-043 and JOB C tuned. **APPROVED 2026-07-26 for the JOB 3 fix pass.** Double-tap zoom is disabled via `touch-action: manipulation`, **not** `maximum-scale`, because the meta route is ignored by modern iOS Safari and would produce a gate that passes while the behaviour persists. The mini-player WIN clipping is fixed with a **measured assertion**, not a nudge, since this sizing has already been tuned twice and both previous attempts moved the truncation rather than removing it. **DOUBLE-TAP ZOOM FIXED 2026-07-26, JOB 3(c).** `touch-action: manipulation` on `html, body` in `app.css`, verified present in the shipped `dist` CSS. Deliberately not the viewport meta route: iOS Safari has ignored `user-scalable=no` and `maximum-scale` since version 10, so that fix would have produced a gate reading green while a player could still double-tap the reels and zoom, which is the exact failure shape this project keeps rediscovering. `manipulation` removes the double-tap delay while leaving PINCH zoom intact, so a player who needs to magnify the paytable still can; suppressing pinch would be an accessibility regression the guideline does not ask for. **THE MINI-PLAYER WIN CLIPPING IS NOT YET CONFIRMED FIXED and is deliberately left open.** The layout gate now reports `clipped=0` at Popout S and `mini_player_proof.mjs` passes `noStatValueIsTruncated`, but that assertion was already passing before this session while the owner's capture plainly shows a cut glyph, so a green result from it is not evidence. Either the `.canvas-slot` clip resolved it incidentally or both gates are measuring the wrong node. Not claimed as fixed until a Popout S capture from the rebuilt bundle is compared against `28_popout_s_mini_strip_TR066_win_clipped.png`. **REPRODUCED 2026-07-26, JOB 3(e), and the cause is now exact.** Two separate failures, both closed except the last step. **(1) The assertion was vacuous.** `noStatValueIsTruncated` tested `v.scrollWidth > v.clientWidth + 1`, and every `.m-stat-value` carries `use:autofitText`, which iteratively shrinks the font until precisely that condition is false. It tested the one thing autofit exists to eliminate, so it could only ever return false. Replaced with four real failure modes it cannot mask: clipped by a clipping ancestor, clipped by the viewport, autofit floor reached with content still overflowing, wrapping instead of fitting, and an effective rendered size under 9px. Per convention (p) it now ships a seeded self-test planting all three forms plus a negative control, and **the repair was itself caught by that self-test**: the first version missed the overflow seed because the seeded text WRAPPED rather than overflowing, which the check could not see either, so a `scrollHeight` test was added. **(2) The proof only ever rendered short numbers.** `START_MICROS` was `100_000_000`, i.e. `$100.00`, four characters. Every legibility assertion passed against it for weeks while the owner's real session showed `BAL $1,040` with the cents dropped and the WIN glyph cut. Raised to `$52,431,098.76`, which is not arbitrary: the owner ran this game at a $50,000,000 balance and the DTT offers presets to $10,000,000,000.00. **At that balance the mini BALANCE readout truncates in ALL FIVE states**, reproducing the owner's capture exactly. **STATUS: the gate is RED and stays red.** It is telling the truth, and this session's whole lesson is that a green gate which lies is worse than a red one. **The remaining fix is escalated rather than taken, per (l.8): it is player money display.** At 400x225 the strip must hold a features button, a menu, BALANCE, WIN, bet down/value/up and SPIN; roughly 170px remains for BALANCE and WIN, and `$52,431,098.76` alone needs about 110px at a legible size. Full precision for both readouts is not geometrically achievable at that width, so something must give. **Options:** (a) abbreviate above a threshold in the mini profile only, e.g. `$52.43M`, full precision everywhere else and the exact figure always available in the session panel, which is the common industry treatment for compact HUDs; (b) drop the cents above a threshold, `$52,431,098`, losing less information but saving less width; (c) drop the WIN readout from the mini strip when idle to give BALANCE the room, which changes what a player can see mid-round; (d) accept a smaller legible floor and re-measure. **Recommend (a).** This is money display, so per THE FACTS DISCIPLINE item 8 the builder does not rule on it. **CLOSED 2026-07-26. Fable ruled option (a), refined: in the 400x225 profile alone, BALANCE and WIN abbreviate (four significant characters plus magnitude suffix, `$52.43M`) exactly when the fully formatted value cannot fit the measured slot at the legible floor; values that fit render in full; every other profile keeps full precision everywhere.** Built as ruled, and the build found the real root cause, which is **not** the one recorded above. **A CORRECTION TO THIS ROW'S OWN DIAGNOSIS, stated plainly because it was wrong and it was mine.** The entry above says `noStatValueIsTruncated` was vacuous because "every `.m-stat-value` carries `use:autofitText`, which iteratively shrinks the font until precisely that condition is false". That is true of `.p-stat-value`, `.c-stat-value` and `.fs-value`. **It was never true of `.m-stat-value`**, and it is the reverse of the truth: that rule read a flat `font-size: 11px` from the day JOB C wrote this profile, so it never multiplied in the `--autofit-scale` custom property the action writes, and **`use:autofitText` has been a complete no-op on the mini strip for its entire existence.** The action set a property nothing read. The comment sitting directly beside the rule asserted the opposite ("autofitText already shrinks the string to fit"), which is how it survived two tuning passes and a written diagnosis. So the mid-glyph cut in the owner's capture was not autofit reaching a limit, it was nothing shrinking at all and `overflow: hidden` cutting the string. Fixed by the one-token change `font-size: calc(11px * var(--autofit-scale, 1))`, plus `font-variant-numeric: tabular-nums` so a counting-up win does not re-fit on every frame. **The abbreviation is still required, and this was derived before it was built.** Measured on the shipped build, the BALANCE stat box at 400x225 is **84.2px** including its 7px label, leaving roughly 58px for the value, and `$52,431,098.76` needs about 95px at the 11px base. Fitting it means about 0.61 scale, an effective **6.7px**, under the 9px legible floor. Shrinking alone cannot solve it; the ruling's abbreviation is the only remaining lever. **The decision is MEASURED, never thresholded.** `frontend/src/lib/actions/fitMoney.ts` renders the full form, shrinks it, and switches to the compact form only if it still overflows at the floor. A magnitude threshold ("abbreviate above $1,000,000") would have been a guess about a pixel width that depends on the currency symbol, the locale's separators, the bet ladder in play and whether the brand font loaded. **The formatter is `formatBalanceCompact` in `utils/currency.ts`, beside `formatBalance` and sharing its symbol table, locale parameter and platform-display-metadata path**, so the two forms cannot disagree about the symbol or leak a raw code. It uses **Intl compact notation rather than a hand-written K/M/B suffix table**, because a table would be English in a game that ships sixteen locales: `de` renders "52,43 Mio. $" and `ja` renders "$5243万" with no authored string, and the test asserts the rendered compact form carries **no prohibited term in any of the sixteen** via `scanProhibited`, so the vocabulary layer covers it. **It TRUNCATES rather than rounds, and that is a money rule.** Rounding to nearest renders $999,999.99 as "$1M", telling a player they hold more than they do; truncation renders "$999.9K" and can only understate. Asserted for four values including that no abbreviated numeral ever exceeds its true value. **PROVED ON BOTH FIXTURES, which is what makes it a proof of the ruling rather than of half of it.** `mini_player_proof.mjs` now runs the hostile `$52,431,098.76` and a fits-in-full fixture, and the fits-in-full value is **not invented**: it is `$1,040.06`, the balance in the owner's own capture, which the shipped build rendered as `BAL $1,040` with the cents cut off. Hostile: `$52.43M` at the full 11px in all five states. Fits: **`$1,040.06` complete, at 9.92px, above the floor**, so the owner's exact figure now renders whole and it is the `--autofit-scale` wiring rather than the abbreviation that achieves it. Desktop, portrait and compact landscape all render `BALANCE $52,431,098.76` in full, asserted. **Convention (p): five seeded violations, all caught**, including two new ones planting the failure of the abbreviation claim itself, one of which is literally the pre-fix build (the full string left at full size in a slot too small for it). Negative control clean. **A THIRD DEFECT, found only by doing the capture comparison the ruling asked for.** Beside the money readouts, the mini strip's menu button renders as an **empty dark box with no icon**, in the owner's live capture and in the rebuilt one. Cause: this profile borrows the portrait profile's `.p-hamburger-bar` markup, that rule paints from `var(--p-acc)`, and `--p-acc` is declared on `.p-hud` and nowhere else, so inside `.m-hud` the property is unset, the declaration is invalid at computed-value time and `background` falls back to transparent. Three bars of nothing, on the only control that reaches the paytable, session panel, turbo, autoplay and MAX in the popout. **`.c-hud` has the identical defect** and was fixed in the same pass. Fixed by declaring the accent on both roots rather than rewriting the shared bar rule, so a future profile reusing that markup cannot inherit the same silence. **A GATE REPAIR THIS FORCED, recorded because the gate was right to go red.** `theSpinActuallyRan` compared the BALANCE readout before and after a spin. At an abbreviated balance a $1.00 bet leaves the string at `$52.43M` both times, so the check failed. The repair is not a looser comparison but a better source: intercepted `/wallet/play` calls are counted, which proves the round reached the wallet and cannot be affected by how the result is formatted. Evidence: `reports/qa/mini_player_proof_2026-07-26.json`, captures in `reports/screens/mini-player-2026-07-26/` (five states plus `idle-fits-in-full.png`), compared against `reports/screens/dtt-live-2026-07-26/28_popout_s_mini_strip_TR066_win_clipped.png`. Gates: mini player proof PASS with all 25 checks and 5 seeded violations caught, currency static 82 assertions PASS, `npm run check` 0 errors at the committed 36-warning baseline, and the full static gate set PASS. |
| TR-067 | Social Mode switches the vocabulary but never forces English, against guideline item 46 | Stake Engine testing guidelines item 46, read 2026-07-26 | **CONFIRMED** | Medium, it is a Stake.US jurisdiction requirement | `main` | **CLOSED** | Guideline: *"English is the only supported language in Social Mode"*. `i18n/tr.ts:14` is `derived([locale, isSocial], ([$locale, $social]) => (key) => t($locale, key, $social ? 'social' : 'real'))`. The social flag selects the **vocabulary variant** and never touches the **locale**, so a social session launched with `lang=de` renders German chrome with social wording. The whole 39-term substitution layer works correctly and is well tested; this is a separate axis nobody had connected. **Options:** (a) force `locale` to `en` whenever `isSocial` is true, one line in `tr.ts`, and accept that a platform-supplied non-English social session is overridden; (b) leave the locale alone on the grounds that the platform will only ever send English for Stake.US, and treat the guideline as a statement about platform behaviour rather than a game requirement; (c) force English and log a warning so a mismatch is visible in testing. **Recommend (a) or (c)**; the guideline is written as a requirement on the game and (a) is the reading a reviewer will apply. Interacts with TR-059: if social forces English, the localisation gap matters less in social jurisdictions and not at all less elsewhere. **APPROVED 2026-07-26 for the JOB 3 fix pass**, option (a): social sessions force the `en` locale **before first paint**, regardless of the language parameter. Before first paint rather than reactively, so a `de` social session never flashes German. Held by a test for a social `da` launch, which exercises the forcing and the unshipped-locale fallback in one case. **CLOSED 2026-07-26, JOB 3(d).** Built as approved, in `frontend/src/lib/stores/socialLocale.ts`. **Two layers, because social arrives by two routes at two different times.** BOOT: the `?social=true` flag and a social `?currency=` are in the URL from the first byte, so `resolveLaunchLocale` decides in `App.svelte`'s script body and there is never a German frame to photograph. LATE: `socialCasino` and the currency code arrive with the authenticate response, after paint, so `enforceSocialEnglish` watches `isSocial` and forces English if it turns true afterwards. Without the second layer a `lang=de` session that the platform flags social only at authenticate would have rendered German social wording, which is the breach the guideline names. The forcing is deliberately ONE WAY: social turning on forces English, social turning off restores nothing, because no legitimate session stops being social and a store that remembered and restored would be inventing a state machine for a transition that cannot occur. **The recorded fix was "one line in `tr.ts`" and that was taken as the disposition, not as the implementation.** A change inside `tr.ts` covers every string routed through `$tr` and leaves the `locale` store itself still reading `de`, so every other consumer of the locale, the currency formatter's locale tag among them, would disagree with the words beside it. The store is forced instead, via the public `.set()` API, so no lock exception arises and every consumer agrees by construction. **The rule and the wiring are proved separately, because they are different claims and this project has been caught by the gap between them twice** (R7/TR-015 was a correctly computed flag with no consumer; TR-067 is itself a correct vocabulary layer nobody had connected to the locale axis). `socialLocale.test.ts` proves the RULE with no browser: **65 assertions**, including the named social `da` case, forcing over all sixteen shipped locales, the twelve malformed inputs, and the regression guard that a real-money session in each of the sixteen still renders its own language, since a fix for item 46 that broke TR-014's localisation would trade one defect for a worse one. `locale_launch_conformance.mjs` proves the WIRING in a real browser across five social launch shapes, asserting both the rendered string and the **locale store itself**, plus a negative control that a real-money `de` session still renders `DREHEN`, with that control's own discriminating power asserted rather than assumed. **A correction the run produced, recorded because the first draft of the assertion was wrong rather than the build.** The check initially compared against `locales.en.spin` and failed all five cases with "rendered PLAY, expected SPIN". `spin` is itself a prohibited phrase on stake.us, so social English correctly renders `PLAY`: the expectation had named the locale axis and forgotten the vocabulary axis, which are exactly the two axes this row exists to keep separate. Now compared against `t('en', 'spin', 'social')`. Evidence: `reports/qa/locale_launch_conformance_2026-07-27.json` (16 locales, 10 fallback cases, 5 social cases, negative control, PASS) and `reports/screens/audit-remediation-v1/locale-social-da.png`, a social `da` session rendering COINS, PRIZE, PLAY and FEATURES entirely in English. CI gate 12a. |
| TR-062 | The published bundle is one commit behind `main`, and nothing in the artefact ties a bundle to a commit | Found while auditing the upload kit, 2026-07-26 | **CONFIRMED** | Medium | `main` | **CLOSED, both halves** | The kit was built before `5674bd7` (the TR-060 dash fix). Verified directly rather than inferred: its shipped `assets/index-DEOd1P6P.js` still contains nine em dash occurrences including the two player-facing paytable strings, and the owner's 05.14.38 capture shows both rendering to a player in the published build. **Immediate consequence**: re-uploading the folder now on the Desktop would restore the hero image and re-ship the em dashes, so the TR-061 remedy must be a fresh-clone build per convention (o), not a reuse of the kit. **The general finding**: `dist/` carries no build stamp, so "what is live" is not answerable from the repository and I could only establish it by grepping the bundle. Same class as TR-047, where a committed size figure was checkable on exactly one machine. **Options**: (a) stamp `dist/` with the commit SHA at build time and have the upload kit refuse to package a dirty or non-HEAD tree; (b) treat the kit as single-use, regenerated per upload, and delete it after; (c) record the SHA in the kit's README only, which is the cheapest and the easiest to forget. Recommend (a) with (b), because (a) makes the question answerable from the artefact and (b) removes the stale-folder trap that caused this. Fable to rule. **RULED 2026-07-26: option (a) with (b), as recommended.** `dist/` carries `build-info.json` (commit SHA, build date, bundle byte total) plus one console boot line, and the network-hygiene gate asserts it adds no request (JOB 4). Kits are **single use**: the builder refuses a dirty tree or a non-HEAD checkout, names the SHA in the kit README, and the kit is deleted after upload. **The Desktop kit `~/Desktop/FS_UPLOAD_KIT/` is DEAD** and must not be uploaded again; kit V3 supersedes it and is frontend only, since maths stays at V1 untouched. **CLOSED 2026-07-26, JOBs 4 and 5, both halves built and run.** **(a) The stamp.** `dist/build-info.json` is written at the very END of `closeBundle`, after every prune, so its figures describe the bundle that actually ships rather than the one Vite emitted before the diet ran. It carries the commit, the branch, whether the tree was clean, the build time and the bundle totals, and `App.svelte` prints one `console.info` line on boot in every mode. **The values in that line are INLINED by Vite's `define`, not fetched**, because the obvious way to print them is a runtime fetch of `build-info.json` and that would add a request to every session. `build_diet_verify.mjs` now asserts explicitly that the file is never requested rather than trusting the implementation to stay honest: measured, **0 requests**. `cleanTree` is not decoration: a stamp naming a commit while the tree carried uncommitted edits names a commit that does not describe the bundle. The recorded byte total EXCLUDES the stamp itself for want of a fixed point, and the name says so; `dist_hygiene_gate.mjs` reconciles the two explicitly, since a provenance file describing a bundle that no longer exists is worse than none. **(b) Kits are single use.** `scripts/kit_build.mjs` builds from a fresh clone per convention (o) and REFUSES an unpushed HEAD (the clone cannot contain it, so the kit would silently be older than the tree the operator is looking at, which is exactly what produced this row) and REFUSES uncommitted changes under `frontend/` (those become the bundle). It WARNS by name about anything else dirty rather than refusing, which is reasoned rather than lax: the artefact comes from a clone, so an untracked file elsewhere cannot reach it by construction. It runs the dist gates IN THE CLONE, writes nothing to the Desktop if one fails, and verifies the copy matches the build file for file and byte for byte. Convention (p): five-case self-test including the negative control that a clean pushed tree builds, since a gate that only ever says no gets routed around. **Both refusals were observed firing on the real tree before the push, and the build then succeeded after it.** **Kit V3 exists**: `~/Desktop/FS_UPLOAD_KIT_V3/`, commit `7dd83e6a`, clean tree, **108 files, 15,515,125 bytes, 14.80 MB** against the 25 MB budget, gates run in the clone all PASS. Its README names the SHA, says the maths stays at V1, and says the kit is single use and that `~/Desktop/FS_UPLOAD_KIT/` is DEAD. |
| TR-063 | The dash gate was widened across files but still cannot see the string form that produced TR-060, and does not scan `App.svelte` | Self-audit before reporting, 2026-07-26, per (l.5) | **CONFIRMED, reproduced** | Minor, but it is a recurrence guard that does not guard | unassigned | **OPEN** | `player_string_dash_check.mjs:65` matches `codePart.match(/'([^'\\]|\\.)*'/g)`, i.e. single-quoted JavaScript literals only. The two strings TR-060 found were markup prose between tags; double-quoted strings and template literals are equally invisible. Separately, `FILES` at line 37 lists `translations.ts`, `fsModes.ts` and `src/lib/components/*.svelte`, so **`src/App.svelte` is not scanned**, and `App.svelte:1215` ships `<title>{$activeTheme.name} — We Roll Spinners</title>`, an em dash in the browser tab title, player-visible, while the gate reports PASS at 25 files. Reproduced this session: the gate runs green with that line present. **This is the same pattern the project has now named four times** (social conformance, books verifier, `build_diet_verify.mjs`, and TR-060 itself), one level further down: the instance was fixed and the class reported closed. **Options**: (a) scan the whole file text for the two characters and allowlist genuine typographic placeholders by line, simplest and noisiest; (b) strip comments then scan all remaining text including markup, and add `src/App.svelte` and `src/routes` to `FILES`; (c) move the check into the build and scan `dist/` instead of source, which catches anything that actually ships regardless of how it was authored. Recommend (c) with (b), since (c) is the only form that would have caught the shipped bundle in TR-062 as well. Fable to rule. **RULED 2026-07-26: option (c) with (b), as recommended, plus a new standing convention.** The gate moves onto the built `dist/`, scanning every emitted byte for U+2014 and U+2013 with a reviewed allowlist, and a whole-text source scan including `App.svelte` sits behind it. Every source instance is fixed including the document title. **And the ruling generalises**: the gate ships a seeded-violation self-test that plants a dash and must FAIL on it before its PASS counts, now recorded as CLAUDE.md convention (p) and binding on every gate that claims a class closed. **CLOSED 2026-07-26, full chain verified post-outage.** `dash_gate.mjs` replaces `player_string_dash_check.mjs`; CI runs the self-test as its own step BEFORE the scan, so a gate that has lost the ability to fail is caught by CI rather than by a reviewer four days later. **Gate outputs: self-test PASS (4 seeded forms caught, negative control clean), source scan PASS, dist scan PASS, `npm run check` 0 errors.** The allowlist is empty and stayed empty. **The dist scan immediately earned its place**: it found 17 em dashes in `assets/themes/future-spinner/sounds/README.md`, a provenance document that ships in the bundle and was served from the portal in the 104-file listing. No source scan of `src/` could ever have seen it, because it is a public asset rather than code, and it is exactly the class of miss that produced TR-060 and TR-063 in the first place. Fixed at source. **Residual, parked**: `gameStore.ts` retains four comment dashes because it is locked and this session carried no exception; all four are comments, so dist reads zero and the shipped artefact is unaffected. Recorded under LOCKED_FILE_DEBTS. **Open question raised, then CLOSED 2026-07-26 by JOB 3(i).** The question was whether that sounds README should ship to the platform at all, exposing tooling provenance (model name, seeds, prompts, licence paths) to anyone who can fetch the game. It should not, and now does not. `vite.config.ts`'s `pruneDocs` strips every documentation file from `dist` at closeBundle, and `frontend/scripts/dist_hygiene_gate.mjs` asserts none is present, reading `dist` as it stands and knowing nothing about how it got that way. **The two are deliberately separate**: a plugin that stops running or has a path edited fails silently and the bundle quietly carries the file again, which is exactly how `build_diet_verify.mjs` sat broken for ten days while its committed result was cited as compliance evidence. **Extension-based rather than a name list**, for the reason this row and TR-060 both learned: a list of files is a list, and the file that hurts is the one nobody added to it. Convention (p): four seeded violations, the first of them the exact path that really shipped, deep inside an asset directory rather than a convenient `foo.md` at the root, since a scan checking only the top level would have passed on the real thing while catching a lazier seed. Negative control asserts every format the bundle genuinely ships survives. **Bundle re-recorded: 107 files, 15,514,619 bytes, 14.80 MB against the 25 MB budget**, computed by the gate on the build it describes and written to `reports/qa/dist_hygiene_2026-07-26.json`. CI gate 13e. |

| TR-068 | Buy-mode rounds present a loss as a win: the HUD headlines gross payout while the balance falls | Owner's third live portal session, 2026-07-26 | **CONFIRMED as presentation; the money path is correct** | Medium, player-money presentation | `main` | **CLOSED, ruled and built** | The owner reported "the balance wasn't updating". It was: I reconciled four HUD balances against the platform's own bet log to the cent (06:20:58 $49,917,330; 06:22:36 $49,972,875; 06:23:37 $49,830,090; 06:28:31 $48,916,485), each derived independently using the true per-mode cost, and all four are exact. `App.svelte:1002` computes `cost = bet * MODE_COST[mode]` and the same `cost` feeds both the balance path and `rgRecordSpin`, so the wallet and the session panel cannot drift. **What the owner actually saw is real though:** at 06:23:37 the HUD reads `WIN $57,215.00` in large green type while the balance falls by $142,785, because the round cost $200,000. **The typical buy round pays back less than it cost** (super break-even 71.8%, bonus 76.5%), so this is the common case rather than the edge: seven of the owner's eight buy rounds were "wins" that lost money, staking $1,550,000 for $553,845 back. Gross-payout WIN readouts are the genre convention and are not wrong in themselves, but at 400x the convention inverts the meaning of the round, and it confused the person who knows the maths. Sits against guideline item 50, *"UI clearly displays bet cost and applied multiplier"*. **Options:** (a) show cost beside win on buy rounds, e.g. `WIN $57,215 / COST $200,000`, most honest and breaks no convention; (b) show a net figure for buy rounds, clearest but a reviewer expecting a WIN readout would read it as wrong; (c) show the multiplier against cost (`x0.29`) rather than against bet level; (d) leave it as conventional. **Recommend (a) with (c).** Player-money presentation, so per THE FACTS DISCIPLINE item 8 it goes to the owner and Fable rather than being decided by the builder. **Separately noted, not a defect:** the platform's own Bets panel shows COST as the bet level and MULT as payout divided by bet level, so its rows read `super $1,000.00, +$102,930.00, x102.93` for a round that actually staked $400,000 and returned 0.257x. Our confirm dialog shows the true price up front (`PRICE $200,000.00` at a $500 bet, `PRICE $400,000.00` at $1,000, both captured), so the game is not hiding the cost anywhere. **RULED 2026-07-26 (Fable, FS_V3_CONSOLIDATED JOB 2): option (a), refined.** The gross WIN readout is RETAINED, because it is the genre convention and a reviewer expecting it would read a net figure as wrong. The headline multiplier is UNCHANGED and stays against the bet level, for the same reason. What is added is a **secondary FEATURE PRICE line on the result banner of bought rounds only**, so the round states what it cost beside what it paid. Built in JOB 3 (f), driven by the same integer-micros cost source as the confirm dialog so the two cannot disagree, and routed through the social vocabulary layer so the word survives Stake.US. Base rounds are untouched. **CLOSED 2026-07-26, JOB 3(f).** Built as ruled. The line renders under the multiplier on the result banner and reads, in the real-money proof capture, `EPIC WIN $29,214.24  386x BET  FEATURE PRICE $40,000.00`: the exact confusion this row names, now legible as the loss it is. **The single cost source is literal, not intended.** `spinCostMicros(bet, mode)` in `stores/buyAffordability.ts` is now the only place that expression exists. It previously existed FIVE times over, in `BuyBonus.svelte`, `App.handleBuy`, `App.handleSpin`, `HudOverlay`'s effective-cost readout and `FeatureMenu`'s current-spin cost, which is five chances for the price a player is quoted to disagree with the price they are charged. All five now call it, and the rounding happens once against the scaled value per CLAUDE.md's integer-micros rule. `stores/boughtRound.ts` carries the round's price in INTEGER MICROS rather than a formatted string, so a currency or locale change mid-session cannot leave a stale rendering behind. **Cleared on the next SPIN rather than on banner dismissal**, deliberately: the celebration for a bought round can be raised by either WinBanner instance, the reactive base-game one or the explicit-trigger one `FreeSpinsPresentation` drives at feature end, and tying the lifetime to a dismissal handler would make the line depend on which of the two showed. **The label is authored in all sixteen locales** as `featurePrice` in `featureI18n`, not composed by concatenation: every locale already ships its own word for feature (`hudFeatures`) and its own cost-of-something phrasing (`hudSpinCost`), but joining them yields "FONCTIONS COUT DU TOUR" in French and equivalents elsewhere, so each is written in that locale's grammar taking the noun from the shipped value and inflecting it (es `PRECIO DE LA FUNCION`, fi `OMINAISUUDEN HINTA`, pl `CENA FUNKCJI`, tr `OZELLIK FIYATI`, ja and zh their own compounds). Routed through `sv()` as ruled; nothing in the label is on the restricted table today, so it is a no-op in social mode now and follows automatically if the platform's table changes. **PROVED AT BOTH SESSIONS, and on four claims rather than one.** `frontend/scripts/feature_price_proof.mjs` drives a real 400x buy at a real-money session and a real 100x buy at a social one, reaching the confirm dialog by clicking as a player does, and asserts: the line is PRESENT; its value is the SAME STRING as the confirm dialog's price, read off the dialog rather than recomputed, since a proof that recomputes the expected value only checks its own arithmetic against itself; the WIN headline is still a money amount and the multiplier line is still `Nx BET/PLAY`, so the RETAINED half of the ruling is asserted rather than assumed and a build that had quietly switched to net presentation (option (b), declined) would fail; and the label carries no prohibited term in either mode, scanned against the real term table. **The negative control is the one that matters most**: a base-game win shows NO price line, without which a build rendering the line on every round would satisfy everything above, and cost-on-every-spin is a different product from the one ruled. **Two harness corrections recorded because both were the proof being wrong rather than the build.** The first waited on the banner and timed out with the feature sitting on its deliberate CLICK TO CONTINUE gate: the round was not slow, it was waiting for a human. The second pressed that gate with `locator.click()` and every press timed out at two seconds with the button plainly visible in the capture, because the button animates continuously and Playwright's stability check never settles; the shared `clickAnyPendingGate` helper, which uses the DOM's own click and is already used by twenty other scripts, exists for exactly this. Evidence: `reports/qa/feature_price_proof_2026-07-26.json`, captures at `reports/screens/feature-price-2026-07-26/`. Social capture reads `EPIC PRIZE $11,551.28  183x PLAY  FEATURE PRICE $10,000.00` with COINS/PRIZE/PLAY throughout. |

| TR-069 | The bottom control row is clipped at the right edge at mobile portrait: a player cannot reach the rightmost control | JOB 1 screenshot deep audit, 2026-07-26 | **CONFIRMED from capture 23** | Medium, a control is unreachable | unassigned | **APPROVED for the JOB 3 fix pass** | `reports/screens/dtt-live-2026-07-26/23_mobile_portrait_TR065_scrollbar.png` shows the bottom row as menu, turbo 4x, SPIN, MAX, and then a fifth circular control rendering only a partial glyph before the viewport boundary cuts it. Same layout family as TR-065 and almost certainly the same root cause, the wrapper sizing rather than the control itself, which is why it rides JOB 3 item (b) rather than becoming its own pass. **This row is the argument for the TR-065 method constraint**: had TR-065 been closed by hiding overflow, this control would have gone from visibly clipped to invisibly unreachable, and the gate would have passed. **FIXED 2026-07-26 with TR-065, same root cause.** The layout gate measures every control against its clipping ancestors rather than only the viewport, which is what a naive viewport-only check would have missed here, and it reports **offscreen=0 at all seven presets**. |
| TR-070 | The FEATURES bar label sits at low contrast over the background art at mobile portrait | JOB 1 screenshot deep audit, 2026-07-26 | **CONFIRMED from capture 23; RESOLVED incidentally by 3(b), proven by measurement** | Minor, readability | `main` | **CLOSED** | The mobile-portrait FEATURES bar renders its label over the unmuted background image, and in capture 23 the text is legible but weak. Recorded rather than fixed blind because the TR-065 re-derivation changes what sits behind that bar, so the contrast may resolve itself; re-measure after (b) and only then decide whether a scrim is needed. **CLOSED 2026-07-26, JOB 3(g). Re-measured after (b), and the prediction held: no scrim is needed.** Worst-case contrast on the shipped build, against the real composited backdrop, across all four portrait presets: Mobile L **10.06:1**, Mobile M **7.98:1**, Mobile S **7.57:1**, Portrait 390x844 **9.05:1** for the FEATURES label, and 10.70 / 10.18 / 9.25 / 10.58 for the active-mode chip beside it. Every figure clears WCAG 2.1 AA (4.5:1 for normal text) and the lowest of them clears **AAA** (7:1). The 3(b) re-derivation resolved this incidentally, exactly as the row anticipated, so adding a scrim would have been a change made against a measurement nobody took. **Measured on COMPOSITED PIXELS rather than on CSS, because CSS cannot answer the question.** The bar's plate is `linear-gradient(160deg, rgba(255,46,196,0.1), rgba(6,9,20,0.9))`: at its start it is ninety percent transparent, so the real backdrop behind the label is whatever the background art happens to be at that point, and no stylesheet states that value. `frontend/scripts/contrast_gate.mjs` hides the glyphs with `visibility:hidden` (which keeps layout, so the backdrop is unchanged, where `display:none` would reflow the row and measure a different one), screenshots exactly the label's box, decodes that PNG back through the browser's own canvas so no image library is needed, and reports the ratio against the **WORST** pixel rather than the mean, because an average lets a small bright highlight hide inside a mostly dark region and a player does not read the average. **A broken measurement was caught by convention (l.2) before it became a finding.** The first run reported every preset failing at about 1.17:1 against a near-black backdrop, which is impossible for light text. The gate was scraping numbers out of the computed colour with a digit regex, and `.p-fm-entry`'s colour is a `color-mix()`, so the computed value arrives as CSS Color 4 `color(srgb 1 0.795098 0.942157)` whose channels run 0 to 1; read as 0 to 255 they are almost black. Rather than add a second syntax to the parser and wait for the third, the browser now paints the colour into a canvas and returns the sRGB bytes, which is correct for every colour syntax that exists or will exist. Had that been reported instead of re-derived, the pass would have 'fixed' a defect that did not exist. **A second inert-assertion catch**: the active-mode chip only renders when a non-default standing mode is selected, so the first run measured it at `present: false` on all four presets and asserted nothing at all. The gate now selects CRUISE the way a player does, by opening the menu and clicking, and an absent chip is reported rather than skipped. **Convention (p)**: a seeded violation pushes the label's colour toward its plate, the form a palette change really takes, and the gate goes red at 1.0:1 while the real label passes on the same run. CI gate 13d. Evidence: `reports/qa/contrast_2026-07-26.json`, captures at `reports/screens/contrast-2026-07-26/`. |
| TR-071 | At Popout S the game logo overlaps the frame's top-right corner | JOB 1 screenshot deep audit, 2026-07-26 | **CONFIRMED from capture 28** | Minor | unassigned | **APPROVED for the JOB 3 fix pass** | At 400x225 the FUTURE SPINNER logo and the frame's top-right corner occupy the same pixels. Not a clipping fault and nothing is unreadable, but overlapping elements are one of the four things the reviewer-tag rubric names explicitly, so it is worth the small change while that layout is already open. **RESOLVED INCIDENTALLY 2026-07-26 by the TR-065 re-derivation.** With `.canvas-slot` clipping the scene bleed and the canvas rescaled to the measured chrome height, the Popout S overlap no longer occurs; the gate reports no offscreen or overlapping control at 400x225 and `mini_player_proof.mjs` still passes `noControlsOverlap`. No bespoke change was needed, which is why it was right to re-measure before reaching for one. |
| TR-072 | Four player-visible literals are hardcoded English, and the gate that forbids them cannot see a literal on its own line | Found 2026-07-26 while driving the JOB 3(f) buy proof through the free-spins entry gate | **CONFIRMED, reproduced, and FIXED** | Medium, untranslated player-visible text in fifteen locales | `main` | **CLOSED** | Found by accident and by looking: the JOB 3(f) proof stalled on the free-spins CLICK TO CONTINUE gate, and reading the markup to find its selector showed the string was a bare English literal. **`locale_completeness_check.mjs` reported PASS with four of these shipping.** Its hardcoded-literal scan was `/>([A-Z][A-Z0-9 &'.-]{2,})</g`, which requires the literal's first character to sit IMMEDIATELY after the `>`. That holds only when an element and its text are on one line. The moment an element carries enough attributes to wrap, which is the normal formatting in this codebase, its text sits on its own line behind a newline and indentation, and the gate could not see it at all. **This is the TR-060 and TR-063 pattern for the third time**: a gate that reads one authoring form of the defect while the defect occurs in another, reporting PASS over the thing it exists to catch. **The four, all reachable by a player**: `FreeSpinsPresentation.svelte:389` CLICK TO CONTINUE, the gate every free-spins entry stops on, so every player in every locale reads English at the most-seen moment in the feature; `MaxWinCelebration.svelte` REACHED! and COLLECT, the 5,000x celebration and its own dismiss control; and `ReplayMode.svelte` PLAY AGAIN, on Bet Replay, which is a mandatory platform surface. A fifth, ThemeSelector's PLAY THIS THEME, is allowlisted with the same reason SELECT THEME and BACK already carry: the component is `import.meta.env.DEV` gated and is not rendered in production. **Fixed by authoring, not by allowlisting**: `featureContinue`, `maxWinReached`, `collect` and `replayAgain` added across all sixteen locales, composed from each locale's own shipped vocabulary rather than invented, per the TR-017 method. `featureContinue` reuses the exact phrasing already shipped as `splashPressAnywhere` (TAP TO CONTINUE), which is device-neutral and already reviewed; `maxWinReached` is the participle agreeing with that locale's own `hudMaxWin` noun; `replayAgain` takes the replay verb from that locale's `hudStartReplay`. **The gate is repaired AND now carries a seeded self-test, per convention (p)**, whose first seed is the exact form that shipped: a wrapped element with its text on its own line. Convention (p) is explicit that seeding a form the gate happens to handle teaches nothing, so the seeds are the real forms, five of them, plus a negative control proving translated markup still passes. `src/App.svelte` was added to the scanned set for the same reason TR-063 added it to the dash gate: a file list is a list, and the one file not on it is where the defect was. **Not closed by this row, and deliberately**: the sentence-case English prose in the same components (`Press COLLECT or hit Enter to continue`, `Replaying round...`) is TR-059's recorded scope, which is parked as a pass of its own of roughly thirty keys times sixteen locales. This row closes the class the gate CLAIMED to cover. Gate output: 24 files scanned, 0 unexplained literals, 5 of 5 seeded violations caught, negative control clean, `npm run check` 0 errors at the committed baseline. |

### Track rows (screenshot-analyst, ingested 2026-07-26)

Promoted by the integrator from `docs/records/screenshots/FINDINGS_LEDGER.md`,
which `track/screenshot-analyst` owns and which the integrator alone promotes
from, per that track's brief. The ledger's SA numbers are kept in the row so the
two records stay traceable to each other.

The track's other twelve ledger rows are NOT promoted: nine are NOT-A-DEFECT
answers to questions that are now closed, two were PROCESS rows already resolved
by the CI triage session (SA-012, SA-013), and one is an observation about
another team's directory (SA-015). Promoting an answered question would make the
tracker longer without making it more useful. They stay readable in the ledger.

| ID | Finding | Source | Disposition | Severity | Branch | Status | Fix evidence |
|---|---|---|---|---|---|---|---|
| TR-073 | The 5,000x max win has fired in live play and the celebration overlay it should present has never been captured | `track/screenshot-analyst` ledger SA-018, 2026-07-26 | **CAPTURED via Bet Replay, 2026-07-26** | Medium, a submission-facing presentation claim resting on inference | `main` | **CLOSED** | On 2026-07-26 a `super` round at a `EUR 750.00` bet level paid `EUR 3,750,000.00`, exactly 5,000.00x, cross-checked between our HUD (`reports/screens/screenshot-analyst-2026-07-26/10_eur_max_win_3750000_exactly_5000x_at_bet_750.png`) and the platform's own row reading `x5000.00` (`11_bets_panel_super_5000x_wincap_row_and_buy_tiers.png`). Frame 10 is the aftermath twelve seconds later: reels at rest, the win in the HUD, no overlay. **Specification says it should have presented**, and `$isWincap` is computed against the bet level at both `gameStore.ts:158` and `roundInterpreter.ts:265`, which this round cleared exactly, so the expected reading is that it fired and was collected inside those twelve seconds. **That is inference and it is not good enough for a submission claim**, which is why this is parked rather than closed either way, per convention (l.6). **Closing it is one capture and does not need another 1-in-100,000 round**: Bet Replay is mandatory and working (`reports/screens/dtt-live-2026-07-26/37_REPLAY_WORKING_event_52121_with_disclaimer.png`) and `ReplayMode.svelte:266` renders the same component, so the round replays from its event id. Written into the owner's walkthrough as Part 9b observation 6. Note the overlap with TR-072, which translated `maxWinReached` and `collect` across sixteen locales without anyone having seen the surface those strings render on. **CLOSED 2026-07-26 by the TR-076 proof**: the shipped `super` cap book round (payoutMultiplier 5000, the round shape of live event 22975) replayed through `ReplayMode` in the production build presents `MaxWinCelebration` in full: MAX WIN REACHED!, 5,000x BET, and a COLLECT control proven really clickable by a real Playwright click (`reports/screens/replay-blocker/05_super_wincap_maxwin_celebration.png`, `06_super_wincap_after_collect.png`; `frontend/scripts/replay_blocker_proof.mjs` pass 3). Same component and interpreter path as live play (`ReplayMode.svelte` renders `MaxWinCelebration` directly), so the presentation claim no longer rests on inference. **What this capture is not**: a replay of live event 22975 itself; the real game UUID lives only in the owner's portal, so the round served was the shipped cap fixture, a real book round of identical shape. The owner's one-click live confirmation via the Bets panel Replay button after the next frontend upload stays listed as belt and braces, not as the closing evidence. |
| TR-074 | The scene is visible through unfilled reel cells during a spin, and the idle board is opaque | `track/screenshot-analyst` ledger SA-019, 2026-07-26 | **CONFIRMED as an observation, NOT confirmed as a defect** | Low | unassigned | **OPEN** | In `reports/screens/screenshot-analyst-2026-07-26/12_cruise_badge_bet_1000_matches_cost_column.png`, taken mid-spin, reels 1 and 2 are populated, reel 3 holds two symbols and reels 4 and 5 hold one each, and the city scene behind the game shows through the cells not yet filled. Reels populating left to right is ordinary. What makes it a row is that the idle board in frame 10 is fully opaque across all twenty cells, so the two states genuinely differ, and a reviewer scrubbing a spin sees the difference. **Deliberately not called a defect**: there is no settled-state capture at the same viewport to compare against, and the game's whole aesthetic is a translucent neon frame over a city scene, so a translucent viewport may well be intended. One settled-state frame at that viewport settles it, and until there is one this row states an observation rather than a fault. |
| TR-075 | `cruise` is the one mode whose wallet debit has never been measured | `track/screenshot-analyst` ledger SA-006, 2026-07-26 | **CONFIRMED as an evidence gap, partially closed** | Low | unassigned | **OPEN** | The between-frame re-sweep proved four of five mode costs against the live wallet to a residual of 0.00: `base` by a 524 spin session panel, `antelite` by frames 03 to 04, `bonus` by frames 41 to 42, `super` by frames 46 to 48 (`reports/qa/live_stats/2026-07-26_mode_cost_reconciliation.md`). **`cruise` is confirmed at DISPLAY level only**: in `12_cruise_badge_bet_1000_matches_cost_column.png` the HUD BET and the platform COST column both read `EUR 10.00`, and since the HUD renders the effective debit (ledger SA-001) while COST carries the bet level (SA-002), the two being equal means the multiplier is 1.0. **That is an inference from two other results, not a measurement**, and convention (l.4) requires saying which results it leans on rather than presenting it as independent. `cruise` has exactly one balance anchor, `EUR 921.50`, and no second frame, so the differencing the other four modes got cannot be done. **Closing it is one short `cruise` run with a session panel captured before AND after**, written into the owner's walkthrough as Part 9b observation 7. |
| TR-076 | BLOCKER: on the live platform Bet Replay launches, the board renders static, and START REPLAY sits at the bottom as an unclickable shadow; nothing plays. Reproduced by the owner on `super` event 22975 (EUR) and on a fresh bet | Owner, live platform, 2026-07-26; brief `reports/briefs/FS_REPLAY_BLOCKER_Prompt.md` | **CONFIRMED, reproduced locally, FIXED** | Blocker, Bet Replay is a mandatory approval requirement | `main` | **CLOSED** | **Reproduced locally at the exact live parameter shape** (every documented query parameter from `docs/stake-engine-live/game-replay-requirements.md`, EUR, amount in micros, mode SUPER, event 22975) against the production build with the RGS replay endpoint served the shipped `super` cap book round: a real Playwright click on START REPLAY was intercepted, and `document.elementFromPoint` at the button's centre returned `.bg-still-container`. **Root cause: `App.svelte`'s fixed `.bg-layer` (z-index 0) paints and hit-tests ABOVE unpositioned content, and `ReplayMode`'s `.replay-container` was unpositioned**, so the backdrop sat over the entire replay UI and swallowed every click; `.game-stage` (z-index 2) covers the backdrop in normal play, which is why only replay mode was exposed. Third appearance of the full-viewport-layer-intercepts-input class (HeroSplash first). **Fixed at root, both directions**: `.bg-layer` is decoration and now carries `pointer-events: none` so it can never take a hit in any mode, and `.replay-container` is positioned at z-index 2, the same relationship the game stage has. Neither file is locked. **Proof: `frontend/scripts/replay_blocker_proof.mjs`, 7 of 7 green**, including the convention (p) seeded pass, which re-introduces the exact shipped CSS into the served page and goes RED (the real click is intercepted by `.bg-still-container` again); the live-shape base-win pass asserts the presentation actually runs (reveal animates, the win counts up across 6 distinct frames to a non-zero total) and that every displayed amount is euro-formatted (`Bet: €10.00`, no raw code, no NaN); the `super` cap pass is TR-073's capture. Evidence `reports/screens/replay-blocker/01..06`; frame 01 is the seeded defect showing the owner's exact symptom. **Note on numbering: the brief says "closing TR-075 with the capture"; TR-075 is the cruise debit row, and the wincap-celebration evidence gap it describes is TR-073.** Recorded rather than silently renumbered, per convention (n)'s surface-the-tension rule. |

### Live shape rows (devtools captures, ingested 2026-07-26)

The owner opened DevTools on a live Stake Engine session and captured the Network
panel with the `wallet` filter applied. These are the first captures of the REAL
authenticate, play and end-round bodies. Everything below is read off those
frames; where a frame does not show something, the row says so rather than
filling the gap.

| ID | Finding | Source | Disposition | Severity | Branch | Status | Fix evidence |
|---|---|---|---|---|---|---|---|
| TR-077 | The live `play` response nests its events at `round.state` as a bare ARRAY, exactly as the parser reads it | Owner devtools capture, 2026-07-26 | **CONFIRMED against the live wire** | n/a, a confirmation | `main` | **CLOSED** | `reports/screens/live-shapes-2026-07-26/05_play_response_state_is_event_array_micros.png` shows the response verbatim: `round.betID` 1055919443, `round.amount` 1000000, `round.payout` 800000, `round.payoutMultiplier` 0.8, `round.active` true, `round.mode` "base", and `round.state` opening directly into `[ { "index": 0, "type": "reveal", "board": [...] } ]`. So `state` IS the event array, not an object containing one, which is what `_extractEvents` assumes. **This is the first first-party confirmation of the schema TR-009 rewrote the parser onto**; until now it was held by the pinned types and by decoded book rows. The board rows carry SIX cells against a five-reel visible grid, which is the padding row the CLAUDE.md worked example records, seen here on the live wire for the first time. |
| TR-078 | Live money fields are integer micros, and the HUD figure reconciles to them exactly | Owner devtools capture, 2026-07-26 | **CONFIRMED against the live wire** | n/a, a confirmation | `main` | **CLOSED** | `reports/screens/live-shapes-2026-07-26/04_authenticate_response_jurisdiction_TOP_LEVEL.png` shows `balance.amount` 996800000 with `currency` "EUR", and the game HUD in the same frame reads `EUR 995.06` after the round in flight. `reports/screens/live-shapes-2026-07-26/06_end_round_response_balance_only.png` shows the end-round response as `{"balance":{"amount":995060000,"currency":"EUR"}}`, and 995060000 micros is exactly the `EUR 995.06` on screen. `CURRENCY_SCALE` is 1,000,000, so the integer-micros rule holds on the live wire in both directions. |
| TR-079 | The live `end-round` response carries balance ONLY, with no round identity | Owner devtools capture, 2026-07-26 | **CONFIRMED against the live wire** | n/a, a confirmation | `main` | **CLOSED** | `reports/screens/live-shapes-2026-07-26/06_end_round_response_balance_only.png` is the whole response on one line: `{"balance":{"amount":995060000,"currency":"EUR"}}`. No `roundId`, no `betID`, nothing else. This is what the TR-009 rewrite assumed when it replaced the invented `{balance: number, roundId}`, and it is now confirmed rather than inferred. The same frame's request list also shows `play` entries with no following `end-round`, confirming that a zero-win round settles without one. |
| TR-080 | The live authenticate carries `jurisdiction` at TOP LEVEL; the parser reads `config.jurisdiction` | Owner devtools capture, 2026-07-26 | **PARKED, sanction request. Defect REACHABLE, not confirmed live** | Medium if reachable: a jurisdiction that disables a control would be read as permitting it | unassigned, LOCKED FILE | **OPEN** | `reports/screens/live-shapes-2026-07-26/04_authenticate_response_jurisdiction_TOP_LEVEL.png` shows the response tail: a modes array closes, then `user`, then `jurisdiction` with all twelve flags, then `balance`, then the root brace. `jurisdiction` is therefore a sibling of `balance`, not nested under `config`. `rgsService.ts:555-559` reads `config.jurisdiction` only. **Proven against the SHIPPED function, not a copy**: `frontend/scripts/live_shape_conformance.mjs` serves the captured body from a local HTTP server to the real exported `authenticate()`, so it cannot agree with the parser by sharing its mistake (convention l.4). Result at `reports/qa/live_shape_conformance_2026-07-26.json`. **What is NOT proven, and it matters**: the frame shows the response TAIL only, so whether a `config` block further up ALSO carries a jurisdiction copy is unknown. Case A (no copy) reproduces the defect; case B (a copy) shows the parser already correct. Both are run. Independent evidence that `config` itself exists: ledger SA-020 observed live bet levels 450, 500, 750 and 1,000, which can only have come from `config.betLevels`. **One screenshot settles it**: the authenticate response scrolled to the TOP. **The fix, verified by running it, is one line**, and it is parked as a sanction request because the file is locked and this brief grants no exception. See the parked-rows table. |
| TR-081 | Four `authenticate` requests in one session, one of them red, and four console errors | Owner devtools capture, 2026-07-26 | **NOT DIAGNOSABLE from the captures supplied** | Unknown until the console is captured | unassigned | **OPEN** | `reports/screens/live-shapes-2026-07-26/05_play_response_state_is_event_array_micros.png` and `06_end_round_response_balance_only.png` both show four `authenticate` entries in the filtered request list with the second marked by a red error icon, and a red `4` badge on the DevTools console tab. **What is derivable from code**: production has exactly two `authenticate` call sites, `rgsService.initRGS()` at line 728 and `sessionRecovery.recoverSession()` at line 174, and `authenticate` is NOT wrapped in `_withRetry` (only `play` and `endRound` are), so our client never retries it. Four requests therefore means at least two session boots, not a retry storm. **What is NOT derivable**: the cause of the red one and the content of the four console errors, because no frame shows the Console panel; every frame has the AI assistance tab open in that slot instead. Recording the guess would be the (l.6) violation. **Two captures settle it**: the Console tab with the four errors expanded, and the red `authenticate` row's own Headers and Response tabs showing its status code and body. |

## Parked rows

Per the operating rules, a finding without a clean answer gets two or three options with
trade-offs in its row and is marked PARKED rather than stalling the wave.

| Row | Why parked |
|---|---|
| TR-080 | **SANCTION REQUEST, awaiting the owner.** The fix is one line in the locked `frontend/src/lib/services/rgsService.ts`, line 558: change `...(config.jurisdiction ?? {}),` to `...(config.jurisdiction ?? raw.jurisdiction ?? {}),`. It is tolerant rather than a swap: `config.jurisdiction` still wins where it exists, the top-level block is used where it does not, and where neither exists the behaviour is unchanged. All three cases are RUN, not argued, in `frontend/scripts/live_shape_conformance.mjs`. **Option (a)**, take the sanction and ship the one line: costs one locked pass, closes the class, and is safe under both shapes. **Option (b)**, capture the authenticate response head first and only sanction if case A is real: costs one screenshot and risks nothing, but leaves the gap open meanwhile. **Option (c)**, do nothing: only defensible if case B turns out to be real, and nobody knows yet. **Recommendation: (b) then (a)**, because the screenshot is already on the owner's list and it converts a maybe into a fact before a locked file is opened. |
| TR-020a | CLOSED 2026-07-25 under ruling 23. Not a product bug: the crossfade fires correctly and the counters prove it. Three harness defects fixed, all traceable to the pinned `?mockCategory` leaking into rounds it was never meant to govern. |
| TR-026 | Antelite tail concentration: passes every gate, maths-adjacent, an author's volatility choice rather than a defect. |
| TR-027 | Art direction: the two reviewers **directly contradict each other** from the same proofs. Not a builder call. |
| TR-047 (PAR component only) | The PAR sheet's six-plus-scatters line is inside `games/future_spinner/**`, whose deny lines the round two brief does not lift. Three options with trade-offs are recorded in the row; (b), a narrow sanction for the single documentation line, is the recommendation. The dossier and bundle-size components of TR-047 are unlocked and are not parked. |

## Score gap, for Fable

### Round two

The spread is **1.33 again**, and it has inverted. Round one's most reliable reviewer scored
0.67 and its least reliable gave no number; round two's spread is 2.00, 1.67 and 0.67, and
the **0.67 is now the reliable one**. Every R2R3 finding tested this session held: the
`sessionID` blocker, the replay legacy search, the three named social surfaces, the
jurisdiction flag names, and `npm run check` reproduced figure for figure. R2R1 at 2.00 is
also reliable but reviewed a narrower surface: its four MAJORs are all already on the
tracker, so it found nothing new, which is what a 2.00 from a careful reviewer who did not
open the wallet contract looks like. R2R2 at 1.67 is again unusable, for the second round
running, on the same failure mode.

The honest reading is that the true score is nearer R2R3's than the average of the three.
The average, 1.45, is arithmetic over one measurement, one narrower measurement and one
description of a different codebase.

### Round one

The two scoring reviewers are **1.33 apart** (0.67 reject versus 2.00), which is not a
matter of taste. Review 1 found the absent event books (TR-023) and the live event
contract mismatch and scored accordingly; review 3 did not open the publish directory,
relied on our own compliance reports as its maths reference, and explicitly recorded
that limitation. Review 1's findings are, on the evidence checked here, the more
reliable: every one I have tested independently has held, including the books.

Review 2 cannot be reconciled with either, because its findings cite files that do not
exist (TR-032, TR-034) and its arithmetic does not reproduce (TR-033).

## Wave plan

| Wave | Branches | Theme |
|---|---|---|
| 1 | `fix/R10-type-zero`, `fix/R12-evidence-hygiene`, `fix/R9-scatter-disclosure`, `fix/R14-popout-refresh`, `fix/R6-locale` | Quick wins, low-hanging fruit first per the owner |
| 2 | `fix/R4-social-currency`, `fix/R5-bet-levels`, `fix/R7-rg-enforcement`, `fix/R8-modal-affordability` | Enforcement cluster |
| 3 | `fix/R1a-rgs-locked-pass`, `fix/R1-event-contract`, `fix/R2-mock-containment`, `fix/R3-books-equality`, `fix/R11-session-recovery` | Integration core |
| then | `feature/scatter-anticipation` | Motion quality, under the visible-state-only integrity ruling |

## Date drift, noted once (Fable ruling 4, 2026-07-25)

Commit dates are authoritative. **Today is 2026-07-25**, and git agrees: the whole of wave 1 and wave 2 was committed on 2026-07-25. The 2026-07-26, 2026-07-27 and 2026-07-28 labels carried by several recent briefs and by the ruling blocks quoted throughout this tracker were drift in the briefs themselves, owned by Fable on 2026-07-25.

**Nothing is relabelled retroactively.** Those dates stay exactly as written wherever they already appear, because rewriting them would falsify the record of what each document actually said when it was issued, and because the ruling says so. This note is the single place the discrepancy is reconciled: where a document says 2026-07-26 to 2026-07-28 and git says 2026-07-25, git is right and the label is a brief-side artefact. Every document from here uses the true date.
