# Fix list from the live portal sessions, 2026-07-26

Owner instruction: *"we need to have a list of potential fixes that need to happen as well,
from what we spotted from the screenshots"*. This is that list, everything found across the
day's three portal sessions, ordered by what should be done first. Each item names its
tracker row, its evidence, and the size of the job.

**Read the two "not a bug" findings first.** The two things the owner flagged this session
are both real observations with correct code underneath, and acting on them as if they were
bugs would be a mistake.

---

## A. The two things flagged this session

### A.1 The bet cost. Our game is right; the platform's Bets panel is confusing.

The owner: *"For the bonus buys, it's showing us 200,000 or 400,000, but then it's only
showing us the bet price is 500 or 1,000."*

**Both numbers are correct, and they are measuring different things.** The platform's Bets
panel row shows **COST = the bet level**. The expanded detail shows **Cost (USD) = what was
actually debited**. For the 06:25:18 round the row reads `super $1,000.00` and the detail
reads `Cost (USD) $400,000.00`, `Cost multiplier x400.00`. Super costs 400x, so a $1,000 bet
level costs $400,000. That is what our maths specifies and what the platform charged.

**Our own confirm dialog shows the true price up front**, verified in two captures: at a $500
bet the NITRO OVERDRIVE dialog reads `PRICE $200,000.00`, and at $1,000 it reads
`PRICE $400,000.00`. So the game is not hiding the cost anywhere.

**The trap is the platform's MULT column, and it is worth understanding.** It is
payout divided by the **bet level**, not by what you paid:

| Mode | Bet | True cost | Payout | Platform MULT | Actual return on cost |
|---|---|---|---|---|---|
| super | $1,000 | $400,000 | $102,930 | **x102.93** | **0.257x** |
| super | $1,000 | $400,000 | $60,270 | x60.27 | 0.151x |
| super | $500 | $200,000 | $50,395 | x100.79 | 0.252x |
| super | $500 | $200,000 | $72,800 | x145.60 | 0.364x |
| super | $500 | $200,000 | $57,215 | x114.43 | 0.286x |
| bonus | $500 | $50,000 | $144,350 | x288.70 | **2.887x** |
| bonus | $500 | $50,000 | $21,125 | x42.25 | 0.422x |
| bonus | $500 | $50,000 | $44,760 | x89.52 | 0.895x |

A green `+$102,930.00` at `x102.93` looks like a large win. It was a **loss of $297,070**.
Across all eight buy rounds the owner staked $1,550,000 and got back $553,845, a 35.7%
return, which is a perfectly ordinary run given super's break-even rate of 71.8%.

**No fix on our side. Nothing to change.** Recorded so it is not re-investigated.

### A.2 The balance. It was updating, correctly, every time.

The owner: *"the balance wasn't updating"*.

I reconciled every balance in the captures against the platform's own bet log, to the cent:

| Time | Expected | HUD showed | |
|---|---|---|---|
| 06:20:58 | $49,917,330.00 | $49,917,330.00 | exact |
| 06:22:36 | $49,972,875.00 | $49,972,875.00 | exact |
| 06:23:37 | $49,830,090.00 | $49,830,090.00 | exact |
| 06:28:31 | $48,916,485.00 | $48,916,485.00 | exact |

Every figure derived independently from the bet log using the true per-mode cost. **The
money path is correct, including the 400x super debit.** Confirmed in code too:
`App.svelte:1002` computes `cost = bet * MODE_COST[mode]`, and the same `cost` feeds both the
balance path and the session panel's Total wagered, so neither can drift from the other.

**What the owner actually saw is real, though, and it is item B.1 below.** At 06:23:37 the
HUD reads `WIN $57,215.00` in large green type while the balance falls by $142,785. The
balance did update; it went **down** on a round the HUD headlines as a win.

---

## B. Confirmed defects, ours to fix

### B.1 Buy-mode rounds present a loss as a win (NEW, TR-068)

**Priority: high, and it is the most player-facing thing on this list.**

The WIN panel shows the round's gross payout. For base play that is fine and conventional.
For buy modes it is actively misleading, because **the typical buy round pays back less than
it cost**: super's break-even rate is 71.8% and bonus's is 76.5%, so roughly three rounds in
four are "wins" that lose money. Seven of the owner's eight buy rounds were.

Showing gross payout is the industry convention and is not wrong in itself. The problem is
that at 400x it makes the common case read as the opposite of what happened, and the person
it confused was the owner, who knows the maths. It also sits directly against guideline item
50, *"UI clearly displays bet cost and applied multiplier"*.

**Options:**
- (a) Show the cost alongside the win in the buy-round result, for example
  `WIN $57,215 / COST $200,000`. Most honest, smallest change, no convention broken.
- (b) Show a net figure for buy rounds instead of gross. Clearest, but breaks the genre
  convention and would look wrong to a reviewer expecting a WIN readout.
- (c) Show the multiplier against cost (`x0.29`) rather than against bet level.
- (d) Leave it, on the grounds that it is standard.

**Recommend (a)**, with (c) as a cheap addition. Fable to rule, since it is player-money
presentation and therefore a (l.8) escalation rather than a builder call.

### B.2 The game frame scrolls on every responsive layout (TR-065, guideline 15)

`App.svelte:1821` gives `.game-wrapper.portrait`, `.compact-landscape` and `.mini-player`
`overflow-y: auto`. Scrollbars visible in captures at mobile portrait, Mobile S and Popout S,
absent on desktop, which is exactly that class list. **One CSS rule**, but check why `auto`
was chosen so the fix clips nothing.

### B.3 Double-tap zoom is not disabled (TR-066, guideline 25)

`frontend/index.html` has no `maximum-scale` or `user-scalable=no`, and `touch-action`
appears nowhere in `frontend/src`. Note the meta tag alone is ignored by modern iOS Safari,
so the durable fix is `touch-action: manipulation` on interactive surfaces. **Small.**

### B.4 The mini-player WIN readout clips mid-glyph (TR-066)

At Popout S the strip renders `$0.0` plus a cut character, and the balance drops its cents
(`BAL $1,040` where the true balance is $1,040.06). The dropped cents may be intentional at
400px; the clipping is not. **Small, but it is in the sizing that has already been tuned
twice, so measure rather than nudge.**

### B.5 Social Mode never forces English (TR-067, guideline 46)

`i18n/tr.ts:14` derives from `[locale, isSocial]` but social selects only the vocabulary
variant, never the locale, so a social session with `lang=de` renders German. **One line**,
pending a ruling on whether to force or to treat the guideline as a statement about platform
behaviour.

### B.6 The published bundle is a commit behind (TR-062)

The V2 re-upload sent four images and did not rebuild, so the em dashes TR-060 removed are
still shipping and still visible in the owner's paytable capture. **The fix is a fresh-clone
rebuild**, which should carry B.2 through B.5 in the same bundle. Also: nothing in `dist/`
records which commit built it, which is why this needed grepping to establish.

### B.7 Player-facing prose is not localised (TR-059)

Roughly 30 keys times 16 locales plus the `fsModes.ts` strings. **A pass of its own**, not a
patch. Now cheaper than it was, because the DTT's Local Testing redirect URL lets it be
tested against real RGS from a local dev server.

### B.8 The dash gate cannot see the string form it was built for (TR-063)

It reads single-quoted JavaScript literals only, and the strings it was written to catch were
markup prose. It also does not scan `App.svelte`, which ships an em dash in the document
title. **Recommend moving the gate onto `dist/`** so it checks what actually ships, which
would also have caught B.6.

---

## C. Escalated, must not be fixed blind

### C.1 Zero-win end-round: the client and the checklist contradict each other (TR-064)

Guideline item 12 says zero-win bets must not send an end-round request. Our gate is the
round's `active` flag, changed deliberately under the JOB 4 sanction because the official
client says *"Only call this API if Play() has returned an Active result"*. **Reverting to a
win-based gate would reintroduce the defect that sanction closed.** One Network-tab capture
on a zero-win spin settles it. **Highest-value single observation left in the project.**

---

## D. Not ours

### D.1 The platform silently dropped four files on the first upload (TR-061, resolved)

Worth reporting upstream. Caught only because the walkthrough told the owner to compare the
panel count against 108.

### D.2 The Bets panel's COST and MULT columns (A.1 above)

A platform display convention. No action beyond knowing it.

---

## E. Owner actions, not build work

1. **Delete `math/HASHES.txt`** so the MATH panel reads twelve files.
2. **Upload the game thumbnail** in the Tile Editor. The portal still shows the "Design
   Thumbnail" placeholder in every capture taken today.
3. **Capture the full DTT Language list**, scrolled, to size TR-059 against the platform's
   real locale set. We ship 16; the platform lists at least 17.
4. **Run the remaining DTT observations**, starting with C.1.

---

## Suggested order

**One rebuild clears most of this.** B.2, B.3, B.4 and B.5 are all small, self-contained
frontend changes touching no locked file and no maths. Do them together, rebuild from a fresh
clone, and that single upload also clears B.6.

1. Get the ruling on C.1 and B.1.
2. Fix B.2, B.3, B.4, B.5 in one pass.
3. Fix B.8 so the gate would catch B.6 next time.
4. Fresh-clone rebuild, re-upload, republish. Clears B.6.
5. B.7 as its own pass.
6. B.1 once ruled on.

---

# DISPOSITIONS AT HEAD, refreshed 2026-07-27

**Why this section exists.** The list above was written on 2026-07-26 and has not been
touched since, so most of its rows describe a state that is two days and roughly thirty
commits out of date. The brief for this pass requires that **no row carries no current
disposition**. Rather than editing the original rows, which would destroy the record of what
was believed at the time, the original is left intact and every row is re-dispositioned here
against HEAD.

Method: each row was checked against `docs/records/reviews/REVIEW_TRACKER.md` and against
`git log`, not against the session report's summary of them, because a report is a summary
and the row is the record.

## Section A, the two not-a-bug findings

| Row | Disposition at HEAD |
|---|---|
| **A.1** The bet cost | **CURRENT, and strengthened from inference to observation.** No longer three modes behaving alike: the platform publishes `costMultiplier` as its own field, and three sessions reconcile to exact round openings under the true costs and to nothing round under the alternative. `SUBMISSION_DOSSIER.md` section 9d and `docs/records/MONEY_DISPLAY_EXPLAINED.md`. **One thing in the row is NOT current**: it says "recorded so it is not re-investigated", but ledger rows SA-002 and SA-007 still await a ruling on whether to raise the convention with the platform before submission. That is an open decision, not a closed one. |
| **A.2** The balance was updating | **CURRENT.** Four HUD balances reconciled to the cent against the platform's own bet log. The row's forward pointer to B.1 is now itself stale, because B.1 is closed. |

## Section B, the confirmed defects

| Row | Disposition at HEAD |
|---|---|
| **B.1** Buy rounds headline a loss as a win (TR-068) | **WAS: awaiting a ruling. NOW: RULED AND BUILT.** Option (a) with (c): a FEATURE PRICE line on bought rounds, from one cost source, with the WIN headline and the multiplier line retained, plus a negative control that a base-game win shows no price line. Commit `17679ce`. |
| **B.2** The frame scrolls on every responsive layout (TR-065) | **WAS: open, "one CSS rule". NOW: FIXED, and the diagnosis was different from the guess.** The root cause was two stale constants, not `overflow`: the portrait wordmark was assumed 28px and is 42, the HUD slot was assumed 290px and needs 287. Both are runtime measurements now. Commit `c5e3bf9`. **Tracker Status cell corrected 2026-07-27**: it read OPEN while the same row's fix evidence said FIXED. |
| **B.3** Double-tap zoom not disabled (TR-066) | **WAS: open. NOW: FIXED**, `touch-action: manipulation` on `html, body`, verified present in the shipped dist CSS. Deliberately not the viewport-meta route, since iOS Safari has ignored `user-scalable=no` since version 10. Commit `c2e7bf6`. |
| **B.4** The mini-player WIN readout clips (TR-066) | **WAS: open. NOW: FIXED at 400x225, AND SUPERSEDED BY A WORSE DEFECT ON THE SAME SURFACE.** Commits `4519b46` then `e4bfbc5`. The new one is **TR-086** (promoted from ledger SA-021 by the integrator, 2026-07-27): below about 390 css px the BALANCE is cut with no ellipsis, so a player holding EUR 479,710.00 is shown `BAL €479`. TR-066 is correctly closed at the one viewport it was proven at; the finding is that `mini_player_proof.mjs` tests exactly one viewport, so the class it proves closed is closed at exactly one width. |
| **B.5** Social Mode never forces English (TR-067) | **WAS: awaiting a ruling. NOW: CLOSED**, option (a) approved: social sessions force `en` before first paint, held by a test for a social `da` launch that exercises the forcing and the unshipped-locale fallback in one case. Commit `e9bf3f6`. |
| **B.6** The published bundle is a commit behind (TR-062) | **STALE AS A ROW, LIVE AS A CONDITION.** The tracker row is CLOSED both halves: `dist/` now carries `build-info.json` with commit SHA and byte total, and `kit_build.mjs` refuses an unpushed HEAD or a dirty `frontend/`. Commits `7dd83e6` and `a1ff78b`. **But the underlying condition recurs and is now worse**: four kits have been built since, and the last frontend version confirmed published in the repository is Front V2. **Which version is live at HEAD is not known**, and that is an owner item, not a builder one. |
| **B.7** Player-facing prose is not localised (TR-059) | **CURRENT, and now measured rather than estimated.** Formally PARKED 2026-07-26 pending the full platform language capture. The 2026-07-27 machine-tell sweep counted it exactly: **27 static player-facing attributes and 48 markup text nodes**, reducing to roughly 35 keys across 16 locales, listed in full at `docs/QUALITY_CHARTER.md` 4.3 so the surgical pass needs no rediscovery. Extracted rather than absorbed, per protocol rule 6. |
| **B.8** The dash gate cannot see the string form it was built for (TR-063) | **WAS: recommendation pending. NOW: CLOSED, full chain verified**, adopting exactly the recommendation. `dash_gate.mjs` replaces `player_string_dash_check.mjs`, CI runs the seeded self-test as its own step, and **the dist scan immediately earned its place** by finding 17 em dashes in a shipped provenance README. This ruling created CLAUDE.md convention (p). Commits `1334934` and `6f20f3d`. **Tracker Status cell corrected 2026-07-27**: it read OPEN while its fix evidence said CLOSED. |

## Section C, escalated

| Row | Disposition at HEAD |
|---|---|
| **C.1** Zero-win end-round, client versus checklist (TR-064) | **CURRENT as a disposition: ESCALATED, ruled 2026-07-26 to OBSERVE FIRST with no code change.** Guideline item 12 is still the only CONFLICT of 58. **Two things this pass flags rather than resolves.** First, **TR-079 already carries a bearing observation nobody has joined up**: the same live frame's request list shows `play` entries with no following `end-round`. If those rounds were zero-win, item 12 is satisfied and TR-064 closes; if the frame cannot show they were zero-win, it does not, and saying so is the honest answer. Second, **the observation has fallen off the owner's live walkthrough entirely.** The project's own "highest-value single observation left" is no longer asked of anyone. Carried into `OWNER_CHECKLIST.md`. |

## Section D, not ours

| Row | Disposition at HEAD |
|---|---|
| **D.1** The platform silently dropped four files (TR-061) | **RESOLVED.** The owner re-dragged the same folder and the portal's own sync dialog read `Upload 4 Files, Skip 104 Files`, naming exactly the four the diff had identified. Cause: the first upload handed the portal 108 and the portal stored 104, dropped platform-side with no error surfaced. **Tracker Status cell corrected 2026-07-27**: it read OPEN while its evidence said RESOLVED. **Still not done: the upstream report.** No record of it exists anywhere in the repository. |
| **D.2** The Bets panel COST and MULT columns | **PARTIALLY CURRENT.** The convention is confirmed and now proven from the payload. But "no action beyond knowing it" is not the settled position: SA-002 and SA-007 explicitly ask the integrator to decide whether it needs saying to the platform before submission, and have been waiting since 2026-07-26. |

## Section E, owner actions

| Row | Disposition at HEAD |
|---|---|
| **E.1** Delete `math/HASHES.txt` | **STALE, AND EXPLICITLY REVERSED.** The kit walkthrough now says it can stay: it is a small notes file, it is harmless, and chasing it is not worth a step. Landed with the kit V4 walkthrough rewrite, commit `dd331da`. |
| **E.2** Upload the game thumbnail in the Tile Editor | **CURRENT, and the repository contradicts itself about it.** The guidelines self-assessment says the portal still shows the Design Thumbnail placeholder in every capture, so it is not yet done. The kit walkthrough tells the owner "The game tile is done" in all three visit sections. The charitable reading is that the walkthrough means the artwork is composed rather than uploaded, but **as written it will read to the owner as done**. Also stale: the self-assessment row names only the BG and FG files and does not know the composed 408x546 portrait tile exists. Carried into `OWNER_CHECKLIST.md`. |
| **E.3** Capture the full DTT Language list, scrolled | **CURRENT AS A NEED, STALE AS AN INSTRUCTION.** TR-059 is parked pending exactly this capture, but the instruction lives only in a superseded part of the walkthrough and does not appear in either current visit section. **It has silently dropped off the live owner list.** |
| **E.4** Run the remaining DTT observations, starting with C.1 | **STALE AS WRITTEN.** The current owner list is three items in one walkthrough part plus four eyeball items in another. C.1 is not among them, and neither is E.3. Of the four cheap captures the analyst asked for, only the Cruise run made it onto the list. |
| **Suggested order**, six steps | **MOSTLY STALE.** Step 1: B.1 ruled and built; C.1 ruled to observe-first and not yet observed. Steps 2 and 3: done. Step 4: the rebuild machinery is done and four kits built, but the re-upload and republish are unconfirmed. Step 5: not started, now parked and measured. Step 6: done. |

## Tally against the brief's requirement

Eighteen items. **All eighteen now carry a disposition current at HEAD.** Seven were stale
and are re-dispositioned above (B.1, B.2, B.3, B.5, B.8, E.1 and the suggested order); three
were stale in part (B.4, B.6, E.4); six were current (A.1, A.2, B.7, C.1, D.1, E.2); two were
current with a caveat now stated (D.2, E.3).

**Three tracker Status cells were corrected in the same pass** (TR-061, TR-065, TR-063): each
read OPEN while the same row's own fix evidence recorded the fix. That is a record-keeping
defect rather than a code one, and it matters because the Status column is what a reviewer
scans.

**Two ledger rows were promoted to tracker rows**, which is the integrator's job under the
screenshot-analyst track's own brief: SA-021 became **TR-086** and SA-022 became **TR-087**.

---

# RECONCILIATION TO HEAD, 2026-07-28

Appended rather than rewritten, because this document is a dated record of what three portal
sessions found and editing it in place would destroy that. What follows is where those items
and everything since actually stand at HEAD.

## Everything from the 2026-07-26 list

Reconciled in full by `docs/records/V7_RECONCILIATION.md`, which is the definitive
audit-to-shipped ledger: sixty six findings, each with its fixing commit, each tested for
ancestry against the kit build SHA, and each confirmed at the tree rather than by ancestry
alone. **47 SHIPPED IN V7, 1 FIXED POST-V7, 11 OPEN, 7 NOT A DEFECT.** Section 1 of that
document is the owner-facing list of what a kit cannot show.

## The player-experience pass, 2026-07-28

Four owner-ordered jobs, all shipped, all in kit V8 and later.

- **The max-win hold.** The celebration holds indefinitely. Six things were moving behind it,
  the worst being that a keypress could place a real bet under the overlay and dismiss it
  with no COLLECT. TR-094.
- **The bet denomination picker.** Tapping the BET window opens a panel of the authenticate
  ladder, never a hardcoded list. TR-095.
- **Feature resume.** Leave mid-bonus, come back, continue. A persisted CURSOR, never
  content, so displayed totals cannot diverge from the round. TR-099.
- **The layout fit gate's silent element fallback**, found by a red main. TR-098.

## This session, 2026-07-28

- **The three background 403s are NOT a missing asset.** Measured: present in dist and in the
  uploaded kit. The failing path is under `scratch/front/`, the platform's unpublished area,
  and it resolved six minutes later with no code change. TR-102.
- **A dangling reference WAS real, and the new gate found it on its first run.**
  `themeStore.ts` pointed at a 6MB video the build prunes by name, with zero consumers, so no
  runtime gate could see it. TR-103, held by `asset_reference_gate.mjs`.
- **TR-075 CLOSED.** Cruise differenced to the cent at exactly 1.00x. The project's last open
  money item.
- **TR-081 updated**: every wallet call in the new frames is 200, so the multiple-authenticate
  observation stands and the red one is not recorded, because the frames do not show it.
- **TR-101 re-verified** clause by clause against JOB 4's independent specification.

## What is genuinely open

| Row | What |
|---|---|
| **TR-096** | The infinite-autoplay option stays visible when a jurisdiction caps autoplay. Pre-existing, attributed by measurement against a stashed tree. A responsible-gambling control failing open, escalated per convention (l.8). **The most serious open item.** |
| **TR-097** | Two more proof scripts write into committed evidence. |
| **TR-098** | The layout fit gate measures the container when a readout is not a span or div. |
| **TR-093** | One sentence in the quality charter claims an edit that was never made. |
| **TR-086, Q-25 to Q-28, Q-16, Q-21, Q-33** | Carried in the V7 reconciliation's owner section. |

**No open item is a blocker for upload.** TR-096 is the one that should be looked at before
submission rather than before the next kit.
