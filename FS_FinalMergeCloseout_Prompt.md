FINAL MERGE, CLOSE-OUT AND HANDOVER, 2026-07-25. Save this brief verbatim per convention, then execute in order. (1) MERGE: merge PR #88 into main, then delete the branch. Record two riders in the session report for external audit prep: (a) add the MaxWinCelebration overlay (.max-win-overlay) to frontend/scripts/lib/dismissOverlays.mjs so auditOverdriveMeterOnScreen unblocks; (b) both heavy suites (the full 24-cell qa_soak matrix and the full portrait_layout_conformance run) must complete clean in the external audit's fresh environment before any submission; they are named gates. (2) FULL STATE SWEEP: list every local branch and confirm each is pushed or explicitly dead; confirm zero open PRs or list any with status; confirm the working tree holds no work-relevant uncommitted files, enumerating the known pre-existing exceptions (frontend/dist artefacts, settings.local.json, the two unrelated untracked directories) one final time; confirm reports/SESSION_REPORT.md, its dated archive copies, and every executed brief are committed. (3) HANDOVER: write HANDOVER_2026-07-25_Fable.md at the repo root as the new arc document per convention (j), superseding the 2026-07-07 living document with a pointer note in that older file. Contents: a brief current verified state (five modes live at 96.35 percent verified, audio shipped and mixed, portrait and landscape layouts locked to HUD_SPEC, feature presentation flow complete, identity locked to the hero emblem with derived icons, trademark gate closed, rounds 1 to 3 of owner audits merged); the board in order (owner full re-test on the new build, round 4 touch-ups plus final animation polish if the re-test warrants, platform-delta and tool vetting session, external audit in a fresh environment carrying the two heavy-suite gates and the reviewer-tag rubric, Developer Testing Tool staging on the platform, portal one-timers and Tile Editor composition, dossier section 5 walk-through, submit); all conventions by reference to CLAUDE.md and CLAUDE_PROJECT_INSTRUCTIONS_v6.md; and boot instructions stating the next session may run in terminal or the desktop app's Code tab against the same folder with identical locks, and must read this handover plus reports/SESSION_REPORT.md before acting. (4) BACKUP CONFIRMATION: commit and push everything with explicit paths, verify with git status and a remote comparison that nothing work-relevant exists only on this machine, and reply with a one-line confirmation that the remote is complete and the machine can be safely updated and restarted.
