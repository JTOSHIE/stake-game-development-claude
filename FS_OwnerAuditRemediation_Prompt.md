OWNER AUDIT REMEDIATION, 2026-07-24. Conventions, locks and reporting as pinned; fresh branch; proofs to reports/screens/owner-audit-v1/; every item ships with its own assert or committed proof. PRIORITY A, FUNCTIONAL. (A1) NITRO price label: the FEATURES menu shows 100x on the NITRO OVERDRIVE card; the debit is correct at 400x, so find where the label diverges from MODE_COST, fix it, and add a UI assert that every menu card's displayed cost equals MODE_COST times current bet, so label drift can never recur. (A2) FEATURES menu clipping: the NITRO card and lower menu content sit half-hidden behind the window edge on phone profiles; make the menu height-aware with internal scroll or compaction so every item is fully visible and tappable; add a visibility-within-viewport assert for all menu items across all four profiles (the touch-target audit measures size, not clipping, which is how this escaped). (A3) Live cost reactivity: the menu and buy modals must update displayed figures live when the bet changes or OVERBOOST toggles: add a persistent "This spin costs $X.XX" line in the SPIN MODES section and make every buy card price reactive to the bet store; assert by changing bet with the menu open. (A4) Autoplay loss limit: the stop-condition setting appears inert in play; reproduce, fix, and add an end-to-end soak assert (autoplay with a loss limit set stops within one spin of crossing it), plus the same for single-win and stop-on-feature conditions. (A5) Mock base pool: curate the base and cruise pools production-representatively from the real books, INCLUDING natural Overdrive trigger rounds at approximately the shipped trigger rate; assert each pool contains at least one natural trigger; add a spin-until-trigger harness test proving natural entry works end to end in dev. PRIORITY B, DISPLAY AND UX. (B1) Currency and number formatting: drop the US prefix (narrow symbol, $1.00 not US$1.00) since currency is fixed at session start; standardise HUD number font sizing with auto-fit so values up to $999,999.99 render without truncation in balance, win and bet plates; assert with stress values. (B2) Win banner text fit: epic and max banners truncate large amounts (e.g. $100,000); auto-scale the amount text or widen the housing so seven-digit wins fit; commit proofs at $1,000, $100,000 and $1,000,000. (B3) Standing-mode clarity: iterate the SPIN MODES section: selection must read as an obvious radio choice with one clearly lit active card, the OVERBOOST card shows its effect inline (1.25x per spin, and the live resulting cost via A3), and switching modes visibly updates the HUD bet figure immediately even with the menu open. (B4) Buy confirmation redesign: the Overdrive and NITRO confirm dialogs become larger, premium presentations: feature name, what you get (8 to 16 free spins by scatters, meter behaviour, NITRO's meter pre-revved to 5x), cost at current bet, mode RTP and max win (reinforcing the per-mode disclosure requirement), then CONFIRM/CANCEL; social strings preserved; proofs both modes both dialogs. (B5) Autoplay infinity: add an infinite option to the autoplay counts, rendered as the lying-eight symbol, available only when jurisdiction flags impose no autoplay cap (the RG module governs; when flags cap, the option hides); explicit-confirm behaviour unchanged; assert both flag states. PRIORITY C, VISUAL. (C1) Rain in the main scene: port the splash's rain streak layer into the in-game backdrop at low opacity and density, performance-gated by the frame gate, disabled under reduced motion; before/after proofs. (C2) VECTOR MARK V3, badge construction replacing the rejected v2: same geometry skeleton but built as a filled dark coin (deep navy plate circle behind everything), chrome rim with gradient fills not outlines, filled gunmetal spokes with magenta edge glows (SVG gaussian filters), the reel window as a lit panel with gold filled 7s and cyan glow, wordmarks in the hero's neon-sign style (filled letterforms with layered glow strokes, not hollow outlines); the hero emblem remains the canonical logo at 96px and above, v3 covers only sub-96 contexts (favicon, watermark); same paths-only, margin and export gates as v2; commit renders for owner judgement. Re-run the full conformance suite at the end; all gates green; session report with a per-item fix-and-assert table. The external audit session is QUEUED to run immediately after this pass merges, not before.
